---
title: "PredVLA: A Sub-Million-Parameter Predictive-Coding Policy for Robot Manipulation"
title_zh: PredVLA：一种用于机器人操作的低至亚百万参数预测编码策略
authors: "Hiroki Sawada, Shunichi Kasahara"
date: 2026-08-27
pdf: "https://arxiv.org/pdf/2608.26673v1"
tags: ["query:sr"]
score: 10.0
evidence: 亚百万参数的视觉语言动作策略，直接服务于机器人控制与规划需求
tldr: "大型预训练视觉-语言-动作模型主导操作基准，但其规模必要性存疑。PredVLA以0.68M参数、无机器人数据预训练，通过层级生成循环动力学预测视觉特征与本体感受，观测仅经预测误差在线推理影响隐状态。LIBERO短程成功率达86.9%，含长程为75.4%，分别是参数匹配Transformer和LSTM的3.7倍与7.4倍。禁用预测误差推理可形成精确开环控制，使校正贡献可测。表明亚百万参数循环生成策略可媲美大模型，并提供可解释的误差驱动在线校正机制。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.26673v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1697, \"height\": 927, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.26673v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1327, \"height\": 827, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.26673v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1709, \"height\": 1655, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.26673v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1690, \"height\": 509, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.26673v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1595, \"height\": 987, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.26673v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1591, \"height\": 884, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.26673v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 570, \"height\": 337, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.26673v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1707, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.26673v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1603, \"height\": 527, \"label\": \"Table\"}]"
motivation: 大型预训练VLA模型成本高昂，但小参数架构的竞争力未知，需探索高效替代策略。
method: 构建0.68M参数的层级生成循环网络，以预测误差在线推理隐状态，预测视觉与本体感受，无机器人数据预训练。
result: "在LIBERO数据集上，短程成功率为86.9%，含长程为75.4%，平均成功率分别是参数匹配Transformer和LSTM的3.7倍与7.4倍。"
conclusion: 亚百万参数循环生成策略即可媲美大模型，验证了预测误差驱动在线校正机制的有效性与可解释性。
---

## 摘要
大型预训练的视觉-语言-动作模型主导着现代机器人操作基准，但仍不清楚模型规模对强语言条件控制有多大必要性，或者根本不同的控制架构是否能在更小的参数预算下保持竞争力。我们提出PredVLA，一种语言条件的预测编码策略，仅含68万可训练网络参数且无机器人数据预训练，其分层生成式循环动力学预测视觉特征与本体感觉，而观察仅通过由此产生的感觉预测误差在线推断来影响潜在状态。在LIBERO上，PredVLA在三个短视野套件中达到86.9%的平均成功率，若包含长视野套件则为75.4%。在使用相同冻结前端、示范、动作解码器和评估协议的受控比较下，PredVLA的平均成功率分别是参数匹配的Transformer和LSTM策略的3.7倍和7.4倍。预测编码公式还使观察驱动的修正贡献可直接测量：由于观察仅通过基于预测误差的潜在推断影响循环状态，禁用此推断便产生精确的开环控制条件。综上，这些结果表明一种亚百万参数循环生成策略能在现代语言条件操作基准上实现强性能，同时为预测误差驱动的在线状态修正提供显式机制。

## Abstract
Large pretrained vision-language-action models dominate modern robot-manipulation benchmarks, but it remains unclear how much model scale is necessary for strong language-conditioned control, or whether fundamentally different control architectures can remain competitive at much smaller parameter budgets. We present PredVLA, a language-conditioned predictive-coding policy with only 0.68 million trainable network parameters and no robot-data pretraining, whose hierarchical generative recurrent dynamics predict visual features and proprioception while observations influence latent state only through online inference from the resulting sensory prediction errors. On LIBERO, PredVLA achieves an 86.9% mean success rate across the three short-horizon suites and 75.4% when the long-horizon suite is included. Under a controlled comparison using the same frozen front end, demonstrations, action decoder, and evaluation protocol, PredVLA achieves 3.7x and 7.4x mean success rates of parameter-matched Transformer and LSTM policies, respectively. The predictive-coding formulation also makes the contribution of observation-driven correction directly measurable: because observations influence the recurrent state only through prediction-error-based latent inference, disabling this inference yields an exact open-loop control condition. Together, these results show that a sub-million-parameter recurrent generative policy can achieve strong performance on modern language-conditioned manipulation benchmarks while providing an explicit mechanism for prediction-error-driven online state correction.

---

## 论文详细总结（自动生成）

# PredVLA 论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：大型预训练视觉-语言-动作（VLA）模型（如 RT-1/2、OpenVLA、π0 等）在机器人操作基准上取得了主导性表现，但普遍包含数十亿参数，训练与部署成本高昂。尽管已有 TinyVLA、SmolVLA、Evo-1 等压缩模型，其参数量仍处于数亿规模。
- **核心问题**：论文提出一个根本性问题——强语言条件操作控制是否真的需要如此大的模型规模？是否可以通过**改变控制架构本身**而非单纯压缩大模型，在远低于百万参数预算下实现有竞争力的性能？
- **整体含义**：论文引入 PredVLA，一种基于**预测编码**（predictive coding）范式的语言条件策略，仅 0.68M 可训练参数且不依赖机器人数据预训练。结果表明预测编码这一替代架构可以在极小参数预算下显著超越参数匹配的传统策略，重新审视了 VLA 模型的规模必要性。

## 2. 论文提出的方法论

### 2.1 核心思想

PredVLA 的架构核心在于：**感官观测永远不直接输入循环动力学**。观测（视觉特征与本体感受）仅存在于自由能目标函数中，通过**预测误差驱动的在线推断**来间接影响循环网络的隐状态。这与传统策略中"观测作为直接输入"的范式有本质区别。

### 2.2 关键技术细节

- **冻结前端（Frozen Front End）**：
  - 语言：使用预训练句编码器 all-MiniLM-L6-v2 编码指令，经固定 PCA 降维（384→64 维）。
  - 视觉：双相机（agentview + eye-in-hand）分别用冻结的 ImageNet 预训练 ResNet18 编码，各自经 PCA 降维（512→96 维）后拼接。
  - 本体感受：直接使用原始 9 维信号。
- **分层循环动力学**：由 4 个循环模块组成层级结构——顶层模块 T、视觉分支 V、动作分支 A_top 和 A_bottom。各模块具有不同的时间常数 τ（短视野任务为 16/8/5/2），构成多时间尺度层次。
- **自由变量先验-后验机制**：每个模块维护自由变量 c，先验由父模块输出或自身前一步输出生成；后验在训练中作为参数优化，在测试中通过在线推断更新。
- **侧向通路**：包含视觉→动作瓶颈（V→A，动态计算的低维表征）和动作→视觉 efference copy 通路（A→V，使用模型自身预测的动作和本体感受而非真实观测）。
- **解码器**：视觉预测从 V 分支解码；动作分布由 A_bottom 解码，使用 5 组分的对角高斯混合模型。

### 2.3 公式与算法流程

- **自由能目标**：E(I) = Σ[λv·εv(t) + λq·εq(t) + Ct]，其中视觉/本体感受预测误差（均方误差逐维平均）构成精确度项，后验与先验偏离的二次惩罚构成复杂度项。
- **训练目标**：L = λv·ε̄v + λq·ε̄q + λa·NLL(a) + C，即自由能加上动作负对数似然。后验自由变量在训练中作为模型参数与网络权重联合优化。
- **在线错误回归推断**：测试时，在长度为 40 的滑动窗口上，通过 Adam 优化器（10 次迭代、步长 0.05）最小化自由能来更新后验自由变量。每次更新后重新生成确定性状态轨迹以保持动力学一致性。
- **精确开环控制**：设置推断迭代次数 n_itr=0，后验等于先验，感官观测完全不进入循环计算，即得到精确的开环控制条件。

## 3. 实验设计

### 3.1 基准与数据集

- 使用 **LIBERO 基准**的四个任务套件：SPATIAL、GOAL、OBJECT（各 10 个短视野任务）和 LONG（libero_10，10 个长视野任务）。
- 每个套件提供 500 个官方示范（每任务 50 个）。短视野任务示范平均 125-149 步，长视野 276 步。
- 每个套件分别独立训练一个控制器。

### 3.2 对比方法

- **参数匹配基线**（使用相同冻结前端、示范数据、高斯混合动作头、评估协议）：
  - BC-Transformer：646,628 参数（0.957×）
  - BC-RNN（LSTM）：674,276 参数（0.998×）
- **动作分块变体**：BC-TF chunk 8（772,558 参数）和 chunk 16（916,478 参数），使用 ACT 风格时间集成。
- 基线训练步数为 50,000（多于 PredVLA 的 30,000），学习率经过 4 点搜索。

### 3.3 评估协议

- 主实验：每任务 50 次 rollout × 10 任务 × 14 个独立训练种子，每个套件 7,000 次评估。每次 rollout 上限 600 步。
- 消融实验：每任务 20 次 rollout × 7 种子。
- 报告 3 个短视野套件的平均值和全部 4 个套件的平均值。

## 4. 资源与算力

- 论文**未明确说明训练所使用的 GPU 类型、数量及训练时间**。
- 测试方面：PredVLA 端到端控制延迟为 46ms/步（21.6 Hz），在 **RTX 5090** 上运行，包括冻结前端和在线错误回归。
- 参数量总计 675,732 个可训练网络参数（0.68M），无机器人数据预训练。

## 5. 实验数量与充分性

### 5.1 实验规模

- **主实验**：4 个 LIBERO 套件 × 14 个种子 × 每任务 50 次 rollout（每套件 7,000 次评估），共 28,000 次评估。
- **消融实验**：6 组消融 × 7 个种子 × 每任务 20 次 rollout。
- **感官扰动实验**：两种通道 × 4 个扰动比例 × 7 种子 × 20 次 rollout。
- **任务级分析**：对视觉/本体感受消融的逐任务分解分析。

### 5.2 充分性与公平性评估

- **优点**：对比基线严格控制变量（相同前端、数据、动作解码器、评估协议），参数匹配控制在 5% 以内；使用配对差异分析减小种子变异性影响；14 个种子提供了统计可靠性。
- **公平性**：基线训练步数更多（50k vs 30k）且经过超参数搜索，PredVLA 在更有利的设置下仍大幅领先，增强了结论可信度。
- **充分性**：消融实验覆盖了架构组件（时间层级、侧向通路）和推断机制（在线错误回归、感官通道），且区分了重训练消融和推断期消融，设计较为全面。
- **局限性**：所有评估均在仿真环境中完成，未涉及物理机器人；仅使用单一机器人形态（LIBERO 设定的 Franka）；未与其他预测编码类模型做直接对比。

## 6. 论文的主要结论与发现

1. **亚百万参数策略可媲美大模型**：PredVLA 在 LIBERO 短视野套件平均成功率 86.94%，含长视野为 75.35%。这一结果优于 OpenVLA（76.5%，7B 参数）和 Octo（75.08%，93M 参数）的公开报告值，但注意这些对比在预训练和评估协议上有差异。
2. **显著优于参数匹配基线**：PredVLA 平均成功率分别是参数匹配 Transformer 和 LSTM 的 3.7 倍和 7.4 倍。比参数更多的动作分块 Transformer 变体也高出数倍。
3. **时间层级贡献广泛**：将层级时间常数替换为单一值导致成功率降低 17-29 个百分点，是影响最稳定的架构组件。
4. **在线错误回归提供可测量的闭环改善**：禁用在线推断（精确开环）导致短视野套件下降 6-12 点、长视野下降 12.57 点，且这种测量无需重训练。
5. **感官误差通道贡献呈任务依赖性**：OBJECT 套件对本体感受误差移除极为敏感（-14.17 点），而 SPATIAL 和 GOAL 对视觉和本体感受误差的依赖相对均衡。任务级分析显示套件平均结果掩盖了显著的逐任务差异。
6. **本体感受扰动比视觉扰动更致命**：仅 25% 的本体感受观测被替换就导致 OBJECT 下降 49.74 点，而视觉扰动在 50% 时影响仍小于 5 点。

## 7. 优点

- **架构创新性**：将预测编码的在线推断机制引入现代 VLA 评估体系，不同于单纯压缩现有架构的路径。观测通过预测误差而非直接输入影响隐状态，从原则上改变了感知-控制耦合方式。
- **极致的参数效率**：0.68M 可训练参数（不包含冻结前端）在同类任务中处于完全不同量级，展示了"改变架构而非扩大规模"的可行性。
- **可解释性与可测量性**：由于观测仅通过推断过程影响策略，可以精确地实现开环控制（n_itr=0），从而无需重训练即可隔离并量化观测驱动校正的贡献。感官误差通道的可分离性也支持了精细的机制分析。
- **实验设计严谨**：控制变量严格（相同前端、数据、评估协议），参数匹配紧密，使用配对差异分析和大种子数，为结论提供可靠统计基础。
- **详细的任务级分析**：超越套件级平均，揭示任务层面的异质性，提供了更深层的洞见。

## 8. 不足与局限

- **仿真与实物差距**：实验仅限于 LIBERO 仿真基准，未在物理机器人上验证。作者明确指出这是未来工作方向，但当前结果无法证明在真实环境中的有效性。
- **长视野性能显著不足**：LONG 套件成功率仅 40.57%，远低于短视野套件（83-89%），说明当前紧凑架构在长视野任务上仍有明显短板。
- **单一基准与形态**：只在 LIBERO 上评估，且只涉及一种机器人形态。跨环境泛化能力和多形态适应性未得到验证。
- **与公开模型的比较非严格受控**：与 OpenVLA、Octo 等公开模型的对比（Figure 2）在预训练数据、优化设置和评估协议上均有差异，论文虽有说明，但该对比的参考价值有限。
- **感官扰动实验的解读边界**：扰动的是提供给推断过程的观测信号而非物理环境，不能推广为对环境干扰的鲁棒性结论。
- **模型假设的限制**：动作执行使用最高权重混合分量的均值，可能限制了对多模态动作分布的建模能力。同时，系统无法处理视觉观测刷新间隔内的快速环境变化。
- **缺失的信息**：论文未报告训练算力消耗（GPU 类型、数量、时长），对于评估方法实际部署成本构成不便。此外，虽然与控制基线比较公平，但与更多当代高效 VLA 方法的横向对比有限。

（完）
