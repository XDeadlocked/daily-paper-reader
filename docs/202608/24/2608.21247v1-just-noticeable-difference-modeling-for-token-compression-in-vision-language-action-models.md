---
title: Just Noticeable Difference Modeling for Token Compression in Vision-Language-Action Models
title_zh: 视觉-语言-动作模型中用于令牌压缩的恰可察觉差异建模
authors: "Zhuoyuan Li, Rui Zhao, Jin Wang, Hanwei Zhu, Cong Zhang, Giuseppe Valenzise, Weisi Lin, Kin-Man Lam"
date: 2026-08-21
pdf: "https://arxiv.org/pdf/2608.21247v1"
tags: ["query:sr"]
score: 9.0
evidence: 面向VLA模型的动作预测进行token压缩，直接匹配VLA模型需求
tldr: VLA模型推理需token压缩以降低延迟，现有方法多用视觉相似性或注意力等间接指标指导压缩，但未考虑下游动作对扰动的容忍度。本文提出Action-JND，基于语言条件动作响应定义可感知变化，用轻量估计器预测各token的最大可容忍扰动，作为即插即用的压缩准则。在LIBERO基准上验证了方法在激进压缩比下能持续提升可靠性，为VLA安全高效压缩提供了新依据。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.21247v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1866, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1795, \"height\": 856, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1564, \"height\": 736, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 871, \"height\": 978, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1861, \"height\": 1897, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 249, \"height\": 334, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 277, \"height\": 326, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 251, \"height\": 323, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 249, \"height\": 319, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 256, \"height\": 331, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 277, \"height\": 346, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 278, \"height\": 345, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.21247v1/fig-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 277, \"height\": 348, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.21247v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1887, \"height\": 1160, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.21247v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1887, \"height\": 1161, \"label\": \"Table\"}]"
motivation: 现有压缩准则以冗余或重要性间接衡量，未直接考虑动作偏差容忍度，导致高压缩比下不安全。
method: 将JND概念扩展至VLA，用语言条件动作偏差定义可感知变化，训练轻量估计器预测token最大可容忍扰动，用于压缩决策。
result: 在LIBERO上结合OpenVLA等模型，Action-JND在激进压缩比下显著提升压缩可靠性，优于现有方法。
conclusion: 以动作容忍度为核心的JND建模为VLA压缩提供新准则，可灵活适配token剪枝与KV缓存复用等范式。
---

## 摘要
令牌压缩已成为降低大型基础模型推理成本的关键技术，诸如令牌剪枝和KV缓存复用等方法已被广泛采用于视觉-语言模型，并最近被探索应用于具身智能体。在具身智能体中，令牌不仅支持感知和语义理解，还直接影响对延迟敏感的闭环机器人动作预测。现有方案通常使用冗余或重要性线索（如视觉相似性、注意力分数和显著性）来引导压缩。然而，这些线索仅间接衡量安全压缩的关键因素：令牌在导致下游动作出现不可接受的偏差之前能够改变多少。这种接收者相关的容差与恰可察觉差异（JND）原理密切相关。经典JND刻画人类视觉系统中的信号容差，而面向机器的JND将这一概念扩展到下游机器响应。基于这一演进，我们引入了Action-JND，通过定义在闭环控制中语言条件下的视觉-语言-动作（VLA）策略动作响应的可察觉性，将JND建模扩展到具身感知。仅当引起的动作偏差保持在允许的容差范围内时，令牌更改才被认为是可接受的。为实现这一概念，我们在深度视觉特征空间中开发了一个轻量级的逐令牌JND估计器，以预测在保持策略响应的同时允许的最大扰动。由此产生的动作容差得分作为VLA压缩范式的即插即用标准，包括陈旧KV复用和令牌剪枝，优先压缩动作容差令牌。在LIBERO基准上与OpenVLA和OpenVLA-OFT的实验表明，Action-JND持续提高压缩可靠性，尤其是在高压缩比下。

## Abstract
Token compression has become a key technique for reducing the inference cost of large foundation models, with approaches such as token pruning and KV-cache reuse widely adopted in vision-language models and recently explored for embodied agents. In embodied agents, tokens not only support perception and semantic understanding but also directly affect latency-sensitive closed-loop robot action prediction. Existing schemes typically guide compression using redundancy or importance cues, such as visual similarity, attention scores, and saliency. However, these cues only indirectly measure the key factor for safe compression: how much a token can change before causing an unacceptable deviation in downstream actions. This receiver-dependent tolerance is closely related to the principle of just noticeable difference (JND). Classical JND characterizes signal tolerance in the human visual system, while machine-oriented JND extends this concept to downstream machine responses. Building on this progression, we introduce Action-JND, which extends JND modeling to embodied perception by defining noticeability through the language-conditioned action response of a vision-language-action (VLA) policy in closed-loop control. A token change is considered admissible only when the induced action deviation remains within a tolerated margin. To realize this concept, we develop a lightweight token-wise JND estimator in deep visual-feature space to predict the maximum tolerable perturbation while preserving policy responses. The resulting action-tolerance score serves as a plug-and-play criterion for VLA compression paradigms, including stale-KV reuse and token pruning, prioritizing action-tolerant tokens for compression. Experiments on the LIBERO benchmark with OpenVLA and OpenVLA-OFT demonstrate that Action-JND consistently improves compression reliability, especially under aggressive compression ratios.

---

## 论文详细总结（自动生成）

## 论文总结：Action-JND——面向视觉-语言-动作模型的令牌压缩恰可察觉差异建模

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：大型基础模型日益依赖长令牌序列进行推理，token 压缩已成为降低推理成本（内存、带宽、算力）的关键技术。在机器人操控场景中，视觉-语言-动作（VLA）模型需在闭环控制中反复处理高维视觉观测并生成动作，推理延迟直接限制策略更新频率，因此高效 token 压缩对具身智能尤为重要。
- **现有方法的缺陷**：现有 VLA 压缩方法（如 VLA-Cache、VLA-Pruner、EfficientVLA 等）通常以视觉相似性、注意力分数、显著性等**间接代理信号**评估 token 的可压缩性。但这些线索无法回答真正关键的问题：**一个 token 被剪除或被替换为陈旧表示后，下游机器人动作是否仍能保持在可接受的偏差范围内？**
- **核心问题**：视觉上稳定或语义上显著的 token 可能对动作生成高度敏感；反之，看似冗余的 token 可能在压缩后不影响动作。因此，VLA 压缩需要一个**直接面向下游动作响应**的容差准则，而非仅依赖冗余/重要性估计。
- **整体含义**：本文将经典 JND（恰可察觉差异，人类视觉感知）和机器 JND（下游任务响应）进一步扩展至**具身感知**，提出 **Action-JND**：将"可察觉"定义为对 VLA 策略语言条件动作响应的偏差，从而将 token 压缩问题转化为**动作条件容差建模**问题，为安全高效压缩提供全新理论视角。

### 2. 论文提出的方法论

#### 核心思想

- **Action-JND 定义**：给定视觉 token 特征 $F_t$、语言指令 $\ell$ 和 VLA 策略 $\pi_\theta$，Action-JND 定义为**在保持动作预测偏差不超过容忍阈值 $\epsilon_a$ 的前提下，可施加的最大视觉 token 扰动**。即一个 token 变化只有在不引起超出容许范围的动作偏差时才被认为是可接受的，而非仅仅对人/机器不可察觉。
- **接收者依赖形式化**：论文从一般化的接收者 JND 公式出发（Eq. 1），经机器感知 JND（Eq. 2）过渡到具身感知 Action-JND（Eq. 3）。动作偏差 $D_{act}$ 根据策略动作参数化方式实例化：
  - **离散动作策略**（如 OpenVLA）：$D_{act}$ 为干净/扰动分支动作 token 分布的 KL 散度（Eq. 4）
  - **连续动作策略**（如 OpenVLA-OFT）：$D_{act}$ 为带权重的 $\ell_1$ 回归距离（平移、旋转、夹爪，Eq. 5）
- **学习目标**：将约束优化松弛为无约束目标（Eq. 6 / Eq. 8），联合最大化扰动幅度、最小化动作偏差：
  $$L_{JND} = \lambda_{act} \cdot D_{act}(r_t, \tilde{r}_t) - \lambda_{mag} \cdot M(\hat{\Delta}_t)$$

#### 关键技术细节

- **JND 估计器架构**：轻量卷积网络，将投影视觉 token（16×16 空间网格）经 LayerNorm、空间卷积（通道压缩 D→C）、多个空间残差块、通道混合残差块、1×1 投影（C→D），最终输出逐 token 扰动图，并以 tanh/clip 约束幅值。对双视图模型（主视图+腕部视图），使用两个独立参数的估计器分别处理。
- **Token 得分转换**：预测扰动幅值转换为两种得分：
  - 绝对得分 $s^{abs}_{t,i} = \|\hat{\delta}_{t,i}\|_2$（用于 token 剪枝）
  - 相对得分 $s^{rel}_{t,i} = \|\hat{\delta}_{t,i}\|_2 / (\|f_{t,i}\|_2 + \varepsilon)$（用于 KV 复用，消除特征尺度影响）
- **KV 缓存复用（算法 1）**：在 VLA-Cache 框架中，先构造时间稳定候选集（排除注意力保护位置），再以 Action-JND 得分降序选择复用 token。当安全候选集不足时，从受保护回退池中按同样标准填补，同时保持高注意力位置受保护。
- **Token 剪枝（算法 2）**：以时间相似性构建稳定集、以动作-视觉注意力构建保护集，安全候选集为稳定集减去保护集，再按绝对 Action-JND 得分降序选择剪除 token。预算不足时扩展至非候选位置。
- **即插即用特性**：Action-JND 不替换现有压缩框架，仅作为候选 token 的**排序标准**叠加在原有压缩线索之上；推理时不会向特征注入扰动，仅用于选择决策。

### 3. 实验设计

- **数据集/基准**：LIBERO 机器人操控基准的 4 个标准套件——Spatial（空间推理）、Object（物体中心操控）、Goal（目标条件执行）、Long（长时程任务），评估指标为各套件成功率及平均值（Acc.）。
- **骨干模型**：
  - **OpenVLA**：7B 参数，单 RGB 视图，离散动作 token 自回归生成
  - **OpenVLA-OFT**：连续动作块回归，支持主视图+腕部视图双输入
- **对比方法**：
  - KV 缓存复用场景：VLA-Cache、FastV、SparseVLM、DivPrune（KV 适配版）
  - Token 剪枝场景：FastV、SparseVLM、DivPrune、VLA-Cache 剪枝基线
- **压缩设置**：
  - KV 复用：自适应软策略 + strict_guarded 策略（目标复用率 30%、40%、60%、80%）
  - Token 剪枝：budgeted 策略（目标剪枝率 25%、50%、75%、87.5%）
- **评估指标**：成功率（各套件及平均）、FLOPs、相对 FLOPs、CUDA 延迟、延迟差、控制频率（Hz）
- **实现环境**：KV 缓存实验基于 VLA-Cache 开源代码库，token 剪枝实验基于官方 OpenVLA 代码库

### 4. 资源与算力

- **论文未明确说明**训练 JND 估计器所使用的 GPU 型号、数量、训练时长等具体算力信息。
- 仅在文中提及估算器为"轻量级"（lightweight），参数量与计算开销均未报告具体数值。
- 实验代码基于公开仓库构建，可推断为标准 VLA 训练规模（OpenVLA 为 7B 参数模型），但具体资源细节缺失。

### 5. 实验数量与充分性

- **主实验**：共 3 张主表——
  - 表 I：OpenVLA + KV 缓存复用（5 种方法 × 5 种设置：软策略 + 4 档严格复用率）
  - 表 II：OpenVLA-OFT + KV 缓存复用（同样 5 种方法 × 5 种设置）
  - 表 III：OpenVLA + Token 剪枝（5 种方法 × 4 档剪枝率）
- **定性可视化**：图 5 展示了不同方法在 KV 复用和剪枝下的成功/失败案例及 JND 热力图。
- **充分性评估**：
  - **优势**：覆盖了两个代表性 VLA 骨干、两种压缩范式、多档压缩率（从保守到极端），跨 4 个任务套件统计，实验规模较为充分；对 FLOPs、延迟、控制频率等效率指标均有报告。
  - **不足**：未见显式消融实验（如 λ 权重敏感性分析、JND 得分形式对比、估计器规模影响、训练数据量影响等）；未与更前沿的 VLA 压缩方法（如 VLA-Pruner、EfficientVLA、SP-VLA 等文中引用的工作）进行实际对比；未报告多组随机种子下的方差。

### 6. 论文的主要结论与发现

- **核心发现**：间接压缩线索（注意力、相似性、显著性）在高压缩比下不可靠，直接估计动作容差能显著提升压缩安全性。
- **KV 缓存复用增益显著**：在 80% 复用率下，OpenVLA 上 Action-JND 较 VLA-Cache 平均成功率提升 **+41.65 个百分点**（55.45% vs 13.80%）；OpenVLA-OFT 上提升 **+10.10 个百分点**（89.55% vs 79.45%）。60% 复用率下分别提升 +23.70 和 +6.20 个百分点。
- **Token 剪枝优势明显**：在 87.5% 剪枝率下，较 VLA-Cache 剪枝基线提升 **+11.65 个百分点**（42.00% vs 30.35%），较最强竞争方法提升 +6.55 个百分点，同时 FLOPs 降至约 33%，控制频率从 18.91 Hz 提升至 33.15 Hz。
- **效率-精度平衡**：Action-JND 在保持与基线相近 FLOPs 的前提下，由于更精准的压缩决策，可换取更低的延迟和更高的控制频率，实现更优的精度-效率权衡。
- **低压缩率下差异较小**：在保守压缩（如 30% 复用、25% 剪枝）下各方法性能接近，Action-JND 的优势主要体现在激进压缩场景。

### 7. 优点

- **理论创新性强**：将 JND 理论从人类视觉感知、静态机器视觉任务逐步推进至**具身动作响应**，形成完整接收者 JND 建模谱系，概念上具有清晰的前后延续性和一般性。
- **问题定义精确**：直接面向 VLA 压缩的根本问题（动作偏差容限），而非间接代理信号，理论动机扎实。
- **即插即用设计**：不修改底层压缩框架，仅替换候选 token 排序准则，与现有 KV 复用和剪枝方案正交兼容，工程落地友好。
- **双动作参数化覆盖**：同时支持离散动作 token（KL）与连续动作回归（加权 ℓ1），方法通用性强。
- **双视图处理合理**：对主视图和腕部视图使用独立估计器，避免不同相机坐标系混叠。
- **幅度与保真联合优化**：训练目标同时最大化扰动幅度和最小化动作偏差，使估计的容差边界更具实际意义。
- **实验结论鲜明**：在高压缩比下展示了远超基线的成功率提升，尤其是 KV 复用 80% 时 41.65 个百分点的增益具有很强说服力，并辅以定性可视化佐证。

### 8. 不足与局限

- **算力资源披露不足**：未报告 GPU 型号/数量、训练时间、估计器参数量等关键资源信息，可复现性评估受限。
- **缺乏系统消融实验**：未分析 λact/λmag 敏感性、绝对/相对得分选择合理性、估计器深度/宽度影响、训练数据规模效应等，对方法理解不够深入。
- **基准覆盖有限**：仅在 LIBERO 单基准上验证，未涉及真实机器人部署、视觉-语言导航、四足机器人等更广泛具身任务；未评估策略泛化到新环境/新指令的能力。
- **对比方法不全**：未与 VLA-Pruner、EfficientVLA、SP-VLA、SpecPrune-VLA 等最新 SOTA 压缩方法直接对比，仅与 VLA-Cache 及通用视觉 token 剪枝方法比较。
- **潜在偏差风险**：JND 估计器在 LIBERO 数据上训练，其学到的动作容差边界可能有过拟合特定任务/环境的偏差；不同代码库导致 OpenVLA 的 KV 复用（表 I，vanilla 70.55%）与剪枝实验（表 III，vanilla 66.40%）基线表现不一致，跨实验直接对比需谨慎。
- **稳定性问题**：高压缩率下仍有明显波动（如 OpenVLA-OFT 80% 复用率下 Object 套件从 96.80% 跌至 78.20%），说明极端压缩下部分任务仍面临较大风险。
- **FLOPs 略高于基线**：表中 Action-JND 的 FLOPs 普遍比对应基线的 VLA-Cache 略高（如 60% 复用 0.793T vs 0.792T），估计器自身推理开销未单独分析，效率优势的解释主要以"同等算力下更高精度"为主。
- **论文状态**：标注 UNDER REVIEW，尚未经同行评审正式发表，结论需谨慎对待。

（完）
