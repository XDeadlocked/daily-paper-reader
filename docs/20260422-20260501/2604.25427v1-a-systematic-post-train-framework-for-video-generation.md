---
title: A Systematic Post-Train Framework for Video Generation
title_zh: 视频生成的系统性后训练框架
authors: "Zeyue Xue, Siming Fu, Jie Huang, Shuai Lu, Haoran Li, Yijun Liu, Yuming Li, Xiaoxuan He, Mengzhao Chen, Haoyang Huang, Nan Duan, Ping Luo"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25427v1"
tags: ["query:sr-0501"]
score: 9.5
evidence: 使用RLHF和GRPO的视频扩散模型后训练框架
tldr: 本文针对大规模视频扩散模型在实际部署中存在的提示敏感性、时序不一致和推理成本高等问题，提出了一个系统的后训练框架。该框架包含四个协同阶段：监督微调、基于组相对策略优化的强化学习、提示增强和推理优化，旨在提升生成视频的感知质量、时序一致性和指令遵循能力，同时保持预训练阶段学到的可控性。实验证明，该统一流程能有效减少常见伪影，显著改善可控性和视觉美学，并遵守严格的采样成本限制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25427v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of our post-training framework for video generation. We organize the pipeline into four complementary stages to bridge pretrained models and practical deployment. In Phase 1, supervised fine-tuning (SFT) uses curated data to establish a stable instruction-following baseline. In Phase 2, RLHF via a GRPO-based trainer aligns the generator with multi-dimensional reward signals, improving aesthetics, motion quality, and text alignment. In Phase 3, Prompt Enhancement (PE) optimizes an LLM using the same reward loop to enrich user inputs for better robustness and visual quality. Finally, Phase 4 applies autoregressive distillation (AD) with a self-forcing objective to transfer these capabilities into a causal architecture, significantly boosting inference efficiency for real-world deployment.\", \"page\": 2, \"index\": 1, \"width\": 808, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.25427v1/fig-002.webp\", \"caption\": \"Figure 2: The visualization of RLHF on Wan-2.1.\", \"page\": 8, \"index\": 2, \"width\": 758, \"height\": 1167}]"
motivation: 现有大规模视频扩散模型在提示敏感性、时序不一致和推理成本等方面存在不足，阻碍实际部署。
method: 提出了一个包含监督微调、强化学习、提示增强和推理优化的四阶段后训练框架。
result: 实验表明，该框架能有效减少伪影，提升可控性和视觉质量，同时满足采样成本约束。
conclusion: 该框架为视频生成模型的部署提供了一个系统化、可扩展的解决方案。
---

## 摘要
尽管大规模视频扩散模型在生成高分辨率和语义丰富的内容方面展现出令人印象深刻的能力，但由于提示词敏感性、时间不一致性以及高昂的推理成本等关键问题，其预训练性能与实际部署需求之间仍存在显著差距。为弥合这一差距，我们提出了一个全面的后训练框架，通过四个协同阶段系统地将预训练模型与用户意图对齐：首先采用监督微调（SFT）将基础模型转化为稳定的指令跟随策略；随后通过基于人类反馈的强化学习（RLHF）阶段，利用专为视频扩散定制的新型组相对策略优化（GRPO）方法，以提升感知质量和时间连贯性；接着，我们通过专用语言模型进行提示增强以优化用户输入；最后通过推理优化解决系统效率问题。这些组件共同构成了一种系统化方法，在保持预训练期间习得的可控性的同时，提升了视觉质量、时间连贯性和指令跟随能力。该框架为构建可扩展的后训练流程提供了实用蓝图，使其在实际部署中具备稳定性、适应性和高效性。大量实验表明，这一统一流程能有效减少常见伪影，在严格遵守采样成本约束的同时，显著提升了可控性和视觉美感。

## Abstract
While large-scale video diffusion models have demonstrated impressive capabilities in generating high-resolution and semantically rich content, a significant gap remains between their pretraining performance and real-world deployment requirements due to critical issues such as prompt sensitivity, temporal inconsistency, and prohibitive inference costs. To bridge this gap, we propose a comprehensive post-training framework that systematically aligns pretrained models with user intentions through four synergistic stages: we first employ Supervised Fine-Tuning (SFT) to transform the base model into a stable instruction-following policy, followed by a Reinforcement Learning from Human Feedback (RLHF) stage that utilizes a novel Group Relative Policy Optimization (GRPO) method tailored for video diffusion to enhance perceptual quality and temporal coherence; subsequently, we integrate Prompt Enhancement via a specialized language model to refine user inputs, and finally address system efficiency through Inference Optimization. Together, these components provide a systematic approach to improving visual quality, temporal coherence, and instruction following, while preserving the controllability learned during pretraining. The result is a practical blueprint for building scalable post-training pipelines that are stable, adaptable, and effective in real-world deployment. Extensive experiments demonstrate that this unified pipeline effectively mitigates common artifacts and significantly improves controllability and visual aesthetics while adhering to strict sampling cost constraints.

---

## 论文详细总结（自动生成）

好的，我将基于您提供的论文内容，为您生成一份详细的结构化中文总结。

### 1. 核心问题与整体含义
*   **研究动机**：尽管大规模视频扩散模型在生成能力上取得了显著进展，但其**预训练性能**与**实际部署需求**之间存在巨大鸿沟。主要问题体现在：
    *   **提示词敏感性**：模型输出对输入提示的措辞非常敏感。
    *   **时序不一致性**：生成长视频时容易出现时间上的抖动、断裂或不连贯。
    *   **高昂推理成本**：扩散模型迭代采样导致生成速度慢，成本高。
    *   **局部伪影**：在生成手、文字、快速运动等复杂细节时容易出错。
*   **整体含义**：本文提出了一种**系统性后训练框架**，旨在将预训练好的视频生成模型进行“对齐”与“优化”，使其更符合人类偏好、更稳定、更高效，从而能够投入实际应用。该框架不是单一的微调技术，而是一个包含四个协同阶段的完整工程化流程。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：设计一个四阶段、端到端的后训练流水线，依次解决指令跟随、生成质量、输入鲁棒性和推理效率问题。
*   **关键技术细节**：
    1.  **监督微调**：使用高质量指令数据对基础模型进行微调。其目的**不是**直接优化生成质量，而是建立一个**稳定、可靠的指令跟随基线策略**，为后续强化学习提供一个良好的起点，避免策略崩溃。
    2.  **基于人类反馈的强化学习**：
        *   **方法**：采用**组相对策略优化**（GRPO），这是一种专为扩散/流匹配模型设计的RL方法，无需训练不稳定的价值函数。
        *   **流程**：将确定性ODE采样过程转化为随机性SDE过程以引入探索。采用 **“等时间分组”** 策略，为每个提示组分配一个特定的时间步进行SDE采样（用于探索和计算梯度），其余时间步使用ODE采样（保证生成质量），以平衡探索与计算成本。
        *   **奖励模型**：集成了四个维度的奖励信号进行联合优化：
            *   **视频美学**：整体视觉质量（光照、构图、色彩、时序一致性）。
            *   **图像美学**：单帧画面的感知质量。
            *   **运动质量**：运动动态的真实性、平滑度和连贯性。
            *   **文本-视频对齐**：生成内容与输入提示的语义一致性。
    3.  **提示增强**：
        *   训练一个大型语言模型作为**提示词优化器**。
        *   **关键创新**：使用与**视频生成器相同的奖励模型和GRPO训练循环**来优化这个LLM。这使得提示增强器能够学习如何改写用户的原始输入，以激发生成器产生更高质量的视频，同时保持与原意的对齐。
    4.  **自回归蒸馏**：
        *   **目标**：将经过上述阶段优化后的、计算昂贵的双向扩散模型，**蒸馏**成一个高效的、**因果架构**的自回归模型，以实现实时或流式生成。
        *   **三阶段训练**：
            *   **分布匹配蒸馏**：将原模型蒸馏为步数更少的双向学生模型。
            *   **因果ODE回归**：引入因果注意力掩码，让学生模型学习基于历史帧进行去噪预测。
            *   **自强制蒸馏**：在训练时进行自回归展开（使用KV缓存），使模型在自身先前生成的帧上进行条件生成，并用DMD损失监督整个视频序列的质量，解决自回归模型训练与测试不一致的问题。

### 3. 实验设计
*   **模型与数据**：
    *   **模型**：基于一个**内部视频生成模型**（未公开具体架构）进行实验。
    *   **数据**：
        1.  构建了用于SFT的**高质量文本-视频数据集**。
        2.  为RLHF、DMD、ODE回归和自强制训练**精心策划了提示词集合**。
*   **评估基准与协议**：
    *   **评估协议**：采用 **“好-相同-坏”** 三选项人工比较协议。该协议允许评估者在差异细微时选择“相同”，减少了在边际情况下做出强制或噪声决策的可能。
    *   **评估维度**：从**视觉质量**、**运动质量**和**文本对齐**三个互补方面进行评估。
*   **对比方法**：论文**未进行**与外部其他后训练或SOTA方法的横向对比。实验主要是**框架内不同阶段的消融验证和效果展示**，例如比较RLHF前后、以及加入提示增强后的效果。

### 4. 资源与算力
*   论文**未明确说明**训练所使用的具体硬件配置（如GPU型号、数量）、计算集群规模或总训练时长。
*   仅提及使用了“内部视频生成模型”，并遵循HPSv3等工作的训练范式来训练奖励模型。

### 5. 实验数量与充分性
*   **实验数量**：主要展示了两组核心实验结果：
    1.  **RLHF效果**：报告了在内部模型上，RLHF阶段带来了**31%** 的整体GSB指标提升，其中视觉质量和运动质量提升显著，文本对齐提升相对较小。
    2.  **提示增强效果**：集成提示增强器后，带来了额外的**20%** 整体GSB指标提升。
*   **充分性与客观性**：
    *   **优点**：实验针对框架的核心主张（提升质量与对齐）进行了定量（GSB指标）和定性（如图2所示的三个案例对比）验证，证明了各阶段的有效性。
    *   **不足**：**缺乏广泛的横向对比**。没有与同期其他先进的视频生成模型或后训练方法（如DPO、PPO等其他RL算法）进行比较，因此无法客观评估该框架在领域内的相对性能优势。实验的“客观性”和“公平性”主要体现在使用了人工评估的GSB协议，但对比基线仅限于自身框架的不同阶段。

### 6. 主要结论与发现
*   **系统性后训练框架有效**：提出的四阶段框架能够系统性地解决视频生成模型部署中的关键问题。
*   **质量显著提升**：RLHF阶段能大幅提升生成视频的**视觉美学**和**运动连贯性**（31% GSB提升）。
*   **输入优化互补**：通过相同奖励循环训练的提示增强器，能进一步显著提升输出质量（额外20% GSB提升），且不损害语义对齐。
*   **效率可行**：通过自回归蒸馏，能够将优化后的能力迁移到高效的因果架构中，满足实时部署需求。
*   **当前局限**：文本对齐方面的改进相对有限，这主要受限于**当前文本-视频奖励模型的准确性**，成为了性能进一步提升的瓶颈。

### 7. 优点
*   **系统性与完整性**：首次提出了一个覆盖“模型对齐-输入优化-推理加速”全链路的、专为视频生成设计的系统性后训练蓝图，具有很高的工程指导价值。
*   **GRPO的针对性设计**：将GRPO方法与视频扩散的SDE采样过程相结合，并创新性地提出“等时间分组”策略，在保证探索的同时控制了视频生成的高计算成本。
*   **奖励驱动的提示增强**：将提示增强器纳入与生成器相同的奖励优化循环中，使两者协同进化，这是一个巧妙的输入-输出联合优化思路。
*   **多维奖励平衡**：明确讨论了多奖励（美学、运动、对齐）联合优化的挑战，并设计了相应的权衡策略，实践指导性强。

### 8. 不足与局限
*   **实验对比不充分**：最大的局限在于缺乏与外部SOTA方法的对比，无法证明其相对于其他后训练方法的优越性。
*   **依赖内部模型与数据**：所有实验均基于未公开的内部模型和数据集，**可复现性较低**，结论的普适性有待在其他开源模型上验证。
*   **文本对齐瓶颈**：作者明确指出，文本-视频奖励模型的质量限制了语义对齐能力的进一步提升，这是整个框架的一个关键短板。
*   **计算复杂度高**：四阶段流水线，尤其是涉及多轮RLHF和多个大型奖励模型，整体**训练成本非常高昂**，对算力资源要求极高，限制了其应用范围。
*   **细节披露有限**：对于SFT数据的具体构建、奖励模型的具体架构和训练细节、GRPO超参数设置等关键实现信息描述较为简略。

（完）
