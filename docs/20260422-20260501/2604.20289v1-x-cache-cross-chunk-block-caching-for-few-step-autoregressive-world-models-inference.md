---
title: "X-Cache: Cross-Chunk Block Caching for Few-Step Autoregressive World Models Inference"
title_zh: X-Cache：用于少步自回归世界模型推理的跨块缓存
authors: "Yixiao Zeng, Jianlei Zheng, Chaoda Zheng, Shijia Chen, Mingdian Liu, Tongping Liu, Tengwei Luo, Yu Zhang, Boyang Wang, Linkun Xu, Siyuan Lu, Bo Tian, Xianming Liu"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20289v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 自动驾驶自回归世界模型推理
tldr: "实时世界模拟是自动驾驶系统评估和在线强化学习的关键基础设施，但现有基于自回归视频扩散的驾驶世界模型推理成本高，成为交互部署瓶颈。本文提出X-Cache，一种无需训练的加速方法，通过跨连续生成块而非去噪步骤进行缓存，维护跨块持久的块级残差缓存，并应用基于结构和动作感知块输入指纹的双度量门控机制，独立决定每个块应重新计算或重用缓存残差。为防止近似误差污染自回归KV缓存，X-Cache识别KV更新块并强制全计算以切断误差传播。在基于少步去噪多块因果DiT的X-world模型上实现71%块跳过率和2.6倍加速，且性能下降最小。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20289v1/fig-001.webp\", \"caption\": \"Table 3: X-Cache ablations on the single 264-frame clip. † The Fn row and the τfloor sweep keep step-0 protection on, so the effect of each knob is isolated rather than entangled with the step-0 decision; the Default and +Step-0 rows already measure that decision on its own.\", \"page\": 11, \"index\": 1, \"width\": 844, \"height\": 188}, {\"url\": \"assets/figures/arxiv/2604.20289v1/fig-002.webp\", \"caption\": \"Figure 5: Per-(step, block) cross-chunk cosine similarity, one panel per scenario, averaged over post-warmup chunks of a representative clip. Block 0 is omitted because the front anchor (Fn=1) never participates in the gate. Color scale is shared across the three panels.\", \"page\": 10, \"index\": 2, \"width\": 827, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2604.20289v1/fig-003.webp\", \"caption\": \"Figure 6: Per-block skip rate, averaged across the four denoising steps and across post-warmup chunks of one representative clip per scenario. Block 0 (front anchor, Fn=1) is omitted. Dashed line marks the theoretical 0.75 plateau implied by W=1 warmup over the 4-chunk reuse window.\", \"page\": 10, \"index\": 3, \"width\": 687, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2604.20289v1/fig-004.webp\", \"caption\": \"Table 1: Default X-Cache hyperparameters used throughout the experiments.\", \"page\": 8, \"index\": 4, \"width\": 827, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2604.20289v1/fig-005.webp\", \"caption\": \"Figure 3: Qualitative frame-level comparison in three scenarios. Rows, top to bottom: highway, urban street, and u-turn. Each row: left baseline full-compute, middle X-Cache under identical seed and conditioning, right absolute pixel difference amplified 20×. Per-view labels match Table 2.\", \"page\": 8, \"index\": 5, \"width\": 818, \"height\": 602}, {\"url\": \"assets/figures/arxiv/2604.20289v1/fig-006.webp\", \"caption\": \"Figure 2: Overall architecture of X-Cache\", \"page\": 4, \"index\": 6, \"width\": 827, \"height\": 633}, {\"url\": \"assets/figures/arxiv/2604.20289v1/fig-007.webp\", \"caption\": \"Figure 4: Per-frame combined-7-view PSNR of X-Cache rollouts against their baseline full-compute references, on one representative session per scenario. Dashed line marks the per-session mean.\", \"page\": 9, \"index\": 7, \"width\": 798, \"height\": 192}, {\"url\": \"assets/figures/arxiv/2604.20289v1/fig-008.webp\", \"caption\": \"Table 2: X-Cache vs. baseline full-compute reference. Per-camera rows are F-C (front centre), F-W (front wide), S-FL/FR/RL/RR (four sides), and Rear. The shaded \\\"7-cam\\\" row is computed on the stitched seven-view image used in the qualitative figures (not the arithmetic mean of the per-camera rows). Skip rate excludes warmup; DiT wall-clock is per-chunk on a single PPU, excluding the first chunk. Baseline DiT is 3.682 s / 3.633 s / 3.688 s per chunk on urban / Highway / u-turn.\", \"page\": 9, \"index\": 8, \"width\": 826, \"height\": 654}]"
motivation: 现有扩散缓存方法不适用于少步去噪的自回归世界模型交互部署，因为缺乏步骤间冗余且序列级并行需要未来条件。
method: X-Cache 通过跨生成块缓存块级残差，并采用双度量门控机制和指纹识别来决定重用或重新计算，同时无条件全计算关键KV更新块以切断误差传播。
result: "在X-world模型上实现了71%的块跳过率和2.6倍的实时加速，且性能下降最小。"
conclusion: X-Cache 是一种有效的训练免费加速方法，能在保持生成质量的同时显著提升推理速度。
---

## 摘要
实时世界仿真正成为自动驾驶系统可扩展评估与在线强化学习的关键基础设施。基于自回归视频扩散构建的最新驾驶世界模型实现了高保真、可控的多摄像头生成，但其推理成本仍是交互式部署的瓶颈。然而，现有的扩散缓存方法专为多步去噪的离线视频生成设计，无法迁移至此场景。少步蒸馏模型已无跨步冗余可供这些方法复用，而序列级并行技术需要未来条件信息，但闭环交互式生成无法提供。我们提出X-Cache，一种免训练的加速方法，其缓存沿不同轴向进行：跨连续生成块而非跨去噪步骤。X-Cache维护跨块持续的逐块残差缓存，并基于结构感知与动作感知的块输入指纹应用双度量门控机制，以独立决定每个块应重新计算或复用其缓存残差。为防止近似误差永久污染自回归KV缓存，X-Cache识别KV更新块（将干净键值写入持久缓存的前向传播）并在此类块上无条件强制完整计算，从而切断误差传播。我们在X-world上实现X-Cache，这是一个基于多块因果DiT构建、采用少步去噪与滚动KV缓存的生产级多摄像头动作条件驾驶世界模型。X-Cache实现了71%的块跳过率与2.6倍的实际加速，同时保持最低的性能下降。

## Abstract
Real-time world simulation is becoming a key infrastructure for scalable evaluation and online reinforcement learning of autonomous driving systems. Recent driving world models built on autoregressive video diffusion achieve high-fidelity, controllable multi-camera generation, but their inference cost remains a bottleneck for interactive deployment. However, existing diffusion caching methods are designed for offline video generation with multiple denoising steps, and do not transfer to this scenario. Few-step distilled models have no inter-step redundancy left for these methods to reuse, and sequence-level parallelization techniques require future conditioning that closed-loop interactive generation does not provide. We present X-Cache, a training-free acceleration method that caches along a different axis: across consecutive generation chunks rather than across denoising steps. X-Cache maintains per-block residual caches that persist across chunks, and applies a dual-metric gating mechanism over a structure- and action-aware block-input fingerprint to independently decide whether each block should recompute or reuse its cached residual. To prevent approximation errors from permanently contaminating the autoregressive KV cache, X-Cache identifies KV update chunks (the forward passes that write clean keys and values into the persistent cache) and unconditionally forces full computation on these chunks, cutting off error propagation. We implement X-Cache on X-world, a production multi-camera action-conditioned driving world model built on multi-block causal DiT with few-step denoising and rolling KV cache. X-Cache achieves 71% block skip rate with 2.6x wall-clock speedup while maintaining minimum degradation.

---

## 论文详细总结（自动生成）

好的，我将基于您提供的论文内容，为您生成一份结构化的详细中文总结。

### 1. 论文的核心问题与整体含义
*   **研究背景**：基于自回归视频扩散的驾驶世界模型是实现自动驾驶策略可扩展评估和在线强化学习的关键基础设施。然而，其推理成本高昂，成为实时交互式部署的瓶颈。
*   **核心问题**：现有的扩散模型加速方法（如跨去噪步骤缓存）**不适用于**少步（如4步）蒸馏的自回归世界模型。原因有二：
    1.  **冗余消失**：少步蒸馏后，相邻去噪步骤间的特征相似性很低，缺乏可供复用的冗余。
    2.  **交互限制**：交互式生成无法提供未来条件信息，因此依赖“未来信息”的序列级并行技术（如块级联）无法应用。
*   **整体含义**：本文提出了一种新的加速范式，**从“跨去噪步骤”转向“跨连续生成块”** 来寻找和利用冗余，专门针对少步自回归世界模型的交互推理场景。

### 2. 论文提出的方法论
*   **核心思想**：利用自动驾驶场景在连续时间片段（块）间的**物理连续性**。相邻生成块对应的DiT块输入高度相似，因此可以缓存并复用前一个块的块输出残差，从而跳过大量计算。
*   **关键技术细节**：
    1.  **跨块残差缓存**：为每个（去噪步`t`, 块索引`b`）位置维护一个残差缓存 `ˆr_t,b`。在生成新块时，若满足条件，则直接用缓存残差与当前块输入相加，近似得到块输出。
    2.  **双度量门控机制**：决定何时可以安全地复用缓存。
        *   **指纹构建**：对块输入进行**3D网格采样**（而非1D均匀采样），以平衡覆盖帧和空间位置。附加**全局均值通道**和**动作条件通道**，以捕捉整体漂移和关键的控制指令变化。
        *   **度量计算**：
            *   **余弦相似度**：衡量全局方向一致性。
            *   **最大令牌偏差**：检测局部异常值。
        *   **跳过决策**：仅当两个度量同时通过阈值时，才跳过该块的计算。
    3.  **自适应阈值**：为每个（`t`, `b`）位置维护一个余弦相似度的指数移动平均（EMA），并以此动态调整跳过阈值 `τ_cos(t, b) = max(τ_floor, ¯s_t,b - m)`。这使得稳定相似的块能更激进地跳过，而变化大的块则保持保守。
    4.  **安全机制**：
        *   **KV更新块保护**：在将干净潜变量写入持久KV缓存的生成块中，**无条件强制所有块进行完整计算**。这是防止误差在自回归过程中无限传播的**最关键**的安全措施。
        *   **去噪步0保护**：可选地强制`t=0`步进行完整计算，因为该步受噪声和条件影响最大。
        *   **锚定块**：强制前`Fn`个块（默认为1，即块0）始终进行完整计算，确保条件信息能被可靠处理。
        *   **最大陈旧度**：限制每个位置连续跳过的次数，强制定期重计算以刷新缓存。

### 3. 实验设计
*   **模型与数据集**：
    *   **模型**：在**X-World**模型上验证，这是一个基于WAN 2.2构建的生产级、多摄像头、动作条件的驾驶世界模型，采用4步去噪和滚动KV缓存。
    *   **数据集**：使用**内部持有的测试集**，模拟交互式闭环推理协议。包含三种驾驶场景：**城市街道**（7段）、**高速公路**（3段）、**U型转弯**（3段）。U型转弯场景用于压力测试，因其跨块运动变化最大。
*   **Benchmark与对比**：
    *   **基准**：与**无缓存的完整计算（Full-Compute）** 版本进行对比。
    *   **对比方法**：**没有与现有缓存方法进行直接对比**。作者指出，现有方法（如FlowCache, SCOPE）基于跨步冗余，与本文的跨块冗余针对不同场景，因此不具备可比性。实验重点在于**绝对性能评估和消融研究**。

### 4. 资源与算力
*   论文中**仅明确说明了推理阶段的硬件配置**。
*   **推理硬件**：所有实验在单颗**阿里巴巴平头哥“真武”810E AI加速器（PPU）** 上运行，该加速器集成96GB HBM2e内存，支持BF16精度。DiT前向传播使用BF16精度。
*   **训练资源**：**未提及**。X-World模型的训练算力、时长等信息未在本文中说明。

### 5. 实验数量与充分性
*   **主实验**：在3类场景（共13段视频）上评估了X-Cache的默认配置，报告了速度、跳过率和多种图像质量指标（PSNR, SSIM, LPIPS）。
*   **消融实验**：在1段高速公路视频上，系统性地关闭或调整了各个核心组件（去噪步0保护、KV更新块保护、前锚定块数量、自适应阈值下限`τ_floor`），验证了各自的作用。
*   **分析与可视化**：提供了跨场景的定性对比图、逐帧质量曲线、以及跨（去噪步，块）的相似性热图和跳过率分布图，深入分析了方法的行为模式。
*   **评价**：
    *   **充分性**：实验设计**较为充分**，覆盖了多种典型驾驶场景，并进行了关键模块的消融，证明了核心机制（尤其是KV更新保护）的必要性。
    *   **客观性与公平性**：与自身完整计算版本对比是公平的。**未与同类加速方法对比是一个明确的局限性**，但作者给出了合理的解释（目标场景不同）。实验指标全面，包含客观指标和可视化结果。

### 6. 论文的主要结论与发现
*   **有效性**：在X-World模型上，X-Cache实现了**71%的块跳过率**，带来了**约2.6倍的DiT部分墙钟时间加速**，且质量下降极小（SSIM > 0.999， LPIPS < 4e-4）。
*   **鲁棒性**：性能在**城市、高速、U型转弯**三种场景下表现一致，跳过率波动很小（~0.3%）。这表明缓存决策更多由模型架构（块位置）决定，而非场景内容。
*   **关键发现**：
    1.  **KV更新块保护是质量保障的绝对必要条件**，关闭它会导致质量崩溃。
    2.  其他安全机制（步0保护、前锚定块）在当前测试集上作用不明显，但作为应对分布外数据的**安全边际**被保留。
    3.  跨块余弦相似度模式在模型中是**结构性的**（不同块的相似度有稳定差异），而非场景依赖的。

### 7. 优点
*   **创新性**：首次提出并验证了**跨生成块缓存**这一新的冗余利用维度，为少步自回归世界模型的加速开辟了新思路。
*   **实用性**：方法**无需训练**，可直接部署。设计考虑了生产系统的需求，如引入多种安全机制保证稳定性。
*   **系统性**：方法设计完整，涵盖了从冗余观察、缓存机制、门控决策到误差控制的全链路。
*   **分析深入**：实验分析不仅看最终指标，还深入探讨了方法在不同块、不同步骤上的行为模式，增强了说服力。

### 8. 不足与局限
*   **场景覆盖有限**：所有实验均在**白天、正常天气、内部数据分布内**的驾驶场景进行。未在**夜间、恶劣天气、极端驾驶**等分布外场景测试，方法的泛化能力有待验证。
*   **对比不全面**：**未与任何现有加速方法进行对比**，尽管理由合理，但无法让读者直观了解其在“加速效率-质量权衡”上的相对竞争力。
*   **参数调优探索不足**：作者承认默认参数是“安全而非激进”的，未对参数空间进行充分的帕累托前沿探索，以挖掘更大的加速潜力。
*   **长期稳定性未验证**：实验仅测试了约22秒的片段，未在**更长时域**的滚动中验证误差累积和性能漂移情况。
*   **模型普适性待考**：方法虽声称可扩展至其他类似世界模型（如WorldPlay），但未经过实际验证，尤其在动作变化更剧烈的模型中效果未知。

（完）
