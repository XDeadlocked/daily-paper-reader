---
title: "DynamicRad: Content-Adaptive Sparse Attention for Long Video Diffusion"
title_zh: DynamicRad：面向长视频扩散的内容自适应稀疏注意力
authors: "Yongji Long, Shijun Liang, Jintao Li, Yun Li"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20470v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 用于长视频扩散模型的内容自适应稀疏注意力机制
tldr: 本文针对长视频扩散模型计算效率低的问题，提出DynamicRad内容自适应稀疏注意力范式。该方法基于径向局部性先验，采用静态比率和动态阈值双模式策略，并结合离线贝叶斯优化与语义运动路由器，实现自适应稀疏选择。实验表明，DynamicRad在保持质量的同时显著提升推理速度，推动了效率-质量的帕累托前沿。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20470v1/fig-001.webp\", \"caption\": \"Figure 3: BO Convergence Analysis. The optimization process (simulated on proxy tasks) typically converges within 30 trials. Left: Static mode struggles with High Motion (red line stays high), necessitating a mode switch. Right: Dynamic mode successfully minimizes reconstruction error for High Motion scenes, validating its capability to capture complex dynamics.\", \"page\": 15, \"index\": 1, \"width\": 786, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.20470v1/fig-002.webp\", \"caption\": \"Table 1: Quality–Efficiency Comparison. All videos are generated at 768p resolution (1280× 768). HunyuanVideo241f uses an 8×H100 cluster; other settings use a single H100. Speedups are relative to the Original baseline per setting. ‡: Fine-tuned backbone for long context. †: Evaluated training-free (w/o LoRA) to demonstrate pure algorithmic gain. Bold: best; underline: best baseline.\", \"page\": 7, \"index\": 2, \"width\": 970, \"height\": 518}, {\"url\": \"assets/figures/arxiv/2604.20470v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of DynamicRad. Offline BO builds a lookup table of motion-regime-specific sparsity configurations. At inference time, a semantic router selects a regime from the prompt embedding, which determines either static-ratio or dynamic-threshold selection within a shared structured candidate set to produce the final block-sparse mask. An optional mask-aware LoRA compensates for sparsity-induced information loss.\", \"page\": 3, \"index\": 3, \"width\": 832, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2604.20470v1/fig-004.webp\", \"caption\": \"Table 4: Hyperparameter Settings and Optimization Bounds. Fixed system parameters are constant across all experiments. For mode-specific parameters, we report the Search Range [min,max] explored by the Bayesian Optimizer.\", \"page\": 13, \"index\": 4, \"width\": 973, \"height\": 461}, {\"url\": \"assets/figures/arxiv/2604.20470v1/fig-005.webp\", \"caption\": \"Table 3: Component ablations on long-horizon generation. Latency is measured on an 8×H100 cluster to align with the setup in Tab. 1. The offline predictor identifies optimal sparsity regimes, effectively reducing latency (e.g., Static mode: 338s→ 324s) while maintaining quality. Mask-aware LoRA further boosts temporal consistency (VBench S.C.) with minimal overhead.\", \"page\": 8, \"index\": 5, \"width\": 982, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2604.20470v1/fig-006.webp\", \"caption\": \"Figure 2: Regime-aware adaptivity. For low-motion prompts, the router selects static-ratio, yielding a highly sparse near-diagonal mask. For high-motion prompts, it switches to dynamic-threshold, preserving off-diagonal long-range connections useful for fast motion.\", \"page\": 9, \"index\": 6, \"width\": 966, \"height\": 550}, {\"url\": \"assets/figures/arxiv/2604.20470v1/fig-007.webp\", \"caption\": \"Figure 4: Visual Ablation of Parameter Tuning and Mode Selection. (a) The Static-ratio mode with naive heuristic parameters (rnear = 0.2, high decay γ = 3.0) imposes a rigid sparsity pattern. In this high-speed FPV scene, it restricts the temporal receptive field too aggressively, causing the repeating neon rings to flicker and the tunnel geometry to distort across frames. (b) The Dynamic-threshold mode configured by our pipeline. The Semantic Router correctly classifies this as a high-motion regime, triggering the retrieval of BO-optimized parameters (relaxed τnear = −2.0, denser λ = 0.9) which are required for high-optical-flow fidelity, perfectly preserving the symmetrical structure and smooth motion blur while satisfying the sparsity constraint. (Notation: r = [rnear, rfar] denotes retention ratios; τ = [τnear, τfar] denotes attention score thresholds).\", \"page\": 16, \"index\": 7, \"width\": 979, \"height\": 349}]"
motivation: 现有基于静态掩码的稀疏注意力方法在长视频扩散中可能丢失关键的长程信息，需要一种自适应机制来平衡效率与质量。
method: 提出DynamicRad范式，采用双模式策略（静态比率和动态阈值）和离线贝叶斯优化管道，结合语义运动路由器实现自适应稀疏选择。
result: "在HunyuanVideo和Wan2.1-14B上实验表明，DynamicRad实现了1.7-2.5倍推理加速，有效稀疏度超过80%，动态模式在长序列中甚至匹配或超过密集基线。"
conclusion: DynamicRad通过内容自适应的稀疏注意力机制，在长视频扩散模型中实现了效率与质量的帕累托前沿突破。
---

## 摘要
利用视频扩散中自然的时空能量衰减为实现高效性提供了一条路径，但仅依赖僵化的静态掩码可能在复杂动态中丢失关键的长程信息。为解决此问题，我们提出 \textbf{DynamicRad}，一种统一的稀疏注意力范式，其在径向局部性先验基础上实现自适应选择。DynamicRad 引入了 \textbf{双模式}策略：\textit{静态比率}模式用于速度优化执行，\textit{动态阈值}模式用于质量优先过滤。为确保鲁棒性且无需在线搜索开销，我们集成了一个离线贝叶斯优化（BO）流程，并结合一个 \textbf{语义运动路由器}。该轻量级投影模块以 \textbf{最小的运行时开销}将提示嵌入映射到最优稀疏度区间。与在线分析方法不同，我们的离线 BO 在基于物理的代理任务上优化注意力重建误差（MSE），确保快速收敛。在 HunyuanVideo 和 Wan2.1-14B 上的实验表明，DynamicRad 推动了效率-质量的帕累托前沿，在实现 \textbf{超过 80\% 有效稀疏度}的同时，获得了 \textbf{1.7$\times$--2.5$\times$ 的推理加速}。在某些长序列设置中，动态模式甚至达到或超过了稠密基线，而掩码感知的 LoRA 进一步提升了长时程一致性。代码发布于 https://github.com/Adamlong3/DynamicRad。

## Abstract
Leveraging the natural spatiotemporal energy decay in video diffusion offers a path to efficiency, yet relying solely on rigid static masks risks losing critical long-range information in complex dynamics. To address this issue, we propose \textbf{DynamicRad}, a unified sparse-attention paradigm that grounds adaptive selection within a radial locality prior. DynamicRad introduces a \textbf{dual-mode} strategy: \textit{static-ratio} for speed-optimized execution and \textit{dynamic-threshold} for quality-first filtering. To ensure robustness without online search overhead, we integrate an offline Bayesian Optimization (BO) pipeline coupled with a \textbf{semantic motion router}. This lightweight projection module maps prompt embeddings to optimal sparsity regimes with \textbf{minimal runtime overhead}. Unlike online profiling methods, our offline BO optimizes attention reconstruction error (MSE) on a physics-based proxy task, ensuring rapid convergence. Experiments on HunyuanVideo and Wan2.1-14B demonstrate that DynamicRad pushes the efficiency--quality Pareto frontier, achieving \textbf{1.7$\times$--2.5$\times$ inference speedups} with \textbf{over 80\% effective sparsity}. In some long-sequence settings, the dynamic mode even matches or exceeds the dense baseline, while mask-aware LoRA further improves long-horizon coherence. Code is available at https://github.com/Adamlong3/DynamicRad.

---

## 论文详细总结（自动生成）

### **论文总结：DynamicRad: Content-Adaptive Sparse Attention for Long Video Diffusion**

#### **1. 核心问题与整体含义**
*   **研究动机**：基于Transformer的视频扩散模型（如DiT）因其自注意力机制的O(N²)复杂度，在生成长视频（高分辨率、多帧数）时面临巨大的计算与内存瓶颈。
*   **核心矛盾**：现有高效注意力方法存在两难选择：
    *   **静态稀疏模式**（如Radial Attention）：硬件友好、效率高，但基于固定先验的掩码过于僵化，在复杂动态场景中可能丢失关键的长程依赖信息。
    *   **动态稀疏方法**：能根据内容自适应，但通常引入显著的在线计算开销或训练成本，难以稳定地扩展到长视频场景。
*   **论文目标**：提出一个统一的框架，**在保持硬件友好型稀疏计算效率的同时，引入低成本的内容自适应能力**，以在长视频生成中更好地权衡效率与质量。

#### **2. 方法论**
*   **核心思想**：在**径向局部性先验**（注意力权重随时空距离衰减）的结构化候选集基础上，实现**内容自适应的稀疏选择**。
*   **关键技术细节**：
    1.  **双模式稀疏选择**：
        *   **静态比率模式**：为追求极致吞吐量，在候选集中按固定比例均匀采样保留token对。无需计算注意力分数，开销极低。
        *   **动态阈值模式**：为追求最佳质量，利用少量注意力头的预softmax分数作为重要性代理，通过归一化和阈值过滤来动态选择候选集中的重要token对。
    2.  **离线贝叶斯优化配置**：为避免在线调参开销，设计了一个**基于物理的代理任务**（模拟不同运动速度的特征漂移），使用贝叶斯优化快速（<15分钟）为不同“运动状态”搜索最优的稀疏超参数（如衰减因子γ、长程因子λ、阈值等），并构建查找表。
    3.  **语义运动路由器**：一个轻量级MLP，利用视频模型已有的文本提示嵌入，预测当前生成任务的“运动强度”（低、中、高），并据此从上述查找表中索引对应的最优稀疏配置。开销极小（<2ms）。
    4.  **掩码感知LoRA**：一种参数高效的微调插件，专门针对稀疏掩码影响下的Q/K/V投影层进行微调，以补偿激进稀疏化可能带来的时序一致性损失，提升长视频质量。
*   **算法流程**：
    1.  **离线阶段**：使用代理任务和贝叶斯优化，为不同运动状态预计算最优稀疏配置表。
    2.  **在线推理**：
        *   输入文本提示，通过语义路由器确定运动状态。
        *   根据运动状态，从表中加载配置，并选择静态或动态模式。
        *   基于径向先验构建结构化候选集。
        *   在候选集内，根据所选模式（固定比例采样或基于分数阈值过滤）生成最终的块稀疏注意力掩码。
        *   （可选）应用掩码感知LoRA进行前向传播。

#### **3. 实验设计**
*   **模型与场景**：在两个先进的视频扩散模型上进行评估：
    *   **HunyuanVideo**：在120帧（标准）和241帧（长上下文）设置下测试。
    *   **Wan2.1-14B**：在72帧（标准）和145帧（长上下文）设置下测试。
    *   生成分辨率均为768p（1280×768）。
*   **评估基准**：
    *   **质量**：使用VBench（评估时序一致性、美学质量等）和VisionReward（评估与人类偏好的对齐度）。
    *   **效率**：测量端到端推理延迟（秒）和计算出的注意力稀疏度（%）。
*   **对比方法**：与最先进的、无需重新训练主干网络的稀疏注意力方法对比：
    *   PowerAttention (PA)
    *   Sparse VideoGen (SVG)
    *   Radial Attention（作为核心对比基线）

#### **4. 资源与算力**
*   **硬件**：主要使用**NVIDIA H100 GPU**。
*   **配置**：
    *   标准长度视频（~12-17万token）在**单张H100**上评估。
    *   超长视频（HunyuanVideo-241f， >20万token）在**8张H100集群**（使用序列并行）上评估以避免内存溢出。
*   **训练/优化开销**：
    *   语义运动路由器：在OpenVid-1M的2万对数据上训练，耗时**<1小时**（单H100）。
    *   掩码感知LoRA：微调**1千步**。
    *   离线贝叶斯优化：每个运动状态的配置搜索在**30次试验内收敛**，总耗时**<15分钟**。

#### **5. 实验数量与充分性**
*   **主实验**：在两个模型、两种帧数设置下，与3个基线方法进行了全面的质量-效率对比（共8组主要对比）。
*   **消融实验**：系统地验证了各个核心组件的贡献（双模式、离线预测器、掩码感知LoRA），清晰地展示了性能增益的来源。
*   **微基准测试**：详细分析了动态评分、路由等模块的运行时开销，证实其可忽略不计。
*   **参数分析**：展示了贝叶斯优化为不同运动状态找到的不同配置，符合物理直觉。
*   **定性展示**：提供了丰富的生成样本对比，直观展示了方法在多种场景（流体、快速运动、复杂场景等）下的有效性。
*   **评价**：实验设计**较为充分、客观和公平**。涵盖了主流模型和长/短上下文设置，对比了最相关的高效注意力基线，使用了公认的评估指标，并通过消融实验严格控制了变量。

#### **6. 主要结论与发现**
*   **效率领先**：DynamicRad显著提升了效率-质量帕累托前沿。静态比率模式在HunyuanVideo-241f上实现了**2.54倍**加速，在Wan2.1-14B-145f上实现了**2.25倍**加速。
*   **质量无损甚至超越**：动态阈值模式在长序列设置下，其生成质量（VisionReward）**匹配甚至超过了原始的稠密注意力基线**，表明自适应稀疏能起到“去噪”作用。
*   **关键组件有效**：双模式设计适应不同需求；离线贝叶斯优化与语义路由器实现了低成本自适应；掩码感知LoRA有效提升了激进稀疏下的时序一致性。
*   **泛化性强**：方法在DiT-based（HunyuanVideo）和VAE-based（Wan2.1）两种架构上均表现一致。

#### **7. 优点**
*   **内容自适应与效率的优雅平衡**：通过“离线优化+轻量级路由”范式，以近乎零的运行时开销实现了内容感知的稀疏化。
*   **实用性强的双模式设计**：为部署提供了灵活性（静态模式用于吞吐优先，动态模式用于质量优先）。
*   **无需主干网络重训练**：方法本质上是训练免费的，降低了应用门槛。
*   **系统性的工程优化**：考虑了从算法（代理任务）、配置（BO）到部署（块稀疏、LoRA补偿）的完整链条。

#### **8. 不足与局限**
*   **对极端快速运动的局限**：文中指出，对于像FPV无人机竞速这类**高度动态、注意力高度分散**的场景，局部的块稀疏模式可能仍无法完全捕捉所有关键依赖，可能导致轻微伪影。
*   **语义路由器的单一性**：路由器为整个视频预测一个全局运动状态。对于**包含多阶段或复杂动态的提示词**，单一配置可能不是最优解。
*   **硬件依赖与调优**：方法依赖于特定的块大小（如32/64/128）以实现内核友好，在非服务器级GPU或其他硬件上可能需要重新校准查找表。
*   **代理任务的假设**：离线BO依赖于基于特征漂移的物理代理任务，其与真实视频生成质量的相关性虽然通过定性实验验证，但未进行严格的定量相关性研究。

（完）
