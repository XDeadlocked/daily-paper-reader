---
title: "CorridorVLA: Explicit Spatial Constraints for Generative Action Heads via Sparse Anchors"
title_zh: CorridorVLA：通过稀疏锚点为生成式动作头施加显式空间约束
authors: "Dachong Li, ZhuangZhuang Chen, Jin Zhang, Jianqiang Li"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21241v1"
tags: ["query:sr-0501"]
score: 9.5
evidence: 具有空间约束的机器人控制视觉-语言-动作（VLA）模型
tldr: "本研究针对视觉-语言-动作模型中空间引导通常隐式注入的问题，提出CorridorVLA方法。该方法预测稀疏空间锚点作为增量物理变化，并利用它们为动作生成定义显式容忍区域（走廊），通过流匹配动作头施加约束。在更具挑战性的LIBERO-Plus基准上，该方法在SmolVLA和GR00T模型上均取得一致提升，成功率提高3.4%-12.4%，其中GR00T-Corr变体达到83.21%的成功率，表明动作对齐的物理线索能为生成动作策略提供直接可解释的约束。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21241v1/fig-001.webp\", \"caption\": \"Fig. 2. Framework. (A) The backbone predicts a small set of future key spatial increments, while the action output is augmented with the corresponding end-effector displacement fields. These key increments are then used to constrain action generation, requiring only a few additional prediction slots with minimal changes to the original VLA pipeline. (B) Spatial-change guidance provides a simple prior: manipulation trajectories tend to evolve smoothly, so sparse key increments can offer a safe, structured signal that reduces unstructured exploration under stochastic generation.\", \"page\": 3, \"index\": 1, \"width\": 512, \"height\": 410}]"
motivation: 现有VLA模型通常通过隐式特征注入空间引导，缺乏对动作生成的直接物理约束。
method: 提出CorridorVLA，通过预测稀疏空间锚点定义物理变化容忍区域，并利用流匹配动作头在训练中施加显式空间约束。
result: "在LIBERO-Plus基准测试中，CorridorVLA显著提升了SmolVLA和GR00T的成功率，其中GR00T-Corr变体达到83.21%的成功率。"
conclusion: 研究表明，将动作对齐的物理线索作为显式约束，能够有效补充视觉或隐式空间编码，提升生成动作策略的性能和可解释性。
---

## 摘要
视觉-语言-动作（VLA）模型通常使用中间表示来连接多模态输入与连续控制，但空间引导往往通过潜在特征隐式注入。我们提出CorridorVLA，它预测稀疏空间锚点作为增量物理变化（例如Δ-位置），并在训练目标中使用它们为动作生成施加明确的容差区域。这些锚点定义了一个引导流匹配动作头的“走廊”：隐含空间演化超出该区域的轨迹会收到校正梯度，同时允许与接触和执行噪声的微小偏差。在更具挑战性的LIBERO-Plus基准测试中，CorridorVLA在SmolVLA和GR00T上均取得一致提升，相比相应基线成功率提高3.4%–12.4%；值得注意的是，我们的GR00T-Corr变体达到83.21%的成功率。这些结果表明，动作对齐的物理线索能为生成式动作策略提供直接且可解释的约束，补充了以视觉或潜在形式编码的空间引导。代码发布于https://github.com/corridorVLA。

## Abstract
Vision--Language--Action (VLA) models often use intermediate representations to connect multimodal inputs with continuous control, yet spatial guidance is often injected implicitly through latent features. We propose $CorridorVLA$, which predicts sparse spatial anchors as incremental physical changes (e.g., $Δ$-positions) and uses them to impose an explicit tolerance region in the training objective for action generation. The anchors define a corridor that guides a flow-matching action head: trajectories whose implied spatial evolution falls outside it receive corrective gradients, while minor deviations from contacts and execution noise are permitted. On the more challenging LIBERO-Plus benchmark, CorridorVLA yields consistent gains across both SmolVLA and GR00T, improving success rate by $3.4\%$--$12.4\%$ over the corresponding baselines; notably, our GR00T-Corr variant reaches a success rate of $83.21\%$. These results indicate that action-aligned physical cues can provide direct and interpretable constraints for generative action policies, complementing spatial guidance encoded in visual or latent forms. Code is available at https://github.com/corridorVLA.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，为您生成一份结构化的详细总结。

### 1. 核心问题与整体含义
*   **研究背景**：视觉-语言-动作（VLA）模型旨在构建统一感知、语言理解和控制的通用机器人策略。一个关键挑战是如何将高层次的空间理解有效地传递给低层连续动作生成器。
*   **核心问题**：现有方法通常通过预测未来图像、视频等**视觉或潜在形式的中间表示**来注入空间引导，这种方式是**隐式**的，可能将任务相关的运动线索与外观细节纠缠在一起，对动作生成的影响不够直接。
*   **研究动机**：探索一种**互补**的路径：能否使用更接近动作空间的、**文本风格的物理量**（如坐标变化）作为中间表示，并在**目标函数层面**为动作生成提供**显式、可解释**的空间约束？

### 2. 方法论：CorridorVLA
*   **核心思想**：预测一组稀疏的未来空间“锚点”（如机械臂末端执行器的增量位置 Δ-pos），并利用它们为生成的动作轨迹定义一个“走廊”式的**显式容忍区域**。在训练时，若轨迹隐含的空间演化超出此区域则施加校正梯度，允许区域内的微小偏差，从而直接约束动作生成。
*   **关键技术细节**：
    1.  **稀疏锚点预测**：在VLA骨干网络中引入少量可学习的“锚点槽”（Anchor Slots），使其能够基于当前视觉-语言上下文，直接预测未来`K`个时间步的末端执行器Δ-位置。
    2.  **动作空间对齐**：将动作向量扩展，额外包含末端执行器的Δ-位置（`extra-A`设计），使动作监督与锚点预测共享相同的物理量，增强一致性。
    3.  **走廊约束损失**：在流匹配（Flow Matching）训练框架中，增加基于锚点的正则项。
        *   **缓冲区损失（`L_buf`）**：计算预测轨迹在锚点处的Δ-位置与真实锚点的距离，若超出以真实轨迹计算的容差宽度`δ`，则施加惩罚。这迫使轨迹快速进入“走廊”。
        *   **走廊内一致性损失（`L_cons`）**：当预测轨迹进入走廊后，该损失项确保其累积的空间变化与真实锚点的累积变化逐渐对齐，实现精细优化。
    4.  **噪声感知加权**：随着流匹配过程中的噪声水平（时间步`t`）增加，降低走廊约束的权重，因为在高度噪声状态下空间约束的可靠性下降。

### 3. 实验设计
*   **基准数据集**：
    *   **LIBERO**：用于初始验证和消融研究的主流终身学习机器人基准。
    *   **LIBERO-Plus**：更具挑战性的新基准，包含更强的扰动和更困难的输入条件（默认256x256分辨率），用于评估鲁棒性和跨主干网络的泛化能力。
*   **对比方法**：
    *   **基线模型**：`SmolVLA-Base` 和 `GR00T-Base`。
    *   **同期先进方法**：包括 `π0`、`GraspVLA`、`NORA`、`UniVLA`、`OpenVLA-OFT` 等。
*   **评估指标**：任务成功率（Success Rate）。

### 4. 资源与算力
*   论文**未明确说明**训练所使用的具体GPU型号、数量及总训练时长。仅提及实验基于 `SmolVLA`（使用LeRobot框架）和 `GR00T`（使用StarVLA公开实现）的官方默认设置进行，以确保公平比较。

### 5. 实验数量与充分性
*   **实验数量与类型**：
    1.  **主实验（2组）**：在LIBERO和LIBERO-Plus两个基准上，分别测试了CorridorVLA在SmolVLA和GR00T主干上的性能。
    2.  **消融实验（系统）**：
        *   **损失组件**：验证了缓冲区损失（`L_buf`）和一致性损失（`L_cons`）的必要性。
        *   **锚点选择策略**：对比了基于几何简化（RDP+DP）的锚点选择与均匀采样。
        *   **骨干网络交互方式**：比较了将状态作为输入、作为输出（双向/因果注意力）等不同设计。
        *   **预测目标**：对比了预测绝对位置（EE-Pos）、增量位置（EE-ΔPos）、同时预测当前和未来状态等不同目标的有效性。
*   **充分性与公平性评估**：
    *   **较为充分**：实验覆盖了主性能验证、核心模块消融、不同设计选择的对比，能够支撑其方法有效性的结论。
    *   **相对客观公平**：遵循了基线模型的官方默认设置进行对比，并在更具挑战性的LIBERO-Plus上验证了方法的鲁棒性和泛化性。消融实验设计合理，能说明各组件贡献。
    *   **局限性**：未与同样致力于空间引导但采用**视觉中间表示**的先进方法（如InterleaveVLA, ReconVLA）进行直接对比，作者解释是这些方法通常基于不同的架构和训练预算。

### 6. 主要结论与发现
*   **有效性**：CorridorVLA在LIBERO和LIBERO-Plus上均能稳定提升基线模型的性能（提升3.4%-12.4%），证明了**显式、动作对齐的物理线索**作为空间约束的有效性。
*   **轻量高效**：该方法仅需在骨干网络中添加少量预测令牌（默认K=3），几乎不增加推理开销，是一种高效的改进方案。
*   **设计洞察**：
    1.  预测**增量变化**（Δ-pos）优于预测**绝对位置**。
    2.  保持当前状态作为输入参考，仅预测未来线索（Keep-C/Predict-F）比同时预测当前和未来状态更有效。
    3.  在骨干网络中使用**双向注意力**进行锚点预测，能获得更好的跨模态交互，优于因果注意力。

### 7. 优点
*   **创新性**：提出了一种新颖的“走廊”约束范式，将空间引导从隐式的特征塑造转变为**显式的目标函数约束**，思路清晰。
*   **可解释性强**：约束基于明确的物理量（末端位移），使得模型的决策过程更易于理解和调试。
*   **通用性与轻量**：方法作为一种即插即用的正则化项，可应用于不同的流匹配VLA主干（如SmolVLA, GR00T），且附加计算成本极低。
*   **对噪声和接触的鲁棒性**：“走廊”设计允许轨迹在合理范围内偏离，能更好地容忍执行噪声和间歇性接触，更符合真实物理交互。

### 8. 不足与局限
*   **缺乏真实机器人验证**：所有实验均在仿真基准上进行。论文未在真实机器人上测试，其方法在sim-to-real差距、实际传感器噪声和系统辨识误差下的有效性尚待验证。
*   **对比范围有限**：如作者所述，未与采用视觉中间表示的最新方法进行头对头比较，因此其相对于这类主流范式的全面优势尚不完全明确。
*   **锚点表示的局限性**：当前工作仅探索了末端执行器Δ-位置这一种简单的物理量作为锚点。对于更复杂的任务（如涉及多个物体或关节协调），可能需要探索更丰富的空间锚点表示。
*   **超参数敏感性**：“走廊”宽度`δ`和损失权重`λ_corr`等超参数可能需要针对不同任务或机器人平台进行调整，其泛化性需进一步研究。

（完）
