---
title: "Latent Inter-Frame Pruning: A Training-Free Method Bridging Traditional Video Compression and Modern Diffusion Transformers for Efficient Generation"
title_zh: 潜在帧间剪枝：一种连接传统视频压缩与现代扩散Transformer以实现高效生成的无训练方法
authors: "Dennis Menn, Chih-Hsien Chou"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23858v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 使用扩散Transformer和潜空间剪枝的高效视频生成
tldr: 本文针对潜在扩散模型视频生成中计算成本高、速度慢的问题，观察到视频潜在表示在时间轴上存在冗余。受传统视频压缩算法启发，提出Latent Inter-Frame Pruning框架，通过剪枝重复的潜在补丁来减少计算负担。直接剪枝会导致视觉伪影，因此引入注意力恢复机制来弥补训练与推理的差距。该方法在保持视频质量的同时，将视频编辑吞吐量提高了1.44倍，达到12.44 FPS，为视频生成与压缩的结合提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23858v1/fig-001.webp\", \"caption\": \"Figure 2. Illustration on approximation of the pruned tokens with full length token sequence.\", \"page\": 4, \"index\": 1, \"width\": 932, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2604.23858v1/fig-002.webp\", \"caption\": \"Figure 3. Noise-aware unpruning.e\", \"page\": 4, \"index\": 2, \"width\": 416, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2604.23858v1/fig-003.webp\", \"caption\": \"Figure 4. Inference speed comparisons between baseline and our pruning method.\", \"page\": 5, \"index\": 3, \"width\": 519, \"height\": 433}, {\"url\": \"assets/figures/arxiv/2604.23858v1/fig-004.webp\", \"caption\": \"Figure 1. Framework overview: The proposed framework consists of three stages.\", \"page\": 3, \"index\": 4, \"width\": 932, \"height\": 332}]"
motivation: 视频生成计算成本高且速度慢，无法实时应用，而潜在扩散模型中的视频潜在表示在时间轴上存在冗余。
method: 提出Latent Inter-Frame Pruning框架，通过剪枝重复的潜在补丁并引入注意力恢复机制来减少计算冗余。
result: 该方法将视频编辑吞吐量提高了1.44倍，在NVIDIA RTX 6000上达到12.44 FPS，同时保持视频质量。
conclusion: Latent Inter-Frame Pruning框架通过剪枝冗余潜在帧并引入注意力恢复机制，在保持视频质量的同时显著提升了生成效率，为视频生成与压缩的结合提供了新思路。
---

## 摘要
视频生成虽然能够生成逼真的视频，但计算成本高昂且速度缓慢，难以实现实时应用。本文观察到，在潜在扩散模型框架下通过自编码器编码的视频潜在表示在时间轴上存在冗余。类似于传统视频压缩算法避免传输冗余帧数据，我们提出了潜在帧间剪枝框架，以剪枝（跳过重复潜在块的重计算）冗余部分，从而减轻计算负担并提高吞吐量。然而，直接剪枝会因全序列训练与剪枝推理之间的差异而导致视觉伪影。为解决这些伪影，我们提出了一种注意力恢复机制来弥合训练与推理之间的差距。通过所提出的方法，我们将视频编辑吞吐量提高了1.44倍，在NVIDIA RTX 6000上达到12.44 FPS，同时保持视频质量。我们希望这项工作能激发将传统视频压缩方法与现代视频生成流程相结合的进一步研究。本工作是关于带注意力恢复的无训练潜在帧间剪枝的初步研究。

## Abstract
Video generation, while capable of generating realistic videos, is computationally expensive and slow, prohibiting real-time applications. In this paper, we observe that video latents encoded via an autoencoder under the Latent Diffusion Model (LDM) framework contain redundancy along the temporal axis. Analogous to how traditional video compression algorithms avoid transmitting redundant frame data, we propose the Latent Inter-frame Pruning framework to prune (skip the re-computation of) duplicated latent patches, thereby reducing computational burden and increasing throughput. However, direct pruning results in visual artifacts due to the discrepancy between full-sequence training and pruned inference. To resolve these artifacts, we propose an Attention Recovery mechanism to bridge the train-inference gap. With our proposed method, we increase video editing throughput by 1.44$\times$, achieving 12.44 FPS on an NVIDIA RTX 6000 while maintaining video quality. We hope our work inspires further research into integrating traditional video compression methods with modern video generation pipelines. This work is a preliminary work on Training-free Latent Inter-Frame Pruning with Attention Recovery.

---

## 论文详细总结（自动生成）

### 论文总结：Latent Inter-Frame Pruning

#### 1. 核心问题与整体含义
*   **研究动机**：基于扩散Transformer的高质量视频生成/编辑模型（如潜在扩散模型）计算开销巨大，难以实现实时应用，限制了其实际部署。
*   **核心洞察**：论文观察到，即使经过VAE编码压缩到潜在空间，视频在**时间轴**上依然存在大量冗余（即相邻帧的相同空间位置内容相似）。这类似于传统视频压缩（如MPEG）中利用的帧间冗余。
*   **整体含义**：提出了一种**无训练**的加速框架，旨在将传统视频压缩中“跳过冗余信息”的思想，无缝集成到现代基于Transformer的视频生成管道中，在保持生成质量的同时显著提升推理速度。

#### 2. 提出的方法论
*   **核心思想**：在视频潜在序列输入扩散Transformer之前，**识别并剪枝（跳过）时间维度上内容重复的潜在块**，从而减少需要模型处理的总令牌数，降低计算复杂度。
*   **关键技术细节**：
    1.  **潜在帧间剪枝**：
        *   将视频潜在张量在空间上划分为块（Patch）。
        *   对于相邻帧中同一空间位置的块，计算其L1距离。若距离小于阈值 `θ`，则认为后一帧的块是冗余的，予以剪枝（即不将其转换为输入令牌送入Transformer）。
    2.  **注意力恢复机制**：
        *   **问题**：直接剪枝会改变模型训练时见过的完整令牌序列分布，导致自注意力计算产生偏差，引发视觉伪影。
        *   **解决方案**：在Transformer的自注意力层进行数学近似，使剪枝后的注意力输出逼近完整序列的注意力输出。
        *   具体地，对于被剪枝的令牌，利用其在KV缓存中内容相似的最近邻令牌的Key进行复制和替换，以模拟其存在。同时，为了**避免复制独立的噪声**（这违反扩散模型的i.i.d.噪声假设），论文提出了**噪声感知复制**策略，确保只复制“干净”的内容信号。
    3.  **令牌恢复**：在扩散去噪过程结束后，为了匹配解码器（VAE Decoder）所需的固定维度，将剪枝掉的令牌位置用其参考帧（根据剪枝记录）对应的已生成令牌进行复制填充。

#### 3. 实验设计
*   **数据集/场景**：在**DAVIS 2017**数据集（视频分割数据集）上，选取了**51个视频-文本提示对**进行视频编辑任务测试。
*   **基准模型**：以**Self-forcing模型**（一种基于因果注意力、旨在缩小训练-推理差距的自回归视频扩散模型）作为完整的、未加速的基线。
*   **对比方法**：主要进行**自身对比**，即比较使用本文提出的剪枝框架前后的模型性能（质量与速度）。
*   **评估指标**：
    *   **生成质量**：使用**VBench**综合评估套件，包含主体一致性、背景一致性、运动平滑度、美学质量和成像质量五个维度。
    *   **推理速度**：测量在固定硬件上的**吞吐量（FPS）**。

#### 4. 资源与算力
*   论文**未明确说明**训练所使用的具体算力资源（如GPU型号、数量、训练时长）。
*   仅在报告推理速度结果时提到，实验是在 **NVIDIA RTX 6000** GPU 上进行的。

#### 5. 实验数量与充分性
*   **实验规模**：实验规模**相对有限**。主要基于一个数据集（DAVIS）和一个基准模型（Self-forcing）进行了测试。
*   **实验设计**：
    *   **主实验**：在51个样本上对比了剪枝方法与基线的VBench得分和FPS。
    *   **消融实验**：文中通过分析（如指出直接复制Key会导致伪影，从而引出噪声感知复制）体现了对关键组件的必要性探讨，但未呈现系统性的、控制变量的消融研究表格或曲线。
*   **客观性与公平性**：
    *   质量评估使用了公认的VBench指标，具有一定客观性。
    *   速度对比是在相同硬件和模型上进行的，公平。
    *   **局限性**：缺乏在更广泛数据集（如WebVid-10M等）和其他视频生成模型（如SVD, Stable Video Diffusion）上的泛化性验证，这影响了结论的普适性。实验充分性有待加强。

#### 6. 主要结论与发现
*   **有效性**：所提出的**潜在帧间剪枝与注意力恢复框架**能够在不进行任何额外训练的情况下，成功集成到现有视频扩散Transformer中。
*   **性能**：在**基本保持视频编辑质量**（VBench各项指标与基线相当）的前提下，实现了**平均1.44倍的推理加速**（吞吐量从约8.6 FPS提升至12.44 FPS）。
*   **核心验证**：验证了生成模型潜在空间中的时间冗余可以被有效利用，且通过精心设计的注意力恢复机制，可以弥补因剪枝造成的训练-推理分布差异。

#### 7. 优点
*   **概念创新**：创造性地将传统视频压缩的经典思想与前沿的扩散生成模型相结合，思路清晰且具有启发性。
*   **实用性**：提出的是**即插即用、无需训练**的推理时加速方案，易于集成到现有管道中，部署成本低。
*   **针对性设计**：明确区分了理解任务与生成任务对剪枝的不同需求，并针对生成任务中必须恢复令牌和避免噪声相关性的特点，设计了**注意力恢复**和**噪声感知复制**等关键机制。
*   **效率提升明确**：从“减少生成令牌数”和“压缩KV缓存”两个角度分析了计算加速的来源，逻辑清晰。

#### 8. 不足与局限
*   **实验覆盖不足**：仅在单一模型（Self-forcing）和相对小规模的数据集上进行验证，缺乏广泛的泛化性实验和与其它加速方法的对比。
*   **方法固有局限**：
    *   **对运动敏感**：该方法基于时间冗余，对于高速运动或场景剧烈变化的视频，冗余减少，加速效果会下降。
    *   **依赖阈值**：剪枝效果依赖于手工设定的相似度阈值（`θ`, `θ2`），可能需要针对不同内容进行调整。
    *   **误差传播风险**：剪枝和恢复过程依赖于“相邻帧内容相似”的假设。若编辑导致某区域内容发生突变，基于前一帧的恢复可能产生错误，并在时间上传播。
*   **应用限制**：目前主要针对**视频编辑**任务（已知源视频），对于**无条件视频生成**或**长视频生成**的适用性未经验证。此外，方法专注于时间维，未探索空间维度的冗余剪枝。

（完）
