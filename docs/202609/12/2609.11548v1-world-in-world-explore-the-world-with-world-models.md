---
title: "World in World: Explore the World with World Models"
title_zh: World in World：用世界模型探索世界
authors: "Chenxi Song, Yanming Yang, Chi Zhang"
date: 2026-09-10
pdf: "https://arxiv.org/pdf/2609.11548v1"
tags: ["query:sr"]
score: 8.0
evidence: 用于探索自回归视频世界模型的免训练接口
tldr: 自回归视频世界模型支持交互式长时程探索，但新视角控制仍难。World in World 提出免训练推理接口，把源视频观测、目标视角投影、几何渲染与缓存外检索状态转为带相机和时间标签的干净视觉状态，并经冻结因果视频模型的原生自注意力读取。对应路由器与证据级注意力 CFG 分别建立 token 对应并调节各辅助通道贡献。同一骨干可支持相机控制重渲染、长时程回访与人体动作迁移，并在多视角变化下评估感知质量、时序一致性和相机跟随精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.11548v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1676, \"height\": 782, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.11548v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1856, \"height\": 981, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.11548v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1869, \"height\": 1604, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.11548v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1869, \"height\": 517, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.11548v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1870, \"height\": 529, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.11548v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1870, \"height\": 1275, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.11548v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1321, \"height\": 445, \"label\": \"Table\"}]"
motivation: 现有方法依赖任务专用模块或额外训练，难以在新视角探索中同时保持事件同步、视角正确、区域补全与回访一致。
method: 提出免训练推理接口，把源视频、目标视角投影、几何渲染和缓存外状态转为带相机与时间标签的视觉状态，经冻结视频模型自注意力读取，并用对应路由器和 EWA 调节。
result: 在相机控制视频重渲染的多视角变化下评估，兼顾感知质量、时序一致性与相机跟随精度。
conclusion: 同一冻结骨干无需额外训练即可统一支持相机重渲染、长时程回访和人体动作迁移，提升世界模型可控探索的灵活性。
---

## 摘要
自回归视频世界模型能够实现交互式、长时程探索，但灵活控制仍具挑战性。从新视角探索源视频要求生成的推演过程与记录事件保持同步，将观测内容置于所请求视角中，合理地补全新暴露区域，并在再次访问时恢复此前生成的外观。现有方法通常通过任务特定模块或额外训练来满足这些要求。我们提出 World in World，一种无需训练的推理时接口，可将异构控制证据转换为带有相机和时间标签的干净视觉状态，并通过冻结因果视频模型的原生自注意力进行读取。这些证据包括源视频观测、目标视角场景投影、用于引导新暴露主体区域补全的几何渲染，以及滚动缓存之外的检索生成状态。每个证据源都带有 token 级支持及其自身的可用性调度。对应关系路由器将持久点标识与几何相结合以建立 token 对应关系，引导受支持的查询指向匹配的源视频 token。随后，证据级注意力 CFG（EWA）利用同一次去噪前向传播中的注意力响应，独立调节每个辅助通道的额外贡献。该共享接口以同一冻结骨干支持相机控制的重新渲染、长时程重访和人体运动迁移。我们在多样视角变化下的相机控制视频重渲染任务上评估 World in World，考察感知质量、时间一致性和相机跟随准确性。

## Abstract
Autoregressive video world models enable interactive, long-horizon exploration, but flexible control remains challenging. Exploring a source video from new viewpoints requires the generated rollout to remain synchronised with the recorded event, place observed content in the requested view, plausibly complete newly exposed regions, and recover previously generated appearance on revisits. Existing methods typically address these requirements through task-specific modules or additional training. We present World in World, a training-free inference-time interface that converts heterogeneous control evidence into camera- and time-labelled clean visual states, which are read through the native self attention of a frozen causal video model. The evidence comprises source-video observations, target-view scene projections, geometry renderings that guide completion of newly exposed subject regions, and retrieved generated states beyond the rolling cache. Each evidence source carries token-level support and its own availability schedule. A correspondence router combines persistent point identities with geometry to establish token correspondences, guiding supported queries towards matching source-video tokens. Evidence-wise attention CFG (EWA) then independently regulates each auxiliary channel's additional contribution using attention responses from the same denoising forward pass. The shared interface supports camera-controlled rerendering, long-horizon revisiting, and human-motion transfer with the same frozen backbone. We evaluate World in World on camera-controlled video rerendering under diverse viewpoint changes, assessing perceptual quality, temporal consistency, and camera-following accuracy.

---

## 论文详细总结（自动生成）

# World in World 论文总结

## 1. 核心问题与整体含义

- **研究动机**：自回归视频世界模型支持交互式、长时程探索，但灵活控制仍困难。从新视角探索一段源视频时，生成推演必须同时满足四点：
  - 与记录事件保持同步；
  - 把观测内容放到请求视角的正确位置；
  - 合理补全新暴露区域；
  - 在回访时恢复此前生成的外观。
- **现有方法局限**：常见做法依赖任务特定模块、控制专用通路或额外训练；支持新证据类型或组合时，往往需要重新设计或再训练。
- **整体含义**：论文提出 **World in World（WiW）**，把世界模型控制从“模型适配问题”转化为“视觉证据构造与编排问题”。核心是：冻结因果视频世界模型，不训练新模块，而是把异构控制证据转换成模型原生自注意力已经能读取的“干净视觉状态”。

## 2. 方法论

### 核心思想

- 因果视频世界模型在自回归生成中，会通过原生自注意力读取初始观测和最近生成的干净状态，并带有相机与时间编码。
- WiW 利用这一既有入口，将以下证据统一表达为带相机、时间、空间有效性的干净视觉状态：
  - 源视频观测；
  - 目标视角场景投影；
  - 几何渲染结果；
  - 滚动缓存之外的生成历史。
- 不同证据不再各走专用控制通路，而是共享同一视觉接口，由冻结骨干的原生自注意力读取。

### 关键技术细节

- **统一证据表示**：在去噪步 \(s\)，转换器 \(\Psi_b\) 把原始证据 \(E_b\) 和目标相机轨迹 \(C\) 映射为  
  \[
  \Psi_b(E_b;C,s)=(X_b,P_b,\omega_b,\gamma_b(s))
  \]
  其中 \(X_b\) 是视觉内容，\(P_b\) 记录相机内参、位姿和事件时间，\(\omega_b\) 是 token 级空间支持，\(\gamma_b(s)\) 控制该证据通道在哪些去噪阶段激活。
- **写入原生注意力**：把干净视觉状态和相机/时间信息送入冻结视频骨干，扩散时间步设为 \(t=0\)，单次前向提取各层 key/value 并缓存为 clean K/V。当前去噪查询通过自注意力读取这些特征。
- **原生块与临时块**：原生块保留 18 个 latent-frame-state 槽位，包括 6 个源锚点、8 个近期历史状态、4 个当前块状态。源视频证据占用源锚点槽，其他证据作为临时辅助注意力块附加，当前块完成后移除。
- **时间与空间控制**：用 RoPE 设置证据与当前生成状态的时间坐标；用空间支持权重 \(\omega_b\) 缩放未归一化注意力权重；用 \(\gamma_b(s)\) 控制证据在去噪过程中的参与阶段。
- **目标视角场景证据**：用 DepthCrafter 估计源视频深度，将源像素反投影到 3D 并从源相机重投影到目标相机，得到目标视角 RGB、可见性 mask 和目标相机深度。可见性和几何可靠性转化为 token 级支持权重。
- **渲染几何证据**：当大视角变化暴露源视频未观测的主体表面时，使用可渲染主体表示提供形状和外观建议。以人体为例，用 LHM++ 重建 avatar，用 SMPL-X 参数驱动，渲染目标视角 RGB、支持 mask 和深度，并与投影场景深度对齐以处理遮挡和边界。
- **检索生成证据**
