---
title: "Denoising, Fast and Slow: Difficulty-Aware Adaptive Sampling for Image Generation"
title_zh: 去噪快与慢：面向图像生成的难度感知自适应采样
authors: "Johannes Schusterbauer, Ming Gui, Yusong Li, Pingchuan Ma, Felix Krause, Björn Ommer"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19141v1"
tags: ["query:diffusion"]
score: 9.0
evidence: 扩散模型图像生成的自适应采样
tldr: 本文针对扩散模型图像生成中区域难度差异问题，提出在补丁层面调整噪声时间步和计算分配的Patch Forcing方法，结合难度预测实现自适应去噪，有效提升了生成质量与计算效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19141v1/fig-001.webp\", \"caption\": \"Table 1. Performance comparison on ImageNet 2562. With fixed architecture and NFE, our Patch Forcing Transformer (PFT) already outperforms the baseline under standard Euler sampling. Our proposed dual-loop and look-ahead samplers further improve performance by allocating function evaluations more effectively.\", \"page\": 6, \"index\": 1, \"width\": 404, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2604.19141v1/fig-002.webp\", \"caption\": \"Figure 14. Text rendering comparison. Our PFT shows superior text rendering compared to an equivalent model trained with vanilla Flow Matching, under identical training and inference settings (plain Euler sampler, fixed NFE, same seed). Additional uncurated samples in Figure S6.\", \"page\": 8, \"index\": 2, \"width\": 458, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.19141v1/fig-003.webp\", \"caption\": \"Figure 13. Qualitative text-to-image results at 512 px resolution.\", \"page\": 8, \"index\": 3, \"width\": 465, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2604.19141v1/fig-004.webp\", \"caption\": \"Figure 4. Illustation of adaptive samplers. Left: 3× 3 patchification of an image. Middle: For 9 patches, we visualize different sampling strategies. In Dual-Loop and Context Look-ahead, confident patches move faster than uncertain patches. Right: Generation performance across scheduling strategies. While Patch Forcing already outperforms the SiT baseline, the ordering choice is important: structured approaches like dual-loop and look-ahead yield better results than randomly selecting confident patches.\", \"page\": 4, \"index\": 4, \"width\": 1008, \"height\": 194}, {\"url\": \"assets/figures/arxiv/2604.19141v1/fig-005.webp\", \"caption\": \"Figure 6. Visualization of one-step predictions at varying timesteps and their corresponding uncertainty maps. These results show that the model develops a strong intuition about which regions are easy or difficult to generate from very early t.\", \"page\": 5, \"index\": 5, \"width\": 394, \"height\": 397}]"
motivation: 现有扩散与流模型统一分配计算量，忽略了图像不同区域去噪难度的差异。
method: 提出Patch Forcing框架，通过补丁级时序采样和难度预测头实现按区域自适应的计算分配。
result: 方法在ImageNet类别条件生成及文本生成任务上优于标准基线，并与其他方法互补。
conclusion: 论文表明基于补丁级的去噪时间调度为自适应图像生成提供了新的有效途径。
---

## 摘要
基于扩散和流的模型通常在空间上均匀分配计算资源，即使用相同的时间步长和函数评估次数来更新所有图像块。虽然这种方法较为方便，但它忽略了自然图像的异质性：某些区域易于去噪，而其他区域则需要更多的细化或额外的上下文信息。受此启发，我们探索了用于图像合成的图像块级噪声尺度。我们发现，在图像 token 之间简单地改变时间步长表现不佳，因为这会让模型暴露于推理阶段不会出现的过度信息化的训练状态。因此，我们提出了一种时间步采样器，它能够在训练过程中显式控制每个图像块可获得的最大信息量。实验表明，从全局时间步转向块级时间步已经可以优于标准基线的图像生成效果。进一步地，通过为模型添加一个轻量级的每块难度预测头，我们实现了自适应采样器，使其能在最需要的地方动态分配计算资源。结合在空间和扩散时间上均变化的噪声水平，这构成了 Patch Forcing（PF）框架，它可以让较容易的区域更早收敛，从而为困难区域提供上下文支持。PF 在类别条件的 ImageNet 上取得了优异的结果，与表征对齐和引导方法相互独立，并且可以扩展到文本到图像的合成任务。我们的研究结果表明，块级去噪调度为自适应图像生成提供了一个有前景的基础。

## Abstract
Diffusion- and flow-based models usually allocate compute uniformly across space, updating all patches with the same timestep and number of function evaluations. While convenient, this ignores the heterogeneity of natural images: some regions are easy to denoise, whereas others benefit from more refinement or additional context. Motivated by this, we explore patch-level noise scales for image synthesis. We find that naively varying timesteps across image tokens performs poorly, as it exposes the model to overly informative training states that do not occur at inference. We therefore introduce a timestep sampler that explicitly controls the maximum patch-level information available during training, and show that moving from global to patch-level timesteps already improves image generation over standard baselines. By further augmenting the model with a lightweight per-patch difficulty head, we enable adaptive samplers that allocate compute dynamically where it is most needed. Combined with noise levels varying over both space and diffusion time, this yields Patch Forcing (PF), a framework that advances easier regions earlier so they can provide context for harder ones. PF achieves superior results on class-conditional ImageNet, remains orthogonal to representation alignment and guidance methods, and scales to text-to-image synthesis. Our results suggest that patch-level denoising schedules provide a promising foundation for adaptive image generation.

---

## 论文详细总结（自动生成）

# 去噪快与慢：面向图像生成的难度感知自适应采样（Denoising, Fast and Slow）

---

## 一、研究核心问题与整体背景

### 研究动机
- **现状问题**：现有的扩散（Diffusion）与流匹配（Flow Matching）模型在图像生成过程中通常假设各个空间区域同等难度，因而在所有像素或补丁上采用**统一的时间步和计算预算**。  
- **缺陷**：这种均匀分配忽略了图像区域的异质性。例如，平滑的背景容易去噪，而边缘、文字和细小结构需要更长的推理与更多上下文信息。  
- **动机**：为了充分利用有限计算预算并提升复杂区域的生成质量，论文提出针对“区域难度”分配计算资源的思想，即允许图像不同部分以不同速度去噪。

---

## 二、方法论：Patch Forcing 框架

### 核心思想
- 提出 **Patch Forcing（PF）** 框架：在图像生成过程中不再使用全局一致的噪声时间步，而是为每个空间补丁分配独立的**时序噪声轨迹**。  
- 更容易的区域先去噪、提前生成，从而为难以去噪的区域提供上下文信息。  
- 结合一个轻量化的 **补丁难度预测头（Difficulty Head）**，实现推理阶段的**难度感知自适应采样**。

### 关键技术细节
1. **时序采样策略改进**  
   - 传统的独立采样会导致模型在训练中见到过多“接近完全去噪”的状态，与推理分布不匹配。  
   - PF提出新的 **Logit-Normal Truncated Gaussian (LTG)** 时间步采样器：  
     - 控制训练中每样本可获得的**最大信息量 (tmax)**；  
     - 从截断的高斯分布低半部分采样补丁时序，使训练阶段不出现过度干净的图像块；  
     - 与 Spatial Reasoning Model 的复杂递归采样相比更快且更符合推理分布。

2. **难度预测机制**  
   - 引入额外分支预测每个补丁的**不确定度 σθ(xt, t)**，作为区域难度指标。  
   - 训练时联合最小化**速度字段回归损失**与**负对数似然（NLL）不确定性损失**：
     \[
     L_{total} = E[(v_{GT}-v_\theta(xt,t))^2] - λ\log N(v_{GT}|sg(v_\theta(xt,t)),σ_\theta^2I)
     \]
     其中 `λ=0.01`。

3. **推理阶段自适应采样器**  
   - **Dual-Loop**：低不确定度区域（易生成）沿较大时间步前进，生成“未来”上下文供其他区域使用；高不确定度区域用小步细化。  
   - **Look-Ahead**：提前推进自信补丁至未来时间步 tctx，作为上下文条件指导不确定补丁；算法流程明确给出。
   - 这两种策略可在同样的采样预算下获得更好生成质量。

---

## 三、实验设计

### 数据集与场景
- **主要任务**：类条件图像生成与文本到图像（T2I）生成。
- **数据集**：
  - ImageNet‑256×256 用于类别条件生成。
  - COYO‑700M 子集（120M图像-文本对）及 JourneyDB 用于文本到图像训练。
  - T2I‑CompBench++ 与 GenEval 用作文本到图像评估。

### 基线与对比方法
- 与多种基线比较：
  - **SiT (Scalable Interpolant Transformer)** [Ma et al., 2024]
  - **SRM (Spatial Reasoning Models)** [2025]
  - **REPA**（Representation Alignment）与 **CFG**（Classifier‑Free Guidance）
  - 其他扩散模型如 SD‑DiT、MaskDiT、SDXL、PixArt‑α 等。
- 对比维度：
  - 生成质量指标：FID‑50k、sFID、IS（Inception Score）、Precision/Recall。
  - 推理速度指标：每 10k 样本时间、NFE（函数评估数）。
  - 文本生成评测：CompBench++ 各子任务得分、GenEval对齐率。

### 消融与验证
- 对采样策略、难度预测头、上下文帮助等进行了系统消融：
  - 比较多种时间步采样分布（Uniform、SRM uniform‑t、Truncated Gaussian、LTG）。
  - 评估不同 patch‑progression 策略（随机、并行、Dual‑Loop、Look‑Ahead）。
  - 分析补丁不确定度与验证误差的相关性（图8、图10）。
  - 研究不同百分位阈值选择、内外循环步数配置。

---

## 四、算力与资源

- 论文提到使用 **JUWELS/JUPITER 超算集群和 NHR@FAU Erlangen 资源**，由 **Gauss Center for Supercomputing** 提供。  
- 具体 GPU 型号、数量、训练时长等**未明确说明**。  
- 实验规模（ImageNet 与 1.2B 参数 T2I 模型）表明其算力要求较高，但无定量披露。

---

## 五、实验数量与充分性

- **实验覆盖广泛**：
  - 类条件生成、文本生成、采样策略消融及跨模型兼容性测试。  
  - 多表格（Tab.1、Tab.2–4）与附录实验（S1–S7）展示性能与机制验证。  
  - 还测试零样本迁移至未改动的 PixArt‑α 模型，显示方法可泛化。
- **充分性与公平性**：
  - 对比模型架构与训练步数保持一致（如 NFE 与参数量固定），确保公平；  
  - 消融实验和可视化结果支持结论充分。  
  - 未报告统计显著性，但结果趋势明确且跨不同尺度模型一致。

---

## 六、主要结论与发现

- **补丁级时间步调度显著提升生成质量**，相比全局时序能更好利用上下文。  
- **LTG 采样策略**有效解决训练–推理分布不匹配问题。  
- **难度预测头提供可靠的区域难度信号**，与验证误差相关性强，可指导动适性采样。  
- **Dual‑Loop 与 Look‑Ahead** 采样策略在相同计算预算下进一步提升 FID 和文本渲染清晰度。  
- 框架可自然扩展到文本到图像任务，与现有引导方法（REPA、CFG）互补且兼容。  
- Heterogeneous 去噪有助于强化文字渲染和结构细节的表达。

---

## 七、优点与亮点

- **创新性强**：首个在图像生成领域实现空间局部噪声时序自适应的框架。  
- **轻量实现**：无需重大架构改动，仅增加一个小型难度预测头，参数开销 <0.01%。  
- **训练与推理一致性改进**：LTG采样器有效减少上下文泄漏。  
- **采样灵活性高**：可与不同指导策略组合，提高质量或速度。  
- **可扩展性验证**：在PixArt‑α等现有模型上可零样本应用，显示泛化潜力。

---

## 八、不足与局限

- **算力未说明**：缺乏硬件与训练时间报告，不利于重现性。  
- **复杂性增加**：补丁级时间步和双循环采样流程在实现上较复杂，可能影响工程效率。  
- **评价指标集中于 FID 等单一指标**，缺少主观一致性或数据分布鲁棒性分析。  
- **尚未验证真实场景的效率收益**：主要提升生成质量，但时间加速收益未定量展示。  
- **方法依赖难度预测的稳定性**：预测头错误或偏差可能造成不均匀去噪与伪上下文风险。

---

（完）
