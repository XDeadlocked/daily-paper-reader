---
title: "Phone2Act: A Low-Cost, Hardware-Agnostic Teleoperation System for Scalable VLA Data Collection"
title_zh: Phone2Act：一个用于可扩展VLA数据收集的低成本、硬件无关遥操作系统
authors: "Om Mandhane, Bipin Yadav, Sangeetha Prasanna Ram, Gopalakrishnan Narayanan"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01948v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 用于VLA数据采集的低成本遥操作系统
tldr: "本文针对 VLA 模型训练数据收集成本高昂的问题，提出了 Phone2Act，一个低成本、硬件无关的遥操作框架。该框架利用 Google ARCore 将普通智能手机转化为 6-DoF 机器人控制器，并基于模块化 ROS 2 架构，通过可互换的桥接节点解耦控制逻辑与硬件细节，支持从工业协作机器人到低成本双臂机器人的多种平台。其通用记录器能同步多摄像头 RGB 流与机器人状态反馈，并以 LeRobot 数据集格式原生导出演示数据，无需后处理即可直接用于 VLA 微调。实验通过收集的 130 条演示数据微调 GR00T-N1.5 模型，在真实世界多阶段抓放任务上实现了 90% 的成功率，验证了框架的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01948v1/fig-001.webp\", \"caption\": \"Fig. 3. Mobile Interface. The custom Phone2Act Android application featuring configurable ROS 2 topics.\", \"page\": 2, \"index\": 1, \"width\": 310, \"height\": 683}, {\"url\": \"assets/figures/arxiv/2605.01948v1/fig-002.webp\", \"caption\": \"Fig. 4. Phone2Act Architecture. The system decouples the smartphone input from the robot hardware via a central, agnostic planner and standardized ROS 2 topics (phone2act/target pose, phone2act/gripper cmd, and phone2act/robot feedback).\", \"page\": 3, \"index\": 2, \"width\": 948, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2605.01948v1/fig-003.webp\", \"caption\": \"Fig. 5. Open-Loop Evaluation of Groot N1.5 trained on Phone2Act data. The predicted actions (orange) closely track the ground truth teleoperation data (blue), indicating successful learning of control signals.\", \"page\": 5, \"index\": 3, \"width\": 446, \"height\": 1211}, {\"url\": \"assets/figures/arxiv/2605.01948v1/fig-004.webp\", \"caption\": \"Fig. 6. Real-world execution of the learned policy. Task: ”Pick up the ball and place it in the purple basket.” The Dobot CR5 smoothly executes the sequence: approaching, grasping the target, and placing it into the designated basket.\", \"page\": 6, \"index\": 4, \"width\": 512, \"height\": 289}]"
motivation: 现有遥操作框架依赖专用硬件或与特定平台绑定，导致 VLA 模型训练数据收集成本高昂。
method: 利用 ARCore 将智能手机转化为 6-DoF 控制器，并通过模块化 ROS 2 架构实现硬件无关的遥操作。
result: "在真实世界多阶段抓放任务上，使用收集的数据微调 GR00T-N1.5 模型，取得了 90% 的成功率。"
conclusion: Phone2Act 为低成本、可扩展的 VLA 数据收集提供了一种实用的解决方案，降低了机器人学习研究的门槛。
---

## 摘要
为视觉-语言-动作模型训练收集多样化、高质量的操作数据，对许多研究团队而言仍然成本高昂，因为现有的遥操作框架依赖于专用硬件或与特定机器人平台紧密耦合。我们提出了Phone2Act，一个低成本、硬件无关的遥操作框架，它通过Google ARCore将普通智能手机转化为6自由度机器人控制器。基于模块化ROS 2架构构建，Phone2Act通过可互换的桥接节点将控制逻辑与硬件细节解耦，支持从工业协作机器人到低成本双臂机器人的多种平台，无需修改代码。通用记录器将多摄像头RGB流与机器人状态反馈同步，并以LeRobot数据集格式原生导出演示数据，消除了后处理需求，支持立即进行VLA微调。我们通过在130个收集的片段上微调GR00T-N1.5来验证该框架，在物理Dobot CR5上部署的真实世界多阶段拾放任务中实现了90%的成功率。

## Abstract
Collecting diverse, high-quality manipulation data for Vision-Language-Action (VLA) model training remains prohibitively expensive for many research groups, as existing teleoperation frameworks rely on specialized hardware or are tightly coupled to specific robot platforms. We present Phone2Act, a low-cost, hardware-agnostic teleoperation framework that transforms a commodity smartphone into a 6-DoF robot controller via Google ARCore. Built on a modular ROS 2 architecture, Phone2Act decouples control logic from hardware specifics through interchangeable bridge nodes, supporting platforms from industrial cobots to low-cost bimanual arms without code modification. A Universal Recorder synchronizes multi-camera RGB streams with robot state feedback and exports demonstrations natively in the LeRobot dataset format, eliminating post-processing and enabling immediate VLA fine-tuning. We validate the framework by fine-tuning GR00T-N1.5 on 130 collected episodes, achieving a 90% success rate on a real-world multi-stage pick-and-place task deployed on a physical Dobot CR5.

---

## 论文详细总结（自动生成）

好的，这是对论文《Phone2Act: A Low-Cost, Hardware-Agnostic Teleoperation System for Scalable VLA Data Collection》的详细结构化总结。

### 1. 核心问题与整体含义
*   **研究动机**：训练通用机器人策略（如视觉-语言-动作模型，VLA）需要大量高质量的操作数据，但现有数据收集方法存在显著瓶颈。
*   **核心问题**：
    1.  **成本高昂**：主流的遥操作方案（如双主从臂系统、VR头显）依赖昂贵的专用硬件。
    2.  **缺乏通用性**：现有框架通常与特定机器人平台深度耦合，难以适配自定义或非标准硬件，集成成本高。
    3.  **数据后处理繁琐**：收集的演示数据格式不统一，需要复杂的后处理才能用于VLA模型训练。
*   **整体含义**：本文旨在提出一个**低成本、硬件无关**的遥操作框架，以**极低的门槛**（仅需智能手机）为研究社区提供一种**可扩展、即用型**的VLA数据收集解决方案。

### 2. 方法论
*   **核心思想**：将普通智能手机（利用其内置的ARCore）转化为一个6自由度空间控制器，并通过一个**模块化、硬件抽象**的软件架构，将控制指令无缝映射到任意机器人平台，同时**原生记录**标准格式的演示数据。
*   **系统架构（三层）**：
    1.  **接口层（智能手机）**：定制Android应用，通过ARCore以50Hz频率估计手机6-DoF位姿，并使用音量键作为“离合器”（重新定位）和“夹爪开关”的输入。
    2.  **无关核心层（规划器）**：
        *   **空间映射**：通过静态轴对齐矩阵 `M` 和缩放因子 `S`，将手机相对位移 `ΔP` 映射到机器人目标位姿 `R_target`：`R_target = R_initial + M(ΔP · S)`。
        *   **浮动零点（离合器）**：允许操作者随时暂停控制并重新定位手机，从而扩展虚拟工作空间。
        *   **安全与工作空间限制**：包含工作空间钳位和零跳变过滤器，防止意外指令损坏机器人。
        *   **欧拉角空间旋转合成**：在内部使用欧拉角（RPY）计算旋转增量，以匹配下游VLA模型的动作表示空间，避免四元数的单位范数约束。
    3.  **硬件桥接与数据收集层**：
        *   **硬件桥接节点**：作为可互换模块，负责将规划器输出的标准化位姿命令转换为特定机器人API的指令（如单位、格式转换），并订阅高时效性QoS配置以确保命令新鲜度。
        *   **通用记录器（关键贡献）**：硬件无关的节点，以20Hz同步记录外部RGB摄像头流、机器人关节状态和末端执行器位姿。它应用**最短路径包装**处理欧拉角不连续性（`Δθ_wrapped = (Δθ + π) mod 2π − π`），并将数据**直接序列化**为LeRobot数据集格式（MP4视频 + Parquet元数据），无需后处理。

### 3. 实验设计
*   **主要验证场景**：
    1.  **系统延迟分析**：在标准Wi-Fi环境下，使用高速摄像测量从手机移动到机器人开始动作的端到端延迟（约350-440ms），并分析延迟主要来源于机器人硬件本身。
    2.  **硬件无关性与可扩展性验证**：
        *   **单臂平台**：在工业协作机器人Dobot CR5上执行高精度拾放任务。
        *   **双臂平台**：通过ROS 2命名空间隔离，零代码修改地控制两台低成本3D打印的LeRobot SO-101机械臂，实现双手同步遥操作。
    3.  **VLA策略学习验证**：
        *   **任务**：多阶段拾放任务（“捡起球并放入紫色篮子/托盘”）。
        *   **数据收集**：使用Phone2Act在Dobot CR5上收集了130条演示片段。
        *   **模型训练**：以此数据集微调NVIDIA的**GR00T-N1.5-3B** VLA基础模型。
        *   **评估方式**：
            *   **开环轨迹分析**：在保留的验证轨迹上，对比模型预测动作与人类演示数据，观察其一致性。
            *   **真实世界部署**：将微调后的策略部署到物理Dobot CR5上，执行10次连续试验，统计成功率。
*   **Benchmark与对比**：论文**没有**与同类其他遥操作框架进行直接的性能对比（如成功率、效率对比）。其验证主要是**自我证明**，即证明系统能工作、能收集有效数据、并能用该数据训练出在真实世界有效的策略。对比主要体现在**问题陈述**中，与依赖专用硬件的方案（如Mobile ALOHA、VR）进行定性比较，突出本工作的低成本与通用性优势。

### 4. 资源与算力
*   **模型训练**：微调GR00T-N1.5-3B模型时，使用了**单块NVIDIA RTX A6000 GPU**。
*   **训练配置**：采用混合精度（bfloat16），有效批次大小为48，峰值学习率为1e-4，共训练**2000步**。
*   **其他算力**：论文未提及数据收集过程中的算力需求（预计较低，主要为ROS 2节点运行和视频流处理），也未说明推理部署时的具体算力配置（仅提及使用云端客户端-服务器节点处理3B参数模型的推理）。

### 5. 实验数量与充分性
*   **实验组别**：主要包含三组实验：1) 延迟测量；2) 跨硬件部署演示（单臂CR5，双臂SO-101）；3) VLA策略学习与验证（包含开环分析和10次真实世界测试）。
*   **充分性与客观性评估**：
    *   **优点**：实验设计**完整地验证了**系统核心主张：低成本、硬件无关、数据即用。从硬件兼容性到最终策略性能形成了闭环验证。真实世界90%的成功率是强有力的实证。
    *   **不足**：
        1.  **实验规模较小**：仅使用130条演示微调模型，且真实世界测试仅10次试验。虽然结果积极，但更大规模的数据集和更多样化的任务测试会更有说服力。
        2.  **缺乏对比实验**：未与使用其他数据收集方法（如VR操作、传统示教器）在相同机器人、相同任务上收集的数据进行训练效果对比，无法量化Phone2Act所收集数据的相对质量优势。
        3.  **主观性**：虽然提到了操作者不易疲劳，但缺乏对操作效率、学习曲线、与专业设备操作精度对比的用户研究。
    *   **结论**：实验足以证明系统的可行性和有效性，但在**量化优势**和**普适性**证明上可以更充分。

### 6. 主要结论与发现
*   Phone2Act成功地将智能手机转化为一个有效的6-DoF机器人遥操作控制器，实现了**低成本、硬件无关**的数据收集。
*   其**模块化ROS 2架构**和**通用记录器**使得系统能轻松适配不同机器人，并直接产出适用于主流VLA训练流程（LeRobot）的标准数据集。
*   使用该系统收集的少量数据（130条演示）成功微调了一个大型VLA模型（GR00T-N1.5），并在真实的**多阶段拾放任务上达到了90%的成功率**，证明了所收集数据的**高质量和实用性**。
*   该系统显著降低了机器人操作数据收集的门槛，有利于推动更广泛的机器人学习研究。

### 7. 优点
*   **极致的低成本与易得性**：核心控制器是几乎人人拥有的智能手机，无需购置昂贵专用设备。
*   **真正的硬件无关性**：通过桥接节点抽象，可快速支持从工业级到开源DIY的各种机器人平台。
*   **数据即用（Data-Ready）设计**：通用记录器直接输出标准LeRobot格式，消除了繁琐、易错的数据对齐与后处理步骤，加速了研究迭代。
*   **优秀的工程设计与鲁棒性**：考虑了浮动零点、工作空间限制、零跳变过滤等实用功能，提升了系统的安全性和用户体验。
*   **良好的可扩展性**：利用ROS 2命名空间轻松支持多机器人（如双臂）控制。

### 8. 不足与局限
*   **延迟较高**：端到端延迟约400ms，虽对静态任务足够，但可能限制对动态、高速操作任务的支持。延迟主要受限于测试机器人硬件，但整体延迟仍高于一些专业有线控制器。
*   **实验验证的广度有限**：
    *   仅在拾放类任务上验证，未测试更复杂的操作（如插孔、旋拧、软体操作）。
    *   未在移动底盘或更复杂形态的机器人上进行验证。
    *   缺乏长期、多用户的操作体验评估。
*   **依赖外部传感器**：系统需要额外的外部RGB摄像头来记录视觉观察，增加了部署的复杂性和成本，未能完全利用手机摄像头。
*   **对比基准缺失**：如前所述，缺乏与现有数据收集方法在数据效率、策略性能上的直接量化对比。
*   **潜在偏差风险**：操作方式（手持手机在空中移动）可能引入特定的人体运动模式偏差，这些偏差是否会影响学习策略的泛化能力未作讨论。

（完）
