---
title: "BEACON: Cross-Domain Co-Training of Generative Robot Policies via Best-Effort Adaptation"
title_zh: BEACON：通过最佳努力适应实现生成式机器人策略的跨域协同训练
authors: "Antong Zhang, Han Qi, Heng Yang"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08571v2"
tags: ["query:sr-0501"]
score: 9.5
evidence: 扩散机器人策略的跨域协同训练
tldr: BEACON是一个理论驱动的框架，用于在源域演示丰富而目标域演示有限的情况下训练生成式机器人策略。该框架将跨域协同训练视为差异感知的重要性重加权问题，联合学习扩散策略和源样本权重，以最小化基于目标域泛化保证的目标。通过开发可扩展的差异估计器、随机交替更新和多源扩展，BEACON在多种操作任务中提升了鲁棒性和数据效率，并实现了隐式的特征对齐。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08571v2/fig-001.webp\", \"caption\": \"Figure 1: Cross-domain policy co-training via best-effort adaptation (BEACON). Given a large amount of (multi-)source demonstrations and a small amount of target demonstrations, BEACON jointly learns a diffusion-based visuomotor policy and a set of per-sample weights that directly minimizes a theoretically grounded discrepancy-based generalization bound on the target domain.\", \"page\": 2, \"index\": 1, \"width\": 826, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2605.08571v2/fig-002.webp\", \"caption\": \"Figure 2: Experimental setup. The source domain is simulated with the default setting in robosuite; Target-T: changes the texture of the table; Target-T+V: changes the texture and the camera viewpoint. The real domain has both visual and physical gaps compared to the simulation domain.\", \"page\": 7, \"index\": 2, \"width\": 664, \"height\": 482}, {\"url\": \"assets/figures/arxiv/2605.08571v2/fig-003.webp\", \"caption\": \"Table 1: Sim-to-sim results. T represents texture gap, and T+V represents texture + viewpoint gap. For block stacking, there are two evaluation criteria, R: success if cube is stacked at any frame, S: success if cube is stacked for at least 5 frames or at the last frame. Average accounts for the R criteria.\", \"page\": 8, \"index\": 3, \"width\": 828, \"height\": 222}, {\"url\": \"assets/figures/arxiv/2605.08571v2/fig-004.webp\", \"caption\": \"Table 2: Sim-to-real results. P is used to represent the physical and visual gaps of the real domain. T represents the texture gap. T-OOD represents the out-of-distribution evaluation results.\", \"page\": 8, \"index\": 4, \"width\": 828, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2605.08571v2/fig-005.webp\", \"caption\": \"Figure 3: UMAP visualization of latent features (block stacking). We visualize latent features of the image alone as well as all observations (visual + proprioception) after the encoder trunk. Feature alignment naturally emerges in BEACON as a byproduct of the discrepancy-based learning objective.\", \"page\": 9, \"index\": 5, \"width\": 826, \"height\": 358}]"
motivation: 为了解决在拥有丰富源域演示但目标域演示有限的情况下训练生成式机器人策略的挑战，研究者提出了BEACON框架。
method: 该框架将跨域协同训练视为差异感知的重要性重加权问题，联合学习基于扩散的视觉运动策略和样本级源权重，并开发了可扩展的实例级差异估计器、策略与权重的随机交替更新以及多源扩展。
result: 在仿真到仿真、仿真到现实以及多源操作任务中，BEACON在鲁棒性和数据效率上优于仅使用目标域、固定比例协同训练和特征对齐基线方法。
conclusion: BEACON 通过差异感知的跨域协同训练，实现了隐式的特征对齐，提升了策略的鲁棒性和数据效率。
---

## 摘要
我们提出BEACON——跨域协同训练的最佳努力适应框架——这是一个理论驱动的框架，用于利用丰富的源演示数据和有限的目标演示数据训练生成式机器人策略。BEACON将跨域协同训练构建为一个感知差异的重要性重加权问题，联合学习一个基于扩散的视觉运动策略和每个样本的源权重，以最小化由目标域泛化保证所启发的目标函数。为了使最佳努力适应适用于高维序列策略，我们开发了可扩展的实例级差异估计器、策略与权重的随机交替更新方法，以及一个平衡异构源域的多源扩展。在仿真到仿真、仿真到现实以及多源操作设置中，BEACON在鲁棒性和数据效率方面优于仅使用目标数据、固定比例协同训练和特征对齐基线方法。重要的是，即使没有显式的对齐目标，BEACON通过感知差异的跨域协同训练，实现了特征对齐的隐式结果。

## Abstract
We introduce BEACON--Best-Effort Adaptation for Cross-Domain Co-Training--a theory-driven framework for training generative robot policies with abundant source demonstrations and limited target demonstrations. BEACON casts cross-domain co-training as a discrepancy-aware importance-reweighting problem, jointly learning a diffusion-based visuomotor policy and per-sample source weights that minimize an objective informed by target-domain generalization guarantees. To make best-effort adaptation practical for high-dimensional sequence policies, we develop scalable instance-level discrepancy estimators, stochastic alternating updates for policy and weights, and a multi-source extension that balances heterogeneous source domains. Across sim-to-sim, sim-to-real, and multi-source manipulation settings, BEACON improves robustness and data efficiency over target-only, fixed-ratio co-training, and feature-alignment baselines. Importantly, even without an explicit alignment objective, BEACON achieves feature alignment as an implicit result of discrepancy-aware cross-domain co-training.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的详细中文总结。

### 1. 核心问题与整体含义
*   **研究动机**：在机器人模仿学习中，收集大量高质量的目标域（如真实世界）演示数据成本高昂且困难。然而，我们通常拥有丰富的源域（如仿真环境）数据。直接混合使用这些存在“域差异”（视觉、物理动力学等）的数据训练策略，会导致在目标域上的性能下降。
*   **核心问题**：如何设计一种原则性的方法，能够**有效利用大量但不完全匹配的源域数据**，同时**仅依赖有限的目标域数据**，来训练出在目标域上泛化能力强的机器人策略？
*   **整体含义**：论文提出了一个**理论驱动**的框架BEACON，将跨域协同训练重新定义为**差异感知的重要性重加权**问题。其核心是直接优化一个基于理论泛化保证的目标函数，从而在算法层面决定“应该信任哪些源数据”，而非依赖启发式的数据混合或特征对齐。

### 2. 提出的方法论
*   **核心思想**：基于 **“最佳努力适应”理论**，推导出目标域泛化误差的上界。通过联合优化策略参数和每个训练样本的权重，来最小化这个上界。权重自动学习的结果是：与目标域相似的源样本获得高权重，差异大的源样本被抑制。
*   **关键技术细节**：
    1.  **理论目标**：将BEA理论应用于扩散策略，形成联合优化目标 `min_{θ, q} Σ [q_i * (损失_i + λ_d * d_i)] + 正则化项`。其中 `q_i` 是样本权重，`d_i` 是样本 `i` 与目标域的差异估计。
    2.  **实例级差异估计**：为解决源数据内部异质性问题，没有使用统一的域间差异，而是为每个源样本计算差异 `d_i`。默认采用**k近邻方法**，在策略视觉编码器的特征空间中，计算源样本特征到其k个最近目标样本特征的平均距离，并进行归一化。
    3.  **随机交替优化**：为适应大规模训练，设计了小批量随机交替更新流程：
        *   **q-阶段**：固定策略参数，根据包含损失、差异估计和正则项的梯度更新样本权重 `q`。
        *   **h-阶段**：固定权重 `q`，用加权的行为克隆损失更新扩散策略参数 `θ`。
    4.  **多源扩展**：当源数据来自多个异构域时，引入**域级权重**和**样本级权重**的两层加权机制，自动平衡不同源域的贡献，防止负迁移。

### 3. 实验设计
*   **任务与场景**：
    *   **任务**：三个Franka机械臂操作任务，难度递增：`方块堆叠`、`杯子清理`、`穿线`。
    *   **场景**：
        1.  **仿真到仿真**：源域与目标域均为仿真，但存在**纹理差异**或**纹理+视角差异**。
        2.  **仿真到现实**：源域为仿真，目标域为真实机器人，存在视觉和物理动力学综合差异。
        3.  **多源**：源数据来自两个视觉上不同的仿真域。
*   **Benchmark与基线方法**：
    *   所有方法共享相同的扩散策略主干网络、编码器和目标域数据集。
    *   **对比基线**：
        1.  **Target-Only**：仅使用目标域数据训练。
        2.  **Co-Training**：以固定比例混合源和目标数据训练。
        3.  **MMD**：在协同训练基础上，增加最大均值差异特征对齐损失。
        4.  **UOT**：在协同训练基础上，使用非平衡最优传输进行特征对齐。

### 4. 资源与算力
*   论文在**附录G**中明确说明：每个实验运行使用**单块NVIDIA L40S Tensor Core GPU (48GB VRAM)**。
*   一次典型的仿真到仿真或仿真到现实训练，在**24小时**内完成。

### 5. 实验数量与充分性
*   **实验数量充分**：
    1.  **主实验**：在3个任务 x 2种跨域场景（sim-to-sim两种差异、sim-to-real）上全面评估，每个任务有大量随机初始状态测试。
    2.  **分布外评估**：在`方块堆叠`和`杯子清理`任务中，测试了在目标演示未覆盖的初始状态上的泛化能力。
    3.  **多源实验**：验证了多源扩展的有效性。
    4.  **消融与分析实验**：
        *   比较了不同的**差异估计器**（k-NN vs. 局部标记差异 vs. 域分类器）。
        *   比较了不同的**权重更新策略**（随机梯度 vs. 全凸求解）。
        *   **超参数敏感性分析**：对关键超参数进行了扫描分析。
        *   **特征可视化**：通过UMAP展示了隐式特征对齐现象。
        *   **权重演化分析**：展示了训练过程中源和目标样本权重的动态变化。
*   **客观性与公平性**：
    *   所有对比方法使用**相同的主干网络、编码器和目标数据集**，确保了比较的公平性。
    *   评估时使用**相同的随机初始状态集**。
    *   对于需要精确判断的任务（如`方块堆叠`），设置了**宽松(R)**和**严格(S)**两种成功标准，评估更全面。

### 6. 主要结论与发现
1.  **性能提升**：在仿真到仿真、仿真到现实及多源设置下，BEACON在成功率和数据效率上均**稳定优于**所有基线方法。
2.  **更强的泛化能力**：在分布外评估中，BEACON表现出比特征对齐基线（如MMD）更优的鲁棒性。
3.  **隐式特征对齐**：尽管目标函数中没有显式的特征对齐项，但通过差异感知的重加权，源域和目标域的特征在编码器空间**自然地实现了对齐**。这是一种涌现现象。
4.  **数据利用的有效性**：增加目标域数据量会**放大**BEACON的优势，而非取代其作用，说明其能更智能地利用新增的目标信息。
5.  **多源有效性**：多源扩展BEACON能有效整合异构源数据，进一步提升性能。

### 7. 优点
1.  **理论驱动**：方法直接最小化目标域泛化误差的理论上界，提供了坚实的原理性解释，而非依赖启发式设计。
2.  **实用性强**：针对高维扩散策略的训练挑战，提出了可扩展的差异估计器（k-NN）和稳定的随机优化流程，使理论框架得以落地。
3.  **隐式对齐的发现**：揭示了通过样本重加权实现特征对齐的新机制，为跨域学习提供了新的视角。
4.  **通用性与灵活性**：方法不局限于特定类型的域差异（视觉或物理），且自然地扩展到多源场景。
5.  **实验全面**：实验设计严谨，覆盖了多种任务、域差异类型和评估场景，并包含了深入的消融与分析。

### 8. 不足与局限
1.  **任务局限性**：实验主要集中于**准静态**的操作任务。文中指出，涉及**高速动态交互**的任务可能带来更大的视觉和动作差异，方法的有效性有待验证。
2.  **仿真依赖**：源数据主要来自仿真（通过MimicGen生成）。虽然处理了sim-to-real gap，但方法在利用**大量异构真实世界视频**作为源域方面的潜力尚未充分探索。
3.  **假设前提**：框架建立在“真实数据稀缺”的假设上。如果拥有海量且多样化的真实数据，其他方法（如纯大规模真实数据训练）的潜力可能未被完全比较。
4.  **超参数调整**：方法引入了一些新的超参数（如λ_d, α等），虽然进行了敏感性分析，但在新任务上的调优仍需一定成本。

（完）
