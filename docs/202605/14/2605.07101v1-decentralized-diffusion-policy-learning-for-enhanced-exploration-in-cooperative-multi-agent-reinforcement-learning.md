---
title: Decentralized Diffusion Policy Learning for Enhanced Exploration in Cooperative Multi-agent Reinforcement Learning
title_zh: 分散式扩散策略学习：增强协作多智能体强化学习中的探索能力
authors: "Yuyang Zhang, Haldun Balim, Na Li"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07101v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 多智能体强化学习中的去中心化扩散策略学习
tldr: 本研究针对合作多智能体强化学习（MARL）中探索策略不足的问题，指出现有去中心化软最大策略梯度（DecSPG）方法采用的高斯策略表达能力有限，尤其随智能体数量增加而恶化。为此，提出去中心化扩散策略学习（DDPL），利用去噪扩散概率模型这一表达力强的生成模型来参数化各智能体策略，以捕获多模态动作分布，并通过重要性采样得分匹配（ISSM）实现高效在线训练。该方法在多个连续动作MARL基准测试中均取得性能提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07101v1/fig-001.webp\", \"caption\": \"Table 3: Hyperparameters for Section 5\", \"page\": 25, \"index\": 1, \"width\": 827, \"height\": 185}, {\"url\": \"assets/figures/arxiv/2605.07101v1/fig-002.webp\", \"caption\": \"Figure 3: Average episode return during training for Algorithm 2 and baselines. Curves and shaded areas show the mean and std. over three random seeds, respectively.\", \"page\": 8, \"index\": 2, \"width\": 826, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2605.07101v1/fig-003.webp\", \"caption\": \"Figure 4: LEFT: the average episode return of DDPL with diffusion and Gaussian policies; RIGHT: the action distribution at a representative state for two action dimensions from two agents.\", \"page\": 9, \"index\": 3, \"width\": 826, \"height\": 336}]"
motivation: 现有去中心化软最大策略梯度（DecSPG）方法中高斯策略的表达能力有限，严重阻碍了多智能体强化学习中的探索。
method: 提出去中心化扩散策略学习（DDPL），使用去噪扩散概率模型参数化策略，并通过重要性采样得分匹配（ISSM）进行高效在线训练。
result: 在多个连续动作多智能体基准测试中，DDPL均表现出持续的性能提升。
conclusion: DDPL通过扩散策略有效提升了多智能体强化学习中的探索能力，并在多个基准测试中表现出优越性能。
---

## 摘要
协作多智能体强化学习（MARL）涉及复杂的智能体交互，需要有效的探索策略。一类重要的MARL算法——分散式Softmax策略梯度（DecSPG）通过基于能量的策略更新来解决这一问题。然而在实践中，此类基于能量的策略难以维持，通常被投影到高斯策略类中。本研究表明，高斯策略有限的表达能力严重阻碍了DecSPG中的探索，且随着智能体数量增加，这一限制会加剧。为解决该问题，我们提出分散式扩散策略学习（DDPL），该方法使用去噪扩散概率模型——一种能够捕捉多模态动作分布以增强探索的表达性生成模型——来参数化每个智能体的策略。DDPL通过重要性采样得分匹配（ISSM）实现扩散策略的高效在线训练，这是一种具有理论保证的新型训练方法。我们在代表性连续动作MARL基准测试中评估DDPL，包括多智能体粒子环境、多智能体MuJoCo、IsaacLab及JAX重实现的星际争霸多智能体挑战，均观察到性能的持续提升。

## Abstract
Cooperative multi-agent reinforcement learning (MARL) involves complex agent interactions and requires effective exploration strategies. A prominent class of MARL algorithms, decentralized softmax policy gradient (DecSPG), addresses this through energy-based policy updates. In practice, however, such energy-based policies are intractable to maintain and are commonly projected onto the Gaussian policy class. In this work, we show that the limited expressiveness of Gaussian policies severely hinders exploration in DecSPG, and this limitation worsens as the number of agents grows. To address this issue, we propose decentralized diffusion policy learning (DDPL), which parameterizes each agent's policy with a denoising diffusion probabilistic model, an expressive generative model that captures multi-modal action distributions for enhanced exploration. DDPL enables efficient online training of diffusion policies via importance sampling score matching (ISSM), a novel training method with theoretical guarantee. We evaluate DDPL on representative continuous-action MARL benchmarks, including multi-agent particle environment, multi-agent MuJoCo, IsaacLab, and JAX-reimplemented StarCraft multi-agent challenge, and observe consistently improved performance.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的详细中文总结。

---

### **1. 核心问题与整体含义**
*   **研究背景**：协作多智能体强化学习（MARL）因其在交通、机器人等领域的广泛应用而备受关注。其中，**探索**是核心挑战，智能体需要尝试多样化的协作模式以避免陷入次优均衡。
*   **核心问题**：一类重要的算法——**去中心化Softmax策略梯度（DecSPG）**，其理论更新会产生一个基于能量的、多模态的目标策略，有利于探索。然而，由于该目标策略难以精确表示，**实践中普遍将其投影到简单的高斯策略类**。论文指出，**高斯策略有限的表达能力（单模态）严重阻碍了探索**，且随着智能体数量增加，这种失败模式会**指数级恶化**，导致算法过早收敛到次优解。

### **2. 提出的方法论：DDPL**
*   **核心思想**：用**去噪扩散概率模型（DDPM）** 这一强大的生成模型，替代高斯模型，来参数化每个智能体的去中心化策略。DDPM能自然地表示复杂的多模态动作分布，从而从根本上解决探索不足的问题。
*   **关键技术**：**重要性采样得分匹配（ISSM）**。
    *   **挑战**：标准扩散模型训练需要从目标分布（即DecSPG的能量基策略）采样，这在在线MARL中无法实现。
    *   **解决方案**：ISSM通过从当前策略（易于采样）中抽取动作样本，并利用重要性权重（正比于 `exp(Q值)`）来重新加权得分匹配损失，从而无需目标分布样本即可训练扩散策略向目标分布靠近。
    *   **理论保证**：论文证明了ISSM的样本复杂度，并推导了在策略更新存在误差的情况下，最终策略的纳什均衡间隙（NE-gap）上界。
*   **算法流程（DDPL）**：
    1.  **初始化**：为每个智能体初始化一个扩散策略网络（得分网络）和一个Q值网络。
    2.  **循环（每个epoch）**：
        a. **数据收集**：使用当前策略与环境交互，收集经验数据。
        b. **Q函数更新**：用TD误差更新各智能体的Q网络。
        c. **策略更新**：对每个智能体，从其当前策略中采样一批动作，计算ISSM损失（权重为 `exp(η * Q值)`），并通过梯度下降更新扩散策略的得分网络参数。

### **3. 实验设计**
*   **基准场景**：在4类共8个**连续动作**MARL基准任务上进行评估：
    1.  **多智能体MuJoCo（MaMuJoCo）**：4个任务（如Ant-v2-2x4）。
    2.  **多智能体粒子环境（MPE）**：1个任务（Simple-Reference-v2）。
    3.  **IsaacLab**：1个任务（Bi-ShadowHandOver）。
    4.  **星际争霸多智能体挑战（SMAX）**：2个任务（3s_vs_5z, 6h_vs_8z）。
*   **对比方法**：
    *   **HAPPO** & **MAPPO**：主流的**同策略**（on-policy）MARL基线。
    *   **HASAC**：一个**异策略**（off-policy）MARL基线。

### **4. 资源与算力**
*   论文明确提到了训练硬件和时长。
*   **硬件**：使用 **NVIDIA RTX 5090 GPU** 进行训练。
*   **时长**：根据任务不同，训练总步数在500万到5000万之间，每个随机种子的**训练时间在1到8小时之间**。

### **5. 实验数量与充分性**
*   **主实验**：在**8个不同性质的任务**上对比了3个强基线方法，每个任务使用3个随机种子，结果以平均回报±标准差报告。
*   **消融实验**：将DDPL中的扩散策略替换为高斯策略（DDPL-Gaussian），直观展示了**多模态探索**是性能提升的关键机制（见图4）。
*   **敏感性分析**：测试了扩散模型的关键超参数（**去噪步数H**和**噪声计划表**），结果表明DDPL的性能对这些参数不敏感，具有较好的鲁棒性（见图5）。
*   **效率对比**：与另外两种单智能体扩散RL算法（DACER, QVPO）对比了训练和推理时间，展示了ISSM带来的**在线训练效率优势**。
*   **评价**：实验设计**较为充分和客观**。覆盖了主流的MARL测试平台，进行了必要的消融和敏感性分析以验证核心论点，并与当前SOTA方法进行了公平对比。实验结果的统计信息（均值、标准差）也提供了可靠性评估。

### **6. 主要结论与发现**
*   **理论验证**：高斯策略的表达能力限制是DecSPG类算法探索失败的关键原因，且该问题随智能体数量增加而加剧。
*   **方法有效性**：提出的**DDPL算法能够有效解决上述问题**。扩散策略能维持多模态的探索分布，从而发现更高回报的均衡。
*   **性能提升**：在绝大多数测试任务上，DDPL**显著且一致地超越了所有基线方法**，尤其在复杂任务（如IsaacLab手部操控和星际争霸任务）上提升幅度巨大（最高达155%）。
*   **高效可行**：ISSM方法使得扩散策略的在线训练变得高效，其训练开销显著低于其他扩散RL方法。

### **7. 优点**
*   **问题洞察深刻**：不仅提出了新方法，更从理论（定理3.1，命题3.2）和构造性示例上，清晰地揭示了现有方法（高斯策略）的根本缺陷。
*   **方法新颖且实用**：首次将扩散模型与去中心化MARL策略学习相结合，并设计了专用于在线训练的ISSM目标，兼具创新性和实用性。
*   **理论支撑扎实**：为ISSM提供了样本复杂度理论保证，并分析了不精确策略更新对最终均衡的影响（定理C.2），形成了“问题分析-方法设计-理论保证”的完整链条。
*   **实验全面且具说服力**：广泛的基准测试、深入的消融分析以及效率对比，全面验证了方法的有效性、鲁棒性和优势。

### **8. 不足与局限**
*   **策略结构限制**：论文专注于**完全去中心化**的策略（每个智能体只依赖全局状态），未考虑允许智能体间有限通信的**部分去中心化**或**网络化MARL**设置，这可能是更现实的场景。
*   **问题设定局限**：研究范围仅限于**完全协作**（identical-interest）的马尔可夫博弈。未涉及一般和博弈或竞争性环境，在这些环境中探索与利用的权衡可能更加复杂。
*   **计算开销**：尽管相比其他扩散RL方法更高效，但**扩散策略的推理（采样）仍需多步去噪过程**，其计算开销仍高于高斯策略（见表2）。在对实时性要求极高的应用中可能成为瓶颈。
*   **理论假设**：部分理论分析（如定理3.1）依赖于对Q函数估计误差的特定假设（在到达关键状态前准确），虽然合理，但在极其复杂的实际环境中可能难以严格满足。

（完）
