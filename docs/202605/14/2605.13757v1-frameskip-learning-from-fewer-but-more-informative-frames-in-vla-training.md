---
title: "FrameSkip: Learning from Fewer but More Informative Frames in VLA Training"
title_zh: FrameSkip：在VLA训练中从更少但信息更丰富的帧中学习
authors: "Bin Yu, Shijie Lian, Xiaopeng Lin, Zhaolong Shen, Yuliang Wei, Changti Wu, Hang Yuan, Haishan Liu, Bailing Wang, Cong Huang, Kai Chen"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13757v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 使用帧选择的VLA策略训练策略
tldr: 本文针对视觉-语言-动作（VLA）策略训练中存在的时序监督不平衡问题，提出了FrameSkip框架。该框架通过评分机制选择信息量更高的关键帧进行训练，而非均匀采样所有帧。实验表明，FrameSkip在多个基准测试中显著提高了性能，同时大幅减少了所需训练帧数。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13757v1/fig-001.webp\", \"caption\": \"Figure 3: FRAMESKIP pipeline. FRAMESKIP scores frames in each demonstration trajectory, retains highimportance frames under a target ratio, and injects the compressed trajectory view into VLA training through dataloader index remapping while leaving the model and inference procedure unchanged.\", \"page\": 4, \"index\": 1, \"width\": 761, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2605.13757v1/fig-002.webp\", \"caption\": \"Table 7: Results of evaluating the VLA models with the GR1 robot in the RoboCasa-GR1 Tabletop simulation environment. The results for Isaac-GR00T N1.5 and Isaac-GR00T N1.6 are sourced from the official Isaac-GR00T GitHub repository (NVIDIA et al., 2025b). We highlight the best results in bold and the second-best results with underline.\", \"page\": 12, \"index\": 2, \"width\": 950, \"height\": 1067}]"
motivation: 传统的VLA策略训练从密集的机器人演示轨迹中采样每一帧，但这种方法存在时间监督不平衡问题，即长低变化段主导训练，而关键操作转换（如对齐、接触、抓取和释放）却稀疏出现。
method: FrameSkip是一个数据层框架，通过动作变化、视觉-动作一致性、任务进度先验和夹爪状态转换等指标对轨迹帧进行评分，并在目标保留比例下将训练样本重新映射到高重要性帧。
result: "在RoboCasa-GR1、SimplerEnv和LIBERO基准测试中，FrameSkip相比全帧训练和简单帧选择变体，在成功率和保留率之间取得了更好的平衡，宏观平均成功率从66.50%提高到76.15%，同时仅保留了20%的唯一帧。"
conclusion: FrameSkip通过智能选择关键帧，在保持性能的同时显著减少了训练数据量，为VLA策略训练提供了一种高效的数据处理方法。
---

## 摘要
视觉-语言-行动（VLA）策略通常通过密集的机器人演示轨迹进行训练，这些轨迹往往通过遥操作收集，其训练方式是将每一帧记录都视为提供同等有用的监督信号进行采样。我们认为，这种惯例造成了时间监督的不平衡：长时间低变化片段主导了训练流，而诸如对准、接触、抓取和释放等操作关键转换仅稀疏出现。我们提出了FrameSkip，一个数据层帧选择框架，它利用动作变化、视觉-动作一致性、任务进度先验以及夹爪转换保持性对轨迹帧进行评分，然后在目标保留率下将训练样本重新映射至高重要性帧。由于FrameSkip仅在数据加载器中运行，因此它保持了VLA架构、动作头、训练目标和推理过程不变。在RoboCasa-GR1、SimplerEnv和LIBERO上的实验表明，与全帧训练及更简单的帧选择变体相比，FrameSkip在成功率和数据保留之间取得了更好的权衡，在三个基准测试中实现了76.15%的宏观平均成功率，而全帧训练为66.50%，同时在使用压缩轨迹视图（主要设置中保留20%的唯一帧）的情况下达成。

## Abstract
Vision-Language-Action (VLA) policies are commonly trained from dense robot demonstration trajectories, often collected through teleoperation, by sampling every recorded frame as if it provided equally useful supervision. We argue that this convention creates a temporal supervision imbalance: long low-change segments dominate the training stream, while manipulation-critical transitions such as alignment, contact, grasping, and release appear only sparsely. We introduce FrameSkip, a data-layer frame selection framework that scores trajectory frames using action variation, visual-action coherence, task-progress priors, and gripper-transition preservation, then remaps training samples toward high-importance frames under a target retention ratio. Because FrameSkip operates only in the dataloader, it leaves the VLA architecture, action head, training objective, and inference procedure unchanged. Across RoboCasa-GR1, SimplerEnv, and LIBERO, FrameSkip improves the success-retention trade-off over full-frame training and simpler frame selection variants, achieving a macro-average success rate of 76.15% across the three benchmarks compared with 66.50% for full-frame training while using a compressed trajectory view that retains 20% of unique frames in the main setting.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容，按照要求生成的结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义
*   **核心问题**：当前视觉-语言-行动（VLA）模型训练中存在**时序监督不平衡**问题。传统方法均匀采样密集演示轨迹中的每一帧，但机器人轨迹在时间上高度冗余（如长距离平稳移动），而决定任务成败的关键交互瞬间（如对准、抓取、释放）却非常稀疏。这导致在固定的优化预算下，关键但稀疏的决策时刻被大量冗余但信息量低的帧所稀释，模型在关键操作环节学习不足。
*   **整体含义**：本文提出，帧选择不应仅仅被视为一种数据压缩手段，而应被重新定义为一种**训练监督信号的再分配**策略。其目标是，在保持总训练步数不变的前提下，通过智能选择信息更丰富的帧，将梯度信号更多地集中在策略学习最脆弱的时刻，从而提升学习效率和最终策略性能。

### 2. 论文提出的方法论
*   **核心思想**：在数据加载层（Dataloader）对训练轨迹进行帧级别的智能选择与重采样，而不改变VLA模型架构、损失函数或推理过程。这是一个**架构无关**的通用训练框架。
*   **关键技术细节**：
    1.  **帧重要性评分**：为轨迹中每一帧计算一个综合重要性分数 `I(t)`，由以下四个可解释的信号加权组合而成：
        *   **动作变化重要性（AVI）**：衡量相邻帧间动作的差异，捕捉运动突变。
        *   **视觉-动作一致性（VAC）**：计算视觉特征变化与动作变化的比值，识别那些动作引起显著环境状态变化的帧（如接触物体）。
        *   **任务进度重要性（TPI）**：基于高斯混合模型（GMM）或简单高斯分布的先验，假设关键交互阶段（如抓取）更可能出现在轨迹的特定进度区间（如中间部分）。
        *   **夹爪转换保持**：显式保留夹爪状态（如开/合）发生变化的帧，确保关键接触事件不被剔除。
    2.  **比率感知的帧剪枝**：给定目标保留比率 `r`（如20%），根据重要性分数保留排名靠前的帧，并强制执行一些约束（如必须保留首尾帧、夹爪转换帧）。
    3.  **训练集成策略**：
        *   **预热阶段**：先使用全部帧（`r=1.0`）进行一定步数的训练，为模型提供稳定的初始监督。
        *   **剪枝采样阶段**：之后，大部分mini-batch来自剪枝后的高重要性帧视图（`r<1.0`），同时**定期混入少量全帧mini-batch**作为“上下文锚点”，以防止模型因轨迹过于稀疏而遗忘全局上下文。

### 3. 实验设计
*   **数据集/场景与Benchmark**：在三个主流的机器人操作仿真基准上进行了评估，覆盖了不同的机器人形态和任务类型：
    1.  **RoboCasa-GR1**：桌面操作任务，使用双灵巧手GR1机器人。
    2.  **SimplerEnv**：WidowX机器人操作任务，测试模型在训练未见场景（Out-of-Domain）下的泛化能力。
    3.  **LIBERO**：Franka机器人语言条件操作任务，使用专家演示轨迹。
*   **对比方法**：
    *   **基线**：**全帧训练**（Uniform Sampling），即标准训练方式。
    *   **消融对比**：随机帧选择、仅使用AVI的选择、AVI+TPI、AVI+VAC等简化版本。
    *   **SOTA模型对比**：在结果表格中，还与当前多个先进的VLA模型（如GR00T系列、OpenVLA、TwinBrainVLA等）进行了性能对比，以展示其相对竞争力。

### 4. 资源与算力
*   论文中明确提到了训练硬件：所有实验均在 **8块 NVIDIA H100 GPU** 上，使用 **DeepSpeed ZeRO-2** 分布式训练策略进行。
*   训练时长或总步数因数据集而异（例如，RoboCasa-GR1训练10万步，SimplerEnv训练6万步，LIBERO训练3万步），但未给出具体的挂钟时间（如小时/天）。

### 5. 实验数量与充分性
*   **实验数量充分**：论文进行了系统且全面的实验。
    1.  **主实验**：在三个不同的基准测试上均验证了FrameSkip相对于全帧基线的显著提升。
    2.  **消融实验**：
        *   **保留比率（r）**：测试了从10%到100%多个比率，验证了适度压缩（如20%-50%）效果最佳。
        *   **重要性度量**：系统拆解了AVI、VAC、TPI各个组件的作用，证明组合使用效果优于单一信号或随机选择。
        *   **预热步数**：测试了不同长度的预热阶段，表明FrameSkip对此超参数不敏感，鲁棒性较强。
*   **客观性与公平性**：
    *   **控制变量**：所有对比实验（包括与基线和消融变体）均使用**相同的VLA模型架构（StarVLA）、训练配方、优化步数和批量大小**，确保性能差异仅源于帧选择策略。
    *   **对比全面**：不仅与均匀采样对比，还与随机采样、仅动作变化采样等简单启发式方法对比，有力地证明了其基于重要性选择的有效性，而非仅仅是“减少数据量”带来的效果。

### 6. 论文的主要结论与发现
*   **核心结论**：通过数据层对训练帧进行重要性感知的选择与重采样，可以有效地重新分配时序监督信号，从而显著提升VLA策略的性能。
*   **关键发现**：
    1.  **性能提升**：在主要设置（保留20%帧）下，FrameSkip将三个基准的**宏观平均成功率从66.50%提升至76.15%**。
    2.  **高效性**：仅使用20%的帧即可超越使用100%帧的全帧训练，证明了**数据质量（信息密度）优于数据数量**。
    3.  **普适性**：该方法在三个不同的机器人平台和任务集上均有效，表明其是一个通用的训练原则。
    4.  **组件有效性**：AVI是核心信号，但VAC和TPI提供了互补信息，共同作用达到最佳效果。

### 7. 优点
*   **架构无关**：作为纯数据层干预，无需修改模型、损失或推理代码，易于集成到现有VLA训练流程中。
*   **原理清晰，可解释性强**：重要性评分基于直观的机器人学先验（动作变化、视觉反馈、任务阶段），而非黑盒学习。
*   **简单高效**：计算在数据预处理阶段完成，训练时仅涉及索引重映射，开销极低。
*   **实证充分**：通过多基准、多组消融实验扎实地验证了方法的有效性和鲁棒性。

### 8. 不足与局限
*   **对先验知识的依赖**：TPI组件的最佳实现（GMM）需要少量轨迹的阶段中心标注（如抓取发生的时刻）。虽然论文提供了无需标注的回退方案（高斯先验），但其效果可能弱于数据集自适应的GMM先验。
*   **仿真环境验证**：所有实验均在仿真环境中进行。在真实世界机器人数据上，由于感知噪声、动作延迟和状态估计误差，重要性评分指标（尤其是VAC）的可靠性和鲁棒性有待进一步验证。
*   **任务类型泛化**：实验集中于短视距的物体抓取与放置类任务。对于需要长时程规划、复杂技能序列或高度依赖时序动态（如推、插）的任务，过度剪枝是否会破坏必要的动作连贯性，仍需探索。
*   **与课程学习等方法的结合**：论文采用了固定的保留比率和混合采样策略。未来可探索动态调整保留比率（如随着训练进行逐渐降低），或与其他课程学习策略结合，以进一步优化学习过程。

（完）
