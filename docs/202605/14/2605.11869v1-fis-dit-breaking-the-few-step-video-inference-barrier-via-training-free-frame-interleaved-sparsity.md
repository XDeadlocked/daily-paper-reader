---
title: "FIS-DiT: Breaking the Few-Step Video Inference Barrier via Training-Free Frame Interleaved Sparsity"
title_zh: FIS-DiT：通过免训练的帧交错稀疏性突破少步视频推理障碍
authors: "Jian Tang, Jiawei Fan, Qingbin Liu, Zheng Wei"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11869v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 视频扩散模型的推理加速
tldr: 视频扩散变换器（DiT）的推理延迟可通过模型蒸馏降低，但每步推理延迟仍是瓶颈，尤其在少步场景中，时间状态稀缺导致现有基于去噪轨迹的加速方法收益递减。为此，本研究提出帧交错稀疏性DiT（FIS-DiT），一种无需训练且与算子无关的框架，将优化焦点从时间轨迹转移到潜在帧维度，利用帧间稀疏性和结构一致性，通过帧交错稀疏性（FIS）执行策略在模型层次中操作帧子集，实现高效计算。实验表明，该方法在多个模型上实现了2.11-2.41倍加速且性能下降可忽略，为实时高清视频生成提供了可扩展方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11869v1/fig-001.webp\", \"caption\": \"Figure 4: Overview of FIS-DiT.\", \"page\": 6, \"index\": 1, \"width\": 826, \"height\": 364}, {\"url\": \"assets/figures/arxiv/2605.11869v1/fig-002.webp\", \"caption\": \"Figure 5: Qualitative comparison on Wan 2.2 under the 4-step 720p setting across three diverse prompts.\", \"page\": 7, \"index\": 2, \"width\": 829, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2605.11869v1/fig-003.webp\", \"caption\": \"Figure 1: Prompt-agnostic frame dynamics. Adjacent-frame relative changes ∆rel i→i+1 remain nearly uniform across time and prompts on both models.\", \"page\": 3, \"index\": 3, \"width\": 829, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2605.11869v1/fig-004.webp\", \"caption\": \"Table 1: Quantitative comparison of inference efficiency and visual quality. MagCache-Fast uses the default caching threshold; MagCache-Force relaxes the threshold to force step skipping. FISBalanced and FIS-Fast denote conservative and aggressive FIS-DiT configurations, respectively.\", \"page\": 8, \"index\": 4, \"width\": 818, \"height\": 636}, {\"url\": \"assets/figures/arxiv/2605.11869v1/fig-005.webp\", \"caption\": \"Table 2: Ablation on interleaved frame scheduling and stride n.\", \"page\": 9, \"index\": 5, \"width\": 694, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2605.11869v1/fig-006.webp\", \"caption\": \"Figure 3: Interpolation error verification. Anchor frames (blue) incur <1% error; interpolated frames (red) show bounded, flat errors with no temporal drift.\", \"page\": 5, \"index\": 6, \"width\": 829, \"height\": 228}]"
motivation: 在少步推理场景中，时间状态稀缺导致现有基于时间轨迹的加速方法收益递减，需要从潜在帧维度寻找优化突破口。
method: 提出了一种无需训练、与算子无关的框架，通过帧交错稀疏性（FIS）执行策略，在模型层次中操作帧子集，无需全规模块计算即可刷新所有潜在位置。
result: 在Wan 2.2和HunyuanVideo 1.5上的实验表明，FIS-DiT在VBench-Q和CLIP指标上性能下降可忽略的情况下，持续实现了2.11-2.41倍的加速。
conclusion: 该框架为实时高清视频生成提供了一条可扩展且稳健的路径。
---

## 摘要
尽管视频扩散变换器（DiTs）的整体推理延迟可通过模型蒸馏大幅降低，但每步推理延迟仍是关键瓶颈。现有加速范式主要利用去噪轨迹中的冗余性；然而，我们发现这些逐步策略在少步数场景下存在收益递减的局限。在此类场景中，时间状态的稀缺性阻碍了有效的特征重用或预测建模，形成了进一步加速的巨大障碍。为克服此问题，我们提出帧交错稀疏DiT（FIS-DiT），这是一种无需训练且与算子无关的框架，将优化重点从时间轨迹转向潜在帧维度。我们的方法受该维度内在二元性启发：存在允许减少计算的帧间稀疏性，同时每个帧位置对全局时空上下文仍具有同等重要的结构一致性。基于此洞见，我们实现帧交错稀疏（FIS）作为执行策略，在模型层次结构中操纵帧子集，无需全规模块计算即可刷新所有潜在位置。在Wan 2.2和HunyuanVideo 1.5上的实证评估表明，FIS-DiT在VBench-Q和CLIP指标上始终实现2.11-2.41倍加速且性能下降可忽略，为实时高清视频生成提供了可扩展且稳健的途径。

## Abstract
While the overall inference latency of Video Diffusion Transformers (DiTs) can be substantially reduced through model distillation, per-step inference latency remains a critical bottleneck. Existing acceleration paradigms primarily exploit redundancy across the denoising trajectory; however, we identify a limitation where these step-wise strategies encounter diminishing returns in few-step regimes. In such scenarios, the scarcity of temporal states prevents effective feature reuse or predictive modeling, creating a formidable barrier to further acceleration. To overcome this, we propose Frame Interleaved Sparsity DiT (FIS-DiT), a training-free and operator-agnostic framework that shifts the optimization focus from the temporal trajectory to the latent frame dimension. Our approach is motivated by an intrinsic duality within this dimension: the existence of frame-wise sparsity that permits reduced computation, coupled with a structural consistency where each frame position remains equally vital to the global spatiotemporal context. Leveraging this insight, we implement Frame Interleaved Sparsity (FIS) as an execution strategy that manipulates frame subsets across the model hierarchy, refreshing all latent positions without requiring full-scale block computation. Empirical evaluations on Wan 2.2 and HunyuanVideo 1.5 demonstrate that FIS-DiT consistently achieves 2.11--2.41$\times$ speedup with negligible degradation across VBench-Q and CLIP metrics, providing a scalable and robust pathway toward real-time high-definition video generation.

---

## 论文详细总结（自动生成）

好的，我将基于您提供的论文内容，为您生成一份详细的结构化中文总结。

### **论文总结：FIS-DiT**

#### **1. 核心问题与整体含义**
*   **研究背景**：视频扩散变换器（DiT）通过模型蒸馏技术，可将生成视频所需的去噪步数（采样步数）大幅减少（例如4步或8步），从而降低整体推理延迟。然而，**每个去噪步骤内部的Transformer计算成本依然高昂**，成为新的瓶颈。
*   **现有方法的局限**：当前主流的免训练加速方法（如DeepCache、MagCache）主要利用**相邻去噪步之间**的特征冗余性，通过缓存、重用或预测来跳过计算。但在“少步”场景下，相邻步之间的状态差异巨大，可用的历史状态稀少，导致这些基于时间轨迹的加速方法**收益急剧下降（甚至失效）**。
*   **核心问题**：如何打破少步（Few-Step）视频生成范式下的推理加速壁垒？论文提出需要**转换优化维度**，从利用“步间冗余”转向挖掘“步内冗余”。

#### **2. 方法论：核心思想与关键技术**
*   **核心思想**：将加速的焦点从**时间（去噪步）维度**转移到**空间（潜在帧）维度**。在单个DiT前向传播过程中，并非完整计算所有帧的潜在特征，而是**稀疏地计算一部分“锚点帧”**，然后通过轻量级插值恢复被跳过的帧。
*   **关键技术细节**：
    1.  **帧重建**：在选定的Transformer模块（Block）中，仅对锚点帧子集执行完整的注意力/前馈网络计算。对于被跳过的帧，使用其前后锚点帧的输出进行**线性插值**来重建其特征。首尾帧始终作为锚点以保持边界稳定性。
    2.  **交错锚点调度**：为了避免同一帧在多个连续层中被持续跳过而导致误差累积，FIS-DiT在不同层中**交错使用不同的锚点帧集合**。例如，若稀疏率为1/2（n=2），则在第l层计算偶数帧，在第l+1层计算奇数帧，确保所有帧都能周期性地得到“刷新”（即被完整计算）。
    3.  **风险感知的稀疏部署**：并非所有模块和去噪步都适合稀疏计算。
        *   **模块感知**：根据实证观察（图2），模型中间层的特征在帧间变化平缓（冗余性高），适合稀疏计算；而头部和尾部的模块变化剧烈，需保持全帧计算。
        *   **步数感知**：在去噪过程的最后几步（噪声水平低），模型进行精细修复，对近似误差敏感，因此在这些步骤中禁用稀疏计算，使用全帧计算以保证质量。

#### **3. 实验设计**
*   **基准模型与场景**：
    *   **Wan 2.2 (14B)**：文本到视频（T2V）任务，4步蒸馏，测试了480p和720p分辨率。
    *   **HunyuanVideo 1.5 (8B)**：图像到视频（I2V）任务，8步蒸馏，测试了480p分辨率。
*   **评估指标**：
    *   **效率**：FLOPs、推理延迟（秒）、加速比。
    *   **质量**：CLIP-Score（文本-视频语义对齐）、VBench-Q（综合视频质量，包括时序一致性、运动平滑度等）。
*   **对比方法**：主要与基于时间轨迹缓存的先进方法 **MagCache** 对比。设置了两种配置：
    *   `MagCache-Fast`：使用默认缓存阈值，在少步场景下几乎无法触发加速（1.00倍）。
    *   `MagCache-Force`：放松阈值以强制重用，获得加速但牺牲质量。

#### **4. 资源与算力**
*   论文中明确提到了实验使用的**GPU型号**：
    *   Wan 2.2 实验在 **NVIDIA H20** GPU 上进行。
    *   HunyuanVideo 1.5 实验在 **NVIDIA L20** GPU 上进行。
*   **未明确说明**的信息包括：具体使用的GPU数量、单次实验的耗时、总训练时长（因为方法是免训练的，所以无训练成本）。

#### **5. 实验数量与充分性**
*   **主实验**：在两个不同的骨干模型（Wan 2.2, HunyuanVideo 1.5）、两种分辨率、两种生成范式（T2V, I2V）上进行了全面的定量与定性评估。
*   **消融实验**：
    1.  **交错调度有效性**：对比了使用交错调度与固定锚点集的效果，证明了交错策略对维持质量的关键作用。
    2.  **风险感知部署**：系统地移除了“模块感知”和“步数感知”保护，分析了各自对速度和质量的影响，验证了设计必要性。
    3.  **稀疏率探索**：测试了不同的锚点步长（n=2,3,4,5），展示了速度-质量的权衡曲线。
    4.  **方法互补性**：将FIS-DiT与强制的MagCache结合，验证了其与步级缓存方法的正交性。
*   **评估客观性**：使用了公认的自动化评估指标（CLIP, VBench-Q）和人工定性对比。实验在100个多样化的文本提示上进行，结果取平均，具有统计意义。对比基线选择了同类SOTA方法，并设置了合理的对比配置。

#### **6. 主要结论与发现**
1.  **有效性**：FIS-DiT在少步场景下显著优于基于时间轨迹的缓存方法。在保持视觉质量（CLIP/VBench-Q分数与基线相当）的前提下，实现了 **2.11倍至2.41倍** 的稳定加速。
2.  **关键洞察**：视频DiT的中间层特征在**潜在帧维度上存在高度且均匀的冗余**，这种冗余是提示词无关的，为帧级稀疏计算提供了理论基础。
3.  **正交性**：FIS-DiT与现有的步级缓存加速方法是**互补**的，两者结合可实现进一步的加速（论文中最高达2.48倍）。
4.  **实用性**：该方法无需训练、无需修改模型权重、兼容现有高效计算内核（如FlashAttention），具备即插即用的部署便利性。

#### **7. 优点**
*   **视角创新**：突破了在时间轨迹上“内卷”的思维定式，开创性地从潜在帧维度挖掘冗余，为解决少步推理瓶颈提供了新思路。
*   **免训练与通用性**：完全无需微调，可应用于任何预训练的视频DiT，是算子无关的通用框架。
*   **硬件友好**：帧级稀疏保持了每个被计算帧内空间令牌的连续性，可直接利用高度优化的密集计算内核（如FlashAttention），无需开发定制CUDA代码。
*   **模块化设计**：风险感知的门控机制（模块/步数感知）使得方法鲁棒，能够在敏感区域自动回退到全精度计算，平衡效率与质量。

#### **8. 不足与局限**
*   **稀疏模式固定**：当前采用均匀间隔的固定稀疏模式（锚点步长n）。对于运动极度不均匀或存在场景切换的视频，**自适应的、内容感知的稀疏策略**可能更有优势。
*   **重建方法简单**：默认使用线性插值进行帧重建，虽然高效但可能不是最优解。更复杂的重建算子（如基于学习的轻量级网络）可能进一步降低误差，但会引入额外开销。
*   **长视频生成验证不足**：实验主要在81帧（约3秒）的视频上进行。对于**生成长度显著增加**的视频，帧间冗余模式是否会发生变化、误差是否会累积，尚未得到验证。
*   **理论分析有限**：对“为何中间层具有高帧冗余”这一核心现象提供了充分的实证观测，但缺乏更深入的理论解释或分析。

（完）
