---
title: "Not All Tokens Need 40 Steps: Heterogeneous Step Allocation in Diffusion Transformers for Efficient Video Generation"
title_zh: 并非所有令牌都需要40步：扩散Transformer中异构步数分配实现高效视频生成
authors: "Ernie Chu, Vishal M. Patel"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06892v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 使用异构步长分配的扩散Transformer进行高效视频生成
tldr: 扩散Transformer在视频生成中计算成本高昂，因为其对所有令牌均匀应用大量去噪步骤。本研究提出异构步数分配（HSA），一种无需训练的推理算法，根据令牌的速度动态分配不同步数预算，并通过KV缓存同步和缓存欧拉更新高效处理跳过令牌。该方法在多个模型和任务上显著提升了推理效率，尤其在激进加速下保持生成质量，实现了更优的质量-运行时权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06892v1/fig-001.webp\", \"caption\": \"Figure 4: Per-dimension VBench profile at different runtime budget on Wan-2.1-1.3B. Each panel compares HSA against vanilla Flow Matching (FM) at reduced T and TeaCache [19] (TC) at the same runtime budget; All scores are normalized by the score of full-budget reference FM (T=100) for better visualization. HSA better tracks the reference envelope across all sixteen dimensions, while the baselines collapse visibly on several dimensions once the budget is aggressive.\", \"page\": 6, \"index\": 1, \"width\": 810, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2605.06892v1/fig-002.webp\", \"caption\": \"Table 1: T2V results on Wan-2.1-1.3B. VBench Total/Quality/Semantic and PSNR/LPIPS to FM (T=40) reference. HSA outperforms FM at reduced T and TeaCache [19] at 50% runtime and below.\", \"page\": 7, \"index\": 2, \"width\": 827, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2605.06892v1/fig-003.webp\", \"caption\": \"Figure 6: Velocity L1 relative change [14]. Top: Average per-token L1 relative change of the velocity prediction over the first 5 to 10 steps out of 40 denoising steps. The heatmap indicates that larger relative changes (yellow) are highly localized to the salient subject, while the background exhibits minimal change (blue). Bottom: Corresponding generated image frames. This spatial variance in velocity dynamics is utilized by the dynamic token selection strategy to allocate higher compute budgets to complex regions while aggressively caching the background.\", \"page\": 14, \"index\": 3, \"width\": 825, \"height\": 646}, {\"url\": \"assets/figures/arxiv/2605.06892v1/fig-004.webp\", \"caption\": \"Figure 1: Overview of Heterogeneous Step Allocation (HSA). (a) Spatiotemporal latent tokens are partitioned into K disjoint groups G1, . . . ,GK , each assigned a step budget sk (a divisor of T ), with G1 as the full-budget baseline group. (b) Each group is denoised on its own schedule, yielding an active set Ai = ⋃ {Gk : i mod (T/sk)=0} (c) Per-step DiT block with KV-cache synchronization: only the active tokens Ai flow through QKV projection and attention, their fresh K,V entries overwrite the cache, and self-attention attends active queries against the full K,V (fresh + cached), reducing the per-step cost from O(N2) to O(|Ai| ·N). (d) Cached Euler update: all N tokens are advanced in a single tensor op using the latest velocity v̂n (freshly computed for n ∈ Ai, cached otherwise).\", \"page\": 3, \"index\": 4, \"width\": 826, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2605.06892v1/fig-005.webp\", \"caption\": \"Figure 5: Qualitative results for Wan-2.2-A14B image-to-video generation at 25% runtime. Each row displays the first and last frames of a video generated using the same image-text prompt across different schedulers. Our proposed HSA-25 successfully preserves strong image-conditioning alignment and high visual fidelity. In contrast, the baseline FM at T=10 experiences severe quality degradation and structural misalignment by the final frame. Full videos are available on our website.\", \"page\": 8, \"index\": 5, \"width\": 829, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2605.06892v1/fig-006.webp\", \"caption\": \"Figure 2: L1 relative change [14] of the QKV vectors for Wan-2.1-1.3B. Each line tracks the average relative change of the Q/K/V vectors across all tokens in each block at each denoising step, normalized by the average L1 norm of the vectors (e.g. q_18 means the query vector in the 18th block). The early and late stages of the trajectory show higher relative change, indicating that they are more sensitive to stale-KV artifacts and motivating our caching window design in Section 2.5.\", \"page\": 5, \"index\": 6, \"width\": 811, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2605.06892v1/fig-007.webp\", \"caption\": \"Table 3: I2V results on Wan-2.1-1.3B. VBench-I2V Total/Quality and image-conditioning alignment (IV-Align., averaging I2V Subject, I2V Background, Camera Motion), plus PSNR/LPIPS to the FM (T=40) reference.\", \"page\": 16, \"index\": 7, \"width\": 827, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2605.06892v1/fig-008.webp\", \"caption\": \"Table 2: Full VBench T2V results on Wan-2.1-1.3B for all dimensions.\", \"page\": 16, \"index\": 8, \"width\": 826, \"height\": 884}]"
motivation: 标准的扩散Transformer在视频生成中对所有时空令牌均匀应用大量去噪步骤，计算成本高昂，而人类视觉会忽略大量冗余运动，因此需要更高效的推理方法。
method: 提出了一种名为异构步数分配（HSA）的训练无关推理算法，根据令牌的速度动态分配不同的去噪步数，并引入 KV 缓存同步机制和缓存欧拉更新来高效处理跳过计算的令牌。
result: 在Wan-2和LTX-2模型上的评估表明，HSA在文本到视频和图像到视频生成任务中，尤其在激进加速场景下，显著优于之前的缓存方法和基线，实现了更优的质量-运行时帕累托边界。
conclusion: HSA 在保持生成质量的同时，显著提升了视频生成的推理效率，且无需额外训练或复杂调优。
---

## 摘要
扩散Transformer（DiTs）在视频生成质量上已达到顶尖水平，但其计算成本巨大，因为标准推理对序列中的每个令牌均采用相同次数的去噪步骤。众所周知，人类视觉会忽略大量冗余运动。既然如此，为何我们最密集的模型却对每个时空令牌给予同等优先级？本文提出异构步数分配（HSA），这是一种无需训练的推理算法，它根据令牌的速度动态为不同的时空令牌分配不同的步数预算。为解决由此产生的序列长度不匹配问题，同时不牺牲全局上下文，HSA引入了KV缓存同步机制，使活跃令牌能够关注完整序列，同时完全绕过非活跃令牌。此外，我们推导出一种缓存欧拉更新方法，可在单次操作中推进被跳过令牌的潜在状态，而无需额外的模型评估。我们在Wan-2和LTX-2模型上对HSA进行了文本到视频（T2V）和图像到视频（I2V）生成的评估。结果表明，HSA显著优于先前最先进的缓存方法及原始流匹配基线，尤其是在激进加速场景下（例如50%和25%的运行时间）。关键的是，HSA在无需昂贵离线分析的情况下实现了更优的质量-运行时帕累托前沿，即使在严格的计算预算下也能稳健保持结构完整性和生成质量。项目页面：https://ernestchu.github.io/hsa

## Abstract
Diffusion Transformers (DiTs) have achieved state-of-the-art video generation quality, but they incur immense computational cost because standard inference applies the same number of denoising steps uniformly to every token in the sequence. It is well known that human vision ignores vast amounts of redundant motion. Why, then, do our densest models treat every spatiotemporal token with equal priority? In this paper, we introduce Heterogeneous Step Allocation (HSA), a training-free inference algorithm that assigns varying step budgets to different spatiotemporal tokens based on their velocity dynamics. To resolve the resulting sequence-length mismatch without sacrificing global context, HSA introduces a KV-cache synchronization mechanism that allows active tokens to attend to the full sequence while entirely bypassing inactive tokens. Furthermore, we derive a cached Euler update that advances the latent states of skipped tokens in a single operation without additional model evaluations. We evaluate HSA on the Wan-2 and LTX-2 models for both text-to-video (T2V) and image-to-video (I2V) generation. Our results demonstrate that HSA significantly outperforms previous state-of-the-art caching methods and the vanilla Flow Matching baseline, especially at aggressive acceleration regimes (e.g., 50% and 25% runtimes). Crucially, HSA achieves a superior quality-runtime Pareto frontier without the need for expensive offline profiling, robustly preserving structural integrity and generation quality even under tight computational budgets.   Project page: https://ernestchu.github.io/hsa

---

## 论文详细总结（自动生成）

### 论文总结：Not All Tokens Need 40 Steps: Heterogeneous Step Allocation in Diffusion Transformers for Efficient Video Generation

#### 1. 核心问题与整体含义
*   **研究动机**：当前基于扩散Transformer（DiT）的视频生成模型虽然质量顶尖，但计算成本极高。其根本原因在于标准的推理过程对所有时空令牌（token）**均匀地**应用大量去噪步骤，忽略了视频数据中固有的**时空不对称性**（如静态背景与动态前景）。这与人类视觉系统会忽略冗余信息的特性相悖，造成了巨大的计算浪费。
*   **研究目标**：提出一种无需重新训练的高效推理方法，通过为不同复杂度的令牌分配差异化的计算资源，在保持生成质量的同时，显著降低视频生成的推理成本。

#### 2. 提出的方法论：异构步数分配（HSA）
*   **核心思想**：根据令牌的“速度动态”（velocity dynamics），将令牌序列划分为K个组，并为每个组分配一个不同的去噪步数预算（sk）。复杂度高、变化快的令牌（如运动物体）获得更多步数（甚至全预算），而简单、静态的令牌（如背景）则被分配更少的步数，从而在整体上减少需要计算的“令牌-步数”总量。
*   **关键技术细节**：
    1.  **令牌划分与调度**：令牌分组策略（如基于速度变化动态选择、均匀分配、随机分配等）。为确保并行执行，每个组的步数预算sk必须是总步数T的除数，使得在任何全局迭代步i，活跃令牌集Ai可以通过简单的模运算确定。
    2.  **KV缓存同步**：为解决不同组令牌处于不同噪声水平导致的注意力不匹配问题，HSA维护一个全局KV缓存。在每一步，仅活跃令牌Ai通过模型计算并更新其对应的KV缓存条目；然后，活跃令牌的查询（Q）会与**完整的KV缓存**（包含新鲜条目和缓存的非活跃令牌条目）进行注意力计算。这保证了全局感受野，同时将每一步的自注意力计算复杂度从O(N²)降至O(|Ai|·N)。
    3.  **缓存欧拉更新**：对于被跳过的非活跃令牌，其潜在状态也需要根据噪声计划更新。HSA让每个令牌缓存其最近一次活跃时预测的“速度”（velocity）。在每一步，**所有令牌**（无论活跃与否）都使用同一个步长(σ_{i+1} - σ_i)和其对应的最新速度（活跃令牌用新计算的，非活跃令牌用缓存的）进行欧拉积分更新。这是一个无分支的单一张量操作，高效且GPU友好。
    4.  **相位感知缓存窗口**：观察到去噪过程早期和晚期对误差更敏感，HSA仅在去噪轨迹的中间阶段（如中间50%的步数）启用上述缓存跳过机制，在首尾阶段对所有令牌执行完整计算，以保护生成质量。

#### 3. 实验设计
*   **模型与场景**：主要在开源视频DiT模型 **Wan-2.1-1.3B** 上进行核心定量评估，并在更大的 **Wan-2.1-14B/2.2-A14B** 和 **LTX-2** 模型上进行定性展示。任务涵盖**文本到视频（T2V）** 和**图像到视频（I2V）** 生成。
*   **评估基准**：主要使用综合视频生成评测套件 **VBench**，它包含16个子维度，汇总为总得分、质量得分和语义得分。同时，作为辅助参考，也汇报了与全步数参考轨迹之间的PSNR和LPIPS。
*   **对比方法**：
    *   **均匀流匹配（FM）**：基线方法，对所有令牌使用相同的减少后的总步数（T=30,20,10）。
    *   **TeaCache**：当时最先进的基于离线分析的步级缓存方法。

#### 4. 资源与算力
*   论文在“附录E”中明确说明：所有实验在一台配备 **8块 NVIDIA A5000 GPU** 的服务器上运行。
*   生成用于VBench评估的视频耗时较长：**每个VBench条目需要大约1.5天的墙钟时间**来计算。

#### 5. 实验数量与充分性
*   **实验设置全面**：设计了多个HSA预设（HSA-75A/B, HSA-50, HSA-25）以瞄准不同的运行时预算（75%， 50%， 25%）。系统比较了四种令牌分配策略（动态、均匀、随机、随机保留首帧），并选择动态策略作为默认。
*   **评估维度丰富**：不仅汇报了VBench总分，还给出了全部16个细分维度的详细结果（附录表2），清晰展示了方法在各个视频质量指标上的表现。同时提供了I2V任务的专门评估结果（附录表3）。
*   **客观性与公平性**：实验遵循了领域内常见的评估流程。与基线（FM, TeaCache）的比较是在**相同或相近的运行时预算下**进行的，确保了对比的公平性。论文也客观指出了在I2V任务上，由于图像条件锚定效应强，所有方法质量差距较小的现象。

#### 6. 主要结论与发现
*   HSA能够实现**显著优于均匀加速和现有缓存方法的质量-运行时帕累托前沿**，尤其在激进加速（50%、25%运行时）场景下优势明显。
*   **无需任何训练或昂贵的离线分析**，即可达到优异性能，具有即插即用的便利性。
*   KV缓存同步和缓存欧拉更新机制被证明是有效的，能够维持生成过程的**结构完整性和全局一致性**。
*   在多个模型规模和生成任务（T2V, I2V）上均验证了其有效性，展示了良好的**泛化能力**。

#### 7. 优点
*   **创新性**：首次将“异构步数预算”的概念引入DiT推理加速，从空间维度而不仅仅是时间维度进行优化，思路新颖。
*   **实用性**：训练无关、模型无关，可直接应用于现有预训练模型，部署门槛低。
*   **机制设计精巧**：KV缓存同步在降低计算量的同时保留了Transformer的核心特性（全局注意力）；缓存欧拉更新实现了高效且统一的状态推进。
*   **实验详实**：提供了从定量到定性、从小模型到大模型、从T2V到I2V的全面验证，并深入分析了不同策略的影响。

#### 8. 不足与局限
*   **分组策略的引导依赖**：默认的动态分组策略依赖于初始若干步的全步数计算来估计令牌速度变化，在极端低步数预算下可能引导不充分或不稳定。
*   **定量评估的模型规模限制**：核心的定量对比（VBench分数）主要在1.3B参数模型上进行，虽然在大模型上有定性展示，但缺乏同样 rigorous 的定量数据来证明其在大规模模型上的绝对优势。
*   **缓存窗口参数**：相位感知缓存窗口（如中间50%）是一个需要手动设定的超参数，其最优值可能因模型、数据集或生成长度而异，论文未进行深入敏感性分析。
*   **潜在偏差**：动态分组基于“速度变化”，这可能隐含地偏好某些类型的运动模式或内容，对于非常规或复杂全局运动的场景，其分组效率可能需要进一步验证。
*   **应用限制**：方法主要针对自回归生成式模型，对于需要严格对齐中间状态的视频编辑等任务，其“偏离参考轨迹但质量仍高”的特性可能不适用。

（完）
