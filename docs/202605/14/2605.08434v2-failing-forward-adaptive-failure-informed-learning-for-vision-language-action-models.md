---
title: "Failing Forward: Adaptive Failure-Informed Learning for Vision-Language-Action Models"
title_zh: 故障前向：视觉-语言-动作模型的自适应故障感知学习
authors: "Meng Zheng, Samhita Marri, Anwesa Choudhuri, Benjamin Planche, Zhongpai Gao, Van Nguyen Nguyen, Terrence Chen, Girish Chowdhary, Ziyan Wu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08434v2"
tags: ["query:sr-0501"]
score: 9.5
evidence: 针对VLA策略的自适应失败告知学习
tldr: 针对视觉-语言-动作模型在机器人操作中因仅依赖成功行为克隆而导致的错误累积问题，本研究提出了自适应失败信息学习框架。该框架利用模型自身在线生成失败轨迹作为负向引导，通过共享骨干网络的双动作生成器进行联合训练，在采样时自适应避开失败区域。实验表明，该方法能有效提升模型在多种任务上的成功率和鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08434v2/fig-001.webp\", \"caption\": \"Figure 3: Visualization of in-domain (left) and out-of-domain (right) manipulation task setup. Row 1: exterior camera image, row 2: wrist camera image. See supplementary for more visualizations.\", \"page\": 8, \"index\": 1, \"width\": 738, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2605.08434v2/fig-002.webp\", \"caption\": \"Figure 2: Dual Action Generator (DAG)-VLA with Adaptive Failure-Informed Learning (AFIL).\", \"page\": 5, \"index\": 2, \"width\": 824, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2605.08434v2/fig-003.webp\", \"caption\": \"Table 4: Performance comparison on out-of-domain manipulation tasks. Success rates (%) reported over 25 configs × 3 runs for each task.\", \"page\": 9, \"index\": 3, \"width\": 764, \"height\": 191}, {\"url\": \"assets/figures/arxiv/2605.08434v2/fig-004.webp\", \"caption\": \"Figure 1: Overview of the proposed Adaptive Failure-Informed Learning (AFIL) pipeline. AFIL uses online-generated failure trajectories as adaptive negative guidance, steering action generation away from failure-prone regions and toward more reliable success modes.\", \"page\": 2, \"index\": 4, \"width\": 782, \"height\": 293}]"
motivation: 现有视觉-语言-动作模型仅依赖成功行为克隆，缺乏纠错信号，导致错误累积和失败。
method: 提出自适应失败信息学习框架，利用失败轨迹作为负向引导，联合训练双动作生成器。
result: 在多种机器人操作任务上的实验表明，该方法相比基线模型显著提高了任务成功率和鲁棒性。
conclusion: 该框架有效提升了VLA模型在机器人操作任务中的成功率和鲁棒性。
---

## 摘要
视觉-语言-动作（VLA）模型为可扩展的机器人操作提供了有前景的范式，但其仅依赖成功行为克隆的特性使其脆弱；由于缺乏纠正性训练信号，微小的执行错误会迅速累积成不可恢复的分布外故障。为解决这一局限，我们提出自适应故障感知学习（AFIL），这是一种端到端框架，利用故障轨迹作为基于扩散和流的VLA策略的自适应负向指导。AFIL使用预训练的VLA在线生成故障推演，避免了手工设计故障模式或人工介入恢复的需求。然后，它联合训练用于成功和失败行为的双重动作生成器（DAGs），同时共享一个共同的视觉-语言骨干网络，从而以有限的参数开销实现高效的故障感知策略学习。在采样过程中，故障生成器自适应地将动作生成引导远离易故障区域，转向更可靠的成功模式，其指导强度由每个扩散步骤中成功与失败分布之间的距离决定。在领域内和领域外的机器人操作任务（涵盖短期和长期设置）上的实验表明，与现有VLA基线相比，AFIL持续提高了任务成功率和鲁棒性，证明了其有效性、效率和通用性。

## Abstract
Vision-language-action (VLA) models provide a promising paradigm for scalable robotic manipulation, yet their reliance on success-only behavioral cloning leaves them brittle; lacking corrective training signals, minor execution errors rapidly compound into unrecoverable, out-of-distribution failures. To address this limitation, we propose Adaptive Failure-Informed Learning (AFIL), an end-to-end framework that leverages failure trajectories as adaptive negative guidance for diffusion- and flow-based VLA policies. AFIL uses a pretrained VLA to generate failure rollouts online, avoiding the need for handcrafted failure-mode design or human-in-the-loop recovery. It then jointly trains Dual Action Generators (DAGs) for successful and failed behaviors while sharing a common vision-language backbone, enabling efficient failure-aware policy learning with limited parameter overhead. During sampling, the failure generator adaptively steers action generation away from failure-prone regions and toward more reliable success modes, with guidance strength determined by the per-diffusion-step distance between success and failure distributions. Experiments across in-domain and out-of-domain robotic manipulation tasks, covering both short- and long-horizon settings, show that AFIL consistently improves task success rates and robustness over existing VLA baselines, demonstrating its effectiveness, efficiency, and generality.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，为您生成一份结构化、深入且客观的中文总结。

---

### **论文总结：Failing Forward: Adaptive Failure-Informed Learning for Vision-Language-Action Models**

#### **1. 核心问题与整体含义**
*   **研究背景**：基于大规模预训练的视觉-语言-动作（VLA）模型已成为机器人操作领域的一种强大范式。然而，现有方法主要依赖于**仅包含成功轨迹的行为克隆**进行训练。
*   **核心问题**：这种“仅成功”的训练数据导致模型学习到的行为流形非常狭窄。在部署时，微小的执行误差因缺乏纠错信号而迅速累积，导致模型陷入分布外状态并最终失败，表现出**脆弱的泛化能力和鲁棒性不足**。

#### **2. 方法论**
*   **核心思想**：受图像生成中“负向提示”技术的启发，将**失败轨迹视为一种自适应的负向引导信号**，在推理时动态地将动作生成“推离”失败区域，“拉向”成功区域。
*   **关键技术细节**：
    1.  **双动作生成器（DAG）架构**：
        *   在共享的视觉-语言模型（VLM）骨干网络上，并行训练两个轻量级的动作生成头：一个学习成功动作分布 (`G_succ`)，另一个学习失败动作分布 (`G_fail`)。
        *   **失败数据生成**：无需人工设计故障模式，而是利用预训练的VLA策略在线执行并收集自然发生的失败轨迹，再通过运动规划器进行纠正，形成“失败-纠正”数据对用于训练。
    2.  **自适应失败信息采样**：
        *   在推理（采样）时，同时查询成功和失败生成器。通过修改扩散/流匹配模型的得分估计，引入失败信息的引导。
        *   **关键公式**：引导后的噪声预测为 `ϵ*_FI = ϵ_succ - λ_η (ϵ_fail - ϵ_succ)`，其中 `λ_η` 是自适应的引导强度。
        *   **自适应引导强度**：`λ_η` 并非固定值，而是根据**成功与失败预测噪声向量之间的余弦距离**动态计算：`λ_η ∝ (1 - cos(ϵ_succ, ϵ_fail))`。当成功与失败行为相似（如任务初期）时，引导较弱；当两者差异显著（如出现明显失败模式）时，引导增强，从而更有效地避开失败。

#### **3. 实验设计**
*   **数据集/场景**：
    *   **ManiSkill**：用于领域内和领域外评估。任务包括堆叠立方体、立起木钉、拉动立方体（短视距）以及放置碗勺、放置罐子和香蕉（长视距）等。
    *   **LIBERO**：用于基准评估。包含四个任务套件（LIBERO-Spatial, Object, Goal, 10），测试模型在空间布局、物体身份、任务目标和长视距任务上的泛化能力。
*   **Benchmark**：在LIBERO上遵循标准评估协议（3个随机种子 × 500次推演）。
*   **对比方法**：
    *   **基线模型**：`π0.5`（仅用成功数据训练）、OpenVLA、CoT-VLA、Octo、Diffusion Policy等。
    *   **消融实验**：对比了“仅成功”、“成功+失败纠正数据”、使用固定引导强度的静态FIL以及最终的自适应FIL（AFIL）。

#### **4. 资源与算力**
*   论文中**未明确说明**训练所使用的具体硬件配置（如GPU型号、数量）、训练时长或总的计算开销。仅提及所有实验均基于`π0.5`架构进行，且AFIL方法是模型无关的，可应用于其他扩散或流匹配VLA策略。

#### **5. 实验数量与充分性**
*   **实验组数**：实验设计较为全面，主要包括：
    1.  **ManiSkill领域内评估**：5个任务（3短视距+2长视距），每个任务150次推演。
    2.  **ManiSkill领域外评估**：3个任务（2短视距+1长视距），每个任务75次推演，测试对未见过的物体、几何形状和杂乱场景的泛化。
    3.  **LIBERO基准评估**：4个任务套件，总计大量推演，与多个SOTA模型对比。
    4.  **消融实验**：包括不同引导强度α（0.5, 1.0, 2.0, 5.0）的对比，验证了自适应机制的有效性。
*   **充分性与客观性**：实验覆盖了**短/长视距、领域内/外、不同任务复杂度**等多个维度，并与当前主流方法进行了公平对比。消融实验系统地验证了各组件（失败数据、引导机制、自适应权重）的贡献。评估指标统一使用**任务成功率**，客观且可量化。

#### **6. 主要结论与发现**
*   AFIL能**一致且显著地**提升VLA策略在各种机器人操作任务上的成功率和鲁棒性。
*   **在长视距和领域外任务上提升尤为明显**，这表明自适应失败引导能有效缓解错误累积问题，并增强对分布偏移的泛化能力。
*   即使对于已经很强的基线模型（如`π0.5`），加入AFIL后性能仍能得到进一步提升。
*   自适应引导机制（基于余弦相似度）比固定强度引导更有效，证明了动态调整引导强度的必要性。

#### **7. 优点**
*   **创新性**：首次将“负向提示”思想系统性地引入到VLA策略的序列动作生成中，为解决模仿学习的脆弱性问题提供了新思路。
*   **高效性**：通过共享骨干网络和轻量级的双生成头设计，以**极小的参数量开销**实现了失败感知学习。
*   **实用性**：失败数据通过预训练策略在线生成，**无需人工设计故障模式或依赖人工实时干预**，易于扩展。
*   **通用性**：方法框架与具体的生成模型（扩散/流匹配）解耦，可广泛应用于各类基于生成的VLA策略。

#### **8. 不足与局限**
*   **对失败数据质量的依赖**：失败数据的多样性和真实性直接影响模型性能。在线生成依赖于初始预训练策略，若该策略的失败模式过于单一或与真实部署差异大，可能限制提升效果。
*   **计算开销**：虽然参数增量小，但在推理时需要**同时运行两个动作生成器并进行额外的相似度计算**，增加了单步推理时间，可能影响实时性要求极高的应用。
*   **评估环境的局限性**：实验主要在模拟器（ManiSkill, LIBERO）中进行。虽然模拟环境能进行大规模可重复测试，但**未在真实物理机器人上进行验证**，其在实际噪声、不确定性下的表现有待检验。
*   **引导机制的理论深度**：论文选择了启发式的余弦相似度作为自适应权重依据，并给出了直观解释，但缺乏更严格的理论分析来证明其相对于其他度量（如KL散度）的最优性。

（完）
