---
title: "OmniNFT: Modality-wise Omni Diffusion Reinforcement for Joint Audio-Video Generation"
title_zh: OmniNFT：面向联合音视频生成的模态级全扩散强化
authors: "Guohui Zhang, XiaoXiao Ma, Jie Huang, Hang Xu, Hu Yu, Siming Fu, Yuming Li, Zeyue Xue, Lin Song, Haoyang Huang, Nan Duan, Feng Zhao"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12480v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 联合音视频生成的扩散强化学习
tldr: 本研究针对音视频联合生成中模态保真度、跨模态对齐和细粒度同步的挑战，提出OmniNFT框架。该框架采用模态感知的在线扩散强化学习，通过模态优势路由、分层梯度手术和区域损失重加权三个关键技术，解决了多目标优势不一致、多模态梯度失衡和均匀信用分配问题。实验证明，该方法能全面提升音视频生成质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12480v1/fig-001.webp\", \"caption\": \"Figure 5: Overview of OmniNFT. Given paired video and audio prompts, the Omni model first generates joint audio-video samples. Building on these samples, OmniNFT performs three coordinated operations: (i) independent advantages derived from video, audio, and cross-modal rewards are dispatched to their corresponding branches (Modality-wise Advantage Routing); (ii) the audioto-video cross-attention cached from the final sampling steps is converted into a critical-region mask that reweights the RL loss (Region-wise Reweighting); and (iii) during loss backward, the key-value gradients of A2V cross-attention in the shallow audio layers are partially detached, while all other gradient flows remain intact (Layer-wise Gradient Surgery).\", \"page\": 6, \"index\": 1, \"width\": 820, \"height\": 385}, {\"url\": \"assets/figures/arxiv/2605.12480v1/fig-002.webp\", \"caption\": \"Figure 2: Advantage inconsistency and asymmetric audio-video interaction. (a) Video and audio advantages are weakly correlated: roughly half of the samples receive opposing rewards across the two modalities. (b) Blocking the V2A KV in mid-layers collapses AV synchronization to 0.41× baseline, whereas (d) the symmetric A2V ablation causes a mild degradation when applied to the later blocks. (c,e) Layer-wise gradient norms of cross-attention show that audio-video interaction is concentrated in the middle and later transformer blocks (AV Sync Zone). On the audio branch, gradients from video KV (A2V) disturb the update direction of the audio shallow blocks in (c).\", \"page\": 3, \"index\": 2, \"width\": 826, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2605.12480v1/fig-003.webp\", \"caption\": \"Table 1: Main results on JavisBench (Liu et al., 2025c). Best results in green , second-best underlined. VQ: Visual Quality, AQ: Audio Quality. (↑: higher is better; ↓: lower is better).\", \"page\": 8, \"index\": 3, \"width\": 830, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2605.12480v1/fig-004.webp\", \"caption\": \"Figure 7: Qualitative examples of joint audio-video generation by OmniNFT. The four cases illustrate improvements across different aspects: enhanced visual quality, improved audio fidelity, better speech-lip synchronization, and coherent multi-speaker scenes.\", \"page\": 9, \"index\": 4, \"width\": 820, \"height\": 241}]"
motivation: 现有音视频联合生成方法在模态保真度、跨模态对齐和细粒度同步方面存在不足，而强化学习在多目标多模态场景中的应用面临挑战。
method: 提出OmniNFT框架，包含模态优势路由、分层梯度手术和区域损失重加权三个创新模块。
result: 在JavisBench和VBench上的实验表明，OmniNFT在音视频感知质量、跨模态对齐和同步方面均取得全面改进。
conclusion: OmniNFT通过模态感知的强化学习框架，显著提升了音视频生成的质量、对齐和同步性能。
---

## 摘要
近年来，联合音视频生成领域取得了显著进展，然而实际应用要求各模态具备高保真度、跨模态对齐以及细粒度同步。强化学习（RL）提供了一个有前景的范式，但其在多目标、多模态联合音视频生成中的应用尚未得到探索。值得注意的是，我们的深入分析首次揭示了在此领域应用RL的主要障碍源于：（i）多目标优势不一致，即多模态输出的优势在组内并不总是一致；（ii）多模态梯度失衡，即视频分支梯度渗入负责模态内生成的浅层音频层；（iii）均匀信用分配，即细粒度跨模态对齐区域未能获得有效探索。这些缺陷表明，采用单一全局优势的原始RL微调策略往往导致次优结果。为应对这些挑战，我们提出了OmniNFT，一种新颖的模态感知在线扩散RL框架，具有三大关键创新：（1）模态优势路由，将独立的各奖励优势路由至其相应的模态生成分支；（2）分层梯度手术，选择性切断浅层音频层上的视频分支梯度，同时保留跨模态交互层的梯度；（3）区域损失重加权，将策略优化调制至与音视频同步及细粒度对齐相关的关键区域。基于LTX-2骨干网络在JavisBench和VBench上的大量实验表明，OmniNFT在音频和视频感知质量、跨模态对齐以及音视频同步方面实现了全面改进。

## Abstract
Recent advances in joint audio-video generation have been remarkable, yet real-world applications demand strong per-modality fidelity, cross-modal alignment, and fine-grained synchronization. Reinforcement Learning (RL) offers a promising paradigm, but its extension to multi-objective and multi-modal joint audio-video generation remains unexplored. Notably, our in-depth analysis first reveals that the primary obstacles to applying RL in this stem from: (i) multi-objective advantages inconsistency, where the advantages of multimodal outputs are not always consistent within a group; (ii) multi-modal gradients imbalance, where video-branch gradients leak into shallow audio layers responsible for intra-modal generation; (iii) uniform credit assignment, where fine-grained cross-modal alignment regions fail to get efficient exploration. These shortcomings suggest that vanilla RL fine-tuning strategy with a single global advantage often leads to suboptimal results. To address these challenges, we propose OmniNFT, a novel modality-aware online diffusion RL framework with three key innovations: (1) Modality-wise advantage routing, which routes independent per-reward advantages to their respective modality generation branches. (2) Layer-wise gradient surgery, which selectively detaches video-branch gradients on shallow audio layers while retaining those for cross-modal interaction layers. (3) Region-wise loss reweighting, which modulates policy optimization toward critical regions related to audio-video synchronization and fine-grained alignment. Extensive experiments on JavisBench and VBench with the LTX-2 backbone demonstrate that OmniNFT achieves comprehensive improvements in audio and video perceptual quality, cross-modal alignment, and audio-video synchronization.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，为您生成一份结构化的详细总结。

---

### **论文总结：OmniNFT: Modality-wise Omni Diffusion Reinforcement for Joint Audio-Video Generation**

#### **1. 核心问题与整体含义**
*   **研究背景**：近年来，联合音视频生成技术发展迅速，但要在实际应用中落地，必须同时满足**高模态保真度、强跨模态语义一致性和细粒度音视频同步**这三个关键要求。现有模型难以同时优化这些多目标。
*   **核心问题**：论文首次系统性地探索将强化学习（RL）应用于多模态（音视频）联合生成任务，并识别出三个关键的优化瓶颈：
    1.  **多目标优势不一致**：一个生成样本的音频奖励和视频奖励经常是冲突的（例如，视频质量高但音频质量差），使用单一的全局优势信号会混淆学习目标。
    2.  **多模态梯度失衡**：在双流模型中，视频分支的梯度会“泄漏”到音频分支的浅层网络，干扰音频本身的模态内生成过程。
    3.  **均匀信用分配**：音视频同步和细粒度对齐通常只与视频中的特定关键区域（如发声的嘴部）强相关，而均匀的损失更新无法有效聚焦于这些区域。

#### **2. 方法论**
*   **核心思想**：提出 **OmniNFT**，一个模态感知的在线扩散模型强化学习微调框架，旨在通过细粒度的信用分配策略，解决上述三个瓶颈，实现音视频质量的协同优化。
*   **关键技术细节**：
    *   **模态优势路由**：
        *   为视频质量、音频质量和跨模态同步奖励分别计算独立的优势函数。
        *   将视频优势路由至视频分支，音频优势路由至音频分支，同步优势则同时广播给两个分支。这样确保了每个分支主要接收与其直接相关的奖励信号，避免了优势冲突。
    *   **分层梯度手术**：
        *   基于观察（浅层负责模态内生成，深层负责跨模态交互），在音频分支的**浅层**，对流向音频到视频交叉注意力中Key和Value的梯度进行**部分断开**。
        *   这阻止了视频梯度对音频浅层参数更新的过度干扰，同时保留了深层跨模态交互所需的梯度。
    *   **区域损失重加权**：
        *   利用模型内在的、从视频到音频的交叉注意力图，作为识别视频中与音频同步相关的**关键区域**的代理。
        *   在训练损失中，为这些关键区域的视频Token分配更高的权重，引导模型将更多的优化能力集中在提升这些区域的同步质量上。

#### **3. 实验设计**
*   **骨干模型**：使用 **LTX-2**（一个19B参数的高效音视频联合生成模型）作为基础模型进行微调。
*   **评估基准与数据集**：
    *   **主评估基准**：**JavisBench**，一个全面的联合音视频生成评估套件。
    *   **辅助评估基准**：**VBench**，用于额外评估生成视频的质量。
*   **对比方法**：
    *   **基线模型**：包括TempoTkn, TPoS, ReWaS, See&Hear, FoleyCrafter, MMAudio, JavisDiT, UniVerse-1, JavisDiT++ 以及最强的基线 **LTX-2**。
    *   **RL基线**：**GDPO**（一种用于视觉生成的RL方法），用于对比验证OmniNFT框架的有效性。
*   **评估维度与指标**：
    *   **音视频质量**：视觉质量、音频质量。
    *   **文本一致性**：文本-视频/音频对齐。
    *   **音视频一致性**：跨模态语义对齐。
    *   **音视频同步性**：细粒度的时间对齐。

#### **4. 资源与算力**
*   论文中**未明确说明**训练所使用的具体GPU型号、数量及总训练时长。仅在第9页的消融实验表格中提及了“Time ↓”列，显示完整OmniNFT训练时间约为24.1小时（相较于原始LTX-2+RL的23.9小时），但未指明硬件配置。

#### **5. 实验数量与充分性**
*   **实验数量与设计**：
    1.  **主实验**：在JavisBench和VBench上与多个SOTA方法进行综合对比，覆盖了4个评估维度的近10个指标，结果全面。
    2.  **消融实验**：系统地、逐步地验证了三个核心组件（优势路由、梯度手术、区域重加权）各自的贡献，证明了每个设计的必要性。
    3.  **超参数分析**：研究了梯度手术的层选择（浅层 vs 深层）和区域重加权系数λ的影响，结果表明默认设置（在浅层手术，λ=1.5）效果最佳，与论文的动机分析相符。
    4.  **定性分析**：提供了多个生成样例，直观展示了在视觉质量、音频保真度、唇语同步和多说话人场景一致性等方面的提升。
*   **充分性与客观性评估**：
    *   **充分性**：实验设计较为全面，包含了定量、定性、消融和超参分析，能够有力地支撑论文的论点。
    *   **客观性**：使用了公开的、公认的评估基准（JavisBench, VBench）和指标，对比了当前主流方法，结果具有可比性。
    *   **公平性**：所有对比均在相同的数据集和评估协议下进行。以LTX-2为共同骨干进行RL微调的比较（vs GDPO）是公平的。

#### **6. 主要结论与发现**
*   OmniNFT能够**全面且显著地提升**联合音视频生成的各项性能。
*   **定量结果**：在LTX-2基础上，视觉质量提升63.2%，音频质量提升10.0%，同步性指标（DeSync）降低52.7%，全面超越了GDPO等基线方法。
*   **关键发现**：
    1.  论文诊断的三个优化瓶颈（优势不一致、梯度失衡、信用分配均匀）是阻碍RL在音视频生成中有效应用的关键。
    2.  通过模态优势路由、分层梯度手术和区域损失重加权这三个针对性设计，可以有效地解决这些瓶颈。
    3.  模型内部的V2A交叉注意力图可以作为定位音视频同步关键区域的可靠内在指标。

#### **7. 优点**
*   **问题诊断精准**：首次系统分析了RL应用于多模态生成时的独特挑战，而非简单套用现有RL方法。
*   **方法设计精巧**：三个组件分别针对三个已识别的瓶颈，逻辑清晰，且相互协同。
*   **高效且轻量**：所提方法主要是在训练流程和损失计算上做修改，没有引入额外的可学习模块或显著增加推理开销。
*   **实验验证扎实**：通过充分的消融实验和超参分析，清晰地展示了每个组件的贡献和设计选择的依据。

#### **8. 不足与局限**
*   **文本-视频对齐未改善**：实验结果显示，在文本-视频一致性指标上，OmniNFT和GDPO均未带来提升，表明优化多模态目标时，文本对齐仍是一个挑战。
*   **架构依赖性**：方法基于特定的“双流+交叉注意力”的联合生成模型架构（如LTX-2）。其有效性在其他架构（如完全统一的单流模型）上的普适性有待验证。
*   **计算开销**：虽然推理阶段无额外开销，但训练过程涉及多奖励模型评估、注意力图缓存和复杂的损失计算，训练成本高于标准微调。
*   **泛化性风险**：区域重加权依赖于V2A注意力图的质量。在训练数据分布外或极端场景下，注意力图可能无法准确识别关键区域，从而影响方法效果。
*   **奖励函数依赖**：方法的性能提升高度依赖于所使用的奖励模型的质量和多样性。如果奖励模型存在偏差或未涵盖某些重要维度，生成模型的优化方向也会受限。

（完）
