---
title: A decoupled diffusion planner that adapts to changing cost limits by using cost-conditioned generation for safety and reward gradients for performance
title_zh: 一种通过成本条件生成实现安全适应与奖励梯度提升性能的解耦扩散规划器
authors: "Rufeng Chen, Zhaofan Zhang, Zhejiang Yang, Hechang Chen, Sihong Xie"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02777v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 用于自适应安全轨迹生成的解耦扩散规划器
tldr: 离线安全强化学习中，策略需适应动态变化的安全预算。现有扩散规划器将奖励与安全约束视为竞争目标，导致安全合规性不足。本文提出安全解耦引导扩散方法，将自适应安全轨迹生成重新解释为从约束轨迹分布中采样，其中预算限制轨迹区域，奖励在该区域内塑造偏好。该方法通过成本条件化无分类器引导确保轨迹满足指定预算，同时使用奖励梯度引导优化回报，并引入可行轨迹重标注抑制奖励引导导致的成本漂移。实验表明，该方法在DSRL基准上实现了最强的安全合规性和最高的奖励性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02777v1/fig-001.webp\", \"caption\": \"Table 1: Normalized DSRL Liu et al. [2023a] benchmark results. ↑ means the higher the better. ↓ means the lower the better. Each value is averaged over 20 evaluation episodes and 3 seeds. Gray: Unsafe agents. Bold: Safe agents whose normalized cost is smaller than 1. Blue: Safe agents with the highest reward.\", \"page\": 7, \"index\": 1, \"width\": 824, \"height\": 720}, {\"url\": \"assets/figures/arxiv/2605.02777v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of Safe Decoupled Guidance Diffusion (SDGD). (a) SDGD learns a cost limit (l) conditioned diffuser to model the safe trajectory distribution from offline data, while an FTR-trained reward classifier predicts rewards. Feasible Trajectory Relabeling (FTR) modifies reward single by replacing the raw reward R(τ) with a new reward target R̂(τ), thereby reshaping reward guidance away from infeasible regions and reducing reward-induced cost drift. (b) SDGD decouples safety and performance: classifier-free guidance enforces cost limits, while reward-gradient guidance refines samples toward high-return trajectories within the safe region.\", \"page\": 3, \"index\": 2, \"width\": 826, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2605.02777v1/fig-003.webp\", \"caption\": \"Figure 4: Performance comparison across different cost limits (10, 20, 30) in four tasks.\", \"page\": 8, \"index\": 3, \"width\": 826, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2605.02777v1/fig-004.webp\", \"caption\": \"Figure 6: Ablation study on the guidance components. The SDGD is compared against two variants: one without Classifier Guidance (w/o CG) and another without Classifier-Free Guidance (w/o CFG).\", \"page\": 9, \"index\": 4, \"width\": 816, \"height\": 261}]"
motivation: 离线安全强化学习需要策略在部署时适应变化的安全预算，而现有基于扩散的规划器将奖励提升与约束满足视为竞争目标，导致安全合规性不可靠。
method: 提出SDGD方法，通过成本条件化的无分类器引导确保轨迹满足安全预算，并结合奖励梯度引导提升回报，同时引入可行轨迹重标注来抑制奖励引导导致的成本漂移。
result: "在DSRL基准测试中，SDGD在94.7%的任务上满足约束，并在21个任务上取得了安全方法中最高的奖励。"
conclusion: 该方法通过解耦成本条件引导与奖励梯度引导，在满足动态安全预算的同时实现了高性能，为离线安全强化学习中的自适应轨迹规划提供了有效解决方案。
---

## 摘要
离线安全强化学习通常要求策略在部署时能够适应不同回合间或单个回合内变化的安全预算。虽然基于扩散的规划器能够实现灵活的轨迹生成，但现有的引导方案往往将奖励提升与约束满足视为相互竞争的梯度目标，这可能导致在成本限制下安全合规性不可靠。我们将自适应安全轨迹生成重新解释为从受约束的轨迹分布中进行采样，其中预算限制了轨迹区域，而奖励则在该区域内塑造偏好。这一视角催生了安全解耦引导扩散（SDGD），它通过成本限制对无分类器引导进行条件化，使采样偏向满足指定限制的轨迹，同时使用奖励梯度引导来优化轨迹以获得更高回报。由于直接奖励引导在提高回报的同时也可能将样本导向具有更高累积成本的轨迹，我们引入了可行轨迹重标注（FTR）来重塑奖励目标并抑制此类方向。我们进一步提供了一阶采样时间分析，表明在前缀恢复对齐条件下，FTR能够抑制奖励引起的成本漂移。在DSRL基准上的广泛评估表明，SDGD在基线中实现了最强的安全合规性，在94.7%的任务（36/38）上满足约束，同时在21项任务上获得了安全方法中最高的奖励。

## Abstract
Offline safe reinforcement learning often requires policies to adapt at deployment time to safety budgets that vary across episodes or change within a single episode. While diffusion-based planners enable flexible trajectory generation, existing guidance schemes often treat reward improvement and constraint satisfaction as competing gradient objectives, which can lead to unreliable safety compliance under cost limits. We reinterpret adaptive safe trajectory generation as sampling from a constrained trajectory distribution, where the budget restricts the trajectory region, and reward shapes preferences within that region. This perspective motivates Safe Decoupled Guidance Diffusion (SDGD), which conditions classifier-free guidance on the cost limit to bias sampling toward trajectories satisfying the specified limit, while using reward-gradient guidance to refine trajectories for higher return. Because direct reward guidance can increase return while also steering samples toward trajectories with higher cumulative cost, we introduce Feasible Trajectory Relabeling (FTR) to reshape reward targets and discourage such directions. We further provide a first-order sampling-time analysis showing that FTR suppresses reward-induced cost drift under a prefix-restorative alignment condition. Extensive evaluations on the DSRL benchmark show that SDGD achieves the strongest safety compliance among baselines, satisfying the constraint on 94.7% of tasks (36/38), while obtaining the highest reward among safe methods on 21 tasks.

---

## 论文详细总结（自动生成）

好的，我将基于您提供的论文内容，按照您要求的要点生成一份详细的结构化中文总结。

### 1. 核心问题与整体含义
*   **研究背景**：在安全关键的机器人、自动驾驶等领域，离线安全强化学习旨在从固定的离线数据集中学习策略，以在满足安全约束的同时优化任务性能。
*   **核心问题**：现有方法通常假设安全预算是固定的。然而，在真实部署中，安全预算（成本限制）可能因任务、用户或环境变化而动态改变。这要求规划器能够在**不重新训练**的情况下，在部署时自适应地响应变化的成本限制。
*   **现有方法的局限**：
    *   **自回归模型**（如约束条件策略、Transformer）：逐步生成轨迹，容易因误差累积导致轨迹级成本控制失败。
    *   **基于扩散的规划器**：通常依赖**分类器引导**，将奖励和安全约束视为相互竞争的梯度目标进行优化。然而，在扩散过程中，对噪声轨迹的成本预测往往不可靠（如图1c所示），导致安全引导失效，无法可靠满足约束。

### 2. 提出的方法论：SDGD
*   **核心思想**：将自适应安全轨迹生成重新解释为从**受约束的轨迹分布**中采样。**成本限制**定义了可采样轨迹的**区域（支持集）**，而**奖励**则在该区域内塑造偏好。基于此，提出将安全与性能的引导机制**解耦**。
*   **关键技术**：
    1.  **安全解耦引导扩散（SDGD）**：
        *   **安全引导（成本限制条件化）**：使用**无分类器引导**，将成本限制 `l` 作为条件输入扩散模型。这直接使去噪过程偏向于生成满足成本限制 `C(τ) ≤ l` 的轨迹，避免了使用不可靠的成本分类器梯度。
        *   **性能引导（奖励梯度优化）**：在去噪过程中，额外添加一个**奖励预测器 `Rϕ` 的梯度** `λ∇Rϕ(τ)`，以在安全区域内进一步优化轨迹的回报。
    2.  **可行轨迹重标注（FTR）**：
        *   **问题**：直接使用原始回报 `R(τ)` 进行梯度引导，可能会同时增加累积成本（即奖励与成本梯度正相关），导致“奖励引起的成本漂移”。
        *   **解决方案**：在训练奖励预测器时，对奖励标签进行重标注。对于在**可执行前缀**（即规划中即将执行的前几步）中产生成本的轨迹，将其原始回报 `R(τ)` 减去一个惩罚项 `rus`（`rus < 0`），得到新目标 `R̂(τ)`。这使奖励预测器学会避开那些依赖早期危险行为来获取高回报的轨迹，从而重塑奖励梯度场，使其与成本增加方向解耦。
        *   **理论分析**：论文提供了命题1，在一阶扰动近似和“前缀恢复对齐条件”下，证明了FTR能有效抑制相对于纯成本条件采样器的成本漂移。

### 3. 实验设计
*   **基准测试**：**DSRL基准**，包含来自Safety-Gymnasium、Bullet-Safety-Gym和MetaDrive的**38个任务**。
*   **评估指标**：归一化奖励（越高越好）和归一化成本（越低越好）。定义安全阈值：归一化成本 ≤ 1 为安全。
*   **对比方法**：与7种先进的离线安全RL方法对比，涵盖不同技术路线：
    *   **分布修正**：COptiDICE
    *   **惩罚方法**：CPQ
    *   **生成模型/规划器**：CDT (Decision Transformer), TREBI (扩散+分类器引导), FISOR (可行性引导扩散)
    *   **自适应方法**：CAPS (策略切换), CCAC (约束条件演员-评论家)

### 4. 资源与算力
*   论文在“实验”章节提到，SDGD在**单张NVIDIA A6000 GPU**上实现了 **2.1 Hz的决策频率**，并指出这比TREBI (1.1 Hz) 和 HD (1.0 Hz) 等先前方法更快。
*   **未明确说明**：模型训练所使用的GPU数量、总训练时长、具体超参数搜索成本等详细信息。

### 5. 实验数量与充分性
*   **主实验**：在DSRL全部38个任务上评估，每个任务运行20个评估回合和3个随机种子，结果详实（见表1）。
*   **消融与分析实验**：
    1.  **不同成本限制下的适应性**：在4个代表性任务上测试了成本限制为10、20、30的情况（图4）。
    2.  **动态时变成本限制**：在单个回合内测试成本限制从1变为3再变为10的场景（图5）。
    3.  **引导组件消融**：分别移除奖励梯度引导（w/o CG）和成本条件无分类器引导（w/o CFG）（图6）。
    4.  **引导角色互换测试**：将分类器引导用于成本、无分类器引导用于奖励的“反向”基线，结果安全性崩溃（表2）。
    5.  **超参数敏感性分析**：测试了无分类器引导权重 `w` 和奖励引导尺度 `λ` 在不同组合下的稳定性（表3）。
    6.  **FTR关键参数分析**：分析了不同可行前缀长度 `f` 和奖励引导尺度的影响（图7）。
*   **评价**：实验设计**较为充分和全面**。不仅评估了最终性能，还通过消融实验验证了各核心组件的必要性，测试了方法对不同预算和动态场景的适应性，并进行了超参数敏感性分析。对比基线涵盖了当前主流方法，评估标准统一（使用归一化指标和统一的安全阈值），**客观且公平**。

### 6. 主要结论与发现
*   **安全性**：SDGD在**36/38个任务**（94.7%）上满足安全约束，是所比较方法中**安全合规性最强**的。
*   **性能**：在满足约束的方法中，SDGD在**21个任务**上获得了**最高的奖励**，实现了出色的安全-性能权衡（见图3帕累托前沿）。
*   **适应性**：**单个训练好的模型**能够成功适应**不同**的部署时成本限制，以及**在单个回合内动态变化**的成本限制，无需重新训练。
*   **有效性验证**：消融实验证实了**解耦设计**（安全与性能引导分离）和**FTR**的必要性。互换引导角色会导致安全性失败，证明了该设计并非任意。

### 7. 优点
*   **概念创新**：提出了对安全轨迹生成问题的新颖解读（约束分布采样），并据此设计了**解耦的引导架构**，清晰区分了安全（支持集限制）和性能（内部优化）的角色。
*   **技术有效**：
    *   利用**无分类器引导**实现可靠的成本条件化，避免了传统分类器引导的信号不可靠问题。
    *   提出**FTR**，巧妙地通过修改奖励预测器的训练目标来对齐安全与性能优化，并有初步的理论分析支持。
*   **实验扎实**：在大型基准测试上进行了全面评估，不仅展示了SOTA性能，还通过丰富的分析实验深入验证了方法各个方面的有效性和鲁棒性。

### 8. 不足与局限
*   **计算成本**：虽然推理速度优于部分基线，但扩散模型通常比非生成式方法（如策略网络）计算量更大。论文未详细讨论训练成本。
*   **理论分析的近似性**：对FTR抑制成本漂移的理论分析（命题1）基于一阶扰动近似，并依赖于“前缀恢复对齐条件”等假设，在实际复杂环境中的普适性有待进一步验证。
*   **应用范围**：实验均在**仿真环境**（DSRL基准）中进行，在更复杂、噪声更大的真实物理系统（如真实机器人、车辆）中的有效性尚未得到验证。
*   **FTR参数选择**：可行前缀长度 `f` 和惩罚值 `rus` 是需要设定的超参数。虽然论文给出了 `rus` 的理论下界，但 `f` 的选择可能依赖于任务特性，其泛化性需要更多研究。
*   **离线数据依赖**：作为离线RL方法，其性能高度依赖于离线数据集的质量和覆盖范围。在数据集中安全可行轨迹稀缺或分布有偏的情况下，方法的性能可能会下降。

（完）
