---
title: "Reinforce Adjoint Matching: Scaling RL Post-Training of Diffusion and Flow-Matching Models"
title_zh: 强化伴随匹配：扩散与流匹配模型的规模化强化学习后训练
authors: "Andreas Bergmeister, Stefanie Jegelka, Nikolas Nüsken, Carles Domingo-Enrich, Jakiw Pidstrigach"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10759v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 扩散与流匹配模型的强化学习后训练
tldr: 扩散和流匹配模型的RL后训练通常依赖高成本的SDE展开或奖励梯度，破坏了预训练的回归结构。本文提出强化伴随匹配（RAM），通过KL正则化奖励最大化理论推导出最优生成过程，并利用伴随匹配最优条件和REINFORCE恒等式，设计了一种修正预训练目标的一致性损失。该方法无需SDE展开或奖励梯度，简单且可扩展，在图像生成任务中显著提升了奖励并大幅减少了训练步骤。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10759v1/fig-001.webp\", \"caption\": \"Table 2 Stable Diffusion 3.5 Medium (SD3.5M) post-training results. We report training rewards on held-out test prompts and image-quality metrics on DrawBench prompts. Higher is better for all metrics. Within each task, the best results are shown in bold and second-best results are underlined. † denotes results obtained by retraining the corresponding baseline.\", \"page\": 10, \"index\": 1, \"width\": 890, \"height\": 544}, {\"url\": \"assets/figures/arxiv/2605.10759v1/fig-002.webp\", \"caption\": \"Figure 3 Path-cost-corrected estimators on a 2D toy. Left: post-trained densities match the tilted target for every estimator. Right: regression loss at convergence, equal to the variance of the control-space target.\", \"page\": 8, \"index\": 2, \"width\": 756, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2605.10759v1/fig-003.webp\", \"caption\": \"Figure 4 Training-reward curves comparing RAM with DiffusionNFT and AWM, two methods designed for training efficiency. The lighter companion curves show DiffusionNFT and AWM re-evaluated with CFG, which lowers their task reward. RAM, by contrast, remains compatible with CFG after post-training and is plotted with CFG.\", \"page\": 9, \"index\": 3, \"width\": 977, \"height\": 232}, {\"url\": \"assets/figures/arxiv/2605.10759v1/fig-004.webp\", \"caption\": \"Figure 2 RAM training. We draw a clean endpoint with an ODE sampler, evaluate its reward, and noise it as in pretraining to obtain training states. This avoids SDE rollouts, and reusing each endpoint across many noise draws amortizes the cost of sampling and reward evaluation.\", \"page\": 5, \"index\": 4, \"width\": 956, \"height\": 415}]"
motivation: 现有RL后训练方法依赖高成本计算，破坏了预训练的回归结构，需要一种更高效的替代方案。
method: 提出强化伴随匹配（RAM），通过一致性损失修正预训练目标，无需SDE展开或奖励梯度计算。
result: 在Stable Diffusion 3.5M上，RAM在可组合性、文本渲染和人类偏好方面达到最高奖励，训练步骤减少多达50倍。
conclusion: 该方法为扩散和流匹配模型的RL后训练提供了一种高效且可扩展的新范式。
---

## 摘要
扩散与流匹配模型之所以能够规模化，是因为其预训练本质上是监督回归：一个干净样本通过解析方式添加噪声，模型则针对闭式目标进行回归。强化学习后训练则使模型与奖励对齐。在图像生成中，这使样本能够正确组合物体、清晰渲染文本并符合人类偏好。现有方法依赖于昂贵的随机微分方程展开、奖励梯度或代理损失，牺牲了预训练的回归结构。我们证明该结构可扩展至强化学习后训练。在KL正则化奖励最大化下，最优生成过程将干净端点分布向高奖励样本倾斜，同时保持加噪规律不变。结合伴随匹配最优性条件与REINFORCE恒等式，我们推导出强化伴随匹配：一种通过奖励修正预训练目标的一致性损失。在每一步，我们从当前模型中采样一个干净端点，评估其奖励，按预训练方式添加噪声，并进行回归。无需随机微分方程展开、反向伴随扫描或奖励梯度。与预训练目标类似，RAM方法简单且可扩展。在Stable Diffusion 3.5M上，RAM在组合性、文本渲染和人类偏好方面实现了最高奖励，仅需最多50倍更少的训练步数即可达到Flow-GRPO的峰值奖励。

## Abstract
Diffusion and flow-matching models scale because pretraining is supervised regression: a clean sample is noised analytically, and a model regresses against a closed-form target. RL post-training aligns the model with a reward. In image generation, this makes samples compose objects correctly, render text legibly, and match human preferences. Existing methods rely on costly SDE rollouts, reward gradients, or surrogate losses, sacrificing pretraining's regression structure. We show that the structure extends to RL post-training. Under KL-regularized reward maximization, the optimal generative process tilts the clean-endpoint distribution towards samples with higher reward and leaves the noising law unchanged. Combining this with the adjoint-matching optimality condition and a REINFORCE identity, we derive Reinforce Adjoint Matching (RAM): a consistency loss that corrects the pretraining target with the reward. At each step, we draw a clean endpoint from the current model, evaluate its reward, noise it as in pretraining, and regress. No SDE rollouts, backward adjoint sweeps, or reward gradients are required. Like the pretraining objective, RAM is simple and scales. On Stable Diffusion 3.5M, RAM achieves the highest reward on composability, text rendering, and human preference, reaching Flow-GRPO's peak reward in up to $50\times$ fewer training steps.

---

## 论文详细总结（自动生成）

### 论文总结：Reinforce Adjoint Matching (RAM)

#### 1. 核心问题与整体含义
*   **研究动机**：扩散与流匹配模型的强化学习后训练（如对齐人类偏好、提升组合生成能力）通常依赖于计算成本高昂的**随机微分方程展开**或需要**奖励梯度**，这破坏了预训练阶段高效、简单的**监督回归结构**。现有方法在可扩展性上存在瓶颈。
*   **核心目标**：提出一种新的后训练方法，旨在**保持预训练的回归范式**，避免昂贵的SDE模拟和梯度计算，从而实现**高效、可扩展**的奖励对齐。

#### 2. 提出的方法论：强化伴随匹配
*   **核心思想**：
    1.  **理论洞察**：从KL正则化奖励最大化的随机最优控制问题出发，证明**最优的后训练生成过程**仅会改变**干净端点（即最终生成图像）的分布**，使其向高奖励样本倾斜，而**给定端点后的加噪过程（前向路径）与预训练时完全一致**。
    2.  **算法设计**：结合“伴随匹配”最优性条件和REINFORCE（得分函数）估计器，推导出一个**闭式的一致性损失**。该损失直接修正预训练的回归目标，将奖励信息融入其中。
*   **关键技术细节**：
    *   **训练流程**：在每一步训练中，首先使用任何ODE采样器从当前模型中**采样一个干净图像（端点）**，然后**评估其奖励值**，接着**像预训练一样对该端点进行解析加噪**，得到带噪状态，最后让模型去拟合一个由奖励修正后的速度场目标。
    *   **关键优势**：无需进行完整的SDE展开、不需要通过采样链反向传播奖励梯度、也不需要求解伴随ODE。每个采样的端点可以被**重复加噪多次**，分摊了采样和奖励评估的成本。
    *   **损失函数**：核心是回归损失，目标是让模型预测的速度场接近一个目标：`v_ref + reward * (ε - x0 - v_θ)`，其中`v_ref`是预训练模型的速度场，`(ε - x0)`是预训练的目标。

#### 3. 实验设计
*   **基础模型**：Stable Diffusion 3.5 Medium (SD3.5M)，一个25亿参数的整流流Transformer模型。
*   **任务与数据集**：
    1.  **组合图像生成**：使用**GenEval**基准，评估模型对物体、属性和空间关系的组合理解能力。
    2.  **视觉文本渲染**：使用**OCR**编辑距离奖励，评估生成图像中文本的清晰度和准确性。
    3.  **人类偏好对齐**：使用**PickScore**模型作为奖励函数，使生成图像更符合人类审美偏好。
*   **对比方法**：
    *   **Flow-GRPO**：基于策略梯度、需要SDE展开的强基线。
    *   **DiffusionNFT** 和 **AWM**：两种同样旨在提升训练效率、采用类似“端点采样+加噪”范式的最新方法。
*   **评估指标**：
    *   **主指标**：各任务对应的奖励分数（GenEval准确率、OCR准确率、PickScore）。
    *   **防“奖励黑客”指标**：在独立的DrawBench提示集上，使用多种图像质量评估模型（如Aesthetic, ImageReward, HPSv2）检查通用质量是否下降。

#### 4. 资源与算力
*   **硬件**：在4张**NVIDIA H100 (80GB)** GPU上进行训练。
*   **配置**：所有方法均使用**LoRA**进行高效微调（秩r=32）。
*   **训练成本**：论文报告了**每步训练的GPU小时数**（墙钟时间×4张GPU）。RAM的每步成本与基线方法相近（例如，在GenEval任务上：RAM为0.666 GPU-小时，Flow-GRPO为0.701 GPU-小时）。因此，RAM在**训练步数上的大幅减少直接转化为总训练时间（算力成本）的显著降低**。

#### 5. 实验数量与充分性
*   **实验组数**：在**三个核心任务**上分别进行了完整的训练和评估。此外，还包括：
    *   **2D玩具示例实验**：用于验证理论并比较不同路径成本估计器的方差。
    *   **消融与分析**：探讨了方法对**分类器无关引导的兼容性**（RAM兼容，而部分基线不兼容），并分析了不同奖励归一化策略的影响。
*   **充分性与公平性**：
    *   **充分性**：实验覆盖了后训练的主要应用场景（组合、文本、偏好），并与当前最有效的多种方法进行了对比，结果具有说服力。
    *   **公平性**：论文强调在对比时保持了**一致的实验设置**，如相同的每步提示词数量（48）、采样数量等，并对AWM等基线因设置不同导致的分数差异进行了说明。

#### 6. 主要结论与发现
*   **高效性**：RAM在达到可比或更高奖励的前提下，**训练步数比Flow-GRPO减少34-50倍**，比DiffusionNFT和AWM也更快收敛。
*   **有效性**：在三个任务上，RAM均取得了**最高的测试奖励分数**。
*   **质量保持**：在追求高任务奖励的同时，RAM**未出现明显的“奖励黑客”现象**，在独立的图像质量评估中保持良好表现。
*   **兼容性**：RAM后训练得到的模型**仍可使用分类器无关引导**来进一步调节生成效果，而部分基线方法会丧失此能力。

#### 7. 优点
*   **方法简洁**：延续了预训练的回归范式，算法实现简单直观。
*   **理论扎实**：基于随机最优控制理论进行推导，为方法提供了坚实的数学基础。
*   **效率卓越**：避免了SDE展开和伴随梯度计算，实现了数量级级别的训练加速。
*   **实用性强**：适用于黑盒奖励函数，并与现有采样技术、引导技术兼容。

#### 8. 不足与局限
*   **理论近似**：RAM损失函数推导中**丢弃了“路径成本”修正项**，作者通过理论分析（在特定条件下精确）和实验验证其有效性，但这仍是一个近似。在高奖励强度或复杂场景下，此近似的长期影响有待进一步研究。
*   **计算瓶颈转移**：虽然避免了SDE展开，但**从大模型中采样干净端点**和**评估奖励函数**（尤其是基于神经网络的奖励模型）本身仍是主要计算开销。RAM通过分摊降低了频率，但未消除这些成本。
*   **实验范围**：目前实验集中于**文生图模型（SD3.5M）**，在其他模态（如视频、音频生成）或更大规模模型上的有效性尚未验证。
*   **奖励函数假设**：方法隐含假设奖励函数在单次生成样本上是可评估的。对于依赖成对比较或更复杂反馈形式的奖励，可能需要适配。

（完）
