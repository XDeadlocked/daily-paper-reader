---
title: Decoupled Guidance Diffusion for Adaptive Offline Safe Reinforcement Learning
title_zh: 自适应离线安全强化学习的解耦引导扩散方法
authors: "Rufeng Chen, Zhaofan Zhang, Zhejiang Yang, Hechang Chen, Sihong Xie"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02777v2"
tags: ["query:sr-0501"]
score: 9.0
evidence: 用于安全强化学习轨迹生成的扩散规划器
tldr: "本文针对离线安全强化学习中策略需要适应变化安全预算的问题，提出安全解耦引导扩散（SDGD）方法。该方法将自适应安全轨迹生成重新解释为从约束轨迹分布中采样，通过成本限制条件引导采样满足约束，并使用奖励梯度引导优化轨迹。同时引入可行轨迹重标记（FTR）来抑制奖励诱导的成本漂移。在 DSRL 基准测试中，SDGD 实现了最强的安全合规性，在 94.7% 的任务中满足约束，同时在 21 项任务中获得安全方法中最高的奖励。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02777v2/fig-001.webp\", \"caption\": \"Table 1: Normalized DSRL Liu et al. [2023a] benchmark results. ↑ means the higher the better. ↓ means the lower the better. Each value is averaged over 20 evaluation episodes and 3 seeds. Gray: Unsafe agents. Bold: Safe agents whose normalized cost is smaller than 1. Blue: Safe agents with the highest reward.\", \"page\": 7, \"index\": 1, \"width\": 824, \"height\": 720}, {\"url\": \"assets/figures/arxiv/2605.02777v2/fig-002.webp\", \"caption\": \"Figure 2: Overview of Safe Decoupled Guidance Diffusion (SDGD). (a) SDGD learns a cost limit (l) conditioned diffuser to model the safe trajectory distribution from offline data, while an FTR-trained reward classifier predicts rewards. Feasible Trajectory Relabeling (FTR) modifies reward single by replacing the raw reward R(τ) with a new reward target R̂(τ), thereby reshaping reward guidance away from infeasible regions and reducing reward-induced cost drift. (b) SDGD decouples safety and performance: classifier-free guidance enforces cost limits, while reward-gradient guidance refines samples toward high-return trajectories within the safe region.\", \"page\": 3, \"index\": 2, \"width\": 826, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2605.02777v2/fig-003.webp\", \"caption\": \"Figure 4: Performance comparison across different cost limits (10, 20, 30) in four tasks.\", \"page\": 8, \"index\": 3, \"width\": 826, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2605.02777v2/fig-004.webp\", \"caption\": \"Figure 6: Ablation study on the guidance components. The SDGD is compared against two variants: one without Classifier Guidance (w/o CG) and another without Classifier-Free Guidance (w/o CFG).\", \"page\": 9, \"index\": 4, \"width\": 816, \"height\": 261}]"
motivation: 离线安全强化学习需要策略在部署时适应变化的安全预算，而现有方法将奖励改进和约束满足视为竞争目标，可能导致安全合规性不可靠。
method: 提出安全解耦引导扩散（SDGD），通过成本限制条件引导采样满足约束，并使用奖励梯度引导优化轨迹，同时引入可行轨迹重标记（FTR）来抑制奖励诱导的成本漂移。
result: "SDGD 在 DSRL 基准测试中实现了最强的安全合规性，在 94.7% 的任务中满足约束，同时在 21 项任务中获得安全方法中最高的奖励。"
conclusion: "SDGD 在 DSRL 基准测试中实现了最强的安全合规性，在 94.7% 的任务中满足约束，同时在 21 项任务中获得安全方法中最高的奖励。"
---

## 摘要
离线安全强化学习通常要求策略在部署时能够适应不同回合间或单个回合内变化的安全预算。虽然基于扩散的规划器能够实现灵活的轨迹生成，但现有的引导方案往往将奖励提升与约束满足视为相互竞争的梯度目标，这可能导致在成本限制下安全合规性不可靠。我们将自适应安全轨迹生成重新解释为从受约束的轨迹分布中进行采样，其中预算限制了轨迹区域，而奖励则在该区域内塑造偏好。这一视角催生了安全解耦引导扩散（SDGD），该方法基于成本限制对无分类器引导进行条件化，以偏向满足指定限制的轨迹采样，同时利用奖励梯度引导来优化轨迹以获得更高回报。由于直接奖励引导在提高回报的同时也可能将样本导向具有更高累积成本的轨迹，我们引入了可行轨迹重标注（FTR）来重塑奖励目标并抑制此类方向。我们进一步提供了一阶采样时间分析，表明FTR在前缀恢复对齐条件下能够抑制奖励诱导的成本漂移。在DSRL基准上的广泛评估表明，SDGD在基线中实现了最强的安全合规性，在94.7%的任务（36/38）中满足约束，同时在21项任务上获得了安全方法中最高的奖励。

## Abstract
Offline safe reinforcement learning often requires policies to adapt at deployment time to safety budgets that vary across episodes or change within a single episode. While diffusion-based planners enable flexible trajectory generation, existing guidance schemes often treat reward improvement and constraint satisfaction as competing gradient objectives, which can lead to unreliable safety compliance under cost limits. We reinterpret adaptive safe trajectory generation as sampling from a constrained trajectory distribution, where the budget restricts the trajectory region, and reward shapes preferences within that region. This perspective motivates Safe Decoupled Guidance Diffusion (SDGD), which conditions classifier-free guidance on the cost limit to bias sampling toward trajectories satisfying the specified limit, while using reward-gradient guidance to refine trajectories for higher return. Because direct reward guidance can increase return while also steering samples toward trajectories with higher cumulative cost, we introduce Feasible Trajectory Relabeling (FTR) to reshape reward targets and discourage such directions. We further provide a first-order sampling-time analysis showing that FTR suppresses reward-induced cost drift under a prefix-restorative alignment condition. Extensive evaluations on the DSRL benchmark show that SDGD achieves the strongest safety compliance among baselines, satisfying the constraint on 94.7% of tasks (36/38), while obtaining the highest reward among safe methods on 21 tasks.

---

## 论文详细总结（自动生成）

好的，我将作为资深学术论文分析助手，对您提供的论文《Decoupled Guidance Diffusion for Adaptive Offline Safe Reinforcement Learning》进行结构化、深入、客观的总结。

### **论文总结：自适应离线安全强化学习的解耦引导扩散方法**

#### **1. 核心问题与整体含义**
*   **研究背景**：在机器人、自动驾驶等安全关键领域，离线安全强化学习旨在从固定的离线数据集中学习策略，以在满足安全约束的同时优化任务性能。然而，实际部署时，安全预算（成本限制）可能在不同任务、用户或同一回合内动态变化。
*   **核心问题**：现有方法（如约束条件策略或自回归轨迹模型）在适应这种**动态变化的安全预算**时存在局限。它们通常将奖励提升和约束满足视为需要权衡的竞争性目标，这可能导致在严格的成本限制下安全合规性不可靠。具体问题包括：
    *   **误差累积**：自回归生成方式可能导致轨迹级成本控制困难，小误差会随着推演步长累积。
    *   **不可靠的安全信号**：基于分类器引导的扩散方法依赖于对噪声轨迹的成本预测，但该预测器与真实累积成本的相关性可能很低且不稳定，导致引导时机错误。

#### **2. 方法论：Safe Decoupled Guidance Diffusion (SDGD)**
*   **核心思想**：将自适应安全轨迹生成重新解释为从**受约束的轨迹分布**中采样。**安全预算**定义了可采样轨迹的**区域**（支持集），而**奖励**则在该区域内塑造偏好。基于此，提出将安全与奖励的引导**解耦**处理。
*   **关键技术细节**：
    1.  **约束轨迹后验建模**：目标分布 `p*(τ|l) ∝ p_β(τ) · I[C(τ) ≤ l] · exp(λR(τ))`。其中，指示函数 `I[C(τ) ≤ l]` 是一个非平滑的支持集限制，不适合用梯度直接优化。
    2.  **解耦引导扩散 (SDGD)**：
        *   **安全引导（区域限制）**：使用**无分类器引导**，将成本限制 `l` 作为条件输入扩散模型。这直接使去噪过程偏向于生成满足成本限制的轨迹，避免了使用不可靠的成本分类器梯度。
        *   **奖励引导（区域内优化）**：在采样时，在安全引导的基础上，添加**奖励梯度引导** `λ∇R_ϕ(τ_s)`，以在安全区域内进一步优化轨迹的回报。
    3.  **可行轨迹重标定 (Feasible Trajectory Relabeling, FTR)**：
        *   **问题**：直接使用原始奖励进行梯度引导，可能会同时增加奖励和成本（即奖励诱导的成本漂移），从而破坏安全性。
        *   **解决方案**：在训练奖励预测器 `R_ϕ` 时，不直接拟合原始轨迹回报 `R(τ)`，而是拟合一个重标定后的目标 `R̂(τ) = R(τ) + r_us * h_f(τ)`。其中 `h_f(τ)` 是一个指示函数，判断轨迹的前 `f` 步（即可执行前缀）是否产生成本，`r_us` 是一个负的惩罚项。
        *   **作用**：FTR 通过降低奖励预测器对“高回报但前缀不安全”轨迹的偏好，重塑了奖励梯度场，使其在提升回报时，不易于同时增加累积成本。论文提供了理论分析（命题1），证明在一定的对齐条件下，FTR 能抑制奖励诱导的成本漂移。

#### **3. 实验设计**
*   **基准与数据集**：在 **DSRL 基准**上进行评估，该基准包含来自 Safety-Gymnasium、Bullet-Safety-Gym 和 MetaDrive 的 **38 个任务**。
*   **评估指标**：标准化奖励（越高越好）和标准化成本（越低越好）。定义安全阈值为标准化成本 ≤ 1。
*   **对比方法**：与七种先进的离线安全RL方法对比，涵盖不同技术路线：
    *   **分布校正类**：COptiDICE
    *   **惩罚函数类**：CPQ
    *   **生成模型/序列建模类**：CDT (Decision Transformer), TREBI (扩散模型+分类器引导)
    *   **可行性引导类**：FISOR
    *   **自适应策略类**：CAPS (策略切换), CCAC (约束条件演员-评论家)

#### **4. 资源与算力**
*   论文中未明确说明**训练过程**所使用的具体GPU型号、数量及训练时长。
*   仅在实验部分提及了**推理速度**：SDGD 在 NVIDIA A6000 GPU 上的决策效率为 **2.1 Hz**，优于对比方法 TREBI (1.1 Hz) 和 HD (1.0 Hz)。

#### **5. 实验数量与充分性**
*   **实验数量充足且设计全面**：
    1.  **主实验**：在全部38个DSRL任务上对比所有基线方法，全面评估安全合规性（成本≤1的任务数）和性能（安全方法中的最高奖励任务数）。
    2.  **适应性实验**：
        *   **不同固定预算**：在四个代表性任务上，测试了成本限制为10、20、30时的性能变化。
        *   **动态变化预算**：在单个回合内测试成本限制从1变为3再变为10时，方法的实时适应能力。
    3.  **消融实验**：
        *   **引导组件消融**：验证移除奖励梯度引导(w/o CG)或成本条件引导(w/o CFG)的影响。
        *   **引导角色互换**：测试“用分类器引导处理成本，用无分类器引导处理奖励”的变体，证明所提解耦架构的必要性。
        *   **FTR消融**：分析不同可行前缀长度 `f` 和奖励引导强度 `λ` 对性能的影响。
    4.  **超参数稳定性分析**：展示了SDGD在较宽范围的无分类器引导权重 `w` 和奖励引导尺度 `λ` 下都能保持安全。
*   **客观性与公平性**：
    *   遵循DSRL标准评估协议，使用标准化指标。
    *   对比方法涵盖了近年主流技术路线，具有代表性。
    *   使用了统一的严格成本限制（绝对限制10）进行评估，而非任务原生的宽松限制，这使得比较更具挑战性且更公平。

#### **6. 主要结论与发现**
*   **卓越的安全性**：SDGD在38个任务中的**36个（94.7%）** 满足了安全约束（成本≤1），安全合规性**显著优于所有基线方法**。
*   **优异的性能**：在满足安全约束的方法中，SDGD在**21个任务**上取得了最高的奖励，表明其在安全性和性能间取得了更好的平衡。
*   **强大的适应性**：**单个训练好的模型**能够无需重新训练，即适应不同部署时的成本限制，包括在回合内动态变化的限制。
*   **方法有效性验证**：解耦引导的设计、FTR机制以及成本条件通过无分类器引导实现，均被消融实验证明是关键且有效的。

#### **7. 优点**
*   **概念创新**：将对安全预算的适应问题，重新框架为从“约束分布”中采样，并据此提出**安全与奖励引导的解耦**，视角清晰且合理。
*   **技术实用**：
    *   利用**无分类器引导**来嵌入硬性安全约束，避免了对噪声轨迹成本分类器准确性的依赖，提升了可靠性。
    *   **FTR机制**设计巧妙，通过修改奖励预测器的训练目标来间接且自然地抑制不安全方向的梯度，而非引入复杂的额外约束。
*   **实验扎实**：评估全面，不仅在静态约束下测试，还验证了动态约束适应能力，并进行了深入的消融分析和超参数鲁棒性检验。
*   **理论支撑**：提供了关于FTR如何抑制成本漂移的一阶采样时间分析，为方法设计提供了理论见解。

#### **8. 不足与局限**
*   **计算成本**：基于扩散模型的规划器通常**采样速度较慢**（文中为2.1 Hz），虽然优于部分基线，但对于需要高频控制的实时系统可能仍是一个挑战。
*   **对离线数据的依赖**：方法的性能和安全保证依赖于离线数据集的质量和覆盖度。如果数据集中安全轨迹的多样性不足，模型学到的“安全区域”可能受限。
*   **理论分析的局限性**：对FTR效果的理论分析基于**一阶近似**和“前缀恢复对齐”的假设条件，在更复杂场景下的普适性有待进一步验证。
*   **应用场景限制**：实验均在**模拟环境**中进行，尚未在真实物理系统（如真实机器人）上验证。真实世界的感知不确定性、动力学模型误差等可能带来新的挑战。
*   **超参数敏感性**：虽然论文展示了稳定性，但方法仍涉及多个超参数（如引导权重 `w`, `λ`，FTR惩罚 `r_us`，前缀长度 `f`），在实际应用中仍需调整。

（完）
