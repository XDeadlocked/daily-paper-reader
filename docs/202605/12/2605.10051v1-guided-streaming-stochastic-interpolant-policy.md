---
title: Guided Streaming Stochastic Interpolant Policy
title_zh: 引导流式随机插值策略
authors: "Puming Jiang, Meiyi Wang, Kelvin Lin, Ce Hao, Harold Soh"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10051v1"
tags: ["query:sr-0501"]
score: 10.0
evidence: 用于实时机器人控制的流式随机插值策略
tldr: 本研究针对生成式机器人策略在推理时难以动态调整的问题，提出了流式随机插值策略（SSIP）框架。通过分析后向柯尔莫哥洛夫方程，推导出随机插值的最优引导项，确保从目标分布采样。该框架结合免训练的随机轨迹集成引导（STEG）和基于训练的条件批评引导（CCG）两种机制，实现了低延迟、高反应性的实时控制。实验证明，该方法在动态非结构化环境中优于传统分块策略，为机器人控制提供了有效的引导方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10051v1/fig-001.webp\", \"caption\": \"Fig. 1: We contribute a principled inference-time guidance framework for streaming generative policies. It enables fast reactive behavior within the action chunk, and can be deployed in either a training-based or training-free manner.\", \"page\": 1, \"index\": 1, \"width\": 477, \"height\": 411}, {\"url\": \"assets/figures/arxiv/2605.10051v1/fig-002.webp\", \"caption\": \"Fig. 2: Safety-Reward trade-off. SSIP consistently achieves more favorable trade-offs compared to the baselines, with its Pareto frontiers closer to the top-right corner of the reward–safety plane. Among the SSIP variants, CCG-P dominates across the evaluated range, while STEG outperforms the CCG-D variant.\", \"page\": 7, \"index\": 2, \"width\": 398, \"height\": 399}, {\"url\": \"assets/figures/arxiv/2605.10051v1/fig-003.webp\", \"caption\": \"Fig. 3: SSIP with CCG-P guidance in static and dynamic obstacle tasks. Red arrows (→) represent the base policy while blue arrows (→) represent guidance. We can observe the guidance acts to push the end-effector away from the obstacles.\", \"page\": 7, \"index\": 3, \"width\": 516, \"height\": 426}, {\"url\": \"assets/figures/arxiv/2605.10051v1/fig-004.webp\", \"caption\": \"Fig. 6: Comprehensive Safety-Reward trade-off (Average Reward vs. Safety Rate) across various baselines. We primarily report ODE variants in the main text, as the stochastic noise introduced by SDE inference was found to degrade performance.\", \"page\": 17, \"index\": 4, \"width\": 454, \"height\": 455}, {\"url\": \"assets/figures/arxiv/2605.10051v1/fig-005.webp\", \"caption\": \"TABLE V: Hyperparameters for Policy Training\", \"page\": 17, \"index\": 5, \"width\": 810, \"height\": 215}]"
motivation: 现有基于分块的策略延迟高、反应性不足，无法满足实时控制中对动态目标或避障的需求。
method: 通过分析后向柯尔莫哥洛夫方程推导出最优引导项，并提出了免训练的STEG和基于训练的CCG两种互补机制。
result: 实验表明，引导流式方法在反应性上显著优于传统分块策略，并能提供物理有效的引导。
conclusion: 该研究提出的SSIP框架结合STEG和CCG机制，为动态非结构化环境中的机器人控制提供了高效且适应性强的解决方案。
---

## 摘要
推理时引导对于在不重新训练的情况下将生成式机器人策略导向动态目标至关重要，但现有方法主要局限于基于分块的架构，这些架构延迟高且缺乏测试时偏好对齐或避障所需的反应性。在本工作中，我们通过反向柯尔莫哥洛夫方程分析值函数的时间演化，正式推导出随机插值的最优引导项，建立了一个理论上保证从目标分布采样的修正漂移项。我们通过流式随机插值策略将此框架应用于实时控制，该策略推广了确定性流式流策略。将此引导定律与流式架构统一起来，实现了快速反应控制。为支持多样化的部署需求，我们提出了两种互补机制：无需训练即可通过即时计算梯度实现零样本适应的随机轨迹集成引导，以及用于摊销推理的基于训练的条件评论家引导。实证评估表明，我们的引导流式方法在反应性上显著优于传统的基于分块策略，并为动态非结构化环境提供了优越且物理有效的引导。

## Abstract
Inference-time guidance is essential for steering generative robot policies toward dynamic objectives without retraining, yet existing methods are largely confined to chunk-based architectures that exhibit high latency and lack the reactivity needed for test-time preference alignment or obstacle avoidance. In this work, we formally derive the optimal guidance term for Stochastic Interpolants (SI) by analyzing the value function's time evolution via the Backward Kolmogorov Equation, establishing a modified drift that theoretically guarantees sampling from a target distribution. We apply this framework to real-time control through the Streaming Stochastic Interpolant Policy (SSIP), which generalizes the deterministic Streaming Flow Policy (SFP). Unifying this guidance law with the streaming architecture enables fast and reactive control. To support diverse deployment needs, we propose two complementary mechanisms: training-free Stochastic Trajectory Ensemble Guidance (STEG) that computes gradients on-the-fly for zero-shot adaptation, and training-based Conditional Critic Guidance (CCG) for amortized inference. Empirical evaluations demonstrate that our guided streaming approach significantly outperforms conventional chunk-based policies in reactivity and provides superior, physically valid guidance for dynamic, unstructured environments.

---

## 论文详细总结（自动生成）

### 论文总结：Guided Streaming Stochastic Interpolant Policy

#### 1. 核心问题与整体含义
*   **研究动机**：现代生成式机器人策略（如扩散策略、流匹配策略）通常采用**分块生成**的方式，即一次性生成未来多步动作序列后再执行。这导致了两个关键问题：
    *   **高延迟**：机器人必须等待整个动作块生成完毕才能开始执行（“等待-执行”瓶颈）。
    *   **低反应性**：在一个动作块的执行过程中，策略处于**开环状态**，无法根据环境动态变化（如移动障碍物、变化的用户偏好）进行实时调整。
*   **整体含义**：本文旨在为生成式机器人策略建立一个**原则性的推理时引导框架**，并将其与一种**流式执行架构**相结合，从根本上解决上述延迟与反应性问题，使机器人能在动态、非结构化环境中进行实时、安全的控制。

#### 2. 提出的方法论
*   **核心思想**：
    1.  **理论奠基**：在**随机插值**的统一框架下，通过分析值函数（合意性）的时间演化（后向柯尔莫哥洛夫方程/费曼-卡茨公式），严格推导出**最优的推理时引导项**。该引导项表现为在基础随机微分方程漂移项上增加一个与未来期望效用对数梯度成正比的修正项（本质上是Doob h-变换）。
    2.  **架构创新**：提出**流式随机插值策略**，将生成过程的时间与机器人物理执行时间对齐，实现**增量式动作生成**，消除“等待-执行”瓶颈。SSIP将确定性流式流策略推广到随机动态，为理论引导项的应用提供了数学基础。
    3.  **引导机制**：针对不同需求，提出两种互补的引导梯度计算策略：
        *   **STEG**：**免训练**的随机轨迹集成引导。在推理时并行展开多条未来轨迹，通过时间反向传播计算期望效用的梯度，实现零样本适应。
        *   **CCG**：**基于训练**的条件评论家引导。训练一个评论家网络来摊销未来期望效用的计算，实现低延迟推理，适用于训练时已知的约束类型。
*   **关键技术细节**：
    *   **SSIP动力学**：`dat = [v_θ + (ϵ(t) - γ(t)γ̇(t)) s_θ] dt + √(2ϵ(t)) dWt`，其中`v_θ`为传输场，`s_θ`为分数，`ϵ(t)`控制随机性。
    *   **最优引导项**：`Δb = 2ϵ(t) ∇_a log u(a_t, t)`，其中`u`是未来轨迹的期望效用（合意性）。
    *   **STEG梯度估计**：使用对数-求和-指数算子对并行 rollout 的代价进行聚合，并通过自动微分求导。
    *   **CCG目标**：训练评论家网络`V_ψ`来回归从基础策略 rollout 得到的期望效用目标值。

#### 3. 实验设计
*   **数据集/场景**：
    *   **仿真**：
        *   **Push-T**：2D平面推动T型块任务。评估静态障碍和三种动态障碍（拦截、振荡、追逐）。
        *   **Robomimic**：3D机器人操作任务（Lift, Can, Square）。引入与示范轨迹相交的静态障碍。
    *   **真实机器人**：Franka Panda机器人，执行移动障碍避障和位置偏好抓取（抓握杯子不同部位）任务。
*   **Benchmark**：任务成功率、推理延迟（单步墙钟时间）、安全性-奖励权衡曲线（Pareto前沿）。
*   **对比方法**：
    *   **基线**：基于分块的扩散策略（DP）和流策略（FP），均配备其经典的推理时引导方法。
    *   **SSIP变体**：
        *   **简单排斥**：基于距离的启发式排斥力。
        *   **STEG**：本文提出的免训练引导。
        *   **CCG-D/P**：本文提出的基于训练的引导（分别使用距离代价和碰撞风险代理作为目标）。

#### 4. 资源与算力
*   论文中明确提及实验和延迟测量在单一工作站上进行，配置为：
    *   **CPU**：AMD EPYC 7543
    *   **GPU**：**单块 NVIDIA RTX A5000**
*   论文**未明确说明**模型训练所需的具体时长、GPU数量或总计算开销。

#### 5. 实验数量与充分性
*   **实验组别**：实验设计较为全面，涵盖了：
    1.  **不同任务领域**：2D简单控制（Push-T）和3D复杂操作（Robomimic）。
    2.  **不同障碍类型**：静态障碍、多种动态模式（三种）。
    3.  **不同评估协议**：报告峰值性能，并绘制安全性-奖励权衡曲线以全面评估。
    4.  **仿真与实物验证**：在仿真基准测试后，在真实机器人上进行了验证。
    5.  **消融对比**：比较了SSIP框架下不同引导机制（STEG, CCG-D, CCG-P, 简单排斥）的性能。
*   **充分性与客观性**：
    *   **较为充分**：实验覆盖了方法提出的核心主张（延迟、反应性、动态适应），并在多个任务和设置下进行了验证。
    *   **相对客观公平**：与主流基线（DP, FP）在相同任务上比较，并采用了两种评估协议。对于分块基线，明确指出了其报告的延迟是“摊销后”的，并提醒读者其真实控制延迟需乘以块大小，这体现了公平性。
    *   **局限性**：实验主要围绕避障和简单偏好调整展开，对于更复杂的推理时目标（如复杂时序逻辑约束）的验证不足。

#### 6. 主要结论与发现
*   **流式架构优势**：SSIP及其引导方法在**动态环境**中显著优于传统分块策略，尤其是在障碍物在动作块执行期间移动的“追逐”场景下，成功率优势巨大（SSIP: 30-36% vs. DP/FP: 8-12%）。
*   **引导机制互补**：
    *   **CCG-P**在静态和中等动态环境中表现最佳，且推理延迟低，适用于约束结构已知的场景。
    *   **STEG**在最具对抗性的动态环境（Chase）中表现出最强的鲁棒性，突出了其免训练、在线适应的价值。
*   **理论有效性**：推导出的引导定律在实践中有效，且优于简单的排斥启发式方法。
*   **实际部署可行性**：方法在真实机器人上成功实现了移动避障和偏好引导，生成的轨迹更平滑。

#### 7. 优点
*   **理论扎实**：从随机插值的一般框架出发，严格推导出最优引导项，为方法奠定了坚实的理论基础。
*   **架构创新**：直指分块策略的“开环执行”根本痛点，提出流式执行架构，是解决反应性问题的重要思路。
*   **机制设计实用**：提出的STEG和CCG两种引导机制分别针对“零样本适应”和“高效摊销”两种常见需求，设计互补且实用。
*   **实验全面**：从简单到复杂的仿真任务，再到真实机器人验证，实验链条完整，有力支撑了主张。

#### 8. 不足与局限
*   **策略分布限制**：引导通过重加权基础策略的轨迹分布生效，因此**避障能力受限于训练数据中存在的运动模式多样性**。如果训练数据中缺乏某种避让模式，策略可能无法生成有效的避障轨迹。
*   **STEG的视野限制**：STEG基于有限步长的未来 rollout 进行梯度估计，**难以应对需要长远规划（长视距）的避障场景**。
*   **SDE推理的实践缺陷**：论文附录发现，在需要高频稳定控制的真实任务中，**随机微分方程推理引入的噪声会损害性能**，确定性ODE反而是更优的骨干网络。这在一定程度上削弱了“随机性”在实践中的必要性主张。
*   **应用范围**：目前主要验证了避障和简单位置偏好，对于更广泛、更复杂的推理时目标（如多目标权衡、复杂语义约束）的泛化能力有待进一步验证。

（完）
