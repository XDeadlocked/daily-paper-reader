---
title: "MCP-Cosmos: World Model-Augmented Agents for Complex Task Execution in MCP Environments"
title_zh: MCP-Cosmos：面向MCP环境中复杂任务执行的世界模型增强智能体
authors: "Giridhar Ganapavarapu, Dhaval Patel"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.09131v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 将生成式世界模型注入智能体以实现预测性任务自动化
tldr: MCP-Cosmos是一个在MCP环境中增强智能体复杂任务执行能力的框架。针对现有方法在任务规划与执行动态结合上的不足，它通过引入生成式世界模型，使智能体能够在潜在空间进行状态模拟和计划优化。该框架融合了MCP、世界模型和智能体三项技术，采用“自带世界模型”策略，在超过20项基准任务上的实验结果显示，其在工具使用成功率和参数准确性等关键指标上均有提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09131v1/fig-001.webp\", \"caption\": \"Table 5: Overall token usage by agent configuration with the gpt-oss-120b planner. Average values are per task; total values are aggregated over the full benchmark.\", \"page\": 11, \"index\": 1, \"width\": 834, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2605.09131v1/fig-002.webp\", \"caption\": \"Figure 1: MCP-Cosmos : World Model-Augmented Agents for Complex Task Execution in MCP Environments\", \"page\": 2, \"index\": 2, \"width\": 776, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2605.09131v1/fig-003.webp\", \"caption\": \"Table 3: Efficiency metrics by planner. Rounds and tool calls are per-task averages; execution time is in seconds. The CLAUDE-SONNET-4.6 baseline issues 29.78 tool calls per task on average, against 1.12–7.91 for world-model-augmented agents under the same planner.\", \"page\": 7, \"index\": 3, \"width\": 840, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2605.09131v1/fig-004.webp\", \"caption\": \"Figure 2: Sample workflow demonstrating simulation based agentic planning, execution and final answer synthesis.\", \"page\": 3, \"index\": 4, \"width\": 844, \"height\": 584}, {\"url\": \"assets/figures/arxiv/2605.09131v1/fig-005.webp\", \"caption\": \"Table 6: Overall token usage across benchmark configurations with the claude-sonnet-4.6 planner. Average values are per task; total values are aggregated over the full benchmark.\", \"page\": 12, \"index\": 5, \"width\": 852, \"height\": 224}]"
motivation: 当前MCP环境中的智能体在任务规划和执行时存在规划忽视动态、执行缺乏远见的问题，需要一种能够预测环境变化的框架。
method: 该框架将MCP、世界模型和智能体技术相结合，采用BYOWM策略，使智能体能够在潜在空间中模拟状态转换并优化计划。
result: 实验表明，该框架在工具成功率和参数准确性等关键指标上有所提升，并引入了执行质量等新评估指标。
conclusion: MCP-Cosmos通过引入世界模型增强了智能体在复杂任务中的执行能力，为MCP环境下的任务自动化提供了新的解决方案。
---

## 摘要
模型上下文协议（MCP）统一了大语言模型（LLM）与外部工具之间的接口，但智能体如何概念化其操作环境仍存在根本性差距。当前范式分为两类：任务级规划常忽略执行时的动态变化，而反应式执行则缺乏长远预见。我们提出MCP-Cosmos框架，将生成式世界模型（WM）融入MCP生态系统，以实现预测性任务自动化。通过统一MCP、世界模型和智能体三项技术，我们证明“自带世界模型”（BYOWM）策略可使智能体在执行前于潜在空间中模拟状态转换并优化计划。我们使用两种策略（ReAct和SPIRAL）、2个规划模型和3个代表性世界模型，在20多个MCP-Bench任务上进行了实验。观察到智能体环境交互关键绩效指标（如工具成功率和工具参数准确率）的提升。该框架还提供了执行质量等新指标，以生成关于世界模型相较于基线有效性的新见解。

## Abstract
The Model Context Protocol (MCP) has unified the interface between Large Language Models (LLMs) and external tools, yet a fundamental gap remains in how agents conceptualize the environments within which they operate. Current paradigms are bifurcated: Task-level planning often ignores execution-time dynamics, while reactive execution lacks long-horizon foresight. We present MCP-Cosmos, a framework that infuses generative World Models (WM) into the MCP ecosystem to enable predictive task automation. By unifying three disparate technologies, namely MCP, World Model, and Agent, we demonstrate that a "Bring Your Own World Model" (BYOWM) strategy allows agents to simulate state transitions and refine plans in a latent space before execution. We conducted experiments using two strategies, namely ReAct and SPIRAL with 2 planning models and 3 representative world models over 20+ MCP-Bench tasks. We observed improvements in Agent's environment interaction KPI such as tool success rate and tool parameter accuracy. The framework also offers new metrics such as Execution Quality to generate new insights about the effectiveness of world models compared to baseline.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，生成一份结构化、深入且客观的中文总结。

### **论文总结：MCP-Cosmos: World Model-Augmented Agents for Complex Task Execution in MCP Environments**

#### **1. 核心问题与整体含义**
*   **研究背景**：模型上下文协议（MCP）标准化了LLM与外部工具的交互，但现有智能体架构存在一个根本矛盾。
*   **核心问题**：
    *   **规划与执行脱节**：以TaskBench为代表的**规划中心型**框架基于静态工具定义进行高层决策，但忽略了真实环境的随机性和动态性。
    *   **执行缺乏远见**：以ReAct为代表的**反应执行型**范式（如MCP-Bench）通过交错观察与行动来导航环境，但容易陷入“视野短视”，无法预见下游后果，导致冗余工具调用或不可逆的状态失败。
*   **整体含义**：本文提出**MCP-Cosmos**框架，旨在弥合这一鸿沟。其核心思想是将**生成式世界模型（WM）** 注入MCP交互循环，使智能体从**反应式执行**转向**预测性认知**。通过让智能体在潜在空间中模拟环境动态（`P(s_{t+1} | s_t, a_t)`），它可以在实际执行前进行前瞻性搜索和计划优化，从而提高任务自动化性能。

#### **2. 方法论**
*   **核心思想**：“自带世界模型”（BYOWM）的模块化策略。允许将异构的世界模型集成到现有的MCP生态系统中，使智能体能够利用世界模型进行模拟，从而在潜在空间中进行预测性规划。
*   **关键技术细节**：
    1.  **两阶段流程**：
        *   **阶段1（模拟规划）**：智能体利用世界模型模拟完整的工具编排，**不实际调用**MCP工具。它迭代生成工具调用，接收模拟观察，并修订计划，直到形成可行方案。
        *   **阶段2（真实执行）**：智能体在真实环境中执行选定的计划序列。若执行失败，可（可选地）触发计划调整机制。
    2.  **抽象世界模型接口**：定义了抽象的 `WorldModel` 类，其核心 `simulate` 方法接收工具调用和用户请求，返回模拟的观察结果。这使得任何符合该接口的世界模型（如通用LLM或专用训练的Agent World Model）都能被集成。
    3.  **世界模型增强的智能体**：定义了 `WMInfusedAgent` 类，将世界模型实例作为输入，在 `execute` 方法中用于模拟。
    4.  **计划选择**：从世界模型轨迹中选择最终执行计划，可使用非确定性策略模型（如LLM）或确定性算法（如基于奖励的MCTS）。
*   **算法流程（文字说明）**：
    1.  **初始化**：接收任务指令、最大步数和世界模型。
    2.  **循环模拟规划**：在未达到终止条件前，规划策略根据当前状态生成一个动作（工具调用），世界模型模拟该动作并返回伪观察，更新世界模型轨迹和状态。
    3.  **计划选择**：从累积的世界模型轨迹中选择最优执行计划。
    4.  **真实环境执行**：按顺序执行计划中的每个动作，接收真实观察。若动作失败，可调整后续计划。记录执行轨迹。
    5.  **答案生成与返回**：基于执行轨迹生成最终答案，并返回答案、执行计划和完整轨迹。

#### **3. 实验设计**
*   **基准数据集**：**MCP-Bench**。作者选择它是因为其具有生态系统级复杂性（28个实时MCP服务器，257个跨领域工具）、真实的模糊指令任务、严格的评估指标以及按难度（服务器数量）分层的特点。
*   **实验场景**：从MCP-Bench中精心挑选了**24个任务**，重点关注需要跨服务器协调的场景，包括6个“3服务器”高协调任务和18个“2服务器”双边交互任务。
*   **对比方法（智能体架构）**：
    1.  **ReAct (Baseline)**：标准的反应式推理与行动智能体，无世界模型能力。
    2.  **ReAct-Plan-Exec**：扩展的ReAct智能体，使用世界模型模拟进行主动规划，然后执行最终计划。
    3.  **SPIRAL-Exec**：采用蒙特卡洛树搜索（MCTS）并由LLM规划器和模拟器辅助的高级智能体，利用世界模型进行预测性模拟。
*   **世界模型配置**：
    *   **通用LLM作为WM**：`gpt-oss-120b`, `claude-sonnet-4.6`。
    *   **专用MCP世界模型**：`Arctic-AWM-4B`（一个在1000个MCP环境上训练的4B参数模型）。
*   **评估指标**：
    *   **继承自MCP-Bench**：任务完成度（任务满足度、答案依据性）、工具选择（工具适当性、参数准确性）、规划有效性（依赖关系意识、并行与效率），以及由前三者合成的**总体得分**。
    *   **本文新提出**：**执行质量**，计算为`工具调用成功率`与`（归一化后的）平均工具调用数`的平均值。该指标旨在惩罚不必要的工具调用，奖励高效执行。

#### **4. 资源与算力**
*   论文**未明确说明**训练世界模型或进行大规模预训练所需的GPU型号、数量或时长。
*   实验运行环境提及为“2021 M1 Max Macbook Pro with 32GB memory”。但文中使用的LLM（如GPT、Claude）和部分世界模型（AWM-4B本地部署）表明，**实际计算是混合的**：大型LLM推理可能在云端进行，而部分实验在本地Mac上运行。论文缺乏对算力消耗的完整描述。

#### **5. 实验数量与充分性**
*   **实验规模**：在24个任务上进行了超过**300条轨迹**的实验。组合了3种智能体架构（ReAct, ReAct-Plan-Exec, SPIRAL-Exec）与3种世界模型（GPT, Claude, AWM-4B）以及1个无世界模型基线，共产生**7种主要配置**进行对比。
*   **消融研究**：进行了深入的消融研究，探究了**规划器能力**（将规划器从`gpt-oss-120b`替换为更强的`claude-sonnet-4.6`）与世界模型贡献之间的关系。
*   **评估充分性与客观性**：
    *   **充分性**：实验覆盖了不同的智能体范式、不同的世界模型类型（通用vs专用）、不同的规划器能力，并从多个维度（准确性、效率、成本）进行评估，设计较为全面。
    *   **客观性与公平性**：
        *   使用了公认的、具有严格评估体系的MCP-Bench基准。
        *   对比了强大的ReAct基线，该基线因其试错恢复能力而被认为是当时的性能天花板。
        *   提供了详细的token使用量和执行时间分析，揭示了性能提升背后的计算成本。
        *   不仅报告了传统成功率指标，还通过新提出的“执行质量”指标，揭示了基线方法（高任务完成度但工具调用效率低）与世界模型方法之间的关键权衡，分析较为客观。

#### **6. 主要结论与发现**
1.  **世界模型提升工具使用效率**：与纯反应式的ReAct基线相比，注入世界模型的智能体（ReAct-Plan-Exec和SPIRAL-Exec）在**工具选择**和**参数准确性**上表现更优，并能提高**并行执行**的可能性。
2.  **最佳配置**：**SPIRAL-Exec + gpt-oss-120b-WM** 取得了最高的原始总体得分（44.8%）。**SPIRAL-Exec + Arctic-AWM-4B-WM** 则实现了完美的执行质量得分（100%），但其任务完成度和规划得分较低。
3.  **专用模型未显优势**：为MCP环境专门训练的**Arctic-AWM-4B** 在大多数配置下效果不如通用的**大型LLM（GPT, Claude）** 作为世界模型。
4.  **计算成本 trade-off**：世界模型带来的性能提升伴随着显著的**计算开销**。Token消耗可能比基线高出66%至504%（SPIRAL-Exec + AWM-4B最高）。
5.  **强大规划器需要世界模型约束**：消融实验发现，使用更强的规划器（Claude）但不使用世界模型时，智能体会进行**大量探索性工具调用**（平均29.78次/任务），导致效率极低。而当强规划器与世界模型结合时，世界模型的模拟步骤迫使规划器专注于经过验证的计划，从而**大幅提升执行效率**。这表明，规划器越强，显式世界模型对约束其探索、控制成本就越重要。
6.  **新评估指标的有效性**：提出的**执行质量**指标成功区分了“通过大量试错最终完成任务”的智能体与“高效、精准完成任务”的智能体，更贴合实际部署中对成本和延迟的考量。

#### **7. 优点**
1.  **模块化与灵活性**：提出的BYOWM架构和抽象接口允许轻松集成任何符合规范的世界模型，具有良好的扩展性。
2.  **深入的评估与洞察**：不仅比较了性能，还通过消融实验深入分析了**规划器能力与世界模型作用**之间的相互作用，得出了“强规划器更需要世界模型来约束”的重要结论。
3.  **提出新的评估视角**：识别出现有评估指标（侧重于任务完成度）的不足，并创新性地提出了**执行质量**这一综合指标，强调工具调用的成功率和效率，对实际应用更有指导意义。
4.  **全面的成本分析**：提供了详细的Token使用量和执行时间数据，明确揭示了性能提升所付出的计算代价，使评估更全面。

#### **8. 不足与局限**
1.  **静态环境评估**：实验基于MCP-Bench的静态服务器环境快照。**真实世界工具和API是动态变化的**，而当前的世界模型不具备在线学习或适应演化的能力。
2.  **高昂的计算成本**：使用大型LLM（如Claude）作为世界模型会带来**显著的延迟和成本**，在资源受限或延迟敏感的场景中可能不适用。
3.  **新指标的局限性**：“执行质量”指标依赖于实验组内的最小-最大归一化，使得**分数无法在不同实验或论文间直接比较**。作者指出这是有意为之，该指标专用于评估特定规划器基线上的世界模型效用。
4.  **评估维度不全**：评估主要关注任务成功率和执行效率，**未评估中间推理步骤的质量、世界模型表征的可解释性**，也未专门测试对“写入操作”或“破坏性工具”的预测能力。
5.  **专用世界模型性能未达预期**：尽管Arctic-AWM-4B是针对MCP环境训练的，但其性能并未超越通用LLM，这引发了关于此类专用模型当前有效性的疑问。

（完）
