---
title: A decoupled diffusion planner that adapts to changing cost limits by using cost-conditioned generation for safety and reward gradients for performance
title_zh: 一种解耦扩散规划器：通过成本条件生成适应变化成本限制，利用奖励梯度提升性能
authors: "Rufeng Chen, Zhaofan Zhang, Zhejiang Yang, Hechang Chen, Sihong Xie"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02777v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 用于机器人学习的扩散规划器
tldr: "本文针对离线安全强化学习中策略需适应变化安全预算的问题，提出了一种解耦扩散规划器（SDGD）。该方法将安全轨迹生成视为从约束轨迹分布中采样，通过成本条件生成确保安全约束，并通过奖励梯度引导优化性能。引入可行轨迹重标注（FTR）来抑制奖励诱导的成本漂移。实验表明，SDGD 在 DSRL 基准上实现了最强的安全合规性（94.7% 任务满足约束）和最高的奖励性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02777v1/fig-001.webp\", \"caption\": \"Table 1: Normalized DSRL Liu et al. [2023a] benchmark results. ↑ means the higher the better. ↓ means the lower the better. Each value is averaged over 20 evaluation episodes and 3 seeds. Gray: Unsafe agents. Bold: Safe agents whose normalized cost is smaller than 1. Blue: Safe agents with the highest reward.\", \"page\": 7, \"index\": 1, \"width\": 824, \"height\": 720}, {\"url\": \"assets/figures/arxiv/2605.02777v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of Safe Decoupled Guidance Diffusion (SDGD). (a) SDGD learns a cost limit (l) conditioned diffuser to model the safe trajectory distribution from offline data, while an FTR-trained reward classifier predicts rewards. Feasible Trajectory Relabeling (FTR) modifies reward single by replacing the raw reward R(τ) with a new reward target R̂(τ), thereby reshaping reward guidance away from infeasible regions and reducing reward-induced cost drift. (b) SDGD decouples safety and performance: classifier-free guidance enforces cost limits, while reward-gradient guidance refines samples toward high-return trajectories within the safe region.\", \"page\": 3, \"index\": 2, \"width\": 826, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2605.02777v1/fig-003.webp\", \"caption\": \"Figure 4: Performance comparison across different cost limits (10, 20, 30) in four tasks.\", \"page\": 8, \"index\": 3, \"width\": 826, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2605.02777v1/fig-004.webp\", \"caption\": \"Figure 6: Ablation study on the guidance components. The SDGD is compared against two variants: one without Classifier Guidance (w/o CG) and another without Classifier-Free Guidance (w/o CFG).\", \"page\": 9, \"index\": 4, \"width\": 816, \"height\": 261}]"
motivation: 离线安全强化学习需要策略在部署时适应变化的安全预算，而现有方法在安全约束和奖励优化之间存在冲突。
method: 提出安全解耦引导扩散（SDGD），通过成本条件生成确保安全，并通过奖励梯度引导提升性能，同时引入可行轨迹重标注（FTR）来抑制奖励诱导的成本漂移。
result: "在 DSRL 基准测试中，SDGD 在 94.7% 的任务中满足约束，并在 21 个任务上获得安全方法中最高的奖励。"
conclusion: SDGD 在满足安全约束的同时实现了高性能，为离线安全强化学习中的自适应规划提供了有效解决方案。
---

## 摘要
离线安全强化学习通常要求策略在部署时能够适应不同回合间或单个回合内变化的安全预算。虽然基于扩散的规划器能够实现灵活的轨迹生成，但现有的引导方案常将奖励提升与约束满足视为相互竞争的梯度目标，这可能导致在成本限制下安全合规性不可靠。我们将自适应安全轨迹生成重新解释为从约束轨迹分布中采样，其中预算限制轨迹区域，而奖励则在该区域内塑造偏好。这一视角催生了安全解耦引导扩散（SDGD），该方法通过成本限制对无分类器引导进行条件化，使采样偏向满足指定限制的轨迹，同时使用奖励梯度引导来优化轨迹以获得更高回报。由于直接奖励引导在提高回报的同时也可能将样本导向具有更高累积成本的轨迹，我们引入了可行轨迹重标注（FTR）来重塑奖励目标并抑制此类方向。我们进一步提供了一阶采样时间分析，表明在前缀恢复对齐条件下，FTR能够抑制奖励引起的成本漂移。在DSRL基准上的广泛评估表明，SDGD在基线中实现了最强的安全合规性，在94.7%的任务（36/38）上满足约束，同时在21个任务上获得了安全方法中最高的奖励。

## Abstract
Offline safe reinforcement learning often requires policies to adapt at deployment time to safety budgets that vary across episodes or change within a single episode. While diffusion-based planners enable flexible trajectory generation, existing guidance schemes often treat reward improvement and constraint satisfaction as competing gradient objectives, which can lead to unreliable safety compliance under cost limits. We reinterpret adaptive safe trajectory generation as sampling from a constrained trajectory distribution, where the budget restricts the trajectory region, and reward shapes preferences within that region. This perspective motivates Safe Decoupled Guidance Diffusion (SDGD), which conditions classifier-free guidance on the cost limit to bias sampling toward trajectories satisfying the specified limit, while using reward-gradient guidance to refine trajectories for higher return. Because direct reward guidance can increase return while also steering samples toward trajectories with higher cumulative cost, we introduce Feasible Trajectory Relabeling (FTR) to reshape reward targets and discourage such directions. We further provide a first-order sampling-time analysis showing that FTR suppresses reward-induced cost drift under a prefix-restorative alignment condition. Extensive evaluations on the DSRL benchmark show that SDGD achieves the strongest safety compliance among baselines, satisfying the constraint on 94.7% of tasks (36/38), while obtaining the highest reward among safe methods on 21 tasks.

---

## 论文详细总结（自动生成）

好的，这是对论文《Safe Decoupled Guidance Diffusion for Adaptive Offline Reinforcement Learning》的详细结构化总结。

### 1. 核心问题与整体含义
*   **研究背景**：在机器人、自动驾驶等安全关键领域，离线安全强化学习旨在从固定的离线数据集中学习策略，以在满足安全约束的同时优化任务性能。
*   **核心问题**：现有方法通常假设部署时的安全预算（成本限制）是固定或弱变化的。然而，实际部署中，预算可能随任务、用户或环境动态变化。现有方法难以在不重新训练的情况下，可靠地适应这种**变化的安全预算**。
*   **现有方法的局限**：
    1.  **自回归模型**：逐步生成轨迹，误差会累积，导致轨迹级成本控制困难。
    2.  **基于分类器引导的扩散模型**：依赖在噪声轨迹上训练的成本分类器提供安全信号，但该信号可能不可靠（如图1c所示），导致引导时机错误，安全合规性不稳定。

### 2. 方法论：Safe Decoupled Guidance Diffusion (SDGD)
*   **核心思想**：重新定义自适应安全轨迹生成问题。将**成本限制**视为对轨迹分布**支持集（可行区域）的限制**，将**奖励**视为在该可行区域内**塑造偏好**。基于此，将安全与性能的引导**解耦**。
*   **关键技术细节**：
    1.  **成本条件扩散模型（安全引导）**：
        *   使用**无分类器引导**，将成本限制 `l` 作为条件输入扩散模型。
        *   公式：`s_safe(τ_s, s, l) = (1+w)*s_θ(τ_s, s, l) - w*s_θ(τ_s, s, ∅)`。这直接使去噪过程偏向于生成满足成本限制 `l` 的轨迹，避免了使用不可靠的成本分类器梯度。
    2.  **奖励梯度引导（性能优化）**：
        *   在成本条件引导的基础上，叠加一个学习到的奖励预测器 `R_φ` 的梯度，以提升轨迹回报。
        *   公式：`s_SDGD(τ_s, s, l) = s_safe(τ_s, s, l) + λ∇_{τ_s} R_φ(τ_s)`。
    3.  **可行轨迹重标注**：
        *   **问题**：直接使用原始奖励 `R(τ)` 进行梯度引导，可能会同时增加奖励和成本（即奖励诱导的成本漂移），因为高回报轨迹可能与高成本相关。
        *   **解决方案**：引入**可行轨迹重标注**。定义轨迹前缀（即将执行的前 `f` 步）的成本。如果前缀成本 > 0，则标记该轨迹为“前缀不可行”。在训练奖励预测器时，将此类轨迹的奖励目标重标注为 `R̂(τ) = R(τ) + r_us`，其中 `r_us` 是一个负的惩罚项。
        *   **作用**：这使得奖励预测器学习到的梯度场会避开那些依赖“前缀不可行”行为来获取高回报的方向，从而在优化奖励时抑制成本增加。论文提供了理论分析（命题1）证明，在当前缀不可行方向与成本梯度正对齐时，FTR能有效抑制成本漂移。

### 3. 实验设计
*   **基准数据集**：**DSRL基准**，包含来自Safety-Gymnasium、Bullet-Safety-Gym和MetaDrive的**38个任务**，涵盖导航、速度控制等多种安全关键场景。
*   **评估指标**：归一化奖励（越高越好）和归一化成本（越低越好）。安全阈值定义为**归一化成本 ≤ 1**。
*   **对比方法**：与7种先进的离线安全RL方法对比，包括：
    *   **基于分布修正**：COptiDICE
    *   **基于惩罚**：CPQ
    *   **基于生成模型/Transformer**：CDT, TREBI (扩散模型)
    *   **基于可行性引导**：FISOR
    *   **自适应策略方法**：CAPS (策略切换), CCAC (条件策略)

### 4. 资源与算力
*   论文中仅提及了**推理效率**：SDGD在**单张NVIDIA A6000 GPU**上能达到 **2.1 Hz** 的决策频率，优于对比方法TREBI (1.1 Hz) 和 HD (1.0 Hz)。
*   对于**模型训练所需的GPU型号、数量、训练时长等关键信息，论文未明确说明**。

### 5. 实验数量与充分性
*   **主实验**：在DSRL全部38个任务上，与7个基线方法进行对比，评估安全合规性（成本≤1的任务数）和性能（安全方法中的最高奖励任务数）。
*   **消融与分析实验**：
    1.  **不同成本限制**：在4个代表性任务上，测试成本限制为10、20、30时SDGD与基线的表现。
    2.  **时间变化限制**：在单个回合内动态改变成本限制（如1→3→10），测试SDGD的动态适应能力。
    3.  **组件消融**：移除奖励梯度引导或成本条件引导，验证两个组件的必要性。
    4.  **角色互换实验**：测试“用分类器引导处理成本，用无分类器引导处理奖励”的变体，验证解耦设计的合理性。
    5.  **超参数稳定性分析**：测试不同无分类器引导权重 `w` 和奖励引导尺度 `λ` 下的性能。
    6.  **FTR参数分析**：分析不同可行前缀长度 `f` 和奖励引导尺度的影响。
*   **评价**：实验设计**较为充分和全面**。不仅在主基准上进行了大规模对比，还通过消融实验验证了核心组件的有效性，并测试了方法在静态、动态成本限制下的鲁棒性。对比基线涵盖了不同类型的主流方法，评估标准统一（归一化成本≤1），实验设计**客观、公平**。

### 6. 主要结论与发现
1.  **卓越的安全合规性**：SDGD在DSRL的38个任务中，在**36个任务（94.7%）** 上满足了安全约束（成本≤1），是所有对比方法中最高的。
2.  **优异的性能**：在满足约束的方法中，SDGD在**21个任务**上获得了最高的奖励，表明其在安全与性能间取得了最佳平衡。
3.  **强大的自适应能力**：**单个训练好的SDGD模型**无需重新训练，即可可靠地适应**不同**的部署时成本限制，甚至能处理**单个回合内动态变化**的成本限制。
4.  **方法有效性验证**：消融实验证实了**解耦设计**（成本条件引导负责安全，奖励梯度引导负责性能）和**FTR机制**的必要性。角色互换实验失败，反证了将分类器引导用于安全限制的不可靠性。

### 7. 优点
1.  **新颖的问题视角**：将安全规划重新定义为从**成本限制约束的轨迹分布**中采样，清晰地区分了安全（定义可行域）和性能（在域内优化）的角色。
2.  **创新的方法设计**：
    *   **解耦引导**：利用**无分类器引导**实现可靠的成本条件生成，利用**梯度引导**进行精细的奖励优化，规避了不可靠成本分类器的问题。
    *   **可行轨迹重标注**：巧妙地通过修改奖励预测器的训练目标，而非直接修改策略或成本模型，来抑制奖励-成本冲突，理论分析支撑了其有效性。
3.  **全面的实验验证**：在标准基准上取得了SOTA结果，并通过丰富的消融和适应性实验，扎实地验证了方法的各个层面。

### 8. 不足与局限
1.  **计算效率**：虽然推理速度优于部分扩散基线，但扩散模型固有的迭代去噪过程相比单步前馈的策略网络或Transformer，**计算开销仍然较大**，可能影响实时性要求极高的应用。论文提到“未来工作将研究更快采样”。
2.  **理论假设的实践满足度**：FTR抑制成本漂移的理论分析基于“前缀恢复对齐条件”。在实践中，该条件是否始终成立，以及对齐强度 `Γ_f` 的大小，会影响FTR的实际效果。
3.  **任务与场景泛化**：
    *   实验主要在模拟的连续控制任务中进行。在更复杂的视觉输入、部分可观或动态变化动力学模型的环境中，方法的有效性有待验证。
    *   方法假设离线数据集中包含多样化的（包括安全和不安全的）行为数据。如果数据集质量极差或分布极其狭窄，扩散模型和条件生成的能力可能会受限。
4.  **超参数敏感性**：尽管论文展示了方法对 `w` 和 `λ` 在一定范围内的鲁棒性，但**无分类器引导权重 `w`、奖励引导尺度 `λ` 以及FTR的前缀长度 `f` 和惩罚 `r_us`** 仍需调整，其最优值可能因任务而异。

（完）
