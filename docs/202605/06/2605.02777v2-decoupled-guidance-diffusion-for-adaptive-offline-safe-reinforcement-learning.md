---
title: Decoupled Guidance Diffusion for Adaptive Offline Safe Reinforcement Learning
title_zh: 自适应离线安全强化学习的解耦引导扩散
authors: "Rufeng Chen, Zhaofan Zhang, Zhejiang Yang, Hechang Chen, Sihong Xie"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02777v2"
tags: ["query:sr-0501"]
score: 9.0
evidence: 用于安全强化学习和训练策略的扩散规划器
tldr: 本文研究离线安全强化学习中的自适应安全轨迹生成问题。针对现有方法在奖励优化和约束满足上的冲突，提出将问题重新解释为从受约束的轨迹分布中采样，并据此设计了安全解耦引导扩散（SDGD）方法。该方法利用成本限制的条件化引导来确保安全合规，同时使用奖励梯度引导来提高回报，还引入了可行轨迹重标注（FTR）来抑制奖励引导导致的成本漂移。实验表明，该方法在安全合规性和奖励获取方面均优于基线方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02777v2/fig-001.webp\", \"caption\": \"Table 1: Normalized DSRL Liu et al. [2023a] benchmark results. ↑ means the higher the better. ↓ means the lower the better. Each value is averaged over 20 evaluation episodes and 3 seeds. Gray: Unsafe agents. Bold: Safe agents whose normalized cost is smaller than 1. Blue: Safe agents with the highest reward.\", \"page\": 7, \"index\": 1, \"width\": 824, \"height\": 720}, {\"url\": \"assets/figures/arxiv/2605.02777v2/fig-002.webp\", \"caption\": \"Figure 2: Overview of Safe Decoupled Guidance Diffusion (SDGD). (a) SDGD learns a cost limit (l) conditioned diffuser to model the safe trajectory distribution from offline data, while an FTR-trained reward classifier predicts rewards. Feasible Trajectory Relabeling (FTR) modifies reward single by replacing the raw reward R(τ) with a new reward target R̂(τ), thereby reshaping reward guidance away from infeasible regions and reducing reward-induced cost drift. (b) SDGD decouples safety and performance: classifier-free guidance enforces cost limits, while reward-gradient guidance refines samples toward high-return trajectories within the safe region.\", \"page\": 3, \"index\": 2, \"width\": 826, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2605.02777v2/fig-003.webp\", \"caption\": \"Figure 4: Performance comparison across different cost limits (10, 20, 30) in four tasks.\", \"page\": 8, \"index\": 3, \"width\": 826, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2605.02777v2/fig-004.webp\", \"caption\": \"Figure 6: Ablation study on the guidance components. The SDGD is compared against two variants: one without Classifier Guidance (w/o CG) and another without Classifier-Free Guidance (w/o CFG).\", \"page\": 9, \"index\": 4, \"width\": 816, \"height\": 261}]"
motivation: 现有基于扩散的规划器在奖励改进和约束满足之间常存在冲突，导致在变化的安全预算下安全合规性不可靠。
method: 提出安全解耦引导扩散（SDGD）和可行轨迹重标注（FTR）方法，分别用于约束满足和奖励优化。
result: "在DSRL基准测试中，SDGD在94.7%的任务上满足约束，并在21个任务上获得安全方法中最高的奖励。"
conclusion: 该方法为离线安全强化学习中的自适应安全轨迹生成提供了一种有效且可靠的解决方案。
---

## 摘要
离线安全强化学习通常要求策略在部署时能够适应不同回合间或单个回合内变化的安全预算。尽管基于扩散的规划器能够实现灵活的轨迹生成，但现有的引导方案往往将奖励改进和约束满足视为相互竞争的梯度目标，这可能导致在成本限制下安全合规性不可靠。我们将自适应安全轨迹生成重新解释为从受约束的轨迹分布中采样，其中预算限制了轨迹区域，而奖励则在该区域内塑造偏好。这一视角催生了安全解耦引导扩散（SDGD），该方法以成本限制为条件进行无分类器引导，使采样偏向满足指定限制的轨迹，同时使用奖励梯度引导来优化轨迹以获得更高回报。由于直接奖励引导在提高回报的同时也可能将样本导向具有更高累积成本的轨迹，我们引入了可行轨迹重标注（FTR）来重塑奖励目标，抑制此类方向。我们进一步提供了一阶采样时间分析，表明在前缀恢复对齐条件下，FTR能够抑制奖励诱导的成本漂移。在DSRL基准上的广泛评估表明，SDGD在基线中实现了最强的安全合规性，在94.7%的任务（36/38）上满足约束，同时在21个任务上获得了安全方法中最高的奖励。

## Abstract
Offline safe reinforcement learning often requires policies to adapt at deployment time to safety budgets that vary across episodes or change within a single episode. While diffusion-based planners enable flexible trajectory generation, existing guidance schemes often treat reward improvement and constraint satisfaction as competing gradient objectives, which can lead to unreliable safety compliance under cost limits. We reinterpret adaptive safe trajectory generation as sampling from a constrained trajectory distribution, where the budget restricts the trajectory region, and reward shapes preferences within that region. This perspective motivates Safe Decoupled Guidance Diffusion (SDGD), which conditions classifier-free guidance on the cost limit to bias sampling toward trajectories satisfying the specified limit, while using reward-gradient guidance to refine trajectories for higher return. Because direct reward guidance can increase return while also steering samples toward trajectories with higher cumulative cost, we introduce Feasible Trajectory Relabeling (FTR) to reshape reward targets and discourage such directions. We further provide a first-order sampling-time analysis showing that FTR suppresses reward-induced cost drift under a prefix-restorative alignment condition. Extensive evaluations on the DSRL benchmark show that SDGD achieves the strongest safety compliance among baselines, satisfying the constraint on 94.7% of tasks (36/38), while obtaining the highest reward among safe methods on 21 tasks.

---

## 论文详细总结（自动生成）

好的，我将基于您提供的论文内容，为您生成一份结构化的详细总结。

### 1. 核心问题与整体含义
*   **研究背景**：在机器人、自动驾驶等安全关键领域，离线安全强化学习（Safe Offline RL）旨在从固定的离线数据集中学习策略，在优化任务性能的同时满足安全约束。然而，实际部署中的安全预算（成本限制）常常是动态变化的（例如，不同任务、用户或同一任务的不同阶段），这要求策略能**在无需重新训练的情况下，自适应地响应部署时的成本限制**。
*   **核心问题**：现有方法（如约束条件策略或自回归轨迹模型）在应对动态成本限制时存在局限。基于扩散的规划器虽能联合生成轨迹，但其常用的**分类器引导**方案将奖励改进和约束满足视为相互竞争的梯度目标，导致在成本限制下的安全合规性不可靠（如图1c所示，成本分类器的预测与真实成本相关性低且不稳定）。

### 2. 方法论：SDGD与FTR
*   **核心思想**：将自适应安全轨迹生成重新解释为**从受约束的轨迹分布中采样**。成本限制定义了可采样轨迹的“区域”（支持集），而奖励则在该区域内塑造偏好。基于此，论文提出应将**安全（约束）与性能（奖励）的角色解耦**。
*   **关键技术**：
    1.  **安全解耦引导扩散（SDGD）**：
        *   **安全引导**：使用**无分类器引导（CFG）**，将成本限制 `l` 作为条件输入扩散模型。这使得模型在去噪过程中直接偏向于生成满足该成本限制的轨迹，避免了依赖不可靠的成本分类器梯度。
        *   **奖励引导**：使用一个独立的奖励预测器 `Rϕ`，通过其**梯度（∇R）** 对采样过程进行微调，以提高轨迹的回报。两者结合形成最终采样分数：`s_SDGD = s_safe + λ∇R`。
    2.  **可行轨迹重标注（FTR）**：
        *   **问题**：直接使用原始回报 `R(τ)` 进行奖励引导，可能会同时增加回报和成本（即奖励诱导的成本漂移），因为高回报轨迹在离线数据中可能与高成本相关。
        *   **解决方案**：在训练奖励预测器时，对轨迹的回报标签进行修改。对于在**可执行前缀**（即规划中即将执行的前几步）中产生成本的轨迹，在其原始回报 `R(τ)` 上减去一个惩罚项 `r_us`（`r_us < 0`），得到新的奖励目标 `R̂(τ)`。这使得奖励预测器学会避开那些需要以“前缀不可行”为代价来获取高回报的方向，从而让奖励梯度引导与安全目标更一致。

### 3. 实验设计
*   **基准与数据集**：在 **DSRL 基准**上进行评估，该基准包含来自 Safety-Gymnasium、Bullet-Safety-Gym 和 MetaDrive 的 **38 个任务**。
*   **评估指标**：采用DSRL的标准化协议，报告**标准化奖励（越高越好）** 和**标准化成本（越低越好）**。若标准化成本 ≤ 1，则认为策略在该任务上是安全的。
*   **对比基线**：与7种先进的离线安全RL方法进行比较，涵盖不同技术路线：
    *   **基于分布修正**：COptiDICE
    *   **基于惩罚**：CPQ
    *   **基于可行性引导**：FISOR
    *   **基于生成模型（Transformer）**：CDT
    *   **基于生成模型（扩散）**：TREBI
    *   **自适应方法（策略切换/条件化）**：CAPS, CCAC

### 4. 资源与算力
*   论文中仅提及了**推理（部署）时的效率**：SDGD在单张NVIDIA A6000 GPU上能达到 **2.1 Hz** 的决策频率，比TREBI (1.1 Hz) 和 HD (1.0 Hz) 等基线方法更快。
*   对于**训练过程所使用的具体算力（如GPU型号、数量、训练时长）**，论文**未明确说明**。

### 5. 实验数量与充分性
*   **主实验**：在DSRL全部38个任务上评估，并与7个基线进行对比，结果以表格形式呈现（表1），并绘制了综合性能帕累托前沿图（图3）。
*   **适应性实验**：
    *   **不同静态预算**：在4个代表性任务上测试了成本限制为10、20、30时的性能变化（图4）。
    *   **动态时变预算**：在单个episode内测试成本限制从1变为3再变为10时，策略的实时适应能力（图5）。
*   **消融与分析实验**：
    *   **组件消融**：验证了去除CFG或去除奖励梯度引导（CG）的影响（图6）。
    *   **角色互换**：测试了“用CFG引导奖励、用CG引导成本”的变体，证明其安全性差（表2）。
    *   **FTR分析**：分析了不同可行前缀长度 `f` 和奖励引导强度 `λ` 对性能的影响（图7）。
    *   **超参数稳定性**：测试了CFG权重 `w` 和CG尺度 `λ` 在不同组合下的性能，证明了方法的鲁棒性（表3）。
*   **评价**：实验设计**较为充分和全面**。涵盖了大规模基准测试、不同适应场景、核心组件消融、超参数敏感性分析以及与多种类型基线的公平对比，能够有力地支持论文的结论。

### 6. 主要结论与发现
*   **安全性领先**：SDGD在 **94.7% (36/38)** 的DSRL任务上满足了安全约束（成本≤1），是所有对比方法中安全合规性最强的。
*   **性能优异**：在满足安全约束的方法中，SDGD在 **21个** 任务上获得了最高的奖励，展现了优秀的奖励-成本权衡能力。
*   **自适应能力强**：**单个训练好的SDGD模型**无需重新训练，即可可靠地适应**不同**的部署时成本限制，甚至能处理** episode 内动态变化**的预算。
*   **方法有效性验证**：消融实验证实了**解耦设计**（CFG用于安全，∇R用于奖励）的必要性，以及**FTR**对于抑制奖励诱导的成本漂移的关键作用。

### 7. 优点
*   **概念创新**：提出了将安全约束视为轨迹分布**支持集限制**、将奖励视为**区域内偏好**的清晰视角，并据此设计了**解耦的引导机制**，概念清晰且有效。
*   **技术实用**：利用成熟的**无分类器引导**技术来可靠地满足约束，避免了不可靠分类器梯度的问题；提出的**FTR**方法巧妙地通过修改训练目标来对齐奖励与安全，无需引入额外复杂的优化。
*   **理论支撑**：提供了关于FTR如何抑制成本漂移的**一阶采样时间分析**（命题1），为方法设计提供了理论见解。
*   **实验扎实**：在大型标准基准上进行了全面评估，并设计了多种实验来验证方法的各个特性。

### 8. 不足与局限
*   **计算效率**：尽管推理速度优于部分扩散基线，但基于扩散模型的规划器**采样速度**通常仍慢于非生成式方法或自回归模型，这可能限制其在需要极高实时性的场景中的应用。论文也将“研究更快采样”列为未来工作。
*   **实验场景局限**：所有实验均在**模拟环境**（DSRL基准）中进行。论文提到“未来工作将研究真实世界验证”，这表明方法在**复杂现实场景**中的有效性尚未得到证明。
*   **FTR的假设**：FTR依赖于“前缀成本不可挽回”的假设，并需要手动设置前缀长度 `f` 和惩罚值 `r_us`。虽然论文给出了 `r_us` 的设置准则，但其**最优性**和在不同环境中的**泛化能力**可能需要进一步研究。
*   **离线数据依赖**：作为离线RL方法，其性能高度依赖于离线数据集的**质量与覆盖度**。在数据集中安全可行轨迹稀缺或分布有偏的情况下，方法的性能可能会下降。

（完）
