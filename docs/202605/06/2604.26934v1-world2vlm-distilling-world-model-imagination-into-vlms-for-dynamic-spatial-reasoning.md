---
title: "World2VLM: Distilling World Model Imagination into VLMs for Dynamic Spatial Reasoning"
title_zh: World2VLM：将世界模型想象力蒸馏至视觉语言模型以实现动态空间推理
authors: "Wanyue Zhang, Wenxiang Wu, Wang Xu, Jiaxin Luo, Helu Zhi, Yibin Huang, Shuo Ren, Zitao Liu, Jiajun Zhang"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26934v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 将世界模型的想象力蒸馏到VLM中，用于动态空间推理
tldr: 视觉语言模型在动态空间推理方面存在不足。本文提出World2VLM框架，通过从生成式世界模型中蒸馏空间想象力来增强视觉语言模型。给定初始观察和参数化相机轨迹，利用世界模型合成几何对齐的未来视图，并生成用于前向和逆向空间推理的结构化监督数据，对模型进行两阶段后训练。该方法在多个基准测试上取得一致提升，且无需昂贵的推理时生成，表明世界模型可作为有效的训练时教师。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26934v1/fig-001.webp\", \"caption\": \"Figure 1. Motivation for World2VLM. Existing approaches either rely on static spatial supervision or defer dynamic reasoning to expensive test-time world-model coupling. World2VLM instead uses a generative world model at training time to synthesize actionaligned view transitions and distill dynamic spatial reasoning into the VLM itself.\", \"page\": 1, \"index\": 1, \"width\": 489, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2604.26934v1/fig-002.webp\", \"caption\": \"Table 2. Main comparison on four dynamic spatial reasoning benchmarks. Results are reported for two teacher world models, SVC and HY-WorldPlay. Across both teachers, World2VLM internalizes world-model guidance at training time, avoids inference-time image generation, and benefits further from task-aware GRPO refinement. The MindJourney-style row is a representative test-time world-model coupling baseline. All scores are reported in percentage units; values in parentheses denote absolute gains or losses in percentage points relative to the base Qwen2.5-VL-7B model. Best results in each column are bold, and second-best results are underlined.\", \"page\": 6, \"index\": 2, \"width\": 1044, \"height\": 209}, {\"url\": \"assets/figures/arxiv/2604.26934v1/fig-003.webp\", \"caption\": \"Figure 3. Absolute benchmark scores of World2VLM-SFT on four dynamic spatial reasoning benchmarks as a function of the amount of world-model-generated training data (0–100K samples). Faint lines and markers denote raw checkpoint evaluations, while solid curves show power-curve fits that smooth short-term fluctuations. Performance improves rapidly in the low-data regime and then gradually saturates, with the clearest gains on SAT-Real and SATSynthesized.\", \"page\": 7, \"index\": 3, \"width\": 478, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2604.26934v1/fig-004.webp\", \"caption\": \"Figure 2. Overview of World2VLM. The framework consists of three components. (1) World-model-guided transition construction: starting from an anchor observation and a parameterized egocentric action, a controllable world model synthesizes an action-aligned future view, together with detector-derived object metadata. (2) Bidirectional task suite for spatial internalization: each source-target transition is converted into complementary inverse tasks that recover the underlying motion and forward tasks that predict motion-conditioned consequences. (3) Two-stage post-training: the student VLM is first optimized with supervised fine-tuning on structured spatial supervision and then refined with task-aware GRPO, which rewards valid formatting, numeric accuracy, spatial logic, and trajectory consistency. After training, World2VLM performs dynamic spatial reasoning directly, without querying the world model or generating auxiliary views at inference time.\", \"page\": 3, \"index\": 4, \"width\": 1018, \"height\": 366}, {\"url\": \"assets/figures/arxiv/2604.26934v1/fig-005.webp\", \"caption\": \"Table 4. Ablation of SFT design choices, compared against the base Qwen2.5-VL-7B model. The (a) subtable isolates the effect of distillation direction, while the (b) subtable isolates the effect of training-source composition. All scores are reported in percentage units; values in parentheses denote absolute gains or losses in percentage points relative to the base model. Best results in each column are bold.\", \"page\": 8, \"index\": 5, \"width\": 860, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2604.26934v1/fig-006.webp\", \"caption\": \"Table 8. Representative SFT→GRPO output changes on held-out evaluation examples. GT denotes ground truth. The examples illustrate three recurring effects of GRPO in our setting: better numeric calibration, more consistent discrete decisions, and cleaner answer formatting.\", \"page\": 19, \"index\": 6, \"width\": 1056, \"height\": 382}, {\"url\": \"assets/figures/arxiv/2604.26934v1/fig-007.webp\", \"caption\": \"Table 1. Task suite used by World2VLM. Each synthesized transition is converted into one of eight concrete spatial reasoning tasks spanning motion inference, action verification, object grounding, and cross-view consistency. Together, these tasks provide complementary supervision for internalizing motion-conditioned spatial structure.\", \"page\": 5, \"index\": 7, \"width\": 1002, \"height\": 440}]"
motivation: 视觉语言模型在静态视觉理解上表现良好，但在需要想象自我运动下场景演变的动态空间推理上仍有困难，现有方法或缺乏对运动条件状态转换的显式建模，或带来高昂计算开销。
method: 提出World2VLM训练框架，利用生成式世界模型合成几何对齐的未来视图，并生成前向与逆向空间推理的结构化监督数据，对视觉语言模型进行两阶段后训练。
result: World2VLM在SAT-Real、SAT-Synthesized、VSI-Bench和MindCube等多个空间推理基准上均优于基线模型，且超越了需要推理时生成的世界模型耦合方法。
conclusion: 研究表明，世界模型不仅能作为推理时工具，更能作为训练时的有效教师，使视觉语言模型以可扩展且高效的方式内化空间想象力。
---

## 摘要
视觉语言模型（VLMs）在静态视觉理解方面表现出色，但在需要想象以自我为中心的运动下场景如何演变的动态空间推理方面仍存在困难。近期研究通过合成数据扩展空间监督或在推理时将VLMs与世界模型耦合来应对这一局限。然而，前者往往缺乏对运动条件状态转移的显式建模，而后者则带来巨大的计算开销。本文提出World2VLM，一种将生成式世界模型的空间想象力蒸馏到视觉语言模型中的训练框架。给定初始观测和参数化相机轨迹，我们使用视图一致的世界模型合成几何对齐的未来视图，并为正向（动作到结果）和逆向（结果到动作）空间推理提供结构化监督。我们利用该流程生成的紧凑数据集，通过两阶段方法对VLM进行后训练，并在多个空间推理基准上评估其性能。World2VLM在包括SAT-Real、SAT-Synthesized、VSI-Bench和MindCube在内的多种基准测试中均较基础模型取得一致提升，同时优于测试时世界模型耦合方法，且无需昂贵的推理时生成。结果表明，世界模型不仅可作为推理时工具，还能作为有效的训练时教师，使VLMs能够以可扩展且高效的方式内化空间想象力。

## Abstract
Vision-language models (VLMs) have shown strong performance on static visual understanding, yet they still struggle with dynamic spatial reasoning that requires imagining how scenes evolve under egocentric motion. Recent efforts address this limitation either by scaling spatial supervision with synthetic data or by coupling VLMs with world models at inference time. However, the former often lacks explicit modeling of motion-conditioned state transitions, while the latter incurs substantial computational overhead. In this work, we propose World2VLM, a training framework that distills spatial imagination from a generative world model into a vision-language model. Given an initial observation and a parameterized camera trajectory, we use a view-consistent world model to synthesize geometrically aligned future views and derive structured supervision for both forward (action-to-outcome) and inverse (outcome-to-action) spatial reasoning. We post-train the VLM with a two-stage recipe on a compact dataset generated by this pipeline and evaluate it on multiple spatial reasoning benchmarks. World2VLM delivers consistent improvements over the base model across diverse benchmarks, including SAT-Real, SAT-Synthesized, VSI-Bench, and MindCube. It also outperforms the test-time world-model-coupled methods while eliminating the need for expensive inference-time generation. Our results suggest that world models can serve not only as inference-time tools, but also as effective training-time teachers, enabling VLMs to internalize spatial imagination in a scalable and efficient manner.

---

## 论文详细总结（自动生成）

### **论文总结：World2VLM**

#### **1. 核心问题与整体含义**
*   **研究动机**：当前视觉语言模型（VLMs）在**静态视觉理解**上表现出色，但在**动态空间推理**（即想象在以自我为中心的相机运动下，场景将如何演变）方面存在根本性缺陷。这限制了其在具身智能等需要心智模拟的应用中的能力。
*   **背景与现有方法局限**：
    *   **方法一（扩展监督）**：通过合成数据增加空间任务训练。**局限**：缺乏对“动作-状态”转移的显式建模，监督信号本质上是静态的。
    *   **方法二（推理时耦合）**：在VLM推理时调用外部世界模型（如MindJourney）生成想象视图辅助推理。**局限**：带来巨大的计算开销和延迟，且VLM自身能力未得到提升。
*   **核心思想**：提出一种**范式转变**——将世界模型从**推理时的“外挂工具”** 转变为**训练时的“教师模型”**。通过蒸馏世界模型生成的动作-视图转换知识，让VLM**内部化**动态空间想象力，从而在推理时无需任何外部生成，实现高效、强大的动态空间推理。

#### **2. 方法论**
*   **核心框架**：包含三个核心组件，遵循“**生成-转换-蒸馏**”的流程。
*   **关键技术细节**：
    1.  **世界模型引导的转换构建**：
        *   **输入**：锚定观测图像 + 参数化的以自我为中心相机动作（如“前进1米，右转30度”）。
        *   **过程**：使用可控的世界模型（论文中使用了**SVC**和**HY-WorldPlay**）合成与动作几何对齐的未来视图。
        *   **增强**：使用检测器-跟踪器获取跨视图的对象边界框元数据，作为结构化监督信号。
        *   **策略**：采用“最大位移配对”，即使用轨迹中最远的有效端点与锚定帧配对，以强调非平凡的视角变化。
    2.  **双向空间内化任务集**：
        *   将每个生成的（源视图，动作，目标视图）三元组，转化为8种具体的问答任务，分为两个互补的方向：
            *   **逆向推理**：从视图变化中推断导致该变化的动作（如估计移动距离、旋转角度、多步动作序列）。
            *   **正向推理**：给定动作预测其结果（如验证动作是否匹配、预测动作后物体的位置框、判断物体可见性）。
        *   **设计目的**：迫使模型同时掌握“解释现象”和“预测未来”的能力，构建统一的空间表征，避免通过浅层捷径学习。
    3.  **两阶段后训练配方**：
        *   **第一阶段（监督微调，SFT）**：使用上述生成的大约10万条数据，以监督学习方式联合优化逆向和正向任务损失，将空间转换知识蒸馏到VLM参数中。
        *   **第二阶段（GRPO强化学习微调）**：在第一阶段模型基础上，使用小组相对策略优化（GRPO）对一个小型平衡数据集（1k条）进行微调。
        *   **奖励函数**：设计**任务感知**的复合奖励，鼓励**格式正确、语义准确、数值精确、几何对齐、顺序一致**的输出，并惩罚冗长、格式错误的回答。此阶段旨在校准和锐化模型的输出质量。

#### **3. 实验设计**
*   **基础模型**：Qwen2.5-VL-7B-Instruct。
*   **评估基准（4个）**：均侧重于动态或视角条件推理。
    *   **SAT-Real / SAT-Synthesized**：评估在以自我为中心运动下的动态空间能力。
    *   **VSI-Bench**：评估从以自我为中心观测中的配置、测量和时空推理。
    *   **MindCube**：评估在部分观测下的视角采择、心智模拟和动作条件推理。
*   **对比方法**：
    *   **Base VLM**：原始Qwen2.5-VL-7B。
    *   **Test-time WM Coupling**：模拟MindJourney风格的推理时世界模型耦合基线（冻结VLM，调用WM生成候选视图进行评分）。
    *   **World2VLM变体**：使用不同教师（SVC / HY-WorldPlay）和不同训练阶段（SFT / GRPO）的自身模型。
    *   **消融实验**：包括单向蒸馏（仅正向/仅逆向）、单数据源训练（仅真实/仅模拟场景）。

#### **4. 资源与算力**
*   论文在附录E中提及了部分实现细节：
    *   **监督微调阶段**：使用了**8张H100 GPU**，采用BF16精度、AdamW优化器、LoRA参数高效微调（视觉编码器冻结）。
    *   **GRPO阶段**：使用了平衡的1k条数据，每提示采样4个响应进行优化。
    *   **训练时长**：论文未明确说明总训练时间（如小时数或天数）。

#### **5. 实验数量与充分性**
*   **实验组数**：实验设计较为系统和充分，主要包括：
    1.  **主实验**：在4个基准上对比了基础模型、测试时耦合基线以及4种World2VLM配置（2教师×2阶段）。
    2.  **消融实验**：系统分析了**双向蒸馏**和**训练数据源构成**的影响。
    3.  **数据量缩放实验**：展示了性能随世界模型生成数据量（0-100K）增加的变化曲线。
    4.  **细粒度分析**：在SAT-Real上进行了5个动态子类别的任务细分分析。
    5.  **定性分析**：提供了GRPO阶段优化输出质量的典型案例。
*   **客观性与公平性**：
    *   **公平对比**：所有方法均基于**相同的VLM骨干**（Qwen2.5-VL-7B），确保了比较的公平性。
    *   **基准隔离**：明确强调**基准评估数据从未用于训练**，避免了数据泄露。
    *   **多基准验证**：在4个独立且具有挑战性的动态空间推理基准上验证，结论更具普适性。
    *   **消融分析**：通过控制变量实验，有效验证了核心设计（双向性、两阶段训练）的必要性。

#### **6. 主要结论与发现**
*   **有效性**：World2VLM在所有四个动态空间推理基准上均**一致且显著地**超越了基础VLM和测试时世界模型耦合基线。
*   **效率优势**：在取得更高性能的同时，**推理时无需调用世界模型**，计算成本与标准VLM相同，实现了更优的“精度-效率”权衡。
*   **关键设计有效**：
    *   **双向蒸馏**优于单向蒸馏，表明逆向与正向任务是互补的。
    *   **GRPO阶段**能进一步优化输出，特别是在数值校准和格式规整方面。
*   **教师模型鲁棒性**：方法对不同的世界模型教师（SVC和HY-WorldPlay）均有效，表明框架具有一般性，不依赖于特定生成器。
*   **能力提升针对性**：在SAT-Real上的细分显示，提升主要集中在需要显式推理相机运动和动作结果的动态类别上，而非静态识别。

#### **7. 优点**
*   **范式创新**：提出了“**训练时内部化**”而非“推理时耦合”的新范式，从根本上解决了动态空间推理的效率瓶颈。
*   **任务设计巧妙**：**双向任务套件**的设计迫使模型建立深度的、可解释和可预测的空间理解，避免了单一任务的捷径学习。
*   **训练流程系统**：**SFT+GRPO两阶段训练**将知识蒸馏与输出校准分离，流程清晰且有效。
*   **实验扎实全面**：评估基准选择恰当，消融实验设计清晰，并对性能提升进行了细粒度的归因分析，论证有力。

#### **8. 不足与局限**
*   **教师模型依赖与噪声**：蒸馏效果的上限受限于世界模型教师的质量。教师生成的视图若存在几何失真、动作不一致或物体身份漂移，会将有噪声甚至错误的监督信号引入学生模型。论文虽使用了两种教师并进行了过滤，但未引入更精细的教师可靠性加权机制。
*   **任务范围局限**：当前方法主要针对**以自我为中心的相机运动**。对于涉及复杂物体交互、物理规律推理或更长规划视野的动态场景，其有效性有待验证。
*   **泛化性未知**：方法在室内场景（ScanNet, MulSeT）数据上训练和评估。其在开放世界、室外或高度非结构化环境中的泛化能力尚未可知。
*   **实现复杂性**：GRPO阶段的奖励函数设计较为复杂，涉及多个手工定制的子奖励和阈值，可能增加复现和调优的难度。
*   **算力成本转移**：虽然推理成本低，但训练成本（包括运行世界模型生成大量数据、两阶段微调）依然可观，且未详细量化。

（完）
