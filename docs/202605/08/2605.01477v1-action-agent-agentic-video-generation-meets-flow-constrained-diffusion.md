---
title: "Action Agent: Agentic Video Generation Meets Flow-Constrained Diffusion"
title_zh: Action Agent：具身视频生成与流约束扩散的融合
authors: "Jeffrin Sam, Nguyen Khang, Yara Mahmoud, Miguel Altamirano Cabrera, Dzmitry Tsetserukou"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01477v1"
tags: ["query:sr-0501"]
score: 10.0
evidence: 将智能体视频生成与流约束扩散统一用于机器人导航
tldr: "本文提出了Action Agent，一个两阶段框架，用于语言引导的多形态机器人导航。第一阶段使用大语言模型编排视频扩散模型，生成物理合理的第一人称导航视频，将成功率从35%提升到86%。第二阶段引入FlowDiT，通过动作空间去噪扩散将优化的视频和语言指令转换为连续速度命令。该方法在仿真和真实机器人上验证有效，为机器人导航提供了一种可扩展且感知形态的新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01477v1/fig-001.webp\", \"caption\": \"Fig. 2: Stage I: agentic trajectory imagination (digital rehearsal). A central LLM agent orchestrates (i) a vision–language model for prompt construction, (ii) a text-to-video diffusion generator, and (iii) a reasoning-based evaluator that scores candidate videos and produces structured critiques. The agent iteratively refines prompts using feedback and memory until a validated reference navigation video Vgoal is obtained.\", \"page\": 4, \"index\": 1, \"width\": 948, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2605.01477v1/fig-002.webp\", \"caption\": \"Fig. 3: Stage II: FlowDiT execution module. FlowDiT conditions on the validated reference video Vgoal (visual features and motion cues) and the instruction L to predict a horizon-H sequence of velocity commands (vx, vy, ω). The policy executes in a recedinghorizon manner by applying the first action and re-evaluating at the next timestep (with optional live observation It when available).\", \"page\": 4, \"index\": 2, \"width\": 514, \"height\": 471}, {\"url\": \"assets/figures/arxiv/2605.01477v1/fig-003.webp\", \"caption\": \"Fig. 1: Action Agent system overview. Stage I performs agentic trajectory imagination by generating and validating a first-person visual intermediate representation (reference navigation video) from a language instruction and an initial observation. Stage II executes this reference using FlowDiT to produce continuous velocity commands for deployment across multiple robot embodiments.\", \"page\": 1, \"index\": 3, \"width\": 462, \"height\": 479}]"
motivation: 为了解决单次视频生成在导航任务中成功率低的问题，并实现语言引导的多形态机器人导航。
method: 第一阶段使用LLM作为编排模块选择视频扩散模型并优化提示，第二阶段使用FlowDiT将优化的目标视频和语言指令转换为连续速度命令。
result: "在50个导航任务中，视频生成成功率从35%提升到86%；仿真导航成功率达到73.2%，真实Unitree G1机器人在未知室内环境中的任务完成率达到64.7%。"
conclusion: Action Agent通过两阶段框架，将语言引导的视频生成与动作执行解耦，为多形态机器人导航提供了一个可扩展且感知形态的范式。
---

## 摘要
我们提出了Action Agent，一个两阶段框架，将具身导航视频生成与流约束扩散控制相统一，用于多具身机器人导航。在第一阶段，大型语言模型（LLM）作为编排模块，选择视频扩散模型，通过迭代验证优化提示，并积累跨任务记忆，从而从语言和图像输入中合成物理合理的第一人称导航视频。这使得在50个导航任务中，视频生成成功率从35%（单次生成）提升至86%。在第二阶段，我们引入了FlowDiT，一种流约束扩散Transformer，它利用动作空间去噪扩散将优化的目标视频和语言指令转换为连续的速度命令。FlowDiT集成了DINOv2视觉特征、用于自运动表示的学习光流以及用于语义停止的CLIP语言嵌入。我们在RECON户外导航数据集上进行预训练，并在Isaac Sim中收集的203个Unitree G1人形机器人片段上进行微调，以校准速度动力学。单个4300万参数的检查点在模拟中实现了73.2%的导航成功率，并在开环执行下，在未见过的室内环境中，在真实的Unitree G1上实现了64.7%的任务完成率，同时以40-47 Hz的频率运行。我们在三种具身上评估了Action Agent：Unitree G1人形机器人（真实硬件）、无人机和轮式移动机器人（Isaac Sim），证明了将轨迹想象与执行解耦为语言引导导航提供了一种可扩展且具身感知的范式。

## Abstract
We present Action Agent, a two-stage framework that unifies agentic navigation video generation with flow-constrained diffusion control for multi-embodiment robot navigation. In Stage I, a large language model (LLM) acts as an orchestration module that selects video diffusion models, refines prompts through iterative validation, and accumulates cross-task memory to synthesize physically plausible first-person navigation videos from language and image inputs. This increases video generation success from 35% (single-shot) to 86% across 50 navigation tasks. In Stage II, we introduce FlowDiT, a Flow-Constrained Diffusion Transformer that converts optimized goal videos and language instructions into continuous velocity commands using action-space denoising diffusion. FlowDiT integrates DINOv2 visual features, learned optical flow for ego-motion representation, and CLIP language embeddings for semantic stopping. We pretrain on the RECON outdoor navigation dataset and fine-tune on 203 Unitree G1 humanoid episodes collected in Isaac Sim to calibrate velocity dynamics. A single 43M-parameter checkpoint achieves 73.2% navigation success in simulation and 64.7% task completion on a real Unitree G1 in unseen indoor environments under open-loop execution, while operating at 40--47 Hz. We evaluate Action Agent across three embodiments: a Unitree G1 humanoid (real hardware), a drone, and a wheeled mobile robot (Isaac Sim), demonstrating that decoupling trajectory imagination from execution yields a scalable and embodiment-aware paradigm for language-guided navigation.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
*   **核心问题**：如何让机器人根据高级语言指令，在多样化的环境和不同形态（具身）下，进行可靠、物理合理的导航。
*   **研究动机与背景**：
    *   **传统方法**（如SLAM+规划+控制）模块化、可解释，但需要针对特定机器人精心调优，且难以应对环境变化或跨平台迁移。
    *   **端到端视觉语言动作模型** 试图直接映射感知到动作，但通常将高层意图与底层控制纠缠在一起，导致“黑箱”特性、难以适应、计算开销大，且易产生空间推理幻觉。
    *   **本文思路**：提出一种**两阶段分解范式**，将**轨迹想象（在像素空间“预演”）** 与**动作执行（在度量空间控制）** 显式解耦，旨在结合生成模型的强大想象力与轻量、可部署控制策略的效率。

### 2. 论文提出的方法论
*   **核心思想**：通过一个**视觉中间表示**（即第一人称导航视频）作为桥梁，连接语言指令与机器人动作。第一阶段专注于生成物理合理的参考视频，第二阶段专注于将该视频稳健地转化为控制命令。
*   **关键技术细节**：
    *   **第一阶段：智能体轨迹想象（数字预演）**
        *   **智能体优化循环**：一个LLM作为编排器，协调视觉语言模型（用于提示构建）、文本到视频生成器（如WAN 2.2）和基于推理的评估器（如Cosmos-Reason）。
        *   **迭代优化**：根据评估器对生成视频在**提示遵循度、物理合理性和视觉质量**三个维度的评分，LLM迭代地优化生成提示或切换模型，直到视频满足阈值要求。
        *   **三阶段记忆架构**：包含静态规则、跨任务长期记忆和单任务短期记忆，以提高优化效率和成功率。
    *   **第二阶段：流约束扩散Transformer**
        *   **输入**：验证后的参考视频 `V_goal`、语言指令 `L`、可选的实时观测 `I_t`。
        *   **输出**：未来H步（H=8）的连续速度命令 `(v_x, v_y, ω)`，采用模型预测控制（MPC）方式执行。
        *   **多模态条件表示**：将参考视频的**DINOv2语义特征**、**学习得到的光流特征**（用于表示自运动）和指令的**CLIP语言嵌入**拼接为一个条件向量。
        *   **扩散策略**：采用去噪扩散概率模型学习动作序列的条件分布，使用DiT架构，并通过adaLN-Zero机制注入条件。
        *   **高效设计**：仅训练DiT模块和光流编码器（共43M参数），冻结大型视觉和语言编码器，实现高频率（40-47 Hz）控制。

### 3. 实验设计
*   **任务与场景**：在包含仓库、医院走廊等环境的室内场景中，设计了50个第一人称导航任务（直线、避障、路径跟随、转向等）。
*   **评估平台**：
    *   **真实硬件**：Unitree G1人形机器人（头部RGB相机），进行开环执行测试。
    *   **仿真**：Isaac Sim中的四旋翼无人机和轮式移动机器人。
*   **评估指标**：
    *   **第一阶段**：视频合成成功率（基于PA, PP, VQ评分）。
    *   **第二阶段**：导航成功率、平均绝对轨迹误差、方向准确率（仿真）；真实环境任务完成率。
*   **对比方法**：
    *   **ViNT**：基于视觉Transformer的导航基础模型。
    *   **NoMaD**：基于扩散的目标条件导航方法。
    *   **OpenVLA**：大规模视觉语言动作基础模型。
    *   **消融基线**：移除光流或语言特征的FlowDiT变体。

### 4. 资源与算力
*   论文中明确提到了训练和推理使用的硬件为 **NVIDIA RTX 5090 (32 GB)**。
*   对于**训练时长、具体GPU数量、总训练成本**等信息，论文**未作明确说明**。

### 5. 实验数量与充分性
*   **实验数量**：实验设计较为全面。
    *   **第一阶段**：在50个任务上评估了视频生成成功率，并分析了智能体优化循环和记忆机制的影响。
    *   **第二阶段**：
        *   **仿真对比**：在G1仿真验证集（41个片段）上与多个SOTA基线比较。
        *   **真实机器人**：在17个未见过的室内环境任务中进行开环部署测试。
        *   **跨平台验证**：在无人机和轮式移动机器人（仿真）上测试了同一套系统的泛化能力。
        *   **消融研究**：系统地评估了视觉、光流、语言三个条件模块的贡献。
*   **充分性与客观性**：
    *   **充分性**：实验覆盖了方法的核心主张（两阶段有效性、跨平台性、模块重要性），并在仿真和真实世界进行了验证，实验数量足以支撑结论。
    *   **客观性**：论文指出了基线方法的数据集和评估标准不同（RECON户外 vs. 本文的室内G1数据），进行了说明而非直接比较绝对数值，态度较为严谨。同时，详细报告了真实机器人测试中的失败案例并分析了原因。
    *   **公平性**：对比是在相近的任务目标下进行的，但完全公平的比较（相同数据集、机器人平台）受限于各方法原始设计，难以实现。

### 6. 论文的主要结论与发现
*   **两阶段解耦有效**：将轨迹想象与执行解耦，并通过智能体循环优化视频生成，显著提升了导航视频的物理合理性和任务成功率（从35%升至86%）。
*   **FlowDiT高效且强大**：仅43M可训练参数的FlowDiT，在仿真中取得了与数十亿参数VLA模型（如OpenVLA）相当的导航成功率（73.2%），并实现了高频控制。
*   **多模态条件至关重要**：消融实验表明，**光流特征**对方向准确性贡献显著，**语言嵌入**对实现语义停止（准确到达目标）至关重要。
*   **具备现实可行性**：经过仿真微调后，系统能**零样本**迁移到真实Unitree G1机器人，在开环执行下达到64.7%的任务完成率，证明了从“像素预演”到“度量控制”的可行性。
*   **跨平台泛化性**：同一套Stage I和Stage II系统，无需针对每个平台重新训练，即可在三种形态迥异的机器人上执行导航任务。

### 7. 优点
*   **范式创新**：提出并验证了“**视觉中间表示**”作为连接高层规划与底层控制的有效接口，概念清晰，具有启发性。
*   **系统设计精巧**：
    *   **Stage I**：利用LLM的推理和规划能力作为“元优化器”，引导生成模型产出符合物理和任务约束的视频，巧妙地解决了生成模型难以直接控制的问题。
    *   **Stage II**：**FlowDiT** 轻量高效，巧妙融合了语义、运动和语言信息，特别是引入**光流**来显式建模自运动，解决了纯语义特征的深度模糊问题。
*   **兼顾性能与效率**：在保持高性能的同时，通过冻结大模型参数，实现了参数效率和高频运行，更利于实际部署。
*   **验证全面**：不仅在仿真中验证，还进行了真实的机器人开环实验，并测试了跨平台能力，增强了工作的说服力。

### 8. 不足与局限
*   **开环执行的局限性**：真实机器人实验采用开环执行，暴露出**视频到度量的尺度模糊**问题，导致轨迹漂移和碰撞。论文指出闭环执行是预期的解决方案。
*   **视频生成长度限制**：当前视频生成模型限制片段长度为5-15秒，这限制了单次规划的视野，对于长程导航需要更复杂的规划策略。
*   **精细操作能力不足**：在需要厘米级精度的狭窄空间（如门框），系统失败率较高。
*   **依赖生成模型质量**：第一阶段完全依赖于现有文本到视频生成模型的能力和物理真实性，其错误会直接影响后续执行。
*   **计算成本权衡**：第一阶段的迭代优化虽然提升了成功率，但增加了离线计算成本，属于用“离线计算时间”换取“在线执行安全性和成功率”的权衡。
*   **仿真到现实的差距**：尽管实现了零样本迁移，但64.7%的真实成功率与73.2%的仿真成功率仍存在差距，表明仿真动力学校准仍有改进空间。

（完）
