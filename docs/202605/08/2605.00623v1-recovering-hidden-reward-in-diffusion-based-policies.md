---
title: Recovering Hidden Reward in Diffusion-Based Policies
title_zh: 基于扩散策略中隐藏奖励的恢复
authors: "Yanbiao Ji, Qiuchang Li, Yuting Hu, Shaokai Wu, Wenyuan Xie, Guodong Zhang, Qicheng He, Deyi Ji, Yue Ding, Hongtao Lu"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00623v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 将生成式动作建模与逆强化学习统一用于扩散策略
tldr: 本文提出EnergyFlow框架，旨在从基于扩散的专家策略中恢复隐藏的奖励函数。该方法通过去噪得分匹配学习得分函数，在最大熵最优性下恢复专家软Q函数的梯度，从而无需对抗训练即可提取奖励。理论证明约束学习场为保守场可降低假设复杂度并收紧分布外泛化界限。实验表明，EnergyFlow在多种操作任务上取得了最先进的模仿性能，其提供的奖励信号在下游强化学习中优于对抗性逆强化学习和基于似然的方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00623v1/fig-001.webp\", \"caption\": \"Figure 3. Real-world evaluation tasks. We evaluate on two contact-rich manipulation tasks: Bottle (top), where the robot must grasp a bottle and place it into a cardboard box, and Drawer (bottom), where the robot must pull the drawer open.\", \"page\": 6, \"index\": 1, \"width\": 1017, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2605.00623v1/fig-002.webp\", \"caption\": \"Figure 6. RoboMimic task demonstrations. Each row visualizes a rollout sequence for a different task.\", \"page\": 22, \"index\": 2, \"width\": 1017, \"height\": 1134}, {\"url\": \"assets/figures/arxiv/2605.00623v1/fig-003.webp\", \"caption\": \"Figure 7. Meta-World task demonstrations. Meta-World task demonstrations. Each row visualizes a rollout sequence for a different task.\", \"page\": 23, \"index\": 3, \"width\": 1017, \"height\": 1123}, {\"url\": \"assets/figures/arxiv/2605.00623v1/fig-004.webp\", \"caption\": \"Figure 5. OOD generalization on RoboMimic. Success rate vs. initial position perturbation magnitude. ENERGYFLOW degrades more gracefully than baselines, with the gap widening at larger perturbations. Shaded regions indicate 95% confidence intervals.\", \"page\": 8, \"index\": 4, \"width\": 1017, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2605.00623v1/fig-005.webp\", \"caption\": \"Table 5. Hyperparameters for ENERGYFLOW Training and Inference.\", \"page\": 19, \"index\": 5, \"width\": 510, \"height\": 464}]"
motivation: 旨在从基于扩散的策略中恢复隐藏的奖励函数，避免对抗训练并提升泛化能力。
method: 提出EnergyFlow框架，通过参数化标量能量函数（其梯度为去噪场）将生成式动作建模与逆强化学习统一起来。
result: EnergyFlow在各种操作任务上实现了最先进的模仿性能，并为下游强化学习提供了优于对抗性IRL方法和基于似然替代方法的有效奖励信号。
conclusion: 该研究表明，用于有效奖励提取的结构约束同时可作为策略泛化的有益归纳偏置。
---

## 摘要
本文介绍了EnergyFlow框架，该框架通过参数化一个标量能量函数（其梯度为去噪场），将生成式动作建模与逆强化学习相统一。我们证明，在最大熵最优性条件下，通过去噪分数匹配学习到的得分函数能够恢复专家软Q函数的梯度，从而无需对抗训练即可提取奖励。形式上，我们证明了约束学习到的场为保守场可降低假设复杂度并收紧分布外泛化边界。我们进一步刻画了恢复奖励的可识别性，并界定了分数估计误差如何传播到动作偏好。实证表明，EnergyFlow在各种操作任务上实现了最先进的模仿性能，同时为下游强化学习提供了有效的奖励信号，其表现优于对抗性逆强化学习方法和基于似然的替代方法。这些结果表明，有效奖励提取所需的结构约束同时可作为策略泛化的有益归纳偏置。代码可在https://github.com/sotaagi/EnergyFlow获取。

## Abstract
This paper introduces EnergyFlow, a framework that unifies generative action modeling with inverse reinforcement learning by parameterizing a scalar energy function whose gradient is the denoising field. We establish that under maximum-entropy optimality, the score function learned via denoising score matching recovers the gradient of the expert's soft Q-function, enabling reward extraction without adversarial training. Formally, we prove that constraining the learned field to be conservative reduces hypothesis complexity and tightens out-of-distribution generalization bounds. We further characterize the identifiability of recovered rewards and bound how score estimation errors propagate to action preferences. Empirically, EnergyFlow achieves state-of-the-art imitation performance on various manipulation tasks while providing an effective reward signal for downstream reinforcement learning that outperforms both adversarial IRL methods and likelihood-based alternatives. These results show that the structural constraints required for valid reward extraction simultaneously serve as beneficial inductive biases for policy generalization. The code is available at https://github.com/sotaagi/EnergyFlow.

---

## 论文详细总结（自动生成）

### **1. 论文的核心问题与整体含义**
*   **研究背景**：基于扩散的策略已成为从专家演示中学习机器人操作技能的有效范式。然而，这类方法通常仅通过行为克隆模仿动作，**缺乏对专家“为何选择此动作”（即底层奖励或意图）的显式建模**，这限制了其在分布外状态下的鲁棒性和泛化能力。
*   **核心问题**：能否从表现优异的扩散策略中，**无需复杂的对抗训练，直接恢复出隐含的、可解释的奖励函数**，从而既能保持强大的模仿能力，又能为策略改进和强化学习提供指导信号？
*   **整体含义**：本文提出了一种**统一生成式动作建模与逆强化学习（IRL）** 的框架。其核心洞见是，在最大熵最优性假设下，扩散模型学习到的“得分函数”本质上就是专家奖励函数梯度的体现。通过施加适当的数学约束（保守场），可以从扩散策略中稳定、高效地提取出有效的奖励信号。

### **2. 论文提出的方法论**
*   **核心思想**：参数化一个**标量能量函数** `E_θ(s, a)`，其关于动作的负梯度 `-∇_a E_θ` 被定义为去噪得分。这强制了学习到的向量场是“保守的”（即是一个标量势能的梯度），从而保证了恢复出的奖励在理论上是良定义的。
*   **关键技术细节**：
    1.  **保守场约束**：直接学习标量能量函数，而非向量化的得分，从架构上保证了 `∇_a × (-∇_a E_θ) ≡ 0`。这避免了非保守场可能导致的循环偏好悖论，并作为一种强大的归纳偏置。
    2.  **去噪得分匹配训练**：训练目标与标准扩散策略类似，但预测对象是能量函数的梯度。最小化噪声预测误差，在理论上等价于恢复专家软Q函数的梯度。
    3.  **奖励提取（中心化整形）**：直接从能量函数 `E_θ` 得到的奖励包含一个与状态相关的未知偏移 `c(s)`。为了获得可用于下游RL的低方差奖励，论文提出计算 `˜r(a, s) = -[E_θ(a, s) - E_a‘~p_ref[E_θ(a‘, s)]]`，即减去在参考动作分布下的期望能量，以此抵消状态偏移，仅保留动作间的相对偏好。
*   **算法流程**：
    *   **训练**：采样状态-动作对 `(s, a)`，添加噪声得到 `a_t`，计算能量梯度 `S_θ = -∇_{a_t} E_θ(a_t, s, t)`，优化其与真实噪声方向的均方误差。
    *   **推理（动作生成）**：从噪声采样，通过以 `∇_a E_θ` 驱动的概率流ODE进行迭代去噪，生成最终动作。
    *   **奖励获取**：在推理的最后一步，利用中心化整形公式计算当前 `(s, a)` 的奖励值。

### **3. 实验设计**
*   **数据集/场景**：
    *   **仿真**：使用两大主流机器人操作基准——**RoboMimic**（Lift, Can, Square, Transport, ToolHang）和 **Meta-World**（ButtonPress, DrawerOpen, Assembly, BinPicking, Hammer），涵盖不同难度的任务。
    *   **真实机器人**：在AGIBOT G1机器人上部署，测试了**抓取放置瓶子**和**打开抽屉**两个接触丰富的任务。
*   **Benchmark与对比方法**：与三大类方法进行对比：
    1.  **生成策略**：Diffusion Policy (SOTA扩散方法), Flow Policy。
    2.  **能量基方法**：Implicit BC, EBT-Policy。
    3.  **逆强化学习方法**：EBIL, NEAR, IQ-Learn (代表非对抗或改进的对抗IRL方法)。
    4.  **自回归策略**：LSTM-GMM (传统基线)。

### **4. 资源与算力**
*   论文**未明确说明**训练所使用的具体GPU型号、数量及总训练时长。
*   仅在附录的推理效率对比中提到，延迟测试是在 **NVIDIA A100** GPU上进行的。可以推断实验使用了高性能计算资源，但缺乏详细的算力消耗报告。

### **5. 实验数量与充分性**
*   实验设计**较为充分和全面**，围绕六个研究问题展开：
    1.  **模仿性能**：在两个仿真基准的10个任务上对比所有基线方法。
    2.  **真实世界部署**：在2个真实机器人任务上验证。
    3.  **奖励质量**：将提取的奖励用于训练SAC智能体，与稀疏奖励、原始能量奖励、Oracle稠密奖励对比。
    4.  **分布外泛化**：系统性地增加初始状态扰动（4个级别），测试鲁棒性。
    5.  **敏感性分析**：分析奖励提取中关键超参数 `γ` 的影响。
    6.  **推理效率**：与基线比较成功率和延迟。
*   **客观性与公平性**：对比方法涵盖了当前主流的技术路线，并在相同的数据集和评估协议下进行测试。对于扩散和流匹配基线，使用了与本文方法相同的网络主干，以确保公平比较。实验结果为多次运行的平均值，并报告了标准差。

### **6. 论文的主要结论与发现**
*   **理论发现**：在最大熵最优性下，扩散模型的得分函数等价于专家软Q函数的梯度。约束学习场为保守场，能降低假设空间的复杂度，并提供更紧的分布外泛化理论边界。
*   **实证结论**：
    1.  **模仿性能**：EnergyFlow在仿真任务上达到了最先进的成功率，显著优于其他能量基方法和IRL方法。
    2.  **奖励有效性**：提取出的“中心化能量”奖励能有效引导下游RL训练，性能接近Oracle稠密奖励，显著优于稀疏奖励。
    3.  **泛化能力**：在分布外测试中，EnergyFlow的性能下降比非保守的基线方法更平缓，验证了保守约束的正则化效果。
    4.  **实用价值**：方法在真实机器人上能成功部署，且推理速度与高效的Flow Policy相当，兼具了能量模型的解释性和生成模型的速度。

### **7. 优点**
*   **理论贡献扎实**：建立了扩散得分匹配与逆强化学习之间清晰的理论桥梁，并提供了泛化误差分析，为方法奠定了坚实基础。
*   **方法优雅统一**：通过一个简单的标量能量函数参数化，同时解决了高质量动作生成和可解释奖励提取两个问题，概念清晰。
*   **实验全面且有说服力**：从仿真到真机，从模仿到强化学习，从性能到泛化，多角度验证了方法的有效性，并包含细致的消融和敏感性分析。

### **8. 不足与局限**
*   **奖励的固有模糊性**：如理论所指，恢复的奖励存在**状态依赖的偏移 `c(s)`**。虽然中心化整形缓解了其在同状态动作比较中的问题，但在需要跨状态绝对价值评估的序列决策中，这可能仍会引入偏差。
*   **理论假设的强依赖性**：核心定理依赖于“专家策略满足最大熵最优性”这一假设。在实际演示数据不完全最优或存在多模态时，该假设可能不严格成立，影响奖励恢复的准确性。
*   **计算开销**：虽然推理速度与流匹配相当，但训练时需要计算标量能量关于高维动作的梯度，并进行二阶优化，**计算成本可能高于标准的扩散策略训练**。
*   **任务范围**：实验集中于**短视程、状态空间相对低维**的机器人操作任务。在更复杂的、长期规划或高维视觉观察的任务中，方法的有效性和可扩展性有待进一步验证。

（完）
