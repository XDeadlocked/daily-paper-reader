---
title: "CoRAL: Contact-Rich Adaptive LLM-based Control for Robotic Manipulation"
title_zh: CoRAL：基于大型语言模型的接触丰富自适应机器人操作控制
authors: "Berk Çiçek, Mert K. Er, Özgür S. Öğüz"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02600v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 基于LLM的机器人操作控制与规划
tldr: "大型语言模型（LLM）和视觉语言模型（VLM）在机器人接触式操作中面临物理基础缺失和自适应控制能力不足的挑战。为此，本研究提出了CoRAL框架，它将高层推理与底层控制解耦，利用LLM设计上下文感知的代价函数，结合VLM提供语义先验并通过在线系统辨识实时细化物理参数，同时引入检索记忆单元复用成功策略。该框架在仿真和真实硬件上的新接触式任务中，平均成功率提升超过50%，有效弥合了语义推理与动态接触需求之间的差距。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02600v1/fig-001.webp\", \"caption\": \"Fig. 1: Real-world execution of CoRAL across six different manipulation tasks.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2605.02600v1/fig-002.webp\", \"caption\": \"Fig. 4: CoRAL on six different tasks with the tracked pose overlay of the object of interest.\", \"page\": 6, \"index\": 2, \"width\": 529, \"height\": 358}, {\"url\": \"assets/figures/arxiv/2605.02600v1/fig-003.webp\", \"caption\": \"Fig. 2: The conceptual workflow of CoRAL, illustrated with the “pick the cutting board” task.\", \"page\": 2, \"index\": 3, \"width\": 858, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2605.02600v1/fig-004.webp\", \"caption\": \"Fig. 5: Online parameter (mass) adaptation in simulation and real-world experiments.\", \"page\": 7, \"index\": 4, \"width\": 525, \"height\": 571}, {\"url\": \"assets/figures/arxiv/2605.02600v1/fig-005.webp\", \"caption\": \"TABLE I: Comparison against the baselines and ablation study. Performance is measured by success rate (x/10 trials).\", \"page\": 7, \"index\": 5, \"width\": 1076, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2605.02600v1/fig-006.webp\", \"caption\": \"Fig. 6: Real-World Force Regulation Profile (T4) The plot shows the measured end-effector force over time during the “Push with Constant Force” task where the intended push direction is +y. The shaded region represents the target force range the LLM is instructed with. CoRAL successfully modulates the control actions to maintain contact forces within the desired bounds.\", \"page\": 8, \"index\": 6, \"width\": 529, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2605.02600v1/fig-007.webp\", \"caption\": \"Fig. 3: The overall architecture of the CoRAL framework. Given an input image I , object models M and task description T , the vision module extracts world parameters θ. If the Memory Unit finds a similar successful experience, its retrieved plan (Jmem, Cmem) is used to guide the MPPI controller. Otherwise, the LLM (Task Formulation) module generates an initial plan (J0, C0). The system then enters the main execution cycle, which is governed by two nested feedback loops labeled (1) and (2). (1) The Inner Loop is a high-frequency re-planning cycle. At each step, the MPPI, guided by the current plan, generates an action ut based on the latest ‘State Metrics’. This loop (‘Retry’) continues until the task succeeds or a refinement is needed. (2) The Outer Loop is a low-frequency, high-level adaptation cycle. If the inner loop fails persistently, the ‘Refine’ path is taken, where the LLM (Online Adaptation) updates both the world model parameters (θ) for the ‘Planning World’ and the strategic ‘Refined Plan (Jt, Ct)’ for the MPPI. Successful episodes are stored back into the Memory Unit.\", \"page\": 4, \"index\": 7, \"width\": 979, \"height\": 314}]"
motivation: 解决LLM/VLM在接触式机器人操作中缺乏物理基础、无法自适应控制的问题，以弥合高层语义推理与底层动态接触需求之间的差距。
method: 提出CoRAL框架，使用LLM作为代价函数设计器而非直接控制器，结合VLM语义先验和在线系统辨识进行神经符号自适应，并通过基于检索的记忆单元复用策略。
result: "在仿真和真实硬件实验中，CoRAL在未见过的接触式任务上平均成功率提升超50%，优于现有VLA和基础模型规划基线，并能有效处理仿真到现实的差距。"
conclusion: 该研究通过将LLM/VLM的高层语义推理与底层自适应控制解耦，为接触式操作任务提供了一种稳定、可解释且能零样本泛化的新框架。
---

## 摘要
尽管大型语言模型（LLM）和视觉语言模型（VLM）在高层推理和语义理解方面展现出卓越能力，但由于缺乏显式的物理基础且无法执行自适应控制，将其直接应用于接触丰富的操作任务仍具挑战性。为弥合这一差距，我们提出了CoRAL（基于LLM的接触丰富自适应控制），这是一个模块化框架，通过将高层推理与底层控制解耦来实现零样本规划。与黑盒策略不同，CoRAL并非将LLM用作直接控制器，而是作为成本函数设计器，为基于采样的运动规划器（MPPI）合成上下文感知的目标函数。针对视觉数据中物理参数的模糊性，我们引入了神经符号自适应循环：VLM为环境动力学（如质量和摩擦估计）提供语义先验，随后通过在线系统辨识进行实时显式优化，同时LLM基于交互反馈迭代调整成本函数结构以修正策略性错误。此外，基于检索的记忆单元使系统能够在重复性任务中复用成功策略。这种分层架构通过将高层语义推理与反应式执行解耦，确保了实时控制的稳定性，有效弥合了缓慢的LLM推理与动态接触需求之间的差距。我们在仿真和真实硬件上对CoRAL进行了验证，涵盖了一系列具有挑战性的新颖任务，例如利用外部接触实现物体对墙翻转。实验表明，在未见过的接触丰富场景中，CoRAL平均将成功率提升超过50%，优于最先进的VLA和基于基础模型的规划基线，并通过其自适应物理理解有效处理了仿真到现实的差距。

## Abstract
While Large Language Models (LLMs) and Vision-Language Models (VLMs) demonstrate remarkable capabilities in high-level reasoning and semantic understanding, applying them directly to contact-rich manipulation remains a challenge due to their lack of explicit physical grounding and inability to perform adaptive control. To bridge this gap, we propose CoRAL (Contact-Rich Adaptive LLM-based control), a modular framework that enables zero-shot planning by decoupling high-level reasoning from low-level control. Unlike black-box policies, CoRAL uses LLMs not as direct controllers, but as cost designers that synthesize context-aware objective functions for a sampling-based motion planner (MPPI). To address the ambiguity of physical parameters in visual data, we introduce a neuro-symbolic adaptation loop: a VLM provides semantic priors for environmental dynamics, such as mass and friction estimates, which are then explicitly refined in real time via online system identification, while the LLM iteratively modulates the cost-function structure to correct strategic errors based on interaction feedback. Furthermore, a retrieval-based memory unit allows the system to reuse successful strategies across recurrent tasks. This hierarchical architecture ensures real-time control stability by decoupling high-level semantic reasoning from reactive execution, effectively bridging the gap between slow LLM inference and dynamic contact requirements. We validate CoRAL on both simulation and real-world hardware across challenging and novel tasks, such as flipping objects against walls by leveraging extrinsic contacts. Experiments demonstrate that CoRAL outperforms state-of-the-art VLA and foundation-model-based planner baselines by boosting success rates over 50% on average in unseen contact-rich scenarios, effectively handling sim-to-real gaps through its adaptive physical understanding.

---

## 论文详细总结（自动生成）

### 论文总结：CoRAL: Contact-Rich Adaptive LLM-based Control for Robotic Manipulation

#### 1. 核心问题与整体含义
*   **研究背景**：大型语言模型（LLM）和视觉语言模型（VLM）在机器人高层任务规划中展现出强大能力，但将其直接用于**接触丰富（Contact-Rich）** 的操作任务（如推、翻、靠墙翻转）时面临根本性挑战。
*   **核心问题**：LLM/VLM缺乏**显式的物理基础**，无法进行**自适应控制**。现有端到端的视觉-语言-动作（VLA）模型依赖大量演示数据，在需要主动利用接触力、应对未知物理参数的新场景中泛化能力差、且决策不透明。
*   **研究目标**：提出一个框架，弥合高层语义推理与底层动态、接触敏感的机器人控制之间的鸿沟，实现**零样本（Zero-Shot）** 的、可解释的接触式操作。

#### 2. 方法论
*   **核心思想**：**解耦与协同**。将LLM/VLM作为高层“策略家”和“诊断师”，而非直接的低层“控制器”，与一个基于采样的模型预测路径积分（MPPI）控制器协同工作。
*   **关键技术细节**：
    1.  **模块化架构**：
        *   **感知与初始化**：使用FoundationPose进行6-DoF物体姿态跟踪，并查询VLM根据视觉和任务描述生成物体质量、摩擦系数等物理参数的**语义先验**，共同构成内部世界模型参数 `θ`。
        *   **任务规划**：LLM作为“代价函数设计器”，将自然语言任务转化为MPPI可执行的**结构化代价函数 `J`**（如包含位置误差、接触惩罚、力约束等项的加权和）。同时，LLM生成**语义接触策略 `C`**，指定有希望的接触区域，大幅缩小MPPI的搜索空间。
        *   **记忆单元**：基于检索增强生成（RAG）存储成功的`(J, C, θ)`元组，在新任务中检索复用，实现经验积累。
    2.  **双层自适应循环**：
        *   **内循环（高频）**：MPPI控制器（10Hz）基于当前代价函数`J`和世界模型`θ`进行滚动时域规划，产生动作，并融合实时力/位姿反馈。
        *   **外循环（低频）**：当内循环持续失败时触发。LLM扮演“在线适应”角色，分析失败历史，执行两项关键操作：(a) **世界模型校正**：像系统辨识器一样更新物理参数信念`θ`（如修正质量估计）；(b) **策略精炼**：修改代价函数`J`的权重或结构，或提出新的接触策略`C`。
*   **流程简述**：给定RGB-D图像、物体模型和语言指令 → 感知模块构建带物理先验的内部世界模型 → 查询记忆或调用LLM生成初始`(J0, C0)` → MPPI内循环执行 → 成功则存储经验，失败则触发外循环LLM进行诊断与调整 → 循环直至任务完成。

#### 3. 实验设计
*   **测试环境与任务**：
    *   **仿真**：使用MuJoCo引擎的RoboSuite，操控7-DoF Franka Panda机械臂。设计了6个任务，涵盖从简单到复杂的接触操作：T1推抓砧板（多阶段）、T2拾取盒子、T3杂乱中拾放、T4恒力推动、T5翻转盒子、T6靠墙翻转（需利用环境接触）。
    *   **真实验证**：在真实的Franka Panda机械臂上部署，使用运动捕捉进行状态估计，验证仿真到现实的迁移能力。
*   **评估指标**：主要使用**成功率**（10次随机初始化的 trials）。
*   **对比方法**：
    *   **SOTA端到端VLA模型**：OpenVLA-OFT, π0.5。
    *   **SOTA基于基础模型的规划器**：Language-to-Rewards (L2R)（静态LLM生成代价函数）。
    *   **人工设计代价函数基线**：单阶段专家设计、有限状态机（FSM）多阶段专家设计（作为性能上限）。
    *   **内部消融实验**：CoRAL (w/o Memory)、CoRAL (w/o Refinement)、CoRAL (Unified VLM)、CoRAL (w/o Pose Tracking)。

#### 4. 资源与算力
*   论文未提及大规模模型训练过程，因其采用**零样本**设定，直接调用预训练的GPT-4o API作为LLM/VLM。
*   **实验运行配置**（附录C提及）：桌面PC，配备Intel Core i9-13900K CPU, 64GB RAM，**单张NVIDIA RTX 4060 Ti GPU**。MPPI规划在CPU上并行化（`K=256`条轨迹）。

#### 5. 实验数量与充分性
*   **实验数量充足**：在6个核心任务上，每个任务进行10次随机化试验。与4类外部基线（共5种方法）和4种内部消融变体进行了全面对比。
*   **实验设计客观公平**：
    *   **任务多样性**：涵盖了拾放、多阶段序列操作、力控、动态翻转、环境接触利用等多种挑战。
    *   **基线选择合理**：既对比了主流的端到端方法，也对比了最相关的LLM-for-Control方法（L2R），并设置了人工性能上限。
    *   **消融实验系统**：逐一验证了记忆、在线优化、VLM/LLM角色分离、专用姿态估计器等核心组件的必要性。
    *   **仿真与真实验证结合**：增强了结论的可信度。总体而言，实验设计较为全面、客观，能有效支撑其论点。

#### 6. 主要结论与发现
*   **性能优势**：CoRAL在**接触丰富的复杂任务（T1, T4, T5, T6）上显著优于所有SOTA基线**，平均成功率提升超50%。在简单任务上与基线相当。其性能接近需要大量手工调试的专家FSM设计。
*   **关键能力验证**：
    1.  **零样本与泛化**：无需任务特定演示数据，即可处理新颖的接触式操作。
    2.  **在线自适应**：外循环能有效修正错误的物理参数估计和策略，是应对仿真到现实差距和VLM“幻觉”的关键。
    3.  **可解释性**：LLM在优化循环中能提供自然语言失败诊断和修正说明。
    4.  **经验复用**：记忆模块能显著提升在相似任务上的性能和收敛速度。

#### 7. 优点
*   **架构创新**：提出了一个清晰、解耦的神经符号架构，将LLM的语义规划优势与MPPI的物理模型、实时控制优势有机结合。
*   **角色重定义**：创造性将LLM用作“代价函数设计器”和“在线诊断师”，而非直接控制器，解决了LLM推理慢、缺乏物理基础的根本问题。
*   **实现零样本自适应**：通过在线系统辨识和策略优化，摆脱了对大规模接触/力觉演示数据的依赖，数据效率高。
*   **兼具性能与可解释性**：在取得高性能的同时，提供了黑盒端到端模型所缺乏的决策透明度。

#### 8. 不足与局限
*   **依赖内部世界模型精度**：整个系统的性能高度依赖于感知模块（姿态估计、VLM先验）构建的内部仿真世界的准确性。严重的初始误差可能导致规划失败。
*   **LLM生成代价函数的可靠性**：依赖通用LLM（如GPT-4o）生成有效的代价函数，对于极其抽象或超出LLM知识范围的任务，其生成质量无法保证。
*   **延迟问题**：虽然通过分层控制缓解，但外循环的LLM调用（~1Hz）仍会引入延迟，在需要极快速高层重新规划的超动态场景中可能受限。
*   **感知模块的瓶颈**：真实实验中用运动捕捉替代了文中的FoundationPose，表明在复杂真实场景中，鲁棒的6-DoF姿态跟踪本身仍是一个挑战。
*   **计算成本**：虽然无需训练，但运行需要调用商用LLM API，并需进行大量并行MPPI滚动计算，实时运行成本较高。

（完）
