---
title: "FIS-DiT: Breaking the Few-Step Video Inference Barrier via Training-Free Frame Interleaved Sparsity"
title_zh: FIS-DiT：通过免训练的帧交错稀疏性突破少步视频推理壁垒
authors: "Jian Tang, Jiawei Fan, Qingbin Liu, Zheng Wei"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11869v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 通过帧间交错稀疏性加速视频扩散Transformer
tldr: 视频扩散变换器（DiT）的推理延迟可通过模型蒸馏降低，但每步推理延迟仍是瓶颈，尤其在少步场景中，时间状态稀缺导致现有基于去噪轨迹的加速方法收益递减。为此，本研究提出帧交错稀疏性DiT（FIS-DiT），一种无需训练且与算子无关的框架，将优化焦点从时间轨迹转移到潜在帧维度，利用帧间稀疏性和结构一致性，通过帧交错稀疏性（FIS）执行策略在模型层次中操作帧子集，实现高效计算。实验表明，该方法在多个基准上实现了2.11-2.41倍加速且性能下降可忽略，为实时高清视频生成提供了可扩展方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11869v1/fig-001.webp\", \"caption\": \"Figure 4: Overview of FIS-DiT.\", \"page\": 6, \"index\": 1, \"width\": 826, \"height\": 364}, {\"url\": \"assets/figures/arxiv/2605.11869v1/fig-002.webp\", \"caption\": \"Figure 5: Qualitative comparison on Wan 2.2 under the 4-step 720p setting across three diverse prompts.\", \"page\": 7, \"index\": 2, \"width\": 829, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2605.11869v1/fig-003.webp\", \"caption\": \"Figure 1: Prompt-agnostic frame dynamics. Adjacent-frame relative changes ∆rel i→i+1 remain nearly uniform across time and prompts on both models.\", \"page\": 3, \"index\": 3, \"width\": 829, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2605.11869v1/fig-004.webp\", \"caption\": \"Table 1: Quantitative comparison of inference efficiency and visual quality. MagCache-Fast uses the default caching threshold; MagCache-Force relaxes the threshold to force step skipping. FISBalanced and FIS-Fast denote conservative and aggressive FIS-DiT configurations, respectively.\", \"page\": 8, \"index\": 4, \"width\": 818, \"height\": 636}, {\"url\": \"assets/figures/arxiv/2605.11869v1/fig-005.webp\", \"caption\": \"Table 2: Ablation on interleaved frame scheduling and stride n.\", \"page\": 9, \"index\": 5, \"width\": 694, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2605.11869v1/fig-006.webp\", \"caption\": \"Figure 3: Interpolation error verification. Anchor frames (blue) incur <1% error; interpolated frames (red) show bounded, flat errors with no temporal drift.\", \"page\": 5, \"index\": 6, \"width\": 829, \"height\": 228}]"
motivation: 在少步推理场景中，时间状态稀缺导致现有基于时间轨迹的加速方法收益递减，需要从潜在帧维度寻找优化突破口。
method: 提出了一种无需训练、与算子无关的框架，通过帧交错稀疏性（FIS）执行策略，在模型层次中操作帧子集，无需全规模块计算即可刷新所有潜在位置。
result: 在Wan 2.2和HunyuanVideo 1.5上的实验表明，FIS-DiT在VBench-Q和CLIP指标上性能下降可忽略的情况下，持续实现了2.11-2.41倍的加速。
conclusion: 该框架为实时高清视频生成提供了一条可扩展且稳健的路径。
---

## 摘要
尽管视频扩散变换器（DiTs）的整体推理延迟可通过模型蒸馏大幅降低，但单步推理延迟仍是关键瓶颈。现有加速范式主要利用去噪轨迹中的冗余性；然而，我们发现这些逐步策略在少步数场景中面临收益递减的局限。在此类场景中，时间状态的稀缺性阻碍了有效的特征重用或预测建模，构成了进一步加速的巨大障碍。为克服此问题，我们提出帧交错稀疏性DiT（FIS-DiT），这是一种无需训练且与算子无关的框架，将优化重点从时间轨迹转向潜在帧维度。我们的方法基于该维度内的一种内在对偶性：存在允许减少计算的帧级稀疏性，同时每个帧位置对全局时空上下文的重要性保持同等关键的结构一致性。基于这一洞见，我们实现帧交错稀疏性（FIS）作为一种执行策略，在模型层次结构中操作帧子集，无需全规模块计算即可刷新所有潜在位置。在Wan 2.2和HunyuanVideo 1.5上的实证评估表明，FIS-DiT在VBench-Q和CLIP指标上以可忽略的退化持续实现2.11-2.41倍加速，为实时高清视频生成提供了可扩展且稳健的途径。

## Abstract
While the overall inference latency of Video Diffusion Transformers (DiTs) can be substantially reduced through model distillation, per-step inference latency remains a critical bottleneck. Existing acceleration paradigms primarily exploit redundancy across the denoising trajectory; however, we identify a limitation where these step-wise strategies encounter diminishing returns in few-step regimes. In such scenarios, the scarcity of temporal states prevents effective feature reuse or predictive modeling, creating a formidable barrier to further acceleration. To overcome this, we propose Frame Interleaved Sparsity DiT (FIS-DiT), a training-free and operator-agnostic framework that shifts the optimization focus from the temporal trajectory to the latent frame dimension. Our approach is motivated by an intrinsic duality within this dimension: the existence of frame-wise sparsity that permits reduced computation, coupled with a structural consistency where each frame position remains equally vital to the global spatiotemporal context. Leveraging this insight, we implement Frame Interleaved Sparsity (FIS) as an execution strategy that manipulates frame subsets across the model hierarchy, refreshing all latent positions without requiring full-scale block computation. Empirical evaluations on Wan 2.2 and HunyuanVideo 1.5 demonstrate that FIS-DiT consistently achieves 2.11--2.41$\times$ speedup with negligible degradation across VBench-Q and CLIP metrics, providing a scalable and robust pathway toward real-time high-definition video generation.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，为您生成一份结构化的详细中文总结。

---

### **论文总结：FIS-DiT**

#### **1. 核心问题与整体含义**
*   **研究背景**：视频扩散Transformer（DiT）通过模型蒸馏技术，已能将生成视频所需的去噪步数大幅减少（如4步或8步），从而降低整体推理延迟。然而，**每一步（单步）的推理延迟**，尤其是处理高分辨率视频潜在表示时庞大的时空Transformer计算，已成为新的关键瓶颈。
*   **现有方法的局限**：当前主流的免训练加速方法（如DeepCache、MagCache等）主要利用**相邻去噪步之间的特征冗余性**，通过跨步缓存、重用或预测来减少计算。但在**少步数**场景下，相邻步之间的状态数量稀少且特征差异巨大，导致这些基于时间轨迹的加速方法收益急剧下降，甚至失效。
*   **核心问题**：如何突破少步视频生成场景下的单步推理延迟瓶颈？论文提出需要将优化焦点从**时间轨迹维度**转向**潜在帧维度**，挖掘同一去噪步内不同视频帧之间的计算冗余。

#### **2. 方法论**
*   **核心思想**：FIS-DiT的核心思想是利用视频DiT内部特征在**潜在帧维度上存在的强时空冗余性**。具体而言，在同一Transformer块的中间层，相邻潜在帧的特征变化平缓且均匀，这使得**稀疏计算部分帧，并通过轻量级插值重建完整帧序列**成为可能。
*   **关键技术细节**：
    1.  **帧交错稀疏性**：在选定的“稳定”Transformer块中，不计算所有帧，而是按固定步长`n`（如n=3）选择一部分“锚点帧”进行计算。
    2.  **轻量级重建**：对于未计算的“跳过帧”，使用其前后两个锚点帧的特征进行**线性插值**来重建。
    3.  **交错调度**：为了避免同一帧在多个连续层中被持续跳过导致误差累积，FIS-DiT在不同层间**循环偏移锚点帧的选择**。这样，在连续的`n`个稀疏块中，每个非边界帧都会至少被精确计算一次。
    4.  **风险感知部署**：
        *   **块感知**：根据实证观察（图2），仅对特征变化平缓的**中间层块**启用稀疏计算，而对变化剧烈的**头部和尾部层块**保持全帧计算。
        *   **步感知**：在去噪过程的**最后几步**（噪声水平低，对误差敏感）禁用稀疏计算，以保证生成质量。
*   **算法流程**：对于每个去噪步`t`和每个Transformer块`l`：
    1.  判断是否满足启用稀疏计算的条件（`l`属于中间层且`t`不是最后几步）。
    2.  若启用，则根据当前块索引`l`和步长`n`，通过取模运算确定本层的锚点帧索引集合`A_l`。
    3.  从输入特征`X_l`中收集锚点帧`X_l^anc`，送入Transformer模块`Φ_m^l`计算得到输出`Y_l^anc`。
    4.  对`Y_l^anc`进行插值，重建出完整帧序列的输出`X_{l+1}`。
    5.  若不启用，则进行标准的全帧计算。

#### **3. 实验设计**
*   **基准模型与场景**：
    *   **Wan 2.2** (14B参数): 4步文生视频，测试了480p和720p分辨率。
    *   **HunyuanVideo 1.5** (8B参数): 8步图生视频，测试了480p分辨率。
*   **评估指标**：
    *   **效率**：FLOPs、端到端推理延迟、加速比。
    *   **质量**：CLIP-Score（文本-视频对齐）、VBench-Q（综合视频质量，包括时序一致性、运动平滑度等）。
*   **对比方法**：与代表性的**跨步缓存**方法**MagCache**进行对比。设置了两个配置：
    *   **MagCache-Fast**：使用默认缓存阈值，在少步场景下几乎无法触发缓存（加速比1.0x）。
    *   **MagCache-Force**：强制降低缓存阈值以触发重用，以获得加速，但会牺牲质量。

#### **4. 资源与算力**
*   论文中**未提及**训练过程，因为FIS-DiT是**免训练**的推理加速框架。
*   **推理实验硬件**：
    *   对于Wan 2.2模型，使用**单张NVIDIA H20** GPU。
    *   对于HunyuanVideo 1.5模型，使用**单张NVIDIA L20** GPU。
*   **其他资源**：未明确说明实验的具体耗时或总计算量。

#### **5. 实验数量与充分性**
*   **主实验**：在两个不同的骨干模型（Wan 2.2, HunyuanVideo 1.5）、两种分辨率、两种生成范式（T2V, I2V）上进行了全面评估，共报告了**6组主要数据**（见表1）。
*   **消融实验**：
    1.  **交错调度有效性**：对比了使用与不使用交错调度在不同稀疏步长（n=3,4,5）下的性能。
    2.  **风险感知部署**：系统性地移除了“块感知保护”和“步感知保护”，验证了其必要性。
*   **组合实验**：测试了FIS-DiT与MagCache-Force的结合效果，探索了不同加速范式的互补性。
*   **客观性与公平性**：
    *   使用了公开的基准模型和标准评估指标（VBench, CLIP）。
    *   对比基线时，对MagCache进行了参数调优以获取其最佳表现。
    *   实验基于100个多样化的文本提示进行，结果具有统计意义。
    *   **结论**：实验设计较为充分、客观，能够有效支撑论文的核心主张。

#### **6. 主要结论与发现**
1.  **有效性**：FIS-DiT在少步视频生成场景下，能够实现**约2倍的稳定加速**（2.11x–2.41x），同时在CLIP和VBench-Q指标上**质量损失可忽略不计**。
2.  **超越现有方法**：在相同的少步设置下，基于跨步冗余的缓存方法（如MagCache）要么无法加速，要么在加速时带来明显的质量下降。FIS-DiT通过挖掘**帧内冗余**，突破了这一瓶颈。
3.  **互补性**：FIS-DiT与跨步缓存方法是**正交且互补**的。将它们结合（如FIS-Balanced + MagCache-Force）能获得**更高的加速比**（最高达2.48x）。
4.  **关键设计有效**：消融实验证实了**交错调度**和**风险感知部署**（保护头尾层和最后去噪步）对于平衡效率与质量至关重要。

#### **7. 优点**
*   **视角新颖**：首次系统性地将视频DiT的加速焦点从“时间轨迹”转移到“潜在帧维度”，为少步推理瓶颈提供了新的解决思路。
*   **实用性强**：
    *   **免训练**：无需任何模型微调或重新训练，可直接部署于现有DiT pipeline。
    *   **算子无关/硬件友好**：在帧级别进行稀疏化，保持了每个帧内空间token的连续性，因此可以直接利用高度优化的现有内核（如FlashAttention），无需定制CUDA代码。
    *   **可扩展**：框架设计通用，可与其他加速技术（如蒸馏、缓存）叠加使用。
*   **设计精巧**：提出的“交错调度”机制巧妙避免了误差累积；“风险感知部署”基于对模型动力学的实证观察，设计合理。

#### **8. 不足与局限**
*   **稀疏策略固定**：当前采用均匀间隔的固定稀疏模式（步长`n`）和简单的线性插值。对于运动复杂或场景切换剧烈的视频，**自适应的、内容感知的稀疏策略**和**更强大的重建模块**可能取得更好的效果。
*   **性能上限**：加速比受限于模型中“稳定”中间层的比例。对于本身层数较少或中间层冗余度不高的模型，加速潜力可能受限。
*   **实验覆盖范围**：虽然测试了两个主流模型，但未来需要在更多样化的视频DiT架构（如不同规模、不同设计）上进行验证，以证明其普适性。
*   **潜在偏差**：方法依赖于对模型中间特征“平稳性”的经验性观察。虽然论文在两个模型上验证了该特性，但其理论保证和在不同数据分布下的鲁棒性仍需进一步探究。

（完）
