---
title: "OA-WAM: Object-Addressable World Action Model for Robust Robot Manipulation"
title_zh: OA-WAM：面向鲁棒机器人操作的对象可寻址世界动作模型
authors: "Yushan Liu, Peibo Sun, Shoujie Li, Yifan Xie, Lingfeng Zhang, Xintao Chao, Shiyuan Dong, Fang Chen, Xiao-Ping Zhang, Wenbo Ding"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06481v1"
tags: ["query:sr-0501"]
score: 10.0
evidence: 用于机器人操作的对象可寻址世界动作模型
tldr: 本文针对现有世界动作模型（WAMs）在指令涉及特定对象时难以处理场景变化的问题，提出了OA-WAM（对象可寻址世界动作模型）。该方法将每帧分解为对象槽和机器人槽，通过地址向量实现对象寻址，分离对象身份与状态，并联合预测场景演化和机器人动作。实验表明，OA-WAM在多个基准上达到或超越先进基线，验证了可寻址对象状态对鲁棒操作的效力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06481v1/fig-001.webp\", \"caption\": \"Figure 6: Architectural attribution of OA-WAM’s robustness gains. Decomposition of the 2×2 factorial behind Tab. 7. (a) LIBERO / LP-camera / LP-avg success rates across the four variants. V2 is the slot-only control (full perception stack, OA constraint OFF); V0 is full OA-WAM. The OA constraint adds +20.0% on the most geometric axis (LP-camera) while LIBERO standard moves only marginally, isolating the gain to OOD-specific routing rather than added capacity. (b) Same variants on the swap-binding cosine (Tab. 4); the dotted line marks the maximum cosine reached by any of the eight holistic VLA / WAM baselines.\", \"page\": 25, \"index\": 1, \"width\": 813, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2605.06481v1/fig-002.webp\", \"caption\": \"Table 8: Diagnostic metrics on LIBERO-Plus. Target attn. is the mean attention mass that role query r1 places on the ground-truth target slot. Swap binding is the cosine alignment between the end-effector residual trajectory and the swap direction. The failure column reports the proportion of evaluation episodes that fail due to exposed perception or policy errors; holistic baselines have no exposed perception cache, so all their failures are charged to the policy side.\", \"page\": 26, \"index\": 2, \"width\": 736, \"height\": 193}, {\"url\": \"assets/figures/arxiv/2605.06481v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of OA-WAM. Under scene perturbations (left, six typical axes), holistic WAMs entangle target identity with context in global tokens and drift to wrong actions (top right). Our OA-WAM (bottom right) decomposes each frame into N+1 addressable object slots whose cross-slot attention key reads only the identity address subvector, keeping robust manipulation.\", \"page\": 2, \"index\": 3, \"width\": 826, \"height\": 469}, {\"url\": \"assets/figures/arxiv/2605.06481v1/fig-004.webp\", \"caption\": \"Table 1: Comparison on LIBERO and SimplerEnv (WidowX, Visual Matching). All numbers are success rates (%).Best results are in bold, and second-best are underlined.\", \"page\": 7, \"index\": 4, \"width\": 830, \"height\": 477}, {\"url\": \"assets/figures/arxiv/2605.06481v1/fig-005.webp\", \"caption\": \"Figure 4: Main results. Left: LIBERO-Plus radar over the seven perturbation axes (Tab. 2); Right: SimplerEnv WidowX (Bridge) per-task success (Tab. 1). OA-WAM sets a new SOTA on the geometric LIBERO-Plus axes (Geo-Avg 84.3, +4.8% over π0.5) and on SimplerEnv (79.3 avg).\", \"page\": 7, \"index\": 5, \"width\": 821, \"height\": 449}, {\"url\": \"assets/figures/arxiv/2605.06481v1/fig-006.webp\", \"caption\": \"Figure 2: OA-WAM architecture. Multi-modal inputs are encoded into each token streams: objectslot tokens via SAM3+DINOv3, projected by a learnable slot adapter; Only slot tokens introduce learnable parameters; the others reuse frozen embed_tokens. Tokens are assembled into a blockcausal sequence terminated by a learnable action query [ACT-Q] and processed by the slot-aware backbone. The world head reads slot hiddens to predict next-frame per-slot ( ˆcnt, ˆpose) as auxiliary supervision; the action head decodes a 16-step action chunk.\", \"page\": 3, \"index\": 6, \"width\": 735, \"height\": 579}, {\"url\": \"assets/figures/arxiv/2605.06481v1/fig-007.webp\", \"caption\": \"Figure 9: LIBERO-Plus perturbation gallery. We visualize the seven perturbation axes of LIBEROPlus (columns: Objects Layout, Background Textures, Light Conditions, Camera Viewpoints, Robot Initial States, Language Instructions, Sensor Noise) applied to four LIBERO suites (rows: Spatial, Object, Goal, Long-horizon). Each row anchors on a single base scene so that the seven panels in that row share identical objects and goal, isolating the visual effect of each axis. The Layout column adds distractor objects, Background swaps table/floor textures, Light varies illumination intensity and color, Camera shifts the agent-view extrinsics, and Robot init perturbs the manipulator’s starting joint configuration. Language instructions are text-only and therefore look visually identical to the anchor scene; the perturbation manifests only through the prompt fed to the policy. The Noise column intentionally uses a different corruption per row — motion blur, Gaussian blur, zoom blur, and glass blur, respectively — to illustrate the diversity of sensor-noise variants in LIBERO-Plus.\", \"page\": 28, \"index\": 7, \"width\": 826, \"height\": 462}, {\"url\": \"assets/figures/arxiv/2605.06481v1/fig-008.webp\", \"caption\": \"Table 2: Comparison on the LIBERO-Plus robustness benchmark (zero-shot, train on standard LIBERO only). Success rate (%) over the seven perturbation axes (Camera / Robot init / Layout / Light / Background / Language / Sensor noise). Geo Avg = mean of Camera/Robot/Layout (the geometric axes per [19]). Baseline rows are from the zero-shot evaluation of [94] or each method’s original report. Best results are in bold, and second-best are underlined.\", \"page\": 8, \"index\": 8, \"width\": 829, \"height\": 482}, {\"url\": \"assets/figures/arxiv/2605.06481v1/fig-009.webp\", \"caption\": \"Figure 5: Mechanism diagnostics (A1, A2). (a) LP-camera success vs. camera-shift angle ∆θ: V0 (full OA-WAM) and V1 (key mask off) overlap in-distribution and split as ∆θ grows. (b) Role-query attention from r1-4 (target/reference/tool/distractor) over slot types, averaged over 300 LIBEROSpatial episodes. (c) End-effector trajectory under an A2 address swap: OA-WAM deflects toward the swapped target, the holistic baseline does not (cosines in Tab. 4).\", \"page\": 8, \"index\": 9, \"width\": 822, \"height\": 228}, {\"url\": \"assets/figures/arxiv/2605.06481v1/fig-010.webp\", \"caption\": \"Figure 8: Representative tasks from the four SimplerEnv WidowX (Bridge) suites [40]: Spoon-Towel (the spoon must be lifted from the table and placed on the towel), Carrot-Plate (the carrot must be placed on the yellow plate), Stack-Cube (the green block must be stacked on the yellow block), and Eggplant-Basket (the eggplant must be deposited inside the yellow basket).\", \"page\": 27, \"index\": 10, \"width\": 756, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2605.06481v1/fig-011.webp\", \"caption\": \"Figure 7: Representative tasks from the four LIBERO suites [45]: Spatial (10 tasks, identical goal with varied initial spatial layout), Object (10 tasks, same scene with varied target object), Goal (10 tasks, same scene with varied goal predicate), and Long (10 long-horizon composite tasks).\", \"page\": 27, \"index\": 11, \"width\": 826, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2605.06481v1/fig-012.webp\", \"caption\": \"Table 4: A2 – causal slot intervention (swap binding). Test-time address swap between the language-bound target slot and another in-scene slot, all other inputs fixed; metric is cosine alignment between the end-effector residual trajectory and the displacement direction toward the swapped target.\", \"page\": 9, \"index\": 12, \"width\": 406, \"height\": 323}]"
motivation: 现有的世界动作模型（WAMs）通常将预测的世界表示为整体图像、视频令牌或全局潜在表示，这使得当指令涉及特定对象时，动作解码器难以处理，尤其是在场景变化导致对象身份与上下文纠缠的情况下。
method: 提出OA-WAM模型，将每帧分解为N+1个槽状态（一个机器人槽和N个对象槽），每个槽包含持久地址向量和时变内容向量，并通过块因果序列与文本、图像、本体感觉和过去动作令牌融合，使用世界头预测下一帧槽状态，同时通过流匹配动作头解码连续动作块。
result: "OA-WAM在LIBERO（97.8%）和SimplerEnv（79.3%）上匹配了强大的VLA和WAM基线，在LIBERO-Plus几何轴上达到了最先进的性能，在七轴聚合上保持竞争力，因果槽干预测试的交换绑定余弦为0.87，而整体基线最多为0.09。"
conclusion: 该研究表明，可寻址的对象状态为场景扰动下的鲁棒世界-动作建模提供了有效的接口。
---

## 摘要
世界动作模型（WAMs）通过联合预测场景演变和机器人动作来增强视觉-语言-动作策略，但现有方法通常将预测的世界表示为整体图像、视频标记或全局潜在表示。当指令涉及特定对象时，尤其是在场景变化导致对象身份与上下文纠缠的情况下，这些表示难以被动作解码器寻址。我们提出了OA-WAM，一种用于鲁棒机器人操作的对象可寻址世界动作模型。OA-WAM将每一帧分解为N+1个槽状态，包括一个机器人槽和N个对象槽。每个槽包含一个持久地址向量和一个时变内容向量，并与文本、图像、本体感觉和过去动作标记在块因果序列中融合。世界头预测下一帧的槽状态，而流匹配动作头在同一前向传递中解码一个16步的连续动作块。通过仅使用地址键路由跨槽注意力，并在每个Transformer层重置地址切片，强制实现可寻址性，从而将“对哪个对象操作”与“该对象当前状态”分离，无需添加额外标记。OA-WAM在LIBERO（97.8%）和SimplerEnv（79.3%）上匹配了强大的VLA和WAM基线，在最相关的LIBERO-Plus几何轴上达到了最先进的性能，并在七轴聚合指标上保持竞争力。因果槽干预测试的交换绑定余弦值为0.87，而整体基线的最大值仅为0.09。这些结果表明，可寻址的对象状态为场景扰动下的鲁棒世界动作建模提供了有效接口。

## Abstract
World Action Models (WAMs) enhance Vision-Language-Action policies by jointly predicting scene evolution and robot actions, but existing methods usually represent the predicted world as holistic images, video tokens, or global latents. These representations are difficult for an action decoder to address when an instruction refers to a particular object, especially under scene shifts where object identity is entangled with context. We propose OA-WAM, an Object-Addressable World Action Model for robust robot manipulation. OA-WAM decomposes each frame into N+1 slot states, with one robot slot and N object slots. Each slot contains a persistent address vector and a time-varying content vector, and is fused with text, image, proprioception, and past-action tokens in a block-causal sequence. A world head predicts next-frame slot states, while a flow-matching action head decodes a 16-step continuous action chunk in the same forward pass. Addressability is enforced by routing cross-slot attention through address-only keys and resetting the address slice at every transformer layer, separating which object to act on from what that object currently is without adding extra tokens. OA-WAM matches strong VLA and WAM baselines on LIBERO (97.8%) and SimplerEnv (79.3%), reaches state-of-the-art performance on the most relevant LIBERO-Plus geometric axes, and remains competitive on the seven-axis aggregate. A causal slot-intervention test yields a swap-binding cosine of 0.87, versus at most 0.09 for holistic baselines. These results suggest that addressable object states provide an effective interface for robust world-action modeling under scene perturbations.

---

## 论文详细总结（自动生成）

好的，我将基于您提供的论文内容，按照您要求的要点生成一份详细的结构化中文总结。

### 1. 核心问题与整体含义
*   **研究背景**：机器人操作本质上是对象层面的任务。当前，视觉-语言-动作模型和世界动作模型在标准基准上表现出色，但在面对**场景扰动**时（如相机视角、物体布局、光照变化），性能会急剧下降。这表明模型并未真正理解语言指令所指的**特定对象**，而是将目标选择与训练时的整体视觉上下文进行了绑定。
*   **核心问题**：现有世界动作模型大多使用**整体性表示**来预测未来世界，这使得对象身份与外观、姿态及周围环境信息在表征中**纠缠在一起**。当场景发生几何变化时，目标对象虽然仍在视野中，但其在整体表征中的“身份信号”已被污染，导致动作解码器选错目标。论文将此定义为模型缺乏**对象可寻址性**。

### 2. 方法论：OA-WAM
*   **核心思想**：将世界模型显式地组织为**可独立寻址的对象状态**，为语言引导的动作生成提供一个稳定的、对象层面的查询接口。
*   **关键技术细节**：
    1.  **对象槽分词**：利用冻结的基础模型将每一帧分解为 N+1 个槽（1个机器人槽 + N个对象槽）。每个槽的向量由四部分组成：
        *   `addr_k`：**地址向量**。根据语言标签和初始帧的DINOv3特征计算，在整个任务 episode 中**固定不变**，代表对象身份。
        *   `cnt_tk`：**内容向量**。每帧根据SAM3掩码和DINOv3特征重新计算，代表对象的时变状态。
        *   `π_t`：帧索引的位置编码。
        *   `ρ_k`：角色嵌入。
    2.  **统一序列构建**：将上述对象槽与文本、图像VQ、本体感觉、历史动作等共六路 token 流，组装成一个**块因果序列**，输入一个7B参数的Chameleon风格主干网络。
    3.  **对象可寻址注意力约束**：这是实现可寻址性的**核心机制**，在每个Transformer层对槽位置执行两个无参数操作：
        *   **地址键投影**：在计算注意力键时，只使用槽向量的前32维（即`addr_k`）进行投影。
        *   **地址流重置**：在前向传播后，用一个钩子将残差流中槽向量的地址部分重置回缓存的`addr_k`。
        *   **效果**：上述约束确保了跨槽注意力的**路由决策**仅依赖于固定的对象身份地址，而与对象当前的外观、姿态等内容无关，从而在张量层面实现了“对哪个对象操作”与“该对象当前状态”的**解耦**。
    4.  **预测头**：
        *   **世界头**：读取槽的隐藏状态，预测下一帧每个槽的内容和姿态，作为辅助监督。
        *   **动作头**：一个基于流匹配的MLP，读取序列末尾的特殊查询令牌`[ACT_Q]`，解码出一个16步的连续动作块。

### 3. 实验设计
*   **数据集与基准**：
    1.  **LIBERO**：标准室内操作基准，用于评估**分布内**性能。
    2.  **SimplerEnv (WidowX)**：视觉匹配基准，用于评估从仿真到真实视觉域的泛化能力。
    3.  **LIBERO-Plus**：**鲁棒性基准**，在七个受控的扰动轴上（相机、机器人初始状态、物体布局、光照、背景、语言、传感器噪声）评估模型，**训练时仅使用标准LIBERO数据**。
*   **对比方法**：与当前最先进的**VLA模型**和**WAM模型**进行了全面对比，包括 π0、π0.5、OpenVLA、VLA-JEPA、Cosmos-Policy、GE-Act等。

### 4. 资源与算力
论文明确描述了三级训练流程所需的算力：
*   **Stage 0（主干网络预训练）**：使用 **384 块 NVIDIA A100-80GB GPU**，训练约60万步，耗时约 **18天**（总计约 **16.6万 A100-小时**）。数据混合了网络图文数据和机器人数据集。
*   **Stage I（槽适配器对齐）**：使用 **8 块 A100**，训练5万步，耗时 **3-4天**。
*   **Stage II（全系统微调）**：使用 **8 块 A100**，采用LoRA技术微调约12.7M参数，训练10万步，耗时 **3-4天**。

### 5. 实验数量与充分性
实验设计**全面且客观**，主要包括：
*   **主实验**：在三个基准（LIBERO, SimplerEnv, LIBERO-Plus）上与众多SOTA基线进行对比，结果以平均成功率和置信区间报告。
*   **消融实验**：
    *   **A1（可寻址性隔离）**：进行了2x2因子实验，分别关闭“地址键投影”和“地址流重置”，清晰证明了OA约束是几何扰动鲁棒性的主要来源，而非感知前端带来的容量提升。
    *   **A2（因果槽干预）**：通过交换目标槽地址并观察末端执行器轨迹偏移，直接验证了策略确实通过地址子空间进行目标选择（交换绑定余弦0.87 vs. 基线 ≤0.09）。
    *   **A3/A4**：验证了世界预测头和干扰物一致性损失的有效性。
*   **诊断分析**：提供了注意力可视化、鲁棒性曲线、失败模式分析等。
*   **公平性**：在LIBERO-Plus上所有方法均使用相同的零样本评估协议（仅用标准LIBERO训练），对比基线数据来自原论文或权威复现，确保了公平比较。

### 6. 主要结论与发现
1.  **性能匹配**：在标准基准LIBERO和SimplerEnv上，OA-WAM达到了与SOTA模型相当甚至略优的性能。
2.  **鲁棒性领先**：在LIBERO-Plus上，OA-WAM在**几何扰动轴**上表现最佳，特别是在相机和机器人初始状态扰动上创造了新SOTA，验证了其核心假设。
3.  **机制验证**：消融实验和因果干预测试强有力地证明，其鲁棒性增益来源于**对象可寻址的架构设计**，而非额外的模型容量或数据。
4.  **核心结论**：显式地将世界模型组织为可寻址的对象状态，是实现场景几何扰动下鲁棒机器人操作的一条有效路径。

### 7. 优点
*   **问题定义清晰**：精准地将鲁棒性不足归因于“对象可寻址性”的缺失。
*   **方法设计优雅**：通过“地址键投影”和“地址流重置”两个简单的、无参数的张量级操作，在主流Transformer架构内优雅地实现了对象寻址的**架构级保障**。
*   **证据链扎实**：实验设计形成了完整的证据闭环：主实验证明有效性，消融实验隔离关键因素，因果干预直接验证内部机制。
*   **兼顾性能与效率**：在提升鲁棒性的同时，未牺牲分布内性能，且通过LoRA微调大幅降低了训练成本。

### 8. 不足与局限
*   **感知依赖**：模型的成功**严重依赖上游感知模块**（SAM3, DINOv3, Qwen3-VL）的准确性和鲁棒性。在传感器噪声扰动上性能下降明显，正是由于光度失真破坏了槽提取。
*   **仿真验证**：所有实验均在仿真环境中进行，**尚未在真实机器人上部署验证**，其现实世界的鲁棒性有待证明。
*   **假设限制**：模型假设目标对象与干扰物是弱耦合的，如果干扰物物理上阻挡了目标，模型可能失败。
*   **实时性瓶颈**：冻结的感知栈（~138ms/帧）是实时控制的主要延迟来源，尽管策略部分（主干+动作头）仅需~90ms。
*   **对象数量上限**：预设了最大对象槽数量（N_max=16），对于更复杂的场景可能构成限制。

（完）
