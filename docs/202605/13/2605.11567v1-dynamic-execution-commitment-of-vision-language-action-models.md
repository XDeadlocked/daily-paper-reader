---
title: Dynamic Execution Commitment of Vision-Language-Action Models
title_zh: 视觉-语言-动作模型的动态执行承诺
authors: "Feng Chen, Xianghui Wang, Yuxuan Chen, Boying Li, Yefei He, Zeyu Zhang, Yicheng Wu"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11567v1"
tags: ["query:sr-0501"]
score: 10.0
evidence: 为视觉-语言-动作（VLA）模型引入自适应动作接受机制，以改进执行承诺
tldr: 视觉-语言-动作模型通常采用固定步长的动作分块预测来平衡推理效率与成功率，但忽略了预测可靠性的状态依赖性，导致在动态环境中性能不稳定。本文提出自适应动作接受机制A3，将其重新定义为自推测前缀验证问题，通过群体采样计算共识分数并验证动作序列的一致性，从而动态确定可执行的最长可靠前缀。该方法无需手动调优执行步长，即可在多种模型和任务上实现执行鲁棒性与推理效率的更好权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11567v1/fig-001.webp\", \"caption\": \"Figure 4: Visualization of the execution horizon across different tasks.\", \"page\": 11, \"index\": 1, \"width\": 829, \"height\": 995}, {\"url\": \"assets/figures/arxiv/2605.11567v1/fig-002.webp\", \"caption\": \"Figure 6: Implementation of dual verification tree.\", \"page\": 15, \"index\": 2, \"width\": 782, \"height\": 455}, {\"url\": \"assets/figures/arxiv/2605.11567v1/fig-003.webp\", \"caption\": \"Figure 1: Performance analysis of π-0.5 under varying execution horizons on LIBERO benchmark [8]. (a) Success rate first increases while then decreases as horizon increases, dropping below 80% when horizon is larger than 15 for most suites. (b) Completion step increases substantially with a larger horizon, as failed recoveries and compounding errors drive total steps up to 1.5× higher than at horizon=1. (c) Forward calls decrease monotonically with a longer horizon, revealing a fundamental trade-off between inference overhead and success rate.\", \"page\": 2, \"index\": 3, \"width\": 805, \"height\": 253}, {\"url\": \"assets/figures/arxiv/2605.11567v1/fig-004.webp\", \"caption\": \"Table 1: Dynamic horizon comparison across LIBERO, MetaWorld, and MainSkill. We report average task success rate (Avg.) and average committed horizon length (Len.).\", \"page\": 7, \"index\": 4, \"width\": 830, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2605.11567v1/fig-005.webp\", \"caption\": \"Table 2: Performance comparison on real-world tasks.\", \"page\": 7, \"index\": 5, \"width\": 830, \"height\": 185}, {\"url\": \"assets/figures/arxiv/2605.11567v1/fig-006.webp\", \"caption\": \"Figure 3: Trade-off between success rate (top row) and forward calls (bottom row) across execution horizons. The first two column experiments are conducted on π-0.5 while the last one is on GR00T.\", \"page\": 8, \"index\": 6, \"width\": 801, \"height\": 518}, {\"url\": \"assets/figures/arxiv/2605.11567v1/fig-007.webp\", \"caption\": \"Figure 2: Overview of A3. Given the current observation and instruction, the VLM backbone and action expert generate K candidate action chunks. The chunks are mapped to induced trajectory states, from which the dominant mode is identified via clustering and its medoid selected as the primary draft; per-step consensus scores reflect the model’s self-consistency at each action position. The draft then undergoes dual parallel verification in a single batched forward pass, yielding the execution horizon l∗ as the longest verified prefix. We also illustrate the implementation of dual verification tree in Figure 6.\", \"page\": 4, \"index\": 7, \"width\": 732, \"height\": 365}, {\"url\": \"assets/figures/arxiv/2605.11567v1/fig-008.webp\", \"caption\": \"Figure 5: Two representative failure cases. (a) Misalignment between the mug handle and the hook in the hang mug task. (b) Self-occlusion of the inverted mug rim by the gripper in the flip mug task.\", \"page\": 12, \"index\": 8, \"width\": 826, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2605.11567v1/fig-009.webp\", \"caption\": \"Table 4: Effect of prediction horizon on execution horizon and LIBERO suites.\", \"page\": 9, \"index\": 9, \"width\": 672, \"height\": 193}]"
motivation: 现有视觉-语言-动作模型采用固定执行步长，忽略了预测可靠性的状态依赖性，导致在动态或分布外环境中性能脆弱。
method: 提出自适应动作接受机制A3，通过群体采样计算轨迹共识分数，并基于共识排序的条件不变性和前缀封闭的顺序一致性来验证动作序列。
result: 在不同VLA模型和基准测试上的实验表明，A3在保持高推理吞吐量的同时显著提升了执行鲁棒性。
conclusion: 该方法实现了执行鲁棒性与推理效率的自动平衡，无需人工调优。
---

## 摘要
视觉-语言-动作（VLA）模型主要采用动作分块策略，即通过单次前向传播预测并承诺执行一个短时域内的连续低级动作序列，以分摊大规模骨干网络的推理成本并降低单步延迟。然而，将这些多步预测承诺到真实世界执行时，需要在成功率和推理效率之间取得平衡，这一决策通常由针对每个任务调整的固定执行时域所决定。此类启发式方法忽略了预测可靠性对状态的依赖性，导致在动态或分布外场景中性能脆弱。本文提出A3，一种自适应动作接受机制，将动态执行承诺重新定义为一种自推测前缀验证问题。A3首先通过分组采样计算动作的轨迹级共识分数，然后选择一个代表性草稿并优先进行下游验证。具体而言，它强制执行：（1）共识排序的条件不变性，即通过判断低共识动作在以高共识动作为条件重新解码时是否保持一致来验证其有效性；（2）前缀封闭的序列一致性，即仅接受从起始开始的最长连续已验证动作序列，从而保证物理执行完整性。因此，执行时域自然显现为满足内部模型逻辑和序列执行约束的最长可验证前缀。在不同VLA模型和基准测试上的实验表明，A3无需手动调整执行时域，同时在执行鲁棒性和推理吞吐量之间实现了更优的权衡。

## Abstract
Vision-Language-Action (VLA) models predominantly adopt action chunking, i.e., predicting and committing to a short horizon of consecutive low-level actions in a single forward pass, to amortize the inference cost of large-scale backbones and reduce per-step latency. However, committing these multi-step predictions to real-world execution requires balancing success rate against inference efficiency, a decision typically governed by fixed execution horizons tuned per task. Such heuristics ignore the state-dependent nature of predictive reliability, leading to brittle performance in dynamic or out-of-distribution settings. In this paper, we introduce A3, an Adaptive Action Acceptance mechanism that reframes dynamic execution commitment as a self-speculative prefix verification problem. A3 first computes a trajectory-wise consensus score of actions via group sampling, then selects a representative draft and prioritizes downstream verification. Specifically, it enforces: (1) consensus-ordered conditional invariance, which validates low-consensus actions by judging whether they remain consistent when re-decoded conditioned on high-consensus actions; and (2) prefix-closed sequential consistency, which guarantees physical rollout integrity by accepting only the longest continuous sequence of verified actions starting from the beginning. Consequently, the execution horizon emerges as the longest verifiable prefix satisfying both internal model logic and sequential execution constraints. Experiments across diverse VLA models and benchmarks demonstrate that A3 eliminates the need for manual horizon tuning while achieving a superior trade-off between execution robustness and inference throughput.

---

## 论文详细总结（自动生成）

好的，我将基于您提供的论文内容，按照您要求的要点生成一份详细的结构化中文总结。

---

### **论文总结：Dynamic Execution Commitment of Vision-Language-Action Models**

#### **1. 核心问题与整体含义**
*   **研究背景**：现代视觉-语言-动作（VLA）模型为平衡大规模骨干网络的推理成本与实时控制需求，普遍采用“动作分块”策略，即一次预测未来多步动作。然而，实际执行时，系统需要决定“承诺”执行该预测序列中的多少步（即“执行时域”），这是一个关键但未被充分研究的设计选择。
*   **研究动机**：现有方法通常为每个任务预设一个固定的执行步长。这种启发式方法隐含假设模型的预测可靠性在时间上是均匀的，且不受环境动态影响。然而，在开放式的具身环境中，观测具有非平稳性，执行误差会累积，导致固定步长策略性能脆弱。如图1所示，**短步长**（频繁重规划）导致推理开销大，**长步长**（开环执行）则因误差累积导致成功率下降，存在根本性的权衡。

#### **2. 方法论：自适应动作接受机制 A³**
*   **核心思想**：将动态执行承诺重新定义为 **“自推测前缀验证”** 问题。将模型预测的动作序列视为一个“草稿”，通过结构化验证来自适应地确定其中可信、可执行的最长前缀，而非执行固定长度。
*   **关键技术细节**：
    1.  **模式感知轨迹共识评分**：
        *   **目标**：评估模型预测的自我一致性，为后续验证提供优先级排序，而非直接判断对错。
        *   **过程**：通过单次批量前向传播，采样生成K个候选动作序列。为避免增量动作误差累积的干扰，将动作序列**积分到诱导轨迹状态空间**进行聚类，识别出“主导模式”（即最一致、最紧凑的轨迹簇）。
        *   **输出**：选择主导模式的中心轨迹作为主草稿，并为其中每个动作步骤计算一个共识分数，分数越高代表该步预测越稳定。
    2.  **双重分层验证**：
        *   在选定草稿后，通过一个并行的验证树（单次前向传播完成）进行双重检查，确保动作既符合模型内部逻辑，又满足物理执行的因果链。
        *   **共识排序的条件不变性**：对于共识分数较低的动作，将其**以所有共识分数更高的动作为条件进行重新解码**。仅当重新解码的结果与原草稿动作足够接近（小于阈值δ）时，才认为该动作是可靠的。这确保了低置信度动作与模型的高置信度长期计划保持一致。
        *   **前缀封闭的序列一致性**：对于一个动作，**以其所有前序已验证的动作为条件进行重新解码**。仅当重新解码结果一致时，该动作才被接受。这保证了最终承诺执行的动作前缀形成一个连续、可靠的因果链，防止跳过未验证的步骤导致状态分布偏移。
*   **算法流程**：最终，**执行时域 `l*` 被确定为同时通过上述两种验证的最长连续前缀的第一个动作索引**。该过程完全自适应，无需人工设定。

#### **3. 实验设计**
*   **数据集/场景**：
    *   **模拟基准**：LIBERO（知识迁移长时程任务）、ManiSkill（泛化操作技能）、MetaWorld（多任务元强化学习）。
    *   **真实机器人**：使用Agilex Piper机械臂执行FlipMug, TapeBox, HangMug, StackCube四个桌面操作任务。
*   **评估指标**：
    *   **主要指标**：任务成功率。
    *   **效率指标**：平均承诺执行步长、每回合推理调用次数（前向传播次数）、总步数。
*   **对比方法**：
    *   **Fixed Horizon**：强基线，通过网格搜索为每个任务/基准选择最优的固定步长。
    *   **Mixture of Horizons (MoH)**：将动作块分解为不同时长的段并进行融合。
    *   **Adaptive Horizon 方法**：包括EveryDayVLA（利用离散与连续动作头的不一致性）和AutoHorizon（利用自注意力权重作为置信度代理）。

#### **4. 资源与算力**
*   论文中**未详细说明**模型训练所需的算力（GPU型号、数量、训练时长）。
*   所有评估实验（包括模拟和真实机器人）均在**单张 NVIDIA RTX 4090 GPU** 上进行。文中提供了A³方法每一步的延迟分解（见表7）。

#### **5. 实验数量与充分性**
*   **实验充分性较高**，涵盖了：
    1.  **主实验**：在3个模拟基准（LIBERO, ManiSkill, MetaWorld）和1个真实机器人场景上，测试了3个不同的VLA骨干模型（π-0, π-0.5, GR00T），并与多个基线方法进行了全面对比。
    2.  **鲁棒性分析**：测试了在观测被遮挡（Masking）和模糊（Blur）等状态扰动下的性能。
    3.  **参数敏感性分析**：分析了关键超参数（采样数K、接受阈值δ）的影响。
    4.  **消融研究**：逐步验证了共识评分、条件不变性验证、序列一致性验证三个核心组件的贡献。
    5.  **可视化分析**：展示了不同任务阶段执行时域的自适应变化行为，并分析了典型失败案例。
*   **客观性与公平性**：
    *   对比基线选择了代表性方法，并为固定步长基线进行了网格搜索以提供“最优”对比，比较公平。
    *   报告了成功率、执行步长、推理调用次数等多个维度指标，呈现了效率-性能的权衡曲线（Pareto前沿）。
    *   公开分析了自身方法的失败案例和局限性，态度客观。

#### **6. 主要结论与发现**
*   A³能够**动态地、自适应地**确定执行时域，**无需针对任何任务进行手动调优**。
*   在多个VLA模型和任务上，A³在**保持或提升任务成功率**的同时，能够**显著延长平均承诺执行步长**（例如在π-0.5上，LIBERO任务步长从6.3增至9.7），从而减少推理调用次数，实现了更优的推理效率-执行鲁棒性权衡。
*   该方法能根据任务难度自动分配控制粒度：在自由空间运动阶段采用较长时域以提升效率，在接触关键阶段（如抓取、对齐）自动缩短时域以保持精细控制。
*   在存在状态扰动（遮挡、模糊）时，A³相比固定步长基线表现出更强的鲁棒性。

#### **7. 优点**
*   **方法创新**：
    *   **问题重构**：将执行承诺从启发式选择转变为结构化的自验证问题，视角新颖。
    *   **双重验证设计**：“条件不变性”确保了动作与模型高层意图一致，“序列一致性”保证了物理执行的因果可靠性，约束设计合理且互补。
    *   **高效实现**：通过批量处理，将双重验证的计算开销压缩为**单次额外前向传播**，并通过延长执行步长摊销了成本。
*   **实验设计**：
    *   **覆盖全面**：涵盖了模拟、真实机器人、多个骨干模型和任务套件。
    *   **分析深入**：不仅报告最终性能，还进行了可视化分析、失败案例剖析和详细的消融实验，增强了结论的可信度。

#### **8. 不足与局限**
*   **无法解决根本性策略错误**：A³旨在验证和筛选模型预测的动作，**并不能提高底层VLA策略本身的感知或控制精度**。如图5所示，因视角遮挡导致感知缺失，或因策略精度不足导致的末端位姿细微偏差，A³无法纠正。
*   **增加单步延迟**：虽然整体效率提升，但A³引入了额外的计算（采样、聚类、验证），导致**单步决策延迟显著增加**（从216.2ms增至485.6ms）。这对于对延迟极度敏感的超高频控制场景可能不适用。
*   **依赖动作积分模型**：共识评分依赖于从动作序列到轨迹状态的积分函数 `G`。该函数的准确性（如是否考虑动力学、碰撞）会影响共识评估的质量。
*   **验证阈值依赖**：接受阈值δ设置为数据集中动作维度的标准差，虽具有一定自适应性，但仍是一个需要预设或校准的超参数。
*   **未来方向**：论文已指出，如用轻量级可靠性估计器替代采样、结合显式世界模型进行验证、支持非连续承诺等，是潜在的改进方向。

（完）
