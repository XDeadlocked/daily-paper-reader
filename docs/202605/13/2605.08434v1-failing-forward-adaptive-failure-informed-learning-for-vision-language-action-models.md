---
title: "Failing Forward: Adaptive Failure-Informed Learning for Vision-Language-Action Models"
title_zh: 故障前向：视觉-语言-动作模型的自适应故障感知学习
authors: "Meng Zheng, Samhita Marri, Anwesa Choudhuri, Benjamin Planche, Zhongpai Gao, Van Nguyen Nguyen, Terrence Chen, Girish Chowdhary, Ziyan Wu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08434v1"
tags: ["query:sr-0501"]
score: 10.0
evidence: 用于机器人控制和故障感知学习的VLA模型
tldr: 本文针对视觉-语言-动作模型仅依赖成功行为克隆导致错误累积的问题，提出自适应失败学习框架，利用失败轨迹作为负向指导，联合训练成功和失败行为生成器，在多种机器人操作任务中显著提升了任务成功率和鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08434v1/fig-001.webp\", \"caption\": \"Figure 3: Visualization of in-domain (left) and out-of-domain (right) manipulation task setup. Row 1: exterior camera image, row 2: wrist camera image. See supplementary for more visualizations.\", \"page\": 8, \"index\": 1, \"width\": 738, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2605.08434v1/fig-002.webp\", \"caption\": \"Figure 2: Dual Action Generator (DAG)-VLA with Adaptive Failure-Informed Learning (AFIL).\", \"page\": 5, \"index\": 2, \"width\": 824, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2605.08434v1/fig-003.webp\", \"caption\": \"Table 4: Performance comparison on out-of-domain manipulation tasks. Success rates (%) reported over 25 configs × 3 runs for each task.\", \"page\": 9, \"index\": 3, \"width\": 764, \"height\": 191}, {\"url\": \"assets/figures/arxiv/2605.08434v1/fig-004.webp\", \"caption\": \"Figure 1: Overview of the proposed Adaptive Failure-Informed Learning (AFIL) pipeline. AFIL uses online-generated failure trajectories as adaptive negative guidance, steering action generation away from failure-prone regions and toward more reliable success modes.\", \"page\": 2, \"index\": 4, \"width\": 782, \"height\": 293}]"
motivation: 现有视觉-语言-动作模型仅依赖成功行为克隆，导致执行错误累积和不可恢复的失败。
method: 提出自适应失败学习框架，利用失败轨迹作为负向指导，联合训练成功和失败行为生成器。
result: 实验表明AFIL在多种机器人操作任务中一致优于基线，提高了任务成功率和鲁棒性。
conclusion: AFIL通过自适应失败引导，显著提升了VLA模型在机器人操作任务中的成功率和鲁棒性。
---

## 摘要
视觉-语言-动作（VLA）模型为可扩展的机器人操作提供了有前景的范式，但其仅依赖成功行为克隆的特性使其脆弱；由于缺乏纠正性训练信号，微小的执行错误会迅速累积成不可恢复的分布外故障。为应对这一局限，我们提出自适应故障感知学习（AFIL），这是一种端到端框架，利用故障轨迹作为基于扩散和流的VLA策略的自适应负向引导。AFIL使用预训练的VLA在线生成故障轨迹，避免了手工设计故障模式或人工介入恢复的需求。随后，它联合训练用于成功和失败行为的双重动作生成器（DAGs），同时共享一个通用的视觉-语言骨干网络，从而以有限的参数开销实现高效的故障感知策略学习。在采样过程中，故障生成器自适应地将动作生成引导远离易故障区域，转向更可靠的成功模式，其引导强度由每个扩散步骤中成功与失败分布之间的距离决定。在领域内和领域外机器人操作任务上的实验，涵盖短期和长期设置，表明AFIL在现有VLA基线上持续提升了任务成功率和鲁棒性，证明了其有效性、效率和通用性。

## Abstract
Vision-language-action (VLA) models provide a promising paradigm for scalable robotic manipulation, yet their reliance on success-only behavioral cloning leaves them brittle; lacking corrective training signals, minor execution errors rapidly compound into unrecoverable, out-of-distribution failures. To address this limitation, we propose Adaptive Failure-Informed Learning (AFIL), an end-to-end framework that leverages failure trajectories as adaptive negative guidance for diffusion- and flow-based VLA policies. AFIL uses a pretrained VLA to generate failure rollouts online, avoiding the need for handcrafted failure-mode design or human-in-the-loop recovery. It then jointly trains Dual Action Generators (DAGs) for successful and failed behaviors while sharing a common vision-language backbone, enabling efficient failure-aware policy learning with limited parameter overhead. During sampling, the failure generator adaptively steers action generation away from failure-prone regions and toward more reliable success modes, with guidance strength determined by the per-diffusion-step distance between success and failure distributions. Experiments across in-domain and out-of-domain robotic manipulation tasks, covering both short- and long-horizon settings, show that AFIL consistently improves task success rates and robustness over existing VLA baselines, demonstrating its effectiveness, efficiency, and generality.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，为您生成一份结构化的深入总结。

---

### **论文总结：Failing Forward: Adaptive Failure-Informed Learning for Vision-Language-Action Models**

#### **1. 核心问题与整体含义**
*   **研究动机**：当前基于扩散或流模型的视觉-语言-动作（VLA）策略主要依赖于“仅成功”的行为克隆数据进行训练。这导致模型在面对执行误差或分布外状态时非常脆弱，因为缺乏从错误中恢复的纠正性信号，微小的错误会迅速累积成不可恢复的失败。
*   **整体含义**：本文提出，失败轨迹与成功轨迹同样富含信息。受图像生成中“负向提示”技术的启发，论文旨在将失败数据作为一种**自适应的负向引导**，集成到VLA策略的训练和推理过程中，从而主动引导策略避开失败模式，提升其在复杂、长时程任务中的鲁棒性和泛化能力。

#### **2. 方法论**
*   **核心思想**：构建一个**双重动作生成器（DAG）**，在共享的视觉-语言骨干网络上，并行训练一个“成功动作生成器”和一个“失败动作生成器”。在推理时，利用失败生成器的输出来动态地、自适应地将动作采样过程从失败倾向区域“推开”。
*   **关键技术细节**：
    1.  **DAG架构**：两个轻量级的动作生成头（`G_succ`, `G_fail`）共享同一个大型VLM骨干网络（如π0.5），分别使用成功和失败轨迹数据进行训练。这以最小的参数开销实现了对成功和失败行为分布的联合建模。
    2.  **失败数据生成**：无需人工设计故障模式。利用预训练的VLA策略在线执行任务，自然产生的失败轨迹即作为失败数据源。这些失败轨迹还可通过运动规划器进行纠正，以生成“失败-纠正”数据用于增强训练。
    3.  **自适应故障感知采样**：
        *   在扩散模型的每一步去噪中，同时查询成功生成器（`ε_succ`）和失败生成器（`ε_fail`）。
        *   引导公式为：`ε*_FI = ε_succ - λ_η(a_η) * (ε_fail - ε_succ)`。核心创新在于**自适应的引导强度λ_η**。
        *   **λ_η的计算**：`λ_η(a_η) = α * (1 - cos(ε_succ, ε_fail))`，其中α是基础引导尺度。**余弦相似度**被用来衡量当前状态下成功与失败预测的“意图”差异。当两者意图相似（如任务初期都执行“接近物体”动作）时，引导弱，避免过度抑制合理动作；当意图分歧（如一个预测“稳定抓握”，一个预测“滑落”）时，引导强，主动排斥失败模式。

#### **3. 实验设计**
*   **数据集/场景**：
    *   **ManiSkill**：在仿真环境中进行领域内和领域外评估。任务包括短时程（堆叠立方体、竖起木钉、拖动立方体）和长时程任务（摆放碗勺、摆放罐头和香蕉）。领域外任务涉及未见过的物体几何形状、纹理和杂乱场景。
    *   **LIBERO**：一个标准化的终身机器人学习基准，包含四个任务套件（空间变化、物体变化、目标变化、长时程），用于评估知识迁移和泛化能力。
*   **Benchmark**：主要使用任务**成功率**作为核心评估指标。
*   **对比方法**：
    *   **基线**：仅用成功数据训练的VLA模型（`Success Only`）。
    *   **增强基线**：用成功数据+（失败及纠正）数据微调的模型（`Success + Failure Correction`）。
    *   **消融对比**：使用固定引导强度的DAG-VLA（`Static-FIL`）。
    *   **SOTA对比**：在LIBERO上与当前先进的VLA模型对比，包括Diffusion Policy、Octo、OpenVLA、CoT-VLA以及强大的π0.5模型。

#### **4. 资源与算力**
*   论文中**未明确说明**训练所使用的具体GPU型号、数量以及总训练时长。实验是基于π0.5架构进行的，但未披露其训练或AFIL微调所需的计算资源细节。

#### **5. 实验数量与充分性**
*   **实验组数**：实验设计较为全面，包括：
    1.  **主实验**：在ManiSkill的5个领域内任务和3个领域外任务上进行评估。
    2.  **基准测试**：在LIBERO的4个任务套件上进行大规模评估（每个套件10个任务，共500次 rollout）。
    3.  **消融实验**：
        *   对比了不同训练数据策略（成功、成功+纠正、成功+失败）的效果。
        *   对比了固定引导与自适应引导（`Static-FIL` vs `Adaptive-FIL`）的性能差异。
        *   对自适应引导中的关键超参数**引导尺度α**进行了消融研究（测试了0.5, 1.0, 2.0, 5.0）。
*   **充分性与客观性**：
    *   **充分性**：实验覆盖了**领域内/外泛化**、**短/长时程任务**、**不同基准**，并进行了关键模块的消融，能够较为全面地验证方法的有效性。
    *   **客观性与公平性**：对比实验均基于相同的骨干网络（π0.5），确保了比较的公平性。在LIBERO上的对比列出了多个近期SOTA模型的结果。实验采用多随机种子运行并报告平均成功率，增加了结果的可靠性。

#### **6. 主要结论与发现**
*   AFIL框架能**一致且显著地**提升VLA策略在各种机器人操作任务上的成功率。
*   **自适应引导机制至关重要**：`Adaptive-FIL`在所有设置下均优于使用固定引导的`Static-FIL`，尤其在长时程任务上优势更明显（最高提升12%），证明了动态调整引导强度的有效性。
*   **提升泛化能力**：在领域外任务上，AFIL相比仅用成功数据训练的基线带来了**巨大的性能提升**（例如在“举起圆柱体”任务上从14.7%提升至62.7%），显著增强了模型对未见过的物体和场景的鲁棒性。
*   **对先进模型仍有增益**：即使在强大的π0.5基线上，加入AFIL后仍在LIBERO基准上取得了进一步的性能提升（平均从96.9%提升至98.4%）。

#### **7. 优点**
*   **方法创新**：首次将“负向提示”思想系统性地引入到VLA策略的序列动作生成中，提出了**自适应失败感知引导**这一新颖且有效的机制。
*   **高效实用**：DAG架构通过共享骨干，以最小的参数开销实现了对失败分布的建模，易于集成到现有的扩散/流式VLA模型中。
*   **数据利用高效**：利用预训练模型在线生成失败数据，避免了繁琐、有偏的人工故障模式设计，使失败数据更贴近真实部署时的分布。
*   **实验验证扎实**：在多个具有挑战性的基准和任务设置上进行了充分验证，结论令人信服。

#### **8. 不足与局限**
*   **仿真局限**：所有实验均在仿真环境（ManiSkill, LIBERO）中进行，未在真实物理机器人上验证。仿真与现实的差距（Sim2Real）可能影响方法的实际效果。
*   **失败数据生成依赖**：失败数据的质量依赖于初始预训练VLA策略的探索能力。如果初始策略完全无法触及某些失败模式，则这些模式无法被学习。
*   **计算开销分析不足**：虽然强调了参数开销小，但未定量分析在推理时由于需要同时运行两个生成器并进行余弦相似度计算所带来的额外**时间开销**，这对于实时控制可能是一个考量因素。
*   **引导机制的可解释性**：虽然使用了余弦相似度作为自适应信号，但其在复杂决策过程中的具体语义（如何精确对应到“即将失败”的状态）仍需进一步分析和可视化。

（完）
