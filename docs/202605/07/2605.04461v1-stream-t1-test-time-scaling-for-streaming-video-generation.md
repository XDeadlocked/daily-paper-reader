---
title: "Stream-T1: Test-Time Scaling for Streaming Video Generation"
title_zh: Stream-T1：流式视频生成的测试时缩放
authors: "Yijing Tu, Shaojin Wu, Mengqi Huang, Wenchuan Wang, Yuxin Wang, Chunxiao Liu, Zhendong Mao"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04461v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 流式视频生成的测试时缩放
tldr: 本文针对现有测试时视频生成方法成本高、缺乏时间指导的问题，提出将重点转向流式视频生成，并设计了首个专为流式视频生成定制的测试时缩放框架 Stream-T1。该框架包含流式缩放噪声传播、奖励剪枝和记忆下沉三个单元，分别用于建立时间依赖、平衡时空质量和管理历史信息。实验表明，Stream-T1 在多个视频基准上显著提升了时间一致性、运动平滑度和视觉质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.04461v1/fig-001.webp\", \"caption\": \"Figure 4 Qualitative ablations studies on each component. Omitting the Stream-Scaled Memory Sinking degrades background stability. Removing the Stream-Scaled Noise Propagation introduces local structural artifacts (e.g., on the subject’s tail). Eliminating the Stream-Scaled Reward Pruning leads to distinct semantic misalignment and deteriorated aesthetic quality.\", \"page\": 11, \"index\": 1, \"width\": 986, \"height\": 506}, {\"url\": \"assets/figures/arxiv/2605.04461v1/fig-002.webp\", \"caption\": \"Figure 3 Qualitative comparisons of Stream-T1 with Causvid, Self-Forcing, and LongLive. Stream-T1 significantly elevates the temporal consistency and frame-level visual fidelity of the generated videos.\", \"page\": 10, \"index\": 2, \"width\": 986, \"height\": 946}, {\"url\": \"assets/figures/arxiv/2605.04461v1/fig-003.webp\", \"caption\": \"Table 1 Quantitative comparison with baseline of 5s video generation. The best results are highlighted in bold\", \"page\": 8, \"index\": 3, \"width\": 972, \"height\": 188}, {\"url\": \"assets/figures/arxiv/2605.04461v1/fig-004.webp\", \"caption\": \"Figure 1 Overview of the Stream-T1 chunk-level scaling pipeline. For each chunk, our test-time scaling mechanism operates in three sequential stages: (1)Stream-Scaled Noise Propagation, where the initial noise latents for the current chunk are actively sampled, conditioned on the optimal noise trajectories retained from the previous chunk. (2)Stream-Scaled Reward Pruning, where fully generated chunk candidates are holistically evaluated via a Long-Short Combined Reward to balance local spatial aesthetics with global temporal coherence. Based on this feedback, suboptimal branches are pruned to propagate only the optimal generation trajectories.(3)Stream-Scaled Memory Sinking, where the memory sink is adaptively updated based on reward scores to preserve long-term semantics and guide the subsequent video generation. We present the full search trajectory of the illustrated case in the Figure 2.\", \"page\": 4, \"index\": 4, \"width\": 986, \"height\": 641}, {\"url\": \"assets/figures/arxiv/2605.04461v1/fig-005.webp\", \"caption\": \"Table 3 Quantitative comparison with test-time scaling methods of 30s video generation. Stream-T1 achieving state-of-the-art performance across all evaluative metrics.\", \"page\": 9, \"index\": 5, \"width\": 968, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2605.04461v1/fig-006.webp\", \"caption\": \"Table 2 Quantitative comparison with baseline of 30s video generation. The best results are highlighted in bold\", \"page\": 9, \"index\": 6, \"width\": 972, \"height\": 193}, {\"url\": \"assets/figures/arxiv/2605.04461v1/fig-007.webp\", \"caption\": \"Figure 2 The complete search path of the case in the Figure 1.\", \"page\": 5, \"index\": 7, \"width\": 986, \"height\": 539}]"
motivation: 现有基于扩散模型的测试时视频生成方法存在候选探索成本高和缺乏时间指导的问题，而流式视频生成因其分块合成和少量去噪步骤的特性，天然适合测试时缩放。
method: Stream-T1 包含三个核心单元：流式缩放噪声传播、流式缩放奖励剪枝和流式缩放记忆下沉，分别用于建立时间依赖、平衡时空质量和管理历史信息。
result: 在5秒和30秒视频基准测试中，Stream-T1 表现出显著优势，大幅提升了时间一致性、运动平滑度和帧级视觉质量。
conclusion: Stream-T1 显著提升了流式视频生成的时间一致性、运动平滑度和帧级视觉质量。
---

## 摘要
尽管测试时缩放（TTS）为在不增加训练成本的情况下提升视频生成质量提供了有前景的方向，但当前基于扩散模型的测试时视频生成方法存在候选探索成本过高且缺乏时序引导的问题。为应对这些结构性瓶颈，我们建议将重点转向流式视频生成。我们发现其分块级合成和较少的去噪步骤本质上适合TTS，能显著降低计算开销，同时实现细粒度的时间控制。基于这一洞见，我们提出了Stream-T1，这是一个专为流式视频生成量身定制的开创性综合TTS框架。具体而言，Stream-T1由三个单元组成：（1）流式缩放噪声传播，它利用历史已验证的高质量前序块噪声主动优化生成块的初始潜在噪声，有效建立时间依赖性，并利用历史高斯先验引导当前生成；（2）流式缩放奖励剪枝，通过整合即时短期评估与基于滑动窗口的长期评估，全面评估生成候选，在局部空间美学与全局时序连贯性之间取得最佳平衡；（3）流式缩放记忆沉淀，它根据奖励反馈的引导，将KV缓存中逐出的上下文动态路由到不同的更新路径中，确保先前生成的视觉信息能有效锚定并引导后续视频流。在5秒和30秒的综合视频基准测试中，Stream-T1展现出显著优势，大幅提升了时序一致性、运动平滑度和帧级视觉质量。

## Abstract
While Test-Time Scaling (TTS) offers a promising direction to enhance video generation without the surging costs of training, current test-time video generation methods based on diffusion models suffer from exorbitant candidate exploration costs and lack temporal guidance. To address these structural bottlenecks, we propose shifting the focus to streaming video generation. We identify that its chunk-level synthesis and few denoising steps are intrinsically suited for TTS, significantly lowering computational overhead while enabling fine-grained temporal control. Driven by this insight, we introduced Stream-T1, a pioneering comprehensive TTS framework exclusively tailored for streaming video generation. Specifically, Stream-T1 is composed of three units: (1) Stream -Scaled Noise Propagation, which actively refines the initial latent noise of the generating chunk using historically proven, high-quality previous chunk noise, effectively establishes temporal dependency and utilizing the historical Gaussian prior to guide the current generation; (2) Stream -Scaled Reward Pruning, which comprehensively evaluates generated candidates to strike an optimal balance between local spatial aesthetics and global temporal coherence by integrating immediate short-term assessments with sliding-window-based long-term evaluations; (3) Stream-Scaled Memory Sinking, which dynamically routes the context evicted from KV-cache into distinct updating pathways guided by the reward feedback, ensuring that previously generated visual information effectively anchors and guides the subsequent video stream. Evaluated on both 5s and 30s comprehensive video benchmarks, Stream-T1 demonstrates profound superiority, significantly improving temporal consistency, motion smoothness, and frame-level visual quality.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，为您生成一份结构化的深入总结。

---

### **论文总结：Stream-T1: Test-Time Scaling for Streaming Video Generation**

#### **1. 核心问题与整体含义**
*   **研究背景**：视频生成领域，尤其是生成**长视频**时，面临维持**长期时间一致性**和**语义对齐**的挑战。传统的训练阶段模型缩放范式成本高昂且面临瓶颈。
*   **现有问题**：现有的**测试时缩放**方法（如ImagerySearch）应用于视频扩散模型时，由于需要全局生成和多次去噪，导致**候选探索计算成本极高**，且**无法进行细粒度的时序引导**（局部瑕疵导致整个序列被拒绝）。
*   **核心洞察与转向**：本文提出将TTS的研究焦点转向**流式视频生成**范式。该范式以“分块”为单位自回归生成，且每个块仅需少量去噪步数。这种“浅搜索树、宽分支”的结构**天然适合TTS**，能大幅降低计算开销，并允许对每个块进行精细的时序控制。
*   **整体含义**：提出了首个专为流式视频生成设计的综合性TTS框架 **Stream-T1**，旨在通过推理阶段的主动优化，低成本地显著提升生成视频的时空质量。

#### **2. 方法论**
Stream-T1在基于LongLive的流式生成骨架上，为每个生成块引入三个顺序执行的主动优化阶段：

*   **核心思想**：将测试时缩放（通过束搜索扩展候选）与对生成过程（噪声、记忆）的**主动引导**相结合，而非被动选择。
*   **关键技术细节**：
    1.  **流式缩放噪声传播**：
        *   **目的**：建立块间时间依赖，引导当前块生成。
        *   **做法**：当前块的初始噪声 `x_n^T` 并非完全随机采样，而是通过对前一个最优块的噪声 `x_{n-1}^T` 进行**球面插值**生成：`x_n^T = β * x_{n-1}^T + sqrt(1-β²) * ε`。这保证了噪声分布仍为标准高斯，但引入了时序相关性。
    2.  **流式缩放奖励剪枝**：
        *   **目的**：设计评估函数，在束搜索中筛选候选，平衡**局部空间质量**与**全局时间连贯性**。
        *   **做法**：设计**长短时结合奖励**。
            *   **短时分数**：对块内所有帧，用**图像奖励模型**（如ImageReward）独立评分后取平均。
            *   **长时分数**：用**视频奖励模型**（如VisionReward）对一个**滑动窗口**内的连续块序列进行评分，评估整体连贯性。
        *   **动态融合**：最终分数 `S_final` 是短时和长时分数的加权和。权重随当前块位置`n`动态变化（`n/N`），早期更关注长时连贯性，后期更关注局部质量，并通过阈值`τ`防止过度偏向短时分数导致画面停滞。
    3.  **流式缩放记忆下沉**：
        *   **目的**：智能管理KV缓存中因滑动窗口而被“挤出”的历史信息，实现短期连续性与长期语义保存的解耦。
        *   **做法**：基于奖励分数进行**语义边界检测**，动态决定对挤出块`(K_n, V_n)`的处理方式：
            *   **质量门控**：若短时分数低于历史平均，则**丢弃**，防止低质量信息污染。
            *   **过渡检测**：若长时分数骤降，表明发生场景/动作切换。
            *   **动态路由**：
                *   **丢弃**：未通过质量门控。
                *   **EMA-Sink**：高质量且无过渡（平滑连续）。将挤出块的KV状态以指数移动平均方式融入现有的“记忆沉淀”中，压缩冗余信息。
                *   **Append-Sink**：高质量且检测到过渡（语义切换）。直接将挤出块的KV状态作为新的独立锚点**追加**到沉淀序列中，避免特征混淆。

#### **3. 实验设计**
*   **数据集/场景**：
    *   **短视频（5s）**：使用 **VBench** 的946个提示词。
    *   **长视频（30s）**：使用 **MovieGen** 的前128个提示词。
*   **评估基准**：
    *   **自动指标**：使用 **VBench**（5s）和 **VBench-long**（30s），评估主题一致性、背景一致性、运动平滑度、成像质量、美学质量。
    *   **人类偏好对齐**：使用 **VideoAlign** 模型，评估视觉质量、运动质量和文本对齐度。
*   **对比方法**：
    *   **流式生成基线**：CausVid, Self-forcing, LongLive。
    *   **测试时缩放基线**：Best-of-N, Beam Search（在LongLive上实现）。

#### **4. 资源与算力**
*   论文**未明确说明**训练或实验所消耗的具体算力资源（如GPU型号、数量、训练/推理时长）。
*   仅提及实现是基于 **LongLive** 模型，而LongLive又基于 **Wan2.1-T2V-1.3B** 模型。所有实验使用随机种子42，视频生成参数为16 FPS，分辨率832×480。

#### **5. 实验数量与充分性**
*   **实验组别**：
    1.  **主实验**：在5s和30s场景下，与3个主流流式生成基线进行定量对比（表1，表2）。
    2.  **TTS方法对比**：在30s场景下，与两种经典TTS策略（Best-of-N, Beam Search）进行定量对比（表3）。
    3.  **消融实验**：在30s场景下，系统地移除了三个核心组件（记忆下沉、噪声传播、奖励剪枝）中的每一个，进行了定量（表4）和定性（图4）分析。
    4.  **定性展示**：提供了与基线方法的生成结果可视化对比（图3）。
*   **充分性与客观性评估**：
    *   **较为充分**：覆盖了短、长两种视频生成任务，进行了全面的定量指标对比和消融研究，验证了每个组件的必要性。
    *   **相对客观**：使用了公开的基准（VBench, MovieGen）和广泛认可的评估指标（VBench, VideoAlign）。对比基线选择了领域内代表性工作。
    *   **潜在局限**：实验主要基于一个基础模型（LongLive/Wan）。未在不同流式生成架构或更大规模模型上验证通用性。未报告不同束宽（Beam Size）等超参数对性能与成本的影响曲线。

#### **6. 主要结论与发现**
*   Stream-T1 在**不增加训练成本**的前提下，通过测试时主动优化，显著提升了流式生成视频的质量。
*   **定量结果**：在5s和30s的生成任务中，在**绝大多数评估指标上（尤其是时间一致性、运动平滑度）全面超越所有基线方法**，包括原始的LongLive和其他TTS策略。
*   **定性结果**：生成的30s视频表现出**卓越的长期稳定性**，有效缓解了基线方法中出现的视觉失真、质量漂移和时间不一致问题。
*   **消融结论**：三个核心组件缺一不可，各自解决了不同层面的问题（记忆下沉维护长期一致性，噪声传播提升稳定性，奖励剪枝平衡时空质量）。

#### **7. 优点**
*   **范式创新**：首次将TTS系统性地应用于流式视频生成，抓住了该范式“分块、少步”的特点，实现了高效且有效的推理时优化。
*   **框架设计精巧**：三个模块（噪声、奖励、记忆）形成了完整的“**引导-评估-反馈**”闭环，协同作用。
*   **奖励设计有效**：提出的动态权重长短时奖励，巧妙地平衡了生成过程中局部与全局的权衡。
*   **记忆管理智能**：基于语义边界检测的动态路由机制，超越了静态沉淀或无条件EMA，能更好地处理场景切换，是方法的关键创新点。

#### **8. 不足与局限**
*   **计算开销**：虽然相比全局视频TTS更高效，但束搜索和多次奖励模型调用仍会带来**显著的额外推理成本**，论文未对此进行量化分析或效率对比。
*   **依赖奖励模型**：框架性能高度依赖于所使用的图像和视频奖励模型的准确性与泛化能力。奖励模型的偏差会直接影响生成结果。
*   **实验广度有限**：所有实验均基于单一的流式生成模型（LongLive）。该方法在其他流式架构（如Self-forcing, CausVid）上的有效性和普适性尚未得到验证。
*   **超参数敏感性**：方法涉及多个超参数（如束宽K、插值系数β、奖励权重阈值τ、滑动窗口大小w、EMA衰减因子α等）。论文未提供详尽的超参数搜索或敏感性分析，其调优可能较为复杂。
*   **应用限制**：方法专注于提升已有流式生成模型的质量，**并未改变模型本身的内容创造或逻辑推理能力**。对于提示词理解错误或逻辑矛盾等根本性问题，可能无法纠正。

（完）
