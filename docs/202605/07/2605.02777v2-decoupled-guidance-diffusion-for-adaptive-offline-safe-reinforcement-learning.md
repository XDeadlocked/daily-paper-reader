---
title: Decoupled Guidance Diffusion for Adaptive Offline Safe Reinforcement Learning
title_zh: 自适应离线安全强化学习的解耦引导扩散
authors: "Rufeng Chen, Zhaofan Zhang, Zhejiang Yang, Hechang Chen, Sihong Xie"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02777v2"
tags: ["query:sr-0501"]
score: 9.0
evidence: 用于自适应离线安全强化学习的扩散规划器
tldr: 本文研究离线安全强化学习中的自适应安全轨迹生成问题。针对现有方法将奖励与约束视为竞争目标导致安全合规性差的问题，作者将问题重新解释为从受约束的轨迹分布中采样，并提出了安全解耦引导扩散（SDGD）方法。该方法利用成本限制进行无分类器引导以确保约束满足，同时使用奖励梯度引导提升回报，还引入可行轨迹重标注（FTR）来抑制奖励引导导致的成本漂移。理论分析表明FTR能抑制成本漂移。在DSRL基准上的大量实验表明，该方法在绝大多数任务上都能满足安全约束，同时在多数任务上获得了安全方法中最高的奖励。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02777v2/fig-001.webp\", \"caption\": \"Table 1: Normalized DSRL Liu et al. [2023a] benchmark results. ↑ means the higher the better. ↓ means the lower the better. Each value is averaged over 20 evaluation episodes and 3 seeds. Gray: Unsafe agents. Bold: Safe agents whose normalized cost is smaller than 1. Blue: Safe agents with the highest reward.\", \"page\": 7, \"index\": 1, \"width\": 824, \"height\": 720}, {\"url\": \"assets/figures/arxiv/2605.02777v2/fig-002.webp\", \"caption\": \"Figure 2: Overview of Safe Decoupled Guidance Diffusion (SDGD). (a) SDGD learns a cost limit (l) conditioned diffuser to model the safe trajectory distribution from offline data, while an FTR-trained reward classifier predicts rewards. Feasible Trajectory Relabeling (FTR) modifies reward single by replacing the raw reward R(τ) with a new reward target R̂(τ), thereby reshaping reward guidance away from infeasible regions and reducing reward-induced cost drift. (b) SDGD decouples safety and performance: classifier-free guidance enforces cost limits, while reward-gradient guidance refines samples toward high-return trajectories within the safe region.\", \"page\": 3, \"index\": 2, \"width\": 826, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2605.02777v2/fig-003.webp\", \"caption\": \"Figure 4: Performance comparison across different cost limits (10, 20, 30) in four tasks.\", \"page\": 8, \"index\": 3, \"width\": 826, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2605.02777v2/fig-004.webp\", \"caption\": \"Figure 6: Ablation study on the guidance components. The SDGD is compared against two variants: one without Classifier Guidance (w/o CG) and another without Classifier-Free Guidance (w/o CFG).\", \"page\": 9, \"index\": 4, \"width\": 816, \"height\": 261}]"
motivation: 现有基于扩散的规划器在奖励提升和约束满足上存在竞争，导致在变化的安全预算下安全合规性不可靠。
method: 提出安全解耦引导扩散（SDGD）和可行轨迹重标注（FTR），前者通过成本限制引导采样满足约束，后者通过重塑奖励目标抑制成本漂移。
result: "在DSRL基准测试中，SDGD在94.7%的任务（36/38）上满足约束，并在21个任务上获得安全方法中最高的奖励。"
conclusion: 该方法为离线安全强化学习中的自适应安全轨迹生成提供了一种可靠且高效的解决方案。
---

## 摘要
离线安全强化学习通常要求策略在部署时能够适应不同回合间或单个回合内变化的安全预算。虽然基于扩散的规划器能够实现灵活的轨迹生成，但现有的引导方案常将奖励提升与约束满足视为相互竞争的梯度目标，这可能导致在成本限制下安全合规性不可靠。我们将自适应安全轨迹生成重新解释为从受约束的轨迹分布中采样，其中预算限制了轨迹区域，而奖励则在该区域内塑造偏好。这一视角催生了安全解耦引导扩散（SDGD），该方法基于成本限制对无分类器引导进行条件化，以将采样偏向满足指定限制的轨迹，同时使用奖励梯度引导来优化轨迹以获得更高回报。由于直接奖励引导在提高回报的同时也可能将样本导向具有更高累积成本的轨迹，我们引入了可行轨迹重标注（FTR）来重塑奖励目标并抑制此类方向。我们进一步提供了一阶采样时间分析，表明在前缀恢复对齐条件下，FTR能够抑制奖励引起的成本漂移。在DSRL基准上的广泛评估表明，SDGD在基线方法中实现了最强的安全合规性，在94.7%的任务（36/38）上满足约束，同时在21个任务上获得了安全方法中最高的奖励。

## Abstract
Offline safe reinforcement learning often requires policies to adapt at deployment time to safety budgets that vary across episodes or change within a single episode. While diffusion-based planners enable flexible trajectory generation, existing guidance schemes often treat reward improvement and constraint satisfaction as competing gradient objectives, which can lead to unreliable safety compliance under cost limits. We reinterpret adaptive safe trajectory generation as sampling from a constrained trajectory distribution, where the budget restricts the trajectory region, and reward shapes preferences within that region. This perspective motivates Safe Decoupled Guidance Diffusion (SDGD), which conditions classifier-free guidance on the cost limit to bias sampling toward trajectories satisfying the specified limit, while using reward-gradient guidance to refine trajectories for higher return. Because direct reward guidance can increase return while also steering samples toward trajectories with higher cumulative cost, we introduce Feasible Trajectory Relabeling (FTR) to reshape reward targets and discourage such directions. We further provide a first-order sampling-time analysis showing that FTR suppresses reward-induced cost drift under a prefix-restorative alignment condition. Extensive evaluations on the DSRL benchmark show that SDGD achieves the strongest safety compliance among baselines, satisfying the constraint on 94.7% of tasks (36/38), while obtaining the highest reward among safe methods on 21 tasks.

---

## 论文详细总结（自动生成）

好的，这是对论文《Decoupled Guidance Diffusion for Adaptive Offline Safe Reinforcement Learning》的详细总结。

### 1. 核心问题与整体含义
*   **研究背景**：在机器人、自动驾驶等安全关键领域，离线安全强化学习（Safe RL）要求策略在部署时能够适应**动态变化的安全预算**（例如，不同任务、用户或同一任务内随时间变化的成本限制）。现有方法通常为固定或弱变化约束设计，难以可靠地适应这种变化。
*   **研究动机**：现有基于扩散模型的规划器在生成安全轨迹时，常将**奖励提升**和**约束满足**视为相互竞争的梯度目标（例如通过分类器引导或梯度切换）。这依赖于对噪声轨迹的成本预测，而该预测信号在扩散过程中可能不可靠（如图1c所示），导致在变化的成本限制下安全合规性不稳定。

### 2. 方法论
*   **核心思想**：将自适应安全轨迹生成重新解释为从**受约束的轨迹分布**中采样。成本限制定义了可采样轨迹的**区域**（支持集），而奖励则在该区域内塑造**偏好**。基于此，作者提出应将安全（区域限制）与性能（区域内优化）的角色**解耦**。
*   **关键技术**：
    1.  **安全解耦引导扩散（SDGD）**：
        *   **安全引导**：使用**无分类器引导（CFG）**，将成本限制 `l` 作为条件输入扩散模型。这直接使模型学习在给定成本限制下的安全轨迹分布，避免了依赖不可靠的成本分类器梯度。
        *   **性能引导**：在反向采样过程中，使用一个独立训练的奖励预测器 `Rϕ` 的**梯度（∇R）** 来优化轨迹的回报。公式表示为：`s_SDGD = (1+w)s_θ(τ_s, s, l) - w s_θ(τ_s, s, ∅) + λ∇R_ϕ(τ_s)`，其中前两项为CFG安全引导，最后一项为奖励梯度引导。
    2.  **可行轨迹重标注（FTR）**：
        *   **问题**：直接使用原始回报 `R(τ)` 进行梯度引导，可能同时增加奖励和成本（即奖励与成本正相关），导致“成本漂移”，破坏安全性。
        *   **解决方案**：在训练奖励预测器时，对轨迹的奖励标签进行修改。对于在可执行前缀（即规划中即将执行的前几步）中产生成本的轨迹，将其原始回报 `R(τ)` 减去一个惩罚项 `r_us`（`r_us < 0`），得到新目标 `R̂(τ)`。这使得奖励预测器学会**避免**那些高回报但会立即导致不安全行为的轨迹特征，从而让奖励梯度引导与安全目标更一致。
        *   **理论分析**：论文提供了命题1，在一阶近似和“前缀恢复对齐”条件下，证明了FTR能有效抑制由原始奖励引导引起的成本漂移。

### 3. 实验设计
*   **基准与数据集**：在 **DSRL 基准**上进行评估，该基准包含来自 Safety-Gymnasium、Bullet-Safety-Gym 和 MetaDrive 的 **38 个** 不同难度和形态（点机器人、车、蚂蚁等）的任务。
*   **对比方法**：与7类先进的离线安全RL方法对比，包括：
    *   **基于分布修正**：COptiDICE
    *   **基于惩罚**：CPQ
    *   **基于生成模型/Transformer**：CDT, TREBI (扩散模型), B2R (文中提及)
    *   **基于可行性引导**：FISOR
    *   **自适应策略方法**：CAPS (策略切换), CCAC (策略条件化)

### 4. 资源与算力
*   论文中仅提及了**推理效率**：SDGD 在 NVIDIA A6000 GPU 上达到 **2.1 Hz** 的决策频率，比 TREBI (1.1 Hz) 和 HD (1.0 Hz) 更快。
*   对于**模型训练所需的GPU型号、数量、时长等具体算力配置**，论文**未明确说明**。

### 5. 实验数量与充分性
*   **主实验**：在全部38个DSRL任务上评估，使用统一的严格成本限制（10），计算标准化奖励和成本。
*   **适应性实验**：
    *   **不同固定成本限制**：在4个代表性任务上测试了成本限制为10、20、30时的性能。
    *   **时变成本限制**：在单个回合内动态改变成本限制（1→3→10），测试实时适应能力。
*   **消融与分析实验**：
    *   **组件消融**：分别移除无分类器引导（w/o CFG）和奖励梯度引导（w/o CG）。
    *   **角色互换**：测试“用分类器引导处理成本，用无分类器引导处理奖励”的基线，验证解耦设计的必要性。
    *   **超参数分析**：系统测试了CFG权重 `w` 和奖励引导尺度 `λ` 的影响。
    *   **FTR分析**：分析了不同可行前缀长度 `f` 和奖励引导尺度对性能的影响。
*   **评价**：实验设计**较为充分和全面**。覆盖了静态、动态预算场景，进行了核心组件消融、与多种类型基线对比，并分析了方法对超参数的鲁棒性。评估遵循DSRL标准协议，结果具有可比性和客观性。

### 6. 主要结论与发现
1.  **卓越的安全性**：SDGD 在 **94.7%** (36/38) 的任务上满足了安全约束（标准化成本 ≤ 1），是所有对比方法中**安全合规性最强**的。
2.  **优异的性能**：在满足约束的方法中，SDGD 在 **21个** 任务上取得了**最高的奖励**，实现了更好的奖励-成本权衡（见图3帕累托前沿）。
3.  **强大的适应性**：**单个**训练好的SDGD模型能够**无需重新训练**，即适应不同严格程度的成本限制，以及**在回合内动态变化**的成本限制。
4.  **解耦的有效性**：消融实验证实，将成本限制（CFG）与奖励梯度引导解耦是必要的；互换角色会导致安全失败。
5.  **FTR的必要性**：FTR 能有效防止强奖励引导导致的安全违规，是平衡性能与安全的关键。

### 7. 优点
1.  **概念创新**：提出了将安全视为“区域限制”、将奖励视为“区域内优化”的**解耦新视角**，为安全轨迹生成提供了清晰的理论框架。
2.  **方法有效**：SDGD+FTR 的组合**简单而有效**，利用成熟的CFG技术处理约束，并用FTR巧妙地解决了奖励-成本冲突问题。
3.  **理论支撑**：提供了关于FTR如何抑制成本漂移的**一阶理论分析**，增强了方法的可信度。
4.  **实验扎实**：在大型标准基准上进行了全面测试，证明了方法在安全性、性能、适应性方面的显著优势。

### 8. 不足与局限
1.  **计算成本**：基于扩散模型的规划器通常**采样速度较慢**。尽管论文报告了2.1Hz的实时性，但这可能仍无法满足某些对实时性要求极高的应用场景（如高速自动驾驶）。论文也将“更快采样”列为未来工作。
2.  **任务性能差异**：虽然整体安全率高，但在某些任务上（如 `PointButton1`），其奖励性能并非最优，表明方法在**某些特定任务或动力学下可能存在优化不足**。
3.  **对离线数据质量的依赖**：作为离线RL方法，其性能**高度依赖离线数据集的质量和覆盖度**。在数据集中安全轨迹稀缺或分布有偏的情况下，方法的有效性可能会下降。
4.  **真实世界验证缺失**：所有实验均在仿真基准中进行，缺乏在**真实物理系统**（如真实机器人）上的验证，其实际部署的鲁棒性有待检验。

（完）
