---
title: Recovering Hidden Reward in Diffusion-Based Policies
title_zh: 基于扩散策略中隐藏奖励的恢复
authors: "Yanbiao Ji, Qiuchang Li, Yuting Hu, Shaokai Wu, Wenyuan Xie, Guodong Zhang, Qicheng He, Deyi Ji, Yue Ding, Hongtao Lu"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00623v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 通过去噪场将生成式动作建模与逆强化学习统一起来
tldr: 本文提出 EnergyFlow 框架，通过参数化标量能量函数，将生成动作建模与逆强化学习统一起来。该框架利用去噪分数匹配学习分数函数，恢复专家软 Q 函数的梯度，从而无需对抗训练即可提取奖励。理论证明约束学习场为保守场可降低假设复杂性并收紧分布外泛化界限。实验表明，EnergyFlow 在各种操作任务上实现了最先进的模仿性能，并为下游强化学习提供了有效的奖励信号。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00623v1/fig-001.webp\", \"caption\": \"Figure 3. Real-world evaluation tasks. We evaluate on two contact-rich manipulation tasks: Bottle (top), where the robot must grasp a bottle and place it into a cardboard box, and Drawer (bottom), where the robot must pull the drawer open.\", \"page\": 6, \"index\": 1, \"width\": 1017, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2605.00623v1/fig-002.webp\", \"caption\": \"Figure 6. RoboMimic task demonstrations. Each row visualizes a rollout sequence for a different task.\", \"page\": 22, \"index\": 2, \"width\": 1017, \"height\": 1134}, {\"url\": \"assets/figures/arxiv/2605.00623v1/fig-003.webp\", \"caption\": \"Figure 7. Meta-World task demonstrations. Meta-World task demonstrations. Each row visualizes a rollout sequence for a different task.\", \"page\": 23, \"index\": 3, \"width\": 1017, \"height\": 1123}, {\"url\": \"assets/figures/arxiv/2605.00623v1/fig-004.webp\", \"caption\": \"Figure 5. OOD generalization on RoboMimic. Success rate vs. initial position perturbation magnitude. ENERGYFLOW degrades more gracefully than baselines, with the gap widening at larger perturbations. Shaded regions indicate 95% confidence intervals.\", \"page\": 8, \"index\": 4, \"width\": 1017, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2605.00623v1/fig-005.webp\", \"caption\": \"Table 5. Hyperparameters for ENERGYFLOW Training and Inference.\", \"page\": 19, \"index\": 5, \"width\": 510, \"height\": 464}]"
motivation: 本文旨在通过去噪分数匹配学习分数函数，恢复专家软 Q 函数的梯度，从而无需对抗训练即可提取奖励。
method: EnergyFlow 框架通过参数化标量能量函数，其梯度为去噪场，将生成动作建模与逆强化学习统一起来。
result: EnergyFlow 在各种操作任务上实现了最先进的模仿性能，并为下游强化学习提供了有效的奖励信号。
conclusion: EnergyFlow 框架通过结构约束实现有效奖励提取，同时为策略泛化提供了有益的归纳偏置。
---

## 摘要
本文提出EnergyFlow框架，通过参数化标量能量函数（其梯度为去噪场）将生成式动作建模与逆强化学习相统一。我们证明在最大熵最优性条件下，通过去噪分数匹配学习到的分数函数可恢复专家软Q函数的梯度，从而无需对抗训练即可提取奖励。形式化证明表明，约束学习场为保守场可降低假设复杂度并收紧分布外泛化边界。我们进一步刻画了恢复奖励的可识别性，并界定了分数估计误差如何传播至动作偏好。实证表明，EnergyFlow在多种操作任务上实现了最先进的模仿性能，同时为下游强化学习提供了优于对抗性IRL方法和基于似然替代方法的有效奖励信号。这些结果表明，有效奖励提取所需的结构约束同时可作为策略泛化的有益归纳偏置。代码发布于https://github.com/sotaagi/EnergyFlow。

## Abstract
This paper introduces EnergyFlow, a framework that unifies generative action modeling with inverse reinforcement learning by parameterizing a scalar energy function whose gradient is the denoising field. We establish that under maximum-entropy optimality, the score function learned via denoising score matching recovers the gradient of the expert's soft Q-function, enabling reward extraction without adversarial training. Formally, we prove that constraining the learned field to be conservative reduces hypothesis complexity and tightens out-of-distribution generalization bounds. We further characterize the identifiability of recovered rewards and bound how score estimation errors propagate to action preferences. Empirically, EnergyFlow achieves state-of-the-art imitation performance on various manipulation tasks while providing an effective reward signal for downstream reinforcement learning that outperforms both adversarial IRL methods and likelihood-based alternatives. These results show that the structural constraints required for valid reward extraction simultaneously serve as beneficial inductive biases for policy generalization. The code is available at https://github.com/sotaagi/EnergyFlow.

---

## 论文详细总结（自动生成）

好的，这是对论文《Recovering Hidden Reward in Diffusion-Based Policies》的详细结构化总结。

### 1. 论文的核心问题与整体含义
*   **研究背景**：基于扩散模型的策略在机器人模仿学习中表现出色，因其能建模复杂的多模态动作分布。然而，它们通常仅通过行为克隆目标进行训练，只模仿“动作”而不理解专家行为背后的“意图”（即奖励函数），这限制了其在分布外情况下的鲁棒性和泛化能力。
*   **核心问题**：能否从表现优异的扩散策略中，**无需复杂的对抗训练**，就提取出隐含的、可解释的奖励信号，从而将强大的生成建模与基于奖励的推理和强化学习结合起来？
*   **整体含义**：本文提出了一种统一框架，证明在最大熵最优性假设下，扩散模型学习到的“去噪方向”（分数函数）本质上就是专家奖励函数（软Q函数）的梯度。通过施加适当的数学约束（保守场），可以稳定地从这个梯度中恢复出一个标量奖励函数，从而同时实现高性能模仿和有效的奖励提取。

### 2. 论文提出的方法论
*   **核心思想**：参数化一个**标量能量函数** `E_θ(s, a, t)`，其关于动作a的负梯度 `-∇_a E_θ` 被定义为去噪分数场。这强制该分数场是一个**保守场**（即某个标量函数的梯度），从而满足了从梯度恢复奖励的数学前提。
*   **关键技术细节**：
    1.  **理论桥梁（定理3.3）**：在最大熵最优策略的假设下，证明专家策略的分数函数 `∇_a log π_E(a|s)` 与其软Q函数的梯度 `∇_a Q*(s, a)` 成正比。因此，学习去噪分数等价于学习奖励梯度。
    2.  **保守场约束**：直接回归向量分数场可能不是保守的，导致恢复的“奖励”不满足传递性。通过将分数场定义为标量能量函数的梯度，从结构上保证了其保守性。
    3.  **训练目标**：采用去噪分数匹配进行训练。损失函数是预测的分数（即 `-∇_a E_θ`）与真实噪声方向之间的均方误差。
    4.  **奖励提取（中心化整形）**：训练后，能量 `E_θ` 可作为一个奖励信号 `r̃(s, a) = -[E_θ(a,s) - baseline(s)]`。通过减去一个状态相关的基线（在参考分布上对能量求期望），来消除状态依赖的积分常数 `c(s)`，使奖励仅反映同一状态下动作间的相对偏好。
*   **算法流程**：
    1.  **训练**：采样状态-动作对 `(s, a0)`、噪声 `ε` 和时间 `t`；构造噪声动作 `a_t = a0 + σ(t)ε`；通过网络前向传播计算能量 `E_θ(a_t, s, t)`；通过自动微分计算分数 `S_θ = -∇_{a_t} E_θ`；计算与真实噪声 `-ε/σ(t)` 的损失并反向传播更新 `θ`。
    2.  **推理（动作生成）**：从噪声分布采样 `a_T`，然后沿概率流ODE（使用 `∇_a E_θ` 驱动）从 `t=T` 积分到 `t≈0`，得到最终动作。
    3.  **奖励提供**：给定状态 `s`，对候选动作 `a` 计算其能量，并应用中心化整形公式得到奖励值 `r̃(s, a)`，可用于下游策略优化。

### 3. 实验设计
*   **数据集/场景**：
    *   **仿真**：使用两大主流机器人操作基准——**RoboMimic**（5个任务：Lift, Can, Square, Transport, ToolHang）和 **Meta-World**（5个任务：ButtonPress, DrawerOpen, Assembly, BinPicking, Hammer）。
    *   **真实机器人**：在AGIBOT G1机器人上部署，测试了两个接触丰富的操作任务（Bottle和Drawer）。
*   **Benchmark与对比方法**：与三大类方法进行对比：
    1.  **生成策略**：Diffusion Policy (SOTA扩散方法), Flow Policy。
    2.  **能量基方法**：Implicit BC, EBT-Policy。
    3.  **逆强化学习方法**：EBIL, NEAR, IQ-Learn。
    4.  **自回归策略**：LSTM-GMM（作为经典基线）。

### 4. 资源与算力
*   论文正文及附录**未明确说明**训练所使用的具体GPU型号、数量及总训练时长。
*   仅在“推理效率”实验部分提到，延迟测试是在 **NVIDIA A100** GPU 上进行的。

### 5. 实验数量与充分性
*   **实验数量**：论文设计了**六大组实验**来系统性地回答六个研究问题（RQ1-RQ6），覆盖了：
    1.  **模仿学习性能**（RQ1）：在两个仿真基准的10个任务上对比所有基线。
    2.  **真实机器人部署**（RQ2）：在2个真实任务上验证。
    3.  **奖励质量**（RQ3）：在2个任务上，用提取的奖励训练SAC智能体，与稀疏奖励、原始能量奖励、Oracle稠密奖励对比。
    4.  **分布外泛化**（RQ4）：在RoboMimic的5个任务上，测试不同级别初始位置扰动下的性能。
    5.  **超参数敏感性**（RQ5）：分析奖励提取时间参数 `γ` 的影响。
    6.  **推理效率**（RQ6）：对比不同方法的成功率和单步推理延迟。
*   **充分性与客观性**：
    *   **充分性**：实验设计全面，覆盖了方法验证的各个关键方面（性能、实用性、鲁棒性、效率），并在多个任务和仿真/真实平台上进行了测试，实验量充足。
    *   **客观性与公平性**：对比了当前最具代表性的多类方法；在仿真实验中报告了多轮运行的平均值和标准差；对于Diffusion Policy等基线，采用了与本文方法相似的网络主干，以进行公平的架构对比。实验设置描述清晰，结果可复现。

### 6. 论文的主要结论与发现
1.  **理论贡献**：严格证明了在最大熵框架下，去噪分数匹配可以恢复专家软Q函数的梯度，为从扩散策略中提取奖励提供了理论基础。
2.  **约束即正则**：强制分数场为保守场（通过能量函数参数化），不仅保证了奖励的数学有效性，还作为一种强大的归纳偏置，**降低了假设空间的复杂度**，从而在理论上和实验上都带来了**更好的分布外泛化性能**。
3.  **卓越的模仿性能**：EnergyFlow在RoboMimic和Meta-World上达到了最先进的平均成功率（93.8%和92.5%），尤其在复杂任务（如ToolHang）上优势明显。
4.  **有效的奖励信号**：提取出的“中心化能量”作为奖励，能有效指导下游强化学习训练，其性能显著优于稀疏奖励和原始能量奖励，接近Oracle稠密奖励。
5.  **实用性与效率**：方法成功部署于真实机器人，且推理速度与高效的Flow Policy相当，远快于需要迭代采样的Implicit BC。

### 7. 优点
1.  **理论坚实且优雅**：建立了扩散模型、能量模型和逆强化学习之间清晰的理论联系，核心证明简洁有力。
2.  **方法统一且巧妙**：通过一个标量能量函数，同时解决了高质量动作生成和可解释奖励提取两个问题，设计非常巧妙。
3.  **约束带来双重收益**：保守场约束不仅是理论必需，更被证明是一种能提升泛化的有效正则，体现了“结构偏差”的正面价值。
4.  **实验验证全面**：从仿真到实物，从模仿性能到奖励效用，从分布内到分布外，进行了系统、充分的评估，结论可信。
5.  **实用价值高**：为机器人学习提供了一种既能模仿又能推理意图的端到端方案，且推理效率满足实时控制需求。

### 8. 不足与局限
1.  **计算开销未量化**：虽然推理效率与Flow Policy相当，但**训练阶段**因需要计算能量函数关于高维动作的梯度（涉及二阶导），其计算成本和内存占用可能高于标准扩散策略。论文未对此进行详细分析与对比。
2.  **理论假设的局限性**：核心定理依赖于“专家策略满足最大熵最优性”的假设。在实际演示数据不完全最优或非平稳时，该假设可能不严格成立，对奖励恢复准确性的影响有待进一步研究。
3.  **奖励的可识别性局限**：如论文所述，恢复的奖励函数存在一个**状态依赖的偏移量 `c(s)`**。虽然中心化整形解决了同一状态下的动作排序问题，但这个偏移量使得跨状态的绝对价值比较变得模糊，可能影响其在某些需要长期信用分配的序列决策中的直接使用。
4.  **实验场景局限**：实验集中于**短视距、基于状态**的机器人操作任务。对于更复杂的、部分可观的、或需要长程规划的任务，方法的有效性仍需验证。
5.  **基线选择的细微偏差**：虽然对比了多类方法，但在某些类别中（如对抗IRL），可能还有其他的强基准（如最新的Diffusion-IRL方法）未被纳入比较。

（完）
