---
title: "Drifting Field Policy: A One-Step Generative Policy via Wasserstein Gradient Flow"
title_zh: 漂移场策略：一种基于Wasserstein梯度流的一步生成策略
authors: "Juil Koo, Mingue Park, Jiwon Choi, Yunhong Min, Minhyuk Sung"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07727v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 通过Wasserstein梯度流实现的单步生成策略
tldr: 本文提出了一种名为 Drifting Field Policy (DFP) 的非 ODE 单步生成策略。该方法将策略更新构建为朝向软目标策略的反向 KL Wasserstein-2 梯度流，每次更新对应概率空间中的梯度步骤。梯度被分解为向更高动作价值区域的上升和与锚定策略的得分匹配作为信任区域。作者进一步推导了一个简单可处理的替代损失，类似于在 critic 选择的前 K 个动作上进行行为克隆。实证表明，这种机制因其非 ODE 参数化而独特地受益于漂移主干。通过单步推理，DFP 在 Robomimic 和 OGBench 的多个操作任务上实现了最先进的性能，超越了基于 ODE 的策略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07727v1/fig-001.webp\", \"caption\": \"Table 1: Success rate (%) on Robomimic [39] and OGBench [44] tasks under the offline-to-online RL. Each cell reports mean ± std over 5 seeds. Best result per column in bold; second-best underlined.\", \"page\": 7, \"index\": 1, \"width\": 840, \"height\": 202}, {\"url\": \"assets/figures/arxiv/2605.07727v1/fig-002.webp\", \"caption\": \"Figure 2: Visualization of the 12 manipulation tasks used in our experiments. The top row shows the Robomimic Multi-Human tasks (Lift, Can, Square), and the remaining rows show the OGBench Cube environments with N ∈ {2, 3, 4} cubes. Each panel depicts the initial configuration of a representative episode.\", \"page\": 14, \"index\": 2, \"width\": 798, \"height\": 1205}, {\"url\": \"assets/figures/arxiv/2605.07727v1/fig-003.webp\", \"caption\": \"Figure 3: Online training curves for the backbone × loss ablation. Success rate over the online phase, comparing MVP [66], MVP w/ Ltop-K , DFP w/o Ltop-K , and DFP.\", \"page\": 18, \"index\": 3, \"width\": 811, \"height\": 190}, {\"url\": \"assets/figures/arxiv/2605.07727v1/fig-004.webp\", \"caption\": \"Figure 1: Success rate over training steps across Robomimic [39] and OGBench [44]. Solid lines and shaded regions show the mean and 95% confidence interval over five runs. Gray and white backgrounds indicate the offline and online phases, respectively.\", \"page\": 8, \"index\": 4, \"width\": 782, \"height\": 846}, {\"url\": \"assets/figures/arxiv/2605.07727v1/fig-005.webp\", \"caption\": \"Figure 4: Online training curves for the top-K ablation. Success rate over the online phase on Robomimic (top row) and the OGBench.\", \"page\": 19, \"index\": 5, \"width\": 782, \"height\": 883}, {\"url\": \"assets/figures/arxiv/2605.07727v1/fig-006.webp\", \"caption\": \"Figure 5: Online training curves for different λ values. Success rate over the online phase.\", \"page\": 19, \"index\": 6, \"width\": 782, \"height\": 229}, {\"url\": \"assets/figures/arxiv/2605.07727v1/fig-007.webp\", \"caption\": \"Table 6: Offline-to-online RL full results. Each cell shows offline → online (mean with std as subscript). Best result per column in bold; second-best underlined.\", \"page\": 17, \"index\": 7, \"width\": 831, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2605.07727v1/fig-008.webp\", \"caption\": \"Table 5: Detailed hyperparameters.\", \"page\": 16, \"index\": 8, \"width\": 568, \"height\": 745}]"
motivation: 提出一种基于漂移模型范式的非 ODE 单步生成策略，以提升策略性能。
method: DFP 将策略更新构建为朝向软目标策略的 Wasserstein-2 梯度流，并通过一个可处理的替代损失实现更新。
result: 在 Robomimic 和 OGBench 的多个操作任务上，DFP 超越了基于 ODE 的策略，取得了最先进的性能。
conclusion: DFP 是一种高效的单步生成策略，在多个机器人操作任务上实现了最先进的性能。
---

## 摘要
我们提出了漂移场策略（DFP），这是一种基于漂移模型范式的非常微分方程一步生成策略。我们将策略更新构建为朝向软目标策略的反向KL Wasserstein-2梯度流，使得每次DFP更新对应于概率空间中的梯度步。通过构造，该梯度被分解为向更高动作价值区域的上升以及与作为信任域的锚定策略的得分匹配。我们进一步推导了一个简单、易处理的替代损失，以替代原本难以处理的更新损失，类似于在批评器选择的前K个动作上进行行为克隆。实证研究发现，由于其非ODE参数化，这种机制独特地有益于漂移主干网络。通过一步推理，DFP在Robomimic和OGBench的多个操作任务上实现了最先进的性能，优于基于ODE的策略。

## Abstract
We propose Drifting Field Policy (DFP), a non-ODE one-step generative policy built on the drifting model paradigm. We frame the policy update as a reverse-KL Wasserstein-2 gradient flow toward a soft target policy, so that each DFP update corresponds to a gradient step in probability space. By construction, this gradient is decomposed into an ascent toward higher action-value regions and a score matching with the anchor policy as a trust region. We further derive a simple, tractable surrogate of the otherwise intractable update loss, akin to behavior cloning on top-K critic-selected actions. We find empirically that this mechanism uniquely benefits the drifting backbone owing to its non-ODE parameterization. With one-step inference, DFP achieves state-of-the-art performance on several manipulation tasks across Robomimic and OGBench, outperforming ODE-based policies.

---

## 论文详细总结（自动生成）

好的，这是对论文《Drifting Field Policy: A One-Step Generative Policy via Wasserstein Gradient Flow》的详细中文总结。

### 1. 核心问题与整体含义
*   **研究背景**：离线到在线强化学习（RL）是连续控制中的实用范式。为捕捉真实演示的多模态动作分布，生成式策略（如扩散、流模型）成为主流。为满足部署的低延迟需求，研究者进一步转向“一步推理”变体。
*   **核心问题**：基于ODE（如扩散、流）的策略，即使在一步推理变体中，其训练目标仍定义在整个ODE轨迹上。这导致在RL微调时，动作级别的奖励信号必须反向传播通过整个轨迹，引发**非平凡的轨迹级信用分配问题**，阻碍了策略的高效更新。
*   **研究动机**：寻求一种**完全绕过轨迹积分**的一步策略参数化方法，使输出级的奖励信号能直接在动作级别发挥作用，从而更高效地进行策略改进。

### 2. 提出的方法论
*   **核心思想**：提出**漂移场策略（DFP）**，一种基于**漂移模型**的非ODE一步生成策略。其核心是将策略更新构建为朝向**软策略改进目标**的**反向KL Wasserstein-2梯度流**，从而直接在概率空间进行梯度下降。
*   **关键技术细节**：
    1.  **策略参数化**：策略是一个单步前推映射 `πθ(·|s) = [fθ(·, s)]#pϵ`，从先验噪声直接生成动作，不含时间变量。
    2.  **理想更新与分解**：理论上，最小化 `KL(πθ∥π+)`（其中 `π+ ∝ πold exp(Q/α)`）的Wasserstein梯度流方向（即漂移场）可分解为两项：一项是向高Q值区域的梯度上升 `(∇aQ)`，另一项是与旧策略 `πold` 的得分匹配，后者充当了信任域约束。
    3.  **可处理的替代损失**：由于 `π+` 的归一化常数难以处理，论文提出一个简单替代方案：从旧策略 `πold` 中采样 `N` 个候选动作，选取其中 `Q` 值最高的 `K` 个，构成一个经验正样本集 `P_K(s)`。然后，训练策略使其生成的样本被“吸引”向这个 `P_K(s)` 集合（同时“排斥”当前策略自身的样本）。这**类似于在批评器筛选出的高质量动作上进行行为克隆**。
    4.  **总损失函数**：最终策略的损失是行为克隆损失 `L_BC`（锚定在离线数据）和上述Top-K策略改进损失 `L_top-K` 的加权和：`L(θ) = L_BC(θ) + λ L_top-K(θ)`。
*   **算法流程**：
    1.  从当前策略采样 `N‘` 个动作，执行 `Q` 值最高的一个，收集在线经验存入回放池。
    2.  从池中采样批次数据。
    3.  对于每个状态，从旧策略采样 `N` 个候选，构建Top-K正样本集 `P_K`；同时从当前策略生成若干样本。
    4.  计算 `L_BC`（以数据集动作为正样本）和 `L_top-K`（以 `P_K` 为正样本）。
    5.  用组合损失更新策略网络，用标准贝尔曼备份更新批评器网络。
    6.  使用指数移动平均更新旧策略网络。

### 3. 实验设计
*   **数据集/场景**：在12个机器人操作任务上进行评估，包括：
    *   **Robomimic** 的3个任务（Lift, Can, Square），使用多人类演示数据集。
    *   **OGBench** 的9个任务，涵盖 `cube-double`, `cube-triple`, `cube-quadruple` 环境，任务复杂度递增。
*   **Benchmark**：采用**离线预训练 → 在线微调**的标准协议，在线交互预算有限。
*   **对比方法**：
    *   **多步推理**：BFN, QC-BFN（带动作分块的BFN）。
    *   **蒸馏一步**：FQL, QC-FQL（带动作分块的FQL）。
    *   **免教师一步**：MVP（基于MeanFlow的一步策略，与DFP最可比）。

### 4. 资源与算力
*   论文在附录D中明确说明了计算资源。
*   **硬件**：使用NVIDIA RTX 3090 GPU和Intel Xeon Gold 6342 CPU。
*   **总耗时**：所有实验（包括主实验、消融实验等）总计约 **6500 GPU小时**。

### 5. 实验数量与充分性
*   **实验数量充足**：
    1.  **主实验**：在12个任务上对比5类基线方法，每个任务5颗随机种子。
    2.  **消融实验**：
        *   **主干网络与损失分离**：对比DFP、去掉`L_top-K`的DFP、MVP、以及给MVP加上`L_top-K`损失，清晰证明了**漂移主干**与**Top-K监督**的协同效应是性能提升的关键。
        *   **超参数敏感性**：对Top-K损失权重 `λ` 和正样本数量 `K` 进行了消融研究，表明方法对这些超参数具有鲁棒性。
    3.  **扩展实验**：还在纯离线RL设定下测试了DFP，展示了其通用性。
*   **客观性与公平性**：
    *   实验设置遵循了最可比基线MVP的协议，确保了对比公平。
    *   消融实验设计严谨，有效隔离了不同贡献因素（主干网络 vs. 损失函数）。
    *   报告了均值和标准差，并提供了完整的训练曲线。

### 6. 主要结论与发现
*   **性能领先**：DFP在12个任务中的9个上取得最佳性能，平均成功率高达**95.8%**，显著优于所有基线（最强基线QC-FQL为86.8%）。
*   **关键发现**：
    1.  **非ODE参数化的优势**：DFP的直接概率空间更新机制，使其能更高效地利用Top-K监督信号。相比之下，相同的Top-K监督应用到ODE-based的MeanFlow主干上（MVP w/ L_top-K）带来的提升非常有限，证明了**性能增益主要源于漂移主干本身**。
    2.  **理论直观的实践有效性**：将策略改进解释为Wasserstein梯度流，并由此推导出的Top-K替代损失，在实践中被证明非常有效且易于实现。

### 7. 优点
*   **理论框架新颖**：首次将漂移模型及Wasserstein梯度流视角引入RL策略改进，为理解策略更新提供了新的理论框架。
*   **方法实用高效**：提出的Top-K替代损失简单、可处理，无需计算批评器雅可比矩阵或显式的KL散度，实现方便。
*   **实验设计严谨**：在广泛的任务上进行测试，并设计了深入、有说服力的消融实验来验证核心主张，论证充分。

### 8. 不足与局限
*   **任务范围局限**：目前仅在模拟的连续控制操作任务上进行验证，尚未扩展到高维观测（如图像）、更复杂的任务或**真实机器人部署**。
*   **依赖批评器质量**：Top-K损失的有效性依赖于学习到的Q函数的准确性，这是所有演员-评论家方法的共同局限。
*   **理论分析的深度**：虽然提供了Wasserstein梯度流的解释和误差上界，但对于为何漂移主干能如此独特地受益于Top-K监督，其与ODE主干在**优化动力学**上的本质区别，还可进行更深入的理论分析。

（完）
