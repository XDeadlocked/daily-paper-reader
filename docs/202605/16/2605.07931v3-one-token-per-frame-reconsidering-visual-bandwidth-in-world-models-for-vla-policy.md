---
title: "One Token Per Frame: Reconsidering Visual Bandwidth in World Models for VLA Policy"
title_zh: 每帧一标记：重新思考VLA策略世界模型中的视觉带宽
authors: "Zuojin Tang, Shengchao Yuan, Xiaoxin Bai, Zhiyuan Jing, De Ma, Gang Pan, Bin Liu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07931v3"
tags: ["query:sr-0501"]
score: 10.0
evidence: 使用压缩视觉标记的世界模型增强型VLA策略用于机器人控制
tldr: 本文针对视觉-语言-动作模型中世界模块的参数化问题，提出了OneWM-VLA模型。该方法通过自适应注意力池化将每帧视觉信息压缩为单个语义令牌，并使用单一的流匹配目标联合生成潜在流和动作轨迹，而非传统的分离解码器。在冻结的2B参数骨干网络上仅使用14.71M LoRA参数进行训练，该方法在多个长时域任务上显著提升了性能，证明了降低每帧视觉带宽的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07931v3/fig-001.webp\", \"caption\": \"Figure 4: Evaluation suites used in this work: the LIBERO and MetaWorld MT50 simulation benchmarks, and a real Piper robot arm.\", \"page\": 6, \"index\": 1, \"width\": 826, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2605.07931v3/fig-002.webp\", \"caption\": \"Table 1: Main results on MetaWorld MT50. Success rates (%) across difficulty tiers and planning horizons H . ∆π0 and ∆π0.5 denote the absolute gain over π0 and π0.5.\", \"page\": 6, \"index\": 2, \"width\": 660, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2605.07931v3/fig-003.webp\", \"caption\": \"Table 13: Inference-time horizon sweep on LIBERO-Long, using the same model weights (trained at AH=20). Tuning only the inference horizon and the replan step moves the success rate from 93.4% to 95.6%.\", \"page\": 15, \"index\": 3, \"width\": 760, \"height\": 187}, {\"url\": \"assets/figures/arxiv/2605.07931v3/fig-004.webp\", \"caption\": \"Table 9: Task difficulty partition for MetaWorld MT50, following [36].\", \"page\": 14, \"index\": 4, \"width\": 802, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2605.07931v3/fig-005.webp\", \"caption\": \"Figure 2: The OneWM-VLA Framework. Through Adaptive Attention Pooling (Adaptive Fusion), we distill multi-view visual features into a dense latent world token for each perspective. These tokens are aligned with action trajectories via Joint Flow Matching to ensure alignment between environment dynamics transitions and robot control.\", \"page\": 3, \"index\": 5, \"width\": 820, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2605.07931v3/fig-006.webp\", \"caption\": \"Figure 5: PCA visualization of visual features on LIBERO-Long. Top: before pooling (256 tokens, mean-aggregated to a single vector per sample). Bottom: after Adaptive Attention Pooling (1 token per view per frame). The class-level structure remains visually separable after pooling.\", \"page\": 16, \"index\": 6, \"width\": 829, \"height\": 356}]"
motivation: 现有视觉-语言-动作模型中的世界模块通常以高视觉带宽处理每帧视觉流，导致在冻结骨干网络时，每帧表示和潜在动作耦合未被充分优化。
method: 提出OneWM-VLA模型，使用自适应注意力池化将每帧视图压缩为单个语义令牌，并通过单一的流匹配目标联合生成潜在流和动作轨迹。
result: "在MetaWorld MT50上平均成功率从47.9%提升至61.3%，在LIBERO-Long上达到95.6%，在真实Piper机械臂的Fold Cloth任务上达到60.0%。"
conclusion: 该研究表明，通过将每帧视觉信息压缩为单个语义令牌并采用流匹配目标，可以在极少的参数下显著提升VLA模型在长时域任务中的性能。
---

## 摘要
视觉-语言-动作（VLA）模型日益依赖辅助世界模块进行长时程规划，然而如何在预训练VLA之上参数化此类模块仍是一个开放的设计问题。现有的世界模型增强VLA通常以高视觉带宽将逐帧视觉流输入世界模块，并将其推演视为动作预测的副产品；在冻结骨干网络的有限适应预算下，这导致逐帧表示与潜在动作耦合均未得到充分检验。我们提出了OneWM-VLA，它通过自适应注意力池化将每帧视图压缩为单个语义标记，并在单一流匹配目标下生成相应的潜在流与动作轨迹，而非通过独立解码器连接二者。实验表明，在我们的设置下，逐帧视觉带宽可缩减至单个标记而不损害长时程性能。在π₀（20亿参数）骨干网络上使用1471万LoRA参数训练后，OneWM-VLA在MetaWorld MT50上的平均成功率从47.9%提升至61.3%，在LIBERO-Long上达到95.6%（对比π₀的85.2%），并在真实Piper机械臂的长时程可变形任务“折叠布料”上达到60.0%（对比π₀的20.0%）。

## Abstract
Vision-language-action (VLA) models increasingly rely on auxiliary world modules to plan over long horizons, yet how such modules should be parameterized on top of a pretrained VLA remains an open design question. Existing world-model-augmented VLAs typically pass the per-frame visual stream into the world module at high visual bandwidth and treat its rollout as a side product of action prediction; under a constrained adaptation budget on a frozen backbone, this leaves both the per-frame representation and the latent action coupling under-examined. We introduce OneWM-VLA, which compresses each view into a single semantic token per frame through an Adaptive Attention Pooling, and produces the resulting latent stream and the action trajectory under a single flow-matching objective rather than connecting them through a separate decoder. Empirically, we find that per-frame visual bandwidth can be reduced to a single token without compromising long-horizon performance under our setup. Trained with 14.71M LoRA parameters on a $π_0$ (2B) backbone, OneWM-VLA improves the average success rate from 47.9% to 61.3% on MetaWorld~MT50, reaches 95.6% on LIBERO-Long (vs.85.2% for $π_0$), and reaches 60.0% on the long-horizon deformable task Fold Cloth on a real Piper arm (vs.20.0% for $π_0$).

---

## 论文详细总结（自动生成）

好的，我将基于您提供的论文内容，按照您要求的要点生成一份详细的结构化总结。

### 1. 核心问题与整体含义
*   **研究背景**：视觉-语言-动作模型已成为机器人学习的主流范式，但其本质是反应式的，缺乏对未来环境演变的建模能力，导致在长时程任务中错误累积。
*   **核心问题**：在**预训练VLA模型冻结骨干、仅微调少量参数**的“适应范式”下，如何为其附加世界模型？具体而言：
    1.  **视觉带宽问题**：世界模块每帧需要多少视觉信息（令牌）才能有效支持长时程控制？
    2.  **耦合方式问题**：预测出的潜在状态流应如何与动作生成过程耦合？

### 2. 提出的方法论：OneWM-VLA
*   **核心思想**：提出“瓶颈-推演”耦合设计。
    *   **瓶颈**：将每帧视觉信息极度压缩为**单个语义令牌**，使世界模块的每步计算成本与规划时域无关。
    *   **推演**：使用**单一的流匹配目标**联合生成未来的潜在状态流和动作轨迹，使潜在流作为动作序列的结构性先验，而非独立的副产品。
*   **关键技术细节**：
    1.  **自适应注意力池化**：
        *   **输入**：预训练视觉编码器（PaliGemma）提取的每帧N个视觉令牌。
        *   **过程**：
            *   **多策略令牌池化**：使用三种互补的评分函数（`MAX`、`SUM`、`LEARN`）分别计算每个令牌的显著性权重，并聚合生成三个池化令牌。
            *   **自适应视图融合**：通过可学习的标量权重，将三个池化令牌融合为**每视图每帧1个**的“世界令牌”。
    2.  **潜在与动作的联合流匹配**：
        *   **输入**：当前时刻的世界令牌、语言指令、机器人状态，以及未来时刻的带噪潜在查询和动作查询。
        *   **过程**：将潜在流和动作轨迹置于同一个最优传输参数化的概率路径上，使用一个共享的Transformer模型，通过**单一的多目标损失函数**同时预测两者的去噪速度场。在推理时，联合去噪，仅执行动作分支。

### 3. 实验设计
*   **数据集/场景**：
    1.  **仿真基准**：
        *   **MetaWorld MT50**：50个操作任务，按难度分为4级，作为主要的长时程测试平台（规划时域H最高至30）。
        *   **LIBERO**：包含4个子套件（空间、物体、目标、长时程），其中“长时程”套件专门测试扩展时域操作。
    2.  **真实机器人**：
        *   **Piper 6-DoF机械臂**：执行三个任务——“捡香蕉”（刚性抓取）、“折叠布料”（长时程可变形操作）、“拉抽屉”（铰接物体）。
*   **主要对比方法**：
    *   **核心基线**：`π0` (2B参数) 和更大的 `π0.5` (3B参数)，这是当前最先进的VLA模型。
    *   **其他参考基线**：包括Diffusion Policy, ACT, Octo, OpenVLA, WorldVLA等，在结果表格中作为领域参考点列出。

### 4. 资源与算力
*   **骨干网络**：基于`π0`，其视觉语言骨干为PaliGemma-2B，联合专家模型为Gemma-300M。
*   **训练配置**：
    *   **GPU**：8张 NVIDIA A800 GPU。
    *   **训练步数**：30,000步。
    *   **微调参数**：仅使用LoRA微调，新增可训练参数量为14.71M（占骨干网络20亿参数的约0.74%）。

### 5. 实验数量与充分性
*   **实验充分性**：实验设计**系统且全面**，旨在直接回答论文提出的核心设计问题。
*   **主要实验组别**：
    1.  **主实验**：在三个平台（MetaWorld, LIBERO, 真实机器人）上对比基线，展示整体性能提升。
    2.  **五个核心消融研究**：
        *   **Q1（带宽扫描）**：将每帧令牌数从1扫到12，验证单令牌的有效性。
        *   **Q2（池化模块消融）**：对比自适应池化与静态平均池化，并分析内部各分支的必要性。
        *   **Q3（语义vs像素空间）**：对比在语义特征空间压缩与在原始像素空间压缩的效果。
        *   **Q4（联合生成消融）**：移除潜在预测分支或潜在损失，验证联合训练的重要性。
        *   **Q5（时域缩放分析）**：分析性能增益随规划时域变化的趋势，验证其对长时程任务的特异性。
    3.  **补充分析**：包括表征可判别性分析（Fisher比率、PCA）、超参数敏感性分析（融合温度、损失函数）、推理时域配置分析、吞吐量与内存分析。
*   **客观性与公平性**：
    *   **控制变量**：消融实验均在相同的训练预算（30K步LoRA）、硬件和骨干网络下进行。
    *   **对比公平**：与主要基线`π0`、`π0.5`使用相同的骨干和相似的训练设置进行对比。
    *   **结论限定**：论文多次强调结论适用于其研究的“适应范式”和特定任务集，避免过度外推。

### 6. 主要结论与发现
1.  **单令牌足够**：在研究的适应范式下，世界模块的每帧视觉带宽可以压缩到**单个语义令牌**，而不损害长时程控制性能；增加令牌数反而会降低性能。
2.  **自适应池化关键**：简单的静态池化性能大幅下降，融合了`MAX`、`SUM`、`LEARN`三种策略的自适应设计是有效的关键。
3.  **语义空间优于像素空间**：在预训练VLA的语义特征空间进行压缩，比在像素空间压缩效果更好。
4.  **联合生成至关重要**：潜在流与动作的联合流匹配训练带来的增益，远大于仅仅增加一个潜在分支的容量。
5.  **优势在长时程**：相比基线，OneWM-VLA的性能优势随着规划时域增长而**越发明显**，尤其在困难任务上，验证了其提升长时程稳定性的设计初衷。

### 7. 优点
*   **概念新颖**：明确提出了在有限适应预算下“世界模块所需视觉带宽”这一被忽视的设计问题。
*   **方法高效**：实现了极致的压缩（每帧1令牌）和极少的新增参数（14.71M），计算成本与视觉分辨率无关，适合长时程推演。
*   **耦合设计优雅**：通过联合流匹配将环境动力学预测与动作生成紧密耦合，而非松散连接。
*   **验证系统全面**：通过一系列环环相扣的消融实验，扎实地验证了每个设计选择的有效性和必要性。

### 8. 不足与局限
*   **范围局限**：所有结论基于单一的预训练VLA骨干（`π0`）和特定的一组操作任务（MetaWorld, LIBERO）。未在更大骨干、更复杂感知场景（如开放世界）或完全不同类型的任务（如导航）上验证。
*   **训练预算固定**：带宽扫描等实验是在固定训练步数（30K）下进行的。作者指出，更长的训练可能使更多令牌变得有效，此点未被探索。
*   **动态与部分可观测性**：方法主要处理多视图观察，但未明确讨论如何处理高度动态场景或部分可观测问题。
*   **推理延迟**：虽然每步令牌数固定，但联合流匹配推理需要多步去噪（文中为10步），可能影响实时性。论文提及降低推理成本是未来工作。
*   **理论分析有限**：对“为何单令牌更有效”提供了实验观察（如隐式正则化）和直观解读，但缺乏严格的理论分析。

（完）
