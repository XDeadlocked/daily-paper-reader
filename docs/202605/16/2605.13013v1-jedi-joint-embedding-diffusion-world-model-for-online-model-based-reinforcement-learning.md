---
title: "JEDI: Joint Embedding Diffusion World Model for Online Model-Based Reinforcement Learning"
title_zh: JEDI：用于在线基于模型强化学习的联合嵌入扩散世界模型
authors: "Jing Yu Lim, Rushi Shah, Zarif Ikram, Samson Yu, Haozhe Ma, Tze-Yun Leong, Dianbo Liu"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13013v1"
tags: ["query:sr-0501"]
score: 10.0
evidence: 用于基于模型的强化学习的扩散世界模型
tldr: "本研究针对在线模型强化学习中扩散世界模型效率与性能的权衡问题，提出JEDI（联合嵌入扩散）模型。该方法结合JEPA框架和扩散去噪损失，直接学习潜在空间以预测未来状态，无需依赖预训练模型。实验表明，JEDI在Atari100k上表现优异，比像素扩散基线节省43%VRAM，采样快3倍以上，训练快2.5倍，且任务性能特征不同，证明端到端潜在学习能带来超越计算效率的改进。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13013v1/fig-001.webp\", \"caption\": \"Table 4: Atari100k tasks with number of actions and shooter-task label.\", \"page\": 25, \"index\": 1, \"width\": 452, \"height\": 625}, {\"url\": \"assets/figures/arxiv/2605.13013v1/fig-002.webp\", \"caption\": \"Figure 14: JEDI versus HI training curves on Atari100k\", \"page\": 25, \"index\": 2, \"width\": 775, \"height\": 187}, {\"url\": \"assets/figures/arxiv/2605.13013v1/fig-003.webp\", \"caption\": \"Table 5: Atari100k overall performance. Bold refers to SOTA. We report results for batch size 32 and 64. Green score means it is the higher score and we take that result. We retrieve the updated results of STORM from [45]. TWISTER does not publish seed-level results, so we cannot compute its exact IQM or optimality gap. JEDI achieves SOTA optimality gap and IQM and runner-up performance in number of super-human games. JEDI achieves SOTA results in 7 games.\", \"page\": 26, \"index\": 3, \"width\": 668, \"height\": 504}, {\"url\": \"assets/figures/arxiv/2605.13013v1/fig-004.webp\", \"caption\": \"Figure 1: Joint Embedding DIffusion (JEDI) world model. During training, observations are encoded into clean latents, the next latent is noised, and the denoiser predicts the direction back toward the clean future latent while gradients update the encoder end to end. During inference, the model conditions on past latents and actions, starts from noise, and denoises to the next latent for imagination and policy learning.\", \"page\": 2, \"index\": 4, \"width\": 636, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2605.13013v1/fig-005.webp\", \"caption\": \"Table 2: Model architecture details for JEDI. Green font refers to changes as compared to pixel-based diffusion world model baseline [7]. Downsampling layers refers to the Maxpool operation that downsamples the input height and width by a factor of 2.\", \"page\": 23, \"index\": 5, \"width\": 644, \"height\": 627}, {\"url\": \"assets/figures/arxiv/2605.13013v1/fig-006.webp\", \"caption\": \"Table 3: Training hyperparameters for JEDI. Green font refers to changes as compared to pixel-based diffusion world model baseline [7].\", \"page\": 24, \"index\": 6, \"width\": 830, \"height\": 988}]"
motivation: 现有扩散世界模型在在线模型强化学习中存在效率与性能的权衡，需要一种端到端的潜在扩散方法。
method: 提出JEDI（联合嵌入扩散）模型，结合JEPA框架和扩散去噪损失，直接学习潜在空间以预测未来状态。
result: "JEDI在Atari100k上表现优异，比像素扩散基线节省43%VRAM，采样快3倍以上，训练快2.5倍。"
conclusion: JEDI通过联合嵌入扩散框架，实现了更高效、性能更优的端到端世界模型，为在线模型强化学习提供了新方向。
---

## 摘要
扩散世界模型最近在基于模型的在线强化学习中变得具有竞争力，但当前方法存在一个矛盾：像素扩散效果显著但计算成本高昂，而最新的潜在扩散方法提高了效率却表现欠佳。后者还依赖于单独训练的潜在表示，而非推动现代基于模型强化学习进展的端到端世界模型目标。特别是，JEPA风格的预测表示学习已成为世界建模和基于模型强化学习中一个极具前景的方向。与此同时，扩散风格的目标在多个领域受到关注，其中迭代优化对于多模态和随机目标是一种有前途的方法。综合这些趋势，我们提出了联合嵌入扩散模型（JEDI），这是首个在线端到端潜在扩散世界模型。JEDI通过JEPA框架直接从扩散去噪损失中学习其潜在空间，利用去噪来学习和预测未来潜在表示，而非依赖重构和预训练模型。我们提供了理论动机，表明传统的JEPA目标会引入预测信息瓶颈，而条件扩散去噪允许一种密切相关的预测-压缩分解。实证表明，JEDI在Atari100k上具有竞争力，并在直接可比的情况下优于使用单独训练潜在表示的基线。相对于像素扩散基线，JEDI减少了43%的显存使用，世界模型采样速度提升超过3倍，训练速度提升2.5倍。JEDI还展现出与像素基线明显不同的任务级性能特征，这表明端到端预测潜在表示带来的改变不仅仅是计算效率的提升。

## Abstract
Diffusion world models have recently become competitive for online model-based reinforcement learning, but current approaches expose a tension: pixel diffusion is effective but computationally expensive while the latest latent diffusion approach improves efficiency yet performs subpar. The latter also relies on separately trained latents rather than the end-to-end world-model objectives that have driven much of modern MBRL progress. In particular, JEPA-style predictive representation learning has emerged as an especially promising direction for world modeling and MBRL. Concurrently, diffusion-style objectives have gained traction across multiple domains, with iterative refinement as a promising approach for multimodal and stochastic targets. Taken together, these trends motivate Joint Embedding DIffusion (JEDI), the first online end-to-end latent diffusion world model. JEDI learns its latent space directly from the diffusion denoising loss with a JEPA framework, using denoising to learn and predict future latents rather than relying on reconstruction and pretrained models. We provide a theoretical motivation showing that conventional JEPA objectives induce a predictive information bottleneck, and that conditional diffusion denoising admits a closely related predictive-compression decomposition. Empirically, JEDI is competitive on Atari100k and outperforms the baseline with seperately trained latents where directly comparable. Relative to the pixel diffusion baseline, JEDI uses 43% less VRAM, over 3$\times$ faster world-model sampling, and 2.5$\times$ faster training. JEDI also exhibits a markedly different task-level performance profile from the pixel baseline, suggesting that end-to-end predictive latents change more than compute alone.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容，按照要求生成的结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前基于扩散模型的世界模型在在线模型强化学习中存在一个核心矛盾：
    *   **像素扩散**（如DIAMOND）：性能强，但直接在像素空间进行去噪计算成本极高（内存、采样、训练时间）。
    *   **潜在扩散**（如HI）：通过使用预训练的潜在空间提高了效率，但性能不及像素扩散，且其潜在表示并非由世界模型目标端到端驱动，违背了现代MBRL通过端到端表示学习取得进展的核心原则。
*   **研究目标**：提出一种**端到端的潜在扩散世界模型**，旨在同时实现**高性能**（媲美像素扩散）和**高效率**（超越潜在扩散），并探索由预测性目标驱动的潜在表示所带来的本质影响。

### 2. 论文提出的方法论
*   **核心思想**：**JEDI (Joint Embedding DIffusion)**，即联合嵌入扩散。其核心是将**JEPA的预测表示学习思想**与**扩散模型的迭代去噪生成能力**相结合，在一个统一的框架内端到端地学习潜在空间并进行未来状态预测。
*   **关键技术细节**：
    1.  **架构**：包含一个编码器（将观测映射到干净潜在表示）、一个条件扩散动力学模型（预测下一时刻潜在）、以及奖励/终止预测头。
    2.  **训练**：
        *   编码器将当前和未来观测编码为干净潜在 `z0_t` 和 `z0_{t+1}`。
        *   对未来的干净潜在 `z0_{t+1}` 添加噪声得到 `zτ_{t+1}`。
        *   去噪器 `Dθ` 以 `zτ_{t+1}`、过去潜在和动作为条件，预测去噪方向（目标是 `z0_{t+1}`）。
        *   **关键**：来自扩散损失和奖励损失的梯度会**直接更新编码器**，使其学习到的潜在空间专为未来的预测和控制而优化，而非为了重建像素。
    3.  **稳定训练技巧**：
        *   **停止梯度**：对目标未来潜在 `z0_{t+1}` 使用停止梯度，防止训练不稳定。
        *   **潜在值截断**：使用 `tanh` 函数将潜在值限制在 `[-3, 3]` 范围内，稳定扩散过程。
        *   **随机切换**：在训练时，随机选择使用去噪器输出或编码器输出作为下一步的潜在条件，平衡一致性与监督。
*   **理论动机**：论文从概率图模型出发，推导出JEPA目标和JEDI的**条件扩散去噪目标**都可以解释为一种**变分信息瓶颈**的形式。该分解表明，这些目标鼓励潜在表示 `Z` 保留对预测未来有用的信息（通过 `I(Z1; Z2)` 项），同时压缩掉与输入 `X` 相关的冗余信息（通过 `-I(X; Z)` 项）。这为使用扩散去噪作为端到端表示学习信号提供了理论依据。

### 3. 实验设计
*   **主要数据集/场景**：
    *   **Atari100k**：包含26个游戏的经典在线MBRL基准，每个游戏仅使用10万次环境交互。这是核心评估场景。
    *   **Craftium**：一个3D、类似《我的世界》的具身决策环境，用于验证方法在2D Atari之外的泛化能力。
    *   **随机跳帧Atari**：在三个Atari游戏上引入随机跳帧（2-6帧），用于测试模型对随机动力学的鲁棒性。
*   **Benchmark与对比方法**：
    *   **主要基线**：
        *   **DIAMOND**：像素空间扩散世界模型（性能强，效率低）。
        *   **HI (Horizon Imagination)**：使用预训练潜在表示的潜在扩散世界模型（效率高，性能较弱）。
    *   **其他SOTA对比**：DreamerV3, STORM, TWM, IRIS, TWISTER（涵盖了RNN、Transformer、扩散等不同架构的世界模型）。

### 4. 资源与算力
*   **硬件**：主要使用 **NVIDIA A100** GPU。
*   **训练时间**：
    *   **JEDI**：在Atari100k上完成一次运行（5个种子）约需 **38 A100小时**。
    *   **对比**：DIAMOND需98小时，HI需27小时（但HI参数量更大，为97M vs JEDI的13.5M）。
*   **效率提升**：相比DIAMOND，JEDI节省 **43% VRAM**，世界模型采样速度快 **3倍以上**，训练速度快 **2.5倍以上**。

### 5. 实验数量与充分性
*   **主实验**：在26个Atari游戏（各5种子）和4个Craftium任务（各3种子）上进行全面评估，实验规模符合领域标准。
*   **消融实验**：
    1.  **潜在学习方式消融**：对比了JEDI（联合预测训练）、添加解码器重建损失、使用MSE损失替代扩散损失、移除扩散梯度、使用预训练VAE潜在等五种方式，验证了端到端扩散去噪信号的有效性。
    2.  **设计选择消融**：测试了EMA目标编码器、确定性切换、移除随机切换、同时移除切换和潜在截断等变体，验证了所提稳定技巧的必要性。
*   **公平性与客观性**：
    *   **基准对比**：与当前SOTA方法在标准Atari100k协议下对比，使用了规范的评估指标（均值、IQM、最优性差距）。
    *   **提出新指标**：针对算术平均HNS可能掩盖在困难任务上进步的缺点，提出了**调和平均HNS**，以更公平地反映在低分游戏上的改进。
    *   **超参数报告**：报告了在部分任务上选择了两个扩散批次大小（32和64）中更好的结果，并指出基线HI也采用了类似的多设置报告策略，这在一定程度上是领域内常见做法，但可能引入轻微的选择偏差。

### 6. 论文的主要结论与发现
1.  **可行性验证**：首次证明了**端到端的潜在扩散世界模型**是可行的，并且具有竞争力。JEDI在Atari100k上的综合表现（IQM、最优性差距）达到SOTA水平。
2.  **效率与性能兼得**：JEDI在保持与像素扩散模型（DIAMOND）相近性能的同时，**大幅提升了计算和内存效率**。
3.  **本质性改变**：端到端预测性潜在表示带来的改变**不仅仅是效率提升**。JEDI与DIAMOND展现出**截然不同的任务级性能特征**：JEDI在**动作空间大、具有射击类动态**的复杂困难任务上（低HNS区域）表现更优；而DIAMOND在相对简单的高HNS任务上优势更明显。这表明学习到的潜在表示改变了策略优化的难度格局。
4.  **理论支撑**：为JEPA和条件扩散去噪提供了**信息瓶颈视角**的理论解释，将二者统一到一个表示学习框架下。

### 7. 优点
*   **方法创新**：巧妙地将JEPA的预测学习与扩散模型的生成能力结合，提出了首个端到端的潜在扩散世界模型框架。
*   **效率显著**：在模型参数量相近的情况下，相比像素扩散基线实现了显著的训练和采样加速，以及显存节省，提升了方法的实用性。
*   **分析深入**：不仅报告了聚合性能，还通过任务特征分析、新评估指标（Har-HNS）和定性轨迹对比，深入揭示了方法带来的**性能特征转变**，论证了其超越单纯计算节省的价值。
*   **理论联系**：提供了基于信息瓶颈的理论动机，增强了方法的设计原则和解释性。

### 8. 不足与局限
*   **实验范围有限**：主要验证集中在Atari100k和Craftium。尚未在更复杂的连续控制领域（如DMControl）、程序生成环境（Procgen）或更高分辨率视觉域进行测试，泛化能力有待进一步证明。
*   **非效率最优**：虽然比DIAMOND快，但JEDI（38小时）的训练时间仍长于使用预训练模型的HI（27小时）。效率优势是相对的。
*   **对设计选择和超参数敏感**：性能依赖于潜在截断、随机切换、停止梯度等稳定技巧，并且对扩散批次大小等超参数敏感（需在32和64间选择）。
*   **理论优化不完整**：论文的理论部分指出了完整的变分信息瓶颈目标包含一个瓶颈正则项 `R1`，但JEDI目前并未显式优化该项，而是依赖于实践技巧（如停止梯度、降低编码器学习率）来隐式防止崩溃。理论推导与工程实现存在一定间隙。

（完）
