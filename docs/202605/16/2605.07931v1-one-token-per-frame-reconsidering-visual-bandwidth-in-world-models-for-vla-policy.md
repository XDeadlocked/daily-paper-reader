---
title: "One Token Per Frame: Reconsidering Visual Bandwidth in World Models for VLA Policy"
title_zh: 每帧一标记：重新思考VLA策略世界模型中的视觉带宽
authors: "Zuojin Tang, Shengchao Yuan, Xiaoxin Bai, Zhiyuan Jin, De Ma, Gang Pan, Bin Liu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07931v1"
tags: ["query:sr-0501"]
score: 10.0
evidence: 该论文提出了OneWM-VLA，将世界模型集成到视觉-语言-动作（VLA）模型中，用于机器人规划。
tldr: 本文针对视觉-语言-动作模型中世界模块的参数化问题，提出了OneWM-VLA模型。该方法通过自适应注意力池化将每帧视觉信息压缩为单个语义令牌，并使用单一的流匹配目标联合生成潜在状态流和动作轨迹，避免了传统方法中高带宽视觉流和分离解码器的限制。在冻结的2B参数主干上仅使用14.71M LoRA参数进行训练，该模型在多个长时域任务上显著提升了性能，证明了降低每帧视觉带宽的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07931v1/fig-001.webp\", \"caption\": \"Figure 4: Evaluation suites used in this work: the LIBERO and MetaWorld MT50 simulation benchmarks, and a real Piper robot arm.\", \"page\": 6, \"index\": 1, \"width\": 826, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2605.07931v1/fig-002.webp\", \"caption\": \"Table 1: Main results on MetaWorld MT50. Success rates (%) across difficulty tiers and planning horizons H . ∆π0 and ∆π0.5 denote the absolute gain over π0 and π0.5.\", \"page\": 6, \"index\": 2, \"width\": 660, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2605.07931v1/fig-003.webp\", \"caption\": \"Table 13: Inference-time horizon sweep on LIBERO-Long, using the same model weights (trained at AH=20). Tuning only the inference horizon and the replan step moves the success rate from 93.4% to 95.6%.\", \"page\": 15, \"index\": 3, \"width\": 760, \"height\": 187}, {\"url\": \"assets/figures/arxiv/2605.07931v1/fig-004.webp\", \"caption\": \"Table 9: Task difficulty partition for MetaWorld MT50, following [36].\", \"page\": 14, \"index\": 4, \"width\": 802, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2605.07931v1/fig-005.webp\", \"caption\": \"Figure 2: The OneWM-VLA Framework. Through Adaptive Attention Pooling (Adaptive Fusion), we distill multi-view visual features into a dense latent world token for each perspective. These tokens are aligned with action trajectories via Joint Flow Matching to ensure alignment between environment dynamics transitions and robot control.\", \"page\": 3, \"index\": 5, \"width\": 820, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2605.07931v1/fig-006.webp\", \"caption\": \"Figure 5: PCA visualization of visual features on LIBERO-Long. Top: before pooling (256 tokens, mean-aggregated to a single vector per sample). Bottom: after Adaptive Attention Pooling (1 token per view per frame). The class-level structure remains visually separable after pooling.\", \"page\": 16, \"index\": 6, \"width\": 829, \"height\": 356}]"
motivation: 现有视觉-语言-动作模型中的世界模块通常以高视觉带宽处理每帧信息，且在冻结主干网络时未能充分优化每帧表示和潜在动作耦合。
method: 提出OneWM-VLA模型，使用自适应注意力池化将每帧视觉信息压缩为单个语义令牌，并通过单一的流匹配目标同时生成潜在状态流和动作轨迹。
result: "在MetaWorld MT50上平均成功率从47.9%提升至61.3%，在LIBERO-Long上达到95.6%，在真实Piper机械臂的Fold Cloth任务上达到60.0%。"
conclusion: 该研究表明，通过将每帧视觉信息压缩为单个语义令牌并采用流匹配目标，可以在极少的参数下显著提升VLA模型在长时域任务中的性能。
---

## 摘要
视觉-语言-动作（VLA）模型日益依赖辅助世界模块进行长时程规划，然而如何在预训练VLA之上参数化此类模块仍是一个开放的设计问题。现有的世界模型增强VLA通常以高视觉带宽将逐帧视觉流输入世界模块，并将其推演视为动作预测的副产品；在冻结骨干网络的有限适应预算下，这导致逐帧表示与潜在动作耦合均未得到充分检验。我们提出了OneWM-VLA，它通过自适应注意力池化将每帧视图压缩为单个语义标记，并在单一流匹配目标下生成相应的潜在流与动作轨迹，而非通过独立解码器连接二者。实验表明，在我们的设置下，逐帧视觉带宽可缩减至单个标记而不损害长时程性能。在π₀（20亿参数）骨干网络上使用1471万LoRA参数训练后，OneWM-VLA在MetaWorld MT50上的平均成功率从47.9%提升至61.3%，在LIBERO-Long上达到95.6%（对比π₀的85.2%），并在真实Piper机械臂的长时程可变形任务“折叠布料”上达到60.0%（对比π₀的20.0%）。

## Abstract
Vision-language-action (VLA) models increasingly rely on auxiliary world modules to plan over long horizons, yet how such modules should be parameterized on top of a pretrained VLA remains an open design question. Existing world-model-augmented VLAs typically pass the per-frame visual stream into the world module at high visual bandwidth and treat its rollout as a side product of action prediction; under a constrained adaptation budget on a frozen backbone, this leaves both the per-frame representation and the latent action coupling under-examined. We introduce OneWM-VLA, which compresses each view into a single semantic token per frame through an Adaptive Attention Pooling, and produces the resulting latent stream and the action trajectory under a single flow-matching objective rather than connecting them through a separate decoder. Empirically, we find that per-frame visual bandwidth can be reduced to a single token without compromising long-horizon performance under our setup. Trained with 14.71M LoRA parameters on a $π_0$ (2B) backbone, OneWM-VLA improves the average success rate from 47.9% to 61.3% on MetaWorld~MT50, reaches 95.6% on LIBERO-Long (vs.85.2% for $π_0$), and reaches 60.0% on the long-horizon deformable task Fold Cloth on a real Piper arm (vs.20.0% for $π_0$).

---

## 论文详细总结（自动生成）

### 论文总结：One Token Per Frame: Reconsidering Visual Bandwidth in World Models for VLA Policy

#### 1. 核心问题与整体含义
*   **研究背景**：视觉-语言-动作模型已成为机器人学习的主流范式，但其本质是反应式的，缺乏对动作所引发环境动态变化的预测能力，导致在长时程任务中误差累积。
*   **核心问题**：在为预训练VLA模型添加世界模块时，存在两个关键设计问题：1) **世界模块需要多少逐帧视觉带宽**才能支持长时程控制？2) 生成的潜在状态流应如何与**动作生成耦合**？
*   **研究动机**：传统方法（如像素级预测）计算成本高、难以扩展；而潜在空间方法又未在“冻结主干、微调少量参数”的VLA适应范式下得到充分探索。本文旨在探索在此范式下，能否通过极致的视觉压缩和更好的耦合设计，实现高效且有效的长时程规划。

#### 2. 提出的方法论
*   **核心思想**：提出 **“瓶颈-推演耦合”** 设计。
    *   **瓶颈**：将每帧、每个视角的视觉信息极度压缩为**单个语义潜在标记**，使世界模块的每步计算开销与规划时域无关。
    *   **推演**：使用**单一的流匹配目标**，联合生成未来的潜在状态流和未来的动作轨迹，使潜在流作为动作序列的结构性先验，而非独立的副产品。
*   **关键技术细节**：
    1.  **自适应注意力池化**：
        *   **视觉编码**：使用预训练的PaliGemma编码器提取每帧的N个视觉标记。
        *   **多策略标记池化**：采用三种互补的评分函数（`MAX`、`SUM`、`LEARN`）为每个标记计算注意力权重，并分别聚合得到三个池化后的标记。
        *   **自适应视图融合**：通过可学习的标量权重，将三个池化标记融合为**单个每帧世界标记**。
    2.  **潜在与动作的联合流匹配**：
        *   **联合概率路径**：将未来动作序列和未来潜在世界标记分别与噪声进行线性插值，共享相同的时间调度。
        *   **多目标流匹配损失**：模型同时预测潜在分支和动作分支的速度场，损失函数为两者加权和。在训练和推理中，两个分支通过自注意力共同演化，实现紧密耦合。

#### 3. 实验设计
*   **数据集/场景**：
    *   **模拟基准**：`MetaWorld MT50`（50个操作任务，按难度分级）、`LIBERO`（包含`Spatial`, `Object`, `Goal`, `Long`四个任务套件）。
    *   **真实机器人**：6自由度`Piper`机械臂，执行“捡香蕉”、“折叠布料”、“拉抽屉”三个任务。
*   **Benchmark**：以长时程规划性能（成功率）为核心评估指标，特别是在高难度和长时域任务上的表现。
*   **对比方法**：
    *   **主要基线**：VLA模型 `π0` (2B参数) 和更大的 `π0.5` (3B参数)。
    *   **其他参考**：在LIBERO上对比了`Diffusion Policy`、`Octo`、`OpenVLA`、`CoT-VLA`、`WorldVLA`、`GR00T-N1`等众多前沿方法。

#### 4. 资源与算力
*   **明确说明**：在**8块NVIDIA A800 GPU**上进行了训练。
*   **训练规模**：总训练步数为**30K步**。
*   **参数量**：在冻结的`π0`（20亿参数）主干上，仅训练了**1471万**个LoRA参数。

#### 5. 实验数量与充分性
*   **实验组数**：实验设计系统且全面，主要包括：
    1.  **主实验**：在三个平台（MT50, LIBERO, 真实机器人）上对比基线，验证整体有效性。
    2.  **核心消融研究（Q1-Q5）**：围绕五个关键问题设计了多组对照实验，包括：逐帧标记数量扫描、自适应池化模块/分支消融、语义vs像素压缩对比、联合生成目标消融、不同规划时域下的性能分析。
    3.  **敏感性分析**：对融合温度τ、流匹配损失的距离度量等超参进行了扫描。
    4.  **表征分析**：通过Fisher比率和PCA可视化，分析了压缩前后特征的判别性保留情况。
*   **充分性与客观性**：实验数量充足，覆盖了模拟与真实场景、短时域与长时域、不同任务难度。消融实验控制变量清晰，能有效归因性能提升的来源。与大量基线进行公平对比，结果具有说服力。

#### 6. 主要结论与发现
*   **核心发现**：在本文研究的适应范式下，**逐帧视觉带宽可以压缩至单个语义标记**，而不会损害长时程控制性能；相反，过多标记会降低性能。
*   **有效性**：OneWM-VLA在多个基准上显著超越了强大的`π0`和`π0.5`基线，尤其在**长时程、高难度任务**（如MetaWorld的Hard/Very Hard级、LIBERO-Long、真实机器人叠布料）上提升最为显著。
*   **归因分析**：性能增益主要来源于**自适应池化设计**（而非简单压缩）和**联合流匹配目标**（而非仅增加潜在分支容量）的耦合效应。

#### 7. 优点
*   **方法创新**：提出了新颖的“瓶颈-推演耦合”设计，概念清晰，为VLA世界模型的设计提供了新视角（视觉带宽）。
*   **高效简洁**：在极少的可训练参数下（14.71M LoRA），实现了显著的性能提升，计算效率高。
*   **实验扎实**：评估体系完整，包含详尽的消融研究，结论支撑有力。同时验证了模拟与真实世界的迁移能力。
*   **可解释性**：自适应池化中各分支的权重在训练后固定，提供了行为可解释性。

#### 8. 不足与局限
*   **范围局限**：研究结论明确限定于特定的VLA主干（`π0`）、特定的操作任务套件以及固定的训练预算（30K步）。尚未在更复杂感知环境、更大主干网络或更长训练周期下验证其普适性。
*   **应用限制**：方法专注于提升规划时域的稳定性，但未显著降低单步推理延迟。对于需要极高实时性的应用可能仍需优化。
*   **偏差风险**：实验主要在相对干净的模拟和实验室环境中进行。尽管进行了包含干扰物的鲁棒性测试，但在完全非结构化、动态的真实环境中性能未知。
*   **未来方向**：作者指出，未来可将每帧压缩与轻量级标记记忆机制结合，以同时提升长时程稳定性和单步效率。

（完）
