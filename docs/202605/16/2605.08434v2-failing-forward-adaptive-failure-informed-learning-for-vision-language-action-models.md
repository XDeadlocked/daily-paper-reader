---
title: "Failing Forward: Adaptive Failure-Informed Learning for Vision-Language-Action Models"
title_zh: 失败前行：视觉-语言-动作模型的自适应故障感知学习
authors: "Meng Zheng, Samhita Marri, Anwesa Choudhuri, Benjamin Planche, Zhongpai Gao, Van Nguyen Nguyen, Terrence Chen, Girish Chowdhary, Ziyan Wu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08434v2"
tags: ["query:sr-0501"]
score: 10.0
evidence: 用于机器人操作的VLA模型与扩散策略
tldr: 本文针对视觉-语言-动作模型仅依赖成功行为克隆导致的脆弱性问题，提出自适应失败学习框架。该框架利用预训练VLA在线生成失败轨迹作为负向指导，联合训练成功和失败行为生成器，在采样时自适应引导动作生成远离失败区域。实验证明该方法能有效提升任务成功率和模型鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08434v2/fig-001.webp\", \"caption\": \"Figure 3: Visualization of in-domain (left) and out-of-domain (right) manipulation task setup. Row 1: exterior camera image, row 2: wrist camera image. See supplementary for more visualizations.\", \"page\": 8, \"index\": 1, \"width\": 738, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2605.08434v2/fig-002.webp\", \"caption\": \"Figure 2: Dual Action Generator (DAG)-VLA with Adaptive Failure-Informed Learning (AFIL).\", \"page\": 5, \"index\": 2, \"width\": 824, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2605.08434v2/fig-003.webp\", \"caption\": \"Table 4: Performance comparison on out-of-domain manipulation tasks. Success rates (%) reported over 25 configs × 3 runs for each task.\", \"page\": 9, \"index\": 3, \"width\": 764, \"height\": 191}, {\"url\": \"assets/figures/arxiv/2605.08434v2/fig-004.webp\", \"caption\": \"Figure 1: Overview of the proposed Adaptive Failure-Informed Learning (AFIL) pipeline. AFIL uses online-generated failure trajectories as adaptive negative guidance, steering action generation away from failure-prone regions and toward more reliable success modes.\", \"page\": 2, \"index\": 4, \"width\": 782, \"height\": 293}]"
motivation: 现有视觉-语言-动作模型仅依赖成功行为克隆，导致执行错误累积和不可恢复的失败。
method: 提出自适应失败学习框架，利用失败轨迹作为负向指导，联合训练成功和失败行为生成器。
result: 实验表明AFIL在多种机器人操作任务中一致优于基线，提高了任务成功率和鲁棒性。
conclusion: AFIL通过自适应失败引导，显著提升了VLA模型在机器人操作任务中的成功率和鲁棒性。
---

## 摘要
视觉-语言-动作（VLA）模型为可扩展的机器人操作提供了一个有前景的范式，但其仅依赖成功行为克隆的特性使其脆弱；由于缺乏纠正性训练信号，微小的执行错误会迅速累积成不可恢复的分布外故障。为解决这一局限，我们提出了自适应故障感知学习（AFIL），这是一个端到端框架，利用故障轨迹作为基于扩散和流的VLA策略的自适应负向指导。AFIL使用预训练的VLA在线生成故障推演，避免了手工设计故障模式或人工介入恢复的需求。然后，它联合训练用于成功和失败行为的双重动作生成器（DAGs），同时共享一个共同的视觉-语言骨干网络，从而以有限的参数开销实现高效的故障感知策略学习。在采样过程中，故障生成器自适应地将动作生成引导远离易故障区域，转向更可靠的成功模式，其指导强度由每个扩散步骤中成功与失败分布之间的距离决定。在领域内和领域外机器人操作任务（涵盖短期和长期设置）上的实验表明，AFIL在任务成功率和鲁棒性方面持续优于现有VLA基线，证明了其有效性、效率和通用性。

## Abstract
Vision-language-action (VLA) models provide a promising paradigm for scalable robotic manipulation, yet their reliance on success-only behavioral cloning leaves them brittle; lacking corrective training signals, minor execution errors rapidly compound into unrecoverable, out-of-distribution failures. To address this limitation, we propose Adaptive Failure-Informed Learning (AFIL), an end-to-end framework that leverages failure trajectories as adaptive negative guidance for diffusion- and flow-based VLA policies. AFIL uses a pretrained VLA to generate failure rollouts online, avoiding the need for handcrafted failure-mode design or human-in-the-loop recovery. It then jointly trains Dual Action Generators (DAGs) for successful and failed behaviors while sharing a common vision-language backbone, enabling efficient failure-aware policy learning with limited parameter overhead. During sampling, the failure generator adaptively steers action generation away from failure-prone regions and toward more reliable success modes, with guidance strength determined by the per-diffusion-step distance between success and failure distributions. Experiments across in-domain and out-of-domain robotic manipulation tasks, covering both short- and long-horizon settings, show that AFIL consistently improves task success rates and robustness over existing VLA baselines, demonstrating its effectiveness, efficiency, and generality.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，为您生成一份结构化的详细总结。

### **论文核心问题与整体含义**
*   **研究背景**：视觉-语言-动作（VLA）模型是机器人操作领域的前沿范式，但其训练通常仅依赖于“成功”的行为克隆数据。这导致模型在面对执行误差或分布外状态时异常脆弱，缺乏从错误中恢复的能力。
*   **核心问题**：仅从成功数据中学习的VLA策略，其动作分布缺乏对“失败”区域的认知，因此在部署时，微小的偏差容易累积并导致任务彻底失败。
*   **整体含义**：本文提出，**失败数据中蕴含了宝贵的纠正性信号**。通过系统性地利用失败轨迹来指导策略学习，可以显著提升VLA模型的鲁棒性和泛化能力，使其能够“从失败中学习”（Failing Forward）。

### **论文提出的方法论**
*   **核心思想**：将失败轨迹视为一种**自适应的负向提示（Negative Guidance）**，在基于扩散/流匹配的VLA模型采样过程中，动态地引导动作生成远离失败区域，靠近成功区域。
*   **关键技术细节**：
    1.  **双动作生成器（DAG）架构**：
        *   在共享的视觉-语言骨干网络（VLM Backbone）上，并行训练两个轻量级的动作生成头：**成功生成器（G_succ）** 和 **失败生成器（G_fail）**。
        *   `G_succ` 使用成功轨迹训练，`G_fail` 使用失败轨迹训练。失败数据通过预训练VLA策略的**在线推演**自动收集，无需人工设计故障模式。
    2.  **自适应故障感知采样**：
        *   在推理时，同时查询`G_succ`和`G_fail`，获得对应噪声预测（扩散模型）或速度场（流匹配模型）。
        *   受图像生成中“负向提示”技术启发，设计引导公式：`ϵ*_FI = ϵ_succ - λ_η · (ϵ_fail - ϵ_succ)`。其中`ϵ_succ`和`ϵ_fail`分别是成功和失败生成器的预测。
        *   **关键创新**：引导强度 `λ_η` 不是固定值，而是**自适应调整**的。它基于当前采样步骤中成功与失败预测的**余弦相似度**计算：`λ_η ∝ (1 - cos(ϵ_succ, ϵ_fail))`。
        *   **自适应逻辑**：当成功与失败预测相似（如任务初期，动作模式重叠），降低引导强度，避免过度干扰；当两者差异显著（如出现明显的失败模式，如掉落），增强引导强度，强力排斥失败动作。

### **实验设计**
*   **数据集/场景**：
    1.  **ManiSkill**：用于领域内和领域外评估。包含短期任务（堆叠立方体、竖起木栓）和长期组合任务（放置碗勺、放置罐头和香蕉）。领域外任务涉及未见过的物体几何、纹理和杂乱场景。
    2.  **LIBERO基准**：用于公平比较。包含四个任务套件：空间泛化、物体泛化、目标泛化和长期任务（LIBERO-10）。
*   **Benchmark**：主要评估指标为**任务成功率**。在ManiSkill中，每个任务在多个初始配置下运行多次取平均；在LIBERO中，遵循标准协议（3个随机种子，共500次推演）。
*   **对比方法**：
    *   **基线模型**：`π0.5`（一个强大的流匹配VLA模型）、OpenVLA、Octo、CoT-VLA、Diffusion Policy等。
    *   **消融实验**：
        *   `Success Only`：仅用成功数据训练的基础VLA。
        *   `Success + Failure Correction`：用成功数据+由失败修正后的轨迹微调。
        *   `DAG-VLA + Static-FIL`：使用双生成器，但采用固定强度的失败引导。
        *   `DAG-VLA + Adaptive-FIL (AFIL)`：本文提出的完整方法。

### **资源与算力**
*   论文**未明确说明**训练和实验所使用的具体硬件配置（如GPU型号、数量）和训练时长。实验是基于`π0.5`架构进行的，并提到AFIL是模型无关的，可集成到其他扩散/流匹配VLA中。

### **实验数量与充分性**
*   **实验数量**：实验设计较为全面，包括：
    1.  **主实验**：在ManiSkill的5个领域内任务和3个领域外任务上评估。
    2.  **基准测试**：在LIBERO的4个任务套件上与多个SOTA模型对比。
    3.  **消融研究**：系统对比了不同训练数据配置（成功、成功+修正、成功+失败）和不同引导策略（静态 vs 自适应）的效果。
    4.  **参数分析**：对自适应引导强度系数 `α` 进行了消融实验（测试了0.5, 1.0, 2.0, 5.0）。
*   **充分性与客观性**：
    *   **充分性**：实验覆盖了短期/长期、领域内/领域外、仿真基准等多个维度，能够较全面地验证方法的有效性和泛化能力。
    *   **客观性**：使用了公开可复现的基准（LIBERO）和标准评估协议。对比了当前有代表性的VLA模型。消融实验设计清晰，能有效说明各组件贡献。
    *   **公平性**：主要对比实验均基于相同的骨干模型（`π0.5`）或相同的训练数据，确保了比较的公平性。

### **论文的主要结论与发现**
1.  **有效性**：AFIL在所有测试任务上均一致且显著地超越了基线方法，特别是在**长期任务**和**领域外任务**上提升幅度最大（例如，在ManiSkill的长期任务上比静态引导提升7-12个百分点）。
2.  **自适应引导的优势**：自适应引导（Adaptive-FIL）始终优于固定强度引导（Static-FIL），证明了根据成功/失败模式相似度动态调整引导强度的必要性。
3.  **数据效率**：即使不依赖人工设计的失败模式或大量额外数据，仅通过在线收集的失败轨迹进行学习，也能显著提升模型性能。
4.  **参数高效**：双生成器架构仅增加了少量参数（额外的动作生成头），共享了庞大的VLM骨干，实现了高效的失败感知学习。

### **优点**
1.  **创新性**：首次将图像生成中的“负向提示”思想系统性地引入到序列决策的VLA模型中，并设计了自适应机制，构思巧妙。
2.  **实用性**：**在线生成失败数据**的方案避免了繁琐、有偏的手工故障模式设计，更贴近真实部署场景。
3.  **高效性**：双头架构参数开销小，易于集成到现有扩散/流匹配VLA框架中。
4.  **泛化性**：方法被证明在多种任务类型（短/长时程）和泛化场景（领域外）中均有效。

### **不足与局限**
1.  **对预训练VLA的依赖**：失败数据的生成依赖于一个预训练的（尽管可能不完美的）VLA策略。如果该预训练策略的失败模式过于单一或与真实分布偏差大，可能影响AFIL的效果。
2.  **仿真环境局限**：所有实验均在仿真环境（ManiSkill, LIBERO）中进行。在更复杂、噪声更大的真实机器人场景中的有效性有待验证。
3.  **计算开销**：虽然参数增加不多，但推理时需要同时运行两个生成器并进行额外的相似度计算，相比原始VLA会增加一定的推理时间。
4.  **失败定义的普适性**：论文中的“失败”主要指未达成任务目标。对于更复杂的任务，可能存在部分成功、安全违规等其他形式的“失败”，当前方法未加区分。
5.  **理论深度**：自适应引导强度公式（基于余弦相似度）虽然直观有效，但更多是启发式设计，缺乏严格的理论推导或与其他度量方式的深入对比分析。

（完）
