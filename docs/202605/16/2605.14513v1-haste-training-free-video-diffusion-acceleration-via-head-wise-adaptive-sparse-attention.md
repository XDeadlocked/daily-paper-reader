---
title: "HASTE: Training-Free Video Diffusion Acceleration via Head-Wise Adaptive Sparse Attention"
title_zh: HASTE：通过头部自适应稀疏注意力实现无需训练的视频扩散加速
authors: "Xuzhe Zheng, Yuexiao Ma, Jing Xu, Xiawu Zheng, Rongrong Ji, Fei Chao"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14513v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 通过自适应稀疏注意力实现无训练的视频扩散加速
tldr: 本文针对基于扩散的视频生成模型因全注意力二次复杂度导致部署受限的问题，提出了一种训练免费的视频扩散加速方法HASTE。该方法通过头级自适应稀疏注意力框架，引入时序掩码重用和误差引导预算校准两个插件组件，有效减少了掩码预测开销并处理了头级异质性。实验表明，该方法在Wan2.1系列模型上实现了显著的推理加速，同时保持了视频生成质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14513v1/fig-001.webp\", \"caption\": \"Figure 3. Head-wise threshold-induced sparsity and error response curves on Wan2.1-1.3B with XAttention [35], measured at a randomly selected layer and denoising timestep. Each polyline contains seven operating points corresponding to top-p thresholds {1.00, 0.95, 0.90, 0.85, 0.80, 0.70, 0.65}. From left to right, the panels show attention-output MSE, model-output MSE on denoising velocity, and head sparsity, all as functions of the threshold. Different heads follow clearly different threshold-induced sparsity and error response trajectories and therefore prefer different operating points.\", \"page\": 6, \"index\": 1, \"width\": 971, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2605.14513v1/fig-002.webp\", \"caption\": \"Figure 6. Realized sparsity over denoising timesteps for several randomly selected heads under a fixed sparsification threshold. Even for the same head, the achieved sparsity varies substantially across timesteps, while different heads exhibit different trajectories. This empirical observation supports interval-based timestep sampling in calibration, rather than measuring each head at only one denoising step.\", \"page\": 11, \"index\": 2, \"width\": 867, \"height\": 528}, {\"url\": \"assets/figures/arxiv/2605.14513v1/fig-003.webp\", \"caption\": \"Table 5. Ablation of the TMR threshold δ on Wan2.1-1.3B at 480P.\", \"page\": 15, \"index\": 3, \"width\": 986, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2605.14513v1/fig-004.webp\", \"caption\": \"Figure 4. Overview of the proposed head-wise sparse-attention framework. Left: Temporal Mask Reuse (TMR) reduces online mask-prediction overhead by deciding, for each head, whether to reuse the previous sparse mask or refresh it using a lightweight query-key stability signal. Right: Error-guided Budgeted Calibration (EBC) operates offline, measuring candidate operating points for each head and selecting head-specific thresholds under a fixed global sparsity budget. The two components are complementary: TMR reduces online overhead, while EBC improves the global speed–quality trade-off without retraining or modifying the sparse-attention kernel.\", \"page\": 7, \"index\": 4, \"width\": 970, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2605.14513v1/fig-005.webp\", \"caption\": \"Table 3. Quantitative comparison at higher resolution on Wan2.1-1.3B at 720P.\", \"page\": 14, \"index\": 5, \"width\": 622, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2605.14513v1/fig-006.webp\", \"caption\": \"Table 2. Main quantitative comparison among the original dense models, representative top-p sparse attention methods, and the corresponding top-p methods enhanced with our method.\", \"page\": 14, \"index\": 6, \"width\": 889, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2605.14513v1/fig-007.webp\", \"caption\": \"Figure 5. Relationship between adjacent-step mask IoU and query-key drift. Both the full-token drift and the mean-pooled drift show clear negative correlations with mask IoU, indicating that the cheaper mean-pooled statistic preserves the predictive trend of the full-token drift while requiring much less cache memory. Each point corresponds to one sampled head-step pair from either conditional or unconditional branch.\", \"page\": 9, \"index\": 7, \"width\": 975, \"height\": 301}, {\"url\": \"assets/figures/arxiv/2605.14513v1/fig-008.webp\", \"caption\": \"Figure 2. Temporal mask similarity is heterogeneous across prompts, layers, and heads. (a) Prompt-level curves for randomly sampled prompts still vary substantially after averaging over layers. (b) A layer-step mask-IoU heatmap for one randomly sampled prompt shows that the same heterogeneity also appears across layers. (c) Per-head mask-IoU heatmap within a randomly selected layer shows that this variation persists even inside one layer. Together, these observations motivate adaptive head-wise mask reuse instead of fixed shared reuse schedules.\", \"page\": 5, \"index\": 8, \"width\": 954, \"height\": 920}]"
motivation: 现有训练免费稀疏注意力方法因掩码预测开销和共享阈值限制，在视频扩散模型中实际速度-质量权衡受限。
method: 提出了一种头级自适应框架，包含基于查询-键漂移跳过不必要掩码预测的时序掩码重用和通过最小化模型输出误差分配每头阈值预算的误差引导预算校准两个插件组件。
result: 在Wan2.1-1.3B和Wan2.1-14B模型上，该方法一致优于现有方法，在720P分辨率下实现高达1.93倍加速，同时保持竞争力的视频质量和相似性指标。
conclusion: 该方法在保持视频质量的同时，显著提升了推理速度，为视频扩散模型的实际部署提供了有效的训练免费加速方案。
---

## 摘要
基于扩散的视频生成在视觉保真度和时间连贯性方面取得了显著进展，但实际部署仍受限于全注意力机制的二次复杂度。无需训练的稀疏注意力方法因其无需重新训练即可加速预训练模型而备受关注，然而现有的在线top-p稀疏注意力在掩码预测上仍消耗不可忽略的成本，且尽管存在显著的头部异质性，仍采用共享阈值。我们发现这两个被忽视的因素限制了Video DiTs中无需训练稀疏注意力的实际速度-质量权衡。为解决这些问题，我们引入了一个头部自适应的框架，包含两个即插即用组件：基于查询-键漂移跳过不必要掩码预测的“时序掩码复用”，以及通过最小化全局稀疏预算下测量的模型输出误差来分配各头部top-p阈值的“误差引导的预算校准”。在Wan2.1-1.3B和Wan2.1-14B模型上，我们的方法持续改进了XAttention和SVG2，在720P分辨率下实现了高达1.93倍的加速，同时保持了有竞争力的视频质量和相似性指标。

## Abstract
Diffusion-based video generation has advanced substantially in visual fidelity and temporal coherence, but practical deployment remains limited by the quadratic complexity of full attention. Training-free sparse attention is attractive because it accelerates pretrained models without retraining, yet existing online top-$p$ sparse attention still spends non-negligible cost on mask prediction and applies shared thresholds despite strong head-level heterogeneity. We show that these two overlooked factors limit the practical speed-quality trade-off of training-free sparse attention in Video DiTs. To address them, we introduce a head-wise adaptive framework with two plug-in components: Temporal Mask Reuse, which skips unnecessary mask prediction based on query-key drift, and Error-guided Budgeted Calibration, which assigns per-head top-$p$ thresholds by minimizing measured model-output error under a global sparsity budget. On Wan2.1-1.3B and Wan2.1-14B, our method consistently improves XAttention and SVG2, achieving up to 1.93 times speedup at 720P while maintaining competitive video quality and similarity metrics.

---

## 论文详细总结（自动生成）

### 论文总结：HASTE: Training-Free Video Diffusion Acceleration via Head-Wise Adaptive Sparse Attention

#### 1. 核心问题与整体含义
*   **研究背景**：基于扩散变换器（Video DiT）的视频生成模型在质量和连贯性上进步显著，但其核心的全注意力机制具有二次复杂度，成为高分辨率、长视频生成的主要计算瓶颈，限制了实际部署。
*   **研究动机**：无需训练（Training-Free）的稀疏注意力方法可直接加速预训练模型，极具实用价值。然而，现有主流方法（如在线Top-p稀疏注意力）存在两大被忽视的效率限制：
    1.  **掩码预测开销**：每次去噪步都需动态预测稀疏模式，产生额外成本。
    2.  **头部异质性未被充分利用**：对所有注意力头使用统一的稀疏化阈值（Top-p），忽略了不同头在时序稳定性和对阈值敏感性上的巨大差异，导致在固定计算预算下无法达到最优的速度-质量权衡。

#### 2. 提出的方法论
论文提出了一个即插即用的**头级自适应稀疏注意力框架（HASTE）**，包含两个互补组件：
*   **核心思想**：针对上述两个瓶颈，分别设计在线和离线的自适应机制，在细粒度的“注意力头”级别进行优化，而非采用全局或层级的固定策略。
*   **关键技术细节**：
    *   **时序掩码复用（TMR - Temporal Mask Reuse）**：
        *   **目标**：减少在线掩码预测开销。
        *   **方法**：为每个头维护一个“锚点”状态（最近一次刷新掩码时的查询/键特征和掩码）。在当前步，计算当前查询/键特征与锚点特征的**平均池化后L1距离（查询-键漂移）**作为稳定性代理指标。
        *   **决策**：若漂移小于预设阈值δ，则复用锚点的稀疏掩码；否则，为该头刷新掩码并更新锚点。此决策在**每个头独立进行**，自适应其演化速度。
    *   **误差引导的预算校准（EBC - Error-guided Budgeted Calibration）**：
        *   **目标**：在全局稀疏预算下，为每个头分配合适的Top-p阈值，以最小化模型输出误差。
        *   **方法**：
            1.  **离线测量**：使用一个提示池，对每个头，在多个去噪步上采样，测量一组候选阈值对应的**实现稀疏度**和**模型输出误差**。误差采用**加权四带3D-FFT目标**（基于去噪速度），该目标能反映不同时空频率区域误差对最终视频质量的不同影响。
            2.  **优化分配**：将阈值选择建模为一个**整数线性规划（ILP）问题**：在满足全局平均稀疏度约束的前提下，为每个头选择一个候选阈值，使得所有头的总测量误差最小。

#### 3. 实验设计
*   **模型与场景**：在文本到视频生成任务上评估，使用**Wan2.1-1.3B**和**Wan2.1-14B**两个预训练Video DiT模型。主要分辨率是**480P**，并补充了**720P**实验以考察缩放性。
*   **评估指标**：
    *   **质量**：使用综合评估套件 **VBench**（包含主体一致性、运动平滑度等多个维度）。
    *   **相似性**：与稠密注意力输出比较**PSNR、SSIM、LPIPS**。
    *   **效率**：端到端生成**延迟**和**加速比**。
*   **对比方法**：
    *   **基线**：原始的稠密注意力（Dense Attention）。
    *   **训练免费稀疏注意力SOTA**：**XAttention** 和 **SVG2**（两者均使用在线Top-p稀疏化）。
    *   **对比方式**：将HASTE（TMR+EBC）作为插件，分别应用于XAttention和SVG2之上，与它们的原始版本进行对比。

#### 4. 资源与算力
*   论文中仅明确提及实验在 **NVIDIA A800 PCIe GPU** 上进行。
*   未详细说明具体使用的GPU数量、离线校准所需的计算时间或总实验耗时。

#### 5. 实验数量与充分性
*   **主实验**：在两个模型（1.3B, 14B）、两个基线方法（XAttention, SVG2）上进行了全面对比，涵盖质量、相似性、效率指标。
*   **缩放性实验**：在1.3B模型上进行了720P高分辨率实验，验证方法在计算压力更大时的有效性。
*   **消融实验**：
    1.  **核心组件消融**：验证TMR和EBC各自的贡献及互补性。
    2.  **TMR阈值δ消融**：研究重用阈值的敏感性和影响。
    3.  **校准目标消融**：比较加权3D-FFT目标与原始速度MSE目标的效果。
*   **充分性与公平性**：
    *   实验设计较为系统，覆盖了不同模型规模、分辨率、以及内部模块分析。
    *   对比公平，均在相同的推理配置（步数、求解器等）下进行，且HASTE作为插件与基线使用相同的稀疏注意力内核和预训练权重。
    *   评估指标兼顾感知质量（VBench）、像素/结构相似性（PSNR/SSIM/LPIPS）和实际速度，较为全面。

#### 6. 主要结论与发现
*   **有效性**：HASTE能一致地提升现有Top-p稀疏注意力方法（XAttention, SVG2）的性能。在相同或更低的延迟下，获得更高的VBench分数和更好的相似性指标。
*   **效率增益**：在480P下获得显著加速（如XAttention从1.30倍提升至1.49倍），在720P下加速比更高（最高达**1.93倍**），表明方法在高计算负载下更有效。
*   **组件作用**：TMR主要贡献于降低延迟（减少掩码预测开销），EBC主要贡献于提升质量（优化阈值分配），两者互补。
*   **关键洞察**：验证了注意力头在掩码时序演化和阈值响应上存在显著异质性，证明了头级自适应策略的必要性和优越性。

#### 7. 优点
*   **即插即用**：无需重新训练模型，可直接应用于现有预训练Video DiT和稀疏注意力管道，实用性强。
*   **细粒度自适应**：创新地在“注意力头”级别进行掩码复用和阈值校准，充分利用了模型内部的异质性。
*   **设计轻量高效**：TMR使用平均池化特征作为轻量代理指标；EBC的离线校准基于单头扰动测量，可并行化，开销可控。
*   **目标函数先进**：校准中使用频率感知的加权3D-FFT误差，比简单的MSE更能贴合人类视觉感知。

#### 8. 不足与局限
*   **校准目标的近似性**：EBC将全局误差近似为各头误差之和（加性代理），忽略了头与头、层与层之间的交互效应。论文在附录中讨论了此近似，但其实际影响未完全量化。
*   **管道依赖性**：TMR的稳定性阈值δ和EBC的频带权重可能需要针对不同的基础稀疏注意力方法（如XAttention vs. SVG2）或模型进行微调，普适性参数有待探索。
*   **实验覆盖范围**：主要实验基于Wan2.1系列模型，未在更多样化的视频扩散架构（如Latte、Sora等）上进行验证。
*   **偏差风险**：离线校准依赖于采样的提示池和去噪步，若采样不能代表真实数据分布，可能产生过拟合或偏差。
*   **应用限制**：方法专注于加速推理阶段的注意力计算，未与其他加速技术（如量化、蒸馏）结合探索，也未处理扩散模型固有的多步采样开销问题。

（完）
