---
title: "Delta Forcing: Trust Region Steering for Interactive Autoregressive Video Generation"
title_zh: Delta Forcing：用于交互式自回归视频生成的信任区域引导
authors: "Yuheng Wu, Xiangbo Gao, Tianhao Chen, Xinghao Chen, Qing Yin, Zhengzhong Tu, Dongman Lee"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14382v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 自回归视频生成与世界模型
tldr: 本文针对交互式自回归视频生成中条件偏差导致的持续漂移问题，提出 Delta Forcing 框架。该方法受信任区域策略优化启发，通过估计教师与生成器轨迹间的潜在差异来构建自适应信任区域，约束不可靠的教师监督，从而平衡反应性与稳定性。主要贡献是显著提高了生成一致性，同时保持事件反应能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14382v1/fig-001.webp\", \"caption\": \"Figure 5: Latent trajectory visualization via PCA under multi-event prompt switching. We project frame-wise denoised latent features (before VAE decoding) into a two-dimensional PCA space and connect them in temporal order. Different colors denote different interaction segments. Left exhibits short and narrow transitions across prompt switches, indicating insufficient semantic displacement despite changed conditions. Middle: shows irregular and scattered patterns with no clear temporal structure, indicating severe inter-event drift. Right evolves with persistent motion regardless of interaction boundaries, suggesting mode-seeking dynamics in which within-event and cross-event transitions become indistinguishable. Comparing against the desirable properties defined in Section A.1, none of these methods achieves both within-interaction aggregation and smooth cross-interaction displacement.\", \"page\": 15, \"index\": 1, \"width\": 824, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2605.14382v1/fig-002.webp\", \"caption\": \"Figure 1: Left: Under evolving events, the frozen teacher, biased toward certain patterns, remains condition-aware but trajectory-agnostic, inducing conditional bias that deviates from the historical trajectory. Right: Decoding both the real teacher model (i.e., Wan2.1-14B-T2V [1]) and generator (MemFlow [16]) shows that the generator’s drift closely follows these teacher-induced shifts.\", \"page\": 2, \"index\": 2, \"width\": 746, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2605.14382v1/fig-003.webp\", \"caption\": \"Table 1: Comparisons with Long-CLIP Score [47] and VideoAlign [46] metrics across different video generation baselines.\", \"page\": 7, \"index\": 3, \"width\": 806, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2605.14382v1/fig-004.webp\", \"caption\": \"Figure 2: (a) Standard DMD fails to handle condition changes. (b) Streaming Long Tuning improves interactivity but still suffers from biased guidance, and (c) our method enforces transition consistency to mitigate conditional bias and preserve temporal coherence.\", \"page\": 3, \"index\": 4, \"width\": 414, \"height\": 419}, {\"url\": \"assets/figures/arxiv/2605.14382v1/fig-005.webp\", \"caption\": \"Figure 7: User study interface.\", \"page\": 18, \"index\": 5, \"width\": 829, \"height\": 781}, {\"url\": \"assets/figures/arxiv/2605.14382v1/fig-006.webp\", \"caption\": \"Figure 3: Qualitative results. Each 10s segment corresponds to one event and the full event prompts are listed in Appendix B.\", \"page\": 9, \"index\": 6, \"width\": 807, \"height\": 727}, {\"url\": \"assets/figures/arxiv/2605.14382v1/fig-007.webp\", \"caption\": \"Figure 6: Extended latent trajectory comparison. Each row shows one example under the same multi-event prompt schedule, comparing three baselines (columns 1–3) against Delta Forcing (column 4). Red arrows highlight segments where Delta Forcing exhibits compact within-interaction clusters connected by smooth cross-interaction transitions, consistent with the desirable properties established in Section A.1.\", \"page\": 16, \"index\": 7, \"width\": 820, \"height\": 333}]"
motivation: 现有方法在交互式自回归视频生成中存在条件偏差，导致生成结果在条件变化后出现持续漂移，需要平衡反应性与稳定性。
method: 提出 Delta Forcing 框架，通过估计教师与生成器轨迹间的潜在差异来构建自适应信任区域，以平衡教师监督与连续性目标。
result: 实验表明 Delta Forcing 显著提高了生成的一致性，同时保持了对新事件的反应能力。
conclusion: Delta Forcing 通过信任区域约束教师监督，有效解决了条件偏差问题，实现了视频生成中反应性与一致性的平衡。
---

## 摘要
交互式实时自回归视频生成对于内容创作和世界建模等应用至关重要，这些应用要求视觉内容能够适应动态变化的事件条件。一个核心挑战在于平衡反应性与稳定性：模型必须对新事件做出快速响应，同时在长时间跨度内保持时间连贯性。现有方法将双向模型蒸馏为自回归生成器，并通过流式长时调优进一步适配，但往往在条件变化后表现出持续的漂移。我们发现其根源在于条件偏差，即教师模型可能提供与条件对齐但与轨迹无关的指导，使生成偏向局部有效但全局不一致的模式。受信任区域策略优化的启发，我们提出Delta Forcing，一个简单而有效的框架，将不可靠的教师监督约束在自适应信任区域内。具体而言，Delta Forcing通过估计教师轨迹与生成器轨迹之间的潜在差异来评估转移一致性，并利用该信息平衡教师监督与单调连续性目标。这抑制了不可靠的教师引导偏移，同时保持了对新事件的响应能力。大量实验表明，Delta Forcing在保持事件反应性的同时显著提升了一致性。

## Abstract
Interactive real-time autoregressive video generation is essential for applications such as content creation and world modeling, where visual content must adapt to dynamically evolving event conditions. A fundamental challenge lies in balancing reactivity and stability: models must respond promptly to new events while maintaining temporal coherence over long horizons. Existing approaches distill bidirectional models into autoregressive generators and further adapt them via streaming long tuning, yet often exhibit persistent drift after condition changes. We identify the cause as conditional bias, where the teacher may provide condition-aligned but trajectory-agnostic guidance, biasing generation toward locally valid yet globally inconsistent modes. Inspired by Trust Region Policy Optimization, we propose Delta Forcing, a simple yet effective framework that constrains unreliable teacher supervision within an adaptive trust region. Specifically, Delta Forcing estimates transition consistency from the latent delta between teacher and generator trajectories, and uses it to balance teacher supervision with a monotonic continuity objective. This suppress unreliable teacher-induced shifts while preserving responsiveness to new events. Extensive experiments demonstrate that Delta Forcing significantly improves consistency while maintaining event reactivity.

---

## 论文详细总结（自动生成）

### 论文总结：Delta Forcing: Trust Region Steering for Interactive Autoregressive Video Generation

#### 1. 核心问题与整体含义
*   **研究背景**：交互式实时自回归视频生成（如根据动态变化的提示词连续生成视频）是内容创作和世界建模的关键能力。其核心挑战在于平衡**反应性**（快速响应新事件/提示）与**稳定性**（保持长期时间一致性）。
*   **核心问题**：现有主流的两阶段方法（先蒸馏、再流式长时调优）在条件变化后会出现**持续的内容漂移**（如场景布局、物体外观突变）。论文指出根本原因并非自回归误差累积，而是**条件偏差**：作为教师的冻结双向模型在遇到新条件时，会提供与条件对齐但**无视已生成历史轨迹**的指导，从而将学生模型拉向局部合理但全局不一致的生成模式。

#### 2. 提出的方法论：Delta Forcing
*   **核心思想**：受**信任区域策略优化**启发，不盲目信任教师的全部指导，而是将其约束在一个**自适应的信任区域**内。该区域根据教师指导与生成器自身历史轨迹的一致性动态调整。
*   **关键技术细节**：
    1.  **可靠性感知的监督调制**：提出混合目标函数 `L = wk * L_DMD + (1 - wk) * L_cont`。其中 `L_DMD` 是标准的分布匹配蒸馏损失，`L_cont` 是保证生成轨迹自身平滑的连续性损失，`wk` 是关键的自适应权重。
    2.  **潜在差异估计**：使用冻结的DINO特征提取器，分别计算教师模型和生成器在连续视频块间的特征变化量（`δ_real` 和 `δ_fake`）。两者的差异 `ρk = ||δ_real - δ_fake||` 衡量了教师指导的“不可靠”程度。
    3.  **自适应信任区域门控**：权重 `wk` 由 `ρk` 通过一个Sigmoid函数计算：`wk = σ(-(ρk - μ) * s)`。当 `ρk` 小（教师与生成器演变一致），`wk` 接近1，信任教师指导；当 `ρk` 大（教师指导出现跳跃性偏差），`wk` 接近0，依赖生成器自身的连续性损失 `L_cont` 来维持轨迹稳定。
    4.  **连续性损失**：`L_cont` 直接约束生成器连续输出块在特征空间中的变化平滑，该信号完全基于生成器自身历史，不受教师偏差影响。
*   **算法流程**（基于Algorithm 1）：
    1.  在流式生成过程中，按事件边界生成新的视频块。
    2.  对当前块计算标准DMD损失，并获得教师去噪后的潜在估计。
    3.  提取当前块与前一块的DINO特征，分别计算生成器和教师的特征变化量 `δ_fake` 和 `δ_real`。
    4.  计算差异 `ρk`，并据此得到自适应权重 `wk`。
    5.  计算连续性损失 `L_cont`。
    6.  将加权后的总损失反向传播，更新生成器参数。

#### 3. 实验设计
*   **基准数据集/场景**：在 **MemFlow benchmark** 上进行评估。该基准包含100个序列，每个序列由6个连续的10秒事件（共60秒视频）组成，要求模型在提示词切换时保持叙事连贯性。
*   **评估指标**：
    *   **VBench**：综合评估生成质量，包括主体一致性、背景一致性、运动平滑度等。
    *   **Long-CLIP Score**：评估长文本提示与生成视频的对齐程度。
    *   **VideoAlign**：基于人类反馈训练的奖励模型，评估视觉质量、运动质量和文本对齐。
    *   **用户研究**：让参与者从美学质量、动态质量和多事件自然度三个维度对视频进行排序。
*   **对比方法**：
    *   **开源因果模型**：SkyReels-V2, MAGI-1。
    *   **蒸馏因果模型**：LongLive, MemFlow, Reward Forcing。为确保公平，所有蒸馏基线均采用Causal Forcing初始化。

#### 4. 资源与算力
*   论文中提到所有实验在 **Nvidia H100 GPU** 上进行。
*   未明确说明具体使用的GPU数量、总训练时长等详细信息。训练分为两个阶段：第一阶段（Causal Forcing初始化）训练700步，第二阶段（Delta Forcing + 流式长时调优）训练3000步。

#### 5. 实验数量与充分性
*   **实验组别**：
    1.  **主实验**：在MemFlow基准上与5个强基线进行定量对比（使用上述所有指标）。
    2.  **消融实验**：验证两个核心设计（连续性损失 `L_cont` 和自适应信任区域权重 `wk`）的必要性。
    3.  **用户研究**：20名参与者，进行主观偏好评估。
    4.  **定性分析**：展示多事件生成的视觉结果对比。
    5.  **动机研究**：通过潜在轨迹PCA可视化，直观展示基线方法的漂移模式和Delta Forcing的改进。
*   **充分性与客观性**：实验设计**较为充分和全面**。涵盖了定量指标、人类主观评价、定性可视化和消融分析。评估指标多样，且包含了基于人类反馈的VideoAlign和直接的用户研究，增强了结论的客观性和说服力。对比实验在相同初始化条件下进行，保证了公平性。

#### 6. 主要结论与发现
*   Delta Forcing 能够**显著提升长时、多事件视频生成的时间一致性**（在主体/背景一致性等指标上领先），同时**不损失对新事件的反应能力**（Long-CLIP Score保持领先）。
*   验证了**条件偏差**是导致漂移的根本原因，且**学生模型的漂移直接继承自教师模型的有偏指导**。
*   **自适应信任区域机制是有效的**：能自动抑制不可靠的教师指导，在平稳期利用教师信号，在条件突变期依赖生成器自身动量。
*   在用户研究中，Delta Forcing 在**多事件自然度**（即过渡平滑性）上获得明显偏好。

#### 7. 优点
*   **问题诊断精准**：深入分析了现有流程失败的根本原因（条件偏差），而非归咎于表面现象。
*   **方法框架优雅**：受TRPO启发，将“信任区域”概念创造性地应用于生成模型蒸馏，核心思想清晰且有效。
*   **评估全面客观**：结合了自动化指标、基于人类反馈的奖励模型和真实的用户研究，结论可信度高。
*   **分析工具直观**：利用潜在空间轨迹的PCA可视化来诊断和展示模型行为，增强了论文的可解释性。

#### 8. 不足与局限
*   **训练规模局限**：论文自身指出，实验所用的模型容量、数据多样性和交互复杂性有限。需要更大规模的训练来验证方法的普适性和鲁棒性。
*   **社会影响风险**：论文在附录中讨论了该技术可能被滥用于制作深度伪造或误导性内容的潜在风险，强调了部署时需添加水印、内容审核等安全措施的重要性。
*   **应用场景限制**：方法主要针对提示词驱动的视频生成，对于其他形式的交互控制（如草图、动作）的泛化能力未经验证。
*   **计算成本**：虽然推理阶段无额外开销，但训练时需要同时计算教师和生成器的特征差异，并引入额外的特征提取器前向传播，增加了训练成本。

（完）
