---
title: A Systematic Post-Train Framework for Video Generation
title_zh: 视频生成的系统性后训练框架
authors: "Zeyue Xue, Siming Fu, Jie Huang, Shuai Lu, Haoran Li, Yijun Liu, Yuming Li, Xiaoxuan He, Mengzhao Chen, Haoyang Huang, Nan Duan, Ping Luo"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25427v1"
tags: ["query:sr-0501"]
score: 10.0
evidence: 视频扩散模型的后训练框架
tldr: 针对大规模视频扩散模型在实际部署中存在的提示敏感性、时间不一致和推理成本高等问题，本文提出了一个系统的后训练框架。该框架通过监督微调、基于人类反馈的强化学习、提示增强和推理优化四个协同阶段，系统地对齐预训练模型与用户意图，以提升视觉质量、时间连贯性和指令跟随能力，同时保持预训练中学到的可控性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25427v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of our post-training framework for video generation. We organize the pipeline into four complementary stages to bridge pretrained models and practical deployment. In Phase 1, supervised fine-tuning (SFT) uses curated data to establish a stable instruction-following baseline. In Phase 2, RLHF via a GRPO-based trainer aligns the generator with multi-dimensional reward signals, improving aesthetics, motion quality, and text alignment. In Phase 3, Prompt Enhancement (PE) optimizes an LLM using the same reward loop to enrich user inputs for better robustness and visual quality. Finally, Phase 4 applies autoregressive distillation (AD) with a self-forcing objective to transfer these capabilities into a causal architecture, significantly boosting inference efficiency for real-world deployment.\", \"page\": 2, \"index\": 1, \"width\": 808, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.25427v1/fig-002.webp\", \"caption\": \"Figure 2: The visualization of RLHF on Wan-2.1.\", \"page\": 8, \"index\": 2, \"width\": 758, \"height\": 1167}]"
motivation: 大规模视频扩散模型在提示敏感性、时间不一致性和高昂推理成本等方面存在性能差距，难以满足实际部署需求。
method: 提出一个包含监督微调、基于人类反馈的强化学习、提示增强和推理优化的四阶段后训练框架。
result: 实验表明该统一流程有效减少了常见伪影，显著提升了可控性和视觉美观度，同时遵守了严格的采样成本限制。
conclusion: 该框架为构建稳定、适应性强且高效的实际部署后训练流程提供了实用蓝图。
---

## 摘要
尽管大规模视频扩散模型在生成高分辨率和语义丰富的内容方面展现出令人印象深刻的能力，但由于提示词敏感性、时间不一致性以及高昂推理成本等关键问题，其预训练性能与实际部署需求之间仍存在显著差距。为弥合这一差距，我们提出一个全面的后训练框架，通过四个协同阶段系统性地将预训练模型与用户意图对齐：首先采用监督微调（SFT）将基础模型转化为稳定的指令遵循策略；随后通过专为视频扩散定制的新型组相对策略优化（GRPO）方法进行基于人类反馈的强化学习（RLHF），以提升感知质量和时间连贯性；接着，我们通过专用语言模型进行提示增强以优化用户输入；最后通过推理优化解决系统效率问题。这些组件共同构成了一套系统性方法，可在保持预训练所学可控性的同时，提升视觉质量、时间连贯性和指令遵循能力。该框架为构建稳定、适应性强且在实际部署中有效的可扩展后训练流程提供了实用蓝图。大量实验表明，这一统一流程能有效减少常见伪影，在严格遵守采样成本约束的同时，显著提升可控性和视觉美感。

## Abstract
While large-scale video diffusion models have demonstrated impressive capabilities in generating high-resolution and semantically rich content, a significant gap remains between their pretraining performance and real-world deployment requirements due to critical issues such as prompt sensitivity, temporal inconsistency, and prohibitive inference costs. To bridge this gap, we propose a comprehensive post-training framework that systematically aligns pretrained models with user intentions through four synergistic stages: we first employ Supervised Fine-Tuning (SFT) to transform the base model into a stable instruction-following policy, followed by a Reinforcement Learning from Human Feedback (RLHF) stage that utilizes a novel Group Relative Policy Optimization (GRPO) method tailored for video diffusion to enhance perceptual quality and temporal coherence; subsequently, we integrate Prompt Enhancement via a specialized language model to refine user inputs, and finally address system efficiency through Inference Optimization. Together, these components provide a systematic approach to improving visual quality, temporal coherence, and instruction following, while preserving the controllability learned during pretraining. The result is a practical blueprint for building scalable post-training pipelines that are stable, adaptable, and effective in real-world deployment. Extensive experiments demonstrate that this unified pipeline effectively mitigates common artifacts and significantly improves controllability and visual aesthetics while adhering to strict sampling cost constraints.

---

## 论文详细总结（自动生成）

好的，这是对论文《A Systematic Post-Train Framework for Video Generation》的详细、结构化总结。

### 1. 核心问题与整体含义
*   **研究背景**：大规模视频扩散模型（如Sora、Pika等）在预训练后，虽然能生成高分辨率、语义丰富的视频，但其**原始性能与真实世界部署需求之间存在显著差距**。
*   **核心问题**：这种差距主要体现在三个方面：
    1.  **提示词敏感性**：模型输出对输入提示的措辞非常敏感，细微改动可能导致质量大幅波动。
    2.  **时间不一致性**：生成的视频在长序列中容易出现运动抖动、物体形变等时序上的不连贯问题。
    3.  **高昂的推理成本**：基于扩散模型的迭代去噪过程计算开销巨大，难以满足实时或高吞吐量的应用场景。
*   **整体含义**：论文提出，**后训练**是弥合这一差距的关键。与追求通用能力的“预训练”不同，“后训练”专注于在严格的采样成本和系统效率约束下，对模型进行**对齐、优化和压缩**，使其更符合人类偏好、更可控、更高效。本文旨在为视频生成模型提供一个**系统化、可部署的后训练流程蓝图**。

### 2. 方法论
*   **核心思想**：设计一个包含四个协同阶段的端到端后训练框架，从不同维度系统性地提升模型。
*   **关键技术细节**：
    1.  **监督微调**：使用高质量指令-视频对数据，将基础模型微调成一个**稳定、可靠的指令跟随策略**。其目的不是直接优化主观质量，而是**消除严重错误**，为后续强化学习提供一个稳定的“参考策略”。
    2.  **基于人类反馈的强化学习**：
        *   **算法**：采用专为视频扩散模型定制的 **Group Relative Policy Optimization** 方法。
        *   **流程**：将流匹配模型的采样过程建模为马尔可夫决策过程。采用**混合ODE-SDE采样策略**：为每个提示组分配一个特定的时间步进行SDE采样以引入探索，其他时间步使用确定性ODE采样以保证生成质量。通过组内样本的**相对比较**来计算优势函数，避免依赖不稳定的价值函数估计。
        *   **奖励模型**：集成了四个奖励模型来提供多维度的优化信号：**视频美学**、**图像美学**、**运动质量**、**文本-视频对齐**。关键挑战在于平衡这些不同尺度、不同倾向的奖励信号。
    3.  **提示增强**：
        *   训练一个大型语言模型作为**提示词优化器**。其目标是在保持用户原始意图的前提下，自动将简短、模糊的用户提示重写为更详细、更具表现力的描述，从而提升生成视频的视觉质量。
        *   使用与阶段2相同的**GRPO框架和奖励信号**来训练这个提示增强器，实现了**生成器策略优化**与**输入提示优化**的协同。
    4.  **自回归蒸馏**：
        *   **目标**：将经过上述阶段优化的、计算昂贵的双向扩散模型，**压缩**成一个高效的、**因果架构**的自回归模型，以实现**实时或流式生成**。
        *   **三阶段训练流程**：
            *   **分布匹配蒸馏**：将原始模型蒸馏成一个只需少量去噪步的双向学生模型。
            *   **因果ODE回归**：引入块因果注意力掩码，训练模型仅基于历史帧进行去噪预测，完成架构迁移。
            *   **自强制蒸馏**：在训练时进行自回归展开，使模型在自身先前生成的帧上进行条件生成，并用DMD损失在视频级别进行监督，直接优化整个生成序列的质量。

### 3. 实验设计
*   **模型与数据**：
    *   使用一个**内部视频生成模型**作为基础模型。
    *   构建了一个用于SFT的**高质量文本-视频数据集**。
    *   为RLHF、DMD等阶段策划了特定的**提示词集**。
*   **评估协议**：
    *   采用 **Good–Same–Bad** 三选项比较协议。该协议允许评估者在差异细微时选择“相同”，减少了在边际情况下的强制或噪声决策，更适合视频评估。
    *   **评估维度**：视觉质量、运动质量、文本对齐。
    *   **评估者**：由人类艺术家进行整体比较。
*   **对比方法**：
    *   论文**未与外部其他SOTA方法进行直接数值对比**。
    *   实验主要展示了**框架内部各阶段（RLHF， RLHF+PE）相对于SFT基线**的性能提升，属于**消融性质**的验证。

### 4. 资源与算力
*   论文**未明确说明**训练所使用的具体硬件配置（如GPU型号、数量）、总训练时长或算力消耗。
*   仅提及使用了“内部视频生成模型”和遵循HPSv3范式训练奖励模型（以Qwen3.5为骨干）。可以推断实验需要**大规模的计算资源**，但具体细节未公开。

### 5. 实验数量与充分性
*   **实验数量**：论文展示的实验数量**相对有限**。主要报告了RLHF阶段和结合提示增强后的整体GSB指标提升（31%和额外的20%），并提供了三个提示词的**可视化案例**（如图2所示）。
*   **充分性与客观性**：
    *   **不足**：
        1.  **缺乏消融研究**：没有系统性地验证四个阶段中每个阶段的独立贡献和必要性。
        2.  **缺乏基准对比**：未在公开基准（如VBench, EvalCrafter）上与当前主流视频生成模型进行对比，无法客观定位其性能水平。
        3.  **评估主观性强**：主要依赖人工进行GSB评估，虽然更贴近人类感知，但样本量、评估者间一致性等细节未披露，可复现性和客观性存疑。
        4.  **内部数据与模型**：使用内部数据和模型进行实验，限制了结果的**可验证性和泛化性**。
    *   **结论**：实验设计**侧重于证明框架流程的有效性和内部一致性**，但**在全面、客观、公平的基准测试方面有所欠缺**，不足以充分证明其相对于领域内其他方法的绝对优势。

### 6. 主要结论与发现
1.  **系统性后训练有效**：提出的四阶段框架能有效提升视频生成模型在视觉质量、运动连贯性和指令跟随方面的表现。
2.  **RLHF效果显著**：基于GRPO的RLHF阶段带来了**31%** 的整体GSB指标提升，尤其在**视觉质量和运动质量**上改善巨大。
3.  **提示增强是有效补充**：提示增强器能带来**额外的20%** GSB提升，通过优化输入侧进一步改善了输出质量。
4.  **文本对齐是瓶颈**：实验发现文本对齐维度的提升相对有限。作者指出，这主要受限于当前**文本-视频对齐奖励模型的准确性**，成为了优化语义保真度的瓶颈。
5.  **提供可行蓝图**：该工作为将大规模预训练视频模型转化为**可实际部署**的系统提供了一个具体、可操作的技术蓝图。

### 7. 优点
1.  **系统性设计**：首次为视频生成提出了一个涵盖**指令对齐、偏好优化、输入增强、效率提升**的完整后训练框架，思路清晰完整。
2.  **方法创新**：将GRPO成功适配并创新应用于视频扩散模型的RLHF，提出了**混合ODE-SDE采样、等时分组、时间梯度校正**等技术，稳定了训练。
3.  **协同优化**：创新性地使用**同一套奖励模型**同时优化视频生成器和提示增强器，实现了输入与输出端的协同改进。
4.  **面向部署**：不仅关注质量提升，还通过**自回归蒸馏**专门解决推理效率这一实际部署中的关键痛点。

### 8. 不足与局限
1.  **实验评估不全面**：如前所述，缺乏充分的消融实验、公平的公开基准对比以及更大规模的客观指标评估。
2.  **依赖内部资源**：整个研究基于未公开的内部模型和数据集，限制了可复现性，且其结论在开源社区或不同架构模型上的普适性有待验证。
3.  **奖励模型局限**：框架性能受限于其集成的奖励模型（尤其是文本对齐模型）的质量。奖励模型本身的偏见和误差会被放大到生成模型中。
4.  **计算成本未知但高昂**：四阶段训练，尤其是涉及大量视频采样的RLHF和蒸馏阶段，必然需要极高的计算成本，但论文未量化，可能影响其实际应用价值评估。
5.  **通用性待验证**：框架虽然系统，但其每个组件（如特定的GRPO变体、蒸馏流程）在不同规模、不同架构的视频生成模型上的适应性和效果仍需进一步探索。

（完）
