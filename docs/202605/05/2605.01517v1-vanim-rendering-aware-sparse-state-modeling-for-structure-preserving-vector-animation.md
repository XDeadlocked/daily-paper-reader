---
title: "VAnim: Rendering-Aware Sparse State Modeling for Structure-Preserving Vector Animation"
title_zh: VAnim：面向结构保持矢量动画的渲染感知稀疏状态建模
authors: "Guotao Liang, Zhangcheng Wang, Chuang Wang, Juncheng Hu, Haitao Zhou, Junhua Liu, Jing Zhang, Dong Xu, Qian Yu"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01517v1"
tags: ["query:sr-0501"]
score: 6.5
evidence: 基于大语言模型的开放域文本到SVG动画生成框架
tldr: VAnim是首个基于大语言模型的开放域文本到SVG动画生成框架。针对现有方法破坏拓扑或仅支持刚性变换的问题，它将动画重新定义为SVG DOM树上的稀疏状态更新，压缩序列长度并保持结构。通过识别优先的运动规划将文本指令锚定于视觉实体，并利用基于组相对策略优化的渲染感知强化学习，结合视频感知编码器的混合奖励，使离散代码更新与高保真视觉反馈对齐。贡献包括提出SVGAnim-134k基准，实验证明其在语义对齐和结构有效性上显著优于先进基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01517v1/fig-001.webp\", \"caption\": \"Figure 5. Qualitative Comparison on SVGAnim-Test. We compare VAnim against state-of-the-art baselines on four challenging prompts requiring precise control and non-rigid deformation. Baselines suffer from severe Identity Drift and Topological Collapse. Specifically, GPT-5.2 changes the toucan’s wing color to red; Gemini distorts the bottle shape; while LiveSketch produces broken lines. Our VAnim generates smooth, semantically aligned animations while strictly preserving the visual identity and topology of the original vector graphics, thanks to its ID-anchored sparse update mechanism.\", \"page\": 7, \"index\": 1, \"width\": 1013, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2605.01517v1/fig-002.webp\", \"caption\": \"Figure 2. Construction Pipeline of SVGAnim-134k. The process operates in three stages: (1) Structural Standardization: Raw Lottie files are rendered into ID-anchored SVG DOM trees and normalized to a unified coordinate space. (2) Sparse Encoding: The animation sequence is compressed into Sparse State Updates, represented by serialized differential tokens (visualized in purple). (3) Dual-Stream Annotation: An MLLM generates both User Prompts (P ) and Structure-Bound CoT (C). Samples failing the ID-consistency check are automatically discarded to ensure high-fidelity grounding.\", \"page\": 3, \"index\": 2, \"width\": 981, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2605.01517v1/fig-003.webp\", \"caption\": \"Figure 6. Qualitative Ablation. Row 1 (w/o Structure-Bound CoT) suffers from grounding failures, manipulating wrong entities (e.g., rotating the whole cabinet instead of the door). Row 2 (w/o RL) exhibits motion laziness, producing conservative updates that fail to fully satisfy the “close” or “open” instructions. Row 3 (Full) achieves accurate semantic alignment with precise deformation.\", \"page\": 8, \"index\": 3, \"width\": 489, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2605.01517v1/fig-004.webp\", \"caption\": \"Figure 4. Overview of the VAnim Framework. The generation process is decomposed into two stages: (1) Identification-First Motion Planning, where the Multimodal LLM explicitly grounds visual entities to persistent SVG IDs (e.g., binding the object inside the orange box to id=06 through reasoning) and reasons about causal motion logic; (2) Sparse State Update, where the model predicts attribute differentials (Dt) only for the targeted nodes on the SVG DOM tree, guaranteeing topological isomorphism. Crucially, to bridge the non-differentiable sampling and rendering gap, we employ Rendering-Aware Reinforcement Learning. Using Group Relative Policy Optimization (GRPO), we align the discrete code updates with continuous visual dynamics via hybrid rewards from the PE-Core perception encoder.\", \"page\": 5, \"index\": 4, \"width\": 1015, \"height\": 208}]"
motivation: 现有SVG动画生成方法难以兼顾结构保持与非刚性形变，导致拓扑不一致或仅支持刚性变换，无法满足开放域文本到动画的需求。
method: 该方法将动画重新定义为SVG DOM树上的稀疏状态更新，并采用识别优先的运动规划和基于组相对策略优化的渲染感知强化学习来确保精确控制和视觉对齐。
result: 实验表明，VAnim在语义对齐和结构有效性上显著优于现有基线，其提出的SVGAnim-134k基准进一步验证了运动质量和身份保持。
conclusion: VAnim 通过创新的稀疏状态更新建模和渲染感知强化学习，为开放域文本到SVG动画生成提供了一个高效且结构保持的解决方案，显著提升了语义对齐和结构有效性。
---

## 摘要
可缩放矢量图形（SVG）动画生成因其结构可编辑性和分辨率无关性，对专业设计至关重要。然而，该任务仍具挑战性，因为它需要弥合离散代码表示与连续视觉动态之间的鸿沟。现有的基于优化的方法常破坏拓扑一致性，而通用大语言模型（LLM）依赖僵化的CSS/SMIL变换，无法建模几何层面的非刚性形变。为应对这些局限，我们提出了VAnim，首个基于LLM的开放域文本到SVG动画生成框架。我们重新将动画概念化为对持久SVG DOM树的稀疏状态更新（SSU），而非序列生成。这一范式在保持SVG DOM结构和非参与元素的同时，将序列长度压缩超过9.8倍。为实现精确控制，我们提出了一种“识别优先运动规划”机制，将文本指令锚定于显式视觉实体。此外，为克服SVG渲染的不可微特性，我们通过组相对策略优化（GRPO）采用渲染感知强化学习。通过利用来自先进视频感知编码器的混合奖励，我们将离散代码更新与高保真视觉反馈对齐。我们还引入了SVGAnim-134k，首个矢量动画基准测试。大量实验表明，VAnim在语义对齐和结构有效性方面显著优于现有先进基线，附加附录指标进一步验证了运动质量和身份保持性。

## Abstract
Scalable Vector Graphics (SVG) animation generation is pivotal for professional design due to their structural editability and resolution independence. However, this task remains challenging as it requires bridging discrete code representations with continuous visual dynamics. Existing optimization-based methods often destroy topological consistency, while general-purpose LLMs rely on rigid CSS/SMIL transformations, failing to model geometry-level non-rigid deformations. To address these limitations, we present VAnim, the first LLM-based framework for open-domain text-to-SVG animation. We reconceptualize animation not as sequence generation, but as Sparse State Updates (SSU) on a persistent SVG DOM tree. This paradigm compresses sequence length by over 9.8x while preserving the SVG DOM structure and non-participating elements by construction. To enable precise control, we propose an Identification-First Motion Planning mechanism that grounds textual instructions in explicit visual entities. Furthermore, to overcome the non-differentiable nature of SVG rendering, we employ Rendering-Aware Reinforcement Learning via Group Relative Policy Optimization (GRPO). By leveraging a hybrid reward from a state-of-the-art video perception encoder, we align discrete code updates with high-fidelity visual feedback. We also introduce SVGAnim-134k, the first benchmark for vector animation. Extensive experiments demonstrate that VAnim significantly outperforms state-of-the-art baselines in semantic alignment and structural validity, with additional appendix metrics further validating motion quality and identity preservation.