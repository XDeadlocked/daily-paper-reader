---
title: "LiteVLA-H: Dual-Rate Vision-Language-Action Inference for Onboard Aerial Guidance and Semantic Perception"
title_zh: LiteVLA-H：面向机载空中引导与语义感知的双速率视觉-语言-行动推理
authors: "Justn williams, Kishor Datta Gupta, Roy George, Mrinmoy Sarkar"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2605.00884v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 用于机载无人机引导的VLA系统
tldr: 本文针对无人机机载部署中视觉-语言-动作模型面临的计算和延迟挑战，提出了LiteVLA-H，一个256M参数的紧凑模型。它采用双速率操作模式，在NVIDIA Jetson AGX Orin嵌入式平台上，通过调度器实现快速动作输出（19.74Hz）和较慢语义输出（6.08-6.67Hz）的并行处理。核心贡献在于利用多模态预填充主导延迟的特性，通过混合数据微调保持模型描述能力，最终在保持语义感知的同时达到了更高的边缘推理速率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00884v1/fig-001.webp\", \"caption\": \"Figure 1. LiteVLA-H system diagram. One shared multimodal backbone is queried at two timescales: a fast action mode for outer-loop guidance and a slower semantic mode for scene awareness, logging, and operator support. The conventional flight controller remains responsible for inner-loop stabilization.\", \"page\": 3, \"index\": 1, \"width\": 954, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2605.00884v1/fig-002.webp\", \"caption\": \"Table 7. Positioning ofLiteVLA-H relative to representative prior work.\", \"page\": 7, \"index\": 2, \"width\": 1017, \"height\": 202}]"
motivation: 现有视觉-语言-动作模型在无人机等严格机载计算和通信约束下难以实现低延迟闭环引导，需要紧凑高效的部署方案。
method: 采用知识保留的微调方法，混合反应式飞行数据、空中语义数据和通用字幕/VQA监督，并设计调度器实现双速率操作。
result: 在NVIDIA Jetson AGX Orin上，动作分支达到19.74Hz（50.65ms）的推理速率，语义分支保持6.08-6.67Hz（149.90-164.57ms），优于对比模型。
conclusion: LiteVLA-H 在嵌入式平台上实现了双速率推理，在保持语义理解能力的同时，显著提升了动作输出的实时性，为无人机机载部署提供了可行的VLA解决方案。
---

## 摘要
视觉-语言-行动（VLA）模型在操控任务中展现出强大的语义基础和任务泛化能力，但空中部署仍然困难，因为无人机需要在严格的机载计算和通信限制下实现低延迟闭环引导。我们提出了LiteVLA-H，一个紧凑的256M参数VLA系统，专为NVIDIA Jetson AGX Orin平台上的双速率操作而设计：一种用于短动作令牌输出的快速外环引导模式，以及一种用于场景理解、危险描述和面向操作员叙述的较慢语义模式。核心实证观察是，在这种紧凑的边缘计算环境中，端到端延迟主要由多模态预填充主导，而非解码少量额外令牌的边际成本。这促使我们设计了一个调度器，能以50.65毫秒（19.74赫兹）的速率发出反应性动作令牌，同时在同一嵌入式平台上支持149.90至164.57毫秒（6.08至6.67赫兹）的句子级语义输出。为了在不削弱其描述能力的情况下使模型专业化，我们采用了一种保留知识的微调方法，混合了反应性飞行数据、空中语义数据以及通用字幕/视觉问答监督。除了报告当前的延迟测量结果，我们还将该系统与最新的先进架构（包括AnywhereVLA、FutureVLA和ReMem-VLA）进行对比，表明在我们的部署条件下，所测量的行动分支达到了更高的边缘推理速率，同时保持了周期性的语义感知能力。

## Abstract
Vision-language-action (VLA) models have shown strong semantic grounding and task generalization in manipulation, but aerial deployment remains difficult because drones require low-latency closed-loop guidance under strict onboard compute and communication constraints. We present LiteVLA-H, a compact 256M-parameter VLA system designed for dual-rate operation on an NVIDIA Jetson AGX Orin: a fast outer-loop guidance mode for short action-token outputs and a slower semantic mode for scene understanding, hazard description, and operator-facing narration. The central empirical observation is that, in this compact edge regime, end-to-end latency is dominated by multimodal pre-fill rather than by the marginal cost of decoding a few extra tokens. This motivates a scheduler that issues reactive action tokens at 50.65,ms (19.74,Hz) while still supporting sentence-level semantic outputs at 149.90--164.57\ms (6.08--6.67,Hz) on the same embedded platform. To specialize the model without collapsing its descriptive competence, we use a knowledge-preserving fine-tuning recipe that mixes reactive flight data, aerial semantic data, and generic caption/VQA supervision. Beyond reporting current latency measurements, we position the system against recent state-of-the-art architectures, including AnywhereVLA, FutureVLA, and ReMem-VLA, showing that the measured action branch reaches a higher edge inference rate under our deployment conditions while retaining periodic semantic awareness.

---

## 论文详细总结（自动生成）

好的，以下是对论文《LiteVLA-H: Dual-Rate Vision-Language-Action Inference for Onboard Aerial Guidance and Semantic Perception》的详细总结。

### 1. 核心问题与整体含义
*   **研究动机**：视觉-语言-行动（VLA）模型在机器人操控领域展现出强大的语义理解和任务泛化能力。然而，将其部署到无人机上极具挑战性，因为无人机需要在严格的**机载计算、功耗和通信限制**下，实现**低延迟的闭环实时引导**。同时，无人机任务也需要**高级语义感知**（如障碍物描述、场景理解）来辅助决策和与操作员交互。现有工作大多关注导航成功率或服务器级推理，未能有效解决**同一紧凑模型如何同时支持高频率的实时控制与低频率的语义理解**这一核心系统调度问题。
*   **整体含义**：本文提出了一个面向无人机机载部署的**紧凑、双速率VLA系统**。其核心思想是，在资源受限的边缘设备上，VLA推理的延迟主要由处理视觉和语言输入的“预填充”阶段主导，而非生成输出令牌的阶段。基于这一洞察，论文设计了一个调度器，让同一个模型以两种速率运行：**高速率（~20 Hz）输出简短动作指令**用于实时控制，**低速率（~6-7 Hz）输出完整句子**用于语义感知，从而在单一嵌入式平台上兼顾了反应速度与场景理解能力。

### 2. 方法论
*   **核心思想**：利用**预填充主导延迟**的特性，将**时间关键**的动作生成与**非时间关键**的语义生成解耦，通过**双速率调度**实现资源高效利用。
*   **关键技术细节**：
    1.  **系统架构**：基于一个共享的256M参数多模态主干网络。系统接收RGB图像、文本上下文和模式指令，输出为动作令牌序列或语义句子。
    2.  **延迟分解与调度**：总延迟 `L(n) = P + ΣD_i`，其中 `P` 是多模态预填充成本，`D_i` 是解码第 `i` 个令牌的成本。在紧凑边缘场景下，`P` 远大于 `D_i`（对于短输出），即系统是**预填充主导**的。因此，优化重点应是减少预填充时间，而非仅仅缩短输出长度。调度器以周期 `Δa`（快）执行动作查询，以周期 `Δs = KΔa`（慢，K>1）执行语义查询，并采用**截止时间优先**策略，确保动作查询立即响应，语义查询则作为非阻塞的后台服务。
    3.  **知识保留微调**：为防止模型在针对空中任务微调时丧失通用的视觉-语言能力（灾难性遗忘），采用混合损失函数：`L = λ_a L_act + λ_s L_sem + λ_g L_gen + λ_kp L_kp`。其中包含动作损失、空中语义损失、通用字幕/VQA损失，以及一个可选的**知识保留正则化项**（如使用KL散度约束微调后模型与原始模型的输出分布）。
*   **公式/算法流程**：
    *   **策略函数**：`y_t = f_θ(I_t, x_t, m_t)`，其中 `m_t ∈ {act, sem}` 决定输出模式。
    *   **调度规则**：语义查询周期是动作查询周期的整数倍（`Δs = KΔa`），并可基于事件（如检测到危险）触发。
    *   **预填充占比**：`ρ = P / T_act`，用于量化预填充成本在动作延迟中的主导程度。

### 3. 实验设计
*   **部署平台与配置**：所有核心推理性能实验均在 **NVIDIA Jetson AGX Orin** 嵌入式平台上进行，使用 **FP16精度**，上下文窗口长度为2048。
*   **评估场景与Benchmark**：
    *   **主要评估**：在Jetson AGX Orin上测量**时间到首个动作**和**端到端语义延迟**，并计算对应的推理频率（Hz）。
    *   **对比方法**：与多个近期先进的VLA框架进行**系统级定位**比较，包括：
        *   **AnywhereVLA**（紧凑，侧重移动操作与探索）
        *   **FutureVLA**（引入视觉-运动预测）
        *   **ReMem-VLA**（引入双级循环记忆查询）
        *   **OpenVLA-OFT**（大规模通用VLA）
        *   作者团队之前的 **LiteVLA-Edge** 基线
    *   **任务评估**：在**模拟跑道导航**和**真实硬件障碍赛道**上评估闭环性能，指标包括任务成功率、干预率、路径偏差和危险召回率。
*   **注意**：论文强调，由于硬件、任务、动作空间的不同，对比是“系统级定位”而非完全受控的排行榜，旨在突出LiteVLA-H在**特定部署条件（紧凑、机载、双速率）** 下的优势。

### 4. 资源与算力
*   论文**未明确说明**模型预训练和微调所使用的具体GPU型号、数量及训练时长。
*   文中仅提到了微调使用的**数据量**（反应动作样本12万，空中语义样本4.5万，通用字幕/VQA样本8.5万）和**训练步数/轮数**（5万步/10轮），以及**批量大小**（256）和**学习率**（3e-4）等超参数。

### 5. 实验数量与充分性
*   **实验组别**：
    1.  **核心延迟测量**：在目标硬件上测量了动作模式和多种语义模式（单句、多句）的延迟。
    2.  **延迟分解分析**：通过性能剖析工具量化了预填充、解码等各环节耗时，验证了“预填充主导”的假设（`ρ ≈ 94.4%`）。
    3.  **消融实验**：
        *   **数据混合与知识保留**：对比了仅动作微调、动作+空中语义、动作+空中语义+通用数据、以及完整方法（含正则化）在动作成功率、保留的通用字幕能力（CIDEr）、空中语义理解（F1）上的表现。
        *   **运行时与调度策略**：对比了单速率（仅动作、仅语义）与双速率调度在延迟、内存、功耗上的差异，并与增加了记忆或预测模块的变体进行了比较。
    4.  **闭环性能评估**：在模拟和真实硬件环境中，与经典基线及其他VLA方法进行了任务性能对比。
*   **充分性与客观性评估**：
    *   **充分性**：实验覆盖了从**底层推理性能**、**调度策略有效性**、**训练方法影响**到**高层任务表现**的多个层面，设计较为全面。消融实验清晰地展示了各组件（数据混合、调度）的贡献。
    *   **客观性**：论文明确指出与SOTA方法的对比并非完全受控，并**谨慎地限定了其结论的适用范围**（即在所述紧凑、机载、双速率部署场景下的优势）。对自身系统的**局限性**也有明确阐述（见下文），体现了客观性。
    *   **公平性**：比较时考虑了模型规模（参数量）、部署目标（机载 vs. 服务器）等差异，旨在进行“角色定位”而非直接排名，方法是公平的。

### 6. 主要结论与发现
1.  **关键发现**：在紧凑型边缘部署的VLA中，**端到端推理延迟主要由多模态预填充阶段主导**（约占首个动作延迟的94.4%），而非令牌解码。
2.  **系统性能**：LiteVLA-H在Jetson AGX Orin上实现了**双速率推理**：动作分支达 **19.74 Hz (50.65 ms)**，语义分支达 **6.08-6.67 Hz (149.90-164.57 ms)**。其动作速率高于对比的同类边缘VLA方法。
3.  **方法有效性**：**混合数据微调配合知识保留正则化**能在保持高动作成功率的同时，有效缓解灾难性遗忘，保留模型的通用描述和语义理解能力。
4.  **设计启示**：对于需要快速反应的空中机器人，VLA系统设计应**分别报告和优化“时间到首个动作”与“句子级响应延迟”**，并采用**双速率调度**来兼顾实时性与语义感知。

### 7. 优点
1.  **问题洞察深刻**：抓住了边缘VLA部署中“预填充主导延迟”这一关键系统特性，并以此为核心设计调度策略。
2.  **实用系统设计**：提出的双速率调度方案非常务实，明确了VLA作为**外环引导器**的定位，与负责内环稳定的传统飞控解耦，降低了安全风险。
3.  **训练策略有效**：混合数据微调与知识保留方法平衡了**专业化**与**通用能力保留**，避免了模型退化为简单的动作分类器。
4.  **评估全面**：不仅报告延迟，还通过消融实验分析了数据、调度的影响，并进行了初步的闭环任务评估。

### 8. 不足与局限
1.  **实验控制不足**：与SOTA方法的对比受限于硬件、任务、动作表示的差异，并非严格的“苹果对苹果”比较，结论更适用于系统定位而非绝对性能宣称。
2.  **闭环评估深度有限**：论文承认，更广泛的闭环飞行评估（覆盖不同风力、光照、运动模糊、载荷条件）是未来需要完成的工作，以证明任务级鲁棒性。
3.  **语义评估不全面**：语义保留能力通过CIDEr、F1等代理指标评估，仍需通过**人工评估危险描述、失败案例分析、分布外场景测试**等进行更严格的验证。
4.  **应用范围限制**：当前系统输出的是高层引导指令（如速度、航向），**仍需依赖底层的经典飞控、安全监控和认证软件**，并未解决端到端自主的所有安全问题。
5.  **算力信息缺失**：未提供完整的训练算力消耗信息，不利于复现或计算效率评估。

（完）
