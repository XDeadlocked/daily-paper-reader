---
title: "Talker-T2AV: Joint Talking Audio-Video Generation with Autoregressive Diffusion Modeling"
title_zh: Talker-T2AV：基于自回归扩散建模的联合说话音视频生成
authors: "Zhen Ye, Xu Tan, Aoxiong Yin, Hongzhan Lin, Guangyan Zhang, Peiwen Sun, Yiming Li, Chi-Min Chan, Wei Ye, Shikun Zhang, Wei Xue"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23586v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 用于音视频联合生成的自回归扩散模型
tldr: 现有音视频联合生成模型在说话头合成任务中，由于跨所有层级进行完全纠缠的跨模态建模，导致不必要的纠缠和效率低下。本文提出Talker-T2AV，一个自回归扩散框架，其中高层跨模态建模在共享主干中进行，而低层细化使用模态特定的解码器。一个共享的自回归语言模型在统一的补丁级令牌空间中联合推理音频和视频。两个轻量级扩散Transformer头将隐藏状态解码为帧级音频和视频潜在表示。在说话肖像基准测试上的实验表明，Talker-T2AV在唇同步准确性、视频质量和音频质量上优于双分支基线，实现了比级联管道更强的跨模态一致性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23586v1/fig-001.webp\", \"caption\": \"Table 1. Comparison with joint text-to-audio-video generation methods on both test sets. Methods above the dashed line target general audio-video generation, while UniAVGen and our method focus on facial talking-head video generation. Best results are in bold, second best are underlined.\", \"page\": 7, \"index\": 1, \"width\": 1017, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2604.23586v1/fig-002.webp\", \"caption\": \"Figure 1. Overview of TALKER-T2AV. Top: The autoregressive backbone processes a unified causal sequence in which the full text token sequence appears first as a prefix, followed by joint audio-video patches where audio and video patch embeddings are element-wise summed at each position. The backbone hidden states are then decoded by two modality-specific diffusion transformer heads into audio waveforms and portrait video, respectively. Bottom right: Each diffusion transformer head takes the hidden state hi, a global embedding, and a context window of history frames as conditioning, and denoises the current frame via flow matching. Bottom left: The patch transformer encoder compresses N audio/video frames into a single patch token, reducing sequence length for efficient autoregressive modeling.\", \"page\": 4, \"index\": 2, \"width\": 954, \"height\": 742}]"
motivation: 现有音视频联合生成模型在说话头合成中，由于跨模态的完全纠缠建模，导致不必要的纠缠和效率低下。
method: 提出Talker-T2AV，一个自回归扩散框架，使用共享主干进行高层跨模态建模，并采用轻量级扩散Transformer头进行模态特定的低层解码。
result: 在说话肖像基准测试中，Talker-T2AV在唇同步准确性、视频质量和音频质量上优于双分支基线，实现了更强的跨模态一致性。
conclusion: Talker-T2AV通过分层建模和共享语言模型，在音视频联合生成中实现了更优的跨模态一致性和生成质量。
---

## 摘要
联合音视频生成模型已证明，统一生成方法比级联方法能产生更强的跨模态一致性。然而，现有模型通过全局注意力机制在去噪过程中耦合各模态，以完全纠缠的方式处理高层语义与低层细节。这对于说话头部合成而言并非最优：虽然音频与面部运动在语义上相关，但它们的低层实现（声学信号与视觉纹理）遵循不同的渲染过程。强制在所有层级进行联合建模会导致不必要的纠缠并降低效率。我们提出Talker-T2AV，一种自回归扩散框架，其中高层跨模态建模在共享骨干网络中进行，而低层细化则使用特定模态的解码器。一个共享的自回归语言模型在统一的补丁级令牌空间中共同推理音频和视频。两个轻量级扩散变换器头将隐藏状态解码为帧级音频和视频潜在表示。在说话人像基准测试上的实验表明，Talker-T2AV在唇形同步准确性、视频质量和音频质量方面均优于双分支基线，实现了比级联流水线更强的跨模态一致性。

## Abstract
Joint audio-video generation models have shown that unified generation yields stronger cross-modal coherence than cascaded approaches. However, existing models couple modalities throughout denoising via pervasive attention, treating high-level semantics and low-level details in a fully entangled manner. This is suboptimal for talking head synthesis: while audio and facial motion are semantically correlated, their low-level realizations (acoustic signals and visual textures) follow distinct rendering processes. Enforcing joint modeling across all levels causes unnecessary entanglement and reduces efficiency. We propose Talker-T2AV, an autoregressive diffusion framework where high-level cross-modal modeling occurs in a shared backbone, while low-level refinement uses modality-specific decoders. A shared autoregressive language model jointly reasons over audio and video in a unified patch-level token space. Two lightweight diffusion transformer heads decode the hidden states into frame-level audio and video latents. Experiments on talking portrait benchmarks show Talker-T2AV outperforms dual-branch baselines in lip-sync accuracy, video quality, and audio quality, achieving stronger cross-modal consistency than cascaded pipelines.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的详细中文总结。

### 1. 核心问题与整体含义
*   **研究背景**：联合音视频生成（如Sora、Veo等）已成为生成式AI的前沿，其核心优势在于能产生跨模态高度一致的输出。当前开源研究的主流范式是**双分支扩散Transformer**，它通过在每个去噪步骤中 pervasive 的跨模态注意力，将音频和视频完全耦合。
*   **核心问题**：作者认为，这种“从高层语义到低层细节”的**完全纠缠式建模**对于“说话头部合成”任务并非最优。因为：
    1.  **效率低下**：音频（声学信号）和视频（视觉纹理）的低层渲染过程本质不同，强制联合建模会造成不必要的纠缠，降低建模效率。
    2.  **固定时长限制**：传统扩散模型需预设输出长度，当文本过长时，模型被迫压缩或截断内容，严重损害语音清晰度，且无法适应多语言、多说话人之间自然的语速变化。

### 2. 提出的方法论
*   **核心思想**：**解耦与分层**。将生成过程分解为两个阶段：
    1.  **高层跨模态建模**：在共享的自回归骨干网络中进行，负责规划“说什么”和“何时动”等语义与时序信息。
    2.  **低层模态特定渲染**：由两个独立的轻量级扩散Transformer头执行，分别负责生成高质量的音频和视频信号。
*   **关键技术细节**：
    *   **共享自回归骨干**：基于一个因果语言模型（如Qwen3-0.6B）。输入序列由**文本前缀**和后续的**联合音频-视频补丁令牌**组成。音频和视频的补丁嵌入通过**元素求和**融合为一个令牌，再输入骨干网络。骨干输出一个共享的隐藏状态 `hi`。
    *   **时序对齐的潜在表示**：使用 **LIA-X**（视频）和 **WhisperX-VAE**（音频）两个冻结的自编码器，将视频帧和音频波形分别编码为**相同帧率（25 Hz）的连续潜在向量**，实现了天然的帧级对齐，无需学习对齐模块。
    *   **补丁Transformer编码器**：将连续的P帧压缩为一个补丁令牌，大幅缩短自回归序列长度，提升效率。
    *   **停止预测器**：一个附加在骨干隐藏状态上的MLP，用于预测生成何时结束，从而实现**可变长度输出**。
    *   **模态特定扩散头**：两个独立的DiT，均以共享隐藏状态 `hi` 为条件，但参数不共享。它们采用**流匹配**目标进行训练，专门负责去噪并生成各自模态的潜在帧。
    *   **灵活的任务支持**：得益于“元素求和”设计，在推理时，若某一模态已知（如给定音频），可直接将其嵌入与另一模态的预测嵌入求和后输入骨干，使**单一模型**无需改动即可支持**文本生成音视频、音频驱动视频、视频配音**三大任务。
    *   **多任务训练**：混合使用音视频对数据和纯TTS数据训练，通过任务标签区分。这利用了大量TTS数据提升了模型（尤其是语音分支）的文本到发音映射能力。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：约100万条带字幕的说话人头音视频剪辑（T2AV任务）+ Emilia TTS数据集（TTS任务）。
    *   **测试集**：
        *   **中文**：从DH-FaceVid-1K采样200个视频。
        *   **英文**：从HDTF和Hallo3采样200个视频。
        *   **视频配音基准**：Chem数据集。
*   **评估指标**：
    *   **音频质量**：字符错误率（CER）/词错误率（WER）、UTMOS（自然度）。
    *   **视频质量**：FID（帧）、FVD（视频）。
    *   **音视频同步**：SyncNet置信度（C，越高越好）和距离（D，越低越好）。
    *   **配音质量**：时长距离（DD）、情感相似度（EMO-SIM）、WER、UTMOS。
*   **对比方法**：
    *   **联合音视频生成**：MoVA, Ovi, LTX-2, UniVerse-1（通用生成），以及专门面向说话人脸的UniAVGen。
    *   **音频驱动视频生成**：FLOAT, EchoMimic, Sonic, Ditto, AniPortrait。
    *   **视频配音**：Speak2Dub, StyleDubber, DeepDubber, ProDubber, InstructDub。

### 4. 资源与算力
*   论文中**未明确说明**训练所使用的具体GPU型号、数量及总训练时长。
*   仅提及的训练配置包括：全局批量大小256，使用AdamW优化器，采用bfloat16混合精度训练，共训练20万步。

### 5. 实验数量与充分性
*   **实验组数**：较为充分。主要包括：
    1.  **主任务对比**：在中文、英文两个测试集上，与5个联合生成基线进行综合对比。
    2.  **条件生成任务验证**：在音频驱动视频和视频配音两个任务上，分别与5个专用基线方法对比。
    3.  **消融研究**：系统对比了自回归序列中不同令牌排列策略（元素求和、交错、延迟）在T2AV和A2V任务下的性能影响。
*   **充分性与公平性**：
    *   **充分性**：实验覆盖了模型宣称的三大核心功能，并从多维度（质量、同步性）进行评估。消融实验深入探讨了关键设计选择。
    *   **客观与公平**：评估采用了广泛认可的客观指标（如WER, FID, SyncNet）和人类感知相关指标（UTMOS）。在音频驱动任务中，为公平比较，为所有模型提供相同的真实音频作为输入，以排除上游TTS质量差异的影响。

### 6. 主要结论与发现
*   **性能领先**：在联合音视频生成任务上，Talker-T2AV在**语音清晰度（CER/WER最低）、视频流畅度（FVD最佳）和唇音同步（SyncNet C最高/D最低）** 上全面优于所有双分支扩散Transformer基线。
*   **任务通用性**：在未经过任何架构修改或微调的情况下，该单一模型在**音频驱动视频生成**和**视频配音**任务上，达到了与甚至超越了专用模型的性能。
*   **设计验证**：消融实验证实了**元素求和**作为令牌融合策略的有效性，并揭示了最优的令牌排列方式**依赖于任务**（联合生成需对齐，条件生成可延迟）。

### 7. 优点
*   **架构设计创新**：提出的“**高层联合规划 + 低层分离渲染**”的分层解耦思想，清晰合理，直接针对现有方法的痛点。
*   **灵活高效**：“元素求和”融合与共享骨干设计，使**单一模型**能优雅支持三种生成模式，极大提升了模型实用性和灵活性。
*   **训练策略巧妙**：引入**多任务训练**，利用海量TTS数据显著提升语音分支的建模能力，进而通过共享骨干惠及视频生成的同步质量。
*   **实现可变长度**：通过自回归生成和停止预测器，**自然支持可变时长输出**，克服了固定时长扩散模型的根本缺陷。

### 8. 不足与局限
*   **误差累积**：自回归骨干在连续潜在空间中进行预测，**每一步的误差会随生成长度累积**，可能导致生成长序列时质量逐渐下降。
*   **视频质量上限**：生成的视频质量受限于所使用的**LIA-X运动自编码器的表达能力**。采用更强大的视觉表示有望进一步提升画质。
*   **数据规模**：虽然使用了百万级配对数据，但作者指出，**继续扩大高质量音视频对数据的规模**，预计能带来进一步的性能提升。
*   **计算成本未量化**：论文未报告训练和推理的具体计算成本，难以评估其效率优势的实际规模。

（完）
