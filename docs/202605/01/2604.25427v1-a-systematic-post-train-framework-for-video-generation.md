---
title: A Systematic Post-Train Framework for Video Generation
title_zh: 视频生成的系统性后训练框架
authors: "Zeyue Xue, Siming Fu, Jie Huang, Shuai Lu, Haoran Li, Yijun Liu, Yuming Li, Xiaoxuan He, Mengzhao Chen, Haoyang Huang, Nan Duan, Ping Luo"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25427v1"
tags: ["query:sr-0501"]
score: 10.0
evidence: 视频扩散模型的后训练框架
tldr: 本文针对预训练视频扩散模型存在的提示敏感性、时序不一致和推理成本高的问题，提出一个系统化的后训练框架。该框架通过监督微调、基于组相对策略优化的强化学习人类反馈、提示增强和推理优化四个阶段，显著提升了生成视频的视觉质量、时序连贯性和指令跟随能力，为实际部署提供了可行方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25427v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of our post-training framework for video generation. We organize the pipeline into four complementary stages to bridge pretrained models and practical deployment. In Phase 1, supervised fine-tuning (SFT) uses curated data to establish a stable instruction-following baseline. In Phase 2, RLHF via a GRPO-based trainer aligns the generator with multi-dimensional reward signals, improving aesthetics, motion quality, and text alignment. In Phase 3, Prompt Enhancement (PE) optimizes an LLM using the same reward loop to enrich user inputs for better robustness and visual quality. Finally, Phase 4 applies autoregressive distillation (AD) with a self-forcing objective to transfer these capabilities into a causal architecture, significantly boosting inference efficiency for real-world deployment.\", \"page\": 2, \"index\": 1, \"width\": 808, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.25427v1/fig-002.webp\", \"caption\": \"Figure 2: The visualization of RLHF on Wan-2.1.\", \"page\": 8, \"index\": 2, \"width\": 758, \"height\": 1167}]"
motivation: 解决预训练视频扩散模型在提示敏感性、时序不一致和推理成本高等部署问题。
method: 提出包含监督微调、强化学习人类反馈、提示增强和推理优化的四阶段后训练框架。
result: 实验表明该框架能有效减少伪影、提升可控性和视觉质量，同时满足采样成本约束。
conclusion: 该框架为视频生成模型的部署提供了一个系统化、可扩展的后训练解决方案。
---

## 摘要
尽管大规模视频扩散模型在生成高分辨率和语义丰富的内容方面展现出令人印象深刻的能力，但由于提示词敏感性、时间不一致性以及高昂的推理成本等关键问题，其预训练性能与实际部署需求之间仍存在显著差距。为弥合这一差距，我们提出一个全面的后训练框架，通过四个协同阶段系统性地将预训练模型与用户意图对齐：首先采用监督微调（SFT）将基础模型转化为稳定的指令遵循策略；随后通过人类反馈强化学习（RLHF）阶段，利用专为视频扩散定制的新型组相对策略优化（GRPO）方法，以提升感知质量和时间一致性；接着，我们通过专门的语言模型进行提示增强以优化用户输入；最后通过推理优化解决系统效率问题。这些组件共同构成了一种系统化方法，旨在提升视觉质量、时间一致性和指令遵循能力，同时保留预训练期间习得的可控性。该框架为构建稳定、适应性强且在实际部署中有效的可扩展后训练流程提供了实用蓝图。大量实验表明，这一统一流程能有效减少常见伪影，显著提升可控性和视觉美感，同时严格遵守采样成本约束。

## Abstract
While large-scale video diffusion models have demonstrated impressive capabilities in generating high-resolution and semantically rich content, a significant gap remains between their pretraining performance and real-world deployment requirements due to critical issues such as prompt sensitivity, temporal inconsistency, and prohibitive inference costs. To bridge this gap, we propose a comprehensive post-training framework that systematically aligns pretrained models with user intentions through four synergistic stages: we first employ Supervised Fine-Tuning (SFT) to transform the base model into a stable instruction-following policy, followed by a Reinforcement Learning from Human Feedback (RLHF) stage that utilizes a novel Group Relative Policy Optimization (GRPO) method tailored for video diffusion to enhance perceptual quality and temporal coherence; subsequently, we integrate Prompt Enhancement via a specialized language model to refine user inputs, and finally address system efficiency through Inference Optimization. Together, these components provide a systematic approach to improving visual quality, temporal coherence, and instruction following, while preserving the controllability learned during pretraining. The result is a practical blueprint for building scalable post-training pipelines that are stable, adaptable, and effective in real-world deployment. Extensive experiments demonstrate that this unified pipeline effectively mitigates common artifacts and significantly improves controllability and visual aesthetics while adhering to strict sampling cost constraints.

---

## 论文详细总结（自动生成）

好的，我将作为您的资深学术论文分析助手，对这篇题为《A Systematic Post-Train Framework for Video Generation》的论文进行结构化、深入且客观的总结。

### 1. 核心问题与整体含义
*   **研究动机与背景**：当前大规模视频扩散模型（如Sora、Pika等）在预训练后，其生成能力与**实际部署要求**之间存在显著差距。主要问题体现在：
    *   **提示词敏感性**：模型输出对输入提示的措辞非常敏感，细微改动可能导致质量大幅波动。
    *   **时序不一致性**：生成的视频在长序列中容易出现物体变形、闪烁、运动不连贯等时序伪影。
    *   **高昂的推理成本**：基于扩散模型的迭代去噪过程计算开销大，难以满足实时或高吞吐量的应用场景。
    *   **指令跟随与可控性不足**：模型难以精确理解并执行复杂的、多条件的生成指令。
*   **整体含义**：本文提出了一种**系统化的后训练（Post-Train）框架**，旨在将预训练好的基础视频生成模型，通过一系列对齐和优化步骤，转化为一个**高质量、高可控、高效率**的、可直接部署的实用系统。其核心思想是**分阶段、协同式地**解决质量、对齐与效率问题，而非孤立优化。

### 2. 提出的方法论
论文提出了一个包含四个互补阶段的统一后训练流程：

*   **第一阶段：监督微调**
    *   **核心思想**：并非直接优化生成质量，而是建立一个**稳定、可靠的指令跟随基线策略**。使用高质量指令-视频对数据，让模型学会理解并响应各种生成指令和可控接口（如风格、构图控制），纠正最严重的失败案例（如拒绝生成、输出无意义内容），为后续强化学习提供一个良好的起点。

*   **第二阶段：基于人类反馈的强化学习**
    *   **核心思想**：使用强化学习直接优化人类偏好的多维指标。
    *   **关键技术**：
        1.  **GRPO for Video**：采用**组相对策略优化**方法，避免了传统PPO中不稳定的价值函数估计。其核心是为每个提示采样一组（N个）生成结果，在组内计算归一化的优势值，通过优化策略使高奖励样本的概率相对增加。
        2.  **混合ODE-SDE采样与Flash-GRPO**：为解决视频生成rollout成本高的问题，提出了**等时分组**策略。仅为每个提示在**单个指定时间步**引入随机性进行SDE采样以探索，其余时间步使用确定性ODE采样以保证生成质量，大幅降低了计算成本。
        3.  **多奖励模型集成**：同时优化四个奖励信号：**视频美学**（整体视觉质量）、**图像美学**（单帧质量）、**运动质量**（时序连贯性）、**文本-视频对齐**（语义一致性）。论文强调平衡这些奖励是系统成功的关键。

*   **第三阶段：提示增强**
    *   **核心思想**：不仅优化模型（输出端），也优化输入（提示端）。训练一个大型语言模型作为**提示词增强器**。
    *   **关键技术**：同样使用**GRPO框架**训练LLM，但其奖励信号与第二阶段生成器的奖励一致（视频美学、文本对齐等）。这使得LLM学会将用户的简短、模糊的提示，自动重写为更详细、更具表现力且能引导生成器产生更高质量视频的“增强版提示”。

*   **第四阶段：自回归蒸馏**
    *   **核心思想**：将经过前三阶段优化的、性能强大但推理慢的**双向扩散模型**，蒸馏成一个高效的**因果（自回归）架构模型**，以实现实时或流式生成。
    *   **关键技术**：
        1.  **分布匹配蒸馏**：首先将原始模型蒸馏为一个步数更少的双向学生模型。
        2.  **因果ODE回归**：引入块因果注意力，初始化学生模型以适应因果架构。
        3.  **自强制蒸馏**：在训练时，让因果模型基于自己之前生成的帧（通过KV缓存）来预测下一帧，并用DMD损失进行监督，从而解决自回归训练中“曝光偏差”问题，提升长序列生成的稳定性。

### 3. 实验设计
*   **模型与数据**：
    *   **基础模型**：使用一个**内部视频生成模型**（未公开具体名称）。
    *   **训练数据**：构建了一个用于SFT的**高质量文本-视频对数据集**，以及一个用于RLHF、DMD等训练的专用提示词集。
    *   **奖励模型**：遵循HPSv3范式，基于Qwen3.5-VL构建了四个独立的奖励模型（视频美学、图像美学、运动质量、文本对齐）。
*   **评估基准与协议**：
    *   **评估协议**：采用 **“好-相同-差”** 人工比较协议。该协议允许评估者在差异细微时选择“相同”，减少了在边际情况下做出强制或噪声决策的风险。
    *   **评估维度**：从**视觉质量**、**运动质量**、**文本对齐**三个互补方面进行综合评估。
    *   **对比方法**：论文**未进行与外部其他SOTA方法的横向对比**。实验主要是**框架自身的消融实验和效果验证**，即展示加入RLHF、PE等阶段前后，模型自身性能的提升。

### 4. 资源与算力
论文**未明确说明**训练所消耗的具体算力资源，例如GPU的型号、数量、训练总时长或计算开销。仅提及使用了“内部视频生成模型”和“内部数据集”。这是一个信息缺失点。

### 5. 实验数量与充分性
*   **实验数量**：主要展示了两组核心实验结果：
    1.  **RLHF效果**：展示了RLHF阶段相比SFT基线的提升，报告了整体GSB指标提升31%，其中视觉和运动质量提升显著，文本对齐提升较小。
    2.  **提示增强器效果**：展示了集成PE后，在RLHF基础上进一步带来20%的整体GSB提升。
    3.  **可视化对比**：提供了3个提示词下的生成效果对比图（RLHF前后），直观展示了在细节、连贯性、美学上的改进。
*   **充分性与客观性评估**：
    *   **优点**：实验设计清晰地验证了**框架内每个核心阶段（RLHF， PE）的有效性**，并通过人工评估和可视化证明了质量提升。
    *   **局限**：
        *   **缺乏外部对比**：最大的局限性是**没有与当前其他先进的视频生成模型或后训练方法进行对比**，无法客观定位其方法在领域内的相对性能水平。
        *   **依赖内部资源**：实验基于未公开的模型和数据集，**可复现性和可比性较低**。
        *   **评估主观性**：尽管GSB协议更合理，但最终依赖人工评估，仍存在一定主观性。未报告评估者间一致性等指标。

### 6. 主要结论与发现
1.  提出的四阶段后训练框架能**系统性地、显著地**提升视频生成模型在视觉质量、运动连贯性和指令跟随方面的性能。
2.  **RLHF阶段是提升感知质量的关键**，使用GRPO和多奖励模型能有效优化模型，带来31%的整体偏好提升。
3.  **提示增强阶段能与RLHF形成互补**，通过优化输入进一步提升20%的整体效果，证明了同时优化输入和输出的有效性。
4.  **自回归蒸馏阶段**能将优化后的模型能力迁移到高效架构中，为实际部署铺平道路。
5.  **当前瓶颈**：文本-视频对齐的奖励模型准确性不足，限制了该维度性能的进一步提升，这是未来需要改进的方向。

### 7. 优点
1.  **系统化与完整性**：首次提出了一个覆盖**质量对齐、输入优化、效率提升**全链条的视频生成后训练系统框架，思路清晰完整。
2.  **技术创新**：将GRPO成功适配并优化于视频扩散模型（如Flash-GRPO），解决了视频RLHF的高成本问题；提出用同一套奖励同时训练生成器和提示增强器，构思巧妙。
3.  **实用导向**：不仅关注生成质量，还通过自回归蒸馏切实考虑**推理效率**，使研究更具落地价值。
4.  **多维奖励平衡**：明确指出了多奖励模型联合优化的挑战，并设计了相应的平衡策略，考虑周全。

### 8. 不足与局限
1.  **实验对比不足**：如前所述，缺乏与外部方法的对比实验，结论的说服力受限。
2.  **可复现性低**：依赖未公开的“内部模型”和数据集，代码、模型权重均未开源，社区难以验证和跟进。
3.  **技术细节模糊**：对于GRPO训练中的关键超参数、奖励模型的具体融合权重、自回归蒸馏的具体架构等细节描述不够充分。
4.  **局限性承认**：文中指出文本对齐奖励模型是当前瓶颈，这确实是该框架乃至整个领域的一个关键挑战。
5.  **应用范围**：框架流程较长，涉及SFT、RLHF、蒸馏等多个复杂阶段，对计算资源和工程实现能力要求极高，可能主要适用于大型机构。

（完）
