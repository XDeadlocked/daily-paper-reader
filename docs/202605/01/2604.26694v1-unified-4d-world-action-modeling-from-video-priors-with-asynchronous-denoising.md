---
title: Unified 4D World Action Modeling from Video Priors with Asynchronous Denoising
title_zh: 基于视频先验与异步去噪的统一4D世界动作建模
authors: "Jun Guo, Qiwei Li, Peiyan Li, Zilong Chen, Nan Sun, Yifei Su, Heyun Wang, Yuan Zhang, Xinghang Li, Huaping Liu"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26694v1"
tags: ["query:sr-0501"]
score: 10.0
evidence: 基于视频先验的统一4D世界模型
tldr: 本文提出X-WAM，一个统一的4D世界模型，旨在克服先前模型（如UWM）仅建模2D像素空间且难以平衡动作效率与世界建模质量的局限。该方法利用预训练视频扩散模型的视觉先验，通过轻量级结构适配预测多视角RGB-D视频以重建未来空间信息，并引入异步噪声采样来联合优化生成质量和动作解码效率。在超过5800小时机器人数据上预训练后，X-WAM在多个基准测试中取得高成功率，同时生成高保真4D重建结果，显著提升了机器人动作执行与世界合成的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26694v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of X-WAM. Top: X-WAM is a unified 4D World Action Model that jointly predicts future multi-view RGB-D videos and robot actions from video priors, featuring a lightweight depth adaptation module for spatial reconstruction and Asynchronous Noise Sampling (ANS) for efficient action decoding. Bottom: X-WAM surpasses existing methods in policy success rate on RoboCasa and RoboTwin 2.0, produces high-fidelity 4D reconstruction and generation, and enables real-time execution deployment on physical robots.\", \"page\": 2, \"index\": 1, \"width\": 826, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2604.26694v1/fig-002.webp\", \"caption\": \"Table 6: Per-task success rate (%) of X-WAM on the RoboCasa benchmark (24 tasks).\", \"page\": 18, \"index\": 2, \"width\": 318, \"height\": 577}, {\"url\": \"assets/figures/arxiv/2604.26694v1/fig-003.webp\", \"caption\": \"Figure 2: Overview of X-WAM. (a) Model architecture: multi-view RGB observations, proprioceptive states, and noisy actions are encoded and jointly denoised by a Diffusion Transformer initialized from Wan2.2-5B, with a lightweight interleaved depth branch for spatial modeling. (b) Asynchronous Noise Sampling (ANS): (i) standard decoupled sampling wastes training on configurations where tO < ta; (ii) our coupled joint sampling ensures tO ≥ ta, faithfully matching the inference distribution; (iii) during inference, actions are decoded in Ta steps and immediately dispatched, while video denoising continues for TO steps.\", \"page\": 4, \"index\": 3, \"width\": 824, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2604.26694v1/fig-004.webp\", \"caption\": \"Table 7: Per-task success rate (%) of X-WAM on the RoboTwin 2.0 benchmark (50 tasks).\", \"page\": 19, \"index\": 4, \"width\": 790, \"height\": 571}, {\"url\": \"assets/figures/arxiv/2604.26694v1/fig-005.webp\", \"caption\": \"Table 4: Ablation studies on the RoboCasa benchmark. Bold: best; underline: second best.\", \"page\": 9, \"index\": 5, \"width\": 830, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.26694v1/fig-006.webp\", \"caption\": \"Figure 4: Qualitative results of X-WAM deployed on a real AC One dual-arm robot for the earphone packing task. Each image shows keyframes from a representative execution rollout.\", \"page\": 20, \"index\": 6, \"width\": 826, \"height\": 202}]"
motivation: 解决现有统一世界模型仅建模2D像素空间且无法平衡动作效率与世界建模质量的问题。
method: 通过轻量级结构适配复制预训练扩散Transformer块来预测深度，并采用异步噪声采样优化生成与动作解码。
result: "在RoboCasa和RoboTwin 2.0基准测试中平均成功率分别达79.2%和90.7%，4D重建与生成质量超越现有方法。"
conclusion: X-WAM 通过统一框架和异步去噪，在机器人动作执行和世界建模质量上取得显著提升，为具身智能提供了高效解决方案。
---

## 摘要
我们提出了X-WAM，一个统一的4D世界模型，它将实时机器人动作执行与高保真4D世界合成（视频+3D重建）统一在单一框架中，解决了先前统一世界模型（例如UWM）的关键局限性，这些模型仅建模2D像素空间且无法平衡动作效率与世界建模质量。为利用预训练视频扩散模型的强大视觉先验，X-WAM通过预测多视角RGB-D视频来想象未来世界，并通过轻量级结构适配高效获取空间信息：将预训练扩散Transformer的最后几个块复制到专用的深度预测分支，以重建未来空间信息。此外，我们提出了异步噪声采样（ANS）来联合优化生成质量和动作解码效率。ANS在推理过程中应用专门的异步去噪调度，以更少的步骤快速解码动作以实现高效实时执行，同时将完整步骤序列用于生成高保真视频。ANS并非在训练期间完全解耦时间步，而是从它们的联合分布中采样以与推理分布对齐。在超过5,800小时的机器人数据上预训练后，X-WAM在RoboCasa和RoboTwin 2.0基准测试中实现了79.2%和90.7%的平均成功率，同时产生的高保真4D重建和生成在视觉和几何指标上均超越现有方法。

## Abstract
We propose X-WAM, a Unified 4D World Model that unifies real-time robotic action execution and high-fidelity 4D world synthesis (video + 3D reconstruction) in a single framework, addressing the critical limitations of prior unified world models (e.g., UWM) that only model 2D pixel-space and fail to balance action efficiency and world modeling quality. To leverage the strong visual priors of pretrained video diffusion models, X-WAM imagines the future world by predicting multi-view RGB-D videos, and obtains spatial information efficiently through a lightweight structural adaptation: replicating the final few blocks of the pretrained Diffusion Transformer into a dedicated depth prediction branch for the reconstruction of future spatial information. Moreover, we propose Asynchronous Noise Sampling (ANS) to jointly optimize generation quality and action decoding efficiency. ANS applies a specialized asynchronous denoising schedule during inference, which rapidly decodes actions with fewer steps to enable efficient real-time execution, while dedicating the full sequence of steps to generate high-fidelity video. Rather than entirely decoupling the timesteps during training, ANS samples from their joint distribution to align with the inference distribution. Pretrained on over 5,800 hours of robotic data, X-WAM achieves 79.2% and 90.7% average success rate on RoboCasa and RoboTwin 2.0 benchmarks, while producing high-fidelity 4D reconstruction and generation surpassing existing methods in both visual and geometric metrics.

---

## 论文详细总结（自动生成）

好的，我将基于您提供的论文内容，按照您要求的要点生成一份详细的结构化中文总结。

### 1. 核心问题与整体含义
*   **研究动机**：当前具身智能领域存在两大分离的范式：**策略模型**（如VLA模型）专注于预测可执行动作但缺乏对世界动态的物理直觉；**世界模型**擅长生成逼真的未来观测但不直接输出动作。现有的统一世界动作模型（如UWM）虽尝试结合两者，但仍**局限于2D像素空间**，缺乏显式的3D空间感知，导致无法进行几何上可信的3D重建，且难以平衡高保真视频生成所需的冗长去噪步骤与实时动作解码所需的快速推理。
*   **整体含义**：本文旨在将统一世界动作建模**从2D提升到4D（3D空间+时间）**，提出一个能同时优化**高保真视频生成、3D空间重建、高策略成功率和高效动作执行**四大目标的单一框架，以释放统一模型在物理世界交互中的全部潜力。

### 2. 提出的方法论
*   **核心思想**：基于预训练的大规模视频扩散模型（Wan2.2-5B）的强大视觉先验，构建一个统一的4D世界动作模型（X-WAM），通过**多视角RGB-D视频预测**来想象未来，并联合预测机器人动作。
*   **关键技术细节**：
    1.  **轻量级深度适配模块**：
        *   **问题**：直接将深度图作为额外通道或序列会破坏预训练权重或导致序列长度爆炸。
        *   **方案**：复制预训练扩散Transformer（DiT）的最后M个块，构建一个**专用的深度预测分支**。该分支通过**单向注意力**与主RGB分支在深层交错连接（深度分支可读取主分支特征，反之则不行），从而在**不修改核心预训练权重、不增加主去噪序列长度**的前提下，实现高质量的深度图回归。
    2.  **异步噪声采样**：
        *   **问题**：视频需要多步去噪以保证质量，而低维动作仅需少数步骤即可准确解码。若使用同步调度，则需在速度和质量间折衷。
        *   **方案**：
            *   **推理**：采用**异步去噪调度**。为动作分配较少的去噪步数（Ta），为视频分配较多的步数（TO，Ta < TO）。动作在Ta步后解码并立即执行，视频则继续完成剩余的TO-Ta步去噪。
            *   **训练**：为确保训练与推理分布一致，提出**耦合的联合噪声采样**。视频和动作的噪声时间步（tO, ta）从一个联合分布中采样，该分布强制`tO ≥ ta`，模拟了“动作先于视频被去噪干净”的异步推理场景，避免了独立采样造成的训练效率低下。
*   **模型流程**：以语言指令、初始状态和多视角RGB观测为条件，将编码后的RGB视频潜变量、状态潜变量和带噪声的动作潜变量拼接为一个序列，输入到基于DiT的模型中进行联合去噪。深度分支从主分支特征回归深度图。训练采用流匹配目标函数。

### 3. 实验设计
*   **数据集/场景**：
    *   **预训练**：使用了超过5800小时（约149万条轨迹）的机器人数据，包括真实机器人（如DROID）和仿真（如InternA1, RoboCasa）数据集。
    *   **微调与评估**：
        1.  **RoboCasa**：大规模厨房操作仿真基准，包含24个多样化任务。
        2.  **RoboTwin 2.0**：双臂操作仿真基准，评估在“清洁”和“随机化”设置下的泛化能力。
        3.  **真实机器人实验**：在AC One双臂平台上进行“耳机打包”长视野任务，测试可部署性和泛化性。
*   **Benchmark与对比方法**：
    *   **策略评估**：以**任务平均成功率**为主要指标。
        *   **对比基线**：包括VLA模型（π0, GR00T-N1.5, π0.5）和WAM模型（UWM, DreamZero, Cosmos Policy, GigaWorld-Policy, Motus）。
    *   **4D重建与生成评估**：在RoboCasa上比较预测视频与仿真器真值。
        *   **评估指标**：RGB质量（PSNR, SSIM, LPIPS）、深度质量（AbsRel, δ1）、点云质量（Chamfer Distance）。
        *   **对比基线**：两阶段方法（DreamZero + Depth Anything 3）、几何感知生成方法（Robot4DGen）。

### 4. 资源与算力
*   **明确提及**：
    *   **预训练**：使用**256块NVIDIA H20 GPU**，批量大小为2048，训练了**40,000步**。
    *   **基准微调**：使用**32块NVIDIA H20 GPU**，批量大小为128，训练了**20,000步**。
    *   **真实机器人实验微调**：使用64块H20 GPU。
*   **数据规模**：预训练数据超过**5,800小时**，体现了大规模训练的特点。

### 5. 实验数量与充分性
*   **实验组别**：
    1.  **主要性能对比实验**：在两个核心仿真基准（RoboCasa, RoboTwin 2.0）上与多类SOTA方法进行对比。
    2.  **4D重建质量评估实验**：在RoboCasa上定量评估生成视频的视觉和几何精度。
    3.  **消融实验**：
        *   **深度架构设计**：对比了无深度、序列拼接、通道拼接和本文的交叉分支四种策略。
        *   **噪声调度策略**：对比了同步训练/推理、解耦训练/同步推理、解耦训练/异步推理和本文的ANS四种配置。
    4.  **真实世界部署实验**：在双臂机器人上完成复杂长视野任务，测试了可扩展性（打包1-3个耳机）和泛化性（新摆放、新桌布、新干扰物）。
*   **充分性与客观性评估**：
    *   **较为充分**：实验覆盖了仿真性能、生成质量、关键模块消融和真实部署，维度全面。
    *   **客观公平**：在基准测试中，对比了当前代表性的VLA和WAM方法；对于需要复现的基线（如DreamZero, UWM），论文声明使用了相同的骨干网络（Wan2.2-5B）以确保公平比较。评估指标是领域内公认的标准指标。

### 6. 主要结论与发现
*   X-WAM在**RoboCasa**和**RoboTwin 2.0**上均取得了最佳的平均成功率（79.2%和90.7%），显著超越了所有基线方法。
*   在4D重建质量上，X-WAM的**端到端联合建模**显著优于“视频生成+后处理深度估计”的两阶段方法，在所有视觉和几何指标上领先。
*   **消融实验证明**：
    1.  显式的深度监督能**提升策略成功率**（从63.0%到67.8%），证实空间建模对决策有益。
    2.  本文的**轻量级深度分支**在几乎不增加动作解码延迟的前提下，达到了与显著增加延迟的“序列拼接”方法相近的重建质量。
    3.  **异步噪声采样**在将动作解码延迟降低4.5倍的同时，成功保持了视频生成质量，且优于简单的解耦采样策略。

### 7. 优点
*   **概念创新**：首次在统一世界动作建模中引入**显式的4D（3D+时间）感知与生成**，统一了动作、视频和几何重建。
*   **技术精巧**：
    1.  **轻量级深度适配**：通过复制块和单向注意力，以极小代价注入3D能力，最大程度保护了预训练视觉先验。
    2.  **异步噪声采样**：从训练到推理的系统性设计，优雅地解决了多模态去噪步调不一的问题，兼顾了速度与质量。
*   **实验规模大且验证全面**：基于超大规模数据预训练，并在仿真基准、真实机器人任务上进行了多维度验证，说服力强。

### 8. 不足与局限
*   **模型自身局限**（论文已指出）：
    1.  **固定上下文窗口**：未整合历史信息或进行自回归推演，可能限制其在长视野任务中对任务进展的理解。
    2.  **推理延迟**：作为统一生成模型，其单步推理延迟（~300ms）仍高于专用策略模型，尽管通过实时分块执行进行了缓解。
*   **潜在限制与偏差风险**：
    1.  **对预训练视频模型的强依赖**：性能上限受限于骨干视频生成模型（Wan2.2-5B）的能力与偏差。
    2.  **多视角与标定要求**：方法依赖于多视角输入和已知的手眼标定矩阵，在视角受限或标定不准的场景中应用会受限。
    3.  **数据需求巨大**：依赖于数千小时的机器人数据，且需要预处理生成深度标签，复现成本高。
    4.  **仿真到真实的差距**：虽然在真实机器人上进行了测试，但任务相对单一，在更复杂、动态的真实环境中的泛化能力有待进一步验证。

（完）
