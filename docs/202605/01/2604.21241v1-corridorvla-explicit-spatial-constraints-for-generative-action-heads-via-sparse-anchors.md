---
title: "CorridorVLA: Explicit Spatial Constraints for Generative Action Heads via Sparse Anchors"
title_zh: CorridorVLA：通过稀疏锚点为生成式动作头施加显式空间约束
authors: "Dachong Li, ZhuangZhuang Chen, Jin Zhang, Jianqiang Li"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21241v1"
tags: ["query:sr-0501"]
score: 9.5
evidence: 提出CorridorVLA，为机器人控制中的视觉-语言-动作模型引入显式空间约束
tldr: "本研究针对视觉-语言-动作模型中空间引导隐式、缺乏直接物理约束的问题，提出CorridorVLA方法。该方法预测稀疏空间锚点作为增量物理变化，构建显式容忍区域来约束动作生成，通过流匹配动作头允许微小偏差但纠正超出区域的轨迹。在LIBERO-Plus基准上，该方法显著提升了SmolVLA和GR00T模型的成功率，最高达83.21%，证明了动作对齐物理线索能为生成式动作策略提供直接可解释的约束。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21241v1/fig-001.webp\", \"caption\": \"Fig. 2. Framework. (A) The backbone predicts a small set of future key spatial increments, while the action output is augmented with the corresponding end-effector displacement fields. These key increments are then used to constrain action generation, requiring only a few additional prediction slots with minimal changes to the original VLA pipeline. (B) Spatial-change guidance provides a simple prior: manipulation trajectories tend to evolve smoothly, so sparse key increments can offer a safe, structured signal that reduces unstructured exploration under stochastic generation.\", \"page\": 3, \"index\": 1, \"width\": 512, \"height\": 410}]"
motivation: 现有视觉-语言-动作模型通常通过隐式特征注入空间引导，缺乏对动作生成的直接物理约束，导致在复杂任务中性能受限。
method: 提出CorridorVLA模型，通过预测稀疏空间锚点定义物理变化容忍区域，并利用流匹配动作头在训练目标中施加显式约束，以引导动作生成。
result: "在LIBERO-Plus基准测试中，CorridorVLA在SmolVLA和GR00T模型上均取得显著提升，成功率提高3.4%至12.4%，其中GR00T-Corr变体达到83.21%的成功率。"
conclusion: 研究表明，将动作对齐的物理线索作为显式约束，能够有效补充视觉或隐式空间编码，提升生成式动作策略的性能和可解释性。
---

## 摘要
视觉-语言-动作（VLA）模型通常使用中间表示来连接多模态输入与连续控制，但空间引导往往通过潜在特征隐式注入。我们提出$CorridorVLA$，它预测稀疏空间锚点作为增量物理变化（例如$Δ$-位置），并在训练目标中使用它们为动作生成施加明确的容忍区域。这些锚点定义了一个引导流匹配动作头的走廊：隐含空间演化超出该区域的轨迹会收到校正梯度，同时允许与接触和执行噪声的微小偏差。在更具挑战性的LIBERO-Plus基准测试中，CorridorVLA在SmolVLA和GR00T上均取得了一致的增益，相比相应基线成功率提高了$3.4\%$--$12.4\%$；值得注意的是，我们的GR00T-Corr变体达到了$83.21\%$的成功率。这些结果表明，动作对齐的物理线索可以为生成式动作策略提供直接且可解释的约束，补充了以视觉或潜在形式编码的空间引导。代码发布于https://github.com/corridorVLA。

## Abstract
Vision--Language--Action (VLA) models often use intermediate representations to connect multimodal inputs with continuous control, yet spatial guidance is often injected implicitly through latent features. We propose $CorridorVLA$, which predicts sparse spatial anchors as incremental physical changes (e.g., $Δ$-positions) and uses them to impose an explicit tolerance region in the training objective for action generation. The anchors define a corridor that guides a flow-matching action head: trajectories whose implied spatial evolution falls outside it receive corrective gradients, while minor deviations from contacts and execution noise are permitted. On the more challenging LIBERO-Plus benchmark, CorridorVLA yields consistent gains across both SmolVLA and GR00T, improving success rate by $3.4\%$--$12.4\%$ over the corresponding baselines; notably, our GR00T-Corr variant reaches a success rate of $83.21\%$. These results indicate that action-aligned physical cues can provide direct and interpretable constraints for generative action policies, complementing spatial guidance encoded in visual or latent forms. Code is available at https://github.com/corridorVLA.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，为您生成一份结构化的深入总结。

---

### **论文总结：CorridorVLA: Explicit Spatial Constraints for Generative Action Heads via Sparse Anchors**

#### **1. 核心问题与整体含义**
*   **研究背景**：视觉-语言-动作（VLA）模型是迈向通用机器人策略的重要路径。当前主流方法通常通过预测未来图像、视频等**视觉或潜在形式的中间表示**来为动作生成提供空间引导，这种引导是**隐式**的，通过特征调制间接影响动作。
*   **研究动机**：本文探索一种**互补且更直接**的路径：能否将空间引导表达为与动作空间更对齐的、**文本风格的物理量**（如末端执行器的位移），并在**目标函数层面**直接约束动作生成？目标是提供更明确、可解释的空间约束，以提升策略性能。

#### **2. 方法论**
*   **核心思想**：预测稀疏的未来空间锚点（如末端执行器在关键时间步的Δ-位置），并利用这些锚点在训练目标中定义一个**显式的“容忍走廊”**，以约束生成的动作轨迹。
*   **关键技术细节**：
    1.  **稀疏锚点预测**：在骨干网络（VLM）中引入少量可学习的“锚点槽”，使其从视觉-语言上下文中预测未来`K`个时间步的末端执行器**增量位移**（Δ-pos），而非绝对位置，以更好匹配控制特性。
    2.  **动作空间扩展**：将动作向量扩展为 `[动作指令；末端执行器Δ-位置]`，使动作头的监督信号与骨干网络预测的锚点在物理量上对齐。
    3.  **走廊约束损失**：在流匹配（Flow Matching）动作头的训练中，引入基于锚点的正则项：
        *   **缓冲区损失 (L_buf)**：计算生成轨迹在锚点时刻的预测位移与真实锚点的距离。若该距离超过一个动态阈值（走廊宽度），则施加惩罚，促使轨迹快速修正至走廊内。
        *   **走廊内一致性损失 (L_cons)**：当预测进入走廊后，该损失确保轨迹在走廊内继续优化，通过惩罚累积位移的误差来防止漂移。
    4.  **噪声感知加权**：根据流匹配的噪声时间步`t`对走廊损失进行加权（`w(t) = 1 - t`），在低噪声（接近真实数据）时加强空间约束，在高噪声时减弱约束。

#### **3. 实验设计**
*   **数据集/场景**：在机器人操作仿真基准 **LIBERO** 和其更具挑战性的变体 **LIBERO-Plus** 上进行评估。
*   **Benchmark**：任务成功率（Success Rate）。
*   **对比方法**：
    *   **基线模型**：**SmolVLA**（轻量级）和 **GR00T**（大规模）的原版。
    *   **同期先进方法**：包括 π0、GraspVLA、NORA、UniVLA、OpenVLA-OFT等。

#### **4. 资源与算力**
*   论文**未明确说明**训练所使用的具体GPU型号、数量及训练时长。实验基于开源框架（LeRobot）和官方默认超参数进行，以确保复现的公平性。

#### **5. 实验数量与充分性**
*   **实验数量充分**，设计较为客观公平：
    1.  **主实验**：在两个骨干网络（SmolVLA, GR00T）和两个基准（LIBERO, LIBERO-Plus）上验证了方法的普适性和有效性，均显示出显著提升。
    2.  **消融实验**：
        *   **损失组件**：验证了缓冲区损失（L_buf）和一致性损失（L_cons）均为必要，共同作用效果最佳。
        *   **锚点选择策略**：对比了基于几何简化（RDP+DP）的锚点选择与均匀采样，证明前者更有效。
        *   **骨干交互与预测目标**：探索了“状态作为输出”的不同变体，最终确定了“保持当前状态为输入，仅预测未来增量位移”的最佳设计。
        *   **预测目标类型**：证明了预测**增量位移**（Δ-pos）优于预测**绝对位置**（pos）。
*   **公平性**：在对比实验中，严格保持了与基线模型相同的训练协议、超参数和输入分辨率，仅添加了极少的预测令牌（K=3），确保了性能增益源于方法本身而非额外容量。

#### **6. 主要结论与发现**
*   **有效提升**：CorridorVLA能一致地提升不同VLA骨干网络的性能。在LIBERO-Plus上，SmolVLA-Corr比基线提升12.4%，GR00T-Corr达到83.21%的成功率。
*   **核心发现**：**动作对齐的、显式的物理空间约束**（通过稀疏锚点和走廊损失实现）是补充现有隐式视觉/潜在空间引导的有效设计轴。它能为生成式动作策略提供直接、可解释的指导。
*   **设计启示**：轻量级的、在目标函数层面的耦合（仅增加少量预测令牌）即可实现显著改进，表明在VLA中设计更接近动作流形的中间表示具有巨大潜力。

#### **7. 优点**
*   **思想新颖**：将空间约束从“隐式特征调制”转变为“显式目标函数约束”，提供了新的设计视角。
*   **轻量高效**：方法仅需在骨干网络中添加极少量的预测令牌，几乎不增加推理开销，易于集成到现有VLA pipeline中。
*   **可解释性强**：“走廊”概念直观地对应了物理轨迹的可行区域，约束机制明确。
*   **实验扎实**：在两个主流骨干和两个基准上进行了全面验证，并设计了详尽的消融实验来分析各组件贡献，结论可信。

#### **8. 不足与局限**
*   **缺乏真实机器人验证**：所有实验均在仿真环境中进行，未在真实机器人上测试。仿真到现实的差距、物理不确定性（如接触动力学）对走廊宽度和噪声加权的影响尚不明确。
*   **对比范围有限**：未与同样提供空间引导但基于**视觉中间表示**的方法（如InterleaveVLA, ReconVLA）进行直接的、同条件的对比。论文承认这些方法在形式和假设上不同，但头对头比较能更清晰地定位本方法的优势场景。
*   **锚点表示的局限性**：当前仅使用了末端执行器的3D Δ-位置作为锚点。对于需要更复杂空间约束（如物体朝向、关节角度、多物体关系）的任务，此表示可能不足，扩展性有待探索。
*   **基准任务特性**：实验集中于桌面操作任务（LIBERO系列），在更动态、非结构化环境中的有效性有待验证。

（完）
