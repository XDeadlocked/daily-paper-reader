---
title: "CodeGraphVLP: Code-as-Planner Meets Semantic-Graph State for Non-Markovian Vision-Language-Action Models"
title_zh: CodeGraphVLP：面向非马尔可夫视觉-语言-动作模型的代码即规划器与语义图状态融合框架
authors: "Khoa Vo, Sieu Tran, Taisei Hanyu, Yuki Ikebe, Duy Nguyen, Bui Duy Quoc Nghi, Minh Vu, Anthony Gunderman, Chase Rainwater, Anh Nguyen, Ngan Le"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22238v1"
tags: ["query:sr-0501"]
score: 10.0
evidence: 用于机器人操作的非马尔可夫视觉-语言-动作模型
tldr: 本文针对现有视觉-语言-动作模型在非马尔可夫长时程机器人操作任务中因部分可观测性、遮挡和干扰导致的性能瓶颈，提出了CodeGraphVLP分层框架。该方法通过维护持久语义图状态来捕捉任务相关实体和关系，结合基于代码的规划器进行高效进度检查和子任务指令生成，并利用进度引导的视觉语言提示构建抑制干扰的观察以聚焦关键证据。实验表明，该方法在真实世界任务中显著优于现有基线，提高了任务完成率并降低了规划延迟。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22238v1/fig-001.webp\", \"caption\": \"Fig. 3. Qualitative rollouts of CodeGraphVLP on our three real-world tasks (Pick-and-Place Twice, Place-and-Stack, and Swap Cups). For each task, we show multi-view RGB inputs with the overall instruction, the semantic-graph state Gt, and the progress-guided prompts used by the VLA policy: clutter-free visual cues that retain only subtask-relevant objects and the planner-produced subtask language cues.\", \"page\": 6, \"index\": 1, \"width\": 734, \"height\": 718}, {\"url\": \"assets/figures/arxiv/2604.22238v1/fig-002.webp\", \"caption\": \"TABLE I COMPARISON OF METHODS ON REAL-WORLD NON-MARKOVIAN LONG-HORIZON TASKS. ALL REPORTED VALUES ARE SUCCESS RATES (%).\", \"page\": 7, \"index\": 2, \"width\": 922, \"height\": 221}, {\"url\": \"assets/figures/arxiv/2604.22238v1/fig-003.webp\", \"caption\": \"Fig. 2. Overview of CodeGraphVLP. (a) Graph state initialization: from the first multi-view observation, we segment objects, identify task-relevant entities, associate them across views, and induce relations to build a compact semantic graph. (b) Programmatic planner construction: given the task and a graph API, an LLM synthesizes an executable policy(graph) that performs progress checks over the graph and maintains lightweight task memory. (c) End-to-end inference: as new observations arrive, we update the semantic graph, execute the planner to produce the next subtask and relevant objects, construct clutter-free visual–language cues, and run a VLA policy for action prediction.\", \"page\": 3, \"index\": 3, \"width\": 942, \"height\": 495}]"
motivation: 解决现有视觉-语言-动作模型在非马尔可夫长时程任务中因部分可观测性、遮挡和干扰而导致的性能下降问题。
method: 采用分层框架，结合持久语义图状态、基于代码的规划器和进度引导的视觉语言提示。
result: 在真实世界非马尔可夫任务中，相比基线方法，CodeGraphVLP提高了任务完成率并降低了规划延迟。
conclusion: CodeGraphVLP通过结合语义图状态与代码规划器，显著提升了非马尔可夫长时程任务中的机器人操作性能。
---

## 摘要
视觉-语言-动作（VLA）模型有望实现通用机器人操作，但通常作为短视程策略进行训练和部署，其假设最新观测足以支持动作推理。这一假设在非马尔可夫的长视程任务中失效，因为任务相关证据可能被遮挡或仅在轨迹早期出现，且杂乱环境和干扰物使细粒度视觉定位变得脆弱。我们提出CodeGraphVLP，一种分层框架，通过将持久性语义图状态与可执行的基于代码的规划器及进度引导的视觉-语言提示相结合，实现可靠的长视程操作。语义图在部分可观测性下维护任务相关实体与关系。合成规划器在该语义图上执行，以进行高效的进度检查，并输出子任务指令及相关对象。我们利用这些输出来构建抑制杂乱的观测，使VLA执行器聚焦于关键证据。在真实世界的非马尔可夫任务中，CodeGraphVLP相比强大的VLA基线及支持历史记录的变体提高了任务完成率，同时相比基于VLM的循环规划大幅降低了规划延迟。我们还进行了广泛的消融研究以确认各组成部分的贡献。

## Abstract
Vision-Language-Action (VLA) models promise generalist robot manipulation, but are typically trained and deployed as short-horizon policies that assume the latest observation is sufficient for action reasoning. This assumption breaks in non-Markovian long-horizon tasks, where task-relevant evidence can be occluded or appear only earlier in the trajectory, and where clutter and distractors make fine-grained visual grounding brittle. We present CodeGraphVLP, a hierarchical framework that enables reliable long-horizon manipulation by combining a persistent semantic-graph state with an executable code-based planner and progress-guided visual-language prompting. The semantic-graph maintains task-relevant entities and relations under partial observability. The synthesized planner executes over this semantic-graph to perform efficient progress checks and outputs a subtask instruction together with subtask-relevant objects. We use these outputs to construct clutter-suppressed observations that focus the VLA executor on critical evidence. On real-world non-Markovian tasks, CodeGraphVLP improves task completion over strong VLA baselines and history-enabled variants while substantially lowering planning latency compared to VLM-in-the-loop planning. We also conduct extensive ablation studies to confirm the contributions of each component.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，为您生成一份结构化的详细中文总结。

### **论文总结：CodeGraphVLP**

#### **1. 核心问题与整体含义**
*   **研究背景**：当前主流的视觉-语言-动作（VLA）模型通常被训练和部署为**短视程策略**，其核心假设是当前时刻的观察足以决定下一个动作。然而，在真实世界的**长视程、非马尔可夫**机器人操作任务中，这一假设不再成立。
*   **核心问题**：
    1.  **部分可观测性与历史依赖**：完成任务所需的关键信息（如物体的初始位置）可能被遮挡或仅在历史中出现，当前观察无法提供。
    2.  **环境杂乱与干扰**：场景中存在大量与当前子任务无关的物体，会干扰VLA模型进行精确的视觉定位和动作推理。
    3.  **规划效率低下**：现有解决方案（如基于VLM的循环规划）虽然能处理长视程任务，但需要频繁调用大模型，导致**规划延迟高**，难以满足实时控制需求。

#### **2. 方法论**
*   **核心思想**：提出一个分层框架，将**持久化的结构化场景表示**、**轻量级的程序化规划**和**进度引导的感知提示**相结合，以解决上述问题。
*   **关键技术细节**：
    1.  **持久化语义图状态**：
        *   **构建**：从初始多视角RGB观测出发，通过实例分割、VLM识别任务相关物体、多视角关联和几何关系归纳，构建一个包含实体（节点）和关系（边）的语义图 `G_t`。
        *   **更新**：在线运行时，使用视频分割模型（Cutie）跟踪已有物体，并持续检测和关联新出现的任务相关物体，动态更新图状态。
    2.  **代码即规划器**：
        *   **一次性合成**：在任务开始时，利用LLM（如GPT-5）根据任务指令和语义图API，**一次性合成**一个可执行的Python程序 `P`。该程序封装了任务逻辑和进度检查规则。
        *   **高效执行**：在每一步，规划器 `P` 查询当前的语义图 `G_t`，通过简单的图谓词（如 `holding()`, `inside()`）判断子任务完成状态，并输出下一个**子任务指令** `l^sub_t` 和**子任务相关物体集合** `O^rel_t`。此过程无需反复调用大模型，延迟极低。
    3.  **抑制干扰的视觉-语言提示**：
        *   **语言提示**：将原始的长视程任务指令替换为规划器生成的简洁、 imperative 的子任务指令 `l^sub_t`。
        *   **视觉提示**：利用语义图中的物体掩码，将原始RGB图像中**非** `O^rel_t` 的物体区域置黑，生成只保留关键物体的“无干扰”观测 `\tilde{o}_t`。
        *   **VLA执行**：底层VLA策略接收 `(\tilde{o}_t, s_t, l^sub_t)` 作为输入，预测动作。在训练时也采用相同的提示方式以匹配部署。

#### **3. 实验设计**
*   **任务/场景**：设计了三个具有非马尔可夫依赖性的真实世界桌面操作任务：
    1.  **Pick-and-Place Twice**：将立方体移到另一个盘子，再移回原盘子（需记忆初始位置）。
    2.  **Place-and-Stack**：将立方体放入最近的杯子，再将另一个杯子叠放上去（立方体放入后即被遮挡）。
    3.  **Swap Cups**：交换两个杯子的位置，需使用一个空盘子作为缓冲区，并遵循起始顺序约束。
*   **基准与对比方法**：
    *   **强VLA基线**：π0 FAST, π0, π0.5, Gr00T N1.5。
    *   **历史增强变体**：Gr00T N1.5 + Multi-frame（输入过去多帧图像）。
    *   **VLM循环规划变体**（用于消融实验）：使用VLM（而非代码）进行在线子任务规划。

#### **4. 资源与算力**
*   论文中未明确说明完整的预训练算力消耗。
*   **微调阶段**：所有VLA模型（包括基线）均使用4张 NVIDIA A6000 GPU，进行了50K次迭代的微调。
*   **推理硬件**：实验在UR10e机械臂上进行，配备Robotiq 2F-85夹爪和两个RealSense相机（全局和腕部视角）。

#### **5. 实验数量与充分性**
*   **主实验**：在3个任务上对比了5种基线方法（共6个模型变体），报告了**任务整体成功率**和**关键子阶段成功率**，评估全面。
*   **消融实验**：
    1.  **规划效率**：在Swap Cups任务上，对比了有无语义图、以及用VLM替代代码规划器的性能与延迟。
    2.  **提示有效性**：在Swap Cups任务上，对比了使用与不使用“无干扰视觉提示”的性能。
*   **评估**：实验设计**较为充分和客观**。任务针对非马尔可夫和杂乱场景的核心挑战而设计；对比了当前代表性的VLA模型及其增强变体；消融实验清晰地分离并量化了各核心组件的贡献；所有对比方法均在相同的数据集、提示和重新规划节奏下进行微调和评估，保证了公平性。

#### **6. 主要结论与发现**
*   **性能领先**：CodeGraphVLP在三个非马尔可夫任务上的**平均成功率（81.7%）显著高于所有基线**，包括最强的历史增强VLA（56.7%）。
*   **高效规划**：其代码规划器相比VLM在线规划，**将每步规划延迟从约3秒降低至0.3秒**，同时取得了更高的成功率（85% vs. 55%）。
*   **关键组件有效**：
    1.  语义图状态为规划提供了有效信息。
    2.  代码规划器是实现高效、可靠进度估计的关键。
    3.  “无干扰视觉提示”对在杂乱场景中实现鲁棒的动作推理至关重要（移除后成功率从85%骤降至40%）。

#### **7. 优点**
*   **创新性整合**：创造性地将**持久化语义图**（结构化记忆）、**程序化规划**（高效推理）和**感知提示**（抗干扰）三个模块有机结合，系统性地解决了非马尔可夫长视程任务中的多个痛点。
*   **效率与性能兼顾**：通过“一次性代码合成 + 轻量图查询”的范式，在保持高性能的同时，**极大降低了在线规划的延迟**，使其更适用于实时机器人控制。
*   **解耦与泛化潜力**：框架模块化程度高，语义图构建、规划器、执行器可相对独立地改进或替换（例如使用更强大的基础模型）。
*   **实验设计扎实**：在真实机器人上进行评估，任务设计有代表性，消融分析清晰，结论令人信服。

#### **8. 不足与局限**
*   **依赖基础模型**：语义图初始化（物体识别、关联）和代码合成严重依赖VLM和LLM的性能。这些模型的错误（如错误识别属性、生成有bug的代码）会直接导致系统级失败。
*   **场景与关系简化**：当前语义图的关系（`in`, `on`, `near`）基于简化的几何规则归纳，可能无法处理更复杂的空间关系或物理交互（如“缠绕”、“部分支撑”）。
*   **任务范围有限**：实验仅在**桌面操作**领域进行，未验证在更复杂、动态或非结构化环境（如家庭厨房、户外）中的有效性。
*   **代码合成的可靠性**：依赖提示工程来确保LLM生成正确、鲁棒的程序逻辑。对于更复杂的任务，可能需要更复杂的代码验证或迭代修复机制。

（完）
