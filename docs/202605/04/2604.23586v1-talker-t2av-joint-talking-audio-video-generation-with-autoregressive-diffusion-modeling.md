---
title: "Talker-T2AV: Joint Talking Audio-Video Generation with Autoregressive Diffusion Modeling"
title_zh: Talker-T2AV：基于自回归扩散建模的联合说话音视频生成
authors: "Zhen Ye, Xu Tan, Aoxiong Yin, Hongzhan Lin, Guangyan Zhang, Peiwen Sun, Yiming Li, Chi-Min Chan, Wei Ye, Shikun Zhang, Wei Xue"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23586v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 基于自回归扩散模型的音视频联合生成
tldr: 现有音视频联合生成模型通过全局注意力完全耦合模态，导致说话头合成中语义关联与低层渲染过程不必要的纠缠。本文提出Talker-T2AV自回归扩散框架，使用共享主干进行高层跨模态建模，配合模态特定解码器进行低层细化。该方法在统一patch级token空间进行联合推理，通过轻量级扩散Transformer头生成帧级音视频潜在表示。实验表明其在唇部同步、视频质量和音频质量上优于基线，实现了更强的跨模态一致性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23586v1/fig-001.webp\", \"caption\": \"Table 1. Comparison with joint text-to-audio-video generation methods on both test sets. Methods above the dashed line target general audio-video generation, while UniAVGen and our method focus on facial talking-head video generation. Best results are in bold, second best are underlined.\", \"page\": 7, \"index\": 1, \"width\": 1017, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2604.23586v1/fig-002.webp\", \"caption\": \"Figure 1. Overview of TALKER-T2AV. Top: The autoregressive backbone processes a unified causal sequence in which the full text token sequence appears first as a prefix, followed by joint audio-video patches where audio and video patch embeddings are element-wise summed at each position. The backbone hidden states are then decoded by two modality-specific diffusion transformer heads into audio waveforms and portrait video, respectively. Bottom right: Each diffusion transformer head takes the hidden state hi, a global embedding, and a context window of history frames as conditioning, and denoises the current frame via flow matching. Bottom left: The patch transformer encoder compresses N audio/video frames into a single patch token, reducing sequence length for efficient autoregressive modeling.\", \"page\": 4, \"index\": 2, \"width\": 954, \"height\": 742}]"
motivation: 现有音视频联合生成模型在去噪过程中完全耦合所有模态，导致不必要的纠缠和效率低下，特别在说话头合成任务中高层语义关联与低层渲染过程需要区别处理。
method: 采用共享主干网络进行高层跨模态建模，配合轻量级模态特定扩散Transformer头进行低层细化，在统一patch级token空间中进行自回归推理。
result: 在说话人像基准测试中，Talker-T2AV在唇部同步准确性、视频质量和音频质量上均优于双分支基线方法，实现了比级联管道更强的跨模态一致性。
conclusion: 该研究提出的解耦式自回归扩散框架在音视频联合生成任务中表现出优越性，为多模态生成提供了更高效的架构思路。
---

## 摘要
联合音视频生成模型表明，统一生成比级联方法能产生更强的跨模态一致性。然而，现有模型通过广泛的注意力机制在整个去噪过程中耦合模态，以完全纠缠的方式处理高层语义和低层细节。这对于说话头合成而言并非最优：虽然音频和面部运动在语义上相关，但它们的低层实现（声学信号和视觉纹理）遵循不同的渲染过程。在所有层级强制联合建模会导致不必要的纠缠并降低效率。我们提出了Talker-T2AV，一种自回归扩散框架，其中高层跨模态建模在共享骨干中进行，而低层细化则使用模态特定的解码器。一个共享的自回归语言模型在统一的补丁级标记空间中共同推理音频和视频。两个轻量级扩散Transformer头将隐藏状态解码为帧级音频和视频潜在表示。在说话人像基准测试上的实验表明，Talker-T2AV在唇形同步准确性、视频质量和音频质量方面优于双分支基线，实现了比级联流水线更强的跨模态一致性。

## Abstract
Joint audio-video generation models have shown that unified generation yields stronger cross-modal coherence than cascaded approaches. However, existing models couple modalities throughout denoising via pervasive attention, treating high-level semantics and low-level details in a fully entangled manner. This is suboptimal for talking head synthesis: while audio and facial motion are semantically correlated, their low-level realizations (acoustic signals and visual textures) follow distinct rendering processes. Enforcing joint modeling across all levels causes unnecessary entanglement and reduces efficiency. We propose Talker-T2AV, an autoregressive diffusion framework where high-level cross-modal modeling occurs in a shared backbone, while low-level refinement uses modality-specific decoders. A shared autoregressive language model jointly reasons over audio and video in a unified patch-level token space. Two lightweight diffusion transformer heads decode the hidden states into frame-level audio and video latents. Experiments on talking portrait benchmarks show Talker-T2AV outperforms dual-branch baselines in lip-sync accuracy, video quality, and audio quality, achieving stronger cross-modal consistency than cascaded pipelines.

---

## 论文详细总结（自动生成）

好的，我将基于您提供的论文内容，按照您要求的要点生成一份详细的结构化中文总结。

### 1. 核心问题与整体含义
*   **研究背景**：联合音视频生成（如Sora、Veo等）已成为生成式AI的前沿。现有主流方法（如双分支扩散Transformer，dual-DiT）通过在整个去噪过程中使用双向跨模态注意力，将音频和视频完全耦合。
*   **核心问题**：作者认为这种**均匀的、全过程的耦合对于说话头（Talking Head）合成是次优的**。因为：
    1.  **高层语义与低层渲染的混淆**：音频（语义、时序）与面部运动高度相关，但声学波形和视觉纹理的低层渲染过程本质不同，持续跨模态注意力无益且降低效率。
    2.  **固定长度限制**：传统扩散模型需预设输出长度，当文本过长时会导致内容压缩或截断，损害语音清晰度，且无法适应不同的语速。

### 2. 方法论
*   **核心思想**：**将联合生成解耦为两个阶段**：1) 在共享自回归主干中进行**高层跨模态语义与时序规划**；2) 在独立的模态特定扩散头中进行**低层信号渲染**。
*   **关键技术细节**：
    1.  **两阶段架构**：
        *   **阶段一（跨模态建模）**：一个共享的因果语言模型（如Qwen3-0.6B）作为自回归主干。输入序列以文本token为前缀，后接一系列“联合补丁token”，其中每个token是**音频和视频补丁嵌入的逐元素和（Element-wise Sum）**。主干输出共享的隐藏状态 `hi`，编码了高层跨模态信息。
        *   **阶段二（模态特定细化）**：两个独立的轻量级扩散Transformer头（DiT），分别以 `hi` 为条件，通过流匹配（Flow Matching）目标，解码生成对应的音频和视频潜在帧补丁。
    2.  **时序对齐的潜在表示**：使用**WhisperX-VAE**（25 Hz, 32维连续向量）编码音频，使用**LIA-X**（25 Hz, 40维运动码）编码视频，确保两者帧率对齐，无需学习时间对齐模块。
    3.  **补丁编码器**：将P个连续帧压缩为一个补丁token，大幅缩短自回归序列长度，提升效率。
    4.  **停止预测器**：一个MLP根据隐藏状态预测生成是否结束，支持可变长度输出。
    5.  **灵活的多任务能力**：得益于“逐元素和”设计，在推理时，若一个模态（如音频）作为条件给定，则直接使用其真实嵌入与预测的另一个模态嵌入相加，使同一模型无需改动即可支持**文本到音视频（T2AV）、音频驱动视频（A2V）、视频配音（V2A）** 三种任务。
    6.  **多任务训练**：混合使用音视频对数据和纯TTS数据训练，提升模型对文本-发音映射的学习能力，从而改善语音清晰度和唇部同步。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：约100万条带字幕的说话人头像音视频剪辑（T2AV任务），以及Emilia TTS数据集。
    *   **测试集**：
        *   **中文**：从DH-FaceVid-1K采样200个视频。
        *   **英文**：从HDTF和Hallo3采样200个视频。
        *   **视频配音基准**：Chem数据集。
*   **评估指标**：
    *   **音频质量**：字符错误率（CER）/词错误率（WER）、UTMOS（自然度）。
    *   **视频质量**：FID（帧）、FVD（视频）。
    *   **音视频同步**：SyncNet置信度（C，越高越好）和距离（D，越低越好）。
    *   **配音质量**：时长距离（DD）、情感相似度（EMO-SIM）、WER、UTMOS。
*   **对比方法**：
    *   **联合音视频生成**：MoVA, Ovi, LTX-2, UniVerse-1（通用生成），以及专门面向说话人的UniAVGen（主要基线）。
    *   **音频驱动视频生成**：FLOAT, EchoMimic, Sonic, Ditto, AniPortrait。
    *   **视频配音**：Speak2Dub, StyleDubber, DeepDubber, ProDubber, InstructDub。

### 4. 资源与算力
*   论文中**未明确说明**训练所使用的具体GPU型号、数量及总训练时长。
*   仅提及的训练配置：使用AdamW优化器，以bfloat16混合精度训练20万步，全局批量大小为256。

### 5. 实验数量与充分性
*   **实验组数**：实验设计较为全面，主要包括：
    1.  **主实验**：在中文和英文测试集上与5种联合生成方法进行对比。
    2.  **扩展任务实验**：在音频驱动视频（对比5种方法）和视频配音（对比5种方法）任务上评估模型灵活性。
    3.  **消融实验**：重点分析了自回归序列中不同token排列方式（相加、交错、延迟）对性能的影响，验证了核心设计“逐元素和”在联合生成任务中的优越性，并揭示了最优排列方式依赖于任务（如音频驱动任务中延迟反而更好）。
*   **充分性与客观性**：
    *   **充分性**：实验覆盖了模型宣称的三大核心能力（T2AV, A2V, V2A），并进行了关键的消融研究，实验数量充足。
    *   **客观性与公平性**：使用了领域内公认的评估指标和基准数据集。在对比实验中，为公平起见，音频驱动任务均使用相同的真实音频作为输入，排除了上游TTS质量的影响。与专用系统对比也体现了其通用性优势。

### 6. 主要结论与发现
*   **性能领先**：在**联合音视频生成**任务上，Talker-T2AV在语音清晰度（CER/WER最低）、视频质量（FVD最优）和唇部同步（SyncNet C最高/D最低）上全面优于所有双分支扩散Transformer基线。
*   **灵活性验证**：在未经过任何架构修改或微调的情况下，该单一模型在**音频驱动视频**和**视频配音**任务上，其性能与专用的先进系统相当或更优。
*   **核心假设证实**：解耦高层规划与低层渲染的设计是有效的。消融实验证明，对于联合生成，**音频和视频token在时间上对齐（通过相加）是最佳策略**。

### 7. 优点
*   **创新架构**：提出了“**高层共享规划 + 低层独立渲染**”的解耦式设计，清晰地区分了跨模态对齐与模态内渲染，理论依据扎实。
*   **灵活统一**：通过简单的“**逐元素和**”融合机制，一个模型优雅地支持三种生成任务，简化了系统部署。
*   **可变长度**：自回归框架配合停止预测器，自然支持可变时长输出，克服了固定长度扩散模型的固有缺陷。
*   **训练策略**：**多任务训练**（混合TTS数据）有效提升了语音质量和唇同步的准确性，是一种实用的数据利用策略。
*   **详实分析**：消融实验深入分析了不同模态融合方式，结论具有启发性，并明确了设计选择的任务依赖性。

### 8. 不足与局限
*   **自回归误差累积**：由于在连续潜在空间中进行自回归预测，错误会随生成长度增加而累积，可能导致生成长语句时质量下降。
*   **视频质量上限**：生成的视频质量受限于底层**LIA-X视频运动自编码器的表达能力**。采用更强大的视觉表示有望进一步提升画质。
*   **数据依赖**：虽然使用了百万级数据，但作者指出**继续扩大高质量音视频配对数据规模**预计能带来进一步增益。
*   **计算细节缺失**：未提供具体的硬件配置和训练成本估算，使得复现难度评估和算力需求不明确。
*   **长序列生成风险**：论文虽提及误差累积问题，但未展示在超长文本（如数分钟）上的生成效果和稳定性测试。

（完）
