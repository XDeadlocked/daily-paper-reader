---
title: "Talker-T2AV: Joint Talking Audio-Video Generation with Autoregressive Diffusion Modeling"
title_zh: Talker-T2AV：基于自回归扩散建模的联合说话音视频生成
authors: "Zhen Ye, Xu Tan, Aoxiong Yin, Hongzhan Lin, Guangyan Zhang, Peiwen Sun, Yiming Li, Chi-Min Chan, Wei Ye, Shikun Zhang, Wei Xue"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23586v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 结合自回归扩散模型的音视频联合生成
tldr: 现有音视频联合生成模型通过注意力机制完全耦合高低级信息，导致说话头合成任务中跨模态建模效率低下。Talker-T2AV提出自回归扩散框架，在共享主干中进行高级跨模态建模，使用模态特定解码器进行低级细化，通过统一token空间联合推理音频和视频。该方法在说话肖像基准测试中优于基线，实现了更强的跨模态一致性和生成质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23586v1/fig-001.webp\", \"caption\": \"Table 1. Comparison with joint text-to-audio-video generation methods on both test sets. Methods above the dashed line target general audio-video generation, while UniAVGen and our method focus on facial talking-head video generation. Best results are in bold, second best are underlined.\", \"page\": 7, \"index\": 1, \"width\": 1017, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2604.23586v1/fig-002.webp\", \"caption\": \"Figure 1. Overview of TALKER-T2AV. Top: The autoregressive backbone processes a unified causal sequence in which the full text token sequence appears first as a prefix, followed by joint audio-video patches where audio and video patch embeddings are element-wise summed at each position. The backbone hidden states are then decoded by two modality-specific diffusion transformer heads into audio waveforms and portrait video, respectively. Bottom right: Each diffusion transformer head takes the hidden state hi, a global embedding, and a context window of history frames as conditioning, and denoises the current frame via flow matching. Bottom left: The patch transformer encoder compresses N audio/video frames into a single patch token, reducing sequence length for efficient autoregressive modeling.\", \"page\": 4, \"index\": 2, \"width\": 954, \"height\": 742}]"
motivation: 现有音视频联合生成模型在去噪过程中完全耦合高低级信息，导致不必要的纠缠和效率低下，不适合说话头合成任务。
method: 提出自回归扩散框架，共享主干处理高级语义，模态特定解码器处理低级细节，使用统一token空间进行联合推理。
result: 在说话肖像基准测试中，Talker-T2AV在唇同步准确性、视频质量和音频质量上优于双分支基线，跨模态一致性更强。
conclusion: Talker-T2AV通过解耦高低级跨模态建模，在音视频联合生成中实现了更优的跨模态一致性与生成质量。
---

## 摘要
联合音视频生成模型表明，统一生成比级联方法能产生更强的跨模态一致性。然而，现有模型通过全局注意力在去噪过程中耦合各模态，以完全纠缠的方式处理高层语义和低层细节。这对于说话头合成而言并非最优：虽然音频与面部动作在语义上相关，但它们的低层实现（声学信号和视觉纹理）遵循不同的渲染过程。强制在所有层级进行联合建模会导致不必要的纠缠并降低效率。我们提出Talker-T2AV，一种自回归扩散框架，其中高层跨模态建模在共享骨干网络中进行，而低层细化则使用模态特定的解码器。一个共享的自回归语言模型在统一的补丁级标记空间中共同推理音频和视频。两个轻量级扩散Transformer头将隐藏状态解码为帧级音频和视频潜在表示。在说话人像基准测试上的实验表明，Talker-T2AV在唇形同步准确性、视频质量和音频质量上均优于双分支基线，实现了比级联流水线更强的跨模态一致性。

## Abstract
Joint audio-video generation models have shown that unified generation yields stronger cross-modal coherence than cascaded approaches. However, existing models couple modalities throughout denoising via pervasive attention, treating high-level semantics and low-level details in a fully entangled manner. This is suboptimal for talking head synthesis: while audio and facial motion are semantically correlated, their low-level realizations (acoustic signals and visual textures) follow distinct rendering processes. Enforcing joint modeling across all levels causes unnecessary entanglement and reduces efficiency. We propose Talker-T2AV, an autoregressive diffusion framework where high-level cross-modal modeling occurs in a shared backbone, while low-level refinement uses modality-specific decoders. A shared autoregressive language model jointly reasons over audio and video in a unified patch-level token space. Two lightweight diffusion transformer heads decode the hidden states into frame-level audio and video latents. Experiments on talking portrait benchmarks show Talker-T2AV outperforms dual-branch baselines in lip-sync accuracy, video quality, and audio quality, achieving stronger cross-modal consistency than cascaded pipelines.

---

## 论文详细总结（自动生成）

好的，我将基于您提供的论文内容，按照您要求的要点生成一份详细的结构化中文总结。

### **1. 核心问题与整体含义**
*   **研究背景**：联合音视频生成（如Sora、Veo等）是生成式AI的前沿。现有开源研究的主流范式是**双分支扩散Transformer**，它通过双向交叉注意力在**整个去噪过程**中耦合音频和视频。
*   **核心问题**：作者认为，这种**完全耦合高低层级信息**的建模方式对于“说话头”合成任务**并非最优**。因为：
    *   **高层**：音频（语音内容）与视频（面部动作）在**语义和时间层面**强相关，需要对齐。
    *   **低层**：声学波形和视觉纹理的**渲染过程截然不同**，持续进行跨模态注意力可能带来不必要的纠缠和效率损失。
*   **研究动机**：提出一种更优的设计原则——**将高层跨模态规划与低层模态特定渲染解耦**，并支持**可变长度输出**以适应不同语速和文本长度。

### **2. 方法论：Talker-T2AV**
*   **核心思想**：采用**两阶段、解耦的“自回归扩散”框架**。
    1.  **阶段一（高层建模）**：一个共享的**自回归骨干网络**，在统一的序列中进行跨模态的语义和时序规划。
    2.  **阶段二（低层渲染）**：两个独立的、**模态特定的扩散Transformer头**，分别将骨干网络的隐藏状态解码为高质量的音频和视频潜在表示。
*   **关键技术细节**：
    *   **时间对齐的潜在表示**：使用 **LIA-X**（视频）和 **WhisperX-VAE**（音频）编码器，将两者编码为**相同帧率（25Hz）的连续潜在向量**，实现帧级自然对齐，无需学习对齐模块。
    *   **元素求和融合**：在自回归序列中，将同一时间步的音频和视频补丁嵌入**直接相加**，形成一个联合令牌输入骨干网络。这种设计使模型能灵活支持**联合生成、音频驱动、视频配音**三种任务，无需修改架构或微调。
    *   **补丁Transformer编码器**：将连续P帧压缩为一个补丁令牌，大幅缩短自回归序列长度，提升效率。
    *   **停止预测器**：基于骨干网络隐藏状态预测生成是否结束，实现**可变长度生成**。
    *   **多任务训练**：混合使用**音视频对数据**和**纯音频（TTS）数据**进行训练，任务标签嵌入区分样本。这利用了大量TTS数据提升语音清晰度和骨干网络的文本-发音映射能力。

### **3. 实验设计**
*   **任务与数据集**：
    *   **主任务（文本到音视频， T2AV）**：在约**100万**个公开说话人头像剪辑（带字幕）的数据集上训练。
    *   **对比任务**：
        *   **音频驱动视频（A2V）**：使用相同测试集。
        *   **视频配音（Video Dubbing）**：在**Chem**基准数据集上评估。
*   **评估指标**：
    *   **音频质量**：字符错误率（CER）/词错误率（WER）、UTMOS（自然度）。
    *   **视频质量**：FID（帧）、FVD（视频）。
    *   **音视频同步**：SyncNet置信度（C，越高越好）和距离（D，越低越好）。
    *   **配音质量**：时长距离（DD）、情感相似度（EMO-SIM）、WER、UTMOS。
*   **对比方法**：
    *   **T2AV基线**：五个基于双分支扩散Transformer的模型，包括通用模型（MoVA, Ovi, LTX-2, UniVerse-1）和专注说话人的模型（UniAVGen）。
    *   **A2V基线**：五个专用音频驱动方法（FLOAT, EchoMimic, Sonic, Ditto, AniPortrait）。
    *   **配音基线**：五个专用配音系统（Speak2Dub, StyleDubber等）。

### **4. 资源与算力**
*   论文**未明确说明**训练所使用的具体GPU型号、数量及集群信息。
*   提到的训练配置包括：**全局批量大小256**，使用 **bfloat16混合精度**，**训练200,000步**，使用AdamW优化器。

### **5. 实验数量与充分性**
*   **实验组别**：
    1.  **主实验**：在**中、英文两个测试集**上与5个T2AV基线进行综合对比。
    2.  **扩展实验**：在A2V和视频配音两个任务上，分别与5个专用基线对比，展示模型通用性。
    3.  **消融研究**：系统对比了自回归序列中不同令牌排列策略（元素求和、交错、延迟）在T2AV和A2V任务下的性能影响。
*   **充分性与公平性**：
    *   **充分性**：实验覆盖了模型设计的三个核心应用方向，并进行了关键的消融实验，验证了设计选择（如元素求和）的合理性。评估指标全面。
    *   **公平性**：在对比实验中，使用了**相同的测试集和评估协议**。在A2V任务中，为所有模型提供**相同的真实音频**作为驱动，以隔离上游语音合成质量的影响，确保对比聚焦于视频生成模型本身。

### **6. 主要结论与发现**
*   **性能优势**：在**联合音视频生成**任务上，Talker-T2AV在**语音清晰度（CER/WER最低）、视频流畅度（FVD最佳）、唇形同步（SyncNet指标最佳）** 方面全面优于所有双分支扩散Transformer基线。
*   **通用性验证**：**无需任何修改或微调**，同一模型在**音频驱动视频**和**视频配音**任务上，其性能**匹配甚至超越**了专门的SOTA系统。
*   **关键洞见**（来自消融实验）：最优的令牌排列方式**依赖于任务**。对于联合生成，**时间对齐（元素求和）** 最佳；对于音频驱动生成，让视频令牌**适当延迟**以观察更多音频上下文则更优。这验证了其设计灵活性。

### **7. 优点**
*   **方法论创新**：提出了**“高层联合规划+低层独立渲染”** 的解耦思想，清晰合理，解决了现有方法在说话头任务中的固有缺陷。
*   **架构优雅灵活**：**元素求和融合**与共享骨干的设计，使单一模型能无缝支持三种生成任务，展现了强大的通用性和可扩展性。
*   **训练策略有效**：**多任务训练**（混合TTS数据）显著提升了语音清晰度和骨干网络的语义理解能力，是一种高效利用数据的方法。
*   **支持可变长度**：自回归框架配合停止预测器，**自然适应不同长度文本和语速**，克服了固定长度扩散模型的局限性。

### **8. 不足与局限**
*   **技术局限**：
    1.  **误差累积**：在连续潜在空间中进行自回归预测，**长序列生成时错误可能传播和累积**，导致质量逐渐下降。
    2.  **视频质量上限**：生成的视频质量受限于**预训练且冻结的LIA-X视频编码器**的能力。采用更强大的视觉表示有望进一步提升画质。
*   **实验与应用局限**：
    1.  **数据规模**：虽然使用了100万对数据，但作者指出**继续扩大高质量音视频对数据规模**有望带来进一步增益。
    2.  **场景单一**：工作专注于**说话人头像生成**，未验证其在更通用、复杂场景（如全身动作、多人物交互）下的有效性。
    3.  **算力细节缺失**：未提供具体的硬件成本和训练能耗信息，不利于完全复现或评估其效率。

（完）
