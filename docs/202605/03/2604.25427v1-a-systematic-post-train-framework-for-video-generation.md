---
title: A Systematic Post-Train Framework for Video Generation
title_zh: 视频生成的系统性后训练框架
authors: "Zeyue Xue, Siming Fu, Jie Huang, Shuai Lu, Haoran Li, Yijun Liu, Yuming Li, Xiaoxuan He, Mengzhao Chen, Haoyang Huang, Nan Duan, Ping Luo"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25427v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 使用RLHF和SFT的视频生成后训练框架
tldr: 针对大规模视频扩散模型在实际部署中存在的提示敏感、时序不一致和推理成本高的问题，本文提出了一个系统的后训练框架。该框架通过监督微调、基于人类反馈的强化学习、提示增强和推理优化四个协同阶段，系统性地将预训练模型与用户意图对齐，有效提升了生成视频的视觉质量、时序一致性和指令跟随能力，同时保持了预训练阶段学到的可控性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25427v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of our post-training framework for video generation. We organize the pipeline into four complementary stages to bridge pretrained models and practical deployment. In Phase 1, supervised fine-tuning (SFT) uses curated data to establish a stable instruction-following baseline. In Phase 2, RLHF via a GRPO-based trainer aligns the generator with multi-dimensional reward signals, improving aesthetics, motion quality, and text alignment. In Phase 3, Prompt Enhancement (PE) optimizes an LLM using the same reward loop to enrich user inputs for better robustness and visual quality. Finally, Phase 4 applies autoregressive distillation (AD) with a self-forcing objective to transfer these capabilities into a causal architecture, significantly boosting inference efficiency for real-world deployment.\", \"page\": 2, \"index\": 1, \"width\": 808, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.25427v1/fig-002.webp\", \"caption\": \"Figure 2: The visualization of RLHF on Wan-2.1.\", \"page\": 8, \"index\": 2, \"width\": 758, \"height\": 1167}]"
motivation: 现有大规模视频扩散模型存在提示敏感、时序不一致和推理成本高等问题，难以满足实际部署需求。
method: 提出一个包含监督微调、基于人类反馈的强化学习、提示增强和推理优化的四阶段后训练框架。
result: 实验表明，该框架能有效减少常见伪影，显著提升可控性和视觉质量，同时满足严格的采样成本约束。
conclusion: 该框架为视频生成模型的实际部署提供了一个稳定、可扩展且高效的解决方案。
---

## 摘要
尽管大规模视频扩散模型在生成高分辨率和语义丰富的内容方面展现出令人印象深刻的能力，但由于提示词敏感性、时间不一致性以及高昂推理成本等关键问题，其预训练性能与实际部署需求之间仍存在显著差距。为弥合这一差距，我们提出一个全面的后训练框架，通过四个协同阶段系统性地将预训练模型与用户意图对齐：首先采用监督微调（SFT）将基础模型转化为稳定的指令跟随策略；随后通过基于人类反馈的强化学习（RLHF）阶段，利用专为视频扩散定制的新型组相对策略优化（GRPO）方法，以提升感知质量和时间连贯性；接着，我们通过专门的语言模型进行提示增强以优化用户输入；最后通过推理优化解决系统效率问题。这些组件共同构成了一套系统性方法，可提升视觉质量、时间连贯性和指令跟随能力，同时保持预训练期间习得的可控性。该框架为构建稳定、适应性强且在实际部署中有效的可扩展后训练流程提供了实用蓝图。大量实验表明，这一统一流程能有效减少常见伪影，在严格遵守采样成本约束的同时显著提升可控性和视觉美感。

## Abstract
While large-scale video diffusion models have demonstrated impressive capabilities in generating high-resolution and semantically rich content, a significant gap remains between their pretraining performance and real-world deployment requirements due to critical issues such as prompt sensitivity, temporal inconsistency, and prohibitive inference costs. To bridge this gap, we propose a comprehensive post-training framework that systematically aligns pretrained models with user intentions through four synergistic stages: we first employ Supervised Fine-Tuning (SFT) to transform the base model into a stable instruction-following policy, followed by a Reinforcement Learning from Human Feedback (RLHF) stage that utilizes a novel Group Relative Policy Optimization (GRPO) method tailored for video diffusion to enhance perceptual quality and temporal coherence; subsequently, we integrate Prompt Enhancement via a specialized language model to refine user inputs, and finally address system efficiency through Inference Optimization. Together, these components provide a systematic approach to improving visual quality, temporal coherence, and instruction following, while preserving the controllability learned during pretraining. The result is a practical blueprint for building scalable post-training pipelines that are stable, adaptable, and effective in real-world deployment. Extensive experiments demonstrate that this unified pipeline effectively mitigates common artifacts and significantly improves controllability and visual aesthetics while adhering to strict sampling cost constraints.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，为您生成一份结构化的详细总结。

### **论文总结：A Systematic Post-Train Framework for Video Generation**

#### **1. 核心问题与整体含义**
*   **研究动机**：大规模视频扩散模型（如Sora、Pika等）在预训练后，其生成质量（如提示词敏感性、时序不一致、局部伪影）和推理效率仍难以满足**实际部署**的严格要求。预训练（追求似然最大）与部署（追求人类偏好对齐、高效率）之间存在显著差距。
*   **整体含义**：本文提出一个**系统性后训练框架**，旨在弥合上述差距。它不提出新的基础模型架构，而是专注于在预训练模型之上，通过一系列对齐与优化步骤，构建一个**稳定、可控、高效且高质量**的、可直接部署的视频生成管道。

#### **2. 方法论**
*   **核心思想**：将后训练流程系统化地组织为四个**协同、渐进**的阶段，分别解决指令跟随、生成质量、输入鲁棒性和推理效率问题。
*   **关键技术细节**：
    1.  **阶段一：监督微调**：使用高质量指令-视频对数据，将基础模型转化为一个**稳定的指令跟随策略**。其目的不是达到最优质量，而是消除严重错误，为后续强化学习提供一个可靠、结构良好的初始策略。
    2.  **阶段二：基于人类反馈的强化学习**：
        *   **核心算法**：采用**组相对策略优化（GRPO）**，这是一种专为扩散/流匹配模型设计的RL方法。它避免了不稳定的价值函数估计，通过在**组内进行相对比较**来计算优势函数。
        *   **视频生成适配**：提出了 **“等时分组”** 策略：为每个提示词组分配一个特定的时间步进行SDE采样（引入探索噪声），其余时间步使用确定性ODE采样（保证生成质量）。这平衡了探索成本与奖励信号可靠性。
        *   **多奖励平衡**：同时优化四个奖励模型：**视频美学**（整体视觉质量）、**图像美学**（单帧质量）、**运动质量**（时序连贯性）、**文本-视频对齐**（语义一致性）。论文强调需精心设计奖励权重，以避免单一目标主导。
    3.  **阶段三：提示增强**：**冻结**视频生成器，使用与阶段二**相同的多维度奖励信号**，训练一个大语言模型作为“提示词增强器”。它自动将用户的简短输入重写为更详细、更具视觉表现力的描述，从而从**输入侧**提升生成质量。
    4.  **阶段四：自回归蒸馏**：为了将经过上述阶段优化的、计算昂贵的双向扩散模型，转化为适合**实时流式生成**的高效因果（自回归）模型，采用三步蒸馏法：
        *   **分布匹配蒸馏**：将原模型蒸馏为步数更少的双向学生模型。
        *   **因果ODE回归**：引入因果注意力掩码，让学生模型适应仅基于历史帧进行去噪预测。
        *   **自强制蒸馏**：在训练时进行自回归展开（使用KV缓存），使模型学会在自身先前生成帧的条件下预测后续帧，解决训练-测试不一致问题。

#### **3. 实验设计**
*   **模型与数据**：基于一个**内部视频生成模型**（未公开具体架构）和内部构建的高质量文本-视频SFT数据集进行实验。
*   **评估基准**：采用 **“好-相同-差”（GSB）** 人工评估协议。评估三个维度：**视觉质量**、**运动质量**、**文本对齐**。
*   **对比方法**：论文**未进行广泛的横向对比**（如与其他视频生成模型比较）。实验主要是**框架自身的消融与效果验证**，即比较：
    *   应用RLHF（阶段二）前后的模型效果。
    *   加入提示增强（阶段三）后的进一步效果提升。

#### **4. 资源与算力**
*   论文**未明确说明**训练所使用的具体硬件配置（如GPU型号、数量）、训练数据量以及各阶段的训练时长。仅提及使用了“内部模型”和“内部数据集”。这对于评估方法的可复现性和计算成本是一个**信息缺口**。

#### **5. 实验数量与充分性**
*   **实验数量**：从论文描述看，实验规模**有限**。主要展示了框架整体流程的有效性，以及RLHF和提示增强两个核心阶段带来的定量（GSB分数提升）和定性（图2展示）改进。
*   **充分性与客观性**：
    *   **充分性不足**：缺乏与当前主流视频生成SOTA方法的直接对比，也缺少在不同公开基准（如VBench, EvalCrafter）上的测试。消融实验不够深入，例如未详细分析四个奖励模型的权重调整过程、GRPO中不同分组策略的影响等。
    *   **客观性**：采用人工评估（GSB）是评估生成质量的主观有效方式。但未说明评估者数量、筛选标准以及是否采用盲评，这可能导致评估存在一定主观偏差。奖励模型基于人类偏好数据训练，其本身也引入了偏好偏差。

#### **6. 主要结论与发现**
*   **有效性**：所提出的系统性后训练框架能显著提升视频生成模型的**实际部署性能**。
*   **量化结果**：RLHF阶段使模型的整体GSB评分提升了**31%**，其中视觉质量和运动质量提升尤为显著。后续加入提示增强器，带来了额外的**20%** 整体提升。
*   **关键发现**：
    1.  SFT为RLHF提供了至关重要的稳定基础策略。
    2.  GRPO结合多奖励模型能有效优化视频的感知质量与时间一致性。
    3.  **提示增强与生成器优化形成互补**，共同提升最终输出。
    4.  当前**文本对齐奖励模型的准确性是瓶颈**，限制了该维度性能的进一步提升。
    5.  自回归蒸馏能将优化后的能力高效迁移至因果架构，满足实时推理需求。

#### **7. 优点**
1.  **系统性设计**：首次为视频生成提出了一个完整、模块化的后训练蓝图，将指令对齐、质量优化、输入鲁棒性、推理效率四大挑战统一在一个框架内解决。
2.  **针对性的RLHF方法**：将GRPO适配到视频扩散模型，提出的“等时分组”和“时序梯度校正”等技术，有效应对了视频生成中rollout成本高、奖励稀疏的挑战。
3.  **奖励模型集成策略**：同时考虑视频级、帧级、运动、语义等多维度奖励，并意识到其平衡的重要性，设计更为精细。
4.  **提示增强的协同优化**：创新性地使用与生成器相同的奖励信号来训练提示增强器，使输入输出两侧优化目标一致，形成了闭环。
5.  **兼顾质量与效率**：通过自回归蒸馏，最终产出既高质量又高效的模型，明确了从研究到落地的完整路径。

#### **8. 不足与局限**
1.  **实验对比不足**：缺乏与现有视频生成后训练或对齐方法的直接、全面的对比实验，难以客观衡量其相对性能优势。
2.  **算力与数据不透明**：未公开训练所需的计算资源和数据细节，框架涉及SFT、RLHF、蒸馏等多个阶段，**复现成本可能极高**，不利于社区验证和推广。
3.  **文本对齐提升有限**：论文承认文本对齐奖励模型是当前瓶颈，这暴露了框架对底层奖励模型质量的依赖。若奖励模型有偏差，RLHF可能放大该偏差。
4.  **框架复杂度高**：四阶段管道需要顺序执行，且涉及多个组件（多个奖励模型、LLM增强器）的训练与调参，**工程复杂度和调试难度大**。
5.  **评估局限性**：主要依赖内部人工评估，缺乏在更广泛、公开的客观指标上的测试，结论的普适性有待进一步验证。

（完）
