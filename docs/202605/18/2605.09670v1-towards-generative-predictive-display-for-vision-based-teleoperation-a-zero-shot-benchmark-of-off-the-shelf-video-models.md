---
title: "Towards Generative Predictive Display for Vision-Based Teleoperation: A Zero-Shot Benchmark of Off-the-Shelf Video Models"
title_zh: 面向基于视觉遥操作的生成式预测显示：现成视频模型的零样本基准测试
authors: "Aws Khalil, Jaerock Kwon"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09670v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 用于机器人遥操作的现成视频模型零样本基准测试
tldr: 本文针对遥操作中通信延迟导致的控制性能下降问题，研究了生成式预测显示的可行性。通过构建零样本基准测试，评估了五种公开的生成视频模型在模拟驾驶数据上的性能。研究发现，现有模型在零样本设置下无法同时满足预测精度、误差稳定性和实时性要求，表明通用视频生成模型与遥操作预测显示的实际需求之间存在差距，需要针对性的监督训练或优化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09670v1/fig-001.webp\", \"caption\": \"Fig. 4. Qualitative comparison on a second driving clip with more dynamic scene elements. Predictions are shown at 512×320 to highlight spatial and temporal inconsistencies across models.\", \"page\": 8, \"index\": 1, \"width\": 1052, \"height\": 488}, {\"url\": \"assets/figures/arxiv/2605.09670v1/fig-002.webp\", \"caption\": \"Fig. 3. Qualitative comparison on a representative driving clip at 512× 320. Each row shows an 8-step rollout for a different model given the same conditioning frames.\", \"page\": 8, \"index\": 2, \"width\": 1052, \"height\": 485}, {\"url\": \"assets/figures/arxiv/2605.09670v1/fig-003.webp\", \"caption\": \"Fig. 1. Tradeoff between prediction accuracy (MAD) and inference time across models and resolutions. Each point corresponds to a model-resolution configuration.\", \"page\": 6, \"index\": 3, \"width\": 484, \"height\": 393}, {\"url\": \"assets/figures/arxiv/2605.09670v1/fig-004.webp\", \"caption\": \"TABLE I AGGREGATE PERFORMANCE ACROSS MODELS, REPORTING MEAN ABSOLUTE DIFFERENCE (MAD), INFERENCE TIME PER FRAME, AND PEAK GPU MEMORY USAGE ACROSS ALL EVALUATED CLIPS.\", \"page\": 6, \"index\": 4, \"width\": 1052, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2605.09670v1/fig-005.webp\", \"caption\": \"Fig. 2. Temporal rollout error across prediction steps.\", \"page\": 6, \"index\": 5, \"width\": 479, \"height\": 306}]"
motivation: 遥操作系统中通信延迟会降低态势感知和控制性能，而生成视频模型能否用于延迟敏感的预测显示尚不明确。
method: 研究构建了一个零样本基准测试，使用CARLA模拟器的驾驶数据，评估了五种公开的生成视频模型在两种分辨率和两种条件模式下的性能。
result: 基准测试显示，没有模型能同时实现低预测误差、非发散的单步误差行为以及实时推理，模型规模或分辨率的提升带来的改进有限甚至有时适得其反。
conclusion: 研究表明，当前通用生成视频模型在零样本设置下无法满足遥操作预测显示的低延迟、高精度要求，需针对性的监督训练或优化。
---

## 摘要
遥操作系统从根本上受到通信延迟的限制，这会降低态势感知和控制性能。预测显示旨在通过呈现当前视觉状态的估计而非延迟观测来缓解这一限制。尽管生成式视频模型的最新进展实现了高质量的视频合成，但它们对延迟敏感的预测显示的适用性仍不明确。本文提出了一个零样本基准测试，用于评估现成的生成式视频模型在短时域预测显示中的表现，无需针对特定任务进行微调。我们将问题表述为基于滚动的未来帧预测，并使用CARLA模拟器的模拟驾驶数据开发了一个统一的基准测试流程。评估了五个公开发布的视频模型，涵盖基于Transformer和基于扩散的模型系列，涉及两种分辨率和两种条件设置（多帧和单帧）。通过预测准确性（平均绝对差异）、每次滚动的延迟、峰值GPU内存使用量以及预测时域内的时间误差演变来评估性能。在此零样本基准测试中，没有测试模型能同时实现低滚动误差、非发散的单步误差行为以及源帧率的实时推理。增加模型规模或分辨率带来的改进有限，在某些情况下甚至适得其反。这些发现凸显了通用生成式视频合成与遥操作中预测显示要求之间的差距，表明实际部署需要明确的短时域时间监督、领域内适应或积极的推理优化，而非直接应用现成模型。代码、配置和定性结果已在项目页面发布：https://bimilab.github.io/paper-GenPD

## Abstract
Teleoperation systems are fundamentally limited by communication latency, which degrades situational awareness and control performance. Predictive display aims to mitigate this limitation by presenting an estimate of the current visual state rather than delayed observations. While recent advances in generative video models enable high-quality video synthesis, their suitability for latency-sensitive predictive display remains unclear. This paper presents a zero-shot benchmark of off-the-shelf generative video models for short-horizon predictive display, without task-specific fine-tuning. We formulate the problem as rollout-based future frame prediction and develop a unified benchmarking pipeline using simulated driving data from the CARLA simulator. Five publicly released video models spanning transformer-based and diffusion-based families are evaluated across two resolutions and two conditioning regimes (multi-frame and single-frame). Performance is assessed using prediction accuracy (mean absolute difference), per-rollout latency, peak GPU memory usage, and temporal error evolution across the prediction horizon. On this zero-shot benchmark, no tested model simultaneously achieves low rollout error, non-divergent per-step error behavior, and real-time inference at the source frame rate. Increasing model scale or resolution yields limited and, in some cases, inverted improvements. These findings highlight a gap between general-purpose generative video synthesis and the requirements of predictive display in teleoperation, suggesting that practical deployment will require either explicit short-horizon temporal supervision, in-domain adaptation, or aggressive inference optimization rather than direct application of off-the-shelf models. Code, configurations, and qualitative results are released on the project page: https://bimilab.github.io/paper-GenPD

---

## 论文详细总结（自动生成）

好的，我将基于您提供的论文内容，按照您要求的八个要点，生成一份详细的结构化中文总结。

---

### **1. 论文的核心问题与整体含义**
*   **研究动机**：在基于视觉的遥操作（如远程驾驶）中，通信延迟会导致操作员看到的画面滞后于真实环境，严重影响操作性能和安全性。传统的预测显示方法（如轨迹叠加）无法直接还原缺失的视觉状态。
*   **核心问题**：近年来，生成式视频模型（如扩散模型、Transformer）在视频合成上取得了显著进展。然而，这些为“视觉逼真度”和“长时域生成”设计的通用模型，是否能够满足**延迟敏感、要求高时间精度和实时性**的预测显示任务，尚不明确。
*   **整体含义**：本研究旨在填补这一认知空白，通过一个**零样本（zero-shot）基准测试**，系统评估现成的（off-the-shelf）生成视频模型在短时域预测显示任务上的直接可用性，从而揭示当前技术与实际应用需求之间的差距。

### **2. 论文提出的方法论**
*   **核心思想**：将预测显示问题形式化为一个**基于滚动的短时域未来帧预测**任务。给定过去若干帧观测，模型需要预测未来K帧，系统则根据当前延迟Δ，从预测序列中选取对应帧呈现给操作员。
*   **关键技术细节**：
    1.  **问题定义**：设当前时间为`t`，通信延迟为`Δ`帧，操作员收到的是`I_{t-Δ}`。预测模型`f`的目标是生成序列 `{Î_{t-Δ+1}, ..., Î_{t-Δ+K}}`，以逼近真实未来帧 `{I_{t-Δ+1}, ..., I_{t-Δ+K}}`。
    2.  **评估维度**：不仅评估整体预测精度，还强调**系统级约束**，包括：
        *   **预测准确性**：使用像素级平均绝对差异（MAD）。
        *   **时间误差行为**：分析误差随预测步长的演变（是否发散）。
        *   **实时性**：要求单帧推理时间 `T_inf` 小于帧间隔 `T_frame`，或整次滚动推理时间 `T_roll` 小于 `K * T_frame`。
        *   **资源消耗**：峰值GPU内存使用量。
*   **流程**：构建统一的评估流程，输入9帧条件帧，要求模型生成后续8帧，然后在相同硬件和协议下，对所有模型进行零样本推理和评估。

### **3. 实验设计**
*   **数据集/场景**：使用**CARLA模拟器**生成模拟驾驶数据。具体采用MILE框架和Roach智能体在Town01中采集，以15 FPS模拟城市慢速驾驶场景，包含动态交通参与者。
*   **Benchmark**：构建了一个**零样本、滚动式预测显示基准**。核心是比较模型在给定相同历史观测下，预测未来8帧的准确性、速度和稳定性。
*   **对比方法**：评估了**五个**公开发布的生成视频模型，涵盖不同架构和条件机制：
    *   **LTX-Video** (Transformer-based): LTX-2B (蒸馏版), LTX-13B。
    *   **Stable Video Diffusion (SVD)** (Diffusion-based): SVD 1.1 (图像到视频)。
    *   **Wan** (Diffusion-based): Wan VACE 1.3B (多帧条件), Wan I2V 1.3B (单帧图像到视频基线)。
*   **变量**：在两个分辨率（256×160, 512×320）和两种条件模式（多帧 vs. 单帧）下进行测试。

### **4. 资源与算力**
*   实验在单台工作站上进行，配置如下：
    *   **CPU**: AMD Ryzen Threadripper Pro 5975WX (32核/64线程)。
    *   **GPU**: **单块 NVIDIA RTX 6000 Ada (48GB VRAM)**。
*   **说明**：论文主要进行**推理评估**，而非模型训练。所有计时和内存测量均在此固定硬件配置下进行，以确保公平比较。未提及训练这些基础模型所需的算力。

### **5. 实验数量与充分性**
*   **实验规模**：对5个模型 × 2种分辨率 × 多个驾驶片段（具体数量未明确给出，但进行了多次采样以确保多样性）进行了评估。产生了定量（MAD、延迟、内存）和定性（预测帧可视化）结果。
*   **充分性与客观性**：
    *   **充分性**：实验覆盖了主流生成视频模型家族，考虑了分辨率、条件模式等关键变量，并从精度、速度、内存、时间误差演变等多个维度进行评估，设计较为全面。
    *   **客观性与公平性**：
        *   **硬件一致**：所有模型在相同GPU上评估。
        *   **协议统一**：使用相同的预热、计时和内存测量方法。
        *   **零样本设置**：所有模型均使用其公开发布的权重，**未进行任何任务特定的微调或优化**，这确保了评估的公平性，反映了模型的“开箱即用”性能。
    *   **局限性（论文自述）**：作者明确指出，由于条件模式（多帧/单帧）与模型架构耦合，跨家族比较（如LTX vs. SVD）是“架构+条件”的混合比较，更干净的架构对比仅限于家族内（如LTX-2B vs. LTX-13B）。此外，基准分辨率与模型原生训练分辨率不匹配，可能影响部分结果。

### **6. 论文的主要结论与发现**
1.  **无模型满足所有要求**：在零样本设置下，**没有任何**测试模型能同时实现**低滚动误差、非发散的单步误差行为、以及满足15 FPS的实时推理**。
2.  **精度-速度权衡**：存在明显的权衡。LTX系列误差最低但计算成本高；SVD速度最快但误差极高；Wan I2V处于中间位置。
3.  **模型规模并非越大越好**：在LTX家族中，较小的**LTX-2B（蒸馏版）在预测误差上反而优于更大的LTX-13B**。这表明，为长时域、高保真生成设计的模型缩放，不一定有益于短时域精确预测。
4.  **时间误差分析至关重要**：仅看聚合误差（如平均MAD）会掩盖重要问题。例如，SVD在低分辨率下误差随时间**下降**，但这被定性分析揭示为“**漂移到一个通用的、与场景无关的表征**”，而非预测质量提高。这强调了分析误差随时间演变的重要性。
5.  **分辨率提升效果有限**：提高分辨率对性能的影响是混合的，有时甚至有害（如SVD在高分辨率下误差剧增），不足以解决预测性能的核心局限。

### **7. 优点**
*   **问题定义清晰**：将预测显示明确为短时域、滚动式、受严格延迟约束的视觉预测问题，抓住了实际应用的核心挑战。
*   **基准构建系统**：建立了一个统一、可复现的零样本评估框架，涵盖了从问题形式化、数据准备、模型接口统一到多维度评估的完整流程。
*   **评估维度全面**：超越了单一的精度指标，综合考察了**预测精度、时间稳定性、推理延迟和内存消耗**，提供了系统级的可行性分析。
*   **洞察深刻**：发现了“更大模型不一定更好”、“聚合误差可能误导”等反直觉结论，对领域有重要启发。明确指出通用生成模型与专用预测任务之间存在**需求鸿沟**。

### **8. 不足与局限**
*   **评估范围有限**：
    *   **无经典基线**：未与简单的基线方法（如最后一帧持久化、基于光流/自我运动的帧扭曲）或轻量级专用预测模型对比，这使得“现成模型表现不佳”的结论缺少相对参照。
    *   **模型覆盖不全**：仅测试了5个公开模型，未涵盖所有最新的视频生成或世界模型。
    *   **仅限模拟数据**：所有实验均在CARLA模拟器中进行，结论向真实世界数据的泛化能力未经验证。
*   **评估指标单一**：主要使用像素级MAD，缺乏**感知质量指标**（如SSIM, LPIPS）和**任务相关指标**（如车道线一致性、目标检测精度），而这些对于遥操作的实际效用可能更重要。
*   **应用场景简化**：
    *   **开环评估**：仅评估了开环预测质量，未进行**闭环或人在环的遥操作实验**，无法验证预测显示对最终操作性能（如车道偏离、碰撞率）的实际影响。
    *   **未做任何优化**：严格坚持“零样本”，未尝试任何针对任务的微调、知识蒸馏、模型量化或推理优化（如TensorRT），因此结论是“直接应用不可行”，而非“此类模型潜力已尽”。
*   **实验细节模糊**：未报告确切的测试片段数量，也未进行模型间的配对统计检验，聚合结果的不确定性分析可以更严谨。

（完）
