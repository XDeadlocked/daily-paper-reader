---
title: "PanoWorld: Geometry-Consistent Panoramic Video World Modeling"
title_zh: PanoWorld：几何一致的全景视频世界建模
authors: "Le Jiang, Xiangyu Bai, Bishoy Galoaa, Shayda Moezzi, Caleb James Lee, Tooba Imtiaz, Edmund Yeh, Jennifer Dy, Yanzhi Wang, Sarah Ostadabbas"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15391v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 用于几何一致性视频生成的全景视频世界模型
tldr: PanoWorld提出了一种几何一致的全景视频世界模型，用于从单张图像和描述生成几何一致的360度视频。针对现有方法忽视3D场景状态约束导致几何不一致的问题，该研究将全景视频生成构建为几何和动态一致的潜在状态建模问题。基于预训练透视视频世界模型，引入了深度一致性损失和轨迹一致性损失两个轻量级正则化器，并采用球面几何感知的条件编码和位置编码。此外，还构建了包含一致深度、轨迹和提示标注的全景视频数据集PanoGeo用于训练和评估。实验表明，该方法在保持视觉真实感的同时显著提升了几何一致性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15391v1/fig-001.webp\", \"caption\": \"Table 1: Quantitative comparison across PanoGeo / Argus testset / Habitat-Sim (150 clips, balanced 50/50/50). We report distributional visual realism (FVD, FAED, FID), caption alignment (CLIP-T), and three correspondence-free geometric self-consistency scores computed on the predicted video alone via DAP depth and CoTracker3 3D tracks (3D-Smooth, Depth-σ, Tr-Life). Formal definitions and equations for all metrics are in Appendix B.2. Top – Stage 1: single perspective image + caption input. Bottom – Stage 2: single ERP first frame + caption (frame-0 anchored). Base models: 360DVD / Imagine360 / Follow-Your-Canvas use AnimateDiff, ARGUS uses SVD, OmniRoam uses WAN 2.1, and PanoWorld uses Cosmos 2.5. Best in bold, second underlined.\", \"page\": 8, \"index\": 1, \"width\": 829, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2605.15391v1/fig-002.webp\", \"caption\": \"Figure 3: Qualitative comparison on PanoGeo under Stage 1 input (single perspective image + caption). One representative ERP frame per method on six clips spanning the three evaluation regimes: PanoGeo (rows 1–2), Argus Testset (rows 3–4), and Habitat-Sim (rows 5–6). The red dashed box on each GT frame marks the perspective crop fed to all methods; the rest of the sphere is hallucinated. Compared methods: OmniRoam [Liu et al., 2026], Imagine360 [Tan et al., 2025], Follow-Your-Canvas (FYC) [Chen et al., 2025a], 360DVD [Wang et al., 2024], and ARGUS [Luo et al., 2025], all run under the input-parity protocol of Sec. 4.1. Stage 2 (ERP first-frame input) is omitted from this figure as a still frame cannot reveal its temporal-anchoring advantage; quantitative Stage 2 results are reported in Table 1.\", \"page\": 8, \"index\": 2, \"width\": 826, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2605.15391v1/fig-003.webp\", \"caption\": \"Figure 2: Training pipeline overview. A ground-truth panoramic video from PanoGeo (blue, left) is VAE-encoded into a clean latent z0 and mixed with noise ϵ to form znoise (green, top). Three conditioning signals (orange, center) are derived from a randomly sampled perspective crop: (1) spatial: the crop is projected onto the equirectangular canvas (P2E) and VAE-encoded into reference latent zref and mask mspatial, concatenated with znoise. (2) semantic: frozen SigLIP2 encodes the crop into per-frame features F, injected via cross-attention. (3) text: caption features ctext, also via cross-attention. The Pano Diffusion Transformer (purple, right) processes zinput=[znoise; zref;mspatial] through N DiT blocks with ERP-aware RoPE and predicts velocity vθ, from which ẑ0 is recovered. Three losses supervise ẑ0: Lvisual (rectified-flow target), Ldepth (Eq. 4, depth head vs. Depth Any Panoramas (DAP) pseudo-labels [Lin et al., 2025]), and Ltrack (Eq. 6, 3D point consistency of CoTracker3 [Karaev et al., 2024] tracks lifted via the predicted depth). All pseudo-labels (blue, bottom left) are precomputed offline at zero training-time cost. See Sec. 3 for details.\", \"page\": 4, \"index\": 3, \"width\": 829, \"height\": 469}, {\"url\": \"assets/figures/arxiv/2605.15391v1/fig-004.webp\", \"caption\": \"Table 3: Per-source Stage 1 breakdown. Same metric panel as Table 1; each source contains 50 clips of the balanced subset. Best per (source, metric) in bold, second underlined.\", \"page\": 16, \"index\": 4, \"width\": 829, \"height\": 423}]"
motivation: 现有全景视频方法主要优化视觉真实感，未显式约束底层3D场景状态，导致输出存在深度不一致、对应关系断裂和运动不合理等问题。
method: 在预训练透视视频世界模型基础上，引入深度一致性损失和轨迹一致性损失两个轻量级正则化器，并应用球面几何感知的条件编码和位置编码。
result: 实验表明，PanoWorld在保持视觉真实感竞争力的同时，相比先前全景生成方法提高了几何一致性。
conclusion: 研究表明，全景视频生成必须作为几何建模问题来处理，才能满足具身AI应用对整体空间理解的需求。
---

## 摘要
我们提出了PanoWorld，一个全景视频世界模型，能够从单张图像和一段描述生成几何一致的全景视频。现有的全景视频方法主要优化视觉真实感，并未对底层三维场景状态施加明确约束，导致生成的视频虽然看起来合理，但在球面空间上存在深度不一致、对应关系断裂以及运动不自然等问题。为解决这一不足，我们将全景视频生成构建为一个几何与动态一致的潜在状态建模问题，而非纯粹的视觉合成。基于一个预训练的透视视频世界模型，我们引入了两个轻量级正则化器：一个是针对伪真实全景深度的深度一致性损失，另一个是监督跟踪点随时间变化的三维世界坐标系位置的轨迹一致性损失。我们进一步对条件编码和位置编码应用了球面几何感知的自适应调整。此外，我们还引入了PanoGeo，一个统一的几何感知全景视频数据集，包含来自多样真实与合成来源的一致深度、轨迹和提示标注，用于训练和分层评估。实验表明，PanoWorld在几何一致性方面优于先前的全景生成方法，同时保持了具有竞争力的视觉真实感，这证实了全景视频生成必须被视为一个几何建模问题，以支持具身AI应用对整体空间理解的需求。代码可在https://github.com/ostadabbas/PanoWorld获取。

## Abstract
We present PanoWorld, a panoramic video world model that generates geometry-consistent 360$\degree$ video from a single image and a caption. Existing panoramic video methods optimize primarily for visual realism and do not explicitly constrain the underlying 3D scene state, producing outputs that appear plausible yet exhibit inconsistent depth, broken correspondences, and implausible motion across the spherical surface. We address this gap by framing panoramic video generation as a geometry- and dynamics-consistent latent state modeling problem rather than pure visual synthesis. Building on a pre-trained perspective video world model, we introduce two lightweight regularizers: a depth consistency loss against pseudo ground-truth panoramic depth, and a trajectory consistency loss that supervises the 3D world-frame positions of tracked points across time. We further apply spherical-geometry-aware adaptation to the conditioning and positional encoding. We additionally introduce PanoGeo, a unified geometry-aware panoramic video dataset with consistent depth, trajectory, and prompt annotations across diverse real and synthetic sources, used for both training and stratified evaluation. Experiments show that PanoWorld improves geometric consistency over prior panoramic generation methods while maintaining competitive visual realism, establishing that panoramic video generation must be treated as a geometric modeling problem to support the holistic spatial understanding requirements of embodied AI applications. Code is available at https://github.com/ostadabbas/PanoWorld.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容，按照要求生成的结构化、深入、客观的中文总结。

---

### **1. 核心问题与整体含义**
*   **研究动机**：现有的全景视频生成方法（如360DVD、ARGUS等）主要优化**视觉真实感**（如FID、FVD指标），但缺乏对底层3D场景状态的显式约束。这导致生成的360°视频在**几何上不一致**，表现为深度漂移、跨帧对应点断裂、物体运动轨迹不合理。这种“视觉合理但几何混乱”的输出，无法满足**具身AI**（如机器人导航、沉浸式遥操作）等下游应用对**整体、一致的空间理解**的需求。
*   **核心主张**：论文认为，全景视频生成不应仅仅是像素级的视觉合成，而应被视为一个**几何与动态一致的潜在世界状态建模问题**。模型需要学习一个结构化的、在时间和视角上保持一致的内部场景表示。

### **2. 方法论**
*   **核心思想**：在一个强大的预训练透视视频世界模型（Cosmos Predict 2.5）基础上，通过**轻量级的几何正则化**和**球面几何感知的模型适配**，将其能力迁移到全景（Equirectangular Projection, ERP）领域，实现从单张透视图像到几何一致全景视频的生成。
*   **关键技术细节**：
    1.  **球面几何适配**：
        *   **纬度感知的RoPE**：修改旋转位置编码，使水平位置与经度线性相关，垂直位置与正弦纬度相关，以匹配ERP投影中像素与球面角度的非线性关系。
        *   **球形面积加权**：在像素空间损失（深度、轨迹）中引入 `cos(纬度)` 权重，使梯度贡献与像素所代表的球面面积成正比。
        *   **水平循环移位增强**：训练时随机滚动ERP图像，强制模型学习球面的连续性，避免边界伪影。
    2.  **多路径条件生成**：从输入的透视视频序列中提取三种条件信号，共同指导全景生成：
        *   **空间条件**：将透视帧投影到ERP画布上，编码为参考潜在特征和空间掩码，与噪声潜在拼接。
        *   **语义条件**：使用冻结的SigLIP2图像编码器提取每帧的密集空间特征，通过交叉注意力注入。
        *   **文本条件**：使用文本描述特征。
    3.  **几何感知训练目标**：在标准整流流视觉损失 (`L_visual`) 基础上，增加两个基于离线预计算伪标签的辅助损失：
        *   **深度一致性损失 (`L_depth`)**：通过一个轻量级深度头（~138K参数），监督预测的干净潜在对应的深度图，使其与DAP模型生成的伪真实全景深度一致。包含L1项和边缘感知梯度项。
        *   **轨迹一致性损失 (`L_track`)**：利用CoTracker3提取的2D轨迹点，结合**预测的深度**将其反投影到3D世界坐标系，监督其3D位置、速度和加速度与伪真实3D轨迹的一致性。该损失将深度预测与时间点身份强耦合。
        *   **总损失**：`L = L_visual + λ_d * L_depth + λ_τ * L_track` (λ_d=0.3, λ_τ=0.06)。

### **3. 实验设计**
*   **数据集**：**PanoGeo**（本文贡献的数据集）。包含：
    *   **训练集**：>8K全景视频片段（自采集4K视频、x360、360DVD公开数据集）。
    *   **评估集**：150个片段，**分层平衡**来自三个来源：`PanoGeo`（域内留存）、`Argus Testset`（真实域外）、`Habitat-Sim`（合成域外）。
*   **评估基准与指标**：
    *   **视觉真实感与对齐**：FVD, FAED, FID, CLIP-T。
    *   **几何自一致性（核心）**：`3D-Smooth`（3D轨迹平滑度）、`Depth-σ`（深度时间标准差）、`Tr-Life`（轨迹生存率）。这些指标在**预测视频上独立计算**，不依赖与GT的对齐，公平衡量内部几何一致性。
*   **对比方法**：与当前主流全景视频生成方法对比，包括 **360DVD**, **Imagine360**, **ARGUS**, **Follow-Your-Canvas**, **OmniRoam**。实验设置了**两种公平的输入模式**：`Stage 1`（单张透视图+描述）和`Stage 2`（单张ERP首帧+描述）。

### **4. 资源与算力**
*   论文未明确说明训练使用的具体GPU集群规模和总训练时长。
*   文中提及训练分为两个阶段：`Stage 1`在256x512分辨率下训练6000次迭代，`Stage 2`在512x1024分辨率下训练2000次迭代，并提到`Stage 1`可在**单张H100 80GB GPU**上完成。这表明计算需求相对可控，主要依赖于预训练大模型。

### **5. 实验数量与充分性**
*   **实验设计全面且客观**：
    1.  **主实验**：在包含3种子集的150个片段上，对比了5个基线方法，报告了7个关键指标，并提供了`Stage 1`和`Stage 2`两种输入模式的结果。
    2.  **消融实验**：系统性地验证了两个核心损失`L_depth`和`L_track`的贡献，表明二者互补且共同作用时效果最佳。
    3.  **分源分析**：在附录中提供了三个数据子集（PanoGeo, Argus, Habitat-Sim）的详细结果，展示了模型在不同分布下的鲁棒性和优势领域。
    4.  **公平性**：采用了“输入对等协议”，确保所有对比方法接收相同的信息量；使用“无对应关系”的几何自洽指标，避免了因生成相机轨迹不同而带来的评估偏差。
*   **结论**：实验数量充足，设计严谨，评估指标能有效支撑论文的核心主张，对比公平。

### **6. 主要结论与发现**
*   PanoWorld在**几何自一致性指标**（3D-Smooth, Depth-σ, Tr-Life）上显著优于所有对比基线，部分指标提升达一个数量级。
*   在**视觉质量指标**（FVD, FID）上保持竞争力，甚至达到最优，证明几何正则化不会损害视觉真实感。
*   消融实验证实，`L_depth`和`L_track`损失对提升几何一致性至关重要，且二者相互促进。
*   **核心结论**：单纯优化视觉外观不足以产生几何可信的全景视频。必须将全景视频生成视为**几何建模问题**，对潜在状态施加明确的几何和动态约束，才能满足具身AI应用的需求。

### **7. 优点**
*   **问题定义新颖**：首次明确将“几何一致性”作为全景视频生成的核心评估维度，并系统性地解决了该问题。
*   **方法轻巧有效**：没有从头训练庞大模型，而是通过巧妙的几何正则化和适配，高效利用了现有视频世界模型的先验知识。
*   **损失设计巧妙**：`L_track`损失通过将2D轨迹与预测深度耦合，同时约束了深度准确性和时间点身份一致性，设计精妙。
*   **数据集与评估体系贡献**：构建的PanoGeo数据集和分层的评估协议，为未来全景视频几何一致性研究提供了有价值的基准。
*   **下游应用链**：展示了从单张透视图到可导航3D场景的完整流水线，凸显了其应用价值。

### **8. 不足与局限**
*   **生成长度固定**：模型仅能生成固定长度（93帧，约5.8秒）的视频，未探索更长的滚动生成，时间累积漂移问题有待研究。
*   **依赖伪标签**：深度和轨迹监督依赖于DAP和CoTracker3等外部模型生成的伪标签，这些伪标签本身的误差和偏差会传递到PanoWorld中。
*   **基础模型偏差**：其性能上限受限于预训练的透视视频世界模型（Cosmos）的偏见和能力。
*   **计算成本**：虽然训练相对高效，但两阶段推理流程和依赖大型预训练模型，整体推理成本仍高于一些轻量级基线。
*   **应用场景局限**：方法主要针对静态或刚体运动占主导的场景，对复杂非刚性变形（如剧烈的人物动作）的几何一致性处理能力未经验证。

（完）
