---
title: "SI-Diff: A Framework for Learning Search and High-Precision Insertion with a Force-Domain Diffusion Policy"
title_zh: SI-Diff：一种基于力域扩散策略的学习搜索与高精度插入框架
authors: "Yibo Liu, Stanko Oparnica, Simon Shewchun-Jakaitis, Guoyi Fu, Jie Wang, Jun Yang, Anand Jagannathan, Tony Hong-Yau Lo"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12247v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 用于机器人装配的力域扩散策略
tldr: 本研究针对机器人接触式装配任务中的位姿不确定性挑战，提出了SI-Diff框架，该框架通过力域扩散策略学习搜索和精密插入操作。主要贡献包括引入模式条件机制使单一模型能够处理不同行为模式，以及开发搜索教师策略生成多样化轨迹。实验证明该方法显著提高了装配任务的容错能力和零样本迁移性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12247v1/fig-001.webp\", \"caption\": \"TABLE I QUANTITATIVE COMPARISON WITH COMPETITORS ON NINE SHAPES.\", \"page\": 6, \"index\": 1, \"width\": 516, \"height\": 754}, {\"url\": \"assets/figures/arxiv/2605.12247v1/fig-002.webp\", \"caption\": \"Fig. 6. An example of the observations in our training data for search. The procedure of peg–hole alignment is highlighted with a light purple background, during which the coaxial force release and the EE’s movement in the z-direction are evident.\", \"page\": 6, \"index\": 2, \"width\": 500, \"height\": 387}, {\"url\": \"assets/figures/arxiv/2605.12247v1/fig-003.webp\", \"caption\": \"Fig. 7. An example of the F ff trajectories generated by our method during a successful peg-in-hole insertion of the pentagonal prism. Note that during the search phase, F ff shows a varying period and decreasing amplitude, unlike the teacher model.\", \"page\": 6, \"index\": 3, \"width\": 500, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2605.12247v1/fig-004.webp\", \"caption\": \"Fig. 1. Framework Overview. SI-Diff takes tactile and spatial observations, together with a mode prompt, as input and generates feedforward forces to drive an impedance controller in executing distinct action patterns for search and insertion. This is achieved by simultaneously learning from two teacher policies through the proposed mode conditioning mechanism. Although SI-Diff is trained on a single shape, it demonstrates zero-shot generalization to unseen shapes.\", \"page\": 2, \"index\": 4, \"width\": 1006, \"height\": 523}, {\"url\": \"assets/figures/arxiv/2605.12247v1/fig-005.webp\", \"caption\": \"Fig. 8. Heatmaps illustrating the distributions of success rates and task efficiency in tests with the cuboid peg: (a) TacDiffusion [1], (b) TeacherD, (c) Teacher-R, (d) FORGE [3], and (e) Ours.\", \"page\": 7, \"index\": 5, \"width\": 500, \"height\": 433}, {\"url\": \"assets/figures/arxiv/2605.12247v1/fig-006.webp\", \"caption\": \"Fig. 2. An illustration of the architecture of the proposed force-domain diffusion policy with mode conditioning.\", \"page\": 3, \"index\": 6, \"width\": 500, \"height\": 265}]"
motivation: 为了解决接触式装配任务中因位姿不确定性导致的搜索和精密插入任务需要分别处理的问题，需要一种能够统一处理这两类任务的智能装配系统。
method: 提出了一种基于力域扩散策略的SI-Diff框架，并引入了模式条件机制和搜索教师策略来生成多样化的轨迹。
result: 实验表明，SI-Diff将x-y方向的对准误差容限从2毫米提高到5毫米，并展示了强大的零样本迁移能力。
conclusion: SI-Diff框架通过单一模型实现了搜索和精密插入任务，显著提高了装配任务的容错能力和零样本迁移能力。
---

## 摘要
接触式装配是机器人学的基础，但由于相对位姿的不确定性（如对位偏差和小间隙），带来了重大挑战。现有方法通常将搜索和高精度插入分开处理，因为这两项任务涉及不同的动作模式。然而，对于智能装配系统而言，在单一模型中同时支持这两项任务而无需切换模型或权重是理想的目标。在本研究中，我们提出了SI-Diff框架，该框架通过力域扩散策略学习搜索和高精度插入。为此，我们引入了一种新的模式条件机制，使策略能够在单一框架下捕捉不同的动作行为。此外，我们开发了一种新的搜索教师策略，能够生成多样化的轨迹。通过在教师策略提供的成功且高效的演示上进行训练，模型学会了从触觉和末端执行器速度观测到有效动作行为的映射。我们进行了全面的实验，结果表明，与最先进的基线TacDiffusion相比，SI-Diff将对位偏差的容忍度从2毫米扩展到5毫米，同时展现出对未见形状的强大零样本迁移能力。

## Abstract
Contact-rich assembly is fundamental in robotics but poses significant challenges due to uncertainties in relative poses, such as misalignments and small clearances in peg-in-hole tasks. Existing approaches typically address search and high-precision insertion separately, because these tasks involve distinct action patterns. However, supporting both tasks within a single model, without switching models or weights, is desirable for intelligent assembly systems. In this work, we propose SI-Diff, a framework that learns both search and high-precision insertion through a force-domain diffusion policy. To this end, we introduce a new mode-conditioning mechanism that enables the policy to capture distinct action behaviors under a single framework. Moreover, we develop a new search teacher policy that can generate diverse trajectories. By training on successful and efficient demonstrations provided by the teacher policy, the model learns the mapping from tactile and end-effector velocity observations to effective action behaviors. We conduct thorough experiments to show that SI-Diff extends the tolerance to x-y misalignments from 2 mm to 5 mm compared to the state-of-the-art baseline, TacDiffusion, while also demonstrating strong zero-shot transferability to unseen shapes.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，为您生成一份结构化、深入且客观的总结。

### **论文总结：SI-Diff**

#### **1. 核心问题与整体含义**
*   **研究背景**：在机器人接触式装配（如轴孔装配）任务中，由于物体放置不精确，存在相对位姿不确定性（如初始位置偏差、装配间隙小）。这要求机器人具备两种能力：1) **搜索**：在平面上探索以找到孔并对准；2) **高精度插入**：在小间隙下将轴插入孔中，避免卡住。
*   **核心问题**：现有方法通常将**搜索**和**插入**作为两个独立的任务来处理，需要切换不同的策略或模型，增加了系统复杂性。虽然近期工作（如TacDiffusion）在插入任务上表现出色，但其对初始位置偏差的容忍度很低（仅2mm），限制了实际应用。
*   **研究目标**：提出一个**统一的框架**，能够像熟练工人一样，无需切换“知识模块”，即可根据情况自主执行搜索或插入动作，并显著提高对初始偏差的容忍度。

#### **2. 方法论**
*   **核心思想**：采用一个**力域扩散策略**，学习生成用于阻抗控制的**前馈力**指令。通过引入**模式条件机制**，使同一个扩散模型能够根据不同的“模式提示”（搜索/插入），生成对应任务所需的、行为模式截然不同的力指令序列。
*   **关键技术细节**：
    *   **统一表示**：模型输入为统一的触觉与运动观测（末端执行器所受外部/内部力/力矩、速度），输出为统一的前馈力指令。
    *   **模式条件机制**：受自然语言处理中词嵌入启发，将离散的“搜索”（0）和“插入”（1）模式索引，映射为可学习的高维嵌入向量。该模式嵌入作为条件信号，与观测数据一同输入到扩散模型中，指导其去噪过程，从而生成对应模式的动作。
    *   **搜索教师策略**：设计了一个基于规则的、引入随机化的搜索策略（算法1）。通过随机化螺旋搜索的参数（如方向、幅度、频率），它能生成**多样化**的搜索轨迹。仅保留其中成功且高效的轨迹作为演示数据供模型学习，使模型能学会根据实时触觉反馈调整搜索策略，而非机械重复固定路径。
    *   **插入教师策略**：直接采用先前工作（1kHz-BT）的策略，其通过监测残余力判断是否卡住，并触发“摆动”动作来脱困。
    *   **训练**：模型在**搜索**（300条）和**插入**（1500条）两种模式的演示数据上联合训练。为解决数据不平衡，采用了**平衡批次采样**。

#### **3. 实验设计**
*   **数据集/场景**：使用7自由度Franka Emika机器人及腕部力传感器收集数据。**训练仅使用一种形状**：长方体轴（间隙0.1mm）。
*   **测试基准与对比方法**：在**九种不同形状**的轴（包括训练用的长方体、以及六棱柱、五棱柱、三棱柱、圆柱体、USB-A接口等**未见形状**）上进行测试。初始偏差设置为2、3、4、5mm八个方向。
*   **对比方法**：
    1.  **TacDiffusion [1]**：最先进的力域扩散策略基线，但仅能处理插入，对初始偏差容忍度差。
    2.  **Teacher-D / Teacher-R**：将本文提出的搜索教师策略（固定参数/随机参数）与TacDiffusion串联的混合方法，作为增强版基线。
    3.  **FORGE [3]**：一种基于力引导强化学习的先进方法。

#### **4. 资源与算力**
*   文中明确提到：使用一张 **48GB NVIDIA RTX A6000 GPU** 进行训练和评估。
*   模型训练了 **2000个epoch**，批次大小为4096。

#### **5. 实验数量与充分性**
*   **实验数量充分**：对**6种不同几何形状**（部分形状测试了0.1mm和0.2mm两种间隙）进行了系统测试，每种形状在每个偏差条件下进行多次重复试验（总计96次/形状）。此外，还进行了详细的**消融实验**，验证了模式嵌入、平衡采样和教师策略随机性的关键作用。
*   **客观性与公平性**：
    *   **公平对比**：所有对比方法均使用**相同的阻抗控制器**，唯一区别是前馈力的生成方式。
    *   **评估指标全面**：不仅报告了**总体成功率**，还报告了**包含失败的平均时间**和**仅成功案例的平均时间**，避免了因成功率低而平均时间短的误导。
    *   **零样本迁移测试**：在多个未见形状上测试，有力地证明了方法的泛化能力。

#### **6. 主要结论与发现**
*   **性能提升**：SI-Diff在**所有测试形状上均取得了最佳或接近最佳的成功率**（普遍>90%）和**效率**（成功任务平均时间最短）。
*   **关键突破**：将初始位置偏差的**容忍度从2mm显著提升至5mm**，极大增强了方法的实用性。
*   **强大的零样本泛化能力**：仅使用**单一形状（长方体）** 训练，即可直接迁移到多种复杂未见形状上，且性能稳健。
*   **统一框架的有效性**：模式条件机制成功使单一模型学会了两种截然不同的行为模式，验证了统一框架的可行性。

#### **7. 优点**
*   **创新性设计**：“模式条件机制”巧妙地将任务模式作为可学习的条件嵌入，使单一模型具备多任务能力，架构简洁优雅。
*   **高效的教师策略**：搜索教师策略通过引入**随机性**来生成多样化、高质量的演示数据，是模型能够学习自适应策略而非简单模仿的关键。
*   **卓越的泛化性能**：仅需单一训练形状，即可实现强大的跨形状零样本迁移，降低了数据收集和模型训练成本，实用价值高。
*   **实验扎实全面**：对比基线选择合理，消融实验设计清晰，评估指标能反映真实性能，结论可信度高。

#### **8. 不足与局限**
*   **模式切换非学习获得**：当前框架中，从“搜索”切换到“插入”模式是由一个**固定的位移阈值（Z轴移动3mm）触发的**，而非由模型根据触觉感知自主决策。这限制了其在更复杂、非结构化场景中的应用。
*   **问题范围有限**：研究仅关注了**平面内（x-y）的位置偏差**，未考虑**平面外（如倾斜、偏航）的姿态误差**。在实际装配中，后者同样常见且具有挑战性。
*   **对教师策略的依赖**：模型的性能上限受限于教师策略提供的演示数据质量。虽然搜索教师策略引入了随机性，但其探索空间和效率可能仍有优化空间。
*   **实时性考虑**：论文提到模型推理频率（~100Hz）低于机器人控制频率（1kHz），需通过低通滤波器进行插值。虽然处理得当，但扩散模型的多步迭代采样本质上面临着与实时控制需求的平衡。

（完）
