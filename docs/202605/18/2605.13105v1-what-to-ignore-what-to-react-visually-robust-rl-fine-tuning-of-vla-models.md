---
title: "What to Ignore, What to React: Visually Robust RL Fine-Tuning of VLA Models"
title_zh: 忽略什么，响应什么：VLA模型的视觉鲁棒强化学习微调
authors: "Yuanfang Peng, Jingjing Fu, Chuheng Zhang, Li Zhao, Jiang Bian, Mingyu Liu, Ling Zhang, Jun Zhang, Rui Wang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13105v1"
tags: ["query:sr-0501"]
score: 10.0
evidence: 针对鲁棒机器人操作的VLA模型强化学习微调
tldr: 本文针对视觉语言动作模型在机器人操作中因视觉变化导致的性能下降问题，提出了PAIR-VLA强化学习微调框架。该方法通过在PPO优化中引入对视觉变体的不变性和敏感性辅助目标，将视觉变化转化为行为层面的指导，从而提升模型对视觉偏移的鲁棒性。实验表明，该方法在多种视觉变化下显著优于标准PPO，并具有良好的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13105v1/fig-001.webp\", \"caption\": \"Table 3: RL fine-tuning and auxiliary-objective hyperparameters for OpenVLA and π0.5. RL fine-tuning hyperparameters are shared by the PPO baseline and our method for each backbone; auxiliary-objective hyperparameters are used only by our method.\", \"page\": 15, \"index\": 1, \"width\": 506, \"height\": 584}, {\"url\": \"assets/figures/arxiv/2605.13105v1/fig-002.webp\", \"caption\": \"Table 1: OOD generalization of our method compared to PPO across four OOD scenarios on OpenVLA and π0.5. Values are success rates (%). The Clutter column reports the average over test settings with 2, 4, 6, and 8 distractors. ∆ Avg. is computed relative to the PPO baseline.\", \"page\": 7, \"index\": 2, \"width\": 690, \"height\": 190}, {\"url\": \"assets/figures/arxiv/2605.13105v1/fig-003.webp\", \"caption\": \"Figure 2: RL fine-tuning efficiency on Maniskill3 with OpenVLA. Success rate versus training step for our method and PPO on (a) an ID scenario and (b) an OOD clutter scenario. Solid lines show the mean over three seeds; shaded regions show the standard deviation. Our method converges substantially faster and reaches a higher plateau in both settings.\", \"page\": 8, \"index\": 3, \"width\": 817, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2605.13105v1/fig-004.webp\", \"caption\": \"Figure 3: OOD generalization under increasing clutter levels. Success rate with 2–8 distractors for (a) OpenVLA and (b) π0.5; half of the distractors in each setting are sampled from a held-out object set.\", \"page\": 9, \"index\": 4, \"width\": 818, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2605.13105v1/fig-005.webp\", \"caption\": \"Figure 1: Overview of the visually robust RL fine-tuning framework. At each environment step, the VLA policy receives an observation ot from the environment and samples an action at. Based on ot, the task-preserving view is constructed by removing distractors and changing background appearance, while the task-altering view is constructed by perturbing the pose of the target object. During policy training, the original view and paired variants are separately passed through the same VLA policy πθ to compute their action distributions. The invariance and sensitivity objectives are computed over these action distributions, and optimized as auxiliary objectives during PPO training.\", \"page\": 5, \"index\": 5, \"width\": 596, \"height\": 485}]"
motivation: 解决VLA模型在部署时因视觉变化导致的性能下降问题，现有任务奖励无法有效区分视觉变化是否影响任务。
method: 在PPO优化中引入两个辅助目标：对任务保持的视觉变体鼓励动作分布不变性，对任务改变的视觉变体鼓励动作分布可区分性。
result: "在多种视觉偏移下，方法相比标准PPO平均提升16.62%（π0.5）和9.10%（OpenVLA），并显示出良好的泛化性。"
conclusion: PAIR-VLA通过行为层面的视觉鲁棒性指导，显著提升了VLA模型在视觉变化下的泛化能力。
---

## 摘要
强化学习（RL）微调在机器人操作任务中为视觉-语言-动作（VLA）模型展现了潜力，但部署时的视觉偏移带来了实际挑战。一个关键难点在于：标准任务奖励虽能监督任务成功，却难以指导模型判断视觉变化是否与任务无关，或是否改变了操作所需的行为。为此，我们提出PAIR-VLA（面向视觉鲁棒VLA的配对动作不变性与敏感性），这是一个RL微调框架，通过在PPO优化过程中为配对的视觉变体添加两个辅助目标来解决这一难题：一个不变性项，用于减少任务保持配对（例如不同的干扰物）之间动作分布的差异；以及一个敏感性目标，鼓励任务改变配对（例如目标物体处于不同位姿）之间产生可区分的动作分布。这些目标共同将视觉变体从单纯的观测多样性转化为RL微调期间策略响应的行为级指导。我们在ManiSkill3环境中，针对两种代表性VLA架构（OpenVLA和$π_{0.5}$），在多种分布外视觉偏移（包括未见过的干扰物、纹理变化、目标物体位姿变化、视角偏移和光照变化）下进行评估。我们的方法相比标准PPO持续取得改进，在$π_{0.5}$上平均提升16.62%，在OpenVLA上平均提升9.10%。值得注意的是，消融实验进一步显示了方法在视觉偏移间的泛化能力：从干扰物和纹理变体学习到的不变性指导可迁移至目标位姿和光照偏移，而针对目标位姿变体添加敏感性指导则能进一步提升对干扰性偏移的鲁棒性，这凸显了行为级RL指导具有更广泛的迁移性。

## Abstract
Reinforcement learning (RL) fine-tuning has shown promise for Vision-Language-Action (VLA) models in robotic manipulation, but deployment-time visual shifts pose practical challenges. A key difficulty is that standard task rewards supervise task success, but offer limited guidance on whether a visual change is task-irrelevant or changes the behavior required for manipulation. We propose PAIR-VLA (Paired Action Invariance & Sensitivity for Visually Robust VLA), an RL fine-tuning framework to address this difficulty by adding two auxiliary objectives over paired visual variants during PPO optimization: an invariance term that reduces the discrepancy between action distributions for a task-preserving pair (e.g., different distractors), and a sensitivity objective that encourages separable action distributions for a task-altering pair (e.g., target object in a different pose). Together, these objectives turn visual variants from mere observation diversity into behavior-level guidance on policy responses during RL fine-tuning. We evaluate on ManiSkill3 across two representative VLA architectures, OpenVLA and $π_{0.5}$, under diverse out-of-distribution visual shifts including unseen distractors, texture changes, target object pose variation, viewpoint shifts, and lighting changes. Our method consistently improves over standard PPO, achieving average improvements of 16.62% on $π_{0.5}$ and 9.10% on OpenVLA. Notably, ablations further show generalization across visual shifts: invariance guidance learned from distractor and texture variants transfers to target-pose and lighting shifts, while adding sensitivity guidance on target-pose variants further improves robustness to nuisance shifts, highlighting the broader transferability of behavior-level RL guidance.

---

## 论文详细总结（自动生成）

### **论文总结：What to Ignore, What to React: Visually Robust RL Fine-Tuning of VLA Models**

#### **1. 核心问题与整体含义**
*   **研究动机**：视觉-语言-动作（VLA）模型在机器人操作任务中展现出潜力，但其在部署时对视觉环境变化（如光照、纹理、干扰物、目标位姿变化）非常敏感。标准的强化学习（RL）微调（如PPO）通过任务奖励优化策略，但**任务奖励本身无法告诉模型“哪些视觉变化应该被忽略，哪些变化需要做出行为调整”**。这导致模型在面对训练时未见的视觉偏移时，泛化能力不足。
*   **研究目标**：提出一种RL微调框架，使VLA策略能够学会区分**任务无关的视觉变化**（应保持行为不变）和**任务相关的视觉变化**（应调整行为），从而提升其在分布外（OOD）视觉场景下的鲁棒性。

#### **2. 方法论：PAIR-VLA框架**
*   **核心思想**：将视觉多样性从“观测层面的数据增强”提升为“行为层面的明确指导”。通过在RL训练中引入两个互补的辅助目标，直接塑造策略对不同类型视觉变化的响应。
*   **关键技术细节**：
    1.  **构建配对视觉变体**：
        *   **任务保持视图**：改变任务无关因素（如移除干扰物、替换背景纹理），但保持任务核心状态（如目标物体、机械臂位姿）不变。
        *   **任务改变视图**：改变任务相关因素（如扰动目标物体的位置和姿态），从而改变完成任务所需的行为。
    2.  **设计辅助目标函数**（在标准PPO目标上叠加）：
        *   **不变性目标**：最小化原始视图与**任务保持视图**之间策略动作分布的KL散度。鼓励模型对干扰性变化“视而不见”，行为保持一致。
        *   **敏感性目标**：最大化原始视图与**任务改变视图**之间策略动作分布的KL散度（并设置上限防止训练不稳定）。鼓励模型对关键状态变化做出不同的行为响应。
*   **流程**：在RL训练每一步，策略接收环境原始观测并采样动作。同时，利用模拟器信息（或分割模型）构建上述两种视觉变体。将三种视图输入**同一个策略网络**，得到三个动作分布，并计算不变性与敏感性损失，与PPO损失共同优化策略。

#### **3. 实验设计**
*   **基准环境**：ManiSkill3仿真器中的**拾取-放置**任务。
*   **评估场景（Benchmark）**：设计了四种OOD视觉偏移进行系统性测试：
    1.  **未见过的桌面纹理**。
    2.  **未见过的光照条件**。
    3.  **未见过的目标物体初始位姿**。
    4.  **未见过的干扰物数量与类型**（“杂乱”场景）。
*   **对比方法**：主要与**标准PPO微调**进行对比，作为强基线。
*   **模型**：在两个代表性VLA架构上验证：自回归模型 **OpenVLA** 和基于流匹配的模型 **π0.5**。

#### **4. 资源与算力**
*   文中在附录A.2节明确说明了算力需求。
*   **硬件**：每项实验使用 **8 块 H100 GPU**。
*   **训练时长**：
    *   微调 **OpenVLA** 约需 **1 天**。
    *   微调 **π0.5** 约需 **3 天**。
*   **备注**：训练使用了LoRA等参数高效微调技术以降低计算开销。

#### **5. 实验数量与充分性**
*   **主实验**：在两个模型（OpenVLA, π0.5）上，针对四种OOD场景，与PPO基线进行对比，结果以平均成功率呈现（表1）。
*   **消融实验**：系统地评估了不变性目标（`L_inv`）和敏感性目标（`L_sens`）各自的贡献（表2）。
*   **参数分析**：探究了不变性损失权重（`α`）对性能的影响（图4）。
*   **深入分析**：
    *   测试了在不同干扰物数量（2至8个）下的性能衰减曲线（图3）。
    *   额外测试了方法对**未见过的摄像机视角**的泛化能力（图5）。
*   **评价**：实验设计**较为充分和客观**。涵盖了多种具有代表性的视觉偏移，在两个不同架构的模型上验证了通用性，并进行了细致的消融和参数分析。对比基线选择合理（标准PPO），公平地使用了相同的训练步数、超参数（除辅助目标外）和计算资源。

#### **6. 主要结论与发现**
*   **性能提升**：PAIR-VLA显著提升了VLA模型在OOD视觉场景下的成功率。在π0.5上平均绝对提升 **16.62%**，在OpenVLA上提升 **9.10%**。
*   **训练效率**：相比标准PPO，PAIR-VLA能**更快地收敛**到更高的性能平台（图2）。
*   **泛化与迁移**：
    *   从**干扰物/纹理变化**中学习到的不变性知识，可以迁移到**光照变化**（尽管训练时未使用光照变体），显示出行为级指导的泛化能力。
    *   结合敏感性目标能进一步提升对干扰性变化的鲁棒性。
    *   方法也能有效泛化到**未见过的摄像机视角**。
*   **核心结论**：在RL微调中引入行为层面的视觉鲁棒性指导（即明确区分“忽略”与“响应”），比单纯增加观测多样性更有效地提升了VLA策略的视觉泛化能力。

#### **7. 优点**
*   **行为级指导**：创新性地将视觉变化与策略行为响应直接关联，提供了比数据增强或表征正则化更明确的优化信号。
*   **无推理开销**：所有辅助训练机制仅在**微调阶段**使用，部署的策略架构和推理成本与原始模型完全相同。
*   **架构通用**：方法不依赖于特定策略网络结构，成功应用于自回归和流匹配两种差异显著的VLA架构。
*   **高效性**：不仅提升了最终性能，还加速了RL微调的收敛过程。

#### **8. 不足与局限**
*   **仿真依赖**：所有实验均在**仿真环境（ManiSkill3）** 中进行，未在真实机器人上验证。仿真到现实的差距（Sim2Real Gap）仍是未知挑战。
*   **对分割掩码的依赖**：构建“任务保持视图”需要前景（机器人、目标物体、容器）的**精确分割掩码**。论文中依赖模拟器提供真值；在现实世界中，这需要依赖额外的分割模型（如SAM），其误差可能会影响视图构建质量和最终效果。
*   **任务类型有限**：实验仅聚焦于**单一类型的任务（拾取-放置）**。方法在更复杂的、需要多步骤推理或工具使用的操作任务上的有效性有待验证。
*   **视图构建的灵活性**：目前“任务改变视图”仅通过扰动目标物体位姿实现。对于其他类型的任务相关变化（如容器位置改变、物体形状变化等），需要设计新的视图构建方法。

（完）
