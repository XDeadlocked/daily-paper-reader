---
title: A Systematic Post-Train Framework for Video Generation
title_zh: 视频生成的系统性后训练框架
authors: "Zeyue Xue, Siming Fu, Jie Huang, Shuai Lu, Haoran Li, Yijun Liu, Yuming Li, Xiaoxuan He, Mengzhao Chen, Haoyang Huang, Nan Duan, Ping Luo"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25427v1"
tags: ["query:sr-0501"]
score: 10.0
evidence: 视频扩散模型的后训练框架与对齐
tldr: 本文针对大规模视频扩散模型在部署时存在的提示敏感性、时序不一致和推理成本高等问题，提出了一个系统的后训练框架。该框架通过监督微调、强化学习、提示增强和推理优化四个阶段，有效提升了生成视频的质量、时序一致性和指令跟随能力，同时控制了采样成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25427v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of our post-training framework for video generation. We organize the pipeline into four complementary stages to bridge pretrained models and practical deployment. In Phase 1, supervised fine-tuning (SFT) uses curated data to establish a stable instruction-following baseline. In Phase 2, RLHF via a GRPO-based trainer aligns the generator with multi-dimensional reward signals, improving aesthetics, motion quality, and text alignment. In Phase 3, Prompt Enhancement (PE) optimizes an LLM using the same reward loop to enrich user inputs for better robustness and visual quality. Finally, Phase 4 applies autoregressive distillation (AD) with a self-forcing objective to transfer these capabilities into a causal architecture, significantly boosting inference efficiency for real-world deployment.\", \"page\": 2, \"index\": 1, \"width\": 808, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.25427v1/fig-002.webp\", \"caption\": \"Figure 2: The visualization of RLHF on Wan-2.1.\", \"page\": 8, \"index\": 2, \"width\": 758, \"height\": 1167}]"
motivation: 解决预训练视频生成模型在提示敏感性、时序不一致和推理成本高等方面的问题。
method: 提出一个包含监督微调、强化学习、提示增强和推理优化的四阶段后训练框架。
result: 实验表明该框架能有效减少伪影，提升控制性和视觉质量。
conclusion: 该框架为视频生成模型的部署提供了一个稳定、可扩展且高效的解决方案。
---

## 摘要
尽管大规模视频扩散模型在生成高分辨率和语义丰富的内容方面展现出令人印象深刻的能力，但由于提示词敏感性、时间不一致性以及高昂推理成本等关键问题，其预训练性能与实际部署需求之间仍存在显著差距。为弥合这一差距，我们提出一个全面的后训练框架，通过四个协同阶段系统性地将预训练模型与用户意图对齐：首先采用监督微调（SFT）将基础模型转化为稳定的指令遵循策略；随后通过人类反馈强化学习（RLHF）阶段，利用专为视频扩散定制的新型组相对策略优化（GRPO）方法，以提升感知质量和时间连贯性；接着，我们通过专用语言模型进行提示增强以优化用户输入；最后通过推理优化解决系统效率问题。这些组件共同构成了一种系统化方法，可提升视觉质量、时间连贯性和指令遵循能力，同时保留预训练期间习得的可控性。该框架为构建稳定、适应性强且在实际部署中有效的可扩展后训练流程提供了实用蓝图。大量实验表明，这一统一流程能有效减少常见伪影，在严格遵守采样成本约束的同时，显著提升可控性和视觉美感。

## Abstract
While large-scale video diffusion models have demonstrated impressive capabilities in generating high-resolution and semantically rich content, a significant gap remains between their pretraining performance and real-world deployment requirements due to critical issues such as prompt sensitivity, temporal inconsistency, and prohibitive inference costs. To bridge this gap, we propose a comprehensive post-training framework that systematically aligns pretrained models with user intentions through four synergistic stages: we first employ Supervised Fine-Tuning (SFT) to transform the base model into a stable instruction-following policy, followed by a Reinforcement Learning from Human Feedback (RLHF) stage that utilizes a novel Group Relative Policy Optimization (GRPO) method tailored for video diffusion to enhance perceptual quality and temporal coherence; subsequently, we integrate Prompt Enhancement via a specialized language model to refine user inputs, and finally address system efficiency through Inference Optimization. Together, these components provide a systematic approach to improving visual quality, temporal coherence, and instruction following, while preserving the controllability learned during pretraining. The result is a practical blueprint for building scalable post-training pipelines that are stable, adaptable, and effective in real-world deployment. Extensive experiments demonstrate that this unified pipeline effectively mitigates common artifacts and significantly improves controllability and visual aesthetics while adhering to strict sampling cost constraints.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容，为您生成一份结构化、深入且客观的中文总结。

# 论文总结：《A Systematic Post-Train Framework for Video Generation》

## 1. 核心问题与整体含义
*   **研究动机**：大规模视频扩散模型（如Sora等）在预训练后，其生成能力与**实际部署要求**之间存在显著差距。主要问题包括：
    *   **提示词敏感性**：模型输出对输入提示的措辞变化非常敏感。
    *   **时序不一致性**：生成的视频在时间维度上可能出现抖动、物体突变等不连贯现象。
    *   **高昂推理成本**：扩散模型迭代采样导致生成速度慢，计算开销大。
    *   **可控性不足**：在遵循复杂指令和进行可控编辑方面表现不佳。
*   **整体含义**：本文提出了一种**系统性后训练框架**，旨在将预训练好的视频生成模型进行“对齐”与“优化”，使其更稳定、高质量、高效，从而满足实际应用需求。其核心思想是将指令跟随、质量提升、效率优化等目标整合到一个统一的四阶段流程中。

## 2. 方法论
论文提出了一个包含四个协同阶段的后训练框架：
1.  **监督微调**：使用高质量指令-视频对数据对预训练模型进行微调。其目的**不是**直接优化生成质量，而是建立一个**稳定、可靠的指令跟随基线策略**，为后续强化学习提供良好的起点和探索空间。
2.  **基于人类反馈的强化学习**：采用**组相对策略优化**方法对模型进行优化。
    *   **核心思想**：将视频扩散模型的去噪采样过程建模为马尔可夫决策过程。在稀疏奖励设定下（仅在生成完整视频后给出奖励），通过在同一提示下生成一组视频，并计算组内归一化的优势值来指导策略更新，避免了传统方法中价值函数估计不稳定的问题。
    *   **关键技术**：
        *   **混合ODE-SDE采样**：在大部分时间步使用确定性ODE采样以保证质量，在特定分配的时间步引入随机性SDE采样以进行探索。
        *   **多维度奖励模型**：同时优化四个奖励信号：视频美学、图像美学、运动质量和文本-视频对齐。需要精心设计奖励聚合策略以平衡不同目标。
3.  **提示增强**：使用与阶段2**相同的GRPO框架和奖励模型**，训练一个大型语言模型作为“提示词优化器”。该模型学习将用户的原始输入自动重写为更详细、更具表现力的提示，从而从**输入侧**提升生成视频的视觉质量和鲁棒性，与阶段2的**输出侧**优化形成互补。
4.  **自回归蒸馏**：将经过上述阶段优化后的、计算昂贵的双向扩散模型，蒸馏成一个高效的**因果自回归架构**模型，以实现实时或流式生成。
    *   **流程**：首先使用分布匹配蒸馏将原模型压缩为少步去噪的双向学生模型；然后进行因果ODE回归，使模型适应因果注意力掩码；最后采用“自强制”蒸馏，在训练时让模型基于自身之前生成的帧进行预测，以解决自回归模型训练与推理不一致的问题。

## 3. 实验设计
*   **模型与数据**：实验基于一个**内部视频生成模型**进行。使用了内部构建的高质量文本-视频数据集进行SFT，并为RLHF和蒸馏阶段策划了特定的提示词集。
*   **评估基准**：采用 **“好-相同-坏”** 人工评估协议。评估者从三个维度对生成视频进行比较：**视觉质量**、**运动质量**和**文本对齐**。
*   **对比方法**：论文**未进行**与外部其他后训练方法的直接对比实验。其主要目的是验证所提出框架**内部各个阶段的有效性**以及**整体流程的收益**。实验展示了加入RLHF阶段、以及进一步加入提示增强阶段后，模型在GSB指标上的提升。

## 4. 资源与算力
论文**未明确说明**训练所使用的具体硬件配置（如GPU型号、数量）、计算集群规模或总训练时长。仅提及使用了“内部视频生成模型”和训练了多个奖励模型。可以推断，训练如此大规模的视频扩散模型并进行多阶段RLHF，需要**相当庞大的计算资源**。

## 5. 实验数量与充分性
*   **实验数量**：实验主要围绕框架本身展开，包括：
        1.  **主实验**：展示了完整框架（SFT+RLHF+PE）在内部模型上的整体效果。
        2.  **阶段性验证**：分别报告了仅应用RLHF、以及结合PE后的性能提升。
        3.  **可视化分析**：提供了RLHF前后生成效果的对比案例（见图2）。
*   **充分性与客观性**：
    *   **优点**：实验设计清晰地验证了框架中每个核心组件（RLHF, PE）的贡献，并通过人工评估（GSB）这一相对可靠的指标进行衡量。
    *   **局限性**：
        1.  **缺乏外部对比**：未在公开基准（如VBench, EvalCrafter）上与同期其他后训练或SOTA视频生成模型进行对比，难以客观判断其绝对性能水平。
        2.  **内部评估**：所有实验均基于内部模型和数据集，结果的泛化能力有待验证。
        3.  **消融实验不足**：未对GRPO中的关键技术（如混合采样策略、奖励权重）进行充分的消融研究。

## 6. 主要结论与发现
1.  提出的四阶段后训练框架能有效提升视频生成模型的**视觉质量、运动连贯性和指令跟随能力**。
2.  **RLHF阶段**带来了显著的性能提升，在GSB总体指标上实现了**31%** 的改进，其中视觉和运动质量提升尤为明显。
3.  **提示增强阶段**能进一步带来**20%** 的总体性能提升，表明优化输入提示与优化生成模型同等重要。
4.  **文本对齐**方面的改进相对有限，作者将其归因于当前文本-视频对齐奖励模型的准确性不足，这成为了性能提升的瓶颈。
5.  通过自回归蒸馏，能够将优化后的模型能力迁移到高效架构中，为实际部署提供了可行性。

## 7. 优点
1.  **系统性与完整性**：首次为视频生成提出了一个覆盖“对齐-优化-加速”全链条的系统性后训练框架，思路清晰，阶段互补。
2.  **方法创新**：将GRPO方法成功适配并应用于视频扩散模型的RLHF，设计了混合采样和时序分组策略以平衡探索与质量。
3.  **协同设计**：提示增强与RLHF共享奖励模型的思路非常巧妙，实现了输入与输出端的联合优化。
4.  **面向部署**：不仅关注质量提升，还通过蒸馏阶段切实考虑了推理效率问题，使研究更具实用价值。

## 8. 不足与局限
1.  **实验验证局限**：最大的局限在于缺乏在公开数据集和基准上与现有方法的对比，削弱了结论的客观性和说服力。
2.  **依赖内部资源**：整个框架的验证严重依赖未公开的内部模型、数据和奖励模型，不利于复现和公平比较。
3.  **计算成本高昂**：多阶段训练（尤其是涉及大量采样rollout的RLHF）的算力需求未被量化，但无疑非常巨大，限制了其可及性。
4.  **文本对齐瓶颈**：作者承认文本-视频对齐奖励模型是当前框架的短板，限制了语义保真度的进一步提升。
5.  **框架复杂度**：四阶段流程涉及多种技术，工程实现复杂，调试和部署门槛较高。

（完）
