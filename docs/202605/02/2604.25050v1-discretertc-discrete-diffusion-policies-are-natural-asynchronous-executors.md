---
title: "DiscreteRTC: Discrete Diffusion Policies are Natural Asynchronous Executors"
title_zh: DiscreteRTC：离散扩散策略是天然的异步执行器
authors: "Pengcheng Wang, Kaiwen Hong, Chensheng Peng, Katherine Driggs-Campbell, Masayoshi Tomizuka, Chenfeng Xu, Chen Tang"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.25050v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 用于机器人学习异步执行的离散扩散策略
tldr: 本文针对物理AI在动态任务中需要异步执行（边行动边思考）的问题，指出现有基于流匹配的实时分块（RTC）方法存在结构缺陷，如需要额外微调、启发式引导和计算开销。为此，作者提出DiscreteRTC，利用离散扩散策略的迭代去掩码作为原生异步执行机制，无需额外调整即可实现高效修补。实验表明，该方法在动态模拟和真实世界操作任务中取得了更高的成功率，且推理效率更高。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25050v1/fig-001.webp\", \"caption\": \"Figure 4: Experimental Results in Kinetix. The throughputs represent the task completed by the policy every 256 steps. Left: Average solve rate and throughputs across all environments with different inference delays; Right: Solve rates for every tasks with different inference delays. The executions horizon follows s = max(1, d) and each datapoint represents 2048 trials.\", \"page\": 6, \"index\": 1, \"width\": 740, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2604.25050v1/fig-002.webp\", \"caption\": \"Table 2: Simulation experiment hyperparameters.\", \"page\": 15, \"index\": 2, \"width\": 768, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2604.25050v1/fig-003.webp\", \"caption\": \"Figure 5: Extend Experimental Results in Kinetix. Left: Required interative steps for each inpainting inference of different policy architectures in Kinetix with s = max(1, d); Right: Average solve rates of extended variants in Kinetix. The evaluation setup keeps the same with Figure 4.\", \"page\": 7, \"index\": 3, \"width\": 740, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2604.25050v1/fig-004.webp\", \"caption\": \"Figure 8: Fine-tuning Ablation in Kinetix. The evaluation setup keeps the same with Figure 4. The small plots represent the results per Kinetix environment\", \"page\": 14, \"index\": 4, \"width\": 825, \"height\": 369}, {\"url\": \"assets/figures/arxiv/2604.25050v1/fig-005.webp\", \"caption\": \"Figure 7: Detailed Main Results in Kinetix. The evaluation setup keeps the same with Figure 4. The small plots represent the results per Kinetix environment\", \"page\": 14, \"index\": 5, \"width\": 829, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2604.25050v1/fig-006.webp\", \"caption\": \"Figure 6: Unmasking Trajectory Sample with Natural Schedule Inference. Green blocks denote unmasked action tokens, yellow blocks denote masked tokens, and the red rectangle marks the earlystop boundary beyond which tokens do not need to be unmasked before the next inference.\", \"page\": 13, \"index\": 6, \"width\": 826, \"height\": 549}, {\"url\": \"assets/figures/arxiv/2604.25050v1/fig-007.webp\", \"caption\": \"Table 1: Real-world results on the two dynamic tasks. We report task success rate over 20 trials per task and average inference time (ms) on a single RTX 4090.\", \"page\": 8, \"index\": 7, \"width\": 682, \"height\": 182}, {\"url\": \"assets/figures/arxiv/2604.25050v1/fig-008.webp\", \"caption\": \"Figure 2: RTC with flow-matching head. Color represents the noise level, where green stands for the clear action and yellow stands for the pure noise. The flow-matching head is ill-suited for RTC because (a) during pre-training, the base policy is not trained on inpainting tasks; (b) to acquire this capability, a specially designed fine-tuning stage is required; (c) at inference time, RTC relies on heuristic guidance weight design; and (d) incurs extra cost for online correction computation.\", \"page\": 4, \"index\": 8, \"width\": 817, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2604.25050v1/fig-009.webp\", \"caption\": \"Table 3: Real-world experiment hyperparameters.\", \"page\": 17, \"index\": 9, \"width\": 822, \"height\": 627}, {\"url\": \"assets/figures/arxiv/2604.25050v1/fig-010.webp\", \"caption\": \"Figure 3: RTC with discrete diffusion head. Color represents the masking status, where green stands for the unmasked token and yellow stands for the masked token. The discrete diffusion head is naturally suited for RTC because (a) during pre-training, the base policy is already trained on inpainting tasks; (b) consequently, no inpainting-specific fine-tuning is required; (c) at inference time, early stopping from the previous inference leaves a natural guidance signal; and (d) fewer tokens need to be unmasked per step, reducing the computational cost of each forward pass.\", \"page\": 5, \"index\": 10, \"width\": 817, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2604.25050v1/fig-011.webp\", \"caption\": \"Figure 9: dynamic Pick and Place Real-world Setup\", \"page\": 16, \"index\": 11, \"width\": 748, \"height\": 421}]"
motivation: 现有基于流匹配的实时分块（RTC）方法在异步执行中存在结构性问题，如需要额外微调、启发式引导和计算开销。
method: 提出DiscreteRTC，利用离散扩散策略的迭代去掩码作为原生异步执行机制，无需额外微调或启发式引导。
result: 在动态模拟基准和真实世界动态操作任务中，DiscreteRTC比基于流匹配的RTC和其他基线方法获得更高的成功率，且推理计算量减少至0.7倍。
conclusion: DiscreteRTC通过离散扩散策略实现了更简单、更高效、性能更优的异步执行，为动态任务提供了新的解决方案。
---

## 摘要
与聊天机器人不同，物理AI必须在世界持续演变的同时采取行动。因此，同步执行器的块间暂停对于动态任务来说是致命的，无论推理速度多快。异步执行——边行动边思考——因此成为一种结构性需求，而实时分块（RTC）通过将块间过渡重构为修复过程（冻结已提交的动作并一致生成剩余部分）使其变得可行。然而，基于流匹配策略的RTC在结构上并非最优：其修复过程依赖于推理时的校正而非基础策略，导致预训练收益甚微、需要特定微调、启发式引导以及额外计算，从而增加了延迟。在本研究中，我们观察到离散扩散策略——通过迭代去掩码生成动作——是天然的异步执行器，能一次性解决所有限制：它们无需微调，因为修复是其固有操作，而早期停止进一步提供了自适应引导并降低了推理成本。我们提出了DiscreteRTC，它用原生去掩码替代外部校正，并在动态模拟基准和真实世界动态操控任务中展示其比连续RTC及其他基线方法获得更高的成功率。总之，DiscreteRTC实现更简单（异步修复无需额外代码），推理更快（相比从头生成动作仅需0.7倍计算量），执行效果更好（在真实世界动态抓取任务中比基于流匹配的RTC成功率高出50%）。更多可视化内容请见https://outsider86.github.io/DiscreteRTCSite/。

## Abstract
Unlike chatbots, physical AI must act while the world keeps evolving. Therefore, the inter-chunk pause of synchronous executors are fatal for dynamic tasks regardless of how fast the inference is. Asynchronous execution -- thinking while acting -- is therefore a structural requirement, and real-time chunking (RTC) makes it viable by recasting chunk transitions as inpainting: freezing committed actions and consistently generating the remainder. However, RTC with flow-matching policy is structurally suboptimal: its inpainting comes from inference-time corrections rather than the base policy, yielding little pre-training benefit, specific fine-tuning, heuristic guidance, and extra computation that inflates the latency. In this work, we observe that discrete diffusion policies, which generate actions by iteratively unmasking, are natural asynchronous executors that resolve all limitations at once: they are fine-tuning free since inpainting is their native operation, while early stopping further provides adaptive guidance and reduces inference cost. We propose DiscreteRTC, which replaces external corrections with native unmasking, and show on dynamic simulated benchmarks and real-world dynamic manipulation tasks that it achieves higher success rates than continuous RTC and other baselines. In summary, DiscreteRTC is simpler to implement with 0 lines of code for async inpainting, faster at inference with only 0.7x computation compared with generating actions from scratch, and better at execution with 50% higher success rate in real-world dynamic pick task compared with flow-matching-based RTC. More visualizations are on https://outsider86.github.io/DiscreteRTCSite/.

---

## 论文详细总结（自动生成）

好的，以下是对论文《DiscreteRTC: Discrete Diffusion Policies are Natural Asynchronous Executors》的详细结构化总结。

### 1. 核心问题与整体含义
*   **研究背景**：物理AI系统（如机器人）必须在动态变化的世界中实时行动。传统的同步执行策略（生成一个完整的动作块后执行）会在推理间隙造成机器人“停顿”，这对于追踪移动目标等动态任务是致命的。因此，**异步执行**（边执行已生成的动作，边计算后续动作）是刚性需求。
*   **现有方法瓶颈**：当前主流的异步执行方法**实时分块（RTC）** 通常与**流匹配（Flow-Matching）** 策略结合。该方法将块间过渡视为一个“修复”问题：冻结已开始执行的动作，并生成与之连贯的剩余动作。然而，该方法存在**结构性缺陷**：
    1.  **预训练不匹配**：流匹配策略在预训练时未见过“部分清晰、部分噪声”的混合状态，因此不具备原生的修复能力。
    2.  **需要额外微调**：为了获得修复能力，必须对模型进行专门的、针对修复任务的微调，增加了工程和计算负担。
    3.  **依赖启发式引导**：推理时需使用ΠGDM等外部校正算法，其引导权重是人工设计的固定启发式规则，缺乏适应性。
    4.  **增加推理开销**：在线校正计算（如向量-雅可比积）使每次推理的计算量几乎翻倍，反而增加了延迟。
*   **论文核心主张**：**离散扩散（Discrete Diffusion）策略是天然的异步执行器**。其通过迭代“去掩码”生成动作的方式，恰好与RTC的“修复”需求完美契合，能一次性解决上述所有问题。

### 2. 方法论
*   **核心思想**：用**离散扩散策略**替换RTC框架中的流匹配策略，形成 **DiscreteRTC**。其核心是利用离散扩散的**原生去掩码操作**来实现异步执行所需的“修复”，无需任何外部校正或算法修改。
*   **关键技术细节**：
    1.  **动作表示**：将连续动作空间通过量化（如k-bin）离散化为令牌序列。
    2.  **策略训练**：离散扩散策略在训练时学习从**随机掩码**的令牌序列中预测并恢复原始动作序列。这本质上就是在学习“修复”。
    3.  **异步执行流程**：
        *   **状态继承**：每次推理开始时，将上一个动作块尾部已确定执行的部分（`d`个动作）作为**已提交前缀**，其对应的令牌保持**未掩码**状态。
        *   **原生修复**：将包含已提交前缀和后续掩码令牌的序列输入策略，策略通过标准的**前向传播**进行迭代去掩码，生成连贯的后续动作。**无需任何额外的校正项或损失函数**。
        *   **早期停止**：只需确保在下次推理前，需要执行的`s`个动作令牌被完全去掩码即可，推理可以提前停止。剩余部分掩码的状态会自然传递给下一次推理，形成一种**自适应的、数据驱动的引导信号**，替代了流匹配RTC中人工设计的固定权重。
*   **优势**：
    *   **零代码修改**：异步修复功能是策略的固有属性，实现简单。
    *   **免微调**：预训练即赋予了模型修复能力。
    *   **计算高效**：由于只需对部分令牌进行去掩码，且可早期停止，单次推理计算量低于从头生成完整动作块。

### 3. 实验设计
*   **模拟基准**：在 **Kinetix** 基准测试（包含12个具有挑战性的物理控制任务）上进行评估。添加了动作高斯噪声以模拟不完美执行，强调闭环纠正的重要性。
*   **真实世界任务**：
    1.  **动态抓取**：抓取在转台上移动的物体。
    2.  **动态放置**：将物体放置到移动的平台上。
*   **对比方法**：
    *   **同步基线**：连续/离散扩散策略的同步执行版本。
    *   **异步基线**：
        *   **Naive Async**：新块一旦生成就立即切换。
        *   **Bidirectional Decoding (BID)**：使用拒绝采样来保持块间连续性。
        *   **ContinuousRTC**：基于流匹配策略和ΠGDM引导的RTC（当前SOTA方法）。
    *   **消融与扩展**：
        *   **DiscreteRTC + Fixed Steps**：固定去掩码步数，以相同计算预算生成更细粒度的动作。
        *   **DiscreteVLASH**：与未来状态感知异步推理方法VLASH结合。
        *   **Training-time Continuous RTC**：使用专门微调过的流匹配策略的RTC。

### 4. 资源与算力
*   **模拟实验**：基于JAX实现，在 **8块 NVIDIA RTX 6000 PRO** GPU上进行训练。训练周期为32个epoch，批量大小为512。
*   **真实世界实验**：
    *   **训练**：使用 **8块 NVIDIA RTX 6000 PRO** GPU，配合DeepSpeed ZeRO-2和梯度检查点技术。
    *   **部署**：在单块 **NVIDIA RTX 4090** GPU上运行，机器人控制频率为20Hz。
*   **未明确信息**：论文未提供具体的总训练时长（小时/天）。

### 5. 实验数量与充分性
*   **实验规模充分**：
    1.  **大规模模拟验证**：在Kinetix的12个任务上，对每个数据点进行了**2048次试验**，并系统测试了不同推理延迟（`d`从0到4）下的性能，涵盖了广泛的动态场景。
    2.  **全面的方法对比**：与多种同步/异步基线进行了对比，并进行了消融实验（如固定步数、结合VLASH）和针对“免微调”主张的专门消融（显示专门微调反而有害）。
    3.  **真实世界验证**：在两个动态操作任务上各进行了**20次试验**，验证了方法在现实噪声和延迟下的有效性。
*   **客观性与公平性**：
    *   对比实验保持了**相同的训练设置**（优化器、学习率、周期数）和**基本相同的策略网络规模**（仅最终输出层因任务不同而调整）。
    *   在真实世界实验中，所有策略共享**相同的视觉语言模型（VLM）骨干网络**（Qwen2.5-VL-3B-Instruct），确保了比较的公平性。

### 6. 主要结论与发现
1.  **性能优势**：DiscreteRTC在**Kinetix模拟基准**和**真实世界动态任务**中，**成功率均显著高于基于流匹配的ContinuousRTC及其他基线方法**。在真实世界动态抓取任务中，成功率高出50%。
2.  **效率优势**：DiscreteRTC的推理计算量仅为从头生成动作的**~0.7倍**，而ContinuousRTC由于需要在线校正，计算量增加至**~1.7倍**。
3.  **实现简易性**：DiscreteRTC的异步修复功能是离散扩散策略的**原生操作**，无需为异步执行编写任何额外的代码（0行代码修改）。
4.  **免微调有效性**：实验证明，对离散扩散策略进行专门的修复微调**反而会损害性能**，这反向证实了其预训练已具备强大的原生修复能力。
5.  **可扩展性**：DiscreteRTC可以与VLASH等先进推理方法结合，获得更稳定的性能。

### 7. 优点
*   **方法创新性**：首次系统性地指出了流匹配RTC的结构性缺陷，并创造性地利用离散扩散的固有特性（随机掩码训练、迭代去掩码）来优雅地解决异步执行问题，视角新颖。
*   **原理简洁高效**：将复杂的异步协调问题转化为模型的原生前向推理，消除了对额外校正算法、启发式规则和专门微调的依赖，大大简化了系统设计和部署。
*   **实验设计扎实**：实验设计全面，涵盖了从模拟到真实世界、从性能对比到消融分析的多个维度，数据量充足，结论支撑有力。
*   **实用价值高**：所提出的方法直接降低了实现高性能异步执行的工程门槛和计算成本，对机器人等实时性要求高的领域具有明确的实用价值。

### 8. 不足与局限
1.  **动作量化方法简单**：论文使用了简单的k-bin均匀量化，忽略了动作序列的时间结构，可能导致令牌序列过长、信息冗余。未来可采用更紧凑的、具有时间感知的令牌化方法（如FAST, OAT）。
2.  **策略架构模块化**：当前采用VLM骨干+离散扩散动作头的**模块化设计**，限制了视觉语言骨干网络参与迭代推理过程。未来可探索**统一的离散扩散VLA架构**，让感知和决策在同一个迭代去掩码框架中进行。
3.  **解码策略非最优**：论文指出，当前使用的“最大置信度”解码策略在去掩码时并未遵循时间上的因果顺序（即优先去掩码即将执行的动作），这限制了早期停止带来的计算收益和自然引导的效果。需要设计更**符合RTC时序结构的去掩码策略**（如AR-block解码）。
4.  **实验场景局限**：实验主要集中于桌面机械臂的抓取和放置任务。方法在更复杂的移动机器人、人形机器人或需要更长时域规划的任务中的有效性有待进一步验证。

（完）
