---
title: "CodeGraphVLP: Code-as-Planner Meets Semantic-Graph State for Non-Markovian Vision-Language-Action Models"
title_zh: CodeGraphVLP：面向非马尔可夫视觉-语言-动作模型的代码即规划器与语义图状态融合方法
authors: "Khoa Vo, Sieu Tran, Taisei Hanyu, Yuki Ikebe, Duy Nguyen, Bui Duy Quoc Nghi, Minh Vu, Anthony Gunderman, Chase Rainwater, Anh Nguyen, Ngan Le"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22238v1"
tags: ["query:sr-0501"]
score: 9.0
evidence: 用于机器人操作和规划的非马尔可夫视觉-语言-动作模型
tldr: 本文针对视觉语言动作模型在非马尔可夫长时程任务中因遮挡、干扰和部分可观测性导致的性能问题，提出了CodeGraphVLP分层框架。该方法结合持久语义图状态来维护任务相关实体和关系，使用基于代码的规划器进行高效进度检查和子任务指令生成，并通过构造去干扰的观察结果来聚焦关键证据。实验表明，该方法在真实世界任务中显著提高了任务完成率并降低了规划延迟，为机器人长时程操作提供了可靠解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22238v1/fig-001.webp\", \"caption\": \"Fig. 3. Qualitative rollouts of CodeGraphVLP on our three real-world tasks (Pick-and-Place Twice, Place-and-Stack, and Swap Cups). For each task, we show multi-view RGB inputs with the overall instruction, the semantic-graph state Gt, and the progress-guided prompts used by the VLA policy: clutter-free visual cues that retain only subtask-relevant objects and the planner-produced subtask language cues.\", \"page\": 6, \"index\": 1, \"width\": 734, \"height\": 718}, {\"url\": \"assets/figures/arxiv/2604.22238v1/fig-002.webp\", \"caption\": \"TABLE I COMPARISON OF METHODS ON REAL-WORLD NON-MARKOVIAN LONG-HORIZON TASKS. ALL REPORTED VALUES ARE SUCCESS RATES (%).\", \"page\": 7, \"index\": 2, \"width\": 922, \"height\": 221}, {\"url\": \"assets/figures/arxiv/2604.22238v1/fig-003.webp\", \"caption\": \"Fig. 2. Overview of CodeGraphVLP. (a) Graph state initialization: from the first multi-view observation, we segment objects, identify task-relevant entities, associate them across views, and induce relations to build a compact semantic graph. (b) Programmatic planner construction: given the task and a graph API, an LLM synthesizes an executable policy(graph) that performs progress checks over the graph and maintains lightweight task memory. (c) End-to-end inference: as new observations arrive, we update the semantic graph, execute the planner to produce the next subtask and relevant objects, construct clutter-free visual–language cues, and run a VLA policy for action prediction.\", \"page\": 3, \"index\": 3, \"width\": 942, \"height\": 495}]"
motivation: 解决视觉语言动作模型在非马尔可夫长时程任务中因遮挡、干扰和部分可观测性导致的性能下降问题。
method: 采用分层框架，结合持久语义图状态、基于代码的规划器和进度引导的视觉语言提示。
result: 在真实世界任务中，相比基线方法，CodeGraphVLP提高了任务完成率并降低了规划延迟。
conclusion: CodeGraphVLP通过结合语义图状态和代码规划器，有效解决了非马尔可夫长时程任务中的视觉语言动作模型挑战。
---

## 摘要
视觉-语言-动作模型有望实现通用机器人操作，但通常被训练和部署为短视程策略，其假设最新观察足以支持动作推理。这一假设在非马尔可夫的长视程任务中会失效，因为任务相关证据可能被遮挡或仅在轨迹早期出现，且杂乱环境和干扰物会使细粒度视觉定位变得脆弱。我们提出CodeGraphVLP，一个分层框架，通过将持久性语义图状态与可执行的基于代码的规划器以及进度引导的视觉语言提示相结合，实现可靠的长视程操作。语义图在部分可观测性下维护任务相关实体和关系。合成的规划器在该语义图上执行，以进行高效的进度检查，并输出子任务指令及相关对象。我们利用这些输出来构建抑制杂乱的观察，使VLA执行器专注于关键证据。在真实世界的非马尔可夫任务中，CodeGraphVLP相比强大的VLA基线和具备历史记忆的变体提高了任务完成率，同时与基于VLM的循环规划相比显著降低了规划延迟。我们还进行了广泛的消融研究，以确认每个组件的贡献。

## Abstract
Vision-Language-Action (VLA) models promise generalist robot manipulation, but are typically trained and deployed as short-horizon policies that assume the latest observation is sufficient for action reasoning. This assumption breaks in non-Markovian long-horizon tasks, where task-relevant evidence can be occluded or appear only earlier in the trajectory, and where clutter and distractors make fine-grained visual grounding brittle. We present CodeGraphVLP, a hierarchical framework that enables reliable long-horizon manipulation by combining a persistent semantic-graph state with an executable code-based planner and progress-guided visual-language prompting. The semantic-graph maintains task-relevant entities and relations under partial observability. The synthesized planner executes over this semantic-graph to perform efficient progress checks and outputs a subtask instruction together with subtask-relevant objects. We use these outputs to construct clutter-suppressed observations that focus the VLA executor on critical evidence. On real-world non-Markovian tasks, CodeGraphVLP improves task completion over strong VLA baselines and history-enabled variants while substantially lowering planning latency compared to VLM-in-the-loop planning. We also conduct extensive ablation studies to confirm the contributions of each component.

---

## 论文详细总结（自动生成）

### 论文总结：CodeGraphVLP

#### 1. 核心问题与整体含义
*   **研究背景**：视觉-语言-动作模型在通用机器人操作上展现出潜力，但通常被设计为**短视程、马尔可夫策略**，即仅根据当前观察决定动作。
*   **核心问题**：在**非马尔可夫的长视程任务**中，关键信息可能被遮挡或仅出现在历史中，且场景中的**杂乱物体**会干扰模型的视觉定位能力，导致现有VLA模型失效。
*   **研究动机**：解决现有两种主流扩展方法（记忆增强VLA和分层VLM-VLA系统）的局限性：前者难以高效维护和利用长时历史信息，后者因频繁调用大模型导致延迟高，且缺乏对任务状态的显式、结构化表示。

#### 2. 方法论
*   **核心思想**：提出一个**分层框架**，将**持久化的语义图状态**、**基于代码的规划器**和**进度引导的视觉-语言提示**相结合，以实现对长时任务状态的显式维护和高效推理。
*   **关键技术细节**：
    1.  **持久化语义图状态**：
        *   **结构**：一个图 `Gt = (Vt, Et)`，节点 `Vt` 代表任务相关实体（含语义、属性、掩码），边 `Et` 代表实体间关系（如 `in`, `on`, `near`）。
        *   **初始化**：利用分割模型（YOLOE）和VLM，从初始多视角观察中识别任务相关对象、进行跨视角关联，并基于几何规则推导关系。
        *   **在线更新**：使用跟踪器（Cutie）更新已有节点的掩码，并运行分割和关联流程以发现新对象、纠正漂移。
    2.  **代码即规划器**：
        *   **生成**：在任务开始时，利用LLM（如GPT-5）**一次性合成**一个可执行的Python程序 `P`。该程序接收任务指令和初始语义图，生成包含任务逻辑和轻量级任务记忆的 `policy(graph)` 函数。
        *   **执行**：在每一步，规划器查询当前语义图 `Gt`，通过检查状态转移约束（如 `holding`, `in`）来评估进度，并输出下一个**子任务指令** `lsub_t` 和**子任务相关对象集** `Orel_t`。
    3.  **去干扰的视觉-语言提示**：
        *   **语言提示**：使用规划器生成的子任务指令 `lsub_t`，而非原始长时任务指令，以聚焦当前步骤。
        *   **视觉提示**：利用语义图中的对象掩码，仅保留 `Orel_t` 中相关对象的像素，将其他区域置黑，生成去干扰的观察 `˜ot` 输入给VLA执行器。
    4.  **VLA执行与训练**：VLA模型（如π0）接收 `(˜ot, st, lsub_t)` 预测动作。训练时，使用模仿学习，数据通过上述流程从长视程演示中生成子任务级别的训练元组。

#### 3. 实验设计
*   **任务/场景**：设计了三个需要非马尔可夫推理的真实世界桌面操作任务：
    1.  **Pick-and-Place Twice**：将立方体移到另一个盘子，再移回原处（需记忆初始位置）。
    2.  **Place-and-Stack**：将立方体放入最近的杯子，再将另一个杯子叠放上去（立方体放入后即被遮挡）。
    3.  **Swap Cups**：交换两个杯子的位置，需使用空盘子作为缓冲区（需记忆初始配置并执行多步序列）。
*   **Benchmark**：任务完成成功率（包括中间里程碑成功率）。
*   **对比方法**：
    *   **VLA基线**：π0 FAST， π0， π0.5， Gr00T N1.5。
    *   **增强基线**：Gr00T N1.5 + Multi-frame（输入历史帧）。
    *   **消融对比**：与VLM在线规划器对比效率；与不使用去干扰视觉提示的变体对比鲁棒性。

#### 4. 资源与算力
*   **训练硬件**：使用了 **4 块 NVIDIA A6000 GPU** 进行模型微调。
*   **训练细节**：所有VLA模型均使用低秩适应技术微调了 **50K 步**，批量大小为128。
*   **未明确信息**：论文未提及单次训练的具体耗时（如小时/天）。

#### 5. 实验数量与充分性
*   **实验数量**：
    *   **主实验**：在3个不同的非马尔可夫任务上评估了5种基线方法和本文方法。
    *   **消融实验**：进行了2组关键消融（规划器效率、去干扰提示有效性），均在最具挑战性的“Swap Cups”任务上进行。
*   **充分性与客观性**：
    *   **任务设计**：覆盖了不同类型的非马尔可夫依赖（位置记忆、遮挡、多步序列），并包含杂乱场景，具有代表性。
    *   **基线选择**：涵盖了当时（论文时间线）主流的开源VLA模型及其增强版本，对比较为全面。
    *   **评估指标**：使用任务成功率和规划延迟（秒/步）进行量化评估，结果客观。
    *   **消融研究**：针对核心组件（代码规划器、语义图、去干扰提示）进行了分离实验，有效证明了各部分的贡献。实验设计总体上是**充分、客观且公平**的。

#### 6. 主要结论与发现
*   **性能领先**：CodeGraphVLP在三个任务上的**平均成功率（81.7%）显著高于所有基线方法**，包括最强的带历史记忆的VLA变体（56.7%）。
*   **效率优势**：其代码规划器相比在线VLM规划器，**将每步规划延迟从约3秒降低至0.3秒**，实现了高效的进度检查。
*   **组件有效性**：
    1.  语义图状态对于提升VLM规划器的成功率至关重要。
    2.  **去干扰的视觉提示**是提升在杂乱场景中动作推理鲁棒性的关键，将其移除后成功率从85%骤降至40%。
*   **核心价值**：通过结合**显式的、结构化的场景记忆**和**轻量级的程序化推理**，CodeGraphVLP能够可靠地处理非马尔可夫依赖，并抵抗视觉干扰。

#### 7. 优点
*   **创新性整合**：创造性地将**符号/程序化规划**（代码规划器）与**子符号感知**（VLA）通过**语义图**这一结构化状态进行桥接，兼具可解释性与泛化能力。
*   **解决关键痛点**：直接针对长视程任务中的**非马尔可夫性**和**视觉杂乱**两大挑战提出了系统化解决方案。
*   **高效设计**：**一次性合成**代码规划器，避免了在每一步都调用大型VLM/LLM，**极大降低了在线规划的延迟**，更适合实时机器人控制。
*   **聚焦机制**：通过语义图提取子任务相关对象并生成去干扰的视觉提示，有效**提升了VLA在复杂场景中的视觉定位鲁棒性**。

#### 8. 不足与局限
*   **依赖基础模型**：语义图构建（物体识别、关联、关系推导）和代码生成严重依赖现成的VLM和LLM，其性能瓶颈和错误（如错误识别属性、关系）会直接传导至下游。
*   **代码生成的可靠性**：生成的规划代码需要精心设计的提示，且可能包含逻辑错误，缺乏自动化的验证和纠错机制。
*   **场景与任务限制**：
    *   实验仅限于**桌面操作**，在更复杂、动态或非结构化的环境中（如家庭厨房）的泛化能力未经验证。
    *   语义图的关系推导基于简单的**几何规则**，可能无法处理更复杂的空间或功能关系。
*   **初始化要求**：需要相对准确的初始观察来构建语义图，在极度混乱或能见度极差的初始场景下可能失效。

（完）
