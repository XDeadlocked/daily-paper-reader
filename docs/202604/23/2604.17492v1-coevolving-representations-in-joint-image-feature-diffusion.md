---
title: Coevolving Representations in Joint Image-Feature Diffusion
title_zh: 联合图像-特征扩散中的协同演化表征
authors: "Theodoros Kouzelis, Spyros Gidaris, Nikos Komodakis"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17492v1"
tags: ["query:diffusion"]
score: 9.0
evidence: 通过协同演化的语义表示改进扩散模型训练
tldr: 本文提出Coevolving Representation Diffusion (CoReDi)框架，通过联合训练轻量线性投影与扩散模型，使语义表示空间可随训练共同演化，从而自适应生成任务需求。实验表明，该方法在VAE潜空间和像素空间扩散生成中均实现更快收敛与更高图像质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17492v1/fig-001.webp\", \"caption\": \"Table 3: Representation Encoder Variation. Results with different Visual Encoders (VE). All models at 200K steps. CoReDi yields consistent performance improvements across different choices for the visual encoder used for joint training compared to fixed ReDi projection used in [22].\", \"page\": 11, \"index\": 1, \"width\": 547, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2604.17492v1/fig-002.webp\", \"caption\": \"Table 10: Configurations of pixel space and latent space experiments.\", \"page\": 21, \"index\": 2, \"width\": 469, \"height\": 598}, {\"url\": \"assets/figures/arxiv/2604.17492v1/fig-003.webp\", \"caption\": \"Fig. 3: Overview of CoReDi. Given an input image, a frozen pretrained visual encoder extracts semantic features, which are projected to a lower-dimensional space via a learnable projection gϕ, followed by batch normalization and a regularization loss to prevent collapse. Both the noisy image tokens and the noisy coevolving feature tokens are passed as input to a diffusion backbone, which jointly predicts the image and representation velocities. A stop-gradient is applied through the clean representation target in the representation loss, allowing the projection to coevolve with the generative model without degeneracy.\", \"page\": 7, \"index\": 3, \"width\": 629, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2604.17492v1/fig-004.webp\", \"caption\": \"Fig. 5: Spatial structure of coevolving representations during training for CoReDi with DINOv2 and MOCOv3 as measured by LDS, CDS, and RMSC. All three metrics improve consistently as training progresses. Dashed horizontal lines indicate the fixed PCA projections used in ReDi [22].\", \"page\": 14, \"index\": 4, \"width\": 672, \"height\": 368}, {\"url\": \"assets/figures/arxiv/2604.17492v1/fig-005.webp\", \"caption\": \"Fig. 8: Selected samples from our CoReDi-XL/2 trained for 1M steps on ImageNet 256 × 256. Images and visual representations are jointly generated by our model. We use Classifier-Free Guidance with w = 4.0.\", \"page\": 23, \"index\": 5, \"width\": 656, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2604.17492v1/fig-006.webp\", \"caption\": \"Fig. 9: Qualitative comparison of feature visualizations. For each image, we show PCA visualization of DINOv2, MOCOv3, SigLIPv2 and MAE features. For each feature, we visualize PCA vs CoReDi’s learned projection.\", \"page\": 23, \"index\": 6, \"width\": 720, \"height\": 463}, {\"url\": \"assets/figures/arxiv/2604.17492v1/fig-007.webp\", \"caption\": \"Fig. 4: Regularization Prevents Feature Collapse. Visualization of all 8 channels of the coevolving representation z̃0 at 200K steps, under two training configurations. Without regularization, the projected channels collapse, failing to capture diverse semantic information. The Feature Variance Regularization strategy successfully prevents collapse, yielding semantically meaningful channel activations.\", \"page\": 8, \"index\": 7, \"width\": 679, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.17492v1/fig-008.webp\", \"caption\": \"Fig. 2: (Left) Comparison of fixed PCA and learned CoReDi representations for DINOv2 and MOCOv3. The learned projections yield cleaner, more structured representations with coherent spatial organization, while the fixed PCA projections produce noisier, less semantically meaningful activations. (Right) By jointly adapting the representation space alongside the generative model, CoReDi consistently speeds up convergence. In latent space (Top), CoReDi outperforms ReDi and, notably, converges ∼ 13× faster than REPA. In pixel space (Bottom) it improves convergence by ×2 over DeCo.\", \"page\": 4, \"index\": 8, \"width\": 690, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.17492v1/fig-009.webp\", \"caption\": \"Table 9: Detailed evaluation for SiT-XL/2, CoReDi-XL/2, ReDi-XL/2, and REPA-XL/2. All results are reported without Classifier-Free Guidance.\", \"page\": 19, \"index\": 9, \"width\": 581, \"height\": 467}]"
motivation: 现有联合图像-特征扩散模型使用静态的语义表示空间，无法根据生成任务自适应优化。
method: 提出Coevolving Representation Diffusion (CoReDi)框架，通过联合学习轻量线性投影与扩散模型，使语义表示空间随训练共同演化。
result: 在VAE潜空间与像素空间扩散任务中，CoReDi实现更快收敛及更佳生成质量。
conclusion: CoReDi通过共同演化图像与语义特征表示，在多种扩散生成任务中实现了更高质量的生成表现。
---

## 摘要
联合图像-特征生成建模近期被提出为一种有效策略，通过将低层次的 VAE 潜变量与从预训练视觉编码器中提取的高层语义特征相结合，从而改进扩散训练。然而，现有方法依赖于一个固定的表征空间，该空间独立于生成目标构建，并在训练过程中保持不变。我们认为，引导扩散的表征空间本身应当能够适应生成任务。为此，我们提出了协同演化表征扩散（Coevolving Representation Diffusion，简称 CoReDi）框架，其中语义表征空间在训练过程中与扩散模型联合学习轻量级线性投影，从而不断演化。尽管直接优化该投影会导致退化解，我们展示了通过结合停梯度目标、归一化以及防止特征塌陷的定向正则化，可以实现稳定的协同演化。这一方法使语义空间能够逐步针对图像合成的需求进行专化，从而增强其与图像潜变量的互补性。我们将 CoReDi 应用于 VAE 潜变量扩散和像素空间扩散两种场景，结果表明自适应语义表征在两种设定下均能提升生成建模性能。实验结果显示，与在固定表征空间中进行的联合扩散模型相比，CoReDi 具有更快的收敛速度和更高的样本质量。

## Abstract
Joint image-feature generative modeling has recently emerged as an effective strategy for improving diffusion training by coupling low-level VAE latents with high-level semantic features extracted from pre-trained visual encoders. However, existing approaches rely on a fixed representation space, constructed independently of the generative objective and kept unchanged during training. We argue that the representation space guiding diffusion should itself adapt to the generative task. To this end, we propose Coevolving Representation Diffusion (CoReDi), a framework in which the semantic representation space evolves during training by learning a lightweight linear projection jointly with the diffusion model. While naively optimizing this projection leads to degenerate solutions, we show that stable coevolution can be achieved through a combination of stop-gradient targets, normalization, and targeted regularization that prevents feature collapse. This formulation enables the semantic space to progressively specialize to the needs of image synthesis, improving its complementarity with image latents. We apply CoReDi to both VAE latent diffusion and pixel-space diffusion, demonstrating that adaptive semantic representations improve generative modeling across both settings. Experiments show that CoReDi achieves faster convergence and higher sample quality compared to joint diffusion models operating in fixed representation spaces.

---

## 论文详细总结（自动生成）

# 联合图像-特征扩散中的协同演化表征（Coevolving Representations in Joint Image-Feature Diffusion）论文总结

---

## 一、研究动机与背景

- **核心问题**：现有联合图像–特征扩散模型通常将图像的低层表示（如 VAE 潜变量）与预训练视觉编码器的高层语义特征结合，以增强生成质量。但语义表示空间在训练过程中始终**保持静态**，未能适应生成任务。
- **问题本质**：固定的语义投影空间未与生成目标协同优化，导致生成模型无法充分利用语义信息，存在特征冗余、空间塌陷等问题。
- **研究目标**：作者希望构建一个机制，使语义表示空间**可随生成模型共同演化（co-evolution）**，实现更高效、更稳健的图像生成。

---

## 二、方法论：CoReDi 框架

### 1. 核心思想
- **Coevolving Representation Diffusion (CoReDi)**：通过联合训练轻量线性投影与扩散模型，实现语义空间与生成模型的同步优化，使表征不断适应图像生成任务。
- **关键洞察**：语义空间应当与扩散目标协同学习，而非静态使用 PCA 或固定编码投影。

### 2. 技术结构
1. **可学习投影层**  
   - 由一冻结的视觉编码器（如 DINOv2）提取语义特征 \( z_0 = VE(x_0) \)；  
   - 使用可学习线性映射 \( \tilde z_0 = g_\phi(z_0) = z_0 W_\phi \)，允许特征维度与任务需求动态匹配。
2. **联合扩散优化目标**  
   两个通道同时建模图像与语义空间的流匹配目标：
   \[
   L_{joint} = L_{image} + \lambda_z L_{rep}
   \]
   其中 \( L_{image} \) 为图像重构损失，\( L_{rep} \) 为表征扩散损失。
3. **稳定训练的三大机制**  
   - **Stop-Gradient**：在表征目标中阻断梯度流，防止模型通过修改目标值而陷入退化最优。
   - **Batch Normalization**：控制输入尺度，保持噪声调度稳定，并抑制特征样本塌陷。
   - **明确正则化防止特征塌陷**：
     - 特征方差正则 \( L_{var} \)：鼓励每通道保持最小方差。
     - 正交约束 \( L_{orth} \)：保持投影矩阵列的独立性。
     - 协方差惩罚 \( L_{cov} \)：减少通道间相关性。
4. **总体训练目标**
   \[
   L(\theta, \phi) = L_{image} + \lambda_z L_{rep} + \lambda_{reg} L_{reg}
   \]
   其中 \(\lambda_z\) 和 \(\lambda_{reg}\) 控制两类损失权重。
5. **像素空间扩展**
   - 在 DeCo 架构上引入共同演化机制，使视觉特征与像素输入共同训练，从而提升高分辨率生成质量并消除 VAE 压缩瓶颈。

---

## 三、实验设计

### 1. 数据集与任务场景
- **ImageNet 256×256**：用于所有生成与泛化实验。  
- 同时在 **VAE 潜空间扩散**与**像素空间扩散**两种场景进行评估。

### 2. 基线与对比方法
- **基线模型**：DiT、SiT、ReDi、REPA、DeCo。
- **对比设置**：
  - 在相同架构与训练预算下比较 FID、sFID、Inception Score、Precision、Recall。
  - 消融实验涉及不同正则化方式、停止梯度、归一化设置的去除。

### 3. 训练配置
- **潜空间实验**：基于 SiT-B/2（130M 参数）和 SiT-XL/2（675M 参数）。
- **像素空间实验**：基于 DeCo-L/16（426M 参数）。
- 使用相同批大小（256）和学习率（1e–4），均在 **ImageNet-256** 上训练。

---

## 四、资源与算力

- 文中未具体说明 GPU 型号或数量，仅提到实验在 **NVIDIA A100 GPU** 上执行评估。
- 使用的硬件资源由项目 GRNET、EuroHPC（e-dev-2026d01-087）与 GENCI-IDRIS 提供。
- 训练时长以迭代步为单位给出（如 200K、400K、2M、4M iteration），但**未披露具体训练时间或 GPU 数量**。

---

## 五、实验数量与充分性

- **总体实验量大且多样**：
  - 潜空间与像素空间两种任务；
  - 四种视觉编码器（DINOv2、MOCOv3、SigLIPv2、MAE）；
  - 正则化与超参数消融（λreg、λz、BN、SG 去除）；
  - 各不同模型规模（B/2、XL/2）。
- **多维度评估**：FID/sFID/IS/Precision/Recall 全面覆盖，包括 CFG（Classifier-Free Guidance）与非CFG两种采样策略。
- **实验公平性**：控制相同迭代步与采样次数，使用相同评估工具（ADM官方TensorFlow套件）。  
➡️ 可以认为实验充分且客观。

---

## 六、主要结论与发现

- **性能提升**：
  - 潜空间中：CoReDi-XL/2 达到 FID = 3.3，仅需 2M 迭代，与 ReDi-XL/2 4M 迭代相同精度，收敛速度提升约 2×。
  - 像素空间中：在相同 200K 步时，CoReDi-L/16 FID = 21.5，相比 DeCo-L/16 (31.3) 提升明显，训练加速约 2×。
- **稳定性**：
  - 移除 BN 或 Stop-Gradient 导致训练完全崩溃或性能显著下降。
  - 加入正则化可防止通道塌陷并提升表征多样性。
- **空间结构演化**：随着训练进行，表征空间在 LDS、CDS、RMSC 三项空间度量上均持续改善，说明表征具备可解释的空间结构演化特征。

---

## 七、优点与创新点

- 🔹 **方法创新**：首次在联合扩散框架中提出表征空间随生成目标协同演化的机制。
- 🔹 **稳定性机制完备**：Stop-Gradient + BatchNorm + Regularization 三重保证稳定训练。
- 🔹 **广泛适应性**：在多种视觉编码器上均获得一致提升。
- 🔹 **扩展性强**：自然扩展到像素空间扩散，验证跨任务有效性。
- 🔹 **效果显著**：收敛速度加快、FID 与 IS 指标同时提高，证明语义自适应的有效性。

---

## 八、不足与局限

- ⚠️ **资源披露不完整**：论文未说明具体 GPU 数量、运行时长，难以评估训练成本。
- ⚠️ **缺乏跨数据集验证**：仅使用 ImageNet，尚未测试更复杂分布或文本条件扩散任务。
- ⚠️ **方法简化假设**：采用线性投影模型，可能限制表征空间学习的非线性能力。
- ⚠️ **评估集中于图像生成**：未探讨表征对其他任务（如编辑、检索）的迁移效应。

---

（完）
