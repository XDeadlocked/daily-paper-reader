---
title: Semi-Supervised Flow Matching for Mosaiced and Panchromatic Fusion Imaging
title_zh: 用于镶嵌与全色融合成像的半监督流匹配方法
authors: "Peiming Luo, Nan Wang, Litong Liu, Jiahan Huang, Chenxu Wu, Renwei Dian, Junming Hou"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20128v1"
tags: ["query:diffusion"]
score: 9.0
evidence: 用于图像融合与修复的半监督流匹配框架
tldr: 论文提出一种半监督流匹配框架，用于将低分辨率马赛克高光谱图与高分辨率全色图融合，通过无监督先验预训练与条件流匹配生成模型实现高质量重建，实验结果显著优于现有方法，具备良好扩展性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20128v1/fig-001.webp\", \"caption\": \"Figure 2: Schematic comparison of existing supervised/unsupervised diffusion models and our proposed semi-supervised flowmatching framework. Supervised diffusionmodels are often trained on protocol-specific simulation data (e.g., Wald’s protocol), introducing a noticeable domain gap in real-world scenarios; while unsupervised approaches rely on predefined assumptions, such as low-rank tensor decomposition and subspace projection, leading to limited flexibility and generalizability. Instead, our method unifies unsupervised pretraining with generative modeling, offering a flexible, task-agnostic framework.\", \"page\": 2, \"index\": 1, \"width\": 494, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2604.20128v1/fig-002.webp\", \"caption\": \"Figure 6: Qualitative evaluation of the competing methods on the CAVE and Chikusei datasets. Odd row: visualizations of the fusion results; even row: MAE maps.\", \"page\": 7, \"index\": 2, \"width\": 1029, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2604.20128v1/fig-003.webp\", \"caption\": \"Table 1: Quantitative evaluation of the competing methods on the CAVE, Chikusei, and Real-world datasets. ↑ stands for the greater value the better, while ↓ stands for the smaller value the better. The best result of each quality index is marked in bold.\", \"page\": 7, \"index\": 3, \"width\": 1056, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.20128v1/fig-004.webp\", \"caption\": \"Figure 8: Ablation study on the random voting mechanism.\", \"page\": 8, \"index\": 4, \"width\": 492, \"height\": 485}, {\"url\": \"assets/figures/arxiv/2604.20128v1/fig-005.webp\", \"caption\": \"Figure 3: Framework overview of the proposed semi-supervised flow matching framework for mosaiced and PAN image fusion, implemented via a two-stage training pipeline. First, a prior network is pretrained in an unsupervised manner to generate pseudo HR-HSI estimate. Subsequently, a flow matching model is trained to generate the desired HR-HSI, where the residual between the pseudo HR-HSI and channel-expanded PAN image serves as the initial training target and is iteratively updated through a random voting mechanism. During inference, the trained model directly generate an HR-HSI with superior spatial and spectral fidelity, guided by a conflict-free gradient guidance strategy.\", \"page\": 4, \"index\": 5, \"width\": 996, \"height\": 577}, {\"url\": \"assets/figures/arxiv/2604.20128v1/fig-006.webp\", \"caption\": \"Figure 4: Variation of the target residual, alongside the spatial and spectral metrics of the updated HR-HSI (illustrated on a simulated example). These progressive refined results clearly validate the effectiveness of the random voting mechanism.\", \"page\": 5, \"index\": 6, \"width\": 490, \"height\": 329}]"
motivation: 为解决低分辨率马赛克高光谱图与高分辨率全色图融合中的病态问题，实现快速高质量的HR-HSI重建。
method: 提出一个半监督流匹配框架，结合无监督先验网络预训练与条件流匹配模型，实现高光谱与全色图融合。
result: 在多项基准数据集上，该方法在定量和定性指标上均显著优于现有方法。
conclusion: 该方法为图像融合任务提供了一种兼具灵活性和泛化性的生成式框架，可扩展至其他融合与图像恢复领域。
---

## 摘要
将低分辨率（LR）的镶嵌型高光谱图像（HSI）与高分辨率（HR）的全色（PAN）图像融合是一种实现视频速率HR-HSI单次采集成像的有前景方案，但其病态问题仍是重大挑战。本文提出了一种用于镶嵌与PAN图像融合的创新半监督流匹配框架。与以往依赖特定协议或人工假设的扩散模型方法不同，我们的方法将无监督方案与流匹配自然结合，形成了一个具有良好泛化性和高效性的生成框架。具体而言，我们的方法采用两阶段训练流程：首先预训练一个无监督先验网络以生成初始伪HR-HSI；在此基础上，再训练一个条件流匹配模型以生成目标HR-HSI，并引入随机投票机制对初始HR-HSI估计进行迭代优化，从而实现稳健且有效的融合。在推理过程中，我们采用无冲突梯度引导策略，以确保HR-HSI的光谱与空间一致性重建。多项基准数据集上的实验结果表明，与代表性基线方法相比，我们的模型在定量和定性性能上均取得显著提升。除镶嵌与PAN融合外，该方法还提供了可扩展的生成框架，可用于其他图像融合任务，并可与无监督或盲图像恢复算法结合。

## Abstract
Fusing a low resolution (LR) mosaiced hyperspectral image (HSI) with a high resolution (HR) panchromatic (PAN) image offers a promising avenue for video-rate HR-HSI imaging via single-shot acquisition, yet its severely ill-posed nature remains a significant challenge. In this work, we propose a novel semi-supervised flow matching framework for mosaiced and PAN image fusion. Unlike previous diffusion-based approaches constrained by specific protocols or handcrafted assumptions, our method seamlessly integrates an unsupervised scheme with flow matching, resulting in a generalizable and efficient generative framework. Specifically, our method follows a two-stage training pipeline. First, we pretrain an unsupervised prior network to produce an initial pseudo HR-HSI. Building on this, we then train a conditional flow matching model to generate the target HR-HSI, introducing a random voting mechanism that iteratively refines the initial HR-HSI estimate, enabling robust and effective fusion. During inference, we employ a conflict-free gradient guidance strategy that ensures spectrally and spatially consistent HR-HSI reconstruction. Experiments on multiple benchmark datasets demonstrate that our method achieves superior quantitative and qualitative performance by a significant margin compared to representative baselines. Beyond mosaiced and PAN fusion, our approach provides a flexible generative framework that can be readily extended to other image fusion tasks and integrated with unsupervised or blind image restoration algorithms.

---

## 论文详细总结（自动生成）

# 《用于镶嵌与全色融合成像的半监督流匹配方法》论文总结

---

## 一、研究问题与整体含义

- **研究背景**  
  高光谱成像（HSI）能提供丰富的光谱信息，是遥感、医学诊断和工业检测等领域的重要技术。然而传统扫描式HSI成像速度较慢，不适合动态场景。  
  镶嵌式高光谱相机通过光谱滤波阵列实现单拍快照成像，但以**空间分辨率**为代价。为弥补这一不足，研究者尝试将**低分辨率镶嵌图（LR-HSI）**与**高分辨率全色图（HR-PAN）**融合重建高分辨率高光谱图（HR-HSI）。

- **核心问题**  
  该融合任务本质上是严重病态的：缺乏配对高分辨率真实标签，且传统假设（低秩分解、稀疏性等）在真实数据中难以成立，导致监督或无监督扩散模型的泛化性和效率均受限。

- **研究目标**  
  提出一个能在**无高分辨率真值监督**条件下运行的生成式框架，实现高效、鲁棒、可泛化的HR-HSI融合重建。

---

## 二、方法论概述

### 1. 总体思路

论文提出一种**半监督流匹配框架（Semi-supervised Flow Matching, SFM）**，结合无监督先验学习与条件流匹配生成。在训练阶段执行两个核心步骤：

1. **无监督先验网络预训练**：生成伪高分辨率高光谱估计；
2. **条件流匹配阶段**：学习由噪声到伪高光谱残差的连续动力系统分布，实现逐步逼近真实HR-HSI的生成过程。

### 2. 核心技术模块

- **（1）无监督先验网络（Prior Network）**
  - 输入：插值后的镶嵌图与PAN图；
  - 输出：初始伪HR-HSI。  
  - 损失设计：
    - **观测一致性损失**  
      \[
      L_{oc} = \|P - A_P(Y)\|^2 + \|m - A_m(Y)\|^2
      \]
      保证重建与原观测一致。
    - **等变损失（Equivariant Loss）**  
      \[
      L_{ei} = \|T(Y) - G_\theta(A_P(T(Y)), A_m(T(Y)))\|^2
      \]
      强化几何变换下的不变性。

- **（2）条件流匹配模型（Conditional Flow Matching, CFM）**
  - 通过U-Net预测连续时间向量场，模拟从高斯噪声到目标残差（Y-PD）的流动；
  - 关键损失包括：
    - **直接匹配损失** \( L_{dir} \)
    - **变换匹配损失** \( L_{trans} \)
    - **退化一致性损失** \( L_{deg} \)  
      综合为  
      \[
      L_{total} = L_{dir} + L_{trans} + L_{deg}
      \]

- **（3）随机投票机制（Random Voting Mechanism, RVM）**
  - 在固定窗口内选取若干训练周期的模型检查点，对候选HR-HSI进行退化一致性评估；
  - 按得分排序更新伪标签，防止早期伪标签误差累积；
  - 提供动态“弱监督”信号，使目标分布不断自校正。

- **（4）无冲突梯度引导（Conflict-free Gradient Guidance, ConFIG）**
  - 用于推理阶段，通过正交化融合空间与光谱梯度：
    \[
    g_{update} = (g_{spa}^T g_v + g_{spe}^T g_v) g_v
    \]
    避免两个目标（空间、光谱）梯度冲突，确保一致的生成路径。

### 3. 核心创新点
- 结合无监督先验与生成流匹配；
- 动态伪标签优化机制；
- 基于PDE思想的无冲突梯度引导；
- 无需真实高分辨率标签即可在真实数据上训练。

---

## 三、实验设计

### 1. 数据集

- **CAVE**：31波段，取16个用于实验，分20张训练、12张测试。  
- **Chikusei**：128波段航拍影像，构建为16波段版本。  
- **Real-world Dataset**：真实室外场景，16波段，1020×1104（mosaic）与2040×2208（PAN）。

### 2. 对比方法

共比较 **10 种代表性方法**：
- 传统两阶段方法组合（PPID/LSAN/PMNet × PanGAN/VBPN/WFANet)
- 单阶段方法：**EFN（Equivariant Fusion Network）**。

### 3. 评估指标
- 有参考指标：PSNR、SSIM、SAM、ERGAS；
- 无参考指标：QNR、Dλ、DS；
- 采用高斯模糊预处理以减小噪声影响。

---

## 四、资源与算力

- **硬件**：NVIDIA RTX 4090 GPU。  
- **训练细节**：
  - Batch size = 16；
  - 学习率：10⁻⁴（CFM）、10⁻⁵（PAN退化网络）；
  - 采样步数：10；
- **算力使用量**：论文未具体给出训练时长或多GPU配置。  
  → **说明**：仅明确单卡 GPU 级别，其他算力细节未披露。

---

## 五、实验数量与充分性

- **主实验**：在3个数据集上进行（两模拟+一真实），每个数据集包括约10种基线对比。  
- **消融实验**：
  - RVM 机制消融；
  - ConFIG 引导强度分析；
- **结果展示**：
  - 定量表格（Table 1）；
  - 多样定性图（含误差图与残差演化图）。  
- **充分性评价**：覆盖了模拟与真实数据、多个基线以及核心模块消融，实验量充足且对比全面，结果客观一致。

---

## 六、主要结论与发现

- 所提框架在所有数据集的定量指标（PSNR、SAM）上领先；
- 特别在真实性能评估中，QNR 达 0.8709（最高），空间失真 DS 最低；
- 随机投票机制显著提升伪监督质量；
- 无冲突梯度引导在推理中提高光谱-空间一致性；
- 框架具备可扩展性，可推广至其他图像融合或盲复原任务。

---

## 七、优点与创新亮点

- **不依赖真实标签**：突破监督扩散模型需合成训练对的困境；
- **动态目标优化**：随机投票提高伪标签置信度；
- **平衡光谱与结构信息**：无冲突梯度指导实现空间–光谱双优化；
- **高效实现**：采用流匹配而非完整扩散过程，仅需较少采样步；
- **可扩展性强**：可轻松迁移至其他融合与低级视觉任务。

---

## 八、不足与局限

- 未报告**消耗资源**明细，如训练时间、GPU 数量， reproducibility 不完全；
- 尽管涵盖真实数据，但**场景种类有限**（主要为远场户外成像）；
- 方法仍依赖一定的退化模型假设（如 \(A_m\)、\(A_P\)），可能对特定传感器失配；
- ConFIG 指导强度需人工调节，缺乏自适应机制；
- 评估未涵盖极端噪声或动态视频场景，泛化性仍需验证。

---

**（完）**
