---
title: "BARD: Bridging AutoRegressive and Diffusion Vision-Language Models Via Highly Efficient Progressive Block Merging and Stage-Wise Distillation"
title_zh: BARD：通过高效的渐进式块合并与分阶段蒸馏连接自回归与扩散视觉语言模型
authors: "Baoyou Chen, Hanchen Xia, Peng Tu, Haojun Shi, Shan Mu, Weihao Yuan, Siyu Zhu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.16514v3"
tags: ["query:diffusion"]
score: 9.0
evidence: 桥接自回归与扩散视觉语言模型
tldr: 本文提出BARD框架，用渐进块合并与分阶段扩散蒸馏将自回归视觉语言模型转为同架构的高效扩散模型，既保持多模态能力，又实现高达3倍的推理速度提升，在公开评测中达到同规模模型的最新性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16514v3/fig-001.webp\", \"caption\": \"Table 2: Ablation study on block sizes and initialization strategies.\", \"page\": 7, \"index\": 1, \"width\": 986, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2604.16514v3/fig-002.webp\", \"caption\": \"Table 4: Ablation of cross-stage dVLM distillation on BARD-VL4B at target block sizes 𝐵 ∈ {8, 16, 32}. The diffusion teacher is the fixed small-block anchor checkpoint 𝜃 (1) .\", \"page\": 8, \"index\": 2, \"width\": 1026, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2604.16514v3/fig-003.webp\", \"caption\": \"Figure 2: The autoregressive VLM produces next-token logits under clean causal prefix states, while the dVLM predicts same-position logits under corrupted diffusion states. This mismatch makes direct AR-to-dVLM KL supervision poorly aligned.\", \"page\": 4, \"index\": 3, \"width\": 456, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2604.16514v3/fig-004.webp\", \"caption\": \"Figure 3: Overview of BARD-VL. (a) Pipeline. We use the packed training layout [𝑄, 𝑥1, 𝑥𝑡 ], where the multimodal context 𝑄 , the clean response 𝑥1, and the corrupted response 𝑥𝑡 are processed in a single sequence. The two objective boxes correspond to the standard masked diffusion loss Lmask and the mixed-noise loss Lmix. (b) Attention mask. Compared with the vanilla block-wise mask, our packed layout removes the redundant duplication of 𝑄 . The 𝑖-th noisy block 𝑥𝑖𝑡 attends only to the shared context 𝑄 , the causal prefix {𝑥01 , . . . , 𝑥𝑖−11 }, and tokens inside the same noisy block. (c) Mixed-noise scheduler. The coefficients 𝜅1𝑡 , 𝜅 2 𝑡 , 𝜅 3 𝑡 control the interpolation among clean tokens, uniformly corrupted visible tokens, and masked tokens.\", \"page\": 5, \"index\": 4, \"width\": 986, \"height\": 555}]"
motivation: 自回归VLM推理慢，扩散VLM并行高效但直接迁移性能劣化，需寻找桥接方案。
method: 通过逐步监督块合并与分阶段扩散蒸馏相结合的方法，将自回归VLM转换为高效的扩散VLM。
result: BARD-VL在4B和8B规模上取得新的开源dVLM最优性能，推理速度最高提升3倍。
conclusion: BARD成功在保持性能的同时实现显著加速，为视觉语言模型的高效推理提供了新方向。
---

## 摘要
自回归视觉语言模型（VLM）在多模态任务上表现出强大的能力，但其逐 token 解码机制导致了推理的根本性瓶颈。扩散式 VLM 提供了一种更为并行的解码范式，然而，直接将一个预训练的自回归 VLM 转换为大块解码的扩散 VLM（dVLM）通常会造成显著的性能下降。在本研究中，我们提出 BARD，一种简单而有效的桥接框架，可将预训练的自回归 VLM 转换为具有相同架构、解码高效的 dVLM。我们的方法结合了渐进式监督块合并（逐步增大解码块大小）与阶段式 dVLM 内部蒸馏，通过从一个固定的小块扩散锚点模型进行蒸馏，以恢复大块模型中丢失的性能。此外，我们引入了混合噪声调度器，以提高去噪过程中的鲁棒性和 token 修正能力，并采用节省内存的训练策略，以支持长多模态序列的高效训练。一个关键的经验发现是，直接进行自回归到扩散的蒸馏效果较差，甚至会损害性能，而在扩散域内进行蒸馏则始终有效。实验结果表明，在不超过 440 万数据的情况下，BARD-VL 成功将 Qwen3-VL 的强多模态能力迁移至大块 dVLM。值得注意的是，BARD-VL 在我们评测基准上，在 4B 和 8B 规模下均达到了同规模开源 dVLM 的最新 SOTA。同时，BARD-VL 的解码吞吐速度相比源模型提高了最多 3 倍。代码可在此获取：https://github.com/fudan-generative-vision/Bard-VL。

## Abstract
Autoregressive vision-language models (VLMs) deliver strong multimodal capability, but their token-by-token decoding imposes a fundamental inference bottleneck. Diffusion VLMs offer a more parallel decoding paradigm, yet directly converting a pretrained autoregressive VLM into a large-block diffusion VLM (dVLM) often leads to substantial quality degradation. In this work, we present BARD, a simple and effective bridging framework that converts a pretrained autoregressive VLM into a same-architecture, decoding-efficient dVLM. Our approach combines progressive supervised block merging, which gradually enlarges the decoding block size, with stage-wise intra-dVLM distillation from a fixed small-block diffusion anchor to recover performance lost at larger blocks. We further incorporate a mixed noise scheduler to improve robustness and token revision during denoising, and memory-friendly training to enable efficient training on long multimodal sequences. A key empirical finding is that direct autoregressive-to-diffusion distillation is poorly aligned and can even hurt performance, whereas distillation within the diffusion regime is consistently effective. Experimental results show that, with $\leq$ 4.4M data, BARD-VL transfers strong multimodal capability from Qwen3-VL to a large-block dVLM. Remarkably, BARD-VL establishes a new SOTA among comparable-scale open dVLMs on our evaluation suite at both 4B and 8B scales. At the same time, BARD-VL achieves up to 3$\times$ decoding throughput speedup compared to the source model. Code is available at: $\href{https://github.com/fudan-generative-vision/Bard-VL}{this~https~URL}$.

---

## 论文详细总结（自动生成）

# BARD：通过渐进块合并与分阶段蒸馏连接自回归与扩散视觉语言模型 — 详细总结

---

## 一、研究背景与问题动机

### 背景
- **自回归视觉语言模型（VLMs）**：目前主流的多模态基础模型，具有很强的视觉理解与语言生成能力（如 Qwen3-VL、InternVL）。  
  - **问题**：逐 token 的因果解码模式导致推理速度慢、难以并行，成为部署瓶颈。
- **扩散视觉语言模型（dVLMs）**：采用迭代去噪式的并行生成，可同时更新多个 token，推理效率高。  
  - **问题**：直接将自回归模型转换为扩散形式通常导致性能严重下降，尤其在大块并行解码场景下。

### 核心问题
如何在保持多模态能力的同时，把一个强大的自回归 VLM 转换为高效率的扩散式 VLM，实现性能与解码效率的兼得？

---

## 二、方法论：BARD 框架

### 总体思路
BARD（**Bridging AutoRegressive and Diffusion VLMs**）提出了一种**双阶段桥接机制**，实现从自回归到扩散模型的稳定迁移：

1. **渐进式监督块合并（Progressive Supervised Block Merging）**  
   - 从小块（4-token）扩散 anchor 开始训练，再逐步合并成大块（8、16、32）。  
   - 每次仅将相邻两块合并，从而减少训练不稳定性。  
   - 每个阶段使用统一的混合噪声去噪目标，保持训练过程一致性。  

2. **分阶段 dVLM 蒸馏（Stage-wise dVLM Distillation）**  
   - 每个更大块的学生模型从固定的小块“扩散锚点”中**蒸馏学习**。  
   - 解决了传统“自回归→扩散”蒸馏的分布不匹配问题（前者预测下一个 token，后者复原被破坏的位置）。  
   - 使用温度调整的 KL 散度损失，使学生在大块扩散条件下保持锚点性能。

### 关键技术细节

- **混合噪声调度器（Mixed-Noise Scheduler）**  
  - 将三类噪声进行组合：  
    1. 完全掩码 ([MASK])  
    2. 可见错误 token（uniform corruption）  
    3. 保留原始 token  
  - 提升模型的 **token 修正能力** 与推理时的**鲁棒性**。  

- **内存友好的打包式训练（Packed Training Layout）**  
  - 将上下文 `Q`、干净答案 `x₁` 与被污染答案 `xₜ` 拼接为单一序列 `[Q, x₁, xₜ]`。  
  - 减少视觉上下文复制、节约显存、扩大训练上下文窗口。  

- **算法流程（Algorithm 1 概要）**  
  1. 从预训练自回归 VLM 检查点初始化。  
  2. 训练小块扩散 anchor（block size = 4）。  
  3. 逐阶段扩大块大小（8→16→32），每阶段包括：
     - **训练阶段**：用混合噪声目标进行教师强制训练。
     - **蒸馏阶段**：以固定小块 anchor 作为教师进行 KL 蒸馏。
  4. 输出最终大块 dVLM 模型。  

---

## 三、实验设计

### 数据与场景
- **训练数据**：
  - LLaVA-OneVision-1.5：多模态指令跟随数据；
  - FineVision：高质量的视觉推理与理解样本；
  - 去重与质量过滤后共计 ≤ 4.4M 样本；
  - 额外 10 万蒸馏样例，用于阶段蒸馏学习。
- **任务类别**（全部基于 VLMEvalKit/OpenCompass 评测）：
  - **多模态推理**：MMMU-val、MMMU-Pro、MME；
  - **视觉问答**：RealWorldQA、MMStar；
  - **文档理解**：AI2D、ChartQA。

### 对比方法
- **自回归模型**：Qwen3-VL、InternVL3.5；
- **扩散模型**：LLaDA-V、Dream-VL、LaviDa、SDAR-VL、Dimple-VL 等；
- 对比规模：2B、4B、8B 级别的参数量。

---

## 四、计算资源

- 训练硬件：**8 × NVIDIA H200 GPU**；
- **全局批量大小**：256；
- **优化器**：AdamW（β=(0.9, 0.95)）；
- **学习率**：
  - 语言部分峰值 2×10⁻⁵；
  - 视觉部分峰值 2×10⁻⁶；
- **调度**：线性预热 + 余弦衰减；
- **未明确**提到总训练时长（steps 或 wall-time），但完整训练覆盖全部阶段。

---

## 五、实验充分性与公平性

- **主实验**：  
  - 七个基准任务 × 三种模型规模 × 两种解码块大小；
  - 对比多种自回归与扩散模型。
- **消融实验**（>5组）：
  1. 数据规模对性能的影响（110K～4.4M）；
  2. 块大小与初始化策略（渐进 vs 直接训练）；
  3. 不同教师信号（无蒸馏 / 自回归教师 / 扩散锚点教师）；
  4. 噪声类型（纯掩码 / 纯随机 / 混合）。
- **评估方法**：统一使用 VLMEvalKit 工具，与主流论文指标保持一致。
- **结论**：实验设置广泛，覆盖充分，且在模型规模、数据量、比较基准上保持公平。

---

## 六、主要发现与结论

- **性能**：  
  - BARD-VL（4B）在七个基准中五个超越 Qwen3-VL（4B）；  
  - BARD-VL（8B）在七个基准中六个优于 Qwen3-VL（8B）；  
  - 相比其他开源 dVLM，BARD-VL 在全部基准上表现最优，达成新的 SOTA。
- **效率**：  
  - 相比源模型，推理吞吐速度最高提升 **3×**；
  - 仅需 4.4M 数据即可完成桥接。
- **分析性结论**：
  - 自回归→扩散直接蒸馏会导致 logit 空间错配；
  - “扩散内蒸馏”可有效恢复大块模型性能；
  - 混合噪声训练有效提升修正能力与迭代稳定性。

---

## 七、优点与创新亮点

- **方法设计新颖**：首个提出“渐进块合并 + 扩散内蒸馏”的桥接机制。
- **兼顾性能与速度**：大幅提升并行性而不牺牲原多模态理解质量。
- **训练简单可复用**：沿用原自回归结构，无需修改模型架构。
- **实证充分**：跨七个多模态基准、多个规模的系统验证。
- **实用性强**：显存优化策略（packed layout）便于现实部署。

---

## 八、不足与局限

- **模型覆盖面有限**：仅验证在 Qwen3-VL 上的迁移效果，尚未测试其他架构（如 LLaVA、InternVL）。
- **领域泛化未探讨**：尚未评估非视觉领域（如语音、多模态动作）。
- **计算代价**：多阶段训练 + 蒸馏过程增加总训练复杂度。
- **评测场景有限**：主要以问答、文档理解为主，缺乏生成型任务（如长文生成、视频理解）的实测结果。
- **延展性问题**：尚未说明超大规模（>10B 参数）模型的适用性与稳定性。

---

**（完）**
