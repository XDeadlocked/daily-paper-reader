---
title: "BARD: Bridging AutoRegressive and Diffusion Vision-Language Models Via Highly Efficient Progressive Block Merging and Stage-Wise Distillation"
title_zh: BARD：通过高效渐进式块合并与分阶段蒸馏连接自回归与扩散视觉语言模型
authors: "Baoyou Chen, Hanchen Xia, Peng Tu, Haojun Shi, Shan Mu, Weihao Yuan, Siyu Zhu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.16514v2"
tags: ["query:diffusion"]
score: 9.0
evidence: 桥接自回归与扩散视觉语言模型
tldr: 本文提出BARD框架，通过渐进式块合并与分阶段蒸馏策略，将自回归视觉语言模型(AR VLM)高效转换为扩散式视觉语言模型(dVLM)，在维持性能的同时显著提升推理速度，成功实现多模态能力迁移并在公开评测中取得领先表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16514v2/fig-001.webp\", \"caption\": \"Table 2: Ablation study on block sizes and initialization strategies.\", \"page\": 7, \"index\": 1, \"width\": 986, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2604.16514v2/fig-002.webp\", \"caption\": \"Table 4: Ablation of cross-stage dVLM distillation on BARD-VL4B at target block sizes 𝐵 ∈ {8, 16, 32}. The diffusion teacher is the fixed small-block anchor checkpoint 𝜃 (1) .\", \"page\": 8, \"index\": 2, \"width\": 1026, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2604.16514v2/fig-003.webp\", \"caption\": \"Figure 2: The autoregressive VLM produces next-token logits under clean causal prefix states, while the dVLM predicts same-position logits under corrupted diffusion states. This mismatch makes direct AR-to-dVLM KL supervision poorly aligned.\", \"page\": 4, \"index\": 3, \"width\": 456, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2604.16514v2/fig-004.webp\", \"caption\": \"Figure 3: Overview of BARD-VL. (a) Pipeline. We use the packed training layout [𝑄, 𝑥1, 𝑥𝑡 ], where the multimodal context 𝑄 , the clean response 𝑥1, and the corrupted response 𝑥𝑡 are processed in a single sequence. The two objective boxes correspond to the standard masked diffusion loss Lmask and the mixed-noise loss Lmix. (b) Attention mask. Compared with the vanilla block-wise mask, our packed layout removes the redundant duplication of 𝑄 . The 𝑖-th noisy block 𝑥𝑖𝑡 attends only to the shared context 𝑄 , the causal prefix {𝑥01 , . . . , 𝑥𝑖−11 }, and tokens inside the same noisy block. (c) Mixed-noise scheduler. The coefficients 𝜅1𝑡 , 𝜅 2 𝑡 , 𝜅 3 𝑡 control the interpolation among clean tokens, uniformly corrupted visible tokens, and masked tokens.\", \"page\": 5, \"index\": 4, \"width\": 986, \"height\": 555}]"
motivation: 自回归VLM推理缓慢且直接转为扩散模型会导致质量下降，亟需高效且可靠的迁移方案。
method: 提出渐进式监督块合并与分阶段扩散内蒸馏，并结合混合噪声调度与节省内存的训练机制。
result: BARD-VL在使用不超过4.4M数据的条件下实现3倍解码提速，并在4B和8B规模上取得最高评测成绩。
conclusion: BARD成功实现AR VLM到dVLM的高效转换，在保持性能的同时显著加速推理，建立同规模开源模型新SOTA。
---

## 摘要
自回归视觉语言模型（VLMs）具有强大的多模态能力，但其逐 token 解码方式在推理过程中存在固有瓶颈。扩散视觉语言模型（Diffusion VLMs）提供了更具并行性的解码范式，然而将预训练的自回归 VLM 直接转换为大块扩散视觉语言模型（dVLM）常常导致质量显著下降。本文提出了 BARD，一个简单而有效的桥接框架，可将预训练的自回归 VLM 转换为具有相同架构、解码高效的 dVLM。我们的方法结合了渐进式的监督块合并（progressive supervised block merging），通过逐步扩大解码块大小实现转换，并引入分阶段的内部 dVLM 蒸馏（stage-wise intra-dVLM distillation），从固定的小块扩散锚点恢复大块模型性能。此外，我们采用混合噪声调度器以增强去噪过程中的鲁棒性和 token 修正能力，并设计了内存友好的训练策略，以支持长多模态序列的高效训练。一个关键的实证发现是，直接的自回归到扩散的蒸馏对齐效果较差，甚至可能降低性能，而在扩散范式内的蒸馏则始终有效。实验结果表明，在不超过 4.4M 数据的情况下，BARD-VL 能够将 Qwen3-VL 的强多模态能力迁移到大块 dVLM 上。值得注意的是，BARD-VL 在我们评测套件中于 4B 和 8B 规模均在同等规模的开放 dVLM 中达到了新的 SOTA。同时，BARD-VL 的解码吞吐量相比源模型提升最高可达 3 倍。

## Abstract
Autoregressive vision-language models (VLMs) deliver strong multimodal capability, but their token-by-token decoding imposes a fundamental inference bottleneck. Diffusion VLMs offer a more parallel decoding paradigm, yet directly converting a pretrained autoregressive VLM into a large-block diffusion VLM (dVLM) often leads to substantial quality degradation. In this work, we present BARD, a simple and effective bridging framework that converts a pretrained autoregressive VLM into a same-architecture, decoding-efficient dVLM. Our approach combines progressive supervised block merging, which gradually enlarges the decoding block size, with stage-wise intra-dVLM distillation from a fixed small-block diffusion anchor to recover performance lost at larger blocks. We further incorporate a mixed noise scheduler to improve robustness and token revision during denoising, and memory-friendly training to enable efficient training on long multimodal sequences. A key empirical finding is that direct autoregressive-to-diffusion distillation is poorly aligned and can even hurt performance, whereas distillation within the diffusion regime is consistently effective. Experimental results show that, with $\leq 4.4M$ data, BARD-VL transfers strong multimodal capability from Qwen3-VL to a large-block dVLM. Remarkably, BARD-VL establishes a new SOTA among comparable-scale open dVLMs on our evaluation suite at both 4B and 8B scales. At the same time, BARD-VL achieves up to \textbf{3$\times$} decoding throughput speedup compared to the source model.

---

## 论文详细总结（自动生成）

# BARD：通过渐进式块合并与分阶段蒸馏连接自回归与扩散视觉语言模型 — 中文总结

---

## 一、核心问题与研究背景

- **背景**：  
  自回归视觉语言模型（Autoregressive VLMs, AR-VLMs）在多模态理解和生成任务中表现强劲，但其逐 token 的因果解码方式导致推理过程**高度串行、速度受限**。  
  与之相对，扩散视觉语言模型（Diffusion VLMs, dVLMs）采用并行式去噪生成，可显著提升解码效率，但若直接将预训练的自回归模型转换为扩散模型，会造成**性能大幅下降**。

- **核心问题**：  
  如何在保持原有多模态能力的基础上，将强自回归模型高效地转换为并行高效的扩散模型，使其在速度与质量之间取得平衡。

- **研究目标**：  
  提出一种结构一致、渐进、且训练成本低的桥接框架——**BARD（Bridging AutoRegressive and Diffusion）**，实现高质量的 AR→dVLM 转换。

---

## 二、方法论：核心思想与关键技术

### 1. 总体思路

- 将预训练的自回归 VLM 转化为扩散式 VLM，保持模型结构一致。  
- 通过**渐进式训练策略**与**内部扩散蒸馏机制**，逐步增加并行解码能力，同时恢复性能损失。

### 2. 核心组成

1. **渐进式监督块合并（Progressive Supervised Block Merging）**
   - 解码块大小从小到大逐步扩展：(4→8→16→32 token/block)。
   - 每一阶段通过“块合并”方式平滑过渡，避免直接跳转导致的训练不稳定。
   - 仅调整解码块粒度，不改变去噪目标或监督位置。

2. **分阶段扩散内蒸馏（Stage-wise dVLM Distillation）**
   - 在每阶段训练完成后，使用固定的小块扩散锚点模型（anchor dVLM）作为教师。
   - 蒸馏目标与学生模型一致的扩散状态下进行，避免自回归 logits 与扩散 logits 之间的监督不匹配。
   - 损失函数采用位置对齐的 KL 散度跨阶段优化，保证性能恢复。

3. **混合噪声调度器（Mixed-Noise Scheduler）**
   - 扩展标准 mask 去噪为混合噪声：结合掩码、随机可见 token 损坏、干净 token。
   - 同时训练模型恢复缺失与修正错误 token，提升生成可修复性与鲁棒性。

4. **内存友好的 packed 训练布局**
   - 使用打包形式 `[Q, x_clean, x_noisy]`，共享多模态上下文 Q，一次前向计算即可完成双分支训练。
   - 为每个噪声块设计相应的注意力掩码，避免未来信息泄露，显著降低显存占用。

5. **算法流程（文字简述）**
   - 初始化：从预训练自回归模型 → 小块扩散 anchor。
   - 阶段式训练：逐步增大块大小并进行混合噪声训练。
   - 阶段式蒸馏：从固定 anchor 蒸馏各阶段模型，迭代至目标块大小，输出最终 dVLM。

---

## 三、实验设计

### 1. 数据与场景

- 使用公开的多模态指令数据：
  - **LLaVA-OneVision-1.5**：覆盖广泛的视觉语言任务。
  - **FineVision**：高质量视觉推理与理解标注。
- 经过数据清洗与去重处理后，最终训练样本约 **4.4M 条**。
- 蒸馏用数据集约 **100K 实例**。

### 2. Benchmark 与评估任务

共 7 个评测基准，覆盖多模态推理、VQA、文档理解三个类别：
- **多模态推理**：MMMU-val、MMMU-Pro-standard、MME-sum  
- **VQA任务**：RealWorldQA、MMStar  
- **文档理解**：AI2D、ChartQA  

评估工具：VLMEvalKit（OpenCompass开放框架），结果与基线工作保持一致。

### 3. 对比方法

- 自回归模型：Qwen3-VL（4B, 8B）、InternVL3.5  
- 扩散模型：LLaDA-V、Dream-VL、LaViDa、SDAR-VL、MMaDA、Dimple-VL  
- BARD-VL 结果对比参数规模 2B、4B、8B。

---

## 四、资源与算力

- **训练硬件**：8 × NVIDIA H200 GPU  
- **批次大小**：global batch size = 256  
- **优化器**：AdamW (β₁=0.9, β₂=0.95)  
- **学习率**：语言模型峰值 2e-5、视觉塔 2e-6  
- 使用线性预热 + 余弦退火调度；梯度裁剪阈值 = 1.0。  
- 论文未明确给出**训练时长**与**总算力成本**，但明确说明训练仅使用 ≤4.4M 数据即可完成模型转换。

---

## 五、实验数量与充分性

- 主实验：完整的七项 benchmark 对比（4B、8B、以及不同B值版本）。  
- 消融实验：
  - **数据规模变化**（110K → 4.4M 数据）；
  - **块大小与warm-start策略**；
  - **跨阶段蒸馏源比较**（无蒸馏 / AR 蒸馏 / dVLM 蒸馏）；
  - **噪声类型比较**（mask、uniform、mixture）。
- 总计至少四组系统性消融实验 + 七项主评价，对比方法全面。
- 对照设置公平（相同评估工具与任务），实验充分体现了方法有效性。

---

## 六、主要结论与发现

1. BARD 能稳定地将自回归模型（Qwen3-VL）转化为扩散模型，在多数任务上匹配或超越源模型性能。  
2. 在相同参数量下（4B、8B），BARD-VL 达到 **开放 dVLM 新 SOTA**，性能领先所有公开扩散基线。  
3. 解码吞吐量提升最高达到 **3 倍**，尤其在长结构化输出任务中显著减少生成步数（例如从 35 步降至 6 步）。  
4. 分阶段 dVLM 蒸馏优于自回归蒸馏，特别在大块尺寸（B=32）时性能恢复显著。  
5. 混合噪声训练有效改善 token 修复与推理稳定性。  

---

## 七、优点与亮点

- **方法创新**：提出渐进式块合并 + 分阶段扩散内蒸馏的系统框架，为AR→Diffusion转换提供稳定路径。  
- **效率显著提升**：通过并行块解码与内存友好训练实现3×推理加速。  
- **数据效率高**：4.4M 数据即可完成迁移，远小于从零训练的扩散模型。  
- **全面验证**：多领域benchmark覆盖推理、问答、文档理解，实验设计科学且公平。  
- **算法兼容性好**：可直接复用自回归结构与权重，无需修改架构。  

---

## 八、不足与局限

- **模型验证局限**：当前仅在 Qwen3-VL 系列上验证，尚未覆盖其他主流 VLM 架构（如 InternVL, LLaVA 等）。  
- **领域适应性未知**：未评估跨模态生成（如视频、三维场景）或下游agent任务。  
- **训练资源未详述**：缺乏训练时长和能耗信息，不便评估真实成本。  
- **极大块尺寸性能仍有回落**：在 B=32 时，尽管通过蒸馏恢复，但仍存在一定精度损失。  
- **尚未探讨推理调度策略**：扩展至动态块或混合扩散解码尚需进一步研究。  

---

**（完）**
