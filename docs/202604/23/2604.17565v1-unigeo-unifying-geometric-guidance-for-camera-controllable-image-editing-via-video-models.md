---
title: "UniGeo: Unifying Geometric Guidance for Camera-Controllable Image Editing via Video Models"
title_zh: UniGeo：通过视频模型统一几何引导的可控相机图像编辑
authors: "Hong Jiang, Wensong Song, Zongxing Yang, Ruijie Quan, Yi Yang"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17565v1"
tags: ["query:diffusion"]
score: 9.0
evidence: 通过视频扩散模型进行相机可控的图像编辑，以保证几何一致性
tldr: 本文提出UniGeo框架，通过在表示、架构和损失函数中统一注入几何引导，解决现有基于图像扩散方法在连续相机运动下的几何漂移问题。该方法利用视频模型的连续视角先验，实现跨视角几何一致的图像编辑，并在多个基准上显著提升视觉质量与结构保真。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17565v1/fig-001.webp\", \"caption\": \"Fig. 2: UniGeo Framework. UniGeo incorporates unified geometric guidance through: (a) Geometry Construction: Lifting input images into 3D point cloud sequences. (b) Frame-Decoupled Geometry Injection: Injecting sequences along the frame dimension. (c) Geometric Anchor Attention: Aligning cross-view features using first-frame tokens as anchors. (d) Trajectory-Endpoint Geometric Supervision: Applying higher loss weights to trajectory endpoints versus intermediate frames.\", \"page\": 6, \"index\": 1, \"width\": 725, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.17565v1/fig-002.webp\", \"caption\": \"Fig. 4: Qualitative comparison under the limited camera motion setting. Our method maintains stable spatial layouts and scene structural consistency across views, while better preserving fine-grained scene details.\", \"page\": 11, \"index\": 2, \"width\": 723, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2604.17565v1/fig-003.webp\", \"caption\": \"Fig. 3: Qualitative comparison under the extensive camera motion setting. Compared with other methods, our approach better preserves the geometric structure of the scene under extensive camera motion, effectively avoiding structural duplication.\", \"page\": 10, \"index\": 3, \"width\": 723, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2604.17565v1/fig-004.webp\", \"caption\": \"Table 2: Quantitative comparison of our model with relevant methods under the limited camera motion setting demonstrates that our model substantially surpasses all relevant baselines across all key metrics. The best and second-best results are demonstrated in bold and underlined, respectively.\", \"page\": 9, \"index\": 4, \"width\": 729, \"height\": 728}]"
motivation: 现有图像编辑方法在几何一致性和连续相机运动中存在漂移与结构退化问题。
method: UniGeo在表示、架构和损失三个层面统一引入几何引导，包括帧解耦几何注入、几何锚点注意力和轨迹端点几何监督。
result: 实验表明，UniGeo在多种公开基准上显著优于现有方法，在视觉质量与几何保真度方面均表现出色。
conclusion: UniGeo有效提升了相机可控图像编辑的视觉质量与几何一致性，为连续视角编辑提供了稳定可靠的解决方案。
---

## 摘要
可控相机图像编辑旨在在不同相机姿态下合成给定场景的新视角，同时严格保持跨视角的几何一致性。然而，现有方法通常依赖于碎片化的几何引导，例如仅在表示层注入点云，尽管模型包含多个层次；并且主要基于在离散视角映射上操作的图像扩散模型。这两种限制共同导致在连续相机运动下的几何漂移和结构退化。我们观察到，尽管利用视频模型可为可控相机图像编辑提供连续视角先验，但若几何引导仍然碎片化，它们仍难以形成稳定的几何理解。为系统性地解决这一问题，我们在共同决定生成输出的三个层面——表示、架构和损失函数——中注入统一的几何引导。为此，我们提出了一个新颖的可控相机编辑框架——UniGeo。具体而言，在表示层，UniGeo 引入帧解耦的几何参考注入机制，以提供稳健的跨视角几何上下文；在架构层，引入几何锚定注意力以对齐多视角特征；在损失函数层，提出轨迹端点几何监督策略，以显式强化目标视角的结构保真度。在多个公开基准上开展的全面实验表明，无论是大范围还是有限相机运动设置下，UniGeo 在视觉质量和几何一致性方面均显著优于现有方法。

## Abstract
Camera-controllable image editing aims to synthesize novel views of a given scene under varying camera poses while strictly preserving cross-view geometric consistency. However, existing methods typically rely on fragmented geometric guidance, such as only injecting point clouds at the representation level despite models containing multiple levels, and are mainly based on image diffusion models that operate on discrete view mappings. These two limitations jointly lead to geometric drift and structural degradation under continuous camera motion.   We observe that while leveraging video models provides continuous viewpoint priors for camera-controllable image editing, they still struggle to form stable geometric understanding if geometric guidance remains fragmented. To systematically address this, we inject unified geometric guidance across three levels that jointly determine the generative output: representation, architecture, and loss function.   To this end, we propose UniGeo, a novel camera-controllable editing framework. Specifically, at the representation level, UniGeo incorporates a frame-decoupled geometric reference injection mechanism to provide robust cross-view geometry context. At the architecture level, it introduces geometric anchor attention to align multi-view features. At the loss function level, it proposes a trajectory-endpoint geometric supervision strategy to explicitly reinforce the structural fidelity of target views.   Comprehensive experiments across multiple public benchmarks, encompassing both extensive and limited camera motion settings, demonstrate that UniGeo significantly outperforms existing methods in both visual quality and geometric consistency.

---

## 论文详细总结（自动生成）

# UniGeo：通过视频模型统一几何引导的可控相机图像编辑

---

## 一、研究问题与背景

**核心问题**  
本文关注“相机可控图像编辑（Camera-controllable image editing）”，即在变化的相机姿态下对场景进行图像合成与编辑，同时保持跨视角几何一致性。  
这一任务对于**影视后期制作、机器人视觉、虚拟场景渲染**等都至关重要，但现有方法存在两大难题：

1. **缺乏连续性**：多数方法基于图像扩散模型，仅在离散视角映射间推理，不能平滑地模拟连续相机运动，导致生成不稳定。  
2. **几何指导碎片化**：常见技术仅在模型的表示层引入几何信息（如深度或点云），忽视架构与优化层，造成3D结构失真或漂移。

作者指出：**视频生成模型天然具备连续时序建模能力**，若能统一几何指导，可能成为解决几何漂移与一致性问题的关键。

---

## 二、方法论与技术设计

论文提出 **UniGeo** —— 一个基于视频扩散模型的统一几何引导框架，其核心思想是**在表示、架构和损失三个层面同时注入几何信息**，实现连续的、结构一致的视角变化。

### 1. 总体思想
通过系统性地整合几何引导，UniGeo 保证生成过程中跨视角的一致性与结构保真：
- **表示层（Representation）**：稳健的几何输入；
- **架构层（Architecture）**：几何对齐的注意力机制；
- **损失层（Loss Function）**：基于轨迹的几何误差加权。

### 2. 核心模块

#### (1) 帧解耦点云注入（Frame-Decoupled Point Cloud Injection）
- 利用预训练 VGGT 模型从输入图像估计相机轨迹与点云。  
- 将点云沿相机运动轨迹渲染成序列图片，作为显式几何指导。  
- 通过沿 **帧维度拼接（frame-dim concat）** 的方式注入视频模型，而非通道拼接，避免像素级不对齐导致噪声干扰。  
- 在推理时，根据用户输入的摄像机运动提示，动态生成对应的虚拟摄像机路径。

#### (2) 几何锚点注意力（Geometric Anchor Attention）
- 使用**第一帧的特征作为锚点（anchor）**，在后续帧之间进行特征对齐。  
- 在传统自注意力中增加一次“锚点注意力”计算：
  \[
  X_{out}^i = Attention(Q_i, K_i, V_i)W_O + \alpha \cdot Attention(Q'_i, K_0, V_0)W'_O
  \]
  其中，\( K_0, V_0 \) 是锚点帧的键值，α控制几何引导强度。
- 仅需两个额外可训练矩阵和一个标量权重，计算开销极低。

#### (3) 轨迹端点几何监督（Trajectory-Endpoint Geometric Supervision）
- 为每个视频序列帧分配权重：
  \[
  w_{loss}(i) = 1 + \gamma \left( 2i/N - 1 \right)^2
  \]
- 对序列首尾帧（关键视角）施加更高的几何误差权重，以强化目标视角的结构正确性。  
- 在训练中重复终点帧以进行多步联合优化，稳定最后生成结果。

---

## 三、实验设计

### 1. 数据集与场景
使用了多种公开视频数据集，覆盖室内、室外和人物场景：
- **DL3DV-10K** — 大规模3D场景数据集  
- **RealEstate10K (RE10K)** — 房地产场景视频  
- **Tanks and Temples (Tanks)** — 大规模室外结构场景  
- **MannequinChallenge** — 人体和人群场景

训练集中约包括：
- 3500样本来自 DL3DV；
- 2500样本来自 MannequinChallenge；
- 9000样本来自 RE10K。

每个样本取 81 帧，训练时稀疏采样 29 帧。

### 2. Benchmark 与评价指标
- 根据“视角变化面积比例”区分 **“大范围相机运动”** 与 **“有限相机运动”** 两种测试场景。  
- 评价指标包括：  
  - **FID**（生成质量）  
  - **SSIM**（结构相似度）  
  - **LPIPS**（感知质量）  
  - **PSNR**（重建保真度）

### 3. 对比方法
与多种现有代表性方法进行对比：
- **CameraCtrl**、**MotionCtrl**、**ViewCrafter**、**FlexWorld**、**PE-Field** 等。

在所有基准上，UniGeo 均取得最优/次优的综合指标。

---

## 四、资源与算力说明

- **基础模型**：Wan2.2-TI2V-5B 视频生成模型；
- **微调方式**：LoRA，秩为 256；
- **训练配置**：
  - 分辨率：704×1248；
  - 视频长度：29 帧；
  - 训练迭代约 10,000 次；
  - **使用 4 个 GPU**（未指明型号）；
  - 学习率 \(1 \times 10^{-4}\)，批量大小 4。

文中未进一步说明 GPU 类型或运行时长，仅指出训练时间约为万次迭代。

---

## 五、实验数量与充分性

- **定量实验**：在四个数据集上进行了针对不同相机运动范围的全面测试，每类场景随机取 50 个样本。  
- **定性实验**：包含多组视觉对比图（Fig. 3–6），展示跨视角结构一致性改进效果。  
- **消融实验**：针对三个关键模块（FDPCI、GAA、TEGS）独立对比验证。  
- **超参数分析**：分别测试 α 和 γ 的不同取值对性能的影响。  

综合来看，实验覆盖充分且设计公平，验证了核心设计的贡献。

---

## 六、主要结论与发现

- **统一几何引导显著改善跨视角一致性**。  
  在 RE10K、DL3DV、Tanks 基准中，UniGeo 的 LPIPS 从 0.30 降至约 0.24，PSNR 提升约 1–1.5 dB。  
- **视频模型的连续视角能力得到有效利用**，生成结果在连续相机轨迹上结构稳定、无几何漂移。  
- **方法泛化性强**，在人物场景（MannequinChallenge）中亦能保持身份一致的视角生成。

---

## 七、优点与创新点

- **多层次几何统一注入**：首次系统性同时在表示、结构和损失层嵌入几何指导。  
- **轻量化设计**：新增计算量极小，仅需少数参数增量。  
- **视频模型的自然连续性利用**：相较图像扩散，能处理真实相机运动轨迹。  
- **明确的可控性**：支持通过“相机运动指令”进行目标视角生成。  
- **实验充分，性能优越**：在多个公开数据集和不同运动范围下均取得 SOTA。

---

## 八、不足与局限

- **复杂场景与极端视角变化**：在高度复杂或视角变化过大的情形下，点云几何参考易失真，导致生成结构不准确。  
- **推理效率偏低**：尽管通过稀疏采样优化，但仍需生成若干中间帧，比单帧扩散模型推理更慢。  
- **依赖预训练几何模型（VGGT）**：若该模型估计的相机轨迹或点云不准确，会间接影响最终结果。  
- **未报告更详细的算力与能耗指标**，评估 reproducibility 仍需补充。

---

**（完）**
