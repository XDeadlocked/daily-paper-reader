---
title: Depth-Regularized JEPA World Models Learn More Transferable Representations from Real Outdoor Robot Data
title_zh: 深度正则化的JEPA世界模型从真实户外机器人数据中学习更可迁移的表征
authors: Usman M. Khan
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.16314v1"
tags: ["query:sr"]
score: 9.0
evidence: 基于深度正则化的JEPA世界模型用于机器人数据
tldr: "从真实户外机器人视频学习世界模型面临视觉复杂性和域偏移挑战。本文在JEPA训练中引入深度几何先验，结合各向同性潜正则化SIGReg，并增加训练时过参数化。在农业机器人数据上，冷冻表征的视觉里程计探针误差降低33%，域内外惊奇分数分离度显著提升，多步 rollout 保真度随地平线延长而改善。轻量级训练时先验增强紧凑世界模型的泛化性和迁移性，且不增加推理开销。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16314v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1622, \"height\": 1150, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16314v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1707, \"height\": 679, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16314v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1709, \"height\": 1449, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16314v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1716, \"height\": 717, \"label\": \"Table\"}]"
motivation: 真实户外机器人数据视觉复杂，现有JEPA世界模型泛化性不足，需几何先验提升潜表征鲁棒性。
method: 在JEPA训练中引入深度监督与各向同性潜正则化SIGReg，并训练时过参数化，以高熵表征符合场景几何。
result: "视觉里程计探针误差降33%，惊奇分数分离度域内外提升，多步 rollout 保真度随域偏移增强。"
conclusion: 深度作为物理先验证实可提升紧凑JEPA世界模型在多种任务上的泛化性与迁移性，零推理开销。
---

## 摘要
世界模型，尤其是基于JEPA架构的模型，已被证明能够学习各种环境的鲁棒动力学。然而，从视觉复杂的真实世界数据中学习仍然是一个挑战，尤其是在不可预测的户外环境中。我们引入了深度作为训练过程中的几何先验，直接从机器人视频数据中学习更鲁棒的潜在动力学，并处理视觉复杂性。这结合了深度监督与各向同性诱导的潜在正则化器（SIGReg），最大化任务无关的潜在多样性，同时约束多样性的组织方式，而组合目标则针对与场景几何一致的最高熵表征。为了在不增加推理时间的情况下满足这种更高的复杂性，我们还增加了仅训练阶段的过参数化。在真实农业机器人的视频上训练一个18M参数的模型，我们通过冻结表征的视觉里程计探测、基于预测器的意外检测以及多步潜在展开保真度进行评估。与基线LeWM相比，我们的方法将视觉里程计探测误差降低了33%，显著提高了域内和域外TartanGround基准上意外分数分离度，并改进了域偏移下的多步展开保真度，其增益随展开步长增加。值得注意的是，我们在与3D几何不直接相关的物理理解（如光照和阴影）上也看到了意外分数分离度的提升。这些结果表明，轻量级的训练时几何先验使得紧凑的JEPA世界模型在具有强基础表征的真实户外数据上更加有用和可迁移，且不增加推理开销。我们的工作表明，深度作为物理基础的先验可以增强世界模型在各种任务上的泛化能力。

## Abstract
World models, especially based on JEPA architectures, have been shown to learn robust dynamics of various environments. However, learning from visually complex real-world data remains a challenge, especially in unpredictable outdoor environments. We introduce depth as a geometric prior during training in learning more robust latent dynamics directly from robot video data and handling visual complexity. This combines depth supervision with an isotropy-inducing latent regularizer (SIGReg), maximizing task-agnostic latent diversity while constraining how that diversity is organized, with the combined objective targeting the highest-entropy representation consistent with scene geometry. To satisfy this greater complexity without increasing inference time, we also add training-only overparameterization. Training an 18M-parameter model on video from a real agricultural robot, we evaluate with frozen-representation visual odometry probes, predictor-based surprise detection, and multi-step latent rollout fidelity. Compared to the baseline LeWM, our method lowers visual odometry probe error by 33%, substantially increases surprise-score separation both in-domain and on the out-of-domain TartanGround benchmark, and improves multi-step rollout fidelity under domain shift, with gains that grow with rollout horizon. Notably, we also see improvements in surprise-score separation on physics understanding that is not directly tied to 3D geometry, such as lighting and shadows. These results show that a lightweight training-time geometric prior makes a compact JEPA world model more useful and more transferable on real outdoor data with strong underlying representations, without adding inference overhead. Our work suggests that depth as a physically grounded prior can enhance world model generalization on a variety of tasks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：真实户外机器人（如农业机器人）视频数据具有视觉复杂性（光照变化、阴影、运动模糊、植被杂乱等），现有世界模型（特别是JEPA架构）在简单模拟环境中表现良好，但在真实户外数据上学习鲁棒的潜在动力学仍面临挑战，容易出现表征坍塌或过度拟合表观噪声。
- **整体含义**：作者旨在通过引入**深度作为几何先验**来提升JEPA世界模型在真实户外数据上的表征质量和可迁移性，使紧凑模型能够在无深度输入的推理条件下依然保持对场景物理结构的理解，从而更好地支持下游任务（如视觉里程计、异常检测、多步预测），且不增加推理开销。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：在LeWorldModel（LeWM）的训练损失中增加一个**仅训练阶段的深度对齐正则项**，将RGB编码器输出的嵌入与通过相同编码器（停止梯度）从深度相机获得的嵌入进行余弦相似度对齐，从而迫使潜在表征编码几何结构。同时结合LeWM原有的SIGReg正则化器（促使潜在分布趋于各向同性高斯，最大化任务无关多样性），形成“最高熵且符合场景几何”的潜在分布目标。
- **关键技术细节**：
  - **基础模型**：LeWM（ViT-Tiny编码器，嵌入维度192，历史帧数3，6层预测器，投影器结构）。
  - **深度对齐损失**：\(L_{\text{depth}} = 1 - \cos(f_\theta(x_{\text{RGB}}), \text{sg}[f_\theta(x_{\text{depth}})])\)，权重0.1。
  - **总损失**：\(L = L_{\text{pred}} + 0.09 L_{\text{sigreg}} + 0.1 L_{\text{depth}}\)。
  - **训练时过参数化**：在预测器的MLP上添加低秩（rank-16）RepLinear分支，增强模型容量以同时满足预测、各向同性和几何对齐三个目标；训练后通过代数合并回基础权重，推理时模型结构与基线完全相同，无额外计算。
  - **推理阶段**：仅使用RGB输入，丢弃深度分支和目标帧分支，过参数化合并，因此推理模型架构和成本与基线LeWM一致。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - **训练/验证**：从Aigen的Element农业机器人平台采集的真实户外数据（前向导航立体相机），训练集约31K帧（85个episode），验证集3,587帧（9个episode）。深度图截断至1米。
  - **域外（OOD）基准**：TartanGround（地面机器人模拟-真实混合基准），15,608帧（6个episode），覆盖多种环境，存在平台、环境和模拟-真实差异。
- **评估任务**：
  - **视觉里程计（VO）线性探测**：冻结编码器后的嵌入通过线性回归预测自我运动，评估表征质量的指标为MSE。
  - **预测器驱动的“惊讶”检测**：对短片段施加合成扰动（传送、时间反转、亮度跳变、颜色交换），测量预测器误差（惊讶分数）在扰动片段与真实片段之间的分离度（分离 = 平均扰动惊讶 - 平均真实惊讶），同时报告分类准确率。
  - **多步潜在展开保真度**：给定真实动作序列，自动回归展开预测未来潜在嵌入，计算与真实编码嵌入的余弦相似度（目标相似度：最后一步；轨迹相似度：所有步平均），展开步长h=10和h=20。
- **对比方法**：
  - **基线**：普通LeWM（无深度正则化，无过参数化）。
  - **本文方法**：深度正则化的LeWM（深度对齐+过参数化）。
  - **参考**：冻结的DINOv2编码器作为非LeWM的单帧表征参考，仅用于VO探测和展开相似度（DINOv2不参与世界模型预测，仅提供嵌入进行线性探测或相似度计算）。

### 4. 资源与算力

- 论文未明确说明使用的GPU型号、数量或训练时长。仅提及训练100个epoch（与基线相同优化器、学习率调度、批次配置），模型参数为18M。因此，算力信息不完整，需读者自行推断（通常ViT-Tiny在单GPU上训练类似规模的视频数据可能需数小时至数天）。

### 5. 实验数量与充分性

- **实验数量**：
  - 主表（Table 1）包含8个指标（VO MSE、域内/域外惊讶分离度各1个、域内h=10展开2个、域外h=10展开2个、域外h=20展开2个）。此外还有分类准确率（文中称饱和，未在表内但提及）。
  - 图3展示了跨域潜在几何分析（主成分重叠、主角度、2D投影、kNN/线性分类分离度）。
  - 对惊讶检测的不同扰动类型（传送、时间反转、亮度跳变、颜色交换）分别报告了域内/域外的分离度（文中给出一部分具体数值，如亮度跳变从0.114提升至0.247）。
- **充分性评价**：
  - **优点**：覆盖了表征质量（VO探测）、预测器物理理解（惊讶检测）、多步动力学（展开保真度）三个不同维度，且同时进行了域内和域外评估。对比基线为同一架构（仅有无深度正则化），控制变量较好。额外提供了与DINOv2的参考对比，有助于说明紧凑世界模型的价值。
  - **不足**：
    - 缺乏对训练时过参数化的单独消融实验（作者在“讨论”中承认未做容量消融）。
    - 缺乏与更大规模预训练世界模型（如V-JEPA、DINO-WM）的直接比较。
    - 数据集规模较小（仅约31K帧，单一机器人平台、单一农业场景），限制了结论的泛化性。
    - 惊讶检测中准确率饱和，仅靠分离度可能不足以全面说明分类能力；且扰动类型有限（4种）。
    - 展开保真度是离线潜在指标，而非闭环机器人性能。
    - 未进行多轮重复实验（仅提及“重复训练得到一致结果，报告代表性运行”），缺乏统计显著性分析。

### 6. 论文的主要结论与发现

- **VO探测**：深度正则化模型MSE从0.0022降至0.0015（降低33%），优于DINOv2（0.0029），表明表征中可线性解码的运动几何信息更丰富。
- **惊讶分离度**：
  - 域内验证集：分离度从0.067提高到0.120。
  - 域外TartanGround：分离度从0.035提高到0.099（接近3倍），分类准确率从87.5%提升至89.2%。
  - 对非几何扰动（亮度跳变、颜色交换）也有显著提升，说明几何先验强化了表观连续性的物理期望。
- **多步展开保真度**：
  - 域内：深度模型略有下降（h=10目标相似度0.890 vs 0.903），但域外优势明显且随展开步长增大（h=10: 0.745 vs 0.716; h=20: 0.537 vs 0.458），表明OOD鲁棒性和积累误差抑制更好。
- **跨域潜在几何**：深度正则化使得两个域（Aigen真实 vs TartanGround模拟）的嵌入在主成分子空间上有更高的重叠和更小的主角度，且域间分离度略微降低（更少的领域特定方向），支持了更可迁移的表征结构。

### 7. 优点

- **方法简洁实用**：仅在训练时增加一个余弦对齐损失和过参数化，无复杂模块，推理零开销。
- **物理先验的有效性**：证明深度几何信息可以诱导世界模型在真实户外数据上学习更语义化、更可迁移的潜在表示，甚至改善非几何物理理解（如光照）。
- **评估全面**：从表征质量（VO探测）、预测器行为（惊讶检测）、动力学精度（多步展开）三个角度验证，涵盖域内和域外。
- **紧凑模型**：18M参数的低端模型即可获得显著提升，验证了在小模型上注入物理先验的价值，不同于大规模预训练范式。
- **开源与可复现性潜力**：基于已知的LeWM架构，实验设置透明，易于复现和扩展。

### 8. 不足与局限

- **实验规模与多样性不足**：仅使用单一机器人平台、单一农业场景（约31K帧），未在更广泛或更具挑战性的户外场景（如城市、森林）中验证。域外基准TartanGround为模拟数据，模拟-真实差距有限。
- **过参数化未消融**：深度正则化与过参数化同时使用，未单独分析每个组件贡献，无法确定性能提升多大程度来自过参数化本身。
- **离线评估**：展开保真度为潜在空间余弦相似度，不能完全反映闭环机器人规划/控制性能。惊讶检测基于合成扰动，可能无法覆盖真实世界中的异常模式。
- **计算资源未明确**：缺乏GPU和训练时间信息，不利于成本评估。作者仅提及“相同批次配置”和“100 epoch”，但未提供具体硬件。
- **对照方法有限**：仅与普通LeWM和DINOv2比较，未与更大规模JEPA模型（V-JEPA、V-JEPA 2）、重建式世界模型（DreamerV2）或基于基础模型的方法（DINO-WM、SAM）对比。
- **可能偏差**：深度对齐仅使用stop-gradient，未采用教师-学生更新策略；深度图截断至1米可能导致感兴趣区域受限（如远处地形几何被忽略）。另外，SIGReg的权重0.09是原始LeWM中的值，深度权重0.1为启发式选择，未做敏感性分析。
- **规划验证缺失**：论文未将模型用于实际规划，作者承认初步的采样式潜在规划实验因各模型潜在空间动态范围差异而结论不明。

（完）
